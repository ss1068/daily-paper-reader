---
title: "Reasoning-Oriented Programming: Chaining Semantic Gadgets to Jailbreak Large Vision Language Models"
title_zh: 面向推理的编程：通过链接语义组件破解大视觉语言模型
authors: "Quanchen Zou, Moyang Chen, Zonghao Ying, Wenzhuo Xu, Yisong Xiao, Deyue Zhang, Dongdong Yang, Zhao Liu, Xiangzheng Zhang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09246v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过推理链对多模态大模型进行越狱攻击
tldr: 本研究揭示了大视觉语言模型（LVLM）在组合推理中的安全漏洞，提出了一种名为“面向推理编程”（ROP）的新型越狱攻击范式。该方法类比系统安全中的ROP攻击，通过串联语义上良性且正交的视觉组件，诱导模型在推理阶段合成有害逻辑，从而绕过感知层面的安全对齐。实验表明，该方法在GPT-4o和Claude 3.7等主流模型上均表现出极高的攻击成功率，显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的LVLM安全防御主要针对显式恶意模式，忽视了模型可能从多个良性前提中合成有害逻辑的推理漏洞。
method: 借鉴系统安全中的ROP思想，通过优化语义正交性和空间隔离性，构建良性视觉组件并诱导模型在推理后期产生语义碰撞。
result: "在7款主流LVLM上的测试显示，该方法在开源和商业模型上的攻击成功率分别比现有最强基准高出4.67%和9.50%。"
conclusion: 该研究证明了仅靠感知层面的对齐不足以防御复杂的推理型攻击，强调了提升LVLM推理过程安全性的必要性。
---

## 摘要
大视觉语言模型（LVLMs）通常经过安全对齐以抑制有害内容。然而，目前的防御手段主要针对输入表示中的显式恶意模式，往往忽略了组合推理中固有的漏洞。在本文中，我们发现了一个系统性缺陷，即 LVLMs 可能被诱导从良性前提中合成有害逻辑。我们将这种攻击范式形式化为“面向推理的编程”（Reasoning-Oriented Programming），并借鉴了系统安全中“返回导向编程”（Return-Oriented Programming, ROP）的结构类比。正如 ROP 通过链接良性指令序列来绕过内存保护一样，我们的方法利用模型的指令遵循能力，编排正交良性输入的语义碰撞。我们通过 \tool{}（一个针对“语义正交性”和“空间隔离”进行优化的自动化框架）实例化了这一范式。通过生成与有害意图在语义上解耦的视觉组件（visual gadgets），并对其进行排列以防止过早的特征融合，\tool{} 迫使恶意逻辑仅在推理过程的后期阶段出现。这有效地绕过了感知层面的对齐。我们在 SafeBench 和 MM-SafetyBench 上针对 7 种最先进的 LVLMs（包括 GPT-4o 和 Claude 3.7 Sonnet）对 \tool{} 进行了评估。结果表明，\tool{} 能够持续绕过安全对齐，在开源模型上平均优于最强现有基准 4.67%，在商用模型上优于 9.50%。

## Abstract
Large Vision-Language Models (LVLMs) undergo safety alignment to suppress harmful content. However, current defenses predominantly target explicit malicious patterns in the input representation, often overlooking the vulnerabilities inherent in compositional reasoning. In this paper, we identify a systemic flaw where LVLMs can be induced to synthesize harmful logic from benign premises. We formalize this attack paradigm as \textit{Reasoning-Oriented Programming}, drawing a structural analogy to Return-Oriented Programming in systems security. Just as ROP circumvents memory protections by chaining benign instruction sequences, our approach exploits the model's instruction-following capability to orchestrate a semantic collision of orthogonal benign inputs. We instantiate this paradigm via \tool{}, an automated framework that optimizes for \textit{semantic orthogonality} and \textit{spatial isolation}. By generating visual gadgets that are semantically decoupled from the harmful intent and arranging them to prevent premature feature fusion, \tool{} forces the malicious logic to emerge only during the late-stage reasoning process. This effectively bypasses perception-level alignment. We evaluate \tool{} on SafeBench and MM-SafetyBench across 7 state-of-the-art 0.LVLMs, including GPT-4o and Claude 3.7 Sonnet. Our results demonstrate that \tool{} consistently circumvents safety alignment, outperforming the strongest existing baseline by an average of 4.67\% on open-source models and 9.50\% on commercial models.

---

## 论文详细总结（自动生成）

以下是对论文《Reasoning-Oriented Programming: Chaining Semantic Gadgets to Jailbreak Large Vision Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大视觉语言模型（LVLM）虽然通过安全对齐（如 RLHF）来过滤有害内容，但现有的防御和攻击研究大多集中在**感知层面**（即输入中是否存在显式的恶意图像或文字）。
*   **研究动机**：作者发现 LVLM 存在一个系统性漏洞——**组合推理漏洞**。攻击者可以诱导模型从多个看似“良性”的前提中，通过逻辑推理合成出“有害”的结论。
*   **整体含义**：论文借鉴了系统安全中的**返回导向编程（ROP）**概念，提出了**面向推理的编程（Reasoning-Oriented Programming）**。其核心逻辑是：不直接注入恶意代码，而是通过串联内存中已有的良性指令片段（Gadgets）来实现攻击。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：通过构建一组语义上与有害意图正交（无关）的良性视觉组件（Visual Gadgets），并利用优化的控制流提示词（Control-Flow Prompt）引导模型进行推理，使有害逻辑仅在推理的后期阶段（Late-Stage）浮现。
*   **关键技术细节（VROP 框架）**：
    1.  **语义组件挖掘（Semantic Gadget Mining）**：利用辅助 LLM 将有害指令分解为 $N$ 个离散的、良性的物理对象描述。例如，将“制造炸弹”分解为“玻璃瓶”、“化学试剂”等中性图像描述。
    2.  **语义正交性与空间隔离**：使用文生图模型生成图像，并将其排列在带有填充（Padding）的 **2x2 网格**中。这种物理隔离防止了视觉编码器在早期阶段提取到整体的恶意特征。
    3.  **无梯度控制流优化**：由于目标模型是黑盒，作者采用**进化策略**优化提示词。提示词分为“提取算子”（引导模型关注特定网格区域）和“组装算子”（引导模型将提取的良性概念组合成有害结论）。
    4.  **后期推理劫持**：通过注意力机制，在模型的深层网络中强制进行语义整合，绕过浅层的安全过滤机制。

### 3. 实验设计
*   **数据集/场景**：
    *   **SafeBench**：包含 10 个类别、500 个有害查询。
    *   **MM-SafetyBench**：涵盖 13 个高风险场景。
*   **目标模型（7款）**：
    *   **开源模型**：Qwen2-VL-7B, LLaVA-v1.6-7B, Llama-3.2-11B-Vision。
    *   **商用模型**：GPT-4o, Claude 3.7 Sonnet, GLM-4V-Plus, Qwen-VL-Plus。
*   **对比方法**：FigStep (FS), MM-SafetyBench (MM), JOOD, MML。
*   **防御基准**：测试了 CIDER, ECSO, JailGuard, AdaShield 等主流防御手段。

### 4. 资源与算力
*   **算力设备**：开源模型的实验在拥有 **8 台 NVIDIA A100 (80GB) GPU** 的集群上运行。
*   **辅助模型**：使用 GPT-4 作为语义分解和进化优化的控制器；使用 Stable Diffusion 3 Medium 生成视觉组件。
*   **商用模型访问**：通过官方 API 进行测试。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个模型和 2 个主流基准上进行了全面测试，涵盖了非法活动、仇恨言论、隐私侵犯等多个维度。
*   **消融实验**：非常充分，包括：
    *   **模态消融**：证明了仅靠图像或仅靠文字无法达到同等攻击强度。
    *   **辅助模型敏感度**：更换不同的 T2I 模型（SD 1.4, DALL-E 3）和辅助 LLM（Grok 3, Gemini 2.5），证明了方法的普适性。
    *   **组件数量与布局**：测试了 1-6 个组件及不同排列方式（网格 vs 条状）的影响。
*   **客观性**：引入了 **LLM-as-a-Judge** 评分，并通过人工审计（200个样本）验证，两者一致性极高（Cohen’s $\kappa = 0.92$）。

### 6. 主要结论与发现
*   **攻击效力**：VROP 在所有模型上均显著优于现有基准。在开源模型上 ASR（攻击成功率）提升约 4.67%，在商用模型上提升达 9.50%。
*   **防御失效**：现有的防御机制（如图像转文字、扰动检测）对 VROP 的效果有限，因为 VROP 的输入在感知上是完全良性的。
*   **推理越强越危险**：实验发现，推理能力越强的模型（如 GPT-4o, Claude 3.7），在面对这种组合推理攻击时反而表现出更高的脆弱性。

### 7. 优点
*   **理论新颖**：将系统安全中的 ROP 概念成功迁移到多模态模型安全领域，提供了一个结构化的攻击视角。
*   **隐蔽性强**：攻击组件在语义上与意图解耦，极难被基于关键词或图像分类的传统过滤器拦截。
*   **自动化程度高**：通过进化算法自动优化提示词，无需人工精调。

### 8. 不足与局限
*   **模态局限**：目前仅限于图像-文本组合，尚未探讨在音频或视频模态中的适用性。
*   **分解策略依赖**：语义分解依赖于预定义的策略，可能无法覆盖所有复杂的现实世界有害推理模式。
*   **伦理风险**：虽然作者进行了脱敏处理，但这种攻击范式如果被恶意利用，可能对当前 LVLM 的安全部署构成严重威胁。

（完）
