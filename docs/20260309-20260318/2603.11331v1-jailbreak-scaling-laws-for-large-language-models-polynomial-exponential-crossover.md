---
title: "Jailbreak Scaling Laws for Large Language Models: Polynomial-Exponential Crossover"
title_zh: 大语言模型的越狱缩放法则：多项式-指数交叉
authors: "Indranil Halder, Annesya Banerjee, Cengiz Pehlevan"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11331v1"
tags: ["query:llm"]
score: 9.0
evidence: 越狱缩放定律与提示词注入的理论分析
tldr: 本研究探讨了大语言模型在对抗性提示注入下的越狱缩放定律。研究发现，随着推理采样次数增加，攻击成功率会从无注入时的缓慢多项式增长转变为注入后的指数级增长。作者提出了一个基于自旋玻璃系统的理论框架，将语言生成建模为吉布斯测度下的采样，并将提示注入类比为磁场。理论分析和实验均证实，长提示注入会诱导系统进入有序相，从而实现攻击成功率的指数级飞跃，揭示了越狱提示增强模型对抗性秩序的本质。
selection_source: fresh_fetch
motivation: 旨在揭示提示注入攻击如何将大语言模型的越狱成功率从多项式增长提升至指数级增长的物理机制。
method: 采用统计物理中的自旋玻璃模型和副本对称破缺理论，将提示注入建模为作用于语言生成过程的外部磁场。
result: 理论推导并实验验证了攻击成功率随采样数在短提示下呈幂律缩放，而在长提示下呈指数缩放的交叉现象。
conclusion: 越狱提示通过在模型中诱导产生“对抗性有序相”，从根本上改变了安全对齐失效的缩放规律。
---

## 摘要
对抗性攻击可以可靠地引导经过安全对齐的大语言模型产生不安全行为。实验发现，对抗性提示注入攻击可以将攻击成功率从无注入时的缓慢多项式增长，放大到随推理样本数量增加而呈现的指数级增长。为了解释这一现象，我们提出了一个代理语言的理论生成模型，将其描述为运行在复制对称性破缺（replica-symmetry-breaking）机制下的自旋玻璃系统，其中生成内容是从相关的吉布斯测度（Gibbs measure）中抽取的，且一部分低能量、具有尺寸偏差的簇被指定为不安全的。在此框架下，我们分析了基于提示注入的越狱行为。短注入提示对应于指向不安全簇中心的弱磁场，并导致攻击成功率随推理样本数量呈幂律缩放；而长注入提示（即强磁场）则导致指数级缩放。我们通过解析推导了这些行为，并在大语言模型上进行了实验验证。这两种机制之间的转换归因于强磁场下自旋链中出现了有序相，这表明注入的越狱提示增强了语言模型中的对抗性秩序。

## Abstract
Adversarial attacks can reliably steer safety-aligned large language models toward unsafe behavior. Empirically, we find that adversarial prompt-injection attacks can amplify attack success rate from the slow polynomial growth observed without injection to exponential growth with the number of inference-time samples. To explain this phenomenon, we propose a theoretical generative model of proxy language in terms of a spin-glass system operating in a replica-symmetry-breaking regime, where generations are drawn from the associated Gibbs measure and a subset of low-energy, size-biased clusters is designated unsafe. Within this framework, we analyze prompt injection-based jailbreaking. Short injected prompts correspond to a weak magnetic field aligned towards unsafe cluster centers and yield a power-law scaling of attack success rate with the number of inference-time samples, while long injected prompts, i.e., strong magnetic field, yield exponential scaling. We derive these behaviors analytically and confirm them empirically on large language models. This transition between two regimes is due to the appearance of an ordered phase in the spin chain under a strong magnetic field, which suggests that the injected jailbreak prompt enhances adversarial order in the language model.

---

## 论文详细总结（自动生成）

这是一份关于论文《Jailbreak Scaling Laws for Large Language Models: Polynomial-Exponential Crossover》的结构化深度总结：

### 1. 核心问题与整体含义
该论文探讨了**大语言模型（LLM）越狱攻击成功率（ASR）随推理采样次数（$k$）增加而变化的缩放规律**。
*   **研究动机**：先前的研究认为，在没有对抗性提示注入的情况下，ASR 随采样次数呈缓慢的多项式增长。然而，作者观察到，当使用对抗性提示（如 GCG 攻击产生的后缀）时，弱模型（如 Vicuna）的 ASR 会迅速转向指数级增长。
*   **核心目标**：通过统计物理学中的**自旋玻璃理论（Spin-glass theory）**，为这种从多项式到指数级的“交叉（Crossover）”现象提供理论解释，并揭示提示注入如何改变模型内部的生成秩序。

### 2. 方法论：SpinLLM 理论框架
作者提出了一个名为 **SpinLLM** 的能量生成模型，将 LLM 的生成过程类比为物理系统：
*   **核心思想**：将 LLM 的每个 token 视为自旋粒子（$\sigma_i$），整个序列的生成过程是在由输入提示确定的能量景观（Energy Landscape）中进行采样。
*   **安全与不安全的定义**：在低温的“副本对称破缺（RSB）”相下，能量景观会分裂成许多层次化的簇（Clusters）。作者将其中一部分低能量簇定义为“不安全”的生成结果。
*   **提示注入 = 磁场**：对抗性提示注入被建模为施加在系统上的**外部磁场（$h$）**，该磁场指向不安全簇的中心。
*   **关键公式**：推导出 ASR（$\Pi_k$）的通用缩放形式：
    $$\log(-\log(1 - \Pi_k)) \sim -\hat{\nu} \log k - \hat{\mu} k + \log \hat{C}$$
    其中 $\hat{\nu}$ 代表模型的推理能力（树深度），$\hat{\mu}$ 代表对抗性秩序的强度（磁场强度）。
    *   **弱场机制**：当注入提示较短时，系统处于 RSB 相，ASR 呈**幂律（多项式）**缩放。
    *   **强场机制**：当注入提示足够长时，系统进入**有序相（Ordered Phase）**，ASR 呈**指数级**缩放。

### 3. 实验设计
*   **数据集**：使用了 **Walledai/AdvBench** 数据集，包含 520 条有害行为提示。
*   **攻击方法**：
    1.  **无注入**（Baseline）。
    2.  **“Sure here is”注入**（简单引导）。
    3.  **GCG（Greedy Coordinate Gradient）**：生成的通用对抗性后缀。
*   **对比模型**：GPT-4.5 Turbo, GPT-3.5 Turbo, Claude-3.5 Haiku, Vicuna-7B v1.5, Llama-3-8B-Instruct, Llama-3.2-3B-Instruct。
*   **评估指标（Benchmark）**：
    *   传统的**拒绝字符串匹配**（容易误判）。
    *   **LLM-as-a-Judge**：使用 GPT-4 或 Mistral-7B 作为裁判，评估回复的真实有害性和连贯性。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或总训练时长。
*   实验涉及 API 调用（OpenAI/Anthropic）以及本地运行开源模型（Llama/Vicuna/Mistral），使用了 HuggingFace 框架。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 GPT-4 级别的多个模型，采样次数 $k$ 从 1 扩展到 125。
*   **消融实验**：通过改变对抗性后缀的长度（0, 14, 20 个 token）来模拟磁场强度的变化，验证了理论中的交叉现象。
*   **充分性评价**：实验设计较为全面，不仅验证了缩放定律，还对比了不同裁判模型的影响，并分析了越狱回复的实际危害程度评分（1-5分制），结果具有较强的客观性和说服力。

### 6 主要结论与发现
*   **缩放定律的转变**：对抗性提示注入不仅提高了 ASR 的基数，更改变了其随采样增加的**增长速率**（从多项式变为指数）。
*   **对抗性有序相**：长提示注入会在模型中诱导出一种“对抗性有序相”，使得模型几乎不可避免地落入不安全生成的陷阱。
*   **模型能力的权衡**：更强的模型（如 GPT-4.5）在面对相同强度的攻击时，仍能保持较长时间的多项式缩放，表现出更强的安全韧性；而弱模型极易发生指数级崩溃。

### 7. 优点
*   **跨学科创新**：首次将自旋玻璃理论中的副本对称破缺（RSB）与 LLM 的安全对齐失效联系起来，提供了深刻的物理直觉。
*   **数学严谨性**：不仅有实验观察，还给出了详细的解析推导，证明了不同磁场强度下的渐近行为。
*   **实用价值**：揭示了“Best-of-$n$”采样在对抗性环境下可能带来的巨大安全风险。

### 8. 不足与局限
*   **理论简化**：SpinLLM 模型假设 token 是二元自旋（$\pm 1$），且采用非自回归采样进行简化分析，这与实际 LLM 的高维嵌入和自回归生成存在差异。
*   **数据集局限**：主要基于 AdvBench，未测试更隐蔽或更复杂的长文本越狱策略。
*   **防御缺失**：论文侧重于揭示攻击规律，未深入探讨如何利用该物理机制设计更有效的防御手段。

（完）
