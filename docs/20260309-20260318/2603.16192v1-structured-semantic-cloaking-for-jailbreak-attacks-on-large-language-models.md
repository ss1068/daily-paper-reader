---
title: Structured Semantic Cloaking for Jailbreak Attacks on Large Language Models
title_zh: 针对大语言模型越狱攻击的结构化语义隐匿
authors: "Xiaobing Sun, Perry Lam, Shaohua Li, Zizhou Wang, Rick Siow Mong Goh, Yong Liu, Liangli Zhen"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16192v1"
tags: ["query:llm"]
score: 10.0
evidence: 新型多维度越狱攻击框架
tldr: 针对现代大语言模型（LLM）能够识别深层语义意图并拒绝恶意请求的问题，本文提出了一种名为“结构化语义掩蔽（S2C）”的多维越狱攻击框架。S2C通过上下文重构、内容碎片化和线索引导伪装，策略性地分散和重塑语义线索，迫使模型在推理过程中延迟整合恶意意图，从而绕过安全防御机制。实验表明，S2C在多个主流模型上显著提升了攻击成功率，超越了现有最先进方法。
selection_source: fresh_fetch
motivation: 现代LLM的安全机制已深入到潜在语义层面，使得传统的表面混淆越狱手段难以奏效。
method: 提出S2C框架，通过上下文重构、内容碎片化和线索引导伪装三种机制，在推理过程中延迟和重构语义整合。
result: "在HarmBench和JBB-Behaviors基准测试中，S2C的攻击成功率分别比现有最先进方法提高了12.4%和9.7%，在GPT-4o-mini上表现尤为突出。"
conclusion: S2C证明了通过操纵语义整合过程可以有效绕过LLM的深层安全防御，为研究模型鲁棒性提供了新视角。
---

## 摘要
现代大语言模型（LLMs）采用的安全机制已不仅限于表层输入过滤，还扩展到了潜在语义表示和生成时的推理，使其能够在推理过程中恢复被混淆的恶意意图并据此拒绝，从而导致许多表层混淆越狱攻击失效。我们提出了结构化语义隐匿（Structured Semantic Cloaking, S2C），这是一种新型的多维越狱攻击框架，旨在操纵模型推理过程中恶意语义意图的重构方式。S2C 策略性地分布和重塑语义线索，使得完整的意图整合需要更深层潜在表示中的多步推理和长距离指代消解。该框架包含三个互补机制：(1) 上下文重构（Contextual Reframing），将请求嵌入到合理的、高风险的场景中，以诱导模型倾向于合规；(2) 内容碎片化（Content Fragmentation），将请求的语义特征分散到不相交的提示段落中；(3) 线索引导伪装（Clue-Guided Camouflage），在伪装残余语义线索的同时嵌入可恢复的标记，以引导输出生成。通过延迟和重构语义整合，S2C 削弱了在解码时依赖连贯或显式重构恶意意图的安全触发器，同时为功能性输出生成保留了足够的指令可恢复性。我们在 HarmBench 和 JBB-Behaviors 上对多个开源和闭源 LLM 进行了评估，结果显示 S2C 的攻击成功率（ASR）分别比当前最先进技术（SOTA）提高了 12.4% 和 9.7%。值得注意的是，S2C 在 GPT-5-mini 上取得了显著提升，在 JBB-Behaviors 上比最强的基准模型高出 26%。我们还分析了哪些组合在针对广泛的模型系列时表现最佳，并刻画了混淆程度与输入可恢复性之间对越狱成功的影响权衡。

## Abstract
Modern LLMs employ safety mechanisms that extend beyond surface-level input filtering to latent semantic representations and generation-time reasoning, enabling them to recover obfuscated malicious intent during inference and refuse accordingly, and rendering many surface-level obfuscation jailbreak attacks ineffective. We propose Structured Semantic Cloaking (S2C), a novel multi-dimensional jailbreak attack framework that manipulates how malicious semantic intent is reconstructed during model inference. S2C strategically distributes and reshapes semantic cues such that full intent consolidation requires multi-step inference and long-range co-reference resolution within deeper latent representations. The framework comprises three complementary mechanisms: (1) Contextual Reframing, which embeds the request within a plausible high-stakes scenario to bias the model toward compliance; (2) Content Fragmentation, which disperses the semantic signature of the request across disjoint prompt segments; and (3) Clue-Guided Camouflage, which disguises residual semantic cues while embedding recoverable markers that guide output generation. By delaying and restructuring semantic consolidation, S2C degrades safety triggers that depend on coherent or explicitly reconstructed malicious intent at decoding time, while preserving sufficient instruction recoverability for functional output generation. We evaluate S2C across multiple open-source and proprietary LLMs using HarmBench and JBB-Behaviors, where it improves Attack Success Rate (ASR) by 12.4% and 9.7%, respectively, over the current SOTA. Notably, S2C achieves substantial gains on GPT-5-mini, outperforming the strongest baseline by 26% on JBB-Behaviors. We also analyse which combinations perform best against broad families of models, and characterise the trade-off between the extent of obfuscation versus input recoverability on jailbreak success.

---

## 论文详细总结（自动生成）

以下是对论文《Structured Semantic Cloaking for Jailbreak Attacks on Large Language Models》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现代大语言模型（LLM）的安全机制已不再局限于表层的输入过滤，而是深入到潜在语义表示和生成时的推理阶段。这使得传统的表层混淆（如简单的 Base64 编码）逐渐失效，因为模型能在推理过程中“恢复”被掩盖的恶意意图。
*   **研究背景**：作者发现，安全机制的激活不仅取决于恶意意图是否可恢复，还取决于意图在生成过程中**何时**以及**如何**被重构。如果能通过结构化手段延迟或分散语义的整合过程，就能绕过模型在解码初期的安全触发器。

### 2. 方法论：结构化语义隐匿（S2C）
S2C 框架通过三个维度的协同操作来操纵模型对恶意语义的重构：
*   **上下文重构（Contextual Reframing）**：利用攻击模型（如 Gemma-9B）将恶意请求嵌入到一个看似合理、高风险或具有说服力的情境中（如学术研究、紧急救援），诱导模型产生合规偏见。
*   **内容碎片化（Content Fragmentation）**：将请求中的关键敏感词（如“爆炸”、“攻击”）替换为占位符（如 [PLACEHO 1]），将完整的语义特征分散到不相交的提示段落中，破坏安全分类器依赖的连贯语义特征。
*   **线索引导伪装（Clue-Guided Camouflage）**：对关键术语进行混淆处理（如字符噪声、反转、Base64、代码化、低资源语言翻译等），并提供恢复这些术语的“线索”和指令。
*   **核心思想**：迫使目标模型进行多步推理和长距离指代消解。通过这种方式，恶意意图的完整重构被推迟到解码后期，从而绕过主要在生成初期起作用的安全防御。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **HarmBench**：包含 200 个标准恶意查询。
    *   **JBB-Behaviors**：包含 100 个不同的滥用行为查询。
*   **目标模型**：涵盖 16 种主流开源和闭源模型，包括 Llama 3.3-70B、DeepSeek-v3/R1、Qwen 2.5-72B、GPT-4o、GPT-5-mini、Claude 3.7-Sonnet、Gemini 2.5 等。
*   **对比方法（Baselines）**：对比了 7 种代表性方法，包括 PAIR、PAP（上下文重写类）、CodeChameleon、Cipher、Word-based Puzzle、HaPLa、ReNeLLM（混淆类）。
*   **评估指标**：攻击成功率（ASR）和恢复成功率（RSR）。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 训练时长或集群规模。
*   **实现细节**：实验主要通过 API（OpenAI, Claude, OpenRouter）访问闭源模型。在本地处理方面，使用了 `spaCy` 进行自然语言处理，并使用 `Gemma-2-9B-it` 和 `GPT-4.1-mini` 作为辅助模型来生成重构上下文和提取关键词。

### 5. 实验数量与充分性
*   **实验规模**：在 16 个模型上进行了详尽的测试，涵盖了指令遵循模型和推理模型。
*   **消融实验**：作者进行了多组消融实验，分别验证了“无上下文重构”、“无级联混淆”、“无高难度线索”以及“显示完整解码步骤”对 ASR 的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了多种 SOTA 方法，还分析了不同混淆策略在不同模型家族中的偏好，并探讨了防御护栏（如 LlamaGuard, OpenAI Moderator）的有效性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **S2C 极其有效**：在 HarmBench 和 JBB-Behaviors 上，S2C 的平均 ASR 分别达到 94.1% 和 89.4%，显著优于所有基准方法。
*   **推理模型依然脆弱**：即使是具备强大推理能力的 GPT-5-mini 和 DeepSeek-R1，在面对 S2C 时也表现出显著漏洞（GPT-5-mini 的 ASR 提升了 26%）。
*   **碎片化是关键**：实验证明，即使语义容易恢复，简单的内容碎片化也能显著降低模型的拒绝率（通过 logprob 差异分析证实）。
*   **安全机制的局限性**：目前的 LLM 安全对齐过度依赖于表层语义特征的检测，而非对推理轨迹的深度监控。

### 7. 优点
*   **多维度攻击视角**：不同于单一的混淆或重写，S2C 从语义整合的时间和空间维度切入，揭示了模型推理与安全对齐之间的深层矛盾。
*   **泛化能力强**：该框架在不同架构（Transformer, MoE）和不同对齐策略的模型上均表现出稳定的高攻击成功率。
*   **深入的机制分析**：通过 logprob 分析和消融实验，深入探讨了为什么碎片化和延迟重构能奏效，具有较高的学术价值。

### 8. 不足与局限
*   **单轮对话限制**：研究仅限于单轮攻击，未探讨在多轮对话中安全机制是否会通过上下文积累逐渐激活。
*   **线索策略固定**：目前使用的 7 种级联线索策略是人工预设的，尚未实现自动化的最优策略搜索。
*   **评判模型偏差**：攻击成功率的判定依赖于单一的 Llama-2-13B 评判模型，可能存在一定的判定偏差（虽然这是目前的行业标准做法）。
*   **防御对策较少**：论文重点在于“破”，对于如何“立”（即如何针对性防御此类攻击）的探讨相对简略。

（完）
