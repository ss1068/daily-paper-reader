---
title: Exposing Long-Tail Safety Failures in Large Language Models through Efficient Diverse Response Sampling
title_zh: 通过高效的多样化响应采样揭示大语言模型中的长尾安全故障
authors: "Suvadeep Hajra, Palash Nandi, Tanmoy Chakraborty"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14355v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过输出空间探索揭示越狱成功率
tldr: 本研究指出安全微调虽能提升大模型鲁棒性，但往往只是抑制而非消除了不安全行为，使其潜藏在输出分布的长尾中。不同于侧重输入提示词优化的传统红队测试，本文提出 PDPS 方法，通过在输出空间进行高效的多样化响应采样来暴露安全漏洞。实验证明，PDPS 能以极低的计算成本实现极高的攻击成功率，并揭示更多样化的不安全输出，为评估模型安全性提供了新视角。
selection_source: fresh_fetch
motivation: 现有的安全微调技术往往只是掩盖了不安全行为，导致在特定提示词下仍存在难以被发现的长尾安全风险。
method: 提出渐进式多样化种群采样（PDPS），结合随机令牌采样与多样性感知选择，从大量候选响应中提取语义多样的子集。
result: "PDPS 仅需 8% 到 29% 的计算成本即可达到与大规模采样相当的攻击成功率，且在有限响应设置下显著优于传统方法。"
conclusion: 通过探索输出空间的多样性可以更高效地揭示大模型的长尾安全失效，为红队测试和安全评估提供了强有力的工具。
---

## 摘要
通过监督微调和人类反馈强化学习进行的安全微调显著提高了大语言模型（LLMs）的鲁棒性。然而，这种方法通常只是抑制而非消除不安全行为，使得罕见但关键的故障隐藏在输出分布的长尾中。虽然大多数红队测试工作强调对抗性提示词搜索（输入空间优化），但我们展示了对于固定的安全关键提示词，通过多样化响应生成（输出空间探索）也可以系统地揭示安全故障，其中增加采样响应的数量和多样性可以将越狱成功率推向接近 1。为了高效地发现此类故障，我们提出了渐进式多样化种群采样（PDPS），它结合了随机词元级采样与多样性感知选择，以探索庞大的候选响应池并保留一个紧凑且语义多样的子集。在多个越狱基准测试和开源 LLM 上，PDPS 实现了与大规模独立同分布（IID）采样相当的攻击成功率，而计算成本仅为后者的 8% 到 29%。在有限响应设置下，其成功率比 IID 采样和多样化束搜索提高了 26% 到 40%。此外，PDPS 生成的响应表现出更高数量和更大多样性的不安全输出，证明了其在揭示更广泛故障方面的有效性。

## Abstract
Safety tuning through supervised fine-tuning and reinforcement learning from human feedback has substantially improved the robustness of large language models (LLMs). However, it often suppresses rather than eliminates unsafe behaviors, leaving rare but critical failures hidden in the long tail of the output distribution. While most red-teaming work emphasizes adversarial prompt search (input-space optimization), we show that safety failures can also be systematically exposed through diverse response generation (output-space exploration) for a fixed safety-critical prompt, where increasing the number and diversity of sampled responses can drive jailbreak success rates close to unity. To efficiently uncover such failures, we propose Progressive Diverse Population Sampling (PDPS), which combines stochastic token-level sampling with diversity-aware selection to explore a large candidate pool of responses and retain a compact, semantically diverse subset. Across multiple jailbreak benchmarks and open-source LLMs, PDPS achieves attack success rates comparable to large-scale IID sampling while using only 8% to 29% of the computational cost. Under limited-response settings, it improves success rates by 26% to 40% over IID sampling and Diverse Beam Search. Furthermore, responses generated by PDPS exhibit both a higher number and greater diversity of unsafe outputs, demonstrating its effectiveness in uncovering a broader range of failures.

---

## 论文详细总结（自动生成）

### 论文总结：通过高效多样化响应采样揭示大语言模型中的长尾安全故障

#### 1. 论文的核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）主要通过监督微调（SFT）和人类反馈强化学习（RLHF）进行安全对齐。然而，这种对齐往往只是“抑制”了不安全行为，而非彻底消除。不安全响应依然潜藏在输出概率分布的“长尾”中。
*   **核心问题**：现有的红队测试（Red-Teaming）大多集中在**输入空间优化**（即寻找能绕过过滤器的对抗性提示词），而忽略了**输出空间探索**。论文探讨了对于一个固定的安全敏感提示词，是否可以通过增加采样响应的数量和多样性，系统地诱导模型产生违规输出。
*   **整体含义**：论文证明了即使是经过严格安全对齐的模型，在多次采样下也会暴露安全漏洞。

#### 2. 论文提出的方法论：PDPS
*   **核心思想**：提出**渐进式多样化种群采样（Progressive Diverse Population Sampling, PDPS）**。该方法不再盲目进行大规模独立同分布（IID）采样，而是通过“扩展-选择”的迭代过程，保留语义上最独特且高质量的响应候选。
*   **关键技术细节**：
    *   **初始化**：从原始提示词开始，复制生成初始种群。
    *   **迭代扩展（Expansion）**：每次只生成一小段词元（Block），使用随机采样（如高温度采样或核采样）来引入多样性。
    *   **多样性感知选择（Selection）**：在每一步扩展后，利用**质量-多样性优化目标**对候选池进行剪枝。
    *   **优化目标**：最大化子集的平均质量（如词元概率的几何平均值）和平均成对语义距离（使用模型最后一层隐藏状态的嵌入表示计算余弦距离）。
    *   **算法流程**：这是一个离散组合优化问题（Max-Avg 分散问题），论文采用了一种具有理论近似保证的贪心算法来高效求解。

#### 3. 实验设计
*   **目标模型**：Llama-2-7B-Chat, Llama-2-13B-Chat, Qwen2.5-7B-Instruct, Qwen3-14B-Instruct。
*   **数据集（Benchmarks）**：HarmBench, JailbreakBench, AdvBench, MaliciousInstruct（每个数据集随机抽取100个样本）。
*   **对比方法**：
    1.  **IID 采样**：标准的独立随机采样。
    2.  **Diverse Beam Search (DBS)**：传统的多样化束搜索。
    3.  **IID 1024**：作为性能上限的暴力采样（生成1024个完整响应）。
*   **评估指标**：攻击成功率（ASR）、响应的多样性指标（Dist-n, Self-BLEU, 嵌入空间距离等）。

#### 4. 资源与算力
*   **硬件设备**：论文在附录中提到使用了 **NVIDIA A100 GPU** 进行实验。
*   **计算效率**：PDPS 显著降低了算力需求。实验显示，PDPS 达到与大规模 IID 采样相当的 ASR，仅需其 **8% 到 29%** 的计算时间。
*   **训练时长**：由于该方法属于推理阶段的采样策略，不涉及模型训练，因此未提及训练时长，但详细对比了不同生成长度下的推理耗时。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流开源模型和 4 个权威安全基准数据集，共进行了 16 组模型-数据集组合实验。
*   **消融与敏感性分析**：论文对采样温度 ($\tau$)、核采样阈值 ($p$) 以及质量-多样性权衡参数 ($\lambda$) 进行了详细的敏感性分析。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还深入分析了不安全响应的语义多样性，证明了 PDPS 发现的是“不同类型”的故障而非重复的错误。实验对比具有客观性，设置了合理的性能上限（IID 1024）。

#### 6. 论文的主要结论与发现
*   **长尾风险真实存在**：增加采样数量和多样性会单调提高越狱成功率，证明安全对齐后的模型在长尾分布中仍是不安全的。
*   **PDPS 的高效性**：在有限响应（如仅生成 16 或 64 个响应）的情况下，PDPS 的 ASR 比 IID 和 DBS 平均高出 **26% - 40%**。
*   **语义分离性**：实验发现不安全响应在语义嵌入空间中通常与安全响应（拒绝回答）形成不同的簇，这验证了通过语义多样性进行采样的合理性。

#### 7. 优点
*   **视角独特**：从输出空间而非输入空间切入红队测试，是对现有对抗攻击手段的重要补充。
*   **计算友好**：通过渐进式剪枝，极大地解决了大规模采样带来的计算开销问题。
*   **理论支撑**：选择算法具有明确的近似比保证（1/2 近似），且利用模型内部表示作为语义度量，比单纯的词元级多样性更有效。

#### 8. 不足与局限
*   **白盒依赖**：PDPS 需要获取模型的内部隐藏状态（Embeddings）来计算语义距离，这限制了其在闭源 API 模型（如 GPT-4）上的直接应用。
*   **GPU 利用率波动**：论文提到在生成短序列时，由于初始种群较大，GPU 利用率可能不够优化，导致实际加速比在某些场景下未达理论极限。
*   **判别器偏差**：安全性评估依赖于外部分类器（如 HarmBench 分类器），如果分类器本身存在偏差，可能会影响 ASR 的准确性。

（完）
