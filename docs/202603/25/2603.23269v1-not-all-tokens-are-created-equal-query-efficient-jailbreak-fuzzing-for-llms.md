---
title: "Not All Tokens Are Created Equal: Query-Efficient Jailbreak Fuzzing for LLMs"
title_zh: 并非所有 Token 都是平等的：针对大语言模型的高查询效率越狱模糊测试
authors: "Wenyu Chen, Xiangtao Meng, Chuanchao Zang, Li Wang, Xinyu Gao, Jianing Wang, Peng Zhan, Zheng Li, Shanqing Guo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23269v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对大语言模型的高查询效率越狱模糊测试
tldr: 本研究针对大语言模型越狱攻击中查询效率低下的问题，提出了TriageFuzz框架。研究发现不同令牌对模型拒绝行为的贡献并不均衡且具有跨模型一致性。TriageFuzz利用代理模型识别提示词中的敏感区域，并结合拒绝引导的进化策略进行高效变异。实验表明，该方法在显著减少查询次数的同时保持了极高的攻击成功率，尤其在极低查询预算下表现优异，为LLM安全性评估提供了高效工具。
selection_source: fresh_fetch
motivation: 现有越狱攻击方法盲目变异所有令牌，忽视了不同令牌对触发模型拒绝行为的贡献差异，导致查询效率低下。
method: 提出一种令牌感知的模糊测试框架，利用代理模型定位敏感令牌，并采用拒绝引导的进化策略优化变异过程。
result: "在多个模型上仅需减少70%以上的查询即可达到90%的攻击成功率，在25次查询的极端限制下ASR提升了20-40%。"
conclusion: 通过识别令牌敏感度并利用跨模型一致性，可以显著提升受限查询场景下LLM越狱攻击的效率和成功率。
---

## 摘要
大语言模型（LLMs）已被广泛部署，但仍容易受到越狱提示词的影响，从而诱发违反策略的输出。尽管先前的研究已经揭示了这些风险，但它们在提示词变异过程中通常将所有 token 视为同等重要，忽略了单个 token 对触发模型拒绝行为的不同贡献。因此，这些攻击在查询受限的场景下引入了大量的冗余搜索，降低了攻击效率，并阻碍了全面的漏洞评估。在这项工作中，我们对拒绝行为进行了 token 级别的分析，并观察到 token 的贡献是高度偏斜的，而非均匀分布。此外，我们发现拒绝倾向具有很强的跨模型一致性，这使得利用代理模型来估计单个 token 对目标模型拒绝行为的贡献成为可能。受这些发现的启发，我们提出了 TriageFuzz，这是一个 token 感知的越狱模糊测试框架，通过一系列定制化设计改进了模糊测试方法。TriageFuzz 利用代理模型来估计单个 token 对拒绝行为的贡献，从而能够识别提示词中的敏感区域。此外，它结合了一种拒绝引导的进化策略，利用轻量级评分器对候选提示词进行自适应加权，以引导进化过程绕过安全约束。在六个开源 LLM 和三个商业 API 上的广泛实验表明，TriageFuzz 在显著降低查询成本的同时，实现了相当的攻击成功率（ASR）。值得注意的是，与基准方法相比，它在查询次数减少 70% 以上的情况下达到了 90% 的 ASR。即使在 25 次查询的极严苛预算下，TriageFuzz 仍优于现有方法，将 ASR 提高了 20-40%。

## Abstract
Large Language Models(LLMs) are widely deployed, yet are vulnerable to jailbreak prompts that elicit policy-violating outputs. Although prior studies have uncovered these risks, they typically treat all tokens as equally important during prompt mutation, overlooking the varying contributions of individual tokens to triggering model refusals. Consequently, these attacks introduce substantial redundant searching under query-constrained scenarios, reducing attack efficiency and hindering comprehensive vulnerability assessment. In this work, we conduct a token-level analysis of refusal behavior and observe that token contributions are highly skewed rather than uniform. Moreover, we find strong cross-model consistency in refusal tendencies, enabling the use of a surrogate model to estimate token-level contributions to the target model's refusals. Motivated by these findings, we propose TriageFuzz, a token-aware jailbreak fuzzing framework that adapts the fuzz testing approach with a series of customized designs. TriageFuzz leverages a surrogate model to estimate the contribution of individual tokens to refusal behaviors, enabling the identification of sensitive regions within the prompt. Furthermore, it incorporates a refusal-guided evolutionary strategy that adaptively weights candidate prompts with a lightweight scorer to steer the evolution toward bypassing safety constraints. Extensive experiments on six open-source LLMs and three commercial APIs demonstrate that TriageFuzz achieves comparable attack success rates (ASR) with significantly reduced query costs. Notably, it attains a 90% ASR with over 70% fewer queries compared to baselines. Even under an extremely restrictive budget of 25 queries, TriageFuzz outperforms existing methods, improving ASR by 20-40%.

---

## 论文详细总结（自动生成）

这是一份关于论文《Not All Tokens Are Created Equal: Query-Efficient Jailbreak Fuzzing for LLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对大语言模型（LLM）的**越狱攻击（Jailbreak Attack）效率低下**的问题展开研究。
*   **研究动机**：现有的黑盒越狱攻击（如 GPTFuzz、PAIR）通常将提示词（Prompt）中的所有 Token 视为同等重要，并进行全局随机变异。这种“盲目”的搜索方式在面对具有严格频率限制（Rate Limits）和高昂成本的真实商业 API 时，会产生大量冗余查询，导致攻击成功率（ASR）在低预算场景下表现极差。
*   **核心发现**：作者通过实证分析发现，模型拒绝行为具有两个关键结构特征：
    1.  **Token 贡献倾斜**：拒绝触发通常由提示词中极少数的“敏感区域”决定，而非整个序列。
    2.  **跨模型一致性**：不同模型对相同提示词的拒绝倾向和内部表征结构高度相似，这意味着可以用开源的“代理模型”来定位黑盒目标模型的敏感区域。

### 2. 方法论：TriageFuzz 框架
TriageFuzz 是一个 Token 感知的越狱模糊测试框架，其核心流程包含三个关键步骤：
*   **Token 重要性估计（Token Importance Estimation）**：
    *   利用一个白盒**代理模型**（如 Llama-3），通过因果干预识别出负责构建拒绝信号的特定注意力头（Refusal-Critical Head）。
    *   提取该注意力头对输入 Token 的注意力权重，作为衡量每个 Token 对拒绝行为贡献的指标。
*   **区域聚焦变异（Region-Focused Mutation）**：
    *   将 Token 级别的评分序列化，利用一个**攻击者模型**（Attacker Model）识别出 1-3 个连续的敏感触发片段（Trigger Spans）。
    *   仅针对这些敏感区域进行语义重写（如混淆、场景注入），而保持其他上下文不变，从而在减少安全信号的同时保留原始任务意图。
*   **拒绝引导的进化（Refusal-Guided Evolution）**：
    *   使用轻量级评分器（在代理模型中间层训练的逻辑回归探针）评估候选提示词的拒绝倾向。
    *   采用进化策略，根据拒绝评分通过 Softmax 函数为候选者分配变异预算，优先让那些“接近绕过边界”的提示词产生更多变体。

### 3. 实验设计
*   **数据集**：使用了 **HarmBench**，涵盖化学/生物危害、非法活动、网络犯罪等 6 个安全类别。
*   **目标模型**：
    *   **开源模型**：Gemma-7B/Gemma2-9B, Llama-3-8B/Llama-3.2-3B, Qwen2.5-7B/3B。
    *   **商业 API**：GPT-3.5-Turbo, GPT-4o, Claude-3.5-Sonnet。
*   **对比基准（Benchmarks）**：
    *   白盒攻击：GCG, AutoDAN。
    *   黑盒攻击：PAIR, TAP, GPTFuzz。
*   **评估指标**：攻击成功率（ASR）、查询效率（ASR 随查询次数变化的曲线下面积 AUC）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX Pro 6000 GPU (96GB VRAM)** 的服务器上运行。
*   **模型配置**：
    *   代理模型：Llama-3.1-8B-Instruct。
    *   攻击者模型：Vicuna-13B。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到拒绝评分器（Probe）是轻量级的，且拒绝头识别是单次离线完成的，暗示计算开销主要集中在与目标模型的推理交互上。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个主流模型上进行了详尽测试，涵盖了从 10 到 100 次查询的不同预算区间。
*   **消融实验**：
    *   对比了“Token 感知变异”与“均匀随机变异”的效果（证明了聚焦变异的必要性）。
    *   测试了不同代理模型（Qwen, Mistral）和不同攻击者模型（Llama, Gemma）的鲁棒性。
*   **防御测试**：在 PPL 过滤器、Llama Guard 和 SmoothLLM 三种独立防御以及混合防御场景下进行了测试。
*   **评价**：实验设计非常充分，通过跨模型、跨预算、跨防御场景的对比，客观地证明了该方法在查询受限环境下的优越性。

### 6. 主要结论与发现
*   **极高的查询效率**：TriageFuzz 在减少 **70% 以上查询次数**的情况下，达到了与基准方法相当的 90% ASR。
*   **低预算表现优异**：在仅 25 次查询的极端限制下，TriageFuzz 在 GPT-4o 上达到了 **84% 的 ASR**，在 GPT-3.5 上达到 94%，远超 PAIR 和 TAP 等方法。
*   **强鲁棒性**：该方法生成的提示词具有较好的语义连贯性，能够有效绕过基于困惑度（PPL）和语义分类（Llama Guard）的防御机制。

### 7. 优点与亮点
*   **粒度精细**：打破了以往将 Prompt 视为整体的局限，深入到 Token 级别分析拒绝机制。
*   **可解释性驱动**：利用机械解释性（Mechanistic Interpretability）的发现（拒绝信号在中间层线性可分且由特定头驱动）来指导攻击。
*   **实用性强**：直接针对真实世界 API 的频率限制痛点，具有极高的红队测试（Red Teaming）参考价值。

### 8. 不足与局限
*   **依赖本地代理模型**：虽然实验证明了跨模型的一致性，但攻击者仍需部署一个本地白盒模型来计算 Token 重要性，这增加了初始的本地计算开销。
*   **自动化评估偏差**：虽然使用了 MD-Judge（基于 Mistral-7B）作为裁判，但自动评估可能存在误判，缺乏大规模的人工标注验证。
*   **防御演进风险**：如果未来模型采用非 Transformer 架构或改变了拒绝信号的编码方式，基于当前注意力机制的 Token 定位方法可能失效。

（完）
