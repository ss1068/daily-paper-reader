---
title: Robust Safety Monitoring of Language Models via Activation Watermarking
title_zh: 通过激活水印实现语言模型的鲁棒安全监控
authors: "Toluwani Aremu, Daniil Ognev, Samuele Poppi, Nils Lukas"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23171v1"
tags: ["query:llm"]
score: 10.0
evidence: 鲁棒的安全监控，用于检测和标记不安全行为及对抗性查询
tldr: "针对大语言模型面临的自适应攻击（即攻击者在诱导不安全行为的同时规避安全监测），本文提出了一种基于“激活水印”的鲁棒监测方法。该方法通过在推理过程中引入随机不确定性，使攻击者难以预测并绕过监测逻辑。实验表明，在攻击者了解监测算法但不知晓密钥的情况下，该方法比现有防御手段的性能提升了高达52%，有效增强了LLM在对抗环境下的安全性。"
selection_source: fresh_fetch
motivation: 现有的LLM安全监测机制容易受到自适应攻击者的规避，导致敏感信息泄露且难以被实时发现。
method: 将安全监测建模为安全博弈，并提出通过在模型激活层引入基于密钥的随机性（激活水印）来增加攻击者的预测难度。
result: "实验证明激活水印在应对知晓算法的自适应攻击者时，检测性能比传统基准方法提升了高达52%。"
conclusion: 激活水印通过引入不可预测性，为防御自适应攻击提供了一种比传统静态监测更具鲁棒性的LLM安全保障方案。
---

## 摘要
大语言模型（LLMs）可能被滥用以泄露敏感信息，例如武器制造指令或编写恶意软件。LLM 提供商依赖“监控”来检测并标记推理过程中的不安全行为。一个公开的安全挑战是“自适应”对手，他们精心设计攻击，以同时实现 (i) 规避检测和 (ii) 诱导不安全行为。自适应攻击者是一个主要担忧，因为 LLM 提供商在不了解其模型被如何滥用的情况下，无法修补其安全机制。我们将“鲁棒” LLM 监控建模为一个安全博弈，其中了解监控机制的对手试图提取敏感信息，而提供商必须在低误报率的前提下准确检测这些对抗性查询。我们的工作 (i) 表明现有的 LLM 监控器容易受到自适应攻击者的攻击，并且 (ii) 通过“激活水印”设计了改进的防御措施，即在推理过程中为攻击者精心引入不确定性。我们发现，在面对了解监控算法但不知道密钥的自适应攻击者时，激活水印的性能优于基准防御手段，提升幅度高达 52%。

## Abstract
Large language models (LLMs) can be misused to reveal sensitive information, such as weapon-making instructions or writing malware. LLM providers rely on $\emph{monitoring}$ to detect and flag unsafe behavior during inference. An open security challenge is $\emph{adaptive}$ adversaries who craft attacks that simultaneously (i) evade detection while (ii) eliciting unsafe behavior. Adaptive attackers are a major concern as LLM providers cannot patch their security mechanisms, since they are unaware of how their models are being misused. We cast $\emph{robust}$ LLM monitoring as a security game, where adversaries who know about the monitor try to extract sensitive information, while a provider must accurately detect these adversarial queries at low false positive rates. Our work (i) shows that existing LLM monitors are vulnerable to adaptive attackers and (ii) designs improved defenses through $\emph{activation watermarking}$ by carefully introducing uncertainty for the attacker during inference. We find that $\emph{activation watermarking}$ outperforms guard baselines by up to $52\%$ under adaptive attackers who know the monitoring algorithm but not the secret key.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**激活水印（Activation Watermarking）**的新技术，旨在解决大语言模型（LLM）在面对“自适应攻击者”时的安全监控脆弱性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管LLM经过了安全对齐，但仍面临“自适应攻击者”的威胁。这类攻击者通过迭代优化提示词（如越狱攻击），能够同时实现两个目标：(1) 诱导模型产生违规输出；(2) 规避现有的安全监控系统（如外部审核模型）。
*   **核心挑战**：现有的监控器（如 Llama Guard）通常是静态且公开的，攻击者可以针对这些监控器进行本地测试和绕过。
*   **整体含义**：论文将安全监控视为一场“安全博弈”，提出通过在模型内部隐藏基于密钥的随机信号（水印），使得攻击者在不知道密钥的情况下，无法预测并规避监控。

### 2. 方法论
*   **核心思想**：将安全检测信号直接嵌入到模型的内部激活层（Hidden States）中。当模型生成违规内容时，其激活向量会向一个特定的“秘密方向”偏移。
*   **关键技术细节**：
    *   **密钥生成**：为特定层采样一个随机高斯向量 $w$ 作为秘密密钥。
    *   **水印训练**：通过微调（Fine-tuning）使模型在处理有害样本时，其激活值与 $w$ 的余弦相似度最大化；处理良性样本时则最小化。
    *   **损失函数**：结合了 KL 散度（保持模型原始能力）和余弦相似度损失。引入了**线性权重（Linear Ramp）**，即在响应的后期（通常有害信息更明确）加强水印信号。
    *   **推理检测**：在推理时，计算模型激活值与秘密向量 $w$ 的余弦相似度均值。若超过预设阈值 $\tau$，则触发警报。
*   **算法流程**：密钥生成 $\rightarrow$ 带有安全标签的数据集准备 $\rightarrow$ 联合损失函数微调 $\rightarrow$ 推理期单次投影计算检测。

### 3. 实验设计
*   **数据集**：
    *   **BeaverTails**：用于基础安全分类训练和评估。
    *   **XSTest**：作为越狱攻击的种子提示词。
*   **攻击场景（Benchmark）**：
    *   **模板攻击**：Jailbroken, DeepInception, Multilingual。
    *   **优化攻击**：AutoDAN（基于遗传算法的自动化越狱）。
*   **对比方法**：
    *   **外部防护模型**：LlamaGuard-3-8B, QwenGuard-Gen-8B。
    *   **内部探针**：ActProbe（基于激活值的线性探针）。
*   **评估指标**：AUROC（检测准确性）、ASR@1% FPR（在极低误报率下的攻击成功率）、能力基准测试（MMLU, GSM8K 等）。

### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA RTX A6000** GPU。
*   **训练时长**：
    *   模型微调：约 4 GPU 小时。
    *   自适应攻击优化：约 14 GPU 小时。
    *   总计：单次完整实验运行约 **18 GPU 小时**。
*   **实现细节**：使用 bfloat16 精度，Adafactor 优化器，最大序列长度 512。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **72 组超参数消融实验**，涵盖了不同的层（6, 14, 23）、学习率、水印强度 $\lambda$ 和权重方案。
    *   测试了不同规模的模型（Qwen2.5 7B 和 14B）。
    *   设计了“秘密提取安全博弈”，包含 20 个合成实体的细粒度监控实验。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了检测率，还深入探讨了“能力税”（Utility Tax）、跨模型迁移攻击以及不同密钥之间的独立性，证明了该方法在对抗环境下的鲁棒性。

### 6 主要结论与发现
*   **性能卓越**：在面对知晓算法但不知密钥的自适应攻击者时，激活水印的检测性能比现有基准提升了高达 **52%**。
*   **鲁棒性强**：攻击者在特定密钥上优化的越狱提示词，难以迁移到使用不同密钥的同一模型上。
*   **低开销**：相比于需要额外前向传播的外部防护模型，激活水印仅增加极小的投影计算开销（$O(Kd)$），推理延迟几乎可忽略。
*   **能力保持**：在通用任务（MMLU, BBH）上性能几乎无损，但在数学推理（GSM8K）上有约 7% 的下降。

### 7. 优点
*   **首创性**：这是首个明确针对“自适应攻击者”设计的 LLM 内部安全监控机制。
*   **安全性高**：通过密钥随机化打破了攻击者的确定性假设。
*   **高效性**：单次前向传播即可完成生成与监控，适合高吞吐量部署。
*   **多策略支持**：能够同时监控多个不同的安全策略（如 PII 泄露、武器制造等），且互不干扰。

### 8. 不足与局限
*   **黑盒假设**：该防御假设攻击者无法访问模型的内部激活或梯度（API 访问模式）。如果攻击者拥有白盒权限，该方法可能失效。
*   **微调成本**：需要对模型进行微调，这对于闭源模型提供商来说可行，但增加了部署前的准备工作。
*   **数学能力下降**：实验观察到在严谨的逻辑/数学任务中存在一定的性能退化。
*   **经验性安全**：虽然实验表现优异，但缺乏类似密码学的可证明安全保证，未来可能出现更强的针对性攻击。

（完）
