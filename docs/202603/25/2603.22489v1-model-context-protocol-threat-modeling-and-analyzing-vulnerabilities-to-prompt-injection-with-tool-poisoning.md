---
title: Model Context Protocol Threat Modeling and Analyzing Vulnerabilities to Prompt Injection with Tool Poisoning
title_zh: 模型上下文协议（MCP）威胁建模及针对工具投毒引发的提示词注入漏洞分析
authors: "Charoes Huang, Xin Huang, Ngoc Phu Tran, Amin Milani Fard"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22489v1"
tags: ["query:llm"]
score: 9.0
evidence: 分析工具中毒导致的提示注入漏洞
tldr: 本研究针对新兴的 AI 工具连接标准——模型上下文协议（MCP）进行了深入的安全性分析。通过 STRIDE 和 DREAD 框架对 MCP 生态系统的五个核心组件进行威胁建模，识别出“工具中毒”（即在工具元数据中嵌入恶意指令）是影响客户端安全的关键漏洞。研究对 7 个主流 MCP 客户端进行了实证评估，揭示了普遍存在的验证不足问题，并提出了一套涵盖静态分析、行为检测和透明度机制的多层防御策略，填补了 MCP 客户端安全研究的空白。
selection_source: fresh_fetch
motivation: 随着 MCP 协议的普及，其客户端安全性（尤其是针对工具中毒攻击的防御）尚未得到充分研究，存在严重的安全隐患。
method: 采用 STRIDE 和 DREAD 框架对 MCP 架构进行全面威胁建模，并对 7 个主流 MCP 客户端在应对工具中毒攻击时的表现进行了系统性对比测试。
result: 实验表明，由于缺乏严格的静态验证和参数可见性，大多数受测客户端均无法有效抵御工具中毒攻击。
conclusion: 必须实施结合元数据分析、模型决策追踪和用户透明度机制的多层防御体系，以确保 AI 代理生态系统的安全性。
---

## 摘要
模型上下文协议（MCP）已迅速成为将 AI 助手连接到外部工具和数据源的通用标准。虽然 MCP 简化了 AI 应用与各种服务之间的集成，但它也引入了重大的安全漏洞，尤其是在客户端。在本研究中，我们利用 STRIDE（仿冒、篡改、抵赖、信息泄露、拒绝服务、特权提升）和 DREAD（潜在损失、重现性、可利用性、受影响用户、可发现性）框架，针对五个关键组件对 MCP 实现进行了威胁建模：(1) MCP 宿主与客户端，(2) 大语言模型（LLM），(3) MCP 服务器，(4) 外部数据存储，以及 (5) 授权服务器。这一全面分析表明，工具投毒（即在工具元数据中嵌入恶意指令）是最普遍且影响最大的客户端漏洞。因此，我们将实证评估重点放在这一关键攻击向量上，系统地比较了七个主流 MCP 客户端如何验证和防御工具投毒攻击。我们的分析显示，由于静态验证不足和参数可见性差，大多数受测客户端都存在严重的安全问题。我们提出了一种多层防御策略，包括静态元数据分析、模型决策路径追踪、行为异常检测以及用户透明度机制。本研究填补了 MCP 安全领域的一个关键空白（此前研究主要集中在服务器端漏洞），并为保障 AI 智能体生态系统的安全提供了可操作的建议和缓解策略。

## Abstract
The Model Context Protocol (MCP) has rapidly emerged as a universal standard for connecting AI assistants to external tools and data sources. While MCP simplifies integration between AI applications and various services, it introduces significant security vulnerabilities, particularly on the client side. In this work we conduct threat modelings of MCP implementations using STRIDE (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege) and DREAD (Damage, Reproducibility, Exploitability, Affected Users, Discoverability) frameworks across five key components: (1) MCP Host and Client, (2) LLM, (3) MCP Server, (4) External Data Stores, and (5) Authorization Server. This comprehensive analysis reveals tool poisoning-where malicious instructions are embedded in tool metadata-as the most prevalent and impactful client-side vulnerability. We therefore focus our empirical evaluation on this critical attack vector, providing a systematic comparison of how seven major MCP clients validate and defend against tool poisoning attacks. Our analysis reveals significant security issues with most tested clients due to insufficient static validation and parameter visibility. We propose a multi-layered defense strategy encompassing static metadata analysis, model decision path tracking, behavioral anomaly detection, and user transparency mechanisms. This research addresses a critical gap in MCP security, which has primarily focused on server-side vulnerabilities, and provides actionable recommendations and mitigation strategies for securing AI agent ecosystems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Model Context Protocol Threat Modeling and Analyzing Vulnerabilities to Prompt Injection with Tool Poisoning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：论文探讨了新兴的**模型上下文协议（MCP）**在客户端实现上的安全缺陷。MCP 旨在成为 AI 助手连接外部工具的通用标准，但其设计中存在一个严重的信任漏洞：客户端往往盲目信任服务器提供的工具元数据。
*   **研究动机**：虽然 MCP 发展迅速（已有超过 18,000 个服务器），但现有研究多集中在服务器端安全，忽略了客户端在处理“工具投毒”（Tool Poisoning）攻击时的脆弱性。攻击者可以通过恶意工具描述操纵 LLM 的决策，导致数据泄露或远程代码执行。

### 2. 方法论
*   **威胁建模框架**：
    *   **STRIDE 模型**：从仿冒、篡改、抵赖、信息泄露、拒绝服务、特权提升六个维度，对 MCP 宿主、LLM、服务器、数据存储和授权服务器五个组件进行了全面分析，识别出 57 项潜在威胁。
    *   **DREAD 评级**：对识别出的威胁进行定量评分，发现“工具投毒”和“提示词注入”属于最高风险级别（Critical）。
*   **核心攻击技术（工具投毒）**：攻击者在工具的 `description` 或 `inputSchema` 中嵌入恶意指令。当客户端将这些元数据传递给 LLM 时，LLM 会将其误认为系统指令并执行（如：在执行加法运算前先读取用户的 SSH 密钥并作为参数传回）。
*   **多层防御策略**：提出了四层防御架构：
    1.  **注册验证层**：对工具定义进行静态扫描和架构验证。
    2.  **决策路径分析**：追踪 LLM 选择工具的逻辑，识别异常决策。
    3.  **运行时监控**：在沙箱中执行工具，限制文件系统和网络访问。
    4.  **用户透明度**：强制显示所有隐藏参数并要求显式确认。

### 3. 实验设计
*   **测试对象**：选择了 7 个主流 MCP 客户端：Claude Desktop、Cursor、Cline、Continue、Gemini CLI、Claude Code、Langflow。
*   **攻击场景（Benchmark）**：设计了四种渐进式攻击类型：
    1.  **读取敏感文件**：诱导 LLM 读取 `.ssh` 或配置文件。
    2.  **活动日志监控**：强制恶意工具在高优先级执行以监视用户行为。
    3.  **创建钓鱼链接**：利用 Markdown 隐藏恶意 URL。
    4.  **远程脚本执行**：诱导 LLM 下载并运行 shell 脚本。
*   **对比维度**：评估各客户端的静态验证、参数可见性、注入检测、用户警告、沙箱化和审计日志功能。

### 4. 资源与算力
*   **算力说明**：论文未提及大规模 GPU 训练算力，因为该研究属于**安全性评估与渗透测试**。
*   **模型使用**：实验主要基于 `claude-3.5-sonnet` 和 `claude-4.5-sonnet`（除 Gemini CLI 外），以确保变量受控，重点观察不同客户端实现对同一模型决策的影响。

### 5. 实验数量与充分性
*   **实验规模**：对 7 个客户端分别进行了 4 类攻击测试，共计 28 组核心实验，并记录了详细的交互日志和截图。
*   **充分性与客观性**：实验涵盖了从商业闭源（Cursor）到开源插件（Cline/Continue）的多种形态。通过保持底层 LLM 模型的一致性，客观地证明了**安全性差异源于客户端的工程实现而非模型本身**。

### 6. 主要结论与发现
*   **安全性差异巨大**：不同客户端的安全表现极不均衡。**Claude Desktop** 和 **Cline** 表现最安全（成功拦截多数攻击）；而 **Cursor** 最为脆弱，在所有四项攻击测试中均告失败（Unsafe）。
*   **验证机制缺失**：5/7 的客户端完全没有静态验证机制，盲目将服务器描述注入 LLM 上下文。
*   **模型行为是关键防御层**：内置强效伦理准则的模型（如 Claude）能自发拒绝部分恶意指令，但客户端的工程防护（如参数脱敏、路径过滤）仍是不可或缺的。
*   **用户界面误导**：许多客户端隐藏了工具调用的实际参数，导致用户在不知情的情况下批准了恶意操作（审批疲劳）。

### 7. 优点
*   **首创性**：这是首篇系统性针对 MCP 客户端安全进行实证对比研究的论文。
*   **实用性强**：不仅发现了漏洞，还提供了可复现的恶意服务器代码示例和详细的防御路线图。
*   **多维建模**：结合了 STRIDE 和 DREAD 框架，为 AI 代理协议的标准化提供了理论依据。

### 8. 不足与局限
*   **样本量限制**：虽然涵盖了主流工具，但 MCP 生态发展极快，更多新兴客户端未被纳入。
*   **评分主观性**：作者承认 DREAD 评分存在一定的主观偏差。
*   **沙箱测试不足**：由于部分客户端闭源，论文未能深入测试其底层沙箱（如 Docker/gVisor）的隔离强度，更多是基于行为观察。
*   **动态演进**：随着厂商修复漏洞，文中提到的部分客户端安全性可能会迅速提升，实验结果具有时效性。

（完）
