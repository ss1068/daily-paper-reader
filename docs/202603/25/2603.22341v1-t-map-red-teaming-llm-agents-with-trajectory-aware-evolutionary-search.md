---
title: "T-MAP: Red-Teaming LLM Agents with Trajectory-aware Evolutionary Search"
title_zh: T-MAP：基于轨迹感知演化搜索的大语言模型智能体红队测试
authors: "Hyomin Lee, Sangwoo Park, Yumin Choi, Sohyun An, Seanie Lee, Sung Ju Hwang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.22341v1"
tags: ["query:llm"]
score: 10.0
evidence: 对LLM智能体进行红队测试，以发现对抗性提示并绕过安全防护栏
tldr: 针对现有红队测试仅关注文本输出而忽视LLM智能体多步工具执行风险的问题，本文提出T-MAP。该方法采用轨迹感知的进化搜索策略，利用执行轨迹引导对抗性提示词的发现，旨在诱导智能体通过实际工具交互实现有害目标。实验表明，T-MAP在MCP环境下显著提升了攻击实现率，揭示了包括GPT-5.2在内的前沿模型在自主交互中的深层安全漏洞。
selection_source: fresh_fetch
motivation: 现有的红队测试难以捕捉LLM智能体在多步工具调用和复杂交互中涌现的特定安全漏洞。
method: 提出一种轨迹感知的进化搜索方法T-MAP，通过分析执行轨迹来自动生成并优化能够绕过安全防护的对抗性提示。
result: T-MAP在攻击实现率上显著超越基准，并能有效攻击GPT-5.2、Gemini-3-Pro等最新前沿模型。
conclusion: 研究证明了自主LLM智能体在工具执行层面存在严重的安全隐患，亟需更有效的轨迹级防御机制。
---

## 摘要
尽管先前的红队测试工作主要集中在诱导大语言模型（LLM）产生有害文本输出，但这些方法未能捕捉到在多步工具执行过程中出现的智能体特有漏洞，特别是在如模型上下文协议（MCP）等快速发展的生态系统中。为填补这一空白，我们提出了一种轨迹感知的演化搜索方法 T-MAP，该方法利用执行轨迹来引导对抗性提示词的发现。我们的方法能够自动生成攻击，不仅能绕过安全防护栏，还能通过实际的工具交互可靠地实现有害目标。在多种 MCP 环境下的实证评估表明，T-MAP 在攻击实现率（ARR）方面显著优于基准方法，并且对包括 GPT-5.2、Gemini-3-Pro、Qwen3.5 和 GLM-5 在内的前沿模型依然有效，从而揭示了自主 LLM 智能体中此前未被充分探索的漏洞。

## Abstract
While prior red-teaming efforts have focused on eliciting harmful text outputs from large language models (LLMs), such approaches fail to capture agent-specific vulnerabilities that emerge through multi-step tool execution, particularly in rapidly growing ecosystems such as the Model Context Protocol (MCP). To address this gap, we propose a trajectory-aware evolutionary search method, T-MAP, which leverages execution trajectories to guide the discovery of adversarial prompts. Our approach enables the automatic generation of attacks that not only bypass safety guardrails but also reliably realize harmful objectives through actual tool interactions. Empirical evaluations across diverse MCP environments demonstrate that T-MAP substantially outperforms baselines in attack realization rate (ARR) and remains effective against frontier models, including GPT-5.2, Gemini-3-Pro, Qwen3.5, and GLM-5, thereby revealing previously underexplored vulnerabilities in autonomous LLM agents.

---

## 论文详细总结（自动生成）

以下是对论文《T-MAP: Red-Teaming LLM Agents with Trajectory-aware Evolutionary Search》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着大语言模型（LLM）智能体通过模型上下文协议（MCP）等标准与外部环境（如文件系统、邮件、代码执行器）深度集成，安全风险已从单纯的“有害文本生成”转向“有害环境操作”（如财务损失、数据外泄）。
*   **核心问题**：现有的红队测试（Red-Teaming）方法主要针对单轮对话或文本输出，无法捕捉智能体在多步工具调用、复杂规划和环境交互中涌现的特定漏洞。
*   **研究动机**：开发一种能够利用智能体执行轨迹（Trajectory）作为反馈，自动发现并优化能够实现复杂有害目标的对抗性提示词的方法。

### 2. 方法论：T-MAP 架构
T-MAP 是一种基于 **MAP-Elites** 演化算法的轨迹感知搜索框架，其核心流程包含四个迭代步骤：
*   **二维存档（2D Archive）管理**：维护一个由“风险类别”（如财产损失、隐私泄露）和“攻击风格”（如角色扮演、假设框架）组成的 8x8 存档，旨在保持攻击的多样性。
*   **交叉诊断（Cross-Diagnosis）**：利用 **LLM Analyst** 分析父代提示词的成功因子（为何能绕过防护）和目标单元格提示词的失败原因（为何工具调用出错或被拒绝），将原始轨迹转化为可操作的改进建议。
*   **工具调用图（TCG, Tool Call Graph）**：构建一个有向图，记录工具间转换的成功/失败次数及原因。**LLM Mutator** 在生成新提示词时会参考 TCG，避开高失败率的路径，选择更可靠的工具链。
*   **轨迹感知变异与评估**：
    1.  **初始化**：生成种子提示词并执行。
    2.  **变异**：结合交叉诊断结果和 TCG 指引生成新提示词。
    3.  **执行与评估**：在环境中运行，由 **LLM Judge** 根据轨迹将结果分为 L0（拒绝）到 L3（完全实现攻击）四个等级。
    4.  **更新**：若新提示词表现更好或更具代表性，则更新存档和 TCG。

### 3. 实验设计
*   **实验场景**：基于 5 个真实的 MCP 服务器环境：CodeExecutor（代码执行）、Slack（社交通讯）、Gmail（邮件）、Playwright（浏览器操作）、Filesystem（文件系统）。
*   **目标模型（Target Models）**：主要使用 GPT-5-mini，并在泛化性实验中测试了 GPT-5.2、Gemini-3-Pro、Qwen3.5、GLM-5、Claude 4.6 等 9 个前沿模型。
*   **对比基准（Baselines）**：
    *   **Zero-Shot (ZS)**：单次生成。
    *   **Multi-Trial (MT)**：多次独立尝试。
    *   **Iterative Refinement (IR)**：基于自身轨迹的自我修正。
    *   **Standard Evolution (SE)**：不带轨迹感知的标准演化算法。
*   **评估指标**：拒绝率（RR）、攻击实现率（ARR，即 L3 等级的占比）、动作多样性（唯一成功轨迹数）、文本多样性（Self-BLEU）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长，因为该方法属于**黑盒攻击**，主要通过 API 调用实现。
*   **成本分析**：文中详细列出了 Token 消耗和 API 成本。
    *   **攻击者模型**：使用 DeepSeek-V3.2。
    *   **成本范围**：单个 MCP 环境的完整红队测试（100 次迭代）成本在 $3.85 到 $13.67 之间。
    *   **目标模型成本**：根据模型定价不同，从 $1.47（GPT-OSS-120B）到 $20.51（Claude Opus 4.6）不等。

### 5. 实验数量与充分性
*   **实验规模**：每个环境进行 100 次迭代，每轮并行生成 3 个提示词，总计为每个环境生成 300 个候选攻击。
*   **充分性验证**：
    *   **消融实验**：分别验证了 TCG 和交叉诊断组件的贡献。
    *   **跨模型迁移**：测试了在一个模型上发现的攻击在其他模型上的成功率（Pass@5）。
    *   **多服务器链式攻击**：设计了跨 Slack+CodeExecutor 等复杂场景，验证了处理长链条攻击的能力。
    *   **人类评估**：通过 4 名专家对 96 个样本进行标注，证明了 LLM Judge 与人类判断的高度一致性（相关性 > 0.83）。
*   **评价**：实验设计全面，覆盖了从单环境到多环境、从模型内部到跨模型的多个维度，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：T-MAP 的平均攻击实现率（ARR）达到 **57.8%**，远超最强基准 SE（32.5%）和 IR（15.6%）。
*   **轨迹反馈的重要性**：单纯的提示词变异（SE）虽能降低拒绝率，但往往卡在工具执行错误（L1）或弱成功（L2）阶段；只有引入轨迹感知，才能显著提升 L3（完全实现）的比例。
*   **前沿模型脆弱性**：即使是经过严格安全对齐的最先进模型（如 GPT-5.2），在面对针对工具执行逻辑设计的对抗性提示词时，仍表现出显著的漏洞。
*   **多样性**：T-MAP 发现的成功攻击轨迹数量是基准方法的 2-18 倍，且文本表达更具多样性。

### 7. 优点（亮点）
*   **视角创新**：将红队测试从“文本层面”提升到了“执行轨迹层面”，更符合当前智能体应用的安全需求。
*   **TCG 机制**：引入工具调用图来积累“环境经验”，使攻击者模型能够像人类黑客一样学习哪些工具组合更容易成功。
*   **多样性维护**：利用 MAP-Elites 框架有效避免了攻击策略的坍缩，能够系统性地绘制智能体的漏洞地图。

### 8. 不足与局限
*   **沙盒环境限制**：实验在受控的沙盒中进行，现实部署中的智能体可能拥有更复杂的权限管理和人工确认环节，ARR 可能会降低。
*   **防御侧缺失**：论文重点在于“破”，对于如何利用轨迹信息进行“立”（即防御增强）仅做了初步探讨。
*   **对攻击模型能力的依赖**：T-MAP 的效果高度依赖于攻击者模型（如 DeepSeek-V3.2）的推理能力，若攻击者模型本身受到严格限制，该框架的效力可能受限。

（完）
