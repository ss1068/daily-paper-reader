---
title: "LLMON: An LLM-native Markup Language to Leverage Structure and Semantics at the LLM Interface"
title_zh: LLMON：一种在 LLM 接口处利用结构与语义的 LLM 原生标记语言
authors: "Michael Hind, Basel Shbita, Bo Wu, Farhan Ahmed, Chad DeLuca, Nathan Fulton, David Cox, Dan Gutfreund"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22519v1"
tags: ["query:llm"]
score: 9.0
evidence: 缓解提示注入等安全风险的标记语言
tldr: 针对大语言模型（LLM）仅使用纯文本字符串交互导致结构与语义信息丢失的问题，本文提出了LLMON（LLM Object Notation）。这是一种LLM原生的标记语言，旨在清晰区分指令与数据等元数据。通过在训练、提示和推理阶段引入结构化信息，LLMON能显著提升模型的准确性、安全性（如防御提示注入）和推理效率，为构建更可靠的LLM接口提供了新思路。
selection_source: fresh_fetch
motivation: 现有的纯文本接口无法区分指令与数据，导致模型容易产生混淆并面临提示注入等安全风险。
method: 提出一种名为LLMON的标记语言，通过结构化和语义元数据来增强LLM的输入输出表达。
result: 初步实验证明，LLMON在模型训练和推理实现中能有效提升任务准确性并增强安全性。
conclusion: LLMON作为一种原生的标记语言，为优化LLM的交互结构、提升模型性能和安全性开辟了新的研究方向。
---

## 摘要
文本大语言模型（LLM）提供了一个简单且熟悉的接口：使用文本字符串进行输入和输出。然而，传递给 LLM 的信息通常具有更丰富的结构和语义，而这些信息在字符串中并未得到体现。例如，大多数提示词（prompt）既包含指令（如“将这篇论文总结成一段话”），也包含数据（待总结的论文），但在传递给模型时，这两者通常不作区分。这可能导致模型混淆和安全风险，例如提示词注入攻击。本研究通过引入一种 LLM 原生标记语言 LLMON（LLM 对象表示法，发音为“Lemon”）来解决这一缺陷，该语言能够以自然的方式将文本的结构和语义元数据传达给 LLM。这些信息随后可用于模型训练、模型提示和推理实现，从而提高模型的准确性、安全性和可靠性。这类似于编程语言中的类型可用于多种用途，如静态检查、代码生成、动态检查和 IDE 高亮显示。我们讨论了 LLM 原生标记语言的一般设计要求，介绍了 LLMON 标记语言并展示了它如何满足这些设计要求，描述了 LLMON 产物中所包含的信息如何使模型训练和推理实现受益，并为这两个用例的价值提供了一些初步的实证证据。我们还讨论了通过 LLM 原生方法实现的更广泛的问题和研究机会。

## Abstract
Textual Large Language Models (LLMs) provide a simple and familiar interface: a string of text is used for both input and output. However, the information conveyed to an LLM often has a richer structure and semantics, which is not conveyed in a string. For example, most prompts contain both instructions ("Summarize this paper into a paragraph") and data (the paper to summarize), but these are usually not distinguished when passed to the model. This can lead to model confusion and security risks, such as prompt injection attacks.   This work addresses this shortcoming by introducing an LLM-native mark-up language, LLMON (LLM Object Notation, pronounced "Lemon"), that enables the structure and semantic metadata of the text to be communicated in a natural way to an LLM. This information can then be used during model training, model prompting, and inference implementation, leading to improvements in model accuracy, safety, and security. This is analogous to how programming language types can be used for many purposes, such as static checking, code generation, dynamic checking, and IDE highlighting.   We discuss the general design requirements of an LLM-native markup language, introduce the LLMON markup language and show how it meets these design requirements, describe how the information contained in a LLMON artifact can benefit model training and inference implementation, and provide some preliminary empirical evidence of its value for both of these use cases. We also discuss broader issues and research opportunities that are enabled with an LLM-native approach.

---

## 论文详细总结（自动生成）

### 论文总结：LLMON —— 一种用于优化 LLM 接口的结构化标记语言

#### 1. 核心问题与整体含义（研究动机和背景）
当前大语言模型（LLM）主要采用纯文本字符串作为交互接口。这种“扁平化”的接口存在显著缺陷：
*   **指令与数据混淆**：模型无法从本质上区分哪些是操作指令，哪些是待处理的数据，导致容易受到**提示注入攻击（Prompt Injection）**。
*   **缺乏引用结构**：在长上下文中，模型难以精确指代特定的文档或对象。
*   **不可移植与非确定性**：提示词在不同模型间表现差异大，且输出难以预测。
*   **维护困难**：长字符串形式的提示词缺乏模块化、可调试性和版本控制能力。
**研究动机**：借鉴编程语言的类型系统和操作系统的数据/代码分离原则，提出一种 LLM 原生的标记语言，为 LLM 交互引入显式的结构和语义。

#### 2. 方法论：核心思想与技术细节
论文提出了 **LLMON (LLM Object Notation)**，其核心思想是通过结构化标签（Tags）为文本片段提供元数据。
*   **两种语法形态**：
    *   **Human LLMON**：面向人类编写，使用类似 `\tag\ 内容 /tag/` 的简洁语法，支持嵌套和命名实例（如 `\instr:a\ ... /instr:a/`）。
    *   **Machine LLMON**：面向模型处理，将标签转换为 6 个预定义的**特殊 Token**（如 `<|open|>`, `<|close|>`, `<|.|>` 等）。这些 Token 在分词阶段被视为原子单位，确保结构边界不被破坏。
*   **关键设计原则**：
    *   **前缀标注**：标签出现在内容之前，引导模型生成。
    *   **扁平化嵌套**：将深层嵌套转换为显式的路径标签（如 `email.header.from`），减轻模型的长程依赖压力。
    *   **可转换性**：提供与 JSON 等结构化格式的双向转换器。
*   **应用机制**：
    *   **训练阶段**：在监督微调（SFT）中使用 LLMON 格式，让模型学习指令、数据和执行绑定（exec）之间的语义关系。
    *   **推理阶段**：利用标签边界实施**约束掩码（Constrained Masking）**，在注意力机制层面屏蔽非目标指令的干扰，确保安全性。

#### 3. 实验设计
*   **数据集/场景**：
    *   **通用能力测试**：使用 MMLU、GSM8K、IFEval 评估模型是否因引入结构化信息而导致通用能力退化。
    *   **Distractor Benchmark（干扰项基准）**：论文自建了包含 100 个手动构造的测试用例，每个用例包含多个竞争指令，测试模型是否能根据 `exec` 标签准确执行指定指令。
*   **对比方法**：
    *   **Base 模型**：未经微调的原始模型。
    *   **传统 Chat 模板 SFT**：使用标准的 User/Assistant 角色格式进行微调。
    *   **LLMON 结构化 SFT**：分为全量微调（Full Fine-tuning）和 LoRA 适配。
*   **骨干模型**：IBM Granite-4.0-Micro-Base (3B) 和 Qwen2.5-3B。

#### 4. 资源与算力
*   **训练规模**：使用了约 340 万个结构化示例，总计约 **29 亿（2.9B）个 Token**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及具体的训练时长。

#### 5. 实验数量与充分性
*   **实验组数**：涵盖了从 2.41 亿到 29 亿 Token 的不同训练规模实验，对比了全量微调与 LoRA 的差异，并分别验证了训练端和推理端的改进。
*   **充分性评价**：实验设计较为系统，通过引入“干扰项基准”精准捕捉了 LLMON 的核心优势。对比了不同家族的模型（Granite 和 Qwen），证明了方法的普适性。但在模型参数规模上（仅测试了 3B 级别），尚未验证其在超大规模模型（如 70B+）上的表现。

#### 6. 主要结论与发现
*   **显著提升执行控制力**：在多指令干扰测试中，经过 LLMON 训练的模型准确率从接近 0% 提升至 **83% 以上**（平均提升 74.2 个百分点）。
*   **推理端增强有效**：即使不进行重新训练，仅在推理时使用 LLMON 边界进行约束掩码，也能将准确率提升约 **29.3 个百分点**。
*   **通用能力保持稳定**：在 MMLU 等标准测试中，LLMON 模型表现与传统 SFT 模型相当，未出现灾难性遗忘。
*   **安全性提升**：通过显式区分指令和数据，LLMON 能有效防御数据中夹带的恶意指令（提示注入）。

#### 7. 优点
*   **系统性解决接口模糊性**：将“提示词工程”转向“提示词编程”，为 LLM 交互提供了严谨的工程化基础。
*   **双重保障机制**：既可以通过训练强化模型意识，也可以通过推理侧的硬性掩码提供确定性保障。
*   **兼容性强**：无需修改模型架构，仅通过分词器添加特殊 Token 即可实现。

#### 8. 不足与局限
*   **命名空间治理**：用户自定义标签可能存在命名冲突或语义歧义，缺乏全局治理机制。
*   **Token 效率**：引入额外的特殊 Token 和冗长的闭合标签会增加序列长度，可能略微提升推理成本。
*   **实验覆盖面**：主要针对指令遵循和干扰防御，尚未在更复杂的长文本推理或多模态场景下进行广泛测试。
*   **推理开销**：虽然提出了推理侧掩码，但未详细量化解析标签边界带来的额外计算延迟。

（完）
