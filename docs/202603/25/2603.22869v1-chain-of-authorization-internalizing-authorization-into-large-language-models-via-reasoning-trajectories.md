---
title: "Chain-of-Authorization: Internalizing Authorization into Large Language Models via Reasoning Trajectories"
title_zh: 授权链：通过推理轨迹将授权机制内化至大语言模型
authors: "Yang Li, Yule Liu, Xinlei He, Youjian Zhao, Qi Li, Ke Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22869v1"
tags: ["query:llm"]
score: 9.0
evidence: 缓解LLM中敏感数据泄露和对抗性操纵的风险
tldr: 针对大语言模型（LLM）缺乏数据所有权和访问边界意识导致的敏感数据泄露风险，本文提出Chain-of-Authorization (CoA) 框架。该框架通过监督微调将授权逻辑内化为模型的推理能力，要求模型在生成最终回答前，先进行资源审查、身份解析和决策判断。CoA在保持模型原有性能的同时，显著提升了对越权访问和对抗性攻击的防御能力，为构建安全可靠的AI系统提供了新思路。
selection_source: fresh_fetch
motivation: 现有LLM无法区分数据权限，且传统防御手段在处理细粒度动态授权和可扩展性方面存在局限。
method: 提出CoA框架，通过在输入中嵌入权限上下文，并训练模型生成包含资源审查、身份解析和决策阶段的显式授权推理轨迹。
result: 实验表明CoA在授权场景下保持了高可用性，并能有效识别并拒绝各种未经授权或对抗性的访问请求。
conclusion: 将授权逻辑内化为LLM的推理过程，利用自然语言理解能力实现动态授权，是提升AI系统安全性的有效机制。
---

## 摘要
大语言模型（LLMs）已成为现代人工智能（AI）系统的核心认知组件，通过结合内部知识与外部上下文来执行复杂任务。然而，LLMs 通常无差别地处理所有可访问数据，缺乏对知识所有权和访问边界的内在意识。这种缺陷增加了敏感数据泄露和对抗性操纵的风险，可能导致未经授权的系统访问和严重的安全危机。现有的保护策略依赖于僵化、统一的防御，阻碍了动态授权。结构隔离方法面临可扩展性瓶颈，而提示引导方法难以进行细粒度的权限区分。在此，我们提出了授权链（Chain-of-Authorization, CoA）框架，这是一种将授权逻辑内化为 LLMs 核心能力的安全性训练与推理范式。与被动的外部防御不同，CoA 重构了模型的信息流：它在输入端嵌入权限上下文，并要求在最终响应前生成包含资源审查、身份解析和决策阶段的显式授权推理轨迹。通过在涵盖各种授权状态的数据上进行监督微调，CoA 将策略执行与任务响应相结合，使授权成为实质性响应的因果前提。广泛的评估表明，CoA 不仅在授权场景下保持了相当的效用，还克服了权限不匹配时的认知混淆。它对各种未经授权和对抗性访问表现出极高的拒绝率。该机制利用 LLMs 的推理能力来执行动态授权，将自然语言理解作为一种主动安全机制，用于在现代 AI 系统中部署可靠的 LLMs。

## Abstract
Large Language Models (LLMs) have become core cognitive components in modern artificial intelligence (AI) systems, combining internal knowledge with external context to perform complex tasks. However, LLMs typically treat all accessible data indiscriminately, lacking inherent awareness of knowledge ownership and access boundaries. This deficiency heightens risks of sensitive data leakage and adversarial manipulation, potentially enabling unauthorized system access and severe security crises. Existing protection strategies rely on rigid, uniform defense that prevent dynamic authorization. Structural isolation methods faces scalability bottlenecks, while prompt guidance methods struggle with fine-grained permissions distinctions. Here, we propose the Chain-of-Authorization (CoA) framework, a secure training and reasoning paradigm that internalizes authorization logic into LLMs' core capabilities. Unlike passive external defneses, CoA restructures the model's information flow: it embeds permission context at input and requires generating explicit authorization reasoning trajectory that includes resource review, identity resolution, and decision-making stages before final response. Through supervised fine-tuning on data covering various authorization status, CoA integrates policy execution with task responses, making authorization a causal prerequisite for substantive responses. Extensive evaluations show that CoA not only maintains comparable utility in authorized scenarios but also overcomes the cognitive confusion when permissions mismatches. It exhibits high rejection rates against various unauthorized and adversarial access. This mechanism leverages LLMs' reasoning capability to perform dynamic authorization, using natural language understanding as a proactive security mechanism for deploying reliable LLMs in modern AI systems.

---

## 论文详细总结（自动生成）

### 论文总结：Chain-of-Authorization (CoA) —— 将授权机制内化至大语言模型

#### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理信息时具有“认知无差别性”，即无法区分内部参数知识或外部检索上下文的数据所有权和访问边界。
*   **研究背景**：随着 LLM 成为 AI Agent 的核心，敏感数据泄露和越权访问风险剧增。
*   **现有局限**：
    *   **结构隔离法**（如分片存储）：扩展性差，存储和计算成本高。
    *   **提示引导法**（如系统提示词约束）：属于“软约束”，容易被对抗性提示词绕过，且在权限不匹配时容易产生认知混淆。
*   **论文目标**：将授权逻辑从外部防御转变为模型的内在推理能力，使模型在生成答案前主动评估权限。

#### 2. 方法论：授权链 (CoA) 框架
*   **核心思想**：通过重构模型的信息流，将授权决策建模为生成过程中的一个不可分割的推理步骤（即“授权轨迹”），使其成为最终响应的因果前提。
*   **关键技术细节**：
    1.  **权限感知输入重构**：在输入端显式注入权限上下文。包括：
        *   用户权限标签（$C_u$）：注入系统提示词。
        *   外部上下文标签（$C_e$）：标注检索文档的所有权。
        *   工具权限标签（$C_g$）：定义工具的可访问级别。
    2.  **授权推理轨迹生成**：要求模型在回答前生成包含三个阶段的推理链：
        *   **资源审查 (Resource Review)**：分析任务所需的知识或工具权限。
        *   **身份解析 (Identity Resolution)**：识别当前用户的权限状态。
        *   **决策判断 (Decision Making)**：基于前两步得出授权结论（匹配、不匹配或无权限）。
    3.  **监督微调 (SFT) 范式**：使用包含三种授权状态（匹配、不匹配、公共访问）的合成数据进行训练，强制模型学习权限边界。

#### 3. 实验设计
*   **数据集与场景**：
    *   **内部参数知识**：WMDP（生物、化学、网络安全风险）、MMLU（多学科知识）。
    *   **外部上下文**：SQuAD、CovidQA（阅读理解与检索增强）。
    *   **工具调用**：Mobile-Actions（移动端操作指令）。
*   **基座模型**：Qwen3-1.7B、Llama-3.1-8B-Instruct、Mistral-7B-v0.3。
*   **对比方法 (Baselines)**：
    *   Base 模型与标准 SFT 模型。
    *   **SFT+Extra**：使用外部分类器（RoBERTa）进行权限预审。
    *   **PermissionLM**：结构隔离方法的代表。
    *   **sudoLM**：提示引导方法的代表。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：
    *   采用 **LoRA (Low-Rank Adaptation)** 进行参数高效微调，秩（rank）设为 64。
    *   使用 DeepSpeed ZeRO-2 策略进行显存优化。
    *   训练轮数为 3 轮（Epochs），学习率在 $1.0 \times 10^{-4}$ 到 $5.0 \times 10^{-4}$ 之间。
    *   扩展了词表以支持新增的权限标签。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流数据集、3 种不同架构和规模的模型，实验设计非常全面。
*   **多维度验证**：
    *   **效用测试**：验证在授权状态下是否保持了原有的任务准确率。
    *   **安全性测试**：在权限不匹配和公共访问状态下的拒绝率。
    *   **对抗性测试**：针对手动越狱、LLM 生成的诱导攻击（PAP）及自动迭代攻击（PAIR）的防御力。
    *   **深入分析**：包括 t-SNE 隐藏层表示可视化、因果干预实验（验证推理轨迹对决策的真实影响）以及消融实验。
*   **客观性**：通过跨模型、跨任务的对比，证明了 CoA 的鲁棒性和通用性，实验设计严谨且客观。

#### 6. 主要结论与发现
*   **高效平衡**：CoA 在授权场景下保持了与标准 SFT 相当的准确率，但在未经授权场景下实现了**接近 100% 的拒绝率**。
*   **强鲁棒性**：相比 sudoLM 等方法，CoA 能有效抵御复杂的对抗性提示词攻击，不易被语义扰动误导。
*   **表征分离**：可视化证明 CoA 成功在隐藏层空间中将不同授权状态的表征进行了去耦合，形成了清晰的决策边界。
*   **因果驱动**：干预实验证实，模型的安全性并非来自表面的提示词约束，而是源于推理轨迹中逻辑状态的因果依赖。

#### 7. 优点与亮点
*   **内生安全性**：将安全策略从“外部过滤器”变为“内在认知”，解决了策略执行与推理过程脱节的问题。
*   **细粒度动态授权**：能够处理复杂的权限包含关系，而非简单的二元分类。
*   **高可扩展性**：无需为每个权限训练独立模型，单个模型即可处理成百上千种权限标签。
*   **可解释性**：通过显式的推理轨迹，用户和审计者可以清晰看到模型拒绝或接受请求的逻辑依据。

#### 8. 不足与局限
*   **微调依赖**：CoA 依赖于高质量的监督微调数据，对于无法进行微调的闭源模型难以直接应用。
*   **推理开销**：生成授权推理轨迹会增加额外的 Token 输出，可能导致推理延迟略微增加。
*   **标签依赖**：系统的安全性高度依赖于输入端权限标签的准确性，如果上游系统（如检索器或身份管理系统）提供的标签错误，CoA 无法纠正。
*   **应用限制**：目前主要针对文本和工具调用，对于多模态数据（如图像、视频）的权限控制尚未探讨。

（完）
