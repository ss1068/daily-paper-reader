---
title: On Optimizing Multimodal Jailbreaks for Spoken Language Models
title_zh: 关于优化语音语言模型多模态越狱攻击的研究
authors: "Aravind Krishnan, Karolina Stańczak, Dietrich Klakow"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19127v1"
tags: ["query:llm"]
score: 10.0
evidence: 语音语言模型的多模态越狱
tldr: 语音语言模型（SLM）结合了语音和文本，但也继承了安全漏洞。本文提出JAMA（联合音频-文本多模态攻击），通过结合GCG和PGD算法同时优化两种模态。实验表明，JAMA在四种主流SLM上的攻击成功率比单模态攻击高出1.5到10倍，且通过序列近似方法显著提升了攻击速度，揭示了单模态防御在多模态环境下的局限性。
selection_source: fresh_fetch
motivation: 现有的针对语音语言模型的越狱攻击大多局限于单模态，忽略了多模态联合优化可能带来的更大安全威胁。
method: 提出JAMA框架，通过结合针对文本的GCG算法和针对音频的PGD算法，实现对两种模态的同步梯度优化。
result: JAMA在四种主流SLM上的越狱成功率比单模态攻击提升了1.5至10倍，且序列近似方法使攻击速度提升了4至6倍。
conclusion: 单模态的安全措施不足以保证语音语言模型的鲁棒性，必须考虑多模态联合攻击带来的风险。
---

## 摘要
随着语音语言模型（SLM）整合了语音和文本模态，它们不仅继承了其大语言模型（LLM）骨干网络的安全性漏洞，还面临着更广的攻击面。此前的研究已表明，SLM 容易受到越狱攻击，即对抗性提示会诱导模型产生有害响应。然而，现有的攻击大多仍局限于单模态，即孤立地优化文本或音频。我们通过引入 JAMA（联合音频-文本多模态攻击）来探索基于梯度的多模态越狱。JAMA 是一个联合多模态优化框架，结合了用于文本的贪婪坐标梯度（GCG）和用于音频的投影梯度下降（PGD），以同时对两种模态施加扰动。在四种最先进的 SLM 和四种音频类型上的评估表明，JAMA 的越狱率比单模态攻击高出 1.5 倍到 10 倍。我们分析了这种联合攻击的运行动态，并展示了一种顺序近似方法可使其速度提高 4 到 6 倍。我们的研究结果表明，单模态安全性不足以保证 SLM 的鲁棒性。代码和数据可在 https://repos.lsv.uni-saarland.de/akrishnan/multimodal-jailbreak-slm 获取。

## Abstract
As Spoken Language Models (SLMs) integrate speech and text modalities, they inherit the safety vulnerabilities of their LLM backbone and an expanded attack surface. SLMs have been previously shown to be susceptible to jailbreaking, where adversarial prompts induce harmful responses. Yet existing attacks largely remain unimodal, optimizing either text or audio in isolation. We explore gradient-based multimodal jailbreaks by introducing JAMA (Joint Audio-text Multimodal Attack), a joint multimodal optimization framework combining Greedy Coordinate Gradient (GCG) for text and Projected Gradient Descent (PGD) for audio, to simultaneously perturb both modalities. Evaluations across four state-of-the-art SLMs and four audio types demonstrate that JAMA surpasses unimodal jailbreak rate by 1.5x to 10x. We analyze the operational dynamics of this joint attack and show that a sequential approximation method makes it 4x to 6x faster. Our findings suggest that unimodal safety is insufficient for robust SLMs. The code and data are available at https://repos.lsv.uni-saarland.de/akrishnan/multimodal-jailbreak-slm

---

## 论文详细总结（自动生成）

以下是对论文《On Optimizing Multimodal Jailbreaks for Spoken Language Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：语音语言模型（SLM）通过整合语音和文本模态，获得了强大的端到端处理能力，但也继承了 LLM 的安全漏洞并新增了音频攻击面。
*   **核心问题**：现有的越狱攻击（Jailbreak）大多是单模态的（仅优化文本或仅优化音频）。作者质疑：**仅具备单模态鲁棒性的模型，在面对多模态联合优化攻击时是否依然安全？**
*   **研究背景**：SLM 对声学变化（如噪声、重音、语速）敏感，且已有研究证明音频模态是 SLM 的“阿喀琉斯之踵”。

### 2. 方法论：JAMA 与 SAMA
*   **JAMA (Joint Audio-text Multimodal Attack)**：
    *   **核心思想**：同时对文本后缀和音频信号进行梯度优化，以最大化模型输出肯定回答（如“Sure, here is...”）的概率。
    *   **技术细节**：
        *   **文本端**：采用 **GCG (Greedy Coordinate Gradient)** 算法，在恶意查询后添加 $N$ 个可优化的离散后缀 Token。
        *   **音频端**：采用 **PGD (Projected Gradient Descent)** 算法，在原始音频 $x$ 上添加微小的连续扰动 $\delta$，并使用 $\epsilon$ 进行裁剪以保证感知不可察觉。
        *   **联合优化**：在每一步迭代中，同时计算文本和音频的梯度，并在音频扰动后的基础上评估文本候选集。
*   **SAMA (Sequential Audio-text Multimodal Attack)**：
    *   **核心思想**：基于 JAMA 训练动态的观察（文本梯度在初期占主导），提出先优化文本后缀、再固定文本优化音频扰动的顺序攻击方法。
    *   **目的**：解决 JAMA 计算开销过大的问题。

### 3. 实验设计
*   **数据集**：使用 **AdvBench** 越狱基准，包含 480 个测试样本。
*   **评估模型**：四种最先进的语音语言模型：**Audio Flamingo 3**、**Qwen2 Audio (7B)**、**Gemma 3N (E2B)**、**Qwen2.5 Omni (7B)**。
*   **音频场景**：四种基础音频：有声读物（男声）、Switchboard 通话记录（男/女声）、音乐表演。
*   **对比基准 (Baselines)**：
    *   仅 GCG（单模态文本攻击）。
    *   仅 PGD（单模态音频攻击）。
*   **评估指标**：字符串匹配（String Matching）和 **LLaMA Guard 3** 自动安全评估。

### 4. 资源与算力
*   **硬件环境**：实验在单个 **NVIDIA H100 80GB GPU** 节点上运行。
*   **计算效率**：
    *   JAMA 由于需要在每个 GCG 步骤中处理音频上下文，计算成本极高。
    *   **SAMA 优化**：相比 JAMA，SAMA 实现了 **4倍至6倍** 的加速（例如在处理 8 个 Token 和 4 秒音频时，耗时显著降低）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种模型 × 4 种音频类型 × 多种 Token 长度（0-64） × 多种音频长度（0-8s）。
*   **重复性**：所有实验均跨 **5 个随机种子** 运行并报告标准差。
*   **充分性评价**：实验设计非常充分。作者不仅对比了成功率，还进行了**训练动态分析**（梯度能量分布）、**表征空间分析**（t-SNE 可视化）和**线性分类器实验**，从底层原理解释了多模态攻击为何更有效。

### 6. 主要结论与发现
*   **联合攻击威力巨大**：JAMA 的越狱成功率在所有模型上均显著超过单模态攻击，提升幅度达 **1.5x 至 10x**。
*   **模态互补性**：文本和音频扰动在模型的决策边界上作用于不同的子空间。即使单模态攻击失败，两者的结合也能诱导模型违规。
*   **模型差异**：Qwen 系列对文本攻击较敏感，而 **Gemma 3N** 表现出极强的文本鲁棒性，但在 JAMA（尤其是音乐背景）攻击下仍会崩溃。
*   **顺序近似可行**：SAMA 在大幅降低算力需求的同时，能达到与 JAMA 相当的攻击效果，尤其在长 Token 场景下。

### 7. 优点与亮点
*   **首创性**：首次系统性地研究了针对 SLM 的**联合梯度优化**多模态攻击。
*   **深度分析**：通过 t-SNE 和 PCA 证明了越狱攻击在嵌入空间中形成了远离正常边界的独立子空间。
*   **实用性**：提出的 SAMA 方法为安全研究人员提供了一个高效的红队测试工具。

### 8. 不足与局限
*   **白盒限制**：该攻击属于白盒攻击，需要访问模型的梯度。在黑盒（仅 API 访问）场景下的有效性尚未验证。
*   **架构依赖**：攻击依赖于可微分的音频特征提取器。对于某些使用了不可微预处理（如某些非 Torch 实现的特征提取）的模型，需要重写代码才能实施。
*   **防御缺失**：论文重点在于揭示漏洞，虽然呼吁加强多模态护栏，但未提出具体的防御算法（如多模态对抗训练）。

（完）
