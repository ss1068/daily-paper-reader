---
title: "Prompt Control-Flow Integrity: A Priority-Aware Runtime Defense Against Prompt Injection in LLM Systems"
title_zh: 提示词控制流完整性：一种针对大语言模型系统中提示词注入的优先级感知运行时防御
authors: "Md Takrim Ul Alam, Akif Islam, Mohd Ruhul Ameen, Abu Saleh Musa Miah, Jungpil Shin"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18433v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对提示词注入和越狱检测的运行时防御
tldr: "针对大语言模型（LLM）易受提示词注入攻击的问题，本文提出了一种名为提示词控制流完整性（PCFI）的运行时防御机制。PCFI 将请求建模为具有优先级的结构化片段（如系统、用户、检索文档等），通过三阶段中间件流水线（词法启发式、角色切换检测和分层策略执行）进行拦截。实验表明，PCFI 在保持极低延迟（0.04ms）的同时，实现了 100% 的攻击拦截率和 0% 的误报率，为 LLM 系统提供了一种高效且实用的防御方案。"
selection_source: fresh_fetch
motivation: 现有的提示词注入防御方法通常将提示词视为扁平字符串，缺乏对不同来源指令优先级的结构化处理。
method: 提出 PCFI 机制，通过将请求划分为不同角色的结构化片段，并利用三阶段中间件流水线实施优先级感知的策略强制执行。
result: 在基准测试中，PCFI 成功拦截了所有攻击请求，误报率为零，且处理开销仅为 0.04 毫秒。
conclusion: 基于来源和优先级的提示词强制执行是防御 LLM 提示词注入的一种轻量级且极其有效的实践方法。
---

## 摘要
部署在 API 和检索增强生成 (RAG) 技术栈之后的大语言模型 (LLM) 容易受到提示词注入攻击，这些攻击可能会覆盖系统策略、破坏预期行为并诱导产生不安全的输出。现有的防御措施通常将提示词视为扁平字符串，并依赖于临时过滤或静态越狱检测。本文提出了提示词控制流完整性 (PCFI)，这是一种优先级感知的运行时防御方法，它将每个请求建模为系统、开发者、用户和检索文档片段的结构化组合。在将请求转发给后端 LLM 之前，PCFI 应用了三阶段中间件流水线、词法启发式、角色切换检测和分层策略执行。我们将 PCFI 实现为基于 FastAPI 的网关，用于已部署的 LLM API，并在包含合成和半真实提示词注入工作负载的自定义基准测试上对其进行了评估。在评估的基准测试套件中，PCFI 拦截了所有标记为攻击的请求，保持了 0% 的误报率，并且引入的中值处理开销仅为 0.04 毫秒。这些结果表明，来源感知和优先级感知的提示词强制执行是针对已部署 LLM 系统的一种实用且轻量级的防御方案。

## Abstract
Large language models (LLMs) deployed behind APIs and retrieval-augmented generation (RAG) stacks are vulnerable to prompt injection attacks that may override system policies, subvert intended behavior, and induce unsafe outputs. Existing defenses often treat prompts as flat strings and rely on ad hoc filtering or static jailbreak detection. This paper proposes Prompt Control-Flow Integrity (PCFI), a priority-aware runtime defense that models each request as a structured composition of system, developer, user, and retrieved-document segments. PCFI applies a three-stage middleware pipeline, lexical heuristics, role-switch detection, and hierarchical policy enforcement, before forwarding requests to the backend LLM. We implement PCFI as a FastAPI-based gateway for deployed LLM APIs and evaluate it on a custom benchmark of synthetic and semi-realistic prompt-injection workloads. On the evaluated benchmark suite, PCFI intercepts all attack-labeled requests, maintains a 0% False Positive Rate, and introduces a median processing overhead of only 0.04 ms. These results suggest that provenance- and priority-aware prompt enforcement is a practical and lightweight defense for deployed LLM systems.

---

## 论文详细总结（自动生成）

### 论文总结：Prompt Control-Flow Integrity (PCFI)

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）广泛应用于检索增强生成（RAG）和 API 服务，系统面临严重的**提示词注入（Prompt Injection）**风险。攻击者通过在用户输入或检索到的第三方文档中嵌入恶意指令，试图覆盖系统策略或诱导模型产生不安全输出。
*   **研究动机**：现有的防御手段（如静态过滤或简单的护栏框架）通常将提示词视为“扁平字符串”，忽略了指令来源的优先级差异。
*   **核心思想**：借鉴软件安全中的**控制流完整性（CFI）**概念，提出 **PCFI** 框架。该框架认为提示词应具有结构化的“权威等级”，低优先级的片段（如检索文档）不应被允许重新定义高优先级的指令（如系统策略）。

#### 2. 方法论：核心思想与技术细节
PCFI 被设计为一个部署在 API 边界的轻量级运行时中间件，其核心流程如下：
*   **结构化表示**：将请求建模为具有来源标签和优先级的片段序列。优先级顺序为：**系统提示词 (S) > 开发者模板 (D) > 用户输入 (U) > 检索内容 (R)**。
*   **三阶段防御流水线**：
    1.  **词法启发式（Lexical Heuristics）**：扫描用户和检索片段，识别明显的注入信号（如 "ignore previous instructions"）。
    2.  **角色切换检测（Role-Switch Detection）**：检测低优先级片段中是否包含伪造的角色标记（如 `system:` 或 `{"role": "system"}`），并进行清理（SANITIZE）。
    3.  **分层策略执行（Hierarchical Policy Enforcement）**：基于预定义的规则库，检查低优先级片段（U, R）是否包含试图覆盖高优先级（S, D）策略的指令模式。
*   **决策逻辑**：根据分析结果，中间件决定是**允许（ALLOW）**、**清理（SANITIZE）**还是**阻断（BLOCK）**该请求。

#### 3. 实验设计
*   **数据集/场景**：构建了一个包含 150 个样本的自定义 JSONL 基准测试集。
    *   **良性请求（50个）**：用户和检索内容与系统指令一致。
    *   **直接注入（Direct Injection）**：恶意用户提示词。
    *   **间接注入（Indirect RAG Injection）**：恶意指令隐藏在检索文档中，而用户请求看似正常。
*   **对比方法**：对比了“无防御基线”与“完整 PCFI 流水线”。
*   **评估指标**：攻击通过率（APR）、误报率（FPR）以及处理延迟（Latency）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **原因**：PCFI 是一种基于规则和模式匹配的运行时中间件，不需要对模型进行重新训练或微调。其实现基于 FastAPI，主要消耗的是 CPU 资源进行字符串处理和逻辑判断，而非大规模 GPU 算力。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 150 组实验（50 组良性，100 组攻击）。
*   **充分性评价**：
    *   **客观性**：实验涵盖了直接和间接注入两种主流攻击路径，具有一定的代表性。
    *   **局限性**：样本量（150个）相对较小，且主要基于合成和半真实的场景。虽然在当前测试集上表现完美，但面对更复杂、更具混淆性的真实世界攻击，其实验充分性仍有待进一步验证。

#### 6. 主要结论与发现
*   **高效拦截**：PCFI 在测试集中实现了 **0% 的攻击通过率（APR）**，成功拦截了所有注入尝试。
*   **零误报**：在良性样本测试中，**误报率（FPR）为 0%**，证明了其对正常业务逻辑的干扰极小。
*   **极低开销**：中值处理延迟仅为 **0.04 毫秒**（p99 延迟为 0.14 毫秒），相对于 LLM 的推理时间几乎可以忽略不计。
*   **实用性**：证明了基于“来源与优先级”的结构化防御是解决提示词注入问题的一种实用且轻量级的方向。

#### 7. 优点
*   **架构解耦**：作为中间件运行，无需修改后端模型或重新训练，易于集成到现有 API 网关。
*   **优先级感知**：通过明确指令的权威等级，从结构上解决了低优先级内容篡改高优先级指令的问题。
*   **性能卓越**：亚毫秒级的延迟使其非常适合对实时性要求高的生产环境。

#### 8. 不足与局限
*   **模式依赖**：目前的实现主要依赖模式匹配和词法启发式，可能难以防御经过高度伪装、语义隐晦或多轮对话中的复杂攻击。
*   **场景限制**：目前仅针对单次请求的提示词组合，尚未扩展到多轮对话、持久记忆或复杂的代理（Agent）工作流。
*   **评估范围**：基准测试集规模较小且包含较多合成数据，可能无法完全反映开放互联网环境下的对抗多样性。

（完）
