---
title: A Framework for Formalizing LLM Agent Security
title_zh: 大语言模型（LLM）智能体安全形式化框架
authors: "Vincent Siu, Jingxuan He, Kyle Montgomery, Zhun Wang, Neil Gong, Chenguang Wang, Dawn Song"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19469v1"
tags: ["query:llm"]
score: 8.0
evidence: 系统化LLM智能体攻击与防御的框架
tldr: LLM智能体安全具有高度上下文相关性，现有定义往往忽略这一点，导致防御时面临效用与安全的权衡。本文提出了一个形式化框架，定义了任务对齐、操作对齐、来源授权和数据隔离四个核心安全属性，并引入预言机函数进行验证。该框架重新定义了提示注入、越狱等攻击，为系统化分析和提升LLM智能体安全性提供了理论基础。
selection_source: fresh_fetch
motivation: 现有的LLM智能体安全定义缺乏上下文感知，导致防御措施在效用和安全性之间难以平衡。
method: 提出包含四个核心安全属性的形式化框架，并利用预言机函数验证智能体执行任务时的属性违规情况。
result: 通过该框架重新形式化了提示注入、越狱、任务漂移等多种攻击，并将其定义为对特定安全属性的违反。
conclusion: 该框架为LLM智能体提供了精确且具上下文感知的安全定义，为未来系统化防御研究奠定了基础。
---

## 摘要
LLM 智能体的安全本质上是上下文相关的。例如，智能体采取的同一动作可能代表合法行为或安全违规，这取决于谁的指令导致了该动作、正在追求什么目标，以及该动作是否服务于该目标。然而，现有的针对 LLM 智能体的安全攻击定义往往未能捕捉到这种上下文特性。因此，防御措施面临着根本的效用-安全权衡：在所有上下文中统一应用防御可能导致显著的效用损失，而在不足或不当的上下文中应用防御则可能导致安全漏洞。在这项工作中，我们提出了一个从上下文安全角度系统化现有攻击和防御的框架。为此，我们提出了四个捕捉 LLM 智能体上下文安全的安全属性：任务对齐（追求授权目标）、动作对齐（单个动作服务于这些目标）、来源授权（执行来自经过身份验证的来源的命令）以及数据隔离（确保信息流遵循权限边界）。我们进一步引入了一组预言机函数（oracle functions），以便在智能体执行用户任务时验证这些安全属性是否遭到破坏。利用该框架，我们将现有的攻击（如间接提示注入、直接提示注入、越狱、任务漂移和内存投毒）重新形式化为对一个或多个安全属性的违背，从而为这些攻击提供了精确且具有上下文意义的定义。同样，我们将防御重新形式化为强化预言机函数或执行安全属性检查的机制。最后，我们讨论了由我们的框架开启的几个重要的未来研究方向。

## Abstract
Security in LLM agents is inherently contextual. For example, the same action taken by an agent may represent legitimate behavior or a security violation depending on whose instruction led to the action, what objective is being pursued, and whether the action serves that objective. However, existing definitions of security attacks against LLM agents often fail to capture this contextual nature. As a result, defenses face a fundamental utility-security tradeoff: applying defenses uniformly across all contexts can lead to significant utility loss, while applying defenses in insufficient or inappropriate contexts can result in security vulnerabilities. In this work, we present a framework that systematizes existing attacks and defenses from the perspective of contextual security. To this end, we propose four security properties that capture contextual security for LLM agents: task alignment (pursuing authorized objectives), action alignment (individual actions serving those objectives), source authorization (executing commands from authenticated sources), and data isolation (ensuring information flows respect privilege boundaries). We further introduce a set of oracle functions that enable verification of whether these security properties are violated as an agent executes a user task. Using this framework, we reformalize existing attacks, such as indirect prompt injection, direct prompt injection, jailbreak, task drift, and memory poisoning, as violations of one or more security properties, thereby providing precise and contextual definitions of these attacks. Similarly, we reformalize defenses as mechanisms that strengthen oracle functions or perform security property checks. Finally, we discuss several important future research directions enabled by our framework.

---

## 论文详细总结（自动生成）

### 大语言模型（LLM）智能体安全形式化框架：深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
LLM 智能体在现实环境中的应用日益广泛，但其安全性定义长期处于模糊状态。论文指出，**智能体安全本质上是“上下文相关”的（Contextual）**。
*   **核心矛盾**：同一动作（如“删除文件”）在不同上下文中可能是合法的（用户授权的清理）或恶意的（攻击者诱导的破坏）。
*   **现有缺陷**：目前的攻击定义（如提示注入）往往只关注输入内容，忽略了执行上下文（谁下达的指令、目标是什么、信息流向何处）。这导致防御措施面临“效用-安全”的权衡：要么防御过严损害功能，要么防御不足留下漏洞。
*   **研究目标**：建立一个形式化框架，通过定义执行上下文和安全属性，系统化地重新审视现有的攻击与防御。

#### 2. 论文提出的方法论
论文提出了一个基于**执行上下文（Execution Context）**的形式化框架，核心包含以下三个部分：

*   **执行上下文的形式化定义**：
    在时间步 $t$ 的上下文 $C_t$ 被定义为一个六元组：$C_t = (p, Tr_{t-1}, M_t, E_t, S_{auth,t}, G)$。
    *   $p$: 用户初始提示词。
    *   $Tr_{t-1}$: 历史执行轨迹（动作-观察对）。
    *   $M_t$: 智能体记忆。
    *   $E_t$: 环境状态。
    *   $S_{auth,t}$: 当前已认证的来源集合。
    *   $G$: 来源权限图（定义谁有权访问哪些资源）。

*   **四个核心安全属性**：
    1.  **任务对齐（Task Alignment）**：确保智能体追求的是用户授权且安全的目标（防止越狱和任务漂移）。
    2.  **操作对齐（Action Alignment）**：确保每一个具体动作都服务于已授权的目标（防止能力滥用）。
    3.  **来源授权（Source Authorization）**：确保指令仅来自经过身份验证的信任源（防止间接提示注入）。
    4.  **数据隔离（Data Isolation）**：确保信息流遵循权限边界，防止跨会话或跨权限泄露。

*   **五类预言机函数（Oracle Functions）**：
    为了验证上述属性，框架引入了理想化的预言机：
    *   目标评估函数（$H_p, H_{Tr}, H_a$）：量化提示词、轨迹和动作的目标一致性。
    *   指令归因函数（$I$）：识别导致特定动作的输入指令。
    *   来源归因函数（$L$）：追踪输入信息的来源。

#### 3. 实验设计
本论文属于**知识系统化（SoK）**类型的论文，其“实验”主要体现为对现有研究的深度映射与分类：
*   **分析对象**：论文深入分析了 **87 篇** 相关的学术论文。
*   **重新形式化攻击**：利用该框架重新定义了 9 种主流攻击，包括间接/直接提示注入、越狱、混淆代理（Confused Deputy）、任务漂移、能力滥用、跨上下文泄露、恶意工具利用和内存投毒。
*   **防御评估**：将现有防御措施（如提示工程、模型训练、沙箱隔离、输入/输出过滤）映射到框架中，分析它们分别是在强化哪类预言机函数或检查哪类安全属性。
*   **Benchmark 引用**：引用了 AgentDojo、AgentHarm、PropensityBench、ToolEmu 等现有基准测试的数据来佐证框架的有效性。

#### 4. 资源与算力
*   **算力说明**：由于本文是理论框架与系统化综述，**文中未提及具体的 GPU 型号、数量或训练时长**。其贡献在于理论模型的构建和对已有文献的逻辑推演，而非大规模模型训练。

#### 5. 实验数量与充分性
*   **充分性**：论文通过对 87 篇文献的覆盖，证明了该框架能够涵盖目前已知的所有主要 LLM 智能体安全威胁。
*   **客观性**：通过将模糊的自然语言攻击定义转化为精确的逻辑谓词（Predicate），增强了安全评估的客观性。
*   **局限性**：虽然理论推导充分，但论文未提供一个端到端的自动化验证工具原型，更多是作为指导未来研究的蓝图。

#### 6. 论文的主要结论与发现
*   **安全是相对的**：脱离上下文谈安全没有意义，防御必须感知“谁、为了什么、在什么环境下”操作。
*   **防御缺失**：现有的防御大多是“上下文无关”的（如简单的关键词过滤），这解释了为什么它们容易被绕过或导致高误报。
*   **研究方向**：未来的核心挑战在于如何实现高效的“指令归因（Instruction Attribution）”和“语义目标评估”，即如何让机器准确判断某个动作到底是谁的指令导致的。

#### 7. 优点
*   **理论严密性**：首次为 LLM 智能体安全提供了完整且形式化的数学描述，填补了该领域的理论空白。
*   **统一性**：成功将看似零散的攻击（如越狱和内存投毒）统一在四个属性的违规框架下。
*   **实用指导**：为开发者提供了清晰的防御清单，明确了防御系统需要近似实现哪些预言机功能。

#### 8. 不足与局限
*   **实现难度**：框架依赖的“预言机函数”（如完美识别指令来源）在现实中极难完美实现，目前仍需依赖 LLM 自身进行近似判断。
*   **同步模型限制**：框架主要针对同步、基于回合的智能体执行模型，对于异步或多智能体协作场景的覆盖尚不完全。
*   **计算开销**：在每一步执行时都进行四种属性的实时验证可能会带来显著的推理延迟。

（完）
