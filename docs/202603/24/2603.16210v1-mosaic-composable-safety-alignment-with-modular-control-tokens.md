---
title: "MOSAIC: Composable Safety Alignment with Modular Control Tokens"
title_zh: MOSAIC：基于模块化控制 Token 的可组合安全性对齐
authors: "Jingyu Peng, Hongyu Chen, Jiancheng Dong, Maolin Wang, Wenxi Li, Yuchen Li, Kai Zhang, Xiangyu Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16210v1"
tags: ["query:llm"]
score: 9.0
evidence: 使用控制令牌的大语言模型模块化安全对齐
tldr: 针对大语言模型安全对齐中静态策略难以适应多变场景的问题，本文提出 MOSAIC 框架。该框架通过在冻结的骨干模型上引入可学习的模块化控制令牌，实现了安全约束的可组合性。推理时可根据需求灵活激活不同令牌。实验证明，MOSAIC 在保持模型通用能力的同时，显著降低了过度拒绝率，并提供了强大的防御性能，为复杂场景下的安全部署提供了新思路。
selection_source: fresh_fetch
motivation: 现有的安全对齐方法多为静态且参数耦合，难以满足不同用户和应用场景下对上下文敏感、可灵活配置的安全规则需求。
method: 提出一种基于可学习控制令牌的模块化框架，通过在冻结模型上优化特定令牌并结合分布级对齐目标，实现安全约束的动态组合。
result: 实验表明 MOSAIC 在提供强力安全防御的同时，显著降低了过度拒绝率，并有效保留了模型的通用指令遵循能力。
conclusion: MOSAIC 证明了通过模块化控制令牌可以实现高效、可组合且低副作用的安全对齐，增强了模型在多样化现实环境中的适用性。
---

## 摘要
大语言模型（LLMs）中的安全性对齐通常被实现为嵌入在模型参数中的单一静态策略。然而，现实世界的部署往往需要根据用户、地区和应用而变化的上下文相关安全规则。现有方法难以提供这种条件控制：参数级对齐将安全行为与通用能力纠缠在一起，而基于提示的方法依赖于自然语言指令，其强制执行力较弱。我们提出了 MOSAIC，这是一个模块化框架，通过在冻结的主干模型上优化可学习的控制 Token，实现可组合的安全性对齐。每个 Token 代表一个安全约束，并可以在推理时灵活地激活和组合。为了高效地训练可组合 Token，我们引入了基于顺序的任务采样和一种减轻过度拒绝的分布级对齐目标。实验表明，MOSAIC 在保持模型效用的同时，实现了强大的防御性能，并显著降低了过度拒绝率。

## Abstract
Safety alignment in large language models (LLMs) is commonly implemented as a single static policy embedded in model parameters. However, real-world deployments often require context-dependent safety rules that vary across users, regions, and applications. Existing approaches struggle to provide such conditional control: parameter-level alignment entangles safety behaviors with general capabilities, while prompt-based methods rely on natural language instructions that provide weak enforcement. We propose MOSAIC, a modular framework that enables compositional safety alignment through learnable control tokens optimized over a frozen backbone model. Each token represents a safety constraint and can be flexibly activated and composed at inference time. To train compositional tokens efficiently, we introduce order-based task sampling and a distribution-level alignment objective that mitigates over-refusal. Experiments show that MOSAIC achieves strong defense performance with substantially lower over-refusal while preserving model utility.

---

## 论文详细总结（自动生成）

以下是对论文《MOSAIC: Composable Safety Alignment with Modular Control Tokens》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 安全对齐方法通常是**静态且全量**的（嵌入在模型参数中），难以适应不同用户、地区和应用场景下多变的安全规则（例如：成年人可看的内容对未成年人需屏蔽；某国合法的言论在另一国可能违规）。
*   **研究动机**：
    *   **参数级对齐的局限**：将安全行为与通用能力深度耦合，难以解耦或更新，且重训成本高，易导致“灾难性遗忘”或干扰模型原有性能。
    *   **提示词（Prompt）对齐的局限**：依赖自然语言指令，模型遵循的一致性较弱，且多条规则叠加会显著增加 Token 开销，挤占上下文窗口。
    *   **目标**：实现一种**模块化、可组合、非侵入式**的安全对齐框架，允许在推理时动态激活或组合特定的安全约束。

### 2. 方法论：MOSAIC 框架
MOSAIC（可组合安全对齐模块化控制 Token）的核心思想是将每种安全约束表示为冻结主干模型嵌入空间中的一小组**可学习控制 Token**。

*   **核心思想**：保持主干模型参数（Backbone）完全冻结，仅针对每个安全类别 $c$ 训练 $m$ 个（通常 $m < 10$）控制 Token $z_c$。推理时，根据需要将对应类别的 Token 拼接在输入序列前。
*   **关键技术细节**：
    1.  **组合任务采样（Compositional Task Sampling）**：为了避免类别组合爆炸（$2^K-1$ 种组合），提出**基于阶数（Order-based）**的平衡采样。将任务按激活约束的数量（1阶、2阶...）分组，并为每阶分配固定预算，确保模型在有限数据下学习到 Token 间的协同工作能力。
    2.  **训练目标函数**：
        *   **正样本（需拒绝）**：使用标准交叉熵损失（$L_{ref}$），强制模型在检测到对应安全 Token 时输出拒绝模板。
        *   **负样本（良性查询）**：引入**反事实知识蒸馏（Counterfactual KD）**。通过对比“带控制 Token”和“不带控制 Token”的输出分布，最小化两者间的 KL 散度。这确保了控制 Token 仅在必要时干预，从而大幅降低过度拒绝（Over-refusal）。
    3.  **增量扩展**：由于主干模型冻结且 Token 独立，可以随时训练并加入新的安全类别 Token，而无需重新训练已有模块。

### 3. 实验设计
*   **数据集与场景**：构建了一个包含 3,000 条指令的新基准数据集，涵盖 5 个特定安全类别（成瘾、酒精、博彩、恐怖、性相关）。这些请求在主流已对齐模型（如 Llama-3）中默认是不被拒绝的，但在特定场景（如青少年模式）下需要拦截。
*   **对比方法（Baselines）**：
    *   **In-context**：基于提示词的系统指令方法。
    *   **SFT (LoRA)**：基于 LoRA 的监督微调。
    *   **ORPO (LoRA)**：一种参数高效的偏好优化方法。
*   **评估指标**：防御成功率（DSR）、过度拒绝率（OR）以及通用能力评估（MMLU）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到使用 Adam 优化器，学习率为 0.01，训练 8 个 Epoch。由于主干模型（8B 和 3B）是冻结的，且可训练参数极少（每个类别仅几千个参数），推测其训练资源消耗远低于全量微调或 LoRA。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Llama-3.1-8B 和 Llama-3.2-3B 两个不同规模的模型上进行了验证。
    *   涵盖了从 1 阶到 4 阶的多种组合测试。
    *   进行了详细的**消融实验**（验证多任务训练和 KLD 损失的作用）。
    *   进行了**参数敏感性分析**（Token 数量、$\lambda$ 权重）和**增量扩展实验**。
*   **充分性评价**：实验设计较为全面，特别是针对“过度拒绝”和“组合有效性”的分析非常深入。通过 GPT-5 作为评判员（LLM-as-Judge）增加了评估的客观性。

### 6. 主要结论与发现
*   **高效防御**：MOSAIC 在 DSR 指标上接近 100%，显著优于 In-context 和 ORPO，与 SFT 持平。
*   **极低副作用**：相比 SFT，MOSAIC 的过度拒绝率（OR）大幅降低（在 8B 模型上低至 1.8%），且对 MMLU 等通用能力几乎无损。
*   **组合性强**：通过多任务联合优化，多个类别的控制 Token 可以同时激活且互不干扰，解决了独立训练 Token 时的“主导效应”问题。
*   **增量友好**：添加新安全类别时，性能保持稳定，证明了其在动态环境下的扩展能力。

### 7. 优点与亮点
*   **架构优雅**：通过极少量的 Embedding 参数实现了复杂的条件控制，实现了安全对齐的“即插即用”。
*   **反事实蒸馏**：巧妙地利用冻结模型自身作为教师，解决了控制 Token 容易导致模型变得过于保守（过度拒绝）的顽疾。
*   **现实意义**：构建的基准数据集更贴近真实部署需求（针对特定人群的细粒度对齐），而非通用的恶意攻击防御。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 8B 及以下规模的模型，尚未在超大规模模型（如 70B+）上验证其有效性。
*   **数据规模**：由于采用了高质量的人工/模型混合过滤，数据集规模（3,000 条）相对较小，可能无法覆盖所有长尾的安全边界案例。
*   **硬件细节缺失**：未提供具体的训练开销和推理延迟数据（虽然理论上 Token 增加带来的延迟微乎其微）。
*   **应用限制**：该方法依赖于推理时能够准确识别并注入对应的控制 Token，如果上游的“场景识别”出错，则防御失效。

（完）
