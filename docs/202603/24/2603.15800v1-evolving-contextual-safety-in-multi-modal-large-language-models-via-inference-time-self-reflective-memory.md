---
title: Evolving Contextual Safety in Multi-Modal Large Language Models via Inference-Time Self-Reflective Memory
title_zh: 通过推理时自反思记忆演进多模态大语言模型中的上下文安全性
authors: "Ce Zhang, Jinxi He, Junyi He, Katia Sycara, Yaqi Xie"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15800v1"
tags: ["query:llm"]
score: 9.0
evidence: 多模态大模型中的越狱防御与上下文安全
tldr: 本研究针对多模态大语言模型（MLLM）在处理细微上下文差异时的安全性漏洞，提出了MM-SafetyBench++基准测试，通过对比相似但意图相反的安全与不安全场景来评估模型的上下文安全能力。同时，提出了一种无需训练的框架EchoSafe，利用自反思记忆库积累并检索过往交互中的安全见解，在推理阶段实现上下文感知推理和安全行为的持续演进。实验证明EchoSafe在多个安全基准上表现优异。
selection_source: fresh_fetch
motivation: 现有的多模态模型安全研究多关注显式违规，忽视了模型在区分看似相似但安全意图截然不同的上下文场景时的脆弱性。
method: 提出了包含对比样本的基准测试MM-SafetyBench++，以及通过自反思记忆库在推理时动态检索并整合安全经验的EchoSafe框架。
result: 实验结果显示，EchoSafe在多个多模态安全基准测试中均取得了领先的性能，显著提升了模型在复杂上下文中的安全判断力。
conclusion: 该研究通过引入上下文安全评估和推理时自反思记忆机制，为提升多模态大语言模型的鲁棒性和安全性提供了新的有效路径。
---

## 摘要
多模态大语言模型（MLLMs）在广泛的视觉推理任务中取得了卓越的性能，但其对安全风险的脆弱性仍然是一个紧迫的问题。虽然先前的研究主要集中在检测和拒绝显式不安全输入的越狱防御上，但这些方法往往忽略了上下文安全性，这要求模型能够区分在外观上相似但在安全意图上存在显著差异的场景之间的细微上下文区别。在这项工作中，我们提出了 MM-SafetyBench++，这是一个专门为上下文安全性评估而精心设计的基准测试。具体而言，对于每个不安全的图文对，我们通过最小程度的修改构建了一个对应的安全副本，在保留底层上下文含义的同时翻转了用户意图，从而能够受控地评估模型是否可以根据上下文理解来调整其安全行为。此外，我们引入了 EchoSafe，这是一个无需训练的框架，它维护一个自反思记忆库，用于积累和检索先前交互中的安全见解。通过将相关的过去经验整合到当前的提示中，EchoSafe 实现了上下文感知推理以及推理过程中安全行为的持续演进。在各种多模态安全基准上的广泛实验表明，EchoSafe 始终如一地实现了卓越的性能，为推进 MLLMs 的上下文安全性奠定了坚实的基础。所有基准数据和代码均可在 https://echosafe-mllm.github.io 获取。

## Abstract
Multi-modal Large Language Models (MLLMs) have achieved remarkable performance across a wide range of visual reasoning tasks, yet their vulnerability to safety risks remains a pressing concern. While prior research primarily focuses on jailbreak defenses that detect and refuse explicitly unsafe inputs, such approaches often overlook contextual safety, which requires models to distinguish subtle contextual differences between scenarios that may appear similar but diverge significantly in safety intent. In this work, we present MM-SafetyBench++, a carefully curated benchmark designed for contextual safety evaluation. Specifically, for each unsafe image-text pair, we construct a corresponding safe counterpart through minimal modifications that flip the user intent while preserving the underlying contextual meaning, enabling controlled evaluation of whether models can adapt their safety behaviors based on contextual understanding. Further, we introduce EchoSafe, a training-free framework that maintains a self-reflective memory bank to accumulate and retrieve safety insights from prior interactions. By integrating relevant past experiences into current prompts, EchoSafe enables context-aware reasoning and continual evolution of safety behavior during inference. Extensive experiments on various multi-modal safety benchmarks demonstrate that EchoSafe consistently achieves superior performance, establishing a strong baseline for advancing contextual safety in MLLMs. All benchmark data and code are available at https://echosafe-mllm.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《Evolving Contextual Safety in Multi-Modal Large Language Models via Inference-Time Self-Reflective Memory》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于多模态大语言模型（MLLM）的**上下文安全性（Contextual Safety）**。
*   **研究动机**：现有的安全研究多关注“越狱攻击”的防御（即拒绝显式有害指令），但忽略了模型在处理视觉上下文时的判断力。例如，同样是“如何使用这把刀”，在厨房场景下是合法的烹饪请求，而在暴力冲突场景下则是有害的。
*   **核心挑战**：现有防御方法往往导致“过度防御”（Over-defensiveness），即模型为了安全而拒绝一切包含敏感词或物体的良性请求，损害了工具的实用性。此外，现有基准测试（Benchmark）难度较低且缺乏对上下文细微差别的评估。

### 2. 方法论：核心思想与技术细节
论文提出了两个核心贡献：**MM-SafetyBench++**（基准测试）和 **EchoSafe**（防御框架）。

#### MM-SafetyBench++ 基准测试
*   **硬负采样（Hard-Negative Queries）**：针对每个不安全样本，通过最小化修改构建一个意图相反但上下文语义相似的安全副本（例如：将“逃税技巧”改为“合法避税策略”）。
*   **高质量图像合成**：利用 Qwen-Image 生成高保真图像，并设计了 GEN（视觉意图）和 GEN OCR（嵌入文本攻击）两种模式，显著提升了攻击成功率（ASR 从 22% 升至 41%）。

#### EchoSafe 防御框架
这是一种**无需训练（Training-free）**的推理时学习框架，核心流程如下：
1.  **记忆检索（Retrieval）**：在推理步骤 $t$，根据当前查询的图文嵌入（Embedding），从记忆库 $M^{(t-1)}$ 中检索前 $k$ 个最相关的安全见解（Safety Insights）。
2.  **上下文感知推理**：将检索到的见解作为提示词（Prompt）的一部分输入模型，引导其进行安全推理并生成响应。
3.  **自反思（Self-Reflection）**：模型对当前的交互进行反思，总结出一条通用的安全原则 $I^{(t)}$。
4.  **记忆更新（Update）**：将新的安全见解及其上下文嵌入存入记忆库，实现安全能力的持续演进。

### 3. 实验设计
*   **评估模型**：LLaVA-1.5-7B/13B、LLaVA-NeXT-7B、Qwen-2.5-VL-7B/32B、InternVL3.5-8B，以及 GPT-5 等闭源模型。
*   **安全基准测试**：MM-SafetyBench++（本文提出）、MM-SafetyBench、MSSBench、SIUO。
*   **通用能力基准测试**：MME、MMBench、ScienceQA、TextVQA（用于评估防御是否损害了模型的通用性能）。
*   **对比方法**：FigStep、ECSO、AdaShield 等 SOTA 推理时防御方法。
*   **评估指标**：拒绝率（RR）、回答率（AR）、质量得分（QS，0-5分制）以及两者的调和平均值（CCR）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA RTX 6000 Ada (48 GB)** GPU。
*   **算力开销**：EchoSafe 引入了约 **1.33倍的推理延迟**和 **1.69倍的总 FLOPs** 消耗。由于是无需训练的推理时方法，不涉及大规模预训练或微调的算力统计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 大安全类别（非法活动、仇恨言论、恶意软件、身体伤害、欺诈、性内容）。
*   **充分性**：
    *   进行了跨模型的广泛测试（从 7B 到 32B，从开源到闭源）。
    *   包含详尽的**消融实验**：测试了不同 Embedding 模型、记忆提取数量 $k$、检索策略（相似度 vs 随机）以及记忆存储格式的影响。
    *   进行了**持续学习实验**：验证了记忆随时间积累对性能的提升。
    *   引入了 **GPT-5-mini 作为自动评测员**，并通过与人类标注及其他模型（Gemini）的一致性分析验证了评测的客观性。

### 6. 主要结论与发现
*   **EchoSafe 达到 SOTA**：在所有安全基准测试中，EchoSafe 显著优于现有防御方法，尤其是在平衡“安全性”与“实用性”方面。
*   **防御-实用性权衡**：传统的微调方法（如 LoRA）虽然能达到近 100% 的拒绝率，但会导致模型在良性请求上的回答率降至接近 0；而 EchoSafe 在保持高拒绝率的同时，几乎不损害良性回答的质量。
*   **记忆的演进性**：随着交互次数增加，记忆库积累的见解越多，模型的上下文安全判断力越强，且这种能力具有跨领域的迁移性。

### 7. 优点（亮点）
*   **创新的评估视角**：首次系统性地定义并评估了 MLLM 的“上下文安全性”，填补了现有安全研究的空白。
*   **无需训练且即插即用**：EchoSafe 不需要昂贵的微调过程，通过推理时的自反思机制即可实现安全能力的动态提升。
*   **高质量数据集**：MM-SafetyBench++ 提供的配对样本为未来的安全研究提供了更严苛、更科学的测试床。

### 8. 不足与局限
*   **推理延迟**：虽然比微调更轻量，但检索和自反思步骤依然增加了推理时间，对于实时性要求极高的应用可能存在限制。
*   **合成数据的偏差**：基准测试中的图像主要由 AI 生成，可能无法完全覆盖真实世界中复杂、模糊或具有特定文化背景的风险场景。
*   **长程记忆管理**：随着记忆库无限增长，检索效率和噪声干扰可能会成为潜在问题（文中虽有初步分析，但未讨论极大规模记忆库的情况）。

（完）
