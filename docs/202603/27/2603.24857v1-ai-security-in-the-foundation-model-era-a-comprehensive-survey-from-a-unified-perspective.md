---
title: "AI Security in the Foundation Model Era: A Comprehensive Survey from a Unified Perspective"
title_zh: 基础模型时代的 AI 安全：统一视角下的全面综述
authors: "Zhenyi Wang, Siyu Luan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24857v1"
tags: ["query:llm"]
score: 9.0
evidence: 大模型时代AI安全与漏洞的全面综述
tldr: 随着基础模型规模扩大，AI安全威胁日益复杂且碎片化。本文针对数据与模型间相互依存的特性，提出了一个统一的闭环威胁分类框架。该框架从四个维度（D→D、D→M、M→D、M→M）系统地分析了数据解密、投毒攻击、隐私提取及模型窃取等安全风险，为构建可扩展且跨模态的防御策略奠定了理论基础。
selection_source: fresh_fetch
motivation: 现有的AI安全研究大多孤立处理各类威胁，缺乏一个统一框架来揭示数据与模型之间相互依存的安全风险。
method: "提出一种统一的闭环威胁分类法，通过数据与模型交互的四个方向轴（D→D, D→M, M→D, M→M）来系统化分析安全威胁。"
result: 该框架成功将碎片化的攻击手段归纳为四类互相关联的威胁，阐明了风险在机器学习流水线中的传播机制。
conclusion: 该研究为基础模型时代开发可扩展、可迁移且跨模态的系统性安全防御策略提供了坚实的理论支撑。
---

## 摘要
随着机器学习（ML）系统在规模和功能上的不断扩展，安全格局变得日益复杂，攻击与防御手段层出不穷。然而，现有研究在很大程度上孤立地对待这些威胁，缺乏一个连贯的框架来揭示其共同原理和相互依赖关系。这种碎片化的视角阻碍了系统性的理解，并限制了全面防御方案的设计。至关重要的是，机器学习的两大核心资产——数据和模型——不再是相互独立的；其中一方的漏洞会直接危及另一方。缺乏整体框架使得关于这些双向风险如何在机器学习流水线中传播的问题悬而未决。为了填补这一关键空白，我们提出了一个统一的闭环威胁分类法，明确地沿着四个方向轴构建了模型与数据之间的交互框架。我们的框架为分析和防御基础模型提供了一个基于原则的视角。由此产生的四类安全威胁代表了不同但相互关联的攻击类别：（1）数据到数据（D→D）：包括数据解密攻击和水印去除攻击；（2）数据到模型（D→M）：包括投毒攻击、有害微调攻击和越狱攻击；（3）模型到数据（M→D）：包括模型逆向攻击、成员推理攻击和训练数据提取攻击；（4）模型到模型（M→M）：包括模型提取攻击。我们的统一框架阐明了这些安全威胁之间的内在联系，并为开发可扩展、可迁移且跨模态的安全策略奠定了基础，特别是在基础模型的背景下。

## Abstract
As machine learning (ML) systems expand in both scale and functionality, the security landscape has become increasingly complex, with a proliferation of attacks and defenses. However, existing studies largely treat these threats in isolation, lacking a coherent framework to expose their shared principles and interdependencies. This fragmented view hinders systematic understanding and limits the design of comprehensive defenses. Crucially, the two foundational assets of ML -- \textbf{data} and \textbf{models} -- are no longer independent; vulnerabilities in one directly compromise the other. The absence of a holistic framework leaves open questions about how these bidirectional risks propagate across the ML pipeline. To address this critical gap, we propose a \emph{unified closed-loop threat taxonomy} that explicitly frames model-data interactions along four directional axes. Our framework offers a principled lens for analyzing and defending foundation models. The resulting four classes of security threats represent distinct but interrelated categories of attacks: (1) Data$\rightarrow$Data (D$\rightarrow$D): including \emph{data decryption attacks and watermark removal attacks}; (2) Data$\rightarrow$Model (D$\rightarrow$M): including \emph{poisoning, harmful fine-tuning attacks, and jailbreak attacks}; (3) Model$\rightarrow$Data (M$\rightarrow$D): including \emph{model inversion, membership inference attacks, and training data extraction attacks}; (4) Model$\rightarrow$Model (M$\rightarrow$M): including \emph{model extraction attacks}. Our unified framework elucidates the underlying connections among these security threats and establishes a foundation for developing scalable, transferable, and cross-modal security strategies, particularly within the landscape of foundation models.

---

## 论文详细总结（自动生成）

这篇论文《AI Security in the Foundation Model Era: A Comprehensive Survey from a Unified Perspective》对基础模型时代的AI安全进行了系统性的梳理。以下是详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的机器学习（ML）安全研究呈现碎片化状态，各种攻击（如投毒、越狱、提取）和防御手段往往被孤立研究，缺乏统一的理论框架。
*   **核心背景**：在基础模型（Foundation Models）时代，数据（Data）与模型（Model）的界限日益模糊且高度互依。数据的漏洞会直接危及模型，反之亦然。
*   **整体含义**：论文旨在提出一个**统一的闭环威胁分类法**，通过分析数据与模型之间的双向风险传播，为构建系统性、跨模态的防御策略提供指导。

### 2. 方法论
论文提出了基于四个方向轴的闭环分类框架：
*   **Data → Data (D→D)**：保护绕过攻击。核心是直接操纵数据内容以绕过所有权约束，如**数据解密**（无密钥恢复明文）和**水印去除**（消除身份标识但保留数据效用）。
*   **Data → Model (D→M)**：模型操纵攻击。通过恶意数据干预学习过程，包括**数据投毒**（注入恶意样本）、**有害微调**（利用少量样本破坏安全对齐）和**越狱攻击**（通过提示词诱导模型输出违规内容）。
*   **Model → Data (M→D)**：隐私违规攻击。利用模型输出推断训练数据信息，包括**模型逆向**（重建输入特征）、**成员推理**（判断样本是否在训练集中）和**训练数据提取**（诱导模型直接输出记忆的原始片段）。
*   **Model → Model (M→M)**：知识窃取攻击。通过查询API克隆模型功能或架构，即**模型提取攻击**。
*   **数学形式化**：论文为每类攻击提供了统一的数学描述（通常基于双层优化或重构损失函数），阐明了攻击者在满足隐蔽性约束下最大化攻击成功率的逻辑。

### 3. 实验设计
论文不仅综述了现有文献，还进行了实证评估和案例研究：
*   **数据集**：涵盖视觉（MNIST, CIFAR-10, MedMNIST/PathMNIST）、语言（WikiMIA, RoleBench, AdvBench, BeaverTails, HEx-PHI）等。
*   **Benchmark与对比方法**：
    *   在**D→M**中对比了SFT、Vaccine、Lisa、Antidote等防御有害微调的方法。
    *   在**M→D**中对比了PPL、Min-K%、MIA-Tuner等成员推理攻击。
    *   在**M→M**中对比了KnockoffNet、JBDA、ModelGuard、QUEEN等提取与防御手段。
*   **全流程案例研究**：以医疗图像分类（PathMNIST）为例，演示了攻击如何从水印去除（D→D）演进到数据投毒（D→M），再到模型提取（M→M）和最终的数据重构（M→D）。

### 4. 资源与算力
*   **算力说明**：论文在实证部分提到了使用 LLaMA-3-8B-Instruct 和 Gemma-2-9b-it 等模型进行评估。
*   **具体细节**：文中**未明确列出**其进行实证评估所使用的具体硬件型号（如GPU型号、数量）及总训练时长。但在引用部分提到了某些攻击可以在“一分钟GPU时间”内完成，暗示了部分攻击的高效性。

### 5. 实验数量与充分性
*   **实验规模**：论文提供了11个详细的实验表格（Table 6-16），覆盖了从传统视觉任务到最新大语言模型的各类安全场景。
*   **充分性与客观性**：实验设计较为全面，不仅涵盖了不同模态（图像、文本），还考虑了不同的攻击者知识水平（黑盒、白盒）。通过“案例研究”展示了攻击的闭环传播，证明了统一框架的实用性。实验对比了多种最先进的（SOTA）防御与攻击算法，结果较为客观。

### 6. 主要结论与发现
*   **攻击互依性**：安全威胁并非孤立。例如，数据投毒（D→M）会显著增强成员推理（M→D）的成功率；模型提取（M→M）是实现下游数据恢复（M→D）的跳板。
*   **基础模型的脆弱性**：基础模型巨大的参数量增加了“记忆”风险，使其更容易受到数据提取攻击；而开放的微调接口为有害微调提供了便利。
*   **防御的复杂性**：针对单一威胁的防御可能会削弱对另一威胁的抵抗力（如对抗训练可能加剧模型提取风险）。

### 7. 优点
*   **视角独特**：首次提出了闭环（Closed-loop）视角，将碎片化的AI安全研究整合进一个逻辑自洽的框架。
*   **跨模态覆盖**：不仅关注LLM，还兼顾了视觉、图数据等，具有很强的通用性。
*   **实战指导意义**：通过分析攻击链条，揭示了风险如何在系统内部循环，有助于设计“深度防御”体系。

### 8. 不足与局限
*   **防御方案的缺失**：虽然系统总结了攻击，但在如何构建一个能同时应对四种方向威胁的“通用防御模型”上，目前仍处于理论探讨阶段。
*   **动态演化考虑不足**：实验大多基于静态数据集，而现实中的越狱和投毒攻击演化极快，静态Benchmark可能无法完全反映实时对抗的复杂性。
*   **算力成本细节**：缺乏对防御机制部署成本（如推理延迟、算力开销）的量化分析，这在实际工业部署中至关重要。

（完）
