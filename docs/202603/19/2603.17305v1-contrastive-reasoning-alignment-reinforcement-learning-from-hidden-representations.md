---
title: "Contrastive Reasoning Alignment: Reinforcement Learning from Hidden Representations"
title_zh: 对比推理对齐：基于隐藏表示的强化学习
authors: "Haozheng Luo, Yimin Wang, Jiahao Yu, Binghui Wang, Yan Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17305v1"
tags: ["query:llm"]
score: 10.0
evidence: 提高对抗越狱攻击鲁棒性的红队对齐框架。
tldr: 本研究提出CRAFT对齐框架，旨在通过大模型的推理能力和隐藏层表示来增强其抵御越狱攻击的鲁棒性。与以往仅关注输出层面的防御不同，CRAFT将对比表示学习与强化学习（GRPO）相结合，在隐藏空间内优化安全推理轨迹，从而实现深层的推理级安全对齐。实验表明，该方法在推理和响应安全性上均有显著提升，有效解决了模型表层对齐的问题。
selection_source: fresh_fetch
motivation: 现有的安全对齐方法多局限于输出层面，难以有效防御针对复杂推理过程的越狱攻击。
method: CRAFT框架通过在隐藏空间中集成对比学习与强化学习，显式优化推理轨迹的潜在几何分布以分离安全与风险状态。
result: "在Qwen和Llama推理模型上的实验显示，CRAFT在推理安全性和最终响应安全性上分别实现了79.0%和87.7%的显著提升。"
conclusion: 通过隐藏空间的推理对齐，可以有效消除模型的虚假对齐现象，为构建更鲁棒的安全推理模型提供了新路径。
---

## 摘要
我们提出了 CRAFT，这是一个红队对齐框架，利用模型的推理能力和隐藏表示来提高针对越狱攻击的鲁棒性。与以往主要在输出层面运行的防御措施不同，CRAFT 通过显式优化在隐藏状态空间上定义的指标，对齐大型推理模型以生成具有安全意识的推理轨迹。在方法论上，CRAFT 将对比表示学习与强化学习相结合，以区分安全和不安全的推理轨迹，从而产生支持鲁棒的推理级安全对齐的潜空间几何结构。在理论上，我们证明了将潜文本一致性（latent-textual consistency）引入 GRPO 可以通过排除局部最优解来消除表面对齐的策略。在实验上，我们在多个安全基准测试中评估了 CRAFT，使用了两个强大的推理模型 Qwen3-4B-Thinking 和 R1-Distill-Llama-8B，结果显示它始终优于 IPO 和 SafeKey 等最先进的防御方法。值得注意的是，与基础模型相比，CRAFT 在推理安全性和最终响应安全性方面分别平均提升了 79.0% 和 87.7%，证明了隐藏空间推理对齐的有效性。

## Abstract
We propose CRAFT, a red-teaming alignment framework that leverages model reasoning capabilities and hidden representations to improve robustness against jailbreak attacks. Unlike prior defenses that operate primarily at the output level, CRAFT aligns large reasoning models to generate safety-aware reasoning traces by explicitly optimizing objectives defined over the hidden state space. Methodologically, CRAFT integrates contrastive representation learning with reinforcement learning to separate safe and unsafe reasoning trajectories, yielding a latent-space geometry that supports robust, reasoning-level safety alignment. Theoretically, we show that incorporating latent-textual consistency into GRPO eliminates superficially aligned policies by ruling them out as local optima. Empirically, we evaluate CRAFT on multiple safety benchmarks using two strong reasoning models, Qwen3-4B-Thinking and R1-Distill-Llama-8B, where it consistently outperforms state-of-the-art defenses such as IPO and SafeKey. Notably, CRAFT delivers an average 79.0% improvement in reasoning safety and 87.7% improvement in final-response safety over the base models, demonstrating the effectiveness of hidden-space reasoning alignment.

---

## 论文详细总结（自动生成）

以下是对论文《Contrastive Reasoning Alignment: Reinforcement Learning from Hidden Representations》（CRAFT）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型推理模型（LRM）存在**表面安全对齐（Superficial Safety Alignment, SSA）**现象。即模型在面对越狱攻击时，虽然最终输出是安全的拒绝回答，但其内部的推理过程（Thinking Process/CoT）却可能包含有害信息、歧视性逻辑或违规内容。
*   **研究动机**：现有的安全对齐方法（如 DPO、RLHF）大多只关注最终的文本输出，忽略了中间推理轨迹的安全性。随着推理模型推理链条的增长，这种内部风险被放大。论文旨在通过对齐模型的**隐藏层表示（Hidden Representations）**，从根源上确保推理过程与最终输出在安全性上的一致性。

### 2. 方法论：CRAFT 框架
CRAFT 结合了对比学习与强化学习，分为两个主要阶段：
*   **阶段一：推理潜空间对比学习（LCLR）**
    *   **核心思想**：在隐藏空间中对推理轨迹进行几何结构化处理。
    *   **技术细节**：通过投影头将推理 token 的隐藏状态映射到超球面上。利用**结构化几何损失（Lproto）**将“安全”、“不安全”和“重新思考（Rethink）”三种状态在潜空间中拉开距离；利用**实例不变性损失（Linst）**确保语义相近的推理轨迹表示接近；利用**安全校准损失（Lcal）**将潜空间坐标与概率安全评分对齐。
*   **阶段二：基于推理潜变量的强化学习（R2L）**
    *   **核心思想**：利用 GRPO（群体相对策略优化）算法，引入潜空间感知的奖励函数。
    *   **奖励机制**：
        1.  **潜空间语义奖励 ($R_{ls}$)**：根据隐藏状态与安全/不安全原型的余弦相似度给予奖励。
        2.  **文本安全奖励 ($R_{txt}$)**：基于外部评估器对最终输出的安全性评分。
        3.  **潜空间-文本一致性奖励 ($R_{cons}$)**：这是关键创新，通过最小化潜空间预测的安全概率与文本实际安全得分之间的差异，消除 SSA 现象。

### 3. 实验设计
*   **数据集与场景**：
    *   **安全评估**：使用 JailbreakBench 和 StrongReject（包含 PAP、PAIR 等攻击变体）评估越狱鲁棒性。
    *   **推理性能评估**：使用 AIME24、MATH-500、Minerva（数学）和 LiveCodeBench（代码）评估对齐是否损害了模型核心能力。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen3-4B-Thinking 和 DeepSeek-R1-Distill-Llama-8B。
    *   **对比基线**：包括 SFT 类方法（SafeChain、RealSafe）、推理对齐类（STAR、SafeKey）、偏好优化类（IPO）以及安全检测模型（ReasoningShield）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **4 张 NVIDIA H100 (80GB) GPU** 的系统上运行。
*   **处理器**：12 核 Intel Xeon Gold 6338 CPU (2.00 GHz)。
*   **软件框架**：基于 PyTorch 和 Hugging Face Transformers。
*   **训练时长**：文中未明确给出具体的总训练小时数，但提到使用了 2,500 条有害提示词进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主流推理模型家族。
    *   进行了多维度的安全基准测试和四个主流推理性能测试。
    *   包含针对高级攻击（GPTFuzzer, AutoDAN）的鲁棒性分析。
    *   **消融实验**：分别验证了 $R_{cons}$、$R_{ls}$ 和 LCLR 模块的必要性。
*   **充分性与客观性**：实验设计较为充分，所有实验均重复三次并取平均值。通过对比多种 SOTA 方法，证明了 CRAFT 在提升安全性的同时，甚至在某些推理任务上实现了性能微增（约 4.7%），显示了其在安全与性能平衡上的优越性。

### 6. 主要结论与发现
*   **显著提升安全性**：相比基础模型，CRAFT 将推理过程的安全性提升了 **79.0%**，最终响应的安全性提升了 **87.7%**。
*   **消除 SSA**：理论证明并实验确认，引入潜空间-文本一致性奖励可以将表面对齐的策略排除在局部最优解之外。
*   **性能保持**：对齐过程不仅没有导致“安全税”（性能下降），反而通过高质量推理轨迹的暴露提升了模型的通用推理能力。
*   **潜空间规律**：发现安全与不安全的推理轨迹在潜空间中天然存在几何分离，这为基于表示的对齐提供了物理基础。

### 7. 优点（亮点）
*   **深度对齐**：突破了传统的“黑盒”对齐（仅看输出），深入到模型的“思维过程”（隐藏层）进行干预。
*   **理论支撑**：提供了关于如何通过一致性奖励消除 SSA 的数学证明。
*   **高效性**：采用 GRPO 算法，无需额外的 Critic 模型，降低了强化学习的显存开销。
*   **鲁棒性强**：在面对 AutoDAN 等复杂的自动化越狱攻击时表现出极强的防御力。

### 8. 不足与局限
*   **评估器依赖**：安全奖励仍部分依赖于外部评估器（如 GPT-4o），若评估器本身存在偏见或误判，会影响对齐效果。
*   **过拒绝风险**：虽然引入了良性数据缓解，但在极端安全约束下，模型仍可能出现对无害请求的“过度拒绝”。
*   **训练预算限制**：作者提到在某些特定指标上未达第一，可能是受限于当前的训练预算和时长。
*   **静态原型**：潜空间的安全原型（Prototypes）是预先定义的，可能无法完全覆盖所有演进中的新型攻击模式。

（完）
