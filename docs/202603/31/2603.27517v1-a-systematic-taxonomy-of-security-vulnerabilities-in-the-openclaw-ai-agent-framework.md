---
title: A Systematic Taxonomy of Security Vulnerabilities in the OpenClaw AI Agent Framework
title_zh: OpenClaw AI 智能体框架安全漏洞的系统性分类研究
authors: "Surada Suwansathit, Yuxuan Zhang, Guofei Gu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27517v1"
tags: ["query:llm"]
score: 9.0
evidence: AI智能体框架中的提示注入和安全漏洞分类法
tldr: 本研究针对开源AI智能体框架OpenClaw，系统性地分类并分析了190项安全漏洞。通过构建涵盖架构层级和攻击技术的双轴分类法，揭示了从LLM工具调用到宿主机远程代码执行（RCE）的完整攻击路径。研究指出，现有的命令过滤机制存在词法解析漏洞，且插件分发缺乏运行时策略，强调了统一策略边界在防御跨层攻击中的重要性。
selection_source: fresh_fetch
motivation: 旨在解决AI智能体框架在连接大模型推理与宿主执行环境时，所面临的独特且复杂的安全挑战。
method: 提出了一种基于系统架构层级和攻击技术的双轴分类法，对OpenClaw框架中的190项安全建议进行了系统化梳理。
result: 发现了可导致未经身份验证的远程代码执行（RCE）路径，并揭示了命令白名单机制在处理复杂Shell指令时的失效问题。
conclusion: 研究表明，AI智能体框架的主要结构性弱点在于缺乏统一的策略边界，导致跨层攻击难以通过局部修复来根除。
---

## 摘要
将大语言模型 (LLM) 推理连接到宿主执行面（包括 shell、文件系统、容器和消息传递）的 AI 智能体框架，引入了在结构上与传统软件截然不同的安全挑战。我们针对开源 AI 智能体运行时 OpenClaw 提交的 190 条安全公告，按架构层级和信任违规类型进行了系统性分类。漏洞沿两个正交轴聚集：(1) 系统轴，反映架构层级（执行策略、网关、通道、沙箱、浏览器、插件、智能体/提示词）；(2) 攻击轴，反映对抗技术（身份欺骗、策略绕过、跨层组合、提示词注入、供应链提权）。补丁差异证据得出了三项主要发现：首先，网关和节点宿主子系统中的三个中高危公告组合成了一条完整的未授权远程代码执行 (RCE) 路径，涵盖了从 LLM 工具调用到宿主进程的交付、利用和命令控制。其次，作为主要命令过滤机制的执行白名单依赖于一个封闭世界假设，即命令身份可以通过词法解析恢复；然而，这一假设被 shell 换行符、busybox 多路复用和 GNU 选项缩写所失效。第三，通过插件通道分发的恶意技能在 LLM 上下文中执行了一个两阶段释放器 (dropper)，绕过了执行流水线，证明了技能分发面缺乏运行时策略强制执行。主要的结构性弱点是逐层信任强制执行而非统一的策略边界，这使得跨层攻击对局部修复具有免疫力。

## Abstract
AI agent frameworks connecting large language model (LLM) reasoning to host execution surfaces--shell, filesystem, containers, and messaging--introduce security challenges structurally distinct from conventional software. We present a systematic taxonomy of 190 advisories filed against OpenClaw, an open-source AI agent runtime, organized by architectural layer and trust-violation type. Vulnerabilities cluster along two orthogonal axes: (1) the system axis, reflecting the architectural layer (exec policy, gateway, channel, sandbox, browser, plugin, agent/prompt); and (2) the attack axis, reflecting adversarial techniques (identity spoofing, policy bypass, cross-layer composition, prompt injection, supply-chain escalation).   Patch-differential evidence yields three principal findings. First, three Moderate- or High-severity advisories in the Gateway and Node-Host subsystems compose into a complete unauthenticated remote code execution (RCE) path--spanning delivery, exploitation, and command-and-control--from an LLM tool call to the host process. Second, the exec allowlist, the primary command-filtering mechanism, relies on a closed-world assumption that command identity is recoverable via lexical parsing. This is invalidated by shell line continuation, busybox multiplexing, and GNU option abbreviation. Third, a malicious skill distributed via the plugin channel executed a two-stage dropper within the LLM context, bypassing the exec pipeline and demonstrating that the skill distribution surface lacks runtime policy enforcement. The dominant structural weakness is per-layer trust enforcement rather than unified policy boundaries, making cross-layer attacks resilient to local remediation.

---

## 论文详细总结（自动生成）

### 论文总结：OpenClaw AI 智能体框架安全漏洞的系统性分类研究

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）演变为能够自主执行任务的“智能体（Agent）”，它们被赋予了操作 Shell、文件系统、容器和浏览器等敏感环境的权限。这种架构引入了不同于传统软件的安全挑战：攻击者不仅可以利用代码逻辑漏洞，还可以通过**提示注入（Prompt Injection）**等手段操纵模型的推理逻辑，进而触发非预期的系统行为。
本论文以 2026 年初极具代表性的开源框架 **OpenClaw** 为研究对象，旨在通过对 190 项真实安全漏洞（Advisories）的系统性梳理，构建一套通用的 AI 智能体安全分类法，揭示该类框架在架构设计上的深层缺陷。

#### 2. 论文提出的方法论
论文提出了一种**双轴安全分类法（Two-Axis Taxonomy）**，用于结构化地分析 AI 智能体漏洞：
*   **系统轴（System Axis）：** 按照架构层级划分，包括通道输入接口（Channel）、网关（Gateway）、插件与技能（Plugins）、智能体运行时（Agent Runtime）、执行策略引擎（Exec Policy）、沙箱边界（Container）及宿主操作系统接口等 10 个层级。
*   **攻击轴（Attack Axis）：** 按照对抗技术划分，包括身份欺骗、策略绕过、跨层组合攻击、提示注入、供应链信任提升等。
*   **AI 智能体杀伤链（Kill Chain）：** 论文适配了 MITRE ATT&CK 框架，并创新性地引入了**“上下文操纵（Context Manipulation）”**阶段。这一阶段是 AI 智能体特有的，指攻击者无需绕过代码控制，仅通过污染 LLM 的推理上下文（如注入恶意指令）即可诱导其调用危险工具。

#### 3. 实验设计
本研究并非基于模拟实验，而是基于对 **OpenClaw 框架 190 条真实安全公告（Advisories）**的实证分析：
*   **数据集/场景：** 涵盖了 2026 年 1 月至 2 月间发现的所有漏洞，涉及 Telegram、Slack、Discord 等 15 个第三方平台的集成通道。
*   **分析方法：** 采用**补丁差异分析（Patch-differential Evidence）**，通过对比漏洞修复前后的代码（Diff），还原攻击路径和架构根因。
*   **Benchmark：** 以传统的网络杀伤链和 MITRE ATT&CK 为基准，对比 AI 智能体环境下的特殊变体。

#### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**使用了具体的 GPU 算力或训练时长。
*   **原因：** 本文属于网络安全架构分析与漏洞审计类论文，重点在于对现有框架代码的静态/动态分析及架构建模，而非训练新的深度学习模型。

#### 5. 实验数量与充分性
*   **实验规模：** 分析了全部 190 条安全公告，并对其中最具代表性的案例（如 `yahoofinance` 恶意插件、网关 RCE 链、执行白名单绕过）进行了深度代码级解构。
*   **充分性：** 实验样本覆盖了从最外层的消息通道到最内层的宿主执行环境的所有层级，具有极高的实证价值。
*   **客观性：** 研究基于已公开修复的漏洞补丁，证据链完整，结论具有较强的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **跨层组合攻击：** 发现三个独立的中/高危漏洞（SSRF、令牌窃取、策略重写）可以串联成一条完整的**未授权远程代码执行（RCE）**路径。
*   **词法解析的局限性：** 框架依赖词法解析（Lexical Parsing）来识别命令身份，但这种“封闭世界假设”被 Shell 换行符、Busybox 多路复用和 GNU 选项缩写轻易瓦解。
*   **供应链风险：** 恶意插件可以在 LLM 上下文中执行两阶段 Dropper，完全绕过运行时的执行策略检查。
*   **架构根因：** 最大的结构性问题在于**“逐层局部防御”**而非**“统一策略边界”**。由于缺乏跨层的上下文感知，各层之间的信任假设在组合时会发生崩溃。

#### 7. 优点
*   **架构视角：** 首次提出了针对 AI 智能体框架的层级化分类法，超越了单纯的“提示注入”研究。
*   **实战导向：** 所有的理论分析均有真实的 CVE 级漏洞支持，提供了详尽的代码 Diff 分析。
*   **前瞻性：** 提出的“上下文操纵”阶段为未来 AI 安全防御（如上下文来源追踪）指明了方向。

#### 8. 不足与局限
*   **单一框架局限：** 研究高度集中于 OpenClaw 框架，虽然该框架具有代表性，但结论在其他架构（如基于 ReAct 的简单 Agent）上的适用性有待进一步验证。
*   **防御验证不足：** 论文虽然提出了防御建议（如语义解释器、统一策略边界），但未对这些防御措施的性能开销和鲁棒性进行大规模实验验证。
*   **部分攻击面缺失：** 如“多智能体通信（Inter-Agent Communication）”层级的漏洞在现有语料库中较少，相关风险分析多基于理论推导。

（完）
