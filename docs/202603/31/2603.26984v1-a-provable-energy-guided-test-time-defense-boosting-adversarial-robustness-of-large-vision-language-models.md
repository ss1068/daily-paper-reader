---
title: A Provable Energy-Guided Test-Time Defense Boosting Adversarial Robustness of Large Vision-Language Models
title_zh: 一种可证明的能量引导测试时防御，旨在提升大型视觉语言模型的对抗鲁棒性
authors: "Mujtaba Hussain Mirza, Antonio D'Orazio, Odelia Melamed, Iacopo Masi"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26984v1"
tags: ["query:llm"]
score: 9.0
evidence: 大型视觉语言模型的对抗鲁棒性
tldr: 针对大型视觉语言模型（LVLM）易受对抗攻击的问题，本文提出了一种名为ET3的轻量级、无需训练的测试时防御方法。该方法通过在推理阶段最小化输入样本的能量来消除对抗扰动，并提供了理论证明。实验结果显示，ET3在图像分类、零样本CLIP分类以及LVLM的图像描述和视觉问答等任务中均能显著提升模型的对抗鲁棒性。
selection_source: fresh_fetch
motivation: 大型视觉语言模型在实际应用中极易受到对抗扰动的影响，而传统的对抗训练成本高昂且难以直接应用于推理阶段。
method: 提出能量引导测试时变换（ET3），通过在推理时最小化输入样本的能量函数来增强模型的鲁棒性。
result: 实验证明ET3在分类任务、零样本CLIP以及LVLM的下游任务中均提供了强大的防御能力。
conclusion: ET3是一种具有理论支撑且通用的轻量级防御策略，能有效提升多种视觉语言模型在面对对抗攻击时的可靠性。
---

## 摘要
尽管多模态模型和大型视觉语言模型（LVLM）取得了快速进展，但它们仍然极易受到对抗扰动的影响，这引发了对其在现实世界应用中可靠性的严重担忧。虽然对抗训练已成为构建对抗攻击鲁棒模型的主流范式，但测试时变换（TTT）已成为一种在推理阶段提升鲁棒性的极具前景的策略。鉴于此，我们提出了能量引导测试时变换（ET3），这是一种轻量级、无需训练的防御方法，通过最小化输入样本的能量来增强鲁棒性。我们的方法基于一套理论，证明了在合理假设下，我们的变换在分类任务中是成功的。我们进行了广泛的实验，证明 ET3 为分类器、CLIP 的零样本分类提供了强大的防御，并提升了 LVLM 在图像描述和视觉问答等任务中的鲁棒性。代码可在 github.com/OmnAI-Lab/Energy-Guided-Test-Time-Defense 获取。

## Abstract
Despite the rapid progress in multimodal models and Large Visual-Language Models (LVLM), they remain highly susceptible to adversarial perturbations, raising serious concerns about their reliability in real-world use. While adversarial training has become the leading paradigm for building models that are robust to adversarial attacks, Test-Time Transformations (TTT) have emerged as a promising strategy to boost robustness at inference.In light of this, we propose Energy-Guided Test-Time Transformation (ET3), a lightweight, training-free defense that enhances the robustness by minimizing the energy of the input samples.Our method is grounded in a theory that proves our transformation succeeds in classification under reasonable assumptions. We present extensive experiments demonstrating that ET3 provides a strong defense for classifiers, zero-shot classification with CLIP, and also for boosting the robustness of LVLMs in tasks such as Image Captioning and Visual Question Answering. Code is available at github.com/OmnAI-Lab/Energy-Guided-Test-Time-Defense .

---

## 论文详细总结（自动生成）

以下是对论文《A Provable Energy-Guided Test-Time Defense Boosting Adversarial Robustness of Large Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大型视觉语言模型（LVLM，如 LLaVA、Qwen-VL）虽然在多模态推理上表现卓越，但其视觉编码器（通常是 CLIP）极易受到对抗攻击。微小的、不可察觉的图像扰动就能导致模型输出完全错误的描述或答案。
*   **核心问题**：现有的防御手段如“对抗训练（AT）”成本极高且难以覆盖所有攻击类型；而现有的“测试时变换（TTT）”防御往往计算开销大（需要优化大量参数或生成多个增强视图）。
*   **整体含义**：本文提出了一种名为 **ET3** 的轻量级、无需训练的测试时防御机制，通过在推理阶段微调输入图像以降低其“能量”，从而在不改变模型参数的情况下显著提升鲁棒性。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将预训练分类器或视觉编码器的输出 Logits 视为能量函数。对抗样本通常偏离自然数据流形（高能量），通过梯度下降降低输入样本的能量，可以将其引导回正确的分类区域。
*   **关键技术细节**：
    *   **能量定义**：定义能量 $E(x)$ 为输出 Logits 的负 LogSumExp，即 $E(x) = -\log \sum_{k=1}^{K} \exp(f_\theta(x)_k)$。
    *   **优化过程**：在推理时，对输入图像 $x$ 进行 $T$ 步（通常仅 1-2 步）梯度下降优化：$\tilde{x} = \text{argmin}_{x^* \in \mathcal{B}_\epsilon(x)} E(x^*)$。
    *   **约束条件**：优化过程被限制在以原图为中心的 $\ell_2$ 球内（半径为 $\epsilon$），以保持图像内容不发生剧烈改变。
    *   **理论保证**：论文证明了在局部线性假设和梯度范数比例条件下，ET3 变换能够确保二分类器对对抗样本做出正确预测。
    *   **扩展至 LVLM**：在 LVLM 任务中，ET3 仅通过 CLIP 视觉编码器优化图像，优化后的特征再输入语言模型，无需语言模型参与反向传播。

### 3. 实验设计
*   **数据集/场景**：
    *   **零样本分类**：涵盖 ImageNet、CIFAR-10/100、Caltech101、EuroSAT 等 15 个基准数据集。
    *   **LVLM 任务**：图像描述（COCO、Flickr30k）和视觉问答（TextVQA、VQAv2）。
    *   **鲁棒分类器**：ImageNet 上的 ResNet-50、ConvNeXt、Swin Transformer 等。
*   **对比方法**：
    *   **基准模型**：标准 CLIP、TeCoA（对抗训练版 CLIP）、FARE（鲁棒 CLIP）。
    *   **防御方法**：TTC（测试时反击）、TPT/C-TPT（提示词调优）、R-TPT、DiffPure（基于扩散模型的净化）、Randomized Smoothing 等。
*   **攻击模型**：AutoAttack（APGD-CE, APGD-DLR）、PGD-100，以及针对测试时防御的**自适应攻击**（BPDA）。

### 4. 资源与算力
*   **硬件支持**：实验使用了 **NVIDIA A100 GPU**。此外，致谢部分提到了 CINECA HPC 和 EuroHPC 提供的计算资源。
*   **推理开销**：在 LLaVA 1.5-7B 模型上，单步 ET3 仅增加 **2.3%** 的推理延迟（从 593.9ms 增至 607.3ms），两步优化增加 **7.7%**。相比于需要生成多个增强视图的方法，ET3 非常高效。

### 5. 实验数量与充分性
*   **实验规模**：论文在 15 个分类数据集上进行了详尽测试，并针对 LVLM 的生成任务进行了 500 个样本的对抗评估。
*   **消融实验**：包括了步数（1步 vs 2步）、防御半径（$\epsilon$）、标签集选择（ImageNet-21k 代理标签 vs 数据集特定标签）、投影方式（$\ell_2$ vs $\ell_\infty$）的对比。
*   **公平性与客观性**：作者不仅在防御者视角下测试，还引入了**防御感知（Defense-aware）的自适应攻击**，使用 BPDA 绕过梯度混淆，证明了 ET3 的鲁棒性并非来自“梯度掩盖”，实验设计严谨且符合学术规范。

### 6. 主要结论与发现
*   **显著提升**：在 TeCoA 和 FARE 等已经过对抗训练的模型上，ET3 能进一步提升约 **8% - 11%** 的鲁棒准确率。
*   **通用性**：ET3 不仅适用于分类，还能有效保护 LVLM 的生成任务，使其在受到攻击时仍能产生有意义的描述。
*   **特征诱导**：可视化分析表明，ET3 能够放大图像中与类别相关的显著特征（如蛇的眼睛、兔子的耳朵），从而抵消对抗噪声的影响。
*   **即插即用**：作为一种无需训练的方法，它可以直接应用于现有的预训练模型，且与提示词调优（Prompt Tuning）等方法具有互补性。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅有出色的实验结果，还提供了二分类场景下的数学证明，增强了方法的可靠性。
*   **极高的效率**：仅需 1-2 步梯度更新，远快于基于扩散模型或多视图聚合的防御方法。
*   **无需额外模型**：直接利用模型自身的 Logits，不需要训练额外的生成模型或 EBM。
*   **跨任务一致性**：在判别式任务（分类）和生成式任务（Captioning/VQA）中均表现稳健。

### 8. 不足与局限
*   **白盒假设**：ET3 在测试时需要访问视觉编码器的梯度，这在某些仅提供 API 调用的黑盒场景下无法实施。
*   **标签集依赖**：能量函数的计算依赖于预定义的标签集（如 ImageNet-21k），对于完全脱离这些概念的极冷门领域，防御效果可能会受限。
*   **干净数据性能微降**：在某些配置下，为了获得极高的鲁棒性，干净数据的准确率会有轻微下降（通常在 1% 以内）。
*   **防御半径敏感性**：虽然 $\ell_2$ 约束有效，但如何针对不同任务自动选择最优的防御半径 $\epsilon$ 仍是一个超参数调优问题。

（完）
