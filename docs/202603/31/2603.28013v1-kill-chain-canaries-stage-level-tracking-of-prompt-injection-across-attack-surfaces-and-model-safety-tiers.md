---
title: "Kill-Chain Canaries: Stage-Level Tracking of Prompt Injection Across Attack Surfaces and Model Safety Tiers"
title_zh: 杀伤链金丝雀：跨攻击面与模型安全层级的提示词注入阶段级追踪
authors: Haochuan Kevin Wang
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28013v1"
tags: ["query:llm"]
score: 10.0
evidence: 跨攻击面追踪提示词注入
tldr: 本研究针对五种前沿大模型智能体，提出了一种阶段分解的提示注入攻击分析方法。通过引入加密金丝雀令牌，研究者追踪了攻击在暴露、持久化、中继和执行四个杀伤链阶段的演变。研究发现，模型安全性并非取决于是否接触到恶意内容，而在于内容是否在流水线阶段间传播。实验揭示了不同模型在防御机制上的显著差异，并指出当前防御手段在应对特定攻击面时存在失效风险，为提升智能体安全性提供了新视角。
selection_source: fresh_fetch
motivation: 传统的提示注入评估仅关注任务层面的成功率，无法定位防御激活的具体阶段及攻击在流水线中的演变过程。
method: 通过在764次实验中植入加密金丝雀令牌，跨四个攻击面和五种防御条件追踪攻击在暴露、持久化、中继和执行阶段的流转。
result: 所有模型均会暴露于攻击，但Claude在记忆写入阶段能有效拦截，而GPT-4o-mini传播率极高，且现有防御措施因攻击面不匹配而失效。
conclusion: 模型安全性由攻击内容在流水线阶段间的传播控制决定，而非单纯的暴露情况，且特定模型作为中继节点具有去污染潜力。
---

## 摘要
我们对针对五种前沿大语言模型（LLM）智能体的提示词注入攻击进行了阶段分解分析。以往的工作主要衡量任务级攻击成功率（ASR）；而我们则定位了每个模型防御机制激活的具体流水线阶段。我们在每次运行中都植入了一个加密金丝雀令牌（SECRET-[A-F0-9]{8}），并在四个攻击面和五种防御条件下，通过四个杀伤链阶段——暴露（Exposed）、持久化（Persisted）、中继（Relayed）、执行（Executed）——对其进行追踪（共计 764 次运行，其中 428 次为无防御攻击）。我们的核心发现是，模型的安全性并不取决于是否看到了对抗性内容，而取决于该内容是否在流水线阶段之间传播。具体而言：(1) 在我们的评估中，所有五个模型的暴露率均为 100%——安全差距完全存在于下游；(2) Claude 在 write_memory 摘要阶段剥离了注入内容（0/164 ASR），而 GPT-4o-mini 则无损地传播了金丝雀（53% ASR，95% 置信区间：41–65%）；(3) DeepSeek 在同一模型中表现出内存攻击面 0% ASR 和工具流攻击面 100% ASR——在不同注入通道间呈现完全相反的结果；(4) 由于威胁模型攻击面不匹配，所有四种主动防御条件（write_filter、pi_detector、spotlighting 及其组合）均产生了 100% 的 ASR；(5) Claude 中继节点可以对下游智能体进行去污染——40 个金丝雀中无一进入共享内存。

## Abstract
We present a stage-decomposed analysis of prompt injection attacks against five frontier LLM agents. Prior work measures task-level attack success rate (ASR); we localize the pipeline stage at which each model's defense activates. We instrument every run with a cryptographic canary token (SECRET-[A-F0-9]{8}) tracked through four kill-chain stages -- Exposed, Persisted, Relayed, Executed -- across four attack surfaces and five defense conditions (764 total runs, 428 no-defense attacked). Our central finding is that model safety is determined not by whether adversarial content is seen, but by whether it is propagated across pipeline stages. Concretely: (1) in our evaluation, exposure is 100% for all five models -- the safety gap is entirely downstream; (2) Claude strips injections at write_memory summarization (0/164 ASR), while GPT-4o-mini propagates canaries without loss (53% ASR, 95% CI: 41--65%); (3) DeepSeek exhibits 0% ASR on memory surfaces and 100% ASR on tool-stream surfaces from the same model -- a complete reversal across injection channels; (4) all four active defense conditions (write_filter, pi_detector, spotlighting, and their combination) produce 100% ASR due to threat-model surface mismatch; (5) a Claude relay node decontaminates downstream agents -- 0/40 canaries survived into shared memory.

---

## 论文详细总结（自动生成）

这篇论文《Kill-Chain Canaries: Stage-Level Tracking of Prompt Injection Across Attack Surfaces and Model Safety Tiers》对大语言模型（LLM）智能体在面对提示词注入攻击时的安全性进行了深入的阶段性剖析。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的提示词注入（Prompt Injection）评估通常只关注最终的攻击成功率（ASR），即“黑盒”结果。这种方法无法区分模型是“没看到注入内容”还是“看到了但拒绝执行”，也无法定位防御机制在复杂流水线的哪个环节发挥了作用。
*   **核心问题**：在多智能体或多步骤的流水线中，攻击载荷是如何流转的？不同模型的防御机制具体激活在哪个阶段？

### 2. 核心方法论
*   **杀伤链金丝雀（Kill-Chain Canary）**：研究者在注入载荷中植入唯一的加密金丝雀令牌（格式为 `SECRET-[A-F0-9]{8}`）。
*   **四个追踪阶段**：
    1.  **EXPOSED（暴露）**：金丝雀出现在工具返回的结果中。
    2.  **PERSISTED（持久化）**：金丝雀被写入内存（通常经过摘要处理）。
    3.  **RELAYED（中继）**：第二个智能体读取了包含金丝雀的内存。
    4.  **EXECUTED（执行）**：金丝雀出现在发往外部工具的参数中（攻击最终成功）。
*   **关键技术**：开发了 `PropagationLogger` 追踪器和 `agent_bench` 评估框架，通过正则表达式在每一步监控令牌的流转。

### 3. 实验设计
*   **模型对象**：GPT-4o-mini、GPT-5-mini、DeepSeek Chat v3、Claude Haiku 4.5、Claude Sonnet 4.5。
*   **攻击面（场景）**：
    *   `memory_poison`：预先植入内存的毒化数据。
    *   `tool_poison`：通过工具返回流（如网页内容）进入的注入。
    *   `propagation`：跨智能体的内存中继攻击。
    *   `permission_esc`：涉及权限提升的两步攻击。
*   **防御条件**：对比了无防御、`write_filter`（写入过滤）、`pi_detector`（注入检测器）、`spotlighting`（标记增强）以及全防御组合。
*   **基准测试**：使用自定义的 `agent_bench`（约600行Python代码），模拟内存存储、工具注册和多智能体编排。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。由于实验主要基于 API 调用（OpenAI、Anthropic、DeepSeek），重点在于推理阶段的评估而非模型训练。
*   **实验周期**：实验在 2026 年 3 月 10 日至 27 日期间分 9 个批次完成。

### 5. 实验数量与充分性
*   **实验规模**：总计 **764 次运行**，其中 428 次为无防御攻击实验，80 次带防御攻击，256 次为干净对照组。
*   **充分性评价**：虽然每个实验单元（Cell）的样本量较小（n=8 到 36），但研究者通过跨越 17 天的多个独立批次重复实验，验证了结果的一致性。实验覆盖了主流的前沿模型和多种典型的攻击/防御场景，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **暴露是普遍的，防御在下游**：所有模型在所有攻击中暴露率均为 100%。安全性的差异不在于是否“看到”攻击，而在于是否将其“传播”到后续阶段。
*   **Claude 的摘要防御**：Claude 模型在 `write_memory`（摘要写入）阶段表现极佳，能主动剥离注入内容（ASR 为 0%），充当了天然的“去污染”节点。
*   **DeepSeek 的攻击面分裂**：DeepSeek 在内存攻击面上 ASR 为 0%，但在工具流攻击面上 ASR 为 100%。这表明模型对不同来源的数据信任度不同。
*   **防御失效与攻击面不匹配**：现有的静态防御（如 Spotlighting）在面对非预设攻击面（如工具调用流而非文档流）时，ASR 依然高达 100%。
*   **目标漂移（Objective Drift）**：攻击发生时，模型行为与原始任务目标的余弦距离会显著增大，但这是一种**事后信号**，无法用于事前预防。

### 7. 优点
*   **颗粒度细**：首次将提示注入的评估从“结果导向”提升到“阶段导向”，提供了更精准的防御定位。
*   **架构启示**：提出了“中继去污染”的概念，即在流水线中使用安全性更高的模型（如 Claude）作为中继节点，可以保护下游安全性较低的模型。
*   **揭示防御盲区**：有力地证明了单一攻击面的评估会严重误导对模型安全性的判断。

### 8. 不足与局限
*   **样本量限制**：部分细分场景的样本量（n=8）较少，统计显著性有待进一步加强。
*   **载荷单一**：实验使用的注入载荷较为直接和人工化，未测试复杂的混淆技术或社会工程学手段。
*   **模型覆盖**：未包含推理模型（如 o1/o3）或开源权重模型（如 Llama 系列）。
*   **黑盒局限**：无法解释 Claude 这种“摘要阶段过滤”行为的底层原理（是训练数据、系统提示词还是工具设计导致的）。

（完）
