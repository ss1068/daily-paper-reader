---
title: "Invisible Threats from Model Context Protocol: Generating Stealthy Injection Payload via Tree-based Adaptive Search"
title_zh: 来自模型上下文协议的隐形威胁：通过基于树的自适应搜索生成隐蔽注入载荷
authors: "Yulin Shen, Xudong Pan, Geng Hong, Min Yang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24203v1"
tags: ["query:llm"]
score: 10.0
evidence: 为大语言模型工具生成隐蔽的注入载荷
tldr: "本研究探讨了模型上下文协议（MCP）中工具增强型智能体面临的间接提示注入威胁。针对现有攻击方法成本高、易被检测等问题，提出了TIP（树状载荷注入）框架。TIP采用黑盒攻击模式，将载荷生成转化为树状搜索问题，通过粗到细的优化框架和路径感知反馈机制，生成自然且隐蔽的恶意载荷。实验表明，TIP在主流LLM上达到95%以上的攻击成功率，且在防御环境下表现优异，揭示了MCP系统中亟待解决的安全漏洞。"
selection_source: fresh_fetch
motivation: 现有针对MCP的间接提示注入攻击存在部署成本高、语义不连贯或依赖白盒信息等局限，且易被防御手段检测。
method: 提出TIP框架，将攻击载荷生成建模为树状搜索问题，利用攻击者LLM在粗到细的优化框架下，结合路径感知反馈和防御信号进行自适应搜索。
result: "在四种主流LLM上实现了超过95%的攻击成功率，查询效率提升一个数量级，且在面对四种防御措施时仍保持50%以上的有效性。"
conclusion: 该研究揭示了MCP部署中隐蔽且实际的安全威胁，强调了在工具增强型智能体中建立更强防御机制的紧迫性。
---

## 摘要
模型上下文协议（MCP）的最新进展使得大语言模型（LLM）能够以前所未有的便捷性调用外部工具。这催生了一类新型的、功能强大的工具增强型智能体。遗憾的是，这种能力也引入了一个尚未被充分探索的攻击面，即对工具响应的恶意操纵。现有的针对 MCP 的间接提示注入技术存在部署成本高、语义连贯性差或对白盒要求过高等问题。此外，它们通常很容易被最近提出的防御措施检测到。在本文中，我们提出了“载荷树状注入”（TIP），这是一种新型黑盒攻击，它能生成自然的载荷，即使在有防御的情况下也能可靠地夺取启用 MCP 的智能体的控制权。在技术上，我们将载荷生成建模为一个树状结构搜索问题，并在我们提出的由粗到精的优化框架下，利用攻击者 LLM 引导搜索。为了稳定学习并避免局部最优，我们引入了一种路径感知反馈机制，仅向攻击者模型呈现高质量的历史轨迹。通过显式地将搜索建立在可观测的防御信号之上并动态重新分配探索预算，该框架进一步增强了对抗防御转换的能力。对四种主流 LLM 的广泛实验表明，在无防御设置下，TIP 的攻击成功率超过 95%，且所需的查询次数比之前的自适应攻击少一个数量级。针对四种代表性防御方法，TIP 保持了 50% 以上的有效性，并显著优于现有的最先进攻击。通过在真实世界的 MCP 系统上实施攻击，我们的结果揭示了 MCP 部署中一个隐形但实际存在的威胁向量。我们还讨论了解决这一关键安全漏洞的潜在缓解方法。

## Abstract
Recent advances in the Model Context Protocol (MCP) have enabled large language models (LLMs) to invoke external tools with unprecedented ease. This creates a new class of powerful and tool augmented agents. Unfortunately, this capability also introduces an under explored attack surface, specifically the malicious manipulation of tool responses. Existing techniques for indirect prompt injection that target MCP suffer from high deployment costs, weak semantic coherence, or heavy white box requirements. Furthermore, they are often easily detected by recently proposed defenses. In this paper, we propose Tree structured Injection for Payloads (TIP), a novel black-box attack which generates natural payloads to reliably seize control of MCP enabled agents even under defense. Technically, We cast payload generation as a tree structured search problem and guide the search with an attacker LLM operating under our proposed coarse-to-fine optimization framework. To stabilize learning and avoid local optima, we introduce a path-aware feedback mechanism that surfaces only high quality historical trajectories to the attacker model. The framework is further hardened against defensive transformations by explicitly conditioning the search on observable defense signals and dynamically reallocating the exploration budget. Extensive experiments on four mainstream LLMs show that TIP attains over 95% attack success in undefended settings while requiring an order of magnitude fewer queries than prior adaptive attacks. Against four representative defense approaches, TIP preserves more than 50% effectiveness and significantly outperforms the state-of-the-art attacks. By implementing the attack on real world MCP systems, our results expose an invisible but practical threat vector in MCP deployments. We also discuss potential mitigation approaches to address this critical security gap.

---

## 论文详细总结（自动生成）

这篇论文针对新兴的**模型上下文协议（Model Context Protocol, MCP）**环境下的安全威胁进行了深入研究，提出了名为 **TIP** 的攻击框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着 Anthropic 发布 MCP 协议，LLM 智能体调用外部工具（如天气、数据库、搜索）变得标准化。这种架构将 LLM 的推理能力与第三方工具服务器解耦。
*   **核心问题**：现有的间接提示注入（IPI）攻击在 MCP 环境下存在局限性：要么生成的是乱码（如 GCG），容易被检测；要么部署成本高，且难以绕过现代防御机制（如困惑度过滤、递归摘要）。
*   **研究动机**：作者识别出一种“隐蔽更新攻击（Stealthy Update Attack）”模式。攻击者可以先发布合法的工具，在获得用户信任后，通过修改服务器端的动态响应字段注入恶意指令，而无需更改受审计的元数据。

### 2. 方法论：TIP 框架
TIP（Tree-structured Injection for Payloads）将载荷生成视为一个**基于树状结构的自适应搜索问题**。
*   **核心思想**：利用一个“攻击者 LLM”作为优化引擎，在离散的文本空间内通过迭代搜索，生成既符合工具语义（隐蔽性）又能劫持智能体行为（有效性）的 JSON 载荷。
*   **关键技术细节**：
    *   **路径感知反馈（Path-aware Feedback）**：不只提供单次失败反馈，而是将高质量的历史优化轨迹（祖先节点）提供给攻击者模型，避免陷入局部最优。
    *   **工具响应模拟（Tool Response Simulation）**：在注入前先模拟合法的工具输出，确保恶意载荷在语义和统计分布上与真实数据对齐，从而绕过困惑度检测。
    *   **双层由粗到精策略（Dual Coarse-to-Fine Strategy）**：第一层确定攻击引导方向（隐式诱导或显式控制）；第二层根据得分动态调整是优化 JSON 结构（Key）还是精炼具体措辞（Value）。
    *   **防御感知机制（Defense-aware Mechanism）**：根据观察到的防御信号（如被重写或被拦截），动态调整搜索策略和预算分配。
*   **算法流程**：分为 **Branch（分支生成）**、**Prune（剪枝评估）** 和 **Early Stop（早停）** 三个阶段，通过蒙特卡洛方法评估载荷在不同指令和模型上的迁移得分。

### 3. 实验设计
*   **实验场景**：
    1.  **欺诈场景（Fraud）**：诱导智能体向用户推荐钓鱼链接（工具：天气查询、产品获取）。
    2.  **数据窃取场景（Data Steal）**：强制智能体读取上下文中的敏感信息并通过邮件发送（工具：预订管理、物流管理）。
*   **Benchmark 与基准方法**：
    *   使用了 **InjecAgent** 评估框架。
    *   对比方法包括：**Fixed（人工构造的静态提示词）** 和 **TAP（Tree of Attacks，目前最先进的自动化攻击框架）**。
*   **受害者模型**：Qwen2.5-7B/72B-Instruct, Llama3.1-8B, Llama3.3-70B。
*   **防御手段**：指令预防（IP）、三明治防御（SP）、微调检测器、困惑度过滤。

### 4. 资源与算力
*   **模型使用**：攻击者引擎使用了 **Qwen2.5-72B-Instruct**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率指标**：论文强调了查询效率，TIP 在达到同等攻击效果时，所需的查询次数比 TAP 少一个数量级（例如从 2500 次降至 100 次左右）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流 LLM、4 种典型工具场景、4 种代表性防御机制。
*   **充分性**：
    *   **消融实验**：分别验证了防御感知、策略框架和路径反馈的贡献，证明了各模块的必要性。
    *   **迁移性测试**：验证了在小模型上生成的载荷能否攻击未见过的大模型（如 GPT-4o）。
    *   **真实案例研究**：在 LM Studio 和 VS Code 的 MCP 插件中进行了端到端验证。
*   **客观性**：实验设置了严格的成功判定标准（ASR），并使用了余弦相似度来量化隐蔽性，评估较为客观。

### 6. 主要结论与发现
*   **高效性**：在无防御环境下，TIP 达到了 **95% 以上**的攻击成功率。
*   **强鲁棒性**：在面对严格防御（如微调检测器）时，TIP 仍能保持 **50%-90%** 的成功率，远超基准方法。
*   **隐蔽性**：生成的载荷与合法响应的语义相似度极高，人类用户和自动化过滤器难以察觉。
*   **安全漏洞**：揭示了 MCP 协议中“信任工具响应”这一根本性缺陷，即使是经过安全对齐的 GPT-4o 也会中招。

### 7. 优点
*   **黑盒适用性**：不需要访问受害者模型的梯度或权重，具有极强的实用价值。
*   **自适应能力**：通过树状搜索和反馈机制，能够针对不同的防御手段自动进化载荷。
*   **语义连贯**：克服了以往对抗攻击产生“乱码”的缺陷，使攻击更具欺骗性。

### 8. 不足与局限
*   **优化延迟**：虽然查询次数减少，但仍需要多次迭代搜索，无法做到完全的“一键实时生成”。
*   **多轮对话持久性**：目前主要关注单次注入后的即时劫持，对于在长对话中埋下长期伏笔的攻击研究较少。
*   **防御演进风险**：如果未来防御方采用更激进的“全量重写”策略（由另一个可信 LLM 重写所有工具输出），攻击成本可能会大幅上升。

（完）
