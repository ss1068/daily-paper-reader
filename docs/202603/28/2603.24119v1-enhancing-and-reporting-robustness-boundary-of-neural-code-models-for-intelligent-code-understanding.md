---
title: Enhancing and Reporting Robustness Boundary of Neural Code Models for Intelligent Code Understanding
title_zh: 增强并报告用于智能代码理解的神经代码模型的鲁棒性边界
authors: "Tingxu Han, Wei Song, Weisong Sun, Hao Wu, Chunrong Fang, Yuan Xiao, Xiaofang Zhang, Zhenyu Chen, Yang Liu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24119v1"
tags: ["query:llm"]
score: 8.0
evidence: 代码模型中的对抗样本与鲁棒性
tldr: 针对神经代码模型（NCM）易受对抗性攻击且现有防御手段成本高、缺乏理论保证的问题，本文提出 ENBECOME。这是一种黑盒、免训练且轻量级的防御框架，通过在推理阶段引入随机语义保持扰动来平滑决策边界。ENBECOME 不仅能显著降低攻击成功率，还能提供认证鲁棒性边界，确保在一定扰动范围内模型预测的稳定性，为代码理解任务提供了高效的安全保障。
selection_source: carryover_cache
motivation: 现有的神经代码模型防御方法通常需要白盒访问且缺乏理论上的鲁棒性保证，难以在实际应用中高效部署。
method: 提出一种名为 ENBECOME 的黑盒免训练防御方法，通过在推理时对输入代码进行随机语义保持扰动来平滑模型决策边界。
result: "实验显示该方法将缺陷检测任务的攻击成功率从 42.43% 降至 9.74%，同时保持了极高的模型准确率，并实现了平均 1.63 的认证鲁棒半径。"
conclusion: ENBECOME 证明了在不改变模型参数的情况下，通过推理侧平滑技术可以同时提升神经代码模型的经验鲁棒性与认证鲁棒性。
---

## 摘要
随着深度学习的发展，CodeBERT 和 CodeLlama 等神经代码模型（NCMs）被广泛应用于代码理解任务，包括缺陷检测和代码分类。然而，最近的研究表明，NCMs 容易受到对抗样本的攻击，这些输入带有细微的扰动，在诱导错误预测的同时难以被检测。现有的防御方法通过数据增强来经验性地提高鲁棒性，但它们成本高昂，无法提供理论上的鲁棒性保证，且通常需要对模型内部（如梯度）进行白盒访问。为了应对上述挑战，我们提出了 ENBECOME，这是一种新型的黑盒、免训练且轻量级的对抗防御方法。ENBECOME 旨在增强 NCMs 的经验鲁棒性，并报告其认证鲁棒性边界。ENBECOME 仅在推理阶段运行，通过向输入代码片段引入随机且保持语义的扰动来平滑 NCM 的决策边界。这种平滑处理使 ENBECOME 能够正式认证一个鲁棒性半径，在该半径内，对抗样本永远不会导致误分类，这一特性被称为认证鲁棒性。我们在多种 NCM 架构和任务上进行了全面的实验。结果表明，ENBECOME 在保持高准确率的同时，显著降低了攻击成功率。例如，在缺陷检测中，它将平均攻击成功率（ASR）从 42.43% 降低到 9.74%，而准确率仅下降了 0.29%。此外，ENBECOME 实现了 1.63 的平均认证鲁棒性半径，这意味着对不超过 1.63 个标识符的对抗性修改在证明上是无效的。

## Abstract
With the development of deep learning, Neural Code Models (NCMs) such as CodeBERT and CodeLlama are widely used for code understanding tasks, including defect detection and code classification. However, recent studies have revealed that NCMs are vulnerable to adversarial examples, inputs with subtle perturbations that induce incorrect predictions while remaining difficult to detect. Existing defenses address this issue via data augmentation to empirically improve robustness, but they are costly, offer no theoretical robustness guarantees, and typically require white-box access to model internals, such as gradients. To address the above challenges, we propose ENBECOME, a novel black-box training-free and lightweight adversarial defense. ENBECOME is designed to both enhance empirical robustness and report certified robustness boundaries for NCMs. ENBECOME operates solely during inference, introducing random, semantics-preserving perturbations to input code snippets to smooth the NCM's decision boundaries. This smoothing enables ENBECOME to formally certify a robustness radius within which adversarial examples can never induce misclassification, a property known as certified robustness. We conduct comprehensive experiments across multiple NCM architectures and tasks. Results show that ENBECOME significantly reduces attack success rates while maintaining high accuracy. For example, in defect detection, it reduces the average ASR from 42.43% to 9.74% with only a 0.29% drop in accuracy. Results show that ENBECOME significantly reduces attack success rates while maintaining high accuracy. For example, in defect detection, it reduces the average ASR from 42.43% to 9.74% with only a 0.29% drop in accuracy. Furthermore, ENBECOME achieves an average certified robustness radius of 1.63, meaning that adversarial modifications to no more than 1.63 identifiers are provably ineffective.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ENBECOME** 的新型防御框架，旨在提升神经代码模型（NCM）在面对对抗性攻击时的鲁棒性，并首次为该领域提供了理论上的“认证鲁棒性”边界。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：神经代码模型（如 CodeBERT、CodeLlama）极易受到对抗样本（AE）的攻击。攻击者只需对代码中的标识符（Identifier）进行微小且保持语义的修改，就能诱导模型做出错误判断（如将有缺陷的代码识别为无缺陷）。
*   **研究动机**：
    *   **现有防御缺陷**：现有的防御手段（如对抗训练、数据增强）通常需要白盒访问模型参数、重新训练模型且计算成本极高。
    *   **缺乏理论保证**：目前的防御大多是经验性的，无法给出一个确定的安全边界（即在多大程度的扰动下模型能保证绝对安全）。
    *   **黑盒需求**：随着大语言模型（LLM）的普及，开发者往往只能通过 API 访问模型，因此需要一种无需训练、黑盒且轻量级的防御方案。

### 2. 方法论（ENBECOME）
ENBECOME 的核心思想是**推理侧随机平滑（Randomized Smoothing）**，其流程分为三个阶段：
*   **平滑样本生成与预测**：
    *   在推理阶段，对输入代码 $x_0$ 进行 $N$ 次随机扰动，生成多个平滑样本。
    *   **关键技术**：扰动仅限于“标识符”，通过**插入（Insert）、替换（Replace）、删除（Delete）**字符的操作来改变标识符名称。这种方式能确保代码的语法正确性和语义不变性。
*   **基于投票的预测聚合**：
    *   将这 $N$ 个平滑样本输入 NCM，收集所有预测结果。
    *   采用**多数投票机制**确定最终输出标签。这种方法能有效抵消对抗样本带来的局部偏差，使决策边界更加平滑。
*   **认证半径生成**：
    *   利用统计学方法（Beta 分布）估计预测概率的上下界。
    *   **理论贡献**：推导出了一个理论边界 $r$（认证半径）。在该半径内（即修改的标识符数量不超过 $r$），可以从数学上证明没有任何对抗样本能导致模型误分类。

### 3. 实验设计
*   **任务与数据集**：
    *   **缺陷检测**（Defect Detection）：Devign 数据集、CodeChef。
    *   **克隆检测**（Clone Detection）：BigCloneBench。
    *   **功能分类**（Function Classification）：OJ 数据集。
*   **目标模型（NCMs）**：涵盖了传统架构（CodeBERT, GraphCodeBERT, CodeT5）和代码大模型（StarCoder, CodeLlama）。
*   **对比方法（Baselines）**：
    *   **攻击方法**：MHM、ALERT、CODA（涵盖了标识符替换和结构化攻击）。
    *   **防御方法**：RoPGen（基于风格转换）、SPACE（基于嵌入空间扰动）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **效率评估**：论文强调了其“免训练”的特性。在对比实验中，ENBECOME 的准备时间（生成平滑样本）远低于 RoPGen 和 SPACE 的重训练时间（例如在克隆检测任务中，RoPGen 需要约 95 小时，而 ENBECOME 仅需约 22 分钟）。
*   **推理开销**：由于需要多次推理（$N=100$），单样本推理时间约为 0.7s 到 1.5s，虽然高于原始模型，但在实际应用中仍属可接受范围。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的大量实验，包括 3 种任务、5 种模型架构、3 种攻击算法的交叉验证。
*   **消融实验**：详细探讨了平滑样本数量 $N$（从 0 到 10,000）和扰动率 $\eta$ 对性能的影响。
*   **泛化性测试**：除了对抗攻击，还测试了对**后门攻击（Backdoor Attack）**的防御效果。
*   **评价**：实验设计非常充分，对比了当前最先进的防御技术，且涵盖了从传统模型到 LLM 的演进，结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **显著降低攻击成功率（ASR）**：在缺陷检测中，将平均 ASR 从 42.43% 降至 9.74%，而准确率（ACC）仅下降 0.29%。
*   **认证鲁棒性**：首次报告了代码模型的认证半径，平均可证明在修改 1.63 个标识符以内模型是绝对安全的。
*   **架构通用性**：该方法对 LLM（如 CodeLlama）同样有效，显著提升了黑盒模型的安全性。
*   **防御后门**：实验发现该方法对后门攻击也有显著的缓解作用，能大幅降低触发器的激活概率。

### 7. 优点
*   **黑盒且免训练**：不需要访问模型梯度或参数，不需要昂贵的重新训练过程，极易部署。
*   **理论保证**：不同于以往的经验性防御，它提供了数学上的安全保证（认证半径）。
*   **语义保持**：通过精细化的标识符字符级扰动，完美解决了代码领域中“扰动易破坏语法”的难题。
*   **轻量级**：相比于对抗训练，其计算资源消耗极低。

### 8. 不足与局限
*   **推理延迟**：由于需要对每个输入进行 $N$ 次推理，实时性要求极高的场景可能会受到影响。
*   **扰动空间限制**：目前的认证边界主要基于 $L_0$ 范数（标识符替换数量），对于更复杂的结构性代码转换（如循环展开、语句重排）的理论认证仍有待探索。
*   **准确率微损**：虽然降幅很小，但随机平滑本质上是以极少量的预测准确率换取大幅度的鲁棒性提升。

（完）
