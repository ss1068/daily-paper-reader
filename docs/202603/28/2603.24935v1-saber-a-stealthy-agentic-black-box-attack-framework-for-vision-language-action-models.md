---
title: "SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models"
title_zh: SABER：一种针对视觉-语言-动作（VLA）模型的隐蔽代理式黑盒攻击框架
authors: "Xiyang Wu, Guangyao Shi, Qingzi Wang, Zongxia Li, Amrit Singh Bedi, Dinesh Manocha"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24935v1"
tags: ["query:llm"]
score: 8.0
evidence: 针对视觉-语言-动作模型的对抗性攻击
tldr: 针对视觉-语言-动作（VLA）模型对指令扰动的敏感性，本文提出了SABER框架。这是一种基于智能体的黑盒攻击方法，利用经过GRPO训练的ReAct攻击者，在有限的编辑预算内生成微小且合理的指令修改。SABER能有效诱导机器人任务失败、增加执行时长或违反约束。实验表明，该方法在LIBERO基准测试中显著降低了多个SOTA VLA模型的性能，揭示了机器人基础模型在指令鲁棒性方面的严重脆弱性。
selection_source: carryover_cache
motivation: 现有的VLA模型容易受到微小文本扰动的影响，需要一种系统性的黑盒攻击框架来评估其在对抗性指令下的鲁棒性。
method: 提出SABER框架，利用GRPO训练的ReAct智能体，通过字符、标记和提示层面的工具生成隐蔽且高效的对抗性指令。
result: "在LIBERO基准上，SABER使任务成功率降低20.6%，执行长度增加55%，且编辑成本和调用次数远低于GPT基准。"
conclusion: 微小且合理的指令编辑足以大幅降低机器人执行性能，基于智能体的黑盒流水线是红队测试机器人基础模型的实用且高效的手段。
---

## 摘要
视觉-语言-动作（VLA）模型使机器人能够遵循基于视觉观测的自然语言指令，但指令通道也引入了一个关键漏洞：微小的文本扰动即可改变下游机器人的行为。因此，系统的鲁棒性评估需要一种黑盒攻击者，能够在不同的 VLA 模型中生成极小但有效的指令编辑。为此，我们提出了 SABER，这是一种以代理为中心的方法，用于在受限的编辑预算下自动生成针对 VLA 模型的基于指令的对抗性攻击。SABER 使用经过 GRPO 训练的 ReAct 攻击者，在受限的编辑预算下，利用字符级、标记级和提示级工具生成微小且合理的对抗性指令编辑，从而诱导目标行为退化，包括任务失败、不必要的超长执行以及增加的约束违反。在涵盖六种最先进 VLA 模型的 LIBERO 基准测试中，SABER 使任务成功率降低了 20.6%，动作序列长度增加了 55%，约束违反增加了 33%，同时与强大的基于 GPT 的基准相比，所需的工具调用减少了 21.1%，字符编辑减少了 54.7%。这些结果表明，微小且合理的指令编辑足以显著降低机器人的执行性能，并且代理式黑盒流水线为机器人基础模型的红队测试提供了一种实用、可扩展且自适应的方法。

## Abstract
Vision-language-action (VLA) models enable robots to follow natural-language instructions grounded in visual observations, but the instruction channel also introduces a critical vulnerability: small textual perturbations can alter downstream robot behavior. Systematic robustness evaluation therefore requires a black-box attacker that can generate minimal yet effective instruction edits across diverse VLA models. To this end, we present SABER, an agent-centric approach for automatically generating instruction-based adversarial attacks on VLA models under bounded edit budgets. SABER uses a GRPO-trained ReAct attacker to generate small, plausible adversarial instruction edits using character-, token-, and prompt-level tools under a bounded edit budget that induces targeted behavioral degradation, including task failure, unnecessarily long execution, and increased constraint violations. On the LIBERO benchmark across six state-of-the-art VLA models, SABER reduces task success by 20.6%, increases action-sequence length by 55%, and raises constraint violations by 33%, while requiring 21.1% fewer tool calls and 54.7% fewer character edits than strong GPT-based baselines. These results show that small, plausible instruction edits are sufficient to substantially degrade robot execution, and that an agentic black-box pipeline offers a practical, scalable, and adaptive approach for red-teaming robotic foundation models.

---

## 论文详细总结（自动生成）

### SABER：针对视觉-语言-动作（VLA）模型的隐蔽代理式黑盒攻击框架总结

#### 1. 核心问题与研究背景
*   **研究动机**：视觉-语言-动作（VLA）模型通过自然语言指令控制机器人，虽然提高了交互灵活性，但也引入了巨大的安全隐患。微小的文本扰动（如拼写错误或词汇替换）可能导致机器人执行失败、效率低下或违反安全约束。
*   **核心问题**：现有的攻击方法多依赖手动设计、固定启发式规则或昂贵的 GPT 搜索，缺乏一种能够针对黑盒、冻结的机器人策略自动生成极小且隐蔽的对抗性指令的通用框架。
*   **整体含义**：论文提出了 SABER 框架，旨在通过一个自动化的智能体（Agent），在有限的编辑预算内，生成能够诱导机器人行为退化的隐蔽指令，作为红队测试工具评估 VLA 模型的鲁棒性。

#### 2. 方法论
*   **核心思想**：将对抗性指令生成建模为一个受限优化问题，利用一个基于 **ReAct** 模式的智能体，通过多轮工具调用来组合字符级、标记（Token）级和提示（Prompt）级的扰动。
*   **关键技术细节**：
    *   **FIND → APPLY 协议**：智能体首先通过 FIND 工具确定扰动位置和策略，再通过 APPLY 工具执行具体的编辑操作。
    *   **扰动工具箱**：
        *   **字符级**：模拟拼写错误（插入、删除、替换等）。
        *   **标记级**：替换、移除或交换词汇属性。
        *   **提示级**：注入额外的验证子句、分解步骤或不确定性描述。
    *   **训练算法**：采用 **GRPO（组相对策略优化）**。这是一种强化学习算法，允许智能体直接从机器人执行的反馈（如是否成功、动作长度）中学习，而无需访问目标 VLA 模型的梯度。
    *   **奖励设计**：奖励函数综合了攻击目标（任务失败、动作膨胀、约束违反）和隐蔽性惩罚（编辑距离、工具调用次数）。

#### 3. 实验设计
*   **数据集与场景**：使用 **LIBERO** 机器人操纵基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **目标模型（Victim Models）**：针对 6 种最先进的 VLA 模型进行了测试，包括 $\pi_0$、$\pi_{0.5}$、GR00T-N1.5、X-VLA、InternVLA-M1 和 DeepThinkVLA。
*   **对比基准**：与基于 **GPT-5 mini** 的强力黑盒攻击基准进行对比。
*   **评估指标**：攻击成功率（ASR）、动作膨胀率（AIR）、约束违反膨胀率（CVI）以及隐蔽性指标（工具调用次数和字符编辑数）。

#### 4. 资源与算力
*   **模型配置**：攻击智能体使用 **Qwen2.5-3B-Instruct** 作为骨干模型，并采用 **LoRA** 进行微调。
*   **算力说明**：论文提到了使用 ART（Agent Reinforcement Trainer）框架进行训练，但**未明确给出具体的 GPU 型号、数量或总训练时长**。实验中提到了在冷启动阶段使用了 GPT-5 mini 生成初始轨迹。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的 VLA 模型上进行了跨任务测试，涵盖了 40 个评估任务。
*   **消融实验**：进行了关于“冷启动（Cold-start）”必要性的消融实验，证明了 SFT 初始化对稳定强化学习训练的关键作用。
*   **充分性评价**：实验设计较为充分，不仅验证了攻击的有效性，还重点分析了攻击的效率（隐蔽性）。通过跨模型测试，展示了该攻击框架的泛化能力和客观性。

#### 6. 主要结论与发现
*   **显著的破坏性**：SABER 平均使任务成功率降低了 **20.6%**，动作序列长度增加了 **55%**，约束违反增加了 **33%**。
*   **高效率与隐蔽性**：相比 GPT 基准，SABER 在达到更好攻击效果的同时，减少了 **21.1%** 的工具调用和 **54.7%** 的字符编辑。
*   **策略演变**：经过训练的智能体会从早期的盲目扰动转向更高杠杆的标记级编辑，学会了用更小的代价诱导更大的行为偏差。
*   **模型脆弱性**：即使是具有推理能力的 VLA 模型（如 DeepThinkVLA），在面对微小的指令扰动时依然表现出显著的脆弱性。

#### 7. 优点
*   **代理式黑盒攻击**：无需目标模型梯度，适应现实中模型权重不公开的场景。
*   **多目标优化**：不仅关注任务失败，还关注执行效率和物理安全约束，更符合机器人领域的红队需求。
*   **隐蔽性强**：通过 GRPO 优化，生成的扰动非常微小且看起来自然，难以被简单的规则检测。

#### 8. 不足与局限
*   **环境局限**：目前仅在 **LIBERO 模拟环境**中进行了验证，尚未在真实物理机器人上进行测试。
*   **模态局限**：攻击仅限于**文本指令**维度，未涉及视觉观测的对抗性扰动或多模态联合攻击。
*   **应用限制**：虽然展示了攻击能力，但未深入探讨相应的防御机制（如指令净化或鲁棒训练）。

（完）
