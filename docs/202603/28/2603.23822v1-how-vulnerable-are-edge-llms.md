---
title: How Vulnerable Are Edge LLMs?
title_zh: 边缘大语言模型有多脆弱？
authors: "Ao Ding, Hongzong Li, Zi Liang, Zhanpeng Shi, Shuxin Zhuang, Shiqin Tang, Rong Feng, Ping Lu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23822v1"
tags: ["query:llm"]
score: 8.0
evidence: 边缘大语言模型在基于查询的知识提取攻击下的脆弱性
tldr: 本研究探讨了边缘部署中量化大语言模型的安全性，重点关注受限查询预算下的知识提取风险。研究发现，尽管量化（如INT8/INT4）引入了噪声，但并未消除底层的语义知识。为此，作者提出了CLIQ框架，通过结构化聚类指令查询来提高语义覆盖并减少冗余。实验证明，CLIQ能更高效地从量化模型中恢复行为，表明量化不足以作为防止知识提取的安全手段，揭示了边缘模型潜在的安全漏洞。
selection_source: fresh_fetch
motivation: 边缘设备上的大模型通常经过量化处理，但其在受限查询预算下的知识提取安全性尚未得到充分研究。
method: 提出 CLIQ 框架，通过聚类指令查询来提高语义覆盖范围并减少冗余，从而实现更高效的知识提取。
result: 在 Qwen 模型（INT8/INT4）上的实验表明，CLIQ 在 BERTScore、BLEU 和 ROUGE 指标上均优于原始查询。
conclusion: 量化本身并不能有效防止基于查询的知识提取，揭示了边缘部署大模型中一个此前被忽视的安全风险。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在具有严格计算和量化约束的边缘设备上，然而其安全性影响尚不明确。我们研究了在现实查询预算下，从量化的边缘部署 LLMs 中进行基于查询的知识提取，并证明尽管量化引入了噪声，但它并未消除底层的语义知识，从而允许通过精心设计的查询实现实质性的行为恢复。为了系统地分析这一风险，我们提出了 CLIQ（Clustered Instruction Querying，聚类指令查询），这是一个结构化的查询构建框架，旨在提高语义覆盖范围并减少冗余。在量化 Qwen 模型（INT8/INT4）上的实验表明，CLIQ 在 BERTScore、BLEU 和 ROUGE 指标上始终优于原始查询，从而在有限预算下实现了更高效的提取。这些结果表明，仅靠量化并不能有效防御基于查询的提取，凸显了边缘部署 LLMs 中一个此前未被充分探索的安全风险。

## Abstract
Large language models (LLMs) are increasingly deployed on edge devices under strict computation and quantization constraints, yet their security implications remain unclear. We study query-based knowledge extraction from quantized edge-deployed LLMs under realistic query budgets and show that, although quantization introduces noise, it does not remove the underlying semantic knowledge, allowing substantial behavioral recovery through carefully designed queries. To systematically analyze this risk, we propose \textbf{CLIQ} (\textbf{Cl}ustered \textbf{I}nstruction \textbf{Q}uerying), a structured query construction framework that improves semantic coverage while reducing redundancy. Experiments on quantized Qwen models (INT8/INT4) demonstrate that CLIQ consistently outperforms original queries across BERTScore, BLEU, and ROUGE, enabling more efficient extraction under limited budgets. These results indicate that quantization alone does not provide effective protection against query-based extraction, highlighting a previously underexplored security risk in edge-deployed LLMs.

---

## 论文详细总结（自动生成）

这篇论文对边缘计算环境下大语言模型（LLM）的安全性进行了深入探讨，以下是结构化的详细总结：

### 1. 核心问题与研究动机
随着大模型向边缘设备（如手机、个人电脑）迁移，为了适应硬件限制，模型通常经过深度量化（如 INT4/INT8）。
*   **研究动机**：业界普遍认为量化带来的离散化噪声和精度损失可能会自然地保护模型免受“模型提取攻击”（Model Extraction）。
*   **核心问题**：在现实的、受限的查询预算（Query Budget）下，攻击者能否通过黑盒交互，从这些“多噪”的量化边缘模型中提取出核心行为知识？
*   **整体含义**：论文揭示了量化并不能作为一种有效的安全防御手段，精心设计的结构化查询依然可以高效地“窃取”模型能力。

### 2. 方法论：CLIQ 框架
作者提出了 **CLIQ (Clustered Instruction Querying，聚类指令查询)** 框架，旨在通过结构化手段最大化语义覆盖并减少冗余。
*   **核心思想**：利用语义聚类将指令空间划分为不同的区域，并为每个区域生成具有代表性的探测查询。
*   **关键技术流程**：
    1.  **语义分区**：使用句子编码器（如 Sentence-BERT）将原始查询池映射到向量空间，通过 `MiniBatchKMeans` 算法将其划分为 $K$ 个语义簇。
    2.  **簇感知查询构建**：针对每个簇，利用强大的 LLM（作为辅助）生成该簇特有的、具有多样性且复杂度受控的指令。这确保了查询能均匀覆盖模型的各项能力，而不是在重复的语义点上浪费预算。
    3.  **行为重建**：将生成的查询输入边缘量化模型（教师），收集其输出，并训练一个紧凑的学生模型（模仿学习）来复现教师的行为。
*   **数学逻辑**：将量化模型建模为 $T_{quant}(x) = T_{full}(x) + \epsilon(x)$。CLIQ 的目标是在存在噪声 $\epsilon(x)$ 的情况下，通过优化查询分布来高效逼近 $T_{full}$ 的行为。

### 3. 实验设计
*   **模型与量化**：
    *   **教师模型**：Qwen2.5-7B，使用 GPTQ 算法量化为 INT4/INT8。
    *   **学生模型**：Qwen3-1.7B、Qwen1.5-4B/7B 等边缘导向的小模型。
*   **数据集/场景**：使用了 Alpaca 和 Dolly 等指令遵循数据集作为原始查询池。
*   **Benchmark 与对比方法**：
    *   **对比方法**：原始查询（Original Queries, OQ），即从自然分布中随机采样。
    *   **评估指标**：BERTScore (F1)、BLEU、ROUGE-1/2/L/Lsum。
*   **查询预算**：设定在 100 到 1000 次查询之间，模拟真实的边缘设备限制。

### 4. 资源与算力
*   **算力说明**：论文提到教师模型通过兼容 OpenAI 的 API 访问（Qwen3-30B 等），学生模型的训练涉及 1.7B 到 7B 参数规模。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100 数量）或总训练时长，但提到了训练参数设置（如 Batch Size 为 64，最大序列长度 512，学习率优化等）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比（CLIQ vs OQ）。
    *   **消融实验**：包括聚类粒度（K=25, 50, 100）、查询预算（100-400）、量化级别（INT4 vs INT8）的影响。
    *   **动态分析**：分析了训练步数与性能提升的关系。
    *   **可视化分析**：使用 UMAP 展示了查询空间的语义覆盖情况。
*   **充分性评价**：实验设计较为全面，涵盖了多种模型架构和量化配置，通过定性和定量双重角度验证了方法的有效性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **量化非防御**：量化虽然引入了噪声，但并未消除底层的语义知识。
*   **结构化查询的优势**：CLIQ 在所有指标上均显著优于随机查询。例如，仅用 300 个结构化查询即可捕获模型的大部分行为。
*   **效率与饱和**：CLIQ 表现出极高的样本效率，性能在 200-300 步训练后迅速趋于饱和，而原始查询则提升缓慢。
*   **关键因素**：决定模型可提取性的核心因素是**查询的设计**，而非模型的量化精度。

### 7. 优点与亮点
*   **视角新颖**：首次系统研究了量化对边缘 LLM 知识提取风险的影响。
*   **实用性强**：针对“有限查询预算”这一现实约束进行优化，比传统的全量蒸馏更接近真实攻击场景。
*   **框架高效**：CLIQ 结合了聚类与生成，有效地解决了指令微调数据中的语义冗余问题。

### 8. 不足与局限
*   **预算下限限制**：在极低预算（如小于 100 次查询）下，所有方法的效果都较差，存在提取门槛。
*   **依赖初始池**：CLIQ 的效果受限于初始查询池的覆盖范围，如果初始池缺失某些能力领域，提取也会存在盲区。
*   **场景覆盖**：实验主要集中在指令遵循任务，尚未验证在复杂工具调用（Tool-use）或带有严苛安全过滤的模型上的表现。
*   **量化损失**：尽管 CLIQ 领先，但 INT4 模型的提取效果上限仍受限于量化本身带来的响应质量下降。

（完）
