---
title: "IH-Challenge: A Training Dataset to Improve Instruction Hierarchy on Frontier LLMs"
title_zh: IH-Challenge：一个用于提升前沿大语言模型指令层级的训练数据集
authors: "Chuan Guo, Juan Felipe Ceron Uribe, Sicheng Zhu, Christopher A. Choquette-Choo, Steph Lin, Nikhil Kandpal, Milad Nasr, Rai, Sam Toyer, Miles Wang, Yaodong Yu, Alex Beutel, Kai Xiao"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10521v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过改进指令层级防御越狱
tldr: 本研究针对大语言模型在处理冲突指令时的优先级排序（指令层级，IH）问题，提出了IH-Challenge强化学习训练数据集。通过在GPT-5-Mini上结合在线对抗样本生成进行微调，显著提升了模型在防御越狱、系统提示词提取及智能体提示词注入方面的鲁棒性。实验表明，该方法在提升安全性的同时减少了过度拒绝，且几乎不影响通用能力，为构建更安全的指令遵循模型提供了重要支持。
selection_source: fresh_fetch
motivation: 指令层级是防御提示词注入等攻击的关键，但由于指令冲突复杂且易导致过度拒绝，训练鲁棒的指令层级行为具有挑战性。
method: 引入了IH-Challenge强化学习数据集，并利用在线对抗样本生成技术对模型进行微调。
result: "模型在多个基准测试上的鲁棒性平均提升了10%，不安全行为比例从6.6%降至0.7%，并成功解决了智能体提示词注入评估中的问题。"
conclusion: IH-Challenge数据集能有效提升前沿大模型的指令层级鲁棒性，是增强模型安全性和可靠性的重要工具。
---

## 摘要
指令层级（IH）定义了大语言模型（LLM）在指令冲突时如何优先处理系统、开发者、用户和工具指令，为解决指令冲突提供了一种具体的、基于信任排序的策略。IH 是防御越狱、系统提示词提取以及智能体提示词注入的关键。然而，鲁棒的 IH 行为难以训练：IH 失效可能与指令遵循失效相混淆，冲突可能非常微妙，且模型可能会学习到诸如过度拒绝之类的捷径。我们推出了 IH-Challenge，这是一个强化学习训练数据集，旨在解决这些难题。在 IH-Challenge 上通过在线对抗样本生成对 GPT-5-Mini 进行微调，在 16 个分布内、分布外及人工红队测试基准中，使 IH 鲁棒性平均提升了 10.0%（从 84.1% 提升至 94.1%），将不安全行为从 6.6% 降低至 0.7%，同时提高了通用安全评估中的帮助性，并在内部静态智能体提示词注入评估中达到饱和，且能力退化极小。我们发布了 IH-Challenge 数据集（https://huggingface.co/datasets/openai/ih-challenge），以支持未来关于鲁棒指令层级的研究。

## Abstract
Instruction hierarchy (IH) defines how LLMs prioritize system, developer, user, and tool instructions under conflict, providing a concrete, trust-ordered policy for resolving instruction conflicts. IH is key to defending against jailbreaks, system prompt extractions, and agentic prompt injections. However, robust IH behavior is difficult to train: IH failures can be confounded with instruction-following failures, conflicts can be nuanced, and models can learn shortcuts such as overrefusing. We introduce IH-Challenge, a reinforcement learning training dataset, to address these difficulties. Fine-tuning GPT-5-Mini on IH-Challenge with online adversarial example generation improves IH robustness by +10.0% on average across 16 in-distribution, out-of-distribution, and human red-teaming benchmarks (84.1% to 94.1%), reduces unsafe behavior from 6.6% to 0.7% while improving helpfulness on general safety evaluations, and saturates an internal static agentic prompt injection evaluation, with minimal capability regression. We release the IH-Challenge dataset (https://huggingface.co/datasets/openai/ih-challenge) to support future research on robust instruction hierarchy.

---

## 论文详细总结（自动生成）

这是一份关于 OpenAI 论文《IH-Challenge: A Training Dataset to Improve Instruction Hierarchy on Frontier LLMs》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现代大语言模型（LLM）接收来自不同角色的指令（系统、开发者、用户、工具）。当这些指令发生冲突时（例如用户要求模型忽略系统设定的安全准则），模型需要一套明确的优先级策略，即**指令层级（Instruction Hierarchy, IH）**。
*   **核心问题**：现有的 IH 训练面临三大挑战：1) IH 失败常与指令遵循能力不足混淆；2) 指令冲突极其微妙，难以定义；3) 模型容易学习到“过度拒绝”（Overrefusal）等捷径。
*   **整体含义**：本文旨在通过构建一个高质量、可程序化评测的强化学习（RL）数据集 **IH-Challenge**，训练模型在复杂冲突下严格遵守 `系统 > 开发者 > 用户 > 工具` 的优先级，从而从根本上防御越狱、提示词提取和提示词注入攻击。

### 2. 论文提出的方法论
核心思想是利用**强化学习（RL）**结合**在线对抗样本生成**来提升模型的 IH 鲁棒性。
*   **IH-Challenge 数据集构建原则**：
    1.  **IF-simple（指令遵循简单化）**：确保任务本身的逻辑很简单（如“输出包含某个词”），难点仅在于识别并忽略低优先级的干扰指令。
    2.  **程序化可评测（Programmatically Gradable）**：每个任务配有 Python 评测脚本，避免使用易受“奖励黑客”攻击的 LLM 评委。
    3.  **避免快捷键学习**：通过引入“反过度拒绝”任务（即低优先级指令不冲突时必须执行），防止模型学会“看到敏感词就拒绝”的简单策略。
*   **关键技术流程**：
    *   **离线任务骨架**：预先生成包含高优先级指令和 Python 评测器的模板。
    *   **在线对抗合成**：在训练过程中，使用一个“攻击者模型”（Attacker LLM）实时针对当前的“防御者模型”生成最具威胁的低优先级冲突指令。
    *   **RL 训练**：防御者模型在这些动态生成的冲突场景中进行采样，根据 Python 评测器的反馈进行策略梯度更新。

### 3. 实验设计
*   **模型对象**：以 **GPT-5-Mini** 为基础模型，训练后的版本称为 **GPT-5-Mini-R**。
*   **数据集与场景**：
    *   **IH-Challenge 四大子集**：单约束、多约束、输入条件化、反过度拒绝。
    *   **分布外（OOD）评估**：包括内部的导师越狱测试、系统/用户冲突测试。
    *   **学术 Benchmark**：Gandalf Password, TensorTrust, RealGuardrails, System IFEval, CyberSecEval 2。
*   **对比方法**：
    *   原始 GPT-5-Mini。
    *   多种系统级防御手段：三明治防御（Sandwich）、系统提示词强化、输出监控器（Output Monitor）、输出重写器、Best-of-4 采样。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、集群规模、训练时长或总算力消耗。考虑到其基于 GPT-5-Mini 进行 RL 微调，且涉及在线对抗生成，推测需要相当规模的计算资源，但具体数值缺失。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 16 个不同的基准测试，包括分布内、分布外以及学术公开数据集。
*   **充分性**：
    *   **多维度评估**：不仅测试了 IH 鲁棒性，还评估了通用能力（GPQA, AIME）、聊天胜率、安全 steerability 以及提示词注入防御。
    *   **消融实验**：对数据集的四个组成部分进行了消融，验证了“反过度拒绝”子集对保持模型帮助性的关键作用。
    *   **红队测试**：引入了自适应自动红队（RL 攻击者）和**自适应人工红队**（127 名受聘专家），实验设计非常严谨且具有对抗性。
*   **客观性**：通过 Python 脚本进行客观打分，减少了主观偏见。

### 6. 论文的主要结论与发现
*   **鲁棒性大幅提升**：GPT-5-Mini-R 在 16 个基准上的平均鲁棒性从 84.1% 提升至 **94.1%**。
*   **安全性与帮助性的平衡**：在提供系统安全规范时，不安全行为从 6.6% 降至 **0.7%**，且没有显著牺牲模型的帮助性。
*   **提示词注入防御**：在内部智能体提示词注入评估中达到**饱和（100% 成功防御）**。
*   **系统防御的局限性**：发现对于已经过 IH 训练的强模型，额外的系统级防御（如输出监控）提升有限，甚至可能损害模型能力。
*   **泛化能力**：在 IH-Challenge 上训练的简单任务能够泛化到复杂的安全场景和未见过的学术测试集。

### 7. 优点
*   **方法论严谨**：通过 Python 评测器解决了 RL 训练中常见的奖励黑客（Reward Hacking）问题。
*   **实战导向**：不仅关注学术指标，还通过人工红队和生产环境模拟验证了实际防御效果。
*   **开源贡献**：公开发布了 IH-Challenge 数据集，为社区研究指令层级提供了标准工具。
*   **多效合一**：证明了提升指令层级这一单一目标，可以同时解决越狱、提示词注入和安全引导等多个安全难题。

### 8. 不足与局限
*   **任务多样性受限**：由于要求“程序化可评测”，数据集中的任务类型（如 JSON 格式、关键词检查）相对单一，可能无法完全覆盖所有复杂的语义冲突。
*   **能力微损**：实验显示模型在聊天胜率和用户偏好得分上有轻微下降（约 5-6%）。
*   **对抗博弈的动态性**：虽然模型在静态评估中达到饱和，但自适应攻击实验显示，随着攻击者算力的增加，防御仍有被突破的可能。
*   **算力透明度**：缺乏具体的训练资源描述，不利于其他研究者估算复现成本。

（完）
