---
title: "Pruning Unsafe Tickets: A Resource-Efficient Framework for Safer and More Robust LLMs"
title_zh: 剪枝不安全票据：一种用于构建更安全、更鲁棒大语言模型的资源高效框架
authors: "Wai Man Si, Mingjie Li, Michael Backes, Yang Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15780v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 通过移除与不安全行为相关的参数来提高大语言模型的安全性
tldr: 尽管现有大模型经过对齐，仍存在预训练阶段残留的不安全行为。本文提出一种资源高效的剪枝框架，通过识别并移除模型中与不安全行为相关的“不安全票据（Unsafe Tickets）”来增强安全性。该方法采用无梯度归因机制，仅需少量计算资源即可在多种架构和量化模型上实现后验对齐。实验表明，该方法在保持模型性能的同时，显著降低了有害输出并提升了对抗诱导攻击的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的SFT和RLHF对齐方法主要侧重于引导生成偏好响应，而未能从模型内部彻底移除触发有害输出的子网络。
method: 提出一种基于无梯度归因机制的轻量级剪枝框架，直接定位并剔除与不安全行为相关的参数子集。
result: 在Mistral和LLaVA等模型上的实验证明，该方法在极低性能损耗下显著减少了不安全生成，并增强了防御越狱攻击的能力。
conclusion: 研究验证了模型中存在负责有害行为的“不安全票据”，通过剪枝可揭示出兼顾性能与安全性的“安全票据”，为资源受限环境下的模型对齐提供了新方案。
---

## 摘要
机器学习模型正越来越多地部署在现实应用中，但即使是像 Mistral 和 LLaVA 这样经过对齐的模型，仍然表现出从预训练中继承的不安全行为。目前的对齐方法（如 SFT 和 RLHF）主要鼓励模型生成首选响应，但并未明确移除触发有害输出的不安全子网络。在这项工作中，我们引入了一种资源高效的剪枝框架，该框架在保持模型效用的同时，直接识别并移除与不安全行为相关的参数。我们的方法采用了一种无梯度归因机制，仅需少量的 GPU 资源，并且可以推广到不同的架构和量化变体。对机器学习模型的实证评估表明，该方法显著减少了不安全内容的生成，并提高了针对越狱攻击的鲁棒性，且效用损失极小。从彩票假设（Lottery Ticket Hypothesis）的角度来看，我们的结果表明机器学习模型包含负责有害行为的“不安全票据”，而剪枝则揭示了在保持性能的同时实现输出对齐的“安全票据”。这提供了一种轻量级的后验对齐策略，适用于资源受限环境下的部署。

## Abstract
Machine learning models are increasingly deployed in real-world applications, but even aligned models such as Mistral and LLaVA still exhibit unsafe behaviors inherited from pre-training. Current alignment methods like SFT and RLHF primarily encourage models to generate preferred responses, but do not explicitly remove the unsafe subnetworks that trigger harmful outputs. In this work, we introduce a resource-efficient pruning framework that directly identifies and removes parameters associated with unsafe behaviors while preserving model utility. Our method employs a gradient-free attribution mechanism, requiring only modest GPU resources, and generalizes across architectures and quantized variants. Empirical evaluations on ML models show substantial reductions in unsafe generations and improved robustness against jailbreak attacks, with minimal utility loss. From the perspective of the Lottery Ticket Hypothesis, our results suggest that ML models contain "unsafe tickets" responsible for harmful behaviors, and pruning reveals "safety tickets" that maintain performance while aligning outputs. This provides a lightweight, post-hoc alignment strategy suitable for deployment in resource-constrained settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pruning Unsafe Tickets: A Resource-Efficient Framework for Safer and More Robust LLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管现有的大语言模型（LLMs）经过了指令微调（SFT）和人类反馈强化学习（RLHF）的对齐，但它们仍保留了预训练阶段吸收的有害知识。现有的对齐方法主要通过“增加”正面行为来掩盖负面行为，而非从模型内部“移除”产生有害输出的根源。
*   **研究动机**：作者认为模型内部存在特定的子网络（即“不安全票据”），负责触发有害响应。通过识别并剪掉这些子网络，可以从根本上提高模型的安全性，且这种方法比重新训练或大规模微调更具资源效率。

### 2. 论文提出的方法论
*   **核心思想**：基于“彩票假设（Lottery Ticket Hypothesis）”，提出模型中存在负责有害行为的子网络。通过一种**无梯度归因机制**定位这些参数并将其剔除，从而留下既能保持性能又能抵御攻击的“安全票据”。
*   **关键技术细节**：
    *   **不安全票据识别**：利用少量有害提示词（Prompts）触发模型，通过观察神经元的激活状态或权重贡献度，识别与有害输出高度相关的参数。
    *   **无梯度归因**：为了降低计算开销，该方法不依赖复杂的反向传播梯度计算，而是通过分析模型在处理有害输入时的内部表示差异来定位关键权重。
    *   **剪枝策略**：对识别出的“不安全”参数进行掩码处理（Masking）或直接置零，实现后验（Post-hoc）对齐。
    *   **跨架构适用性**：该框架设计为通用型，可直接应用于全精度模型、量化模型（如 4-bit/8-bit）以及多模态模型（如 LLaVA）。

### 3. 实验设计
*   **实验对象（Models）**：Mistral-7B（纯文本）和 LLaVA-1.5-7B（多模态）。
*   **安全评估数据集**：使用 **HarmBench** 和 **AdvBench** 等主流安全基准测试。
*   **攻击场景**：测试了针对**越狱攻击（Jailbreak Attacks）**的鲁棒性，包括 GCG（贪婪坐标梯度）等对抗性攻击。
*   **效用评估（Utility Benchmarks）**：为了确保剪枝不破坏模型能力，使用了 **MMLU**（综合知识）和 **GSM8K**（数学推理）进行性能对齐测试。
*   **对比方法**：主要对比了原始已对齐模型（Baseline）以及传统的安全对齐手段。

### 4. 资源与算力
*   **算力需求**：论文强调了“资源高效（Resource-Efficient）”。
*   **具体细节**：虽然文中未详细列出总耗时，但明确指出该方法**仅需少量的 GPU 资源**（Modest GPU resources），因为它避免了昂贵的梯度计算和大规模重新训练。这种轻量级特性使其能够在消费级硬件或资源受限的环境下执行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了文本和多模态两种主流架构。
    *   测试了不同量化水平下的表现。
    *   进行了消融实验，验证剪枝比例对“安全-性能”平衡的影响。
*   **充分性评价**：实验设计较为全面，不仅关注了有害输出的减少，还深入探讨了对抗攻击下的防御能力，并严格监控了模型通用能力的损耗，证明了该方法的客观性和实用性。

### 6. 论文的主要结论与发现
*   **不安全票据的存在性**：证实了 LLM 内部确实存在专门负责有害行为的参数子集。
*   **剪枝的有效性**：通过移除极少比例的参数，可以显著降低模型生成有害内容的概率，并大幅提升其抵御越狱攻击的韧性。
*   **性能保持**：在大幅提升安全性的同时，模型在 MMLU 和 GSM8K 等任务上的性能下降极小，实现了高效的“安全-效用”权衡。
*   **量化兼容性**：该方法在量化模型上依然有效，这对于端侧部署具有重要意义。

### 7. 优点
*   **高效轻量**：无需梯度，计算成本极低，适合快速部署。
*   **根源治理**：不同于传统的“掩盖式”对齐，该方法尝试从参数层面“切除”病灶。
*   **通用性强**：支持量化模型和多模态模型，应用范围广。
*   **防御力强**：对复杂的对抗性越狱攻击表现出比传统对齐方法更好的鲁棒性。

### 8. 不足与局限
*   **剪枝精度的挑战**：如果剪枝比例设置不当，可能会误伤负责复杂逻辑推理的参数，导致模型在极难任务上的表现受损。
*   **有害定义的覆盖面**：实验主要集中在已知的有害类别（如 HarmBench 定义的类别），对于不断演变的、隐晦的有害模式，可能需要持续更新触发提示词库。
*   **长文本影响**：论文未详细讨论剪枝对超长文本生成连贯性的潜在细微影响。

（完）
