---
title: "EVA: Editing for Versatile Alignment against Jailbreaks"
title_zh: EVA：针对越狱攻击的通用对齐编辑
authors: "Yi Wang, Hongye Qiu, Yue Xu, Sibei Yang, Zhan Qin, Minlie Huang, Wenjie Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14750v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过直接模型编辑进行针对越狱的安全对齐
tldr: 针对大语言模型（LLM）和视觉语言模型（VLM）易受越狱攻击的问题，本文提出了一种名为EVA的新型框架。EVA首次将模型编辑技术应用于安全对齐，将其视为精确的知识修正任务。通过定位并修改负责有害指令的特定神经元，EVA在不重新训练大量参数的情况下，有效中和了有害行为，同时保持了模型的通用推理能力，为部署后的安全对齐提供了高效且精准的解决方案。
selection_source: fresh_fetch
motivation: 现有的安全防御方法如微调或外部过滤存在计算开销大且容易损害模型通用性能的问题。
method: EVA将安全对齐视为知识修正任务，通过定位并手术式地编辑负责有害响应的特定神经元来消除安全隐患。
result: 实验证明EVA在LLM和VLM上均能有效抵御越狱攻击，且在缓解有害行为的同时保持了优异的通用推理能力。
conclusion: EVA为模型部署后的安全对齐提供了一种精准且高效的新途径，证明了模型编辑在提升安全性方面的巨大潜力。
---

## 摘要
大语言模型 (LLMs) 和视觉语言模型 (VLMs) 展示了令人印象深刻的能力，但仍然容易受到越狱攻击的影响，攻击者利用文本或视觉触发器来绕过安全护栏。最近的防御措施通常依赖于安全微调或外部过滤器，以降低模型产生有害内容的可能性。虽然在一定程度上有效，但这些方法通常会产生显著的计算开销，并面临安全与效用之间的权衡，从而降低模型在良性任务上的性能。为了应对这些挑战，我们提出了 EVA（针对越狱攻击的通用对齐编辑），这是一个开创性地将直接模型编辑应用于安全对齐的新型框架。EVA 将安全对齐重新定义为一个精确的知识修正任务。EVA 并非重新训练海量参数，而是识别并精确编辑负责模型对有害指令敏感性的特定神经元，同时保持模型的绝大部分不变。通过局部化更新，EVA 在不损害模型通用推理能力的情况下，有效地中和了有害行为。大量实验表明，EVA 在缓解 LLMs 和 VLMs 的越狱攻击方面优于基准方法，为部署后的安全对齐提供了一种精确且高效的解决方案。

## Abstract
Large Language Models (LLMs) and Vision Language Models (VLMs) have demonstrated impressive capabilities but remain vulnerable to jailbreaking attacks, where adversaries exploit textual or visual triggers to bypass safety guardrails. Recent defenses typically rely on safety fine-tuning or external filters to reduce the model's likelihood of producing harmful content. While effective to some extent, these methods often incur significant computational overheads and suffer from the safety utility trade-off, degrading the model's performance on benign tasks. To address these challenges, we propose EVA (Editing for Versatile Alignment against Jailbreaks), a novel framework that pioneers the application of direct model editing for safety alignment. EVA reframes safety alignment as a precise knowledge correction task. Instead of retraining massive parameters, EVA identifies and surgically edits specific neurons responsible for the model's susceptibility to harmful instructions, while leaving the vast majority of the model unchanged. By localizing the updates, EVA effectively neutralizes harmful behaviors without compromising the model's general reasoning capabilities. Extensive experiments demonstrate that EVA outperforms baselines in mitigating jailbreaks across both LLMs and VLMs, offering a precise and efficient solution for post-deployment safety alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《EVA: Editing for Versatile Alignment against Jailbreaks》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 和 VLM 尽管经过了安全对齐（如 RLHF/DPO），但仍易受到越狱攻击（Jailbreak Attacks）的威胁。
*   **研究动机**：
    *   **现有防御的局限性**：内部权重微调（Fine-tuning）计算开销大且容易导致“灾难性遗忘”，损害模型在良性任务上的性能（安全-效用权衡）；外部过滤器则会增加推理延迟。
    *   **内在机制发现**：研究表明越狱攻击并非因为模型缺乏安全知识，而是因为攻击触发了错误的内部激活模式。
*   **整体含义**：本文提出 **EVA** 框架，首次将**模型编辑（Model Editing）**技术引入安全对齐领域，通过“手术式”修改特定神经元来纠正有害激活，实现高效、精准且不损性能的防御。

### 2. 方法论：核心思想与关键技术
EVA 将安全对齐视为一个精确的知识修正任务，分为三个阶段：
1.  **有害键（Harmful Keys $k^*$）识别**：
    *   **文本模态**：利用辅助模型提取有害核心词（如“hack”），通过生成多样化上下文并聚合 MLP 层的中间激活，形成鲁棒的文本键表示。
    *   **视觉模态**：利用跨模态注意力机制（Cross-modal Attention），定位与有害文本关联度最高的图像 Token，提取其视觉键表示。
2.  **安全目标值（Target Values $v^*$）优化**：
    *   在保持模型权重冻结的情况下，优化一个目标向量 $v^*$。
    *   **损失函数**：结合了最大化安全拒绝回答的概率（$L_{safe}$）和基于 **KL 散度**的正则化项（$L_{stab}$），后者确保模型在处理良性查询时的分布偏移最小化。
3.  **统一权重更新**：
    *   将 MLP 的下投影矩阵 $W_{down}$ 视为键值存储器。
    *   使用**闭式解（Closed-form solution）**的最小二乘法，将识别出的有害键映射到优化的安全值上。
    *   采用分层更新策略，将修改分布在多个关键层，以增强编辑的稳定性。

### 3. 实验设计
*   **实验对象（Models）**：
    *   **LLMs**: Vicuna-7B, Llama2-7B, Mistral-7B, Llama3.1-8B, Qwen2.5-7B。
    *   **VLMs**: LLaVA-1.5-7B, Qwen2.5-VL-7B, InternVL3.5-8B。
*   **数据集与 Benchmark**：
    *   **安全评估**：HarmBench, AdvBench, JailbreakBench, MaliciousInstruct (文本)；MM-SafetyBench, MultiTrust (视觉)。
    *   **效用评估**：MT-Bench (通用对话)；BoolQ, GSM8K, CoNLL03 等 7 项下游任务 (文本)；MM-Vet, MMMU, MMStar (视觉)。
*   **对比方法**：
    *   **LLM 防御**：LoRA 微调、SafeDecoding (解码干预)、LED (层特定编辑)、Circuit Breakers (表征防御)。
    *   **VLM 防御**：JailGuard, AdaShield, VLGuard。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA A40 GPU** 上完成。
*   **训练/编辑时长**：
    *   **LLM (Vicuna-7B)**：编辑训练仅需约 **0.4 小时**。
    *   **VLM (InternVL3.5-8B)**：编辑训练约 **0.8 小时**。
*   **推理开销**：由于是直接修改权重，EVA 在推理阶段的额外开销为 **0**（1× overhead），优于需要辅助模块的方法。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种 LLM 和 3 种 VLM，测试了包括 GCG、AutoDAN、PAIR 在内的多种先进攻击手段。
*   **消融实验**：非常详尽，包括关键层选择、视觉 Token 选择策略（对比了随机、固定、距离、梯度等 6 种方法）、正则化指标对比、优化目标（通用拒绝 vs. 特定拒绝）对比。
*   **充分性与公平性**：实验不仅关注攻击成功率（ASR），还深入探讨了跨类别泛化性、自适应攻击（Adaptive Attacks）下的鲁棒性，以及内部表征的可解释性分析（PCA 可视化），实验设计客观且全面。

### 6. 主要结论与发现
*   **极高的防御效能**：EVA 在多个模型上将 ASR 降至接近 0%，显著优于 LoRA 和 LED 等基线。
*   **优异的效用保持**：在提升安全性的同时，模型在 MT-Bench 和下游推理任务上的得分几乎无损，甚至在某些任务上略有提升。
*   **跨模态协同**：证明了视觉越狱的根源可以定位到少数关键视觉 Token，通过编辑这些 Token 即可阻断多模态攻击。
*   **高效性**：相比于全量或参数高效微调，EVA 消耗的算力和数据量极小，且支持顺序更新以应对新出现的威胁。

### 7. 优点（亮点）
*   **首创性**：这是首个将直接模型编辑应用于 VLM 安全对齐的工作。
*   **精准性**：通过注意力机制定位视觉有害触发器，避免了对视觉编码器的盲目修改。
*   **零推理延迟**：闭式权重更新确保了部署后的模型运行速度与原始模型一致。
*   **泛化能力**：实验展示了“编辑一个类别，防御多个类别”的跨行为泛化特性。

### 8. 不足与局限
*   **依赖辅助模型**：在识别有害 Token 阶段需要 GPT-4o 或高性能开源模型进行预处理，这增加了流程的复杂性。
*   **静态拒绝模板**：目前主要采用统一的拒绝语（Universal Refusal），虽然防御力强，但相比于多样化的解释性拒绝，用户体验可能略显生硬。
*   **动态输入挑战**：目前主要针对静态图像和文本，对于视频等动态多模态输入的适用性尚未验证。
*   **潜在的逆向风险**：虽然论文未提及，但模型编辑技术若被恶意利用，也可能被用于快速植入后门或移除安全护栏。

（完）
