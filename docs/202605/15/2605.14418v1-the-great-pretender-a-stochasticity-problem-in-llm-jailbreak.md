---
title: "The Great Pretender: A Stochasticity Problem in LLM Jailbreak"
title_zh: 伟大的伪装者：大语言模型越狱中的随机性问题
authors: "Jean-Philippe Monteuuis, Cong Chen, Jonathan Petit"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14418v1"
tags: ["query:llm"]
score: 8.0
evidence: 大语言模型越狱创建与评估中的随机性问题
tldr: 本研究揭示了大型语言模型（LLM）越狱攻击中存在的随机性问题，指出传统的攻击成功率（ASR）指标因不稳定性而存在虚高现象。作者通过研究攻击生成和评估过程中的随机性，提出了新的评估框架 CAS-eval 和生成框架 CAS-gen。研究发现，当要求越狱提示词在多次尝试中保持一致成功时，ASR 会大幅下降，而 CAS-gen 能有效提升攻击的鲁棒性，弥补这一性能损失。
selection_source: fresh_fetch
motivation: 现有的 LLM 越狱攻击成功率（ASR）指标受随机性影响严重，导致实验结果不稳定且难以在不同研究间进行公平比较。
method: 提出了 CAS-eval 评估框架来量化随机性对 ASR 的影响，并开发了 CAS-gen 框架以优化生成更具鲁棒性的越狱提示词。
result: 实验表明，在多次尝试的要求下，传统攻击的 ASR 可能下降多达 30 个百分点，而 CAS-gen 框架能显著恢复这些损失。
conclusion: 越狱研究应关注攻击的稳定性而非单一的 ASR 数值，通过考虑随机性可以更真实地评估并提升 LLM 的安全性防御。
---

## 摘要
“噢，是的，我是伟大的伪装者。假装我做得很好。我的需求如此之大，我伪装得太多……”这句歌词总结了越狱生成与评估领域的现状。你发现这种由知名机构（如 Anthropic 的 BoN 或微软研究院的 Crescendo）提出的对抗性攻击生成方法，尽管在针对工业级大语言模型（LLM）时拥有极高的攻击成功率（ASR）得分，但并未兑现论文中所宣称的承诺。你成功针对目标（开源）模型生成了越狱提示词，然而，尽管在论文中针对带有护栏系统的最新闭源模型拥有 80% 的 ASR，生成的越狱提示词在针对目标模型时的连续成功率仅为 50%（10 次尝试中成功 5 次）！这一观察引发了我们的思考。首先，作为 LLM 越狱基准测试的主要指标，攻击成功率（ASR）并不是一个稳定的量。其次，已发表的 ASR 数值因此存在系统性虚高，且在不同论文之间不可比。因此，我们想知道：“为什么一个成功的越狱提示词在针对其优化过的目标模型时，表现并不一致？”为了回答这个问题，我们不仅研究了攻击评估过程中的随机性影响，还研究了攻击生成过程中的随机性影响。我们的评估涵盖了多种越狱攻击、不同规模和供应商的模型以及评判器。此外，我们提出了一个新的指标和两个新框架（CAS-eval 和 CAS-gen）。我们的评估框架 CAS-eval 表明，当越狱提示词需要在多次尝试中均获得成功时，攻击的 ASR 可能会下降多达 30 个百分点。值得庆幸的是，我们的攻击生成框架（CAS-gen）改进了以往的越狱方法，并帮助它们弥补了这 30 个百分点的损失！

## Abstract
"Oh-Oh, yes, I'm the great pretender. Pretending that I'm doing well. My need is such, I pretend too much..." summarizes the state in the area of jailbreak creation and evaluation. You find this method to generate adversarial attacks proposed by a reputable institution (e.g., BoN from Anthropic or Crescendo from Microsoft Research). However, this method does not deliver on the promise claimed in the paper despite having top ASR scores against industry-grade LLMs. You successfully generate the jailbreak prompts against your target (open) model. However, the generated jailbreak prompt works against the target model with a 50% consecutive success rate (5 out of 10 attempts) despite having an 80% ASR (on paper) on the latest closed-source model (with a guardrail system)! This observation leads us to think. First, Attack Success Rate (ASR), the primary metric for LLM jailbreak benchmarking, is not a stable quantity. Second, published ASR numbers are therefore systematically inflated and incomparable across papers. Therefore, we wonder "Why a successful jailbreak prompt does not perform consistently well against a target model on which the prompts have been optimized?". To answer this question, we study the impact of stochasticity not only during attack evaluation but also during attack generation. Our evaluation includes several jailbreak attacks, models (different sizes and providers), and judges. In addition, we propose a new metric and two new frameworks (CAS-eval and CAS-gen). Our evaluation framework, CAS-eval, shows that an attack can have an ASR drop of up to 30 percentage points when a jailbreak prompt needs to succeed on more than one attempt. Thankfully, our attack generation framework (CAS-gen) improves previous jailbreak methods and helps them recover this loss of 30 percentage points!

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）越狱攻击评估中的随机性问题，指出当前学术界通用的“攻击成功率（ASR）”指标存在严重的虚高和不可比性。

### 1. 研究动机与核心问题
*   **核心问题**：为什么在论文中声称具有高 ASR 的越狱方法，在实际复现时成功率往往大幅缩水？
*   **研究动机**：作者发现 ASR 并非攻击方法的固定属性，而是一个受随机性影响的变量。这种随机性主要源于两个方面：
    1.  **生成随机性**：大多数越狱攻击（如 Best-of-N）本质上是随机采样，不同的随机种子会导致完全不同的结果。
    2.  **评估随机性**：基于 LLM 的评判器（如 Llama-Guard）在温度（Temperature）大于 0 时，对同一响应的判定结果并不一致。
*   **整体含义**：论文旨在揭示 ASR 指标的脆弱性，并提出一套更稳定、更具鲁棒性的评估与生成框架。

### 2. 方法论：CAS 框架
作者提出了 **CAS (Consistency for Attack Success，攻击成功一致性)** 指标及其应用框架：
*   **核心思想**：将“单次成功即判定为越狱”改为“连续 $k$ 次成功才判定为越狱”。
*   **CAS 指标公式**：$CAS(r, k) = \prod_{j=1}^{k} r_j$，其中 $r_j \in \{0, 1\}$ 是评判器在第 $j$ 次独立评估中的判定结果。只有当 $k$ 次尝试全部为“有害”时，该提示词才被视为成功。
*   **CAS-eval（评估框架）**：针对固定的越狱提示词，进行 $k_{eval}$ 次独立评估，消除评判器非确定性带来的假阳性。
*   **CAS-gen（生成框架）**：在攻击生成阶段，要求候选提示词必须连续 $k_{gen}$ 次触发有害响应才能被采纳，从而过滤掉靠运气获得的“偶然成功”，生成更具鲁棒性的攻击。

### 3. 实验设计
*   **数据集**：使用 **JailbreakBench** 的子集。
*   **对比方法（攻击方式）**：Best-of-N (BoN)、PAIR、TAP、Crescendo。
*   **目标模型**：涵盖不同规模和厂商的 5 个模型：Llama-3.2-1B、Llama-3.1-8B、Llama-3.1-70B、Gemma-3-1B、Granite-4.0-1B。
*   **评判器（Judges）**：Llama-Guard-3-1B 和 Llama-Guard-3-8B。
*   **变量控制**：系统性地改变了 6 个随机性参数：目标模型温度 ($T_{gen}, T_{eval}$)、评判器温度 ($\theta_{gen}, \theta_{eval}$)、一致性阈值 ($k_{gen}, k_{eval}$) 以及随机种子。

### 4. 资源与算力
*   **硬件设备**：使用了包含 **2 台 Nvidia A100 (80GB RAM)** 的集群。
*   **算力分配**：大多数实验可在单台 A100 上完成，涉及 70B 目标模型与 8B 评判器的组合实验则需要两台 A100 协作。
*   **训练/运行时间**：文中未明确给出总耗时，但提到 CAS 框架带来的额外开销在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了跨模型、跨攻击方法、跨评判器的多维度对比。每组实验运行了 5 个独立种子以量化方差。
*   **充分性与客观性**：实验设计非常详尽，通过热图（Heatmap）和消融实验展示了不同参数对 ASR 的影响。研究不仅关注攻击者视角，也关注评估者视角，对比了开源模型与不同规模评判器的表现，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **ASR 虚高严重**：当要求连续 10 次成功（$k=10$）而非单次成功（$k=1$）时，ASR 会下降 **12 到 24 个百分点**。
*   **评判器温度是主因**：将评判器温度从 0 调至 1.0，会导致 ASR 下降多达 **54 个百分点**。
*   **生成预算的影响**：通过 CAS-gen 提高生成时的门槛（$k_{gen}=10$），可以使 ASR 提升 **12 到 30 个百分点**，从而弥补评估时的性能损失。
*   **标准缺失**：目前大多数论文不报告评判器温度和随机种子，导致实验结果几乎无法公平比较。

### 7. 优点与亮点
*   **揭露行业痛点**：首次系统性地量化了越狱攻击中的随机性，挑战了当前 ASR 指标的权威性。
*   **实用性强**：提出的 CAS-gen 框架不仅是评估工具，还能直接用于提升越狱攻击的质量，使其在实际应用中更可靠。
*   **严谨的统计学方法**：使用了 Wilson 得分区间来处理 ASR 接近 0 或 1 时的置信度问题，比传统的正态近似更准确。

### 8. 不足与局限
*   **攻击覆盖有限**：主要关注基于采样的黑盒攻击，未涵盖基于梯度的攻击（如 GCG），后者的随机性特征可能不同。
*   **模型范围限制**：实验主要在开源模型上进行，由于 API 限制和合规性原因，未在 GPT-4o 或 Claude 等重度对齐的闭源模型上进行大规模验证。
*   **评判器类型**：主要使用了分类器型评判器（Llama-Guard），未深入探讨生成式评判器（如 GPT-4 作为评判器）的温度敏感性。

（完）
