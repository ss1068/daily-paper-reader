---
title: "MASCing: Configurable Mixture-of-Experts Behavior via Activation Steering Masks"
title_zh: MASCing：通过激活引导掩码实现可配置的混合专家模型行为
authors: "Jona te Lintelo, Lichao Wu, Marina Krček, Sengim Karayalçin, Stjepan Picek"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27818v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 针对安全目标灵活重新配置MoE行为
tldr: 针对混合专家模型（MoE）中行为受路由决策影响且难以控制的问题，本文提出MASCing框架。该框架无需重新训练，通过LSTM代理模型捕捉跨层路由依赖，并利用转向掩码在推理时干预专家选择。实验表明，MASCing能有效增强多轮越狱防御并灵活调整内容生成策略，为MoE模型的安全配置提供了一种轻量且高效的方案。
selection_source: fresh_fetch
motivation: MoE模型的稀疏激活机制导致行为与路由耦合，且通过微调来快速配置不同安全目标成本过高。
method: 利用LSTM代理模型识别行为相关的专家电路，并在推理时通过转向掩码覆盖原始路由决策以引导模型行为。
result: "在七个开源MoE模型上，将越狱防御成功率从52.5%提升至83.9%，并将特定内容生成成功率从52.6%提升至82.0%。"
conclusion: MASCing是一个实用、轻量且灵活的框架，能够在不损失通用语言能力的前提下实现MoE模型的场景化安全重构。
---

## 摘要
大语言模型（LLM）中的混合专家（MoE）架构通过稀疏激活显著降低了推理成本。然而，这种稀疏激活范式也带来了新的安全挑战。由于每个输入仅激活一部分专家子集，模型行为与路由决策耦合在一起，产生了一种难以控制的机制，这种机制在不同的安全相关场景中可能会有所不同。同时，通过全量微调或重新训练来调整模型行为的成本很高，特别是当开发人员需要针对不同的安全目标快速配置同一模型时。我们提出了 MASCing（MoE 激活引导配置），这是第一个无需重新训练即可在多种安全场景中灵活重新配置 MoE 行为的框架。MASCing 使用基于 LSTM 的代理模型来捕获跨层路由依赖关系，并将路由 logits 映射到下游行为。然后，它优化一个引导矩阵以识别与行为相关的专家电路，并在推理时将引导掩码应用于路由门以覆盖专家选择。这使得在保持通用语言效用的同时，能够有针对性地增强或抑制特定行为。为了证明其可配置性，我们将 MASCing 应用于两个不同的安全相关目标，并在七个开源 MoE 模型中观察到了持续的增益，且开销可以忽略不计。对于多轮越狱防御，它将平均防御成功率从 52.5% 提高到 83.9%，增幅高达 89.2%。对于成人内容生成，MASCing 使模型能够遵循原本会被拒绝的此类请求，将平均生成成功率从 52.6% 提高到 82.0%，增幅高达 93.0%。这些结果表明，MASCing 是一个实用、轻量且灵活的框架，适用于 MoE 模型中特定场景的安全重新配置。

## Abstract
Mixture-of-Experts (MoE) architectures in Large Language Models (LLMs) have significantly reduced inference costs through sparse activation. However, this sparse activation paradigm also introduces new safety challenges. Since only a subset of experts is engaged for each input, model behavior becomes coupled to routing decisions, yielding a difficult-to-control mechanism that can vary across safety-relevant scenarios. At the same time, adapting model behavior through full fine-tuning or retraining is costly, especially when developers need to rapidly configure the same model for different safety objectives. We present MASCing (MoE Activation Steering Configuration), the first framework that enables flexible reconfiguration of MoE behavior across diverse safety scenarios without retraining. MASCing uses an LSTM-based surrogate model to capture cross-layer routing dependencies and map routing logits to downstream behaviors. It then optimizes a steering matrix to identify behavior-relevant expert circuits and, at inference time, applies steering masks to the routing gates to override expert selection. This enables targeted enhancement or suppression of specific behaviors while preserving general language utility. To demonstrate its reconfigurability, we apply MASCing to two different safety-related objectives and observe consistent gains with negligible overhead across seven open-source MoE models. For multi-turn jailbreak defense, it improves the average defense success rate from 52.5% to 83.9%, with gains of up to 89.2%. For adult-content generation, MASCing enables models to comply with such requests that would otherwise be refused, increasing the average generation success rate from 52.6% to 82.0%, with gains of up to 93.0%. These results establish MASCing as a practical, lightweight, and flexible framework for scenario-specific safety reconfiguration in MoE models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MASCing**（MoE Activation Steering Configuration）的框架，旨在通过轻量级、无需重新训练的方法，灵活配置混合专家（MoE）架构大语言模型的安全行为。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：MoE 架构通过稀疏激活（仅调用部分专家）降低了推理成本，但其行为高度依赖于路由（Routing）决策。这种机制使得模型行为难以控制，且容易受到针对路由机制的攻击。
*   **核心挑战**：传统的安全对齐方法（如全量微调或重新训练）成本极高，且难以应对动态变化的安全策略（如防御新型多轮越狱攻击或根据政策调整特定内容的生成限制）。
*   **整体含义**：MASCing 提供了一种“即插即用”的方案，通过干预路由层的 Logits 来引导专家选择，从而在不修改模型权重的前提下，实现对模型行为的精准重构。

### 2. 方法论
MASCing 的核心思想是利用代理模型识别与特定行为相关的“专家电路”，并生成稀疏的引导掩码。流程分为三个阶段：
1.  **行为序列建模（Sequential Modeling）**：
    *   使用一个轻量级的 **LSTM 代理模型**。输入是 MoE 各层未归一化的路由 Logits（连续值而非离散的 Top-k 选择），输出是行为分类（如：是否拒绝）。
    *   LSTM 能够捕捉跨层的路由依赖关系以及多轮对话中的时间序列特征。
2.  **引导掩码创建（Steering Mask Creation）**：
    *   冻结 LSTM，通过反向传播优化一个连续的引导矩阵 $S$。
    *   引入 **$L_1$ 正则化** 和 **对称幅度剪枝（Symmetric Magnitude Pruning）**，强制引导矩阵变得稀疏，从而识别出最关键的专家电路。
3.  **引导掩码应用（Steering Mask Application）**：
    *   在推理时，将生成的静态掩码 $\hat{S}$ 加到原始路由 Logits 上：$g' = g + \alpha(\sigma \cdot \hat{S})$。
    *   通过超参数 $\alpha$ 控制引导强度，强制模型选择或避开特定专家。

### 3. 实验设计
*   **应用场景**：
    1.  **多轮越狱防御**：针对复杂的、隐藏意图的攻击，增强模型的拒绝能力。
    2.  **成人内容生成**：针对特定政策调整（如 OpenAI 允许受控的成人内容），降低模型的过度拒绝。
*   **数据集**：AdvBench（有害指令）、MHJ（多轮越狱）、EroticaAnalysis（成人内容）、NaturalReasoning（通用推理）。
*   **测试模型**：涵盖 7 个主流开源 MoE 模型，包括 DeepSeek-MoE-16B、Mixtral-8x7B、Phi-3.5-MoE、Qwen1.5/Qwen3 等。
*   **对比方法**：SteerMoE（基于激活频率的引导）、直接专家硬编码（Expert Steering）。
*   **评估指标**：成功率（Success Rate）、通用能力评估（MMLU、GSM8K）。

### 4. 资源与算力
*   **算力设备**：使用了两台 NVIDIA GH200 (120GB) Grace Hopper 超级芯片和 NVIDIA H100 GPU。
*   **训练时长**：LSTM 代理模型的训练非常高效，在单张 H100 上仅需 **5 分钟** 即可完成。
*   **推理开销**：推理时仅增加了一个元素级加法操作，计算开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同架构（标准 MoE 和共享专家 MoE）的模型上进行了验证，具有较强的普适性。
*   **消融实验**：对引导强度 $\alpha$、正则化权重 $\lambda$、剪枝阈值 $\tau$ 进行了详尽的网格搜索分析。
*   **充分性评价**：实验设计较为充分，不仅验证了安全性的提升，还通过 MMLU 和 GSM8K 评估了对模型通用能力的影响，确保了实验的客观性。

### 6. 主要结论与发现
*   **防御效果显著**：在多轮越狱防御中，平均成功率从 52.5% 提升至 **83.9%**（最高达 89.2%）。
*   **灵活配置能力**：在成人内容生成任务中，成功率从 52.6% 提升至 **82.0%**，证明了该方法具有双向调节能力。
*   **性能保持良好**：模型在通用能力测试中仅有约 **4.1%** 的轻微下降，未发生灾难性遗忘或逻辑崩溃。
*   **优于现有方法**：相比 SteerMoE，MASCing 在处理复杂的多轮对话场景时表现更优，因为它利用了连续 Logits 的丰富信息。

### 7. 优点
*   **训练成本极低**：无需微调大模型，仅需训练微型 LSTM。
*   **双向可配置性**：既能增强安全性，也能根据需要降低过度拒绝，适应不同政策。
*   **非侵入性**：不改变模型原始权重，易于在现有推理框架中部署。
*   **多轮感知**：通过 LSTM 捕捉上下文路由模式，比单轮静态分析更精准。

### 8. 不足与局限
*   **依赖专家固有能力**：如果模型专家本身完全没有相关知识（如被彻底剪枝或未对齐），仅靠路由引导无法凭空产生安全行为。
*   **静态掩码限制**：目前生成的掩码是静态的，虽然对同类任务泛化良好，但可能难以应对极端的、分布外的（OOD）新型攻击。
*   **代理模型误差**：LSTM 作为代理模型可能无法完美模拟极深层模型中复杂的非线性路由逻辑。

（完）
