---
title: "Revisiting JBShield: Breaking and Rebuilding Representation-Level Jailbreak Defenses"
title_zh: 重新审视 JBShield：破解与重建表示层级的越狱防御
authors: "Kemal Derya, Berk Sunar"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03095v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 表示层级的越狱防御及JBShield评估
tldr: "本研究重新评估了表征级越狱防御机制 JBShield 的安全性。作者设计了自适应攻击 JB-GCG，通过抑制拒绝方向并正则化毒性概念得分，成功将 JBShield 的攻击成功率从 0% 提升至 46.2% 以上，揭示了其结构性漏洞。基于此，作者提出了基于多层拒绝方向指纹的马氏距离异常检测防御方案 RTV。实验表明 RTV 在应对自适应攻击时表现优异，AUROC 达 0.99，证明了多层表征一致性在越狱检测中比单层相似度更可靠。"
selection_source: fresh_fetch
motivation: 旨在验证现有的表征级越狱防御机制在面对直接针对其检测逻辑的自适应攻击时是否依然稳固。
method: 开发了结合拒绝方向抑制与毒性正则化的 JB-GCG 攻击，并提出了基于多层表征轨迹一致性的 RTV 检测防御。
result: "JB-GCG 成功突破了 JBShield 的防御，而 RTV 防御在面对高强度自适应攻击时仅有 7% 的攻击成功率且检测准确率极高。"
conclusion: 研究证明了多层表征一致性是比单层概念相似度更具鲁棒性的越狱检测基础。
---

## 摘要
防御大语言模型（LLMs）免受越狱攻击（如贪婪坐标梯度 GCG）的侵害仍然是一项挑战，特别是在攻击者直接针对防御机制的自适应威胁模型下。JBShield 是最近提出的一种越狱防御方法，在某些设置下攻击成功率为 0%，它通过两种概念信号（毒性概念和越狱概念）来检测恶意提示词。我们设计了 JB-GCG，它修改了 GCG 的目标函数以结合两项：通过拒绝方向与隐藏状态表示之间的余弦相似度进行拒绝方向抑制，以及通过 JBShield 自身的毒性概念得分进行毒性概念正则化。在 Llama-3-8B 的五种配置中，JB-GCG 实现了 46.2% 的平均攻击成功率（ASR），在最强设置下达到了 53.4%。我们进一步表明，我们的攻击对 JBShield-M 仍然有效，在评估设置中 ASR 最高可达 30.7%。该攻击在多次 JBShield 重新校准后依然存在，证实了该漏洞是结构性的，而非特定于校准。我们分析了越狱表示的余弦相似度特征，发现它们在拒绝方向指纹空间中占据了一个独特的区域，而无害或有害提示词均不处于该区域。我们引入了表示轨迹验证（RTV），这是一种基于多层拒绝方向指纹的马氏距离（Mahalanobis）离群点检测的新型防御方法。RTV 针对我们的攻击达到了 0.99 的 AUROC。最后，我们在完全掌握防御白盒知识的情况下，设计并评估了针对 RTV 的额外自适应攻击；最佳攻击在 13 倍计算成本下仅实现了 7% 的 ASR。我们的结果表明，强大的非自适应检测并不意味着在自适应威胁模型下的鲁棒性，且多层表示一致性比单层概念相似度是更可靠的越狱检测基础。

## Abstract
Defending large language models (LLMs) against jailbreak attacks, such as Greedy Coordinate Gradient (GCG), remains a challenge, particularly under adaptive threat models where an attacker directly targets the defense mechanism. JBShield, a recent jailbreak defense with a 0% attack success rate in some settings, detects malicious prompts via two concept signals, a toxic concept and a jailbreak concept. We design JB-GCG, which modifies GCG's objective to combine two terms: refusal-direction suppression via cosine similarity between the refusal direction and hidden-state representations, and toxic-concept regularization via JBShield's own toxic concept score. Across five configurations on Llama-3-8B, JB-GCG achieves an average ASR of 46.2%, reaching up to 53.4% in the strongest setting. We further show that our attack remains effective against JBShield-M, achieving ASR up to 30.7% across evaluated settings. The attack persists across multiple JBShield recalibrations, confirming that the vulnerability is structural rather than calibration-specific. We analyze the cosine-similarity signatures of jailbreak representations and find that they occupy a distinctive region in refusal-direction fingerprint space that neither harmless nor harmful prompts inhabit. We introduce Representation Trajectory Verification (RTV), a new defense based on Mahalanobis outlier detection over multi-layer refusal-direction fingerprints. RTV attains an AUROC of 0.99 against our attack. Finally, we design and evaluate an additional adaptive attack against RTV with full white-box knowledge of the defense; the best attack achieves only 7% ASR at 13x the computational cost. Our results show that strong non-adaptive detection does not imply robustness under adaptive threat models, and that multi-layer representation consistency is a more reliable foundation for jailbreak detection than single-layer concept similarity.

---

## 论文详细总结（自动生成）

这篇论文对表征级越狱防御机制（特别是 JBShield）进行了深入的安全性评估，并提出了改进的防御方案。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）安全对齐技术的提升，越狱攻击（Jailbreak Attacks）也演变得更加复杂。JBShield 是近期提出的一种表征级防御，声称通过检测“毒性”和“越狱”两个概念的激活情况，能实现极高的防御成功率（甚至 0% ASR）。
*   **研究动机**：作者质疑 JBShield 在“自适应威胁模型”下的鲁棒性。即如果攻击者完全了解防御机制的逻辑（白盒），是否能通过优化手段绕过它？
*   **核心问题**：现有的单层表征防御是否存在结构性漏洞？如何构建一个更能抵抗自适应攻击的防御系统？

### 2. 方法论
论文提出了两个核心组件：一种新型攻击（JB-GCG）和一种新型防御（RTV）。

*   **JB-GCG 攻击**：
    *   **核心思想**：利用 JBShield 的“与门（AND-gate）”逻辑漏洞。JBShield 只有在毒性得分和越狱得分同时超过阈值时才会拦截。
    *   **技术细节**：在标准 GCG 攻击的基础上修改目标函数。公式结合了两项：
        1.  **拒绝方向抑制**：最小化隐藏状态与预定义的“拒绝方向（Refusal Direction）”的余弦相似度，迫使模型不拒绝请求。
        2.  **毒性正则化**：使用平方余弦项压低 JBShield 的毒性概念得分，使其低于检测阈值。
    *   **结果**：攻击者只需成功伪装其中一个概念，即可使整个防御系统失效。

*   **RTV 防御 (Representation Trajectory Verification)**：
    *   **核心思想**：监控模型在处理输入时，多个层级和多个 Token 位置上的“拒绝信号轨迹”是否一致。
    *   **技术细节**：
        1.  **指纹提取**：在第 18、25、32 层以及最后 5 个 Token 位置提取与拒绝方向的余弦相似度，构成一个 15 维的“指纹”向量。
        2.  **异常检测**：使用**马氏距离（Mahalanobis Distance）**计算测试样本到“无害”和“有害”校准簇的距离。如果样本落入这两个簇之外的“独特区域”，则判定为越狱攻击。
        3.  **优势**：攻击者很难在抑制拒绝信号（为了越狱）的同时，还能精准地模仿正常样本在多层间的复杂指纹轨迹。

### 3. 实验设计
*   **目标模型**：Meta-Llama-3-8B-Instruct。
*   **数据集**：
    *   **HarmBench**：用于生成有害请求。
    *   **Alpaca**：用于提供无害请求。
    *   **SORRY-Bench**：作为自动化的安全性评估器。
*   **Benchmark 与对比方法**：
    *   **防御对比**：JBShield-D（检测）、JBShield-M（缓解）。
    *   **攻击泛化性测试**：对比了 IJP, GCG, SAA, AutoDAN, PAIR, DrAttack, Puzzler, Zulu, Base64 等 9 种主流攻击。
*   **自适应评估**：在攻击者完全掌握 RTV 内部参数（拒绝方向、马氏距离均值/协方差、阈值）的极端白盒条件下进行测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号（如 A100 或 H100）及数量。
*   **计算成本**：
    *   标准攻击设定为 600 次 GCG 迭代。
    *   论文提到，针对 RTV 的最强自适应攻击需要约 **13 倍**于普通攻击的计算成本，且平均迭代次数接近预算上限（约 514 次），说明绕过 RTV 的优化难度极大。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 JBShield 进行了 5 种不同层配置的攻击测试。
    *   对 JBShield 的校准规模（N=10 到 50）进行了灵敏度分析。
    *   对 RTV 进行了消融实验，验证了层数（1/2/3/5 层）和 Token 位置数量对检测性能的影响。
    *   进行了 5 次随机划分的校准稳定性实验。
*   **充分性评价**：实验设计非常严谨且充分。作者不仅证明了现有防御的脆弱性，还通过极端白盒测试验证了新防御的上限，具有很强的说服力和客观性。

### 6. 主要结论与发现
1.  **JBShield 的脆弱性**：JBShield 的 ASR 从宣称的 0% 被提升至 **53.4%**，证明其单层检测逻辑极易被针对性绕过。
2.  **表征轨迹特征**：越狱攻击在中间层会抑制拒绝信号，但在深层表征中会表现出与有害样本相似的漂移，这种“不一致性”是检测的关键。
3.  **RTV 的效能**：RTV 在检测 JB-GCG 攻击时达到了 **0.99 AUROC**。
4.  **防御韧性**：即使在完全白盒的自适应攻击下，RTV 的 ASR 仅为 **7%**，证明了多层一致性约束比单层概念检测更具鲁棒性。

### 7. 优点
*   **揭示结构性缺陷**：深刻指出了“与门”逻辑在安全防御中的风险。
*   **无需越狱样本**：RTV 的校准仅需正常和有害样本，不需要提前获知越狱攻击的样本，具有极强的零样本检测能力。
*   **计算高效**：检测过程仅涉及简单的余弦相似度和低维马氏距离计算，推理开销极低。

### 8. 不足与局限
*   **模型覆盖面**：实验仅限于 Llama-3-8B，虽然拒绝方向在多个模型中存在，但 RTV 在更大规模模型（如 70B）或不同架构（如 MoE）上的表现尚未验证。
*   **自然语言攻击挑战**：对于像 PAIR 这样通过语义迭代生成的、语言高度流畅的攻击，RTV 的检测率（58%）仍有提升空间。
*   **静态防御风险**：RTV 依赖于静态的校准统计量，如果模型发生分布偏移（Distribution Shift），可能需要重新校准。

（完）
