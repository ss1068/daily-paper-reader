---
title: Multilingual Safety Alignment via Self-Distillation
title_zh: 通过自蒸馏实现多语言安全对齐
authors: "Ruiyang Qin, Qingzhuo Wang, Dongrui Liu, Qiang Li, Zhihua Wei, Wen Shen"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.02971v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 多语言安全对齐与越狱漏洞缓解
tldr: 针对大语言模型在低资源语言中安全性不足的问题，本文提出多语言自蒸馏（MSD）框架。该框架无需目标语言的响应数据，通过将模型在英语等高资源语言中的安全能力迁移至低资源语言，实现了跨语言安全对齐。结合提出的双视角安全加权（DPSW）策略，该方法在提升多语言安全性的同时，有效保持了模型的通用能力。
selection_source: fresh_fetch
motivation: 现有的多语言安全对齐方法高度依赖难以获取的高质量多语言响应数据，导致模型在低资源语言下极易受到越狱攻击。
method: 提出多语言自蒸馏框架（MSD），包含在线和离线两种策略，并引入双视角安全加权（DPSW）来优化蒸馏目标，重点惩罚安全关键词。
result: 实验表明，该方法在多个多语言越狱和通用能力基准测试中均取得了优异表现，且能有效泛化至未见过的语言。
conclusion: MSD框架为解决LLM多语言安全失配提供了一种高效且无需响应数据的方案，显著增强了模型的全球安全性。
---

## 摘要
大语言模型（LLMs）表现出严重的多语言安全失配：它们在高资源语言中拥有强大的安全防护，但在低资源语言中仍然极易受到越狱攻击。目前的安全对齐方法通常依赖于每种目标语言的高质量回复数据，而这些数据的生成成本高昂且困难。在本文中，我们提出了一个名为多语言自蒸馏（MSD）的跨语言安全防护迁移框架。该框架将 LLM 固有的安全能力从高资源语言（如英语）迁移到低资源语言（如爪哇语），从而克服了对任何语言回复数据的需求。我们的框架非常灵活，可以与不同的自蒸馏策略相结合。具体而言，我们实现了两种具体方法——在线（on-policy）MSD 和离线（off-policy）MSD，两者都仅使用多语言查询即可实现有效的跨语言安全迁移。此外，我们提出了双视角安全加权（DPSW），这是一种用于优化蒸馏目标的散度度量。通过共同考虑教师和学生的视角，DPSW 自适应地增加了安全关键标记（tokens）的惩罚权重，同时降低了非关键标记的权重。在代表性 LLM 上进行的涵盖多种多语言越狱和效用基准的大量实验表明，我们的方法始终能获得卓越的多语言安全性能。值得注意的是，它能有效地泛化到更具挑战性的数据集和未见过的语言，同时保留模型的通用能力。

## Abstract
Large language models (LLMs) exhibit severe multilingual safety misalignment: they possess strong safeguards in high-resource languages but remain highly vulnerable to jailbreak attacks in low-resource languages. Current safety alignment methods generally rely on high-quality response data for each target language, which is expensive and difficult to generate. In this paper, we propose a cross-lingual safeguard transfer framework named Multilingual Self-Distillation (MSD). This framework transfers an LLM's inherent safety capabilities from high-resource (e.g., English) to low-resource (e.g., Javanese) languages, overcoming the need for response data in any language. Our framework is flexible and can be integrated with different self-distillation strategies. Specifically, we implement two concrete methods -- on-policy MSD and off-policy MSD -- both of which enable effective cross-lingual safety transfer using only multilingual queries. Furthermore, we propose Dual-Perspective Safety Weighting (DPSW), a divergence measure to optimize the distillation objective. By jointly considering the perspectives of both the teacher and the student, DPSW adaptively increases the penalty weights on safety-critical tokens while reducing the weights on non-critical tokens. Extensive experiments on representative LLMs across diverse multilingual jailbreak and utility benchmarks demonstrate that our method consistently achieves superior multilingual safety performance. Notably, it generalizes effectively to more challenging datasets and unseen languages while preserving the model's general capabilities.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MSD (Multilingual Self-Distillation)** 的框架，旨在解决大语言模型（LLM）在不同语言间安全水平不一致的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LLM 存在严重的“多语言安全失配”现象。模型在高资源语言（如英语）中经过严格对齐，安全性较高；但在低资源语言（如爪哇语、斯瓦希里语）中，安全防护极易被越狱攻击突破。
*   **核心挑战**：现有的安全对齐方法（如 SFT 或 DPO）高度依赖目标语言的高质量安全回复数据。然而，为数百种低资源语言生成高质量、无翻译错误的回复数据成本极高且极其困难。
*   **论文目标**：提出一种**无需回复数据（Response-free）**的框架，利用模型自身在高资源语言中固有的安全能力，通过自蒸馏技术迁移至低资源语言。

### 2. 方法论
*   **核心思想**：将同一个 LLM 同时作为“教师”和“学生”。教师模型通过英语辅助信息和思维链（CoT）触发其内在的安全推理能力，生成安全的概率分布；学生模型仅接收低资源语言查询，通过学习教师的分布来实现安全对齐。
*   **关键技术细节**：
    *   **MSD 框架**：支持**离线（Off-policy）**和**在线（On-policy）**两种模式。离线模式基于教师生成的序列进行训练，在线模式则基于学生自身的采样序列进行实时分布对齐。
    *   **教师端增强**：给教师模型提供：1. 目标查询的英语翻译；2. CoT 指令（要求其用英语推理并以目标语言回复）。这能最大化激发模型的安全防护意识。
    *   **双视角安全加权 (DPSW)**：这是本文的核心优化算法。它认为并非所有 token 对安全都同样重要（如“我不能”比“你好”更关键）。
        *   **教师视角**：利用 Top-K 熵衡量教师的置信度，置信度越高，权重越大。
        *   **学生视角**：衡量学生对教师预测 token 的偏离程度，偏离越大（风险越高），权重越大。
    *   **损失函数**：使用加权的 KL 散度（Reverse-KL）来最小化学生与教师之间的分布差异。

### 3. 实验设计
*   **模型对象**：Qwen-2.5-7B-Instruct, Qwen-3-8B, LLaMA-2-7B-chat, LLaMA-3-8B-Instruct。
*   **数据集**：
    *   **训练集**：XSafety（仅使用其中的多语言查询，不使用回复）。
    *   **安全测试集**：MultiJail（包含 10 种语言的越狱攻击）、PKU-SafeRLHF（跨分布测试）。
    *   **通用能力测试集**：MMMLU（知识）、MGSM（数学推理）。
*   **对比方法**：
    *   传统对齐：SFT, DPO, rDPO, KTO, ORPO, R-DPO, SimPO。
    *   多语言专用：MPO, SDRRL。
    *   训练无关方法：PolyRefuse（表征工程）, Self-Defense（提示词防御）。
*   **语言覆盖**：涵盖高（英、中、意、越）、中（阿、韩、泰）、低（孟、斯、爪）三个资源层级的 10 种语言。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA RTX PRO 6000 (96GB)** GPU。
*   **软件框架**：基于 Hugging Face TRL 库和 DeepSpeed ZeRO-3 优化。
*   **训练细节**：进行了全参数微调（Full Fine-tuning），训练 1 个 epoch。搜索了学习率（5e-7 到 2e-5）和 Batch Size（16, 32）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流模型系列上进行了验证，涵盖了 10 种语言，并对比了 11 种基准方法，实验量非常大。
*   **消融实验**：
    *   验证了 DPSW 权重的有效性。
    *   验证了教师端“英语翻译+CoT”组合的必要性。
    *   探讨了冻结教师模型参数与 EMA 更新的影响。
    *   测试了不同散度度量（FKL, RKL, JSD）的效果。
*   **客观性**：使用了 GPT-4o 作为自动评测器，并进行了 3% 的人工抽检（一致性 >97%），确保了评测的公正性。

### 6. 主要结论与发现
*   **安全提升显著**：MSD 在所有模型和基准测试中均显著降低了越狱攻击成功率（ASR），尤其在低资源语言上表现远超 DPO 等强基准。
*   **泛化能力强**：即使在训练集中未出现的语言（如意大利语、斯瓦希里语），MSD 依然能有效迁移安全防护。
*   **能力保留**：在提升安全性的同时，模型在 MMMLU 和 MGSM 上的得分几乎没有下降，甚至在某些推理任务上有所提升，避免了“对齐税”。
*   **零成本数据**：证明了无需生成任何安全回复数据即可实现高质量对齐。

### 7. 优点
*   **高效经济**：彻底摆脱了对昂贵的多语言安全回复数据的依赖。
*   **精准对齐**：DPSW 机制通过动态调整 token 权重，解决了安全对齐中“关键 token 稀疏”的问题。
*   **灵活性高**：框架兼容在线和离线蒸馏，且可以灵活更换高资源参考语言（如将英语换成中文）。

### 8. 不足与局限
*   **基础能力依赖**：该方法依赖于基座模型本身在英语（或某种高资源语言）中已经具备较强的安全意识和上下文学习能力。如果基座模型本身在所有语言下都不安全，该方法将失效。
*   **翻译质量敏感**：虽然不需要回复数据，但仍需要高质量的查询翻译。如果查询翻译存在严重歧义，会干扰教师模型的判断。
*   **低资源生成瓶颈**：在某些极低资源语言（如斯瓦希里语）中，虽然模型能识别有害意图，但由于生成能力太弱，输出仍可能被判定为“无效/不安全”。

（完）
