---
title: "RefusalGuard: Geometry-Preserving Fine-Tuning for Safety in LLMs"
title_zh: RefusalGuard：保持几何结构的大语言模型安全性微调
authors: "Sadia Asif, Mohammad Mohammadi Amiri"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01913v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对大模型安全与拒绝行为的微调
tldr: 针对微调导致大语言模型安全对齐性能下降的问题，本文研究了其表征层面的机制，发现标准微调会扭曲安全相关的几何结构并引入干扰。为此，提出了 RefusalGuard 框架，通过在隐藏表征空间施加约束，在允许任务学习的同时保持安全组件的稳定性。实验证明该方法在多个模型和基准测试中显著降低了攻击成功率，且不损失下游任务性能，为安全微调提供了新思路。
selection_source: fresh_fetch
motivation: 旨在揭示微调导致模型安全对齐退化的表征层机制，并解决任务优化与安全特征之间的冲突。
method: 提出 RefusalGuard 框架，通过在微调过程中约束隐藏表征空间的更新，确保安全相关的几何结构保持稳定。
result: 在多个主流模型和安全基准测试中，该方法在维持任务性能的同时，将攻击成功率降低至与原始安全模型相当的水平。
conclusion: 通过保护模型内部安全相关的表征结构，可以有效防止微调过程中的安全退化，实现任务学习与安全性的平衡。
---

## 摘要
对经过安全对齐的语言模型进行下游任务微调通常会导致拒绝行为的显著退化，使模型容易受到对抗性滥用。虽然先前的研究表明安全相关特征被编码在模型激活空间的结构化表示中，但这些表示在微调过程中如何变化以及对齐为何退化仍不清楚。在这项工作中，我们研究了对齐退化背后的表示层级机制。我们的分析表明，标准微调会导致安全相关表示发生系统性漂移，扭曲其几何结构，并在任务优化与安全特征之间引入干扰。这些效应共同导致了有害合规性的增加。受这些发现的启发，我们提出了 REFUSALGUARD，这是一个在模型适配过程中保留安全相关结构的表示层级微调框架。我们的方法约束了隐藏表示空间中的更新，确保调节安全的组件保持稳定，同时允许在互补方向上进行特定任务的学习。我们在 LLaMA、Gemma 和 Qwen 等多个模型系列上，针对 AdvBench、DirectHarm4 和 JailbreakBench 等对抗性安全基准以及下游效用任务评估了 REFUSALGUARD。我们的方法实现了与基础安全对齐模型相当的攻击成功率，同时保持了具有竞争力的任务性能，显著优于基准方法。

## Abstract
Fine-tuning safety-aligned language models for downstream tasks often leads to substantial degradation of refusal behavior, making models vulnerable to adversarial misuse. While prior work has shown that safety-relevant features are encoded in structured representations within the model's activation space, how these representations change during fine-tuning and why alignment degrades remains poorly understood. In this work, we investigate the representation-level mechanisms underlying alignment degradation. Our analysis shows that standard fine-tuning induces systematic drift in safety-relevant representations, distorts their geometric structure, and introduces interference between task optimization and safety features. These effects collectively lead to increased harmful compliance. Motivated by these findings, we introduce REFUSALGUARD, a representation-level fine-tuning framework that preserves safety-relevant structure during model adaptation. Our approach constrains updates in hidden representation space, ensuring that safety-mediating components remain stable while allowing task-specific learning in complementary directions. We evaluate REFUSALGUARD across multiple model families, including LLaMA, Gemma, and Qwen, on adversarial safety benchmarks such as AdvBench, DirectHarm4, and JailbreakBench, as well as downstream utility tasks. Our approach achieves attack success rates comparable to base safety-aligned models while maintaining competitive task performance, significantly outperforming baselines.

---

## 论文详细总结（自动生成）

### 论文总结：RefusalGuard：保持几何结构的大语言模型安全性微调

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了安全对齐的大语言模型（LLMs）在下游任务微调后，其“拒绝回答有害请求”的能力为何会显著退化。现有的研究多从提示词或数据集层面分析这种退化，而本文从**表征几何（Representation Geometry）**的视角切入，揭示了微调导致安全性崩溃的内部机制：标准微调会引起安全相关表征的系统性漂移、扭曲其几何结构，并使任务优化方向与安全特征产生干扰。研究动机在于开发一种既能进行任务适配，又能从底层机制上保护模型安全性的微调框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **RefusalGuard** 框架，其核心思想是在微调过程中保护模型内部已形成的“拒绝几何结构”。
*   **拒绝锥（Refusal Cone）提取**：首先从原始安全模型中识别出介导拒绝行为的低维子空间（拒绝锥），并提取其正交基 $B_{ref}$。
*   **表征干预（ReFT）**：基于表征微调（Representation Finetuning）技术，不改变模型原始参数，而是学习作用于隐藏状态的低秩干预模块。
*   **几何保持损失（Geometry-Preservation Loss）**：引入一个正则化项 $L_{geom}$，专门惩罚那些落在拒绝子空间内的隐藏状态更新。
*   **优化目标**：最终损失函数为 $L_{RG} = L_{task} + \lambda_{geom} L_{geom}$。通过调节 $\lambda_{geom}$，强制要求任务相关的更新发生在与安全方向正交的补空间中，从而在学习新任务的同时保持安全组件的稳定性。

#### 3. 实验设计
*   **模型选择**：涵盖了多个主流模型家族，包括 LLaMA-3.1-8B-Instruct、Gemma-2 (2B/9B)、Qwen2.5 (7B/14B)。
*   **安全基准（Safety Benchmarks）**：AdvBench、DirectHarm4、JailbreakBench。
*   **效用基准（Utility Benchmarks）**：GSM8K（数学推理）、OpenOrca（指令遵循）、ARC（常识问答）。
*   **对比方法**：
    1.  **LoRA**：标准参数高效微调。
    2.  **Circuit Breaker (CB)**：通过重定向有害表征来中断生成的安全性方法。
    3.  **REPBEND**：通过重塑隐藏状态来分离安全/不安全行为的方法。
*   **压力测试场景**：采用极端的“有害适配”设置，即仅使用 10 个有害示例进行微调，观察模型安全性的崩溃程度及防御效果。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。但考虑到其基于 ReFT（表征微调）和 LoRA，这类方法通常比全参数微调更节省算力，且实验中提到的微调样本量极小（10个样本），推测其算力需求在单卡或少量消费级/企业级 GPU 范围内即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同规模和架构的模型上进行了验证，涵盖了 3 个安全基准和 3 个效用基准。
*   **消融实验**：详细分析了 $\lambda_{geom}$ 系数的影响、不同层的退化情况、以及训练过程中的动态演变。
*   **充分性评价**：实验设计较为充分。通过“有害微调”和“良性微调（GSM8K）”两种场景对比，证明了安全性退化不仅源于恶意攻击，也可能源于普通任务适配。对比了多种表征层面的前沿基线，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **安全性极度脆弱**：仅需 10 个有害样本的微调，LoRA 就能让模型的攻击成功率（ASR）从接近 0 飙升至 70% 以上。
*   **几何失效机制**：微调导致拒绝行为失效的根本原因是隐藏状态在拒绝子空间上的投影强度减弱、方向漂移以及坐标分布的扭曲（熵减、集中化）。
*   **RefusalGuard 的有效性**：在所有测试模型中，RefusalGuard 都能将 ASR 维持在极低水平（通常 < 3%），同时保留了大部分下游任务性能，其表现显著优于 CB 和 REPBEND。

#### 7. 优点
*   **机制驱动**：不同于启发式的防御，该方法基于对模型内部表征几何的深入理解，具有更强的理论支撑。
*   **高效性**：基于 ReFT 架构，仅需训练极少量参数，且无需大规模安全数据集进行重新对齐。
*   **通用性**：在 LLaMA、Gemma、Qwen 等不同架构上表现一致，证明了拒绝几何结构的普遍存在性。

#### 8. 不足与局限
*   **效用权衡**：虽然性能保持较好，但在极高安全性要求下（$\lambda_{geom}$ 较大时），下游任务的准确率仍有一定程度的下降。
*   **依赖预提取**：防御效果高度依赖于初始安全模型中拒绝子空间提取的准确性。如果原始模型对齐不佳，该方法将难以发挥作用。
*   **静态防御**：目前主要针对微调阶段的保护，对于推理阶段通过复杂提示词绕过几何结构的动态攻击，其防御能力尚需进一步验证。

（完）
