---
title: "MultiBreak: A Scalable and Diverse Multi-turn Jailbreak Benchmark for Evaluating LLM Safety"
title_zh: MultiBreak：一个用于评估大语言模型（LLM）安全性的可扩展且多样化的多轮越狱基准测试
authors: "Jialin Song, Xiaodong Liu, Weiwei Yang, Wuyang Chen, Mingqian Feng, Xuekai Zhu, Jianfeng Gao"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01687v1"
tags: ["query:llm"]
score: 9.0
evidence: 用于评估 LLM 安全性的多轮越狱基准测试
tldr: 针对现有大模型多轮越狱基准规模小、多样性不足的问题，本文提出MultiBreak。该基准包含超过一万个多轮对抗提示，涵盖2665个有害意图。通过主动学习流水线迭代微调生成器，MultiBreak在DeepSeek和GPT-4o-mini等模型上展现出极高的攻击成功率，揭示了模型在复杂对话场景下的安全漏洞，为提升LLM安全性提供了重要资源。
selection_source: fresh_fetch
motivation: 现有的多轮越狱基准规模有限且依赖模板，难以模拟真实对话中复杂且多样的安全威胁。
method: 引入一种主动学习流水线，通过迭代微调生成器并结合基于不确定性的细化，自动扩展高质量且多样化的多轮对抗提示。
result: "MultiBreak在DeepSeek-R1-7B和GPT-4o-mini上的攻击成功率分别比现有最优基准高出54.0%和34.6%。"
conclusion: 该研究证明了多轮对话能显著放大单轮下看似无害的攻击效果，MultiBreak为评估和增强LLM在现实对抗环境下的安全性提供了有力工具。
---

## 摘要
我们提出了 MultiBreak，这是一个可扩展且多样化的多轮越狱基准测试，用于评估大语言模型（LLM）的安全性。多轮越狱模拟了自然的对话场景，使其比单轮越狱更容易绕过经过安全对齐的 LLM。现有的多轮基准测试在规模上有限，或者过度依赖模板，这限制了它们的多样性。为了填补这一空白，我们统一了广泛的有害越狱意图，并引入了一个主动学习流水线来扩展高质量的多轮对抗性提示词。在该流水线中，生成器在基于不确定性的细化指导下进行迭代微调，以产生更强大的攻击候选。我们的 MultiBreak 包含 10,389 个多轮对抗性提示词，涵盖 2,665 个不同的有害意图，并覆盖了迄今为止最广泛的主题。实证评估表明，在 DeepSeek-R1-7B 和 GPT-4.1-mini 上，我们的基准测试所实现的攻击成功率（ASR）分别比排名第二的数据集高出 54.0% 和 34.6%。更重要的是，安全性评估表明，多样化的攻击类别揭示了细粒度的 LLM 漏洞，并且在单轮场景下看似无害的类别在多轮场景中可能表现出显著更高的对抗有效性。这些发现强调了 LLM 在现实对抗设置下持续存在的漏洞，并将 MultiBreak 确立为推进 LLM 安全性的可扩展资源。

## Abstract
We present MultiBreak, a scalable and diverse multi-turn jailbreak benchmark to evaluate large language model (LLM) safety. Multi-turn jailbreaks mimic natural conversational settings, making them easier to bypass safety-aligned LLM than single-turn jailbreaks. Existing multi-turn benchmarks are limited in size or rely heavily on templates, which restrict their diversity. To address this gap, we unify a wide range of harmful jailbreak intents, and introduce an active learning pipeline for expanding high-quality multi-turn adversarial prompts, where a generator is iteratively fine-tuned to produce stronger attack candidates, guided by uncertainty-based refinement. Our MultiBreak includes 10,389 multi-turn adversarial prompts, spans 2,665 distinct harmful intents, and covers the most diverse set of topics to date. Empirical evaluation shows that our benchmark achieves up to a 54.0 and 34.6 higher attack success rate (ASR)} than the second-best dataset on DeepSeek-R1-7B and GPT-4.1-mini, respectively. More importantly, safety evaluations suggest that diverse attack categories uncover fine-grained LLM vulnerabilities}, and categories that appear benign under single-turn can exhibit substantially higher adversarial effectiveness in multi-turn scenarios. These findings highlight persistent vulnerabilities of LLMs under realistic adversarial settings and establish MultiBreak as a scalable resource for advancing LLM safety.

---

## 论文详细总结（自动生成）

### MultiBreak：一个用于评估大语言模型（LLM）安全性的可扩展且多样化的多轮越狱基准测试

#### 1. 论文的核心问题与整体含义
随着 LLM 安全对齐技术的进步，单轮越狱攻击的效果逐渐减弱。然而，在更符合真实使用场景的**多轮对话**中，攻击者可以通过逐步引导或隐藏恶意意图来绕过安全防线。
*   **研究动机**：现有的多轮越狱基准测试存在两个主要瓶颈：一是**多样性不足**（主题覆盖窄，意图重复率高）；二是**可扩展性差**（依赖人工编写或固定模板，规模难以扩大）。
*   **核心目标**：构建一个大规模、高多样性且具有强攻击性的多轮越狱基准，以揭示 LLM 在复杂对话中的细粒度安全漏洞。

#### 2. 方法论：主动学习流水线
论文提出了一种基于**主动学习（Active Learning）**的自动化构建框架，核心流程如下：
*   **数据多样化初始化**：从多个现有的单轮和多轮数据源收集有害意图，通过语义相似度去重和闭源模型过滤，构建初始意图池。
*   **迭代微调生成器**：使用 Llama3-8B、Qwen2.5-7B 等开源模型作为攻击生成器，通过监督微调（SFT）使其学习如何将有害意图转化为多轮对话。
*   **不确定性引导的重写（Uncertainty-Guided Rewrite）**：
    *   **评估**：将生成的提示词投喂给多个受害者模型，并由多个法官模型评分。
    *   **分区**：根据攻击成功率（ASR）、法官间的不确定性（标准差 $\sigma$）和语义忠实度进行分类。
    *   **重写**：针对那些在某些模型上成功但在另一些模型上失败的“边界样本”（高不确定性），利用重写模型（Rewriter）优化表达，增强攻击信号。
*   **循环扩展**：将验证成功的样本加入训练集，迭代训练更强的生成器，从而实现基准规模的指数级增长。

#### 3. 实验设计
*   **数据集规模**：MultiBreak 包含 **10,389** 个多轮对抗提示，涵盖 **2,665** 个独特有害意图，分为 9 个大类和 26 个细粒度类别。
*   **对比基准（Baselines）**：CoSafe、MHJ、SafeDialBench、RedQueen。
*   **受害者模型（Victims）**：
    *   开源：DeepSeek-R1-Distill-Qwen-7B、Qwen3-8B、Llama3.1-8B。
    *   闭源：GPT-4.1-mini、Gemini-2.5-flash-lite。
*   **法官模型（Judges）**：Llama Guard、GPT-4o-mini，以及额外的 Qwen3Guard 和 GPT-OSS-safeguard。
*   **实验场景**：评估不同轮数（2-6 轮）下的 ASR@1、ASR@5、ASR@10，以及跨模型的攻击迁移性。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **NVIDIA RTX A6000 GPU (48GB)** 上进行。
*   **训练细节**：论文提到了对 Llama3-8B 和 Qwen2.5-7B 进行全参数微调，对 DeepSeek-14B 进行 LoRA 微调。
*   **说明**：文中未明确给出具体的 GPU 数量、总训练时长或推理总开销，但从迭代次数（5 轮以上）和数据规模来看，算力消耗处于中等规模。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了非常详尽的实验，包括 5 个受害者模型和 4 个法官模型的交叉验证。
*   **消融与对比**：
    *   对比了单轮与多轮的攻击效果差异。
    *   分析了重写机制对降低不确定性的贡献。
    *   进行了人类评估（Human Evaluation）以验证自动化法官的可靠性。
    *   测试了针对现有防御方法（如 X-Boundary, NBF-LLM）的鲁棒性。
*   **客观性**：通过引入多个法官模型和人类评估，有效缓解了“LLM-as-a-judge”可能存在的偏见问题。实验覆盖了从开源到最先进闭源模型的广泛谱系，具有较强的说服力。

#### 6. 主要结论与发现
*   **攻击效力显著**：MultiBreak 在 DeepSeek-R1-7B 上的 ASR 比现有最优基准高出 **54.0%**，在 GPT-4.1-mini 上高出 **34.6%**。
*   **多轮放大效应**：许多在单轮对话中被模型轻松拒绝的“无害”意图，在增加到 6 轮对话后，攻击成功率最高提升了 **44.8%**。
*   **细粒度漏洞**：不同模型在特定类别（如高风险医疗建议、金融犯罪）上表现出截然不同的脆弱性，多样化的基准有助于发现这些隐蔽弱点。
*   **开源到闭源的迁移**：仅使用开源模型微调得到的攻击生成器，依然能有效突破 GPT-5 等顶级闭源模型的防御。

#### 7. 优点
*   **可扩展框架**：主动学习流水线解决了高质量对抗数据获取难的问题，允许基准随模型能力的提升而自动进化。
*   **高多样性**：不依赖死板的模板，生成的对话更自然且覆盖了极其广泛的有害主题（26 类）。
*   **不确定性视角**：创新性地利用法官间的分歧（不确定性）来识别最有价值的攻击样本进行重写，提高了数据质量。

#### 8. 不足与局限
*   **预设对话流**：MultiBreak 的提示词是预先生成的（Pre-scripted），而非根据受害者模型的实时反馈动态生成的交互式攻击。虽然论文验证了其连贯性，但与真实黑客的动态博弈仍有差距。
*   **法官一致性风险**：尽管使用了多个法官，但在某些特定类别（如法律/政府建议）上，不同法官之间仍存在显著的判定差异，这可能影响 ASR 的绝对准确性。
*   **语言限制**：目前主要集中在英文环境，尚未探讨多语言场景下的多轮越狱表现。

（完）
