---
title: "Catching the Infection Before It Spreads: Foresight-Guided Defense in Multi-Agent Systems"
title_zh: 在感染扩散前捕捉：多智能体系统中的前瞻引导防御
authors: "Yue Ma, Ziyuan Yang, Yi Zhang"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01758v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 多智能体系统中的传染性越狱防御
tldr: "针对多智能体系统（MAS）中越狱攻击易在智能体间传染的问题，本文提出了一种无需训练的预见性引导局部净化框架（FLP）。该框架通过模拟未来交互轨迹并利用多角色模拟策略检测感染，结合相册回滚和递归二分诊断（RBD）定位并清除恶意样本。实验表明，FLP能将感染率从95%以上降至5.47%以下，同时有效保留了智能体交互的多样性。"
selection_source: fresh_fetch
motivation: 现有的全局防御方法难以应对局部交互导致的传染性越狱，且容易导致智能体响应同质化，缺乏真正的恢复能力。
method: 提出FLP框架，通过多角色模拟预测未来行为并利用响应多样性检测感染，采用回滚和递归二分诊断进行局部净化。
result: "实验显示该方法将最大累积感染率从95%以上降至5.47%以下，且检索和语义指标接近良性基准。"
conclusion: FLP框架在不牺牲交互多样性的前提下，为多智能体系统提供了一种高效且鲁棒的传染性越狱防御方案。
---

## 摘要
基于大模型的多智能体系统（MASs）通过专业化的智能体实现协作解决复杂问题。然而，MASs 容易受到传染性越狱（infectious jailbreak）的影响，即攻击单个智能体可能会扩散到其他智能体，导致大规模的系统沦陷。现有的防御措施通过训练一种更具传染性的“治愈因子”（cure factor）来应对这一问题，使智能体倾向于检索该因子而非病毒对抗样本（VirAEs）。然而，这会导致智能体响应的同质化，仅提供表层的抑制而非真正的恢复。我们重新审视了这些防御措施，它们通过共享的治愈因子进行全局操作，而传染性越狱则源于局部的交互行为。这种不匹配限制了它们的有效性。为此，我们提出了一种无需训练的前瞻引导局部净化（FLP）框架，其中每个智能体通过对未来交互进行推理来追踪行为演化并消除感染。具体而言，每个智能体在随后的对话轮次中模拟未来的行为轨迹。为了反映 MASs 中的多样性，我们引入了一种多角色模拟策略，以便在不同的交互语境下进行鲁棒预测。随后，我们利用响应多样性作为诊断信号，通过分析基于角色的预测在检索结果和语义层面的不一致性来检测感染。对于受感染的智能体，我们应用局部净化：通过立即回滚相册来缓解近期感染，而长期感染则使用递归二分诊断（RBD）处理，该方法递归地划分图像相册并应用相同的诊断策略来定位和消除 VirAEs。实验表明，FLP 将最大累积感染率从 95% 以上降低到 5.47% 以下。此外，检索和语义指标与良性基准紧密匹配，表明其有效地保留了交互多样性。

## Abstract
Large multimodal model-based Multi-Agent Systems (MASs) enable collaborative complex problem solving through specialized agents. However, MASs are vulnerable to infectious jailbreak, where compromising a single agent can spread to others, leading to widespread compromise. Existing defenses counter this by training a more contagious cure factor, biasing agents to retrieve it over virus adversarial examples (VirAEs). However, this homogenizes agent responses, providing only superficial suppression rather than true recovery. We revisit these defenses, which operate globally via a shared cure factor, while infectious jailbreak arise from localized interaction behaviors. This mismatch limits their effectiveness. To address this, we propose a training-free Foresight-Guided Local Purification (FLP) framework, where each agent reasons over future interactions to track behavioral evolution and eliminate infections. Specifically, each agent simulates future behavioral trajectories over subsequent chat rounds. To reflect diversity in MASs, we introduce a multi-persona simulation strategy for robust prediction across interaction contexts. We then use response diversity as a diagnostic signal to detect infection by analyzing inconsistencies across persona-based predictions at both retrieval-result and semantic levels. For infected agents, we apply localized purification: recent infections are mitigated via immediate album rollback, while long-term infections are handled using Recursive Binary Diagnosis (RBD), which recursively partitions the image album and applies the same diagnosis strategy to localize and eliminate VirAEs. Experiments show that FLP reduces the maximum cumulative infection rate from over 95% to below 5.47%. Moreover, retrieval and semantic metrics closely match benign baselines, indicating effective preservation of interaction diversity.

---

## 论文详细总结（自动生成）

这篇论文针对多智能体系统（MAS）中日益严重的“传染性越狱”攻击问题，提出了一种创新的防御框架。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着多模态大模型（LMM）的发展，多智能体系统在协作解决复杂问题方面表现出色。然而，这类系统面临**传染性越狱（Infectious Jailbreak）**的威胁：攻击者只需向一个智能体注入病毒对抗样本（VirAE），该样本就会通过智能体间的交互（如RAG检索、对话记录共享）像病毒一样在整个系统中指数级扩散。
*   **核心问题**：现有的防御方法（如Cowpox）通常引入全局共享的“治愈因子”来竞争检索权重。这种方法存在两个致命缺陷：
    1.  **响应同质化**：强制智能体检索治愈因子会导致所有智能体的输出变得单一，丧失了MAS原有的交互多样性。
    2.  **治标不治本**：仅是表层抑制，并未从智能体的存储（相册）中真正清除恶意样本。
*   **研究动机**：如何在不牺牲系统多样性的前提下，实现对受感染智能体的真正恢复，并在攻击扩散前主动拦截？

### 2. 方法论：前瞻引导局部净化（FLP）
FLP是一个**无需训练（Training-free）**的防御框架，核心思想是让智能体在实际交互前进行“自我模拟”和“自我诊断”。
*   **核心思想**：利用恶意行为在不同语境下往往表现出“语义坍缩”（即无论什么角色都会输出相似的恶意内容）的特性，通过内部模拟来识别感染。
*   **关键技术细节**：
    1.  **多角色模拟（Multi-Persona Simulation）**：智能体从角色池中实例化多个不同性格、背景的角色，在内部模拟多轮“计划-检索-提问-回答”的交互轨迹。
    2.  **感染诊断（Infection Diagnosis）**：
        *   **检索熵（Retrieval Entropy, $E_{ret}$）**：量化检索结果的多样性。受感染智能体会反复检索同一个VirAE，导致熵值极低。
        *   **语义多样性（Semantic Diversity, $S_{div}$）**：计算不同角色生成回复的语义向量余弦相似度。恶意回复通常会坍缩到相似的语义空间。
    3.  **局部净化策略（Localized Purification）**：
        *   **语义漂移（Semantic Drift）监测**：通过对比连续两轮模拟的语义中心变化，区分近期感染和长期感染。
        *   **近期回滚**：若检测到剧烈语义漂移，直接回滚最近的一条记录。
        *   **递归二分诊断（Recursive Binary Diagnosis, RBD）**：针对长期感染，将相册递归地二分拆解并重复诊断流程，精准定位并剔除隐藏在相册深处的VirAE。

### 3. 实验设计
*   **模型与场景**：主要基于 LLaVA-1.5-7B 构建128个智能体的MAS系统，模拟64轮长期交互。
*   **数据集/攻击方式**：采用了 **AgentSmith** 攻击框架，包含边界扰动（Border）和像素扰动（Pixel）两种攻击设置，涵盖不同的扰动预算（如 $h=6$ 到 $12$，$\epsilon=4/255$ 到 $32/255$）。
*   **对比方法（Benchmark）**：
    *   **AgentSmith**：无防御状态下的攻击基准。
    *   **Cowpox**：目前最先进的基于全局治愈因子的防御方法。
*   **评估指标**：当前感染率（$r_t$）、累积感染率（$\rho_t$）、检索熵保留率（$\zeta_t$）、语义漂移距离（$\theta_t$）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的GPU型号、数量及总训练/推理时长。
*   **实现细节**：由于是无需训练的方法，算力消耗主要集中在推理阶段。文中提到使用了 LLaVA-1.5-7B、InternVL2-8B、Qwen2VL-8B 等模型，这些模型通常需要 A100 或 H100 等级别的显卡进行高效推理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨模型验证（4种不同架构的LMM）。
    *   针对不同攻击类型和强度进行了详尽测试。
    *   包含800个智能体规模的大型诊断性能测试。
    *   **消融实验**：验证了多角色异质性、检索熵和语义多样性指标的各自贡献，以及角色数量 $n$ 对性能的影响。
*   **充分性评价**：实验设计非常全面，不仅关注防御成功率，还深入探讨了系统多样性的保留情况，对比实验和消融实验均显示出该方法的客观性和优越性。

### 6. 主要结论与发现
*   **防御效能**：FLP 将最大累积感染率从 95% 以上降低到了 **5.47% 以下**，在多数情况下甚至能实现 0% 的感染。
*   **多样性保留**：FLP 的检索熵和语义指标几乎与良性基准（无攻击状态）重合，证明其在防御的同时完美保留了智能体的行为多样性。
*   **主动性**：通过前瞻性模拟，FLP 能够在病毒传播给下一个智能体之前就将其拦截。

### 7. 优点
*   **无需训练**：完全在推理侧运行，不需要更新模型参数，部署成本低。
*   **精准恢复**：通过RBD算法实现了对相册的“外科手术式”清理，而非简单的全局抑制。
*   **普适性强**：实验证明该方法在多种主流多模态大模型上均有效。
*   **理论支撑**：论文提供了关于信息瓶颈和熵保留的理论分析，证明了局部净化优于全局偏置。

### 8. 不足与局限
*   **推理开销**：内部多角色模拟会显著增加推理延迟（实验显示 $n=4$ 时时间开销约为 2.0x）。虽然文中提到可以异步执行，但在实时性要求极高的场景下仍是挑战。
*   **场景覆盖**：目前主要集中在多模态（图像-文本）越狱，对于纯文本传染性越狱或其他复杂任务类型的泛化性有待进一步验证。
*   **对抗演进**：如果攻击者专门针对“多样性检测”进行对抗优化（即让恶意输出也表现出伪装的多样性），该防御的有效性可能会受影响。

（完）
