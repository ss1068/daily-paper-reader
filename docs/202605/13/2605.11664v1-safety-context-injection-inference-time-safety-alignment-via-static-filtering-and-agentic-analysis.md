---
title: "Safety Context Injection: Inference-Time Safety Alignment via Static Filtering and Agentic Analysis"
title_zh: 安全上下文注入：通过静态过滤与智能体分析实现的推理侧安全对齐
authors: "Zhenhao Xu, Wenhan Chang, Yichuan Chen, Yuxin Fang, Junhao Liu, Tianqing Zhu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11664v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 推理侧安全对齐与过滤
tldr: 针对大语言模型在推理阶段的安全对齐难题，本文提出安全上下文注入（SCI）框架。该框架将安全评估与任务生成分离，通过注入外部生成的风险报告来引导模型。SCI 包含轻量级的静态模型过滤（SMF）和基于智能体循环的动态代理过滤（DAF）两种变体，分别应对低延迟需求和复杂的长文本伪装攻击，在多种越狱场景下显著提升了黑盒模型的安全性。
selection_source: fresh_fetch
motivation: 在黑盒部署环境下，大模型面临伪装意图、高延迟压力及长文本干扰等推理侧安全挑战，导致模型可能在思考后仍输出不安全内容。
method: 提出 SCI 框架，利用 SMF 进行快速单次过滤或通过 DAF 智能体循环迭代合成风险报告，并将其作为前置上下文注入受保护模型。
result: 在 AdvBench 和 GPTFuzz 等基准测试中，该方法在五类越狱攻击下均显著降低了攻击成功率和输出毒性。
conclusion: SCI 框架通过解耦安全分析与内容生成，为黑盒模型提供了一种兼顾效率与防御深度的推理侧安全对齐方案。
---

## 摘要
大语言推理模型（LRMs）提升了复杂任务的性能，但也增加了部署时的安全控制难度。在黑盒场景下，防御者无法修改模型权重，必须在推理阶段进行干预。这种设定带来了三个实际挑战：有害意图可能被教育或角色扮演框架所掩盖；深层安全分析可能引入显著的延迟；长对抗性上下文可能会稀释简单过滤器所依赖的局部线索。这些挑战可能导致明显的“思考-输出差距”，即模型在推理过程中表现得十分谨慎，但最终仍会产生不安全的回答。为解决这一问题，我们提出了安全上下文注入（SCI），这是一种推理侧框架，它将安全评估与任务生成分离，并将结构化的外部风险报告作为注入的安全上下文前置于受保护模型。该框架通过两种互补的变体实现：静态模型过滤（SMF），一种用于快速部署的轻量级单次通过防御；以及动态智能体过滤（DAF），一种基于智能体循环的分析器，能够针对模糊或长上下文攻击迭代地收集并综合证据。在 AdvBench 和 GPTFuzz 数据集上，涵盖五类越狱家族的基础模型和推理模型测试中，两种变体在评估设置下均降低了攻击成功率和毒性。SMF 提供了一种高效的低延迟选项，而 DAF 在有害意图被语义伪装或分散在长上下文中时更为有效。

## Abstract
Large Reasoning Models (LRMs) improve performance on complex tasks, but they also make safety control harder at deployment time. In black-box settings, defenders cannot modify model weights and must instead intervene at inference time. This setting creates three practical challenges: harmful intent may be hidden by educational or role-play framing, deep safety analysis can introduce non-trivial latency, and long adversarial contexts can dilute the local cues that simpler filters rely on. These challenges can expose an apparent thinking--output gap, where the model appears cautious during reasoning but still produces an unsafe final answer. To address this problem, we propose Safety Context Injection (SCI), an inference-time framework that separates safety assessment from task generation and prepends a structured external risk report as injected safety context for the protected model. The framework is instantiated in two complementary variants: Static Model Filtering (SMF), a lightweight one-pass guard for fast deployment, and Dynamic Agents Filtering (DAF), an agentic-loop-based analyzer that iteratively gathers and synthesizes evidence for ambiguous or long-context attacks. Across AdvBench and GPTFuzz, spanning base and reasoning models under five jailbreak families, both variants reduce attack success rate and toxicity in the evaluated settings. SMF offers an efficient low-latency option, while DAF is more effective when harmful intent is semantically disguised or dispersed across long contexts.

---

## 论文详细总结（自动生成）

以下是对论文《Safety Context Injection: Inference-Time Safety Alignment via Static Filtering and Agentic Analysis》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言推理模型（LRMs，如 DeepSeek-R1）通过思维链（CoT）提升了复杂任务处理能力，但也暴露了新的安全风险。
*   **核心问题**：论文发现 LRMs 存在“**思考-输出差距**”（Thinking-Output Gap），即模型在推理过程中表现得十分谨慎，但最终输出却可能产生违规内容。
*   **研究动机**：在黑盒部署环境下（无法修改模型权重），现有的防御手段面临三大挑战：
    1.  **意图模糊性**：有害意图常被教育、虚构或角色扮演等外壳伪装。
    2.  **效率成本**：深层安全分析会带来显著的延迟和 Token 消耗。
    3.  **长上下文稀释**：攻击信号可能分散在长文本中，导致浅层过滤器失效。

### 2. 方法论：安全上下文注入（SCI）
SCI 的核心思想是将**安全评估**与**内容生成**解耦，通过外部防御器生成结构化的“风险报告”，并将其作为前置上下文注入到受保护模型的 Prompt 中。

*   **核心流程**：
    1.  **拦截**：防御器拦截用户查询 $x$。
    2.  **评估**：通过安全决策函数 $\Phi(x)$ 判断是否直接拦截。
    3.  **注入**：若不拦截，生成结构化报告 $R$，构造增强上下文 $x' = R \oplus x$。
    4.  **生成**：受保护模型基于增强上下文生成最终回答。

*   **两种变体技术细节**：
    *   **静态模型过滤（SMF）**：轻量级“单次通过”防御。使用专用 Guard 模型（如 Qwen-Guard）进行快速分类，生成包含安全等级和风险类别的简短报告。适用于低延迟需求。
    *   **动态智能体过滤（DAF）**：基于“智能体循环”的深度分析。实例化一个“安全分析师”智能体，可自主调用工具箱（实体分析、模式检测、外部分类器等）迭代收集证据，生成详尽的风险分析报告。适用于复杂伪装或长文本攻击。

### 3. 实验设计
*   **数据集与基准测试**：使用了 **AdvBench** 和 **GPTFuzz** 两个公开安全基准。
*   **攻击场景**：涵盖五类代表性越狱攻击家族：
    *   DarkCite（引用伪装）、DRA（伪装与重构）、CoL-SingleTurn/MultiTurn（合成叙事诱导）、AutoRAN（针对推理模型的自动化劫持）。
*   **受保护模型**：
    *   基础模型：DeepSeek-V3, Qwen3-Instruct。
    *   推理模型：DeepSeek-R1, Qwen3-Thinking。
*   **对比方法**：对比了无防御状态（Original）与开启 SMF 或 DAF 防御后的表现。
*   **评估指标**：攻击成功率（ASR）和毒性评分（TS，1-5分制，由独立裁判模型评分）。

### 4. 资源与算力
*   **硬件环境**：实验在 **1 台 NVIDIA A100 GPU** 上运行。
*   **算力用途**：主要用于托管本地 Guard 模型（如 Qwen-Guard 4B）、运行指标聚合算法、进行嵌入空间可视化（PCA）以及调用远程 API 时的预处理。
*   **训练时长**：文中未提及模型训练时长，因为该方法属于**推理侧（Inference-time）**防御，侧重于即时干预而非重新训练。

### 5. 实验数量与充分性
*   **实验规模**：覆盖了 4 种主流大模型、5 类攻击家族、2 个数据集，每组实验包含 100 到 520 个 Prompt 样本。
*   **分析维度**：除了核心的 ASR 和 TS 指标外，还进行了：
    *   **显著性检验**（Paired t-test）。
    *   **Token 开销分析**（评估防御带来的额外成本）。
    *   **语义空间分析**（通过 PCA 降维展示注入报告如何改变推理轨迹）。
*   **客观性**：实验设计较为客观，采用了独立于防御系统的裁判模型进行评分，并对比了不同后端（DeepSeek-V3.2 和 GPT-OSS-20B）对 DAF 效果的影响。

### 6. 主要结论与发现
*   **防御有效性**：SMF 和 DAF 均能显著降低 ASR 和输出毒性。例如，在 AdvBench 上，SMF 将 DS-R1 的 DarkCite ASR 从 0.75 降至 0.14。
*   **弥合差距**：注入的安全报告能有效引导模型在推理过程中识别隐藏风险，从而消除“思考-输出差距”。
*   **性能权衡**：SMF 延迟极低（额外 Token 约 28 个），而 DAF 虽然防御力更强，但 Token 开销显著增加（约 180-320 个）。
*   **语义耦合**：PCA 分析显示，DAF 生成的报告在语义空间上与模型的推理轨迹有重叠，证明了注入上下文的引导作用。

### 7. 优点与亮点
*   **即插即用**：无需修改受保护模型的权重，非常适合闭源 API 模型的安全增强。
*   **解耦防御**：将安全逻辑从生成逻辑中分离，避免了模型在处理复杂指令时因“分心”而忽略安全准则。
*   **多层级策略**：提供了 SMF 和 DAF 两种方案，允许开发者根据业务对延迟和安全性的不同要求进行灵活选择。

### 8. 不足与局限
*   **Token 成本**：DAF 变体带来的额外 Token 消耗较大，在大规模高并发场景下可能增加运营成本。
*   **统计样本限制**：显著性检验仅基于 5 类攻击家族，样本量相对较小，可能限制了统计效能。
*   **防御上限**：虽然显著降低了风险，但在面对某些极端复杂的推理劫持攻击（如 AutoRAN）时，仍存在一定的残余攻击成功率。
*   **应用限制**：该方法依赖于防御器本身的能力，如果防御器（Guard Model 或 Agent）被针对性绕过，整个防御链条将失效。

（完）
