---
title: On-Policy Self-Evolution via Failure Trajectories for Agentic Safety Alignment
title_zh: 基于失败轨迹的同策略自进化智能体安全对齐
authors: "Bo Yin, Qi Li, Xinchao Wang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11882v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 针对智能体安全对齐的在线策略自我演化
tldr: 针对工具使用型LLM智能体在复杂轨迹中可能出现的安全风险（如不安全工具调用、过度拒绝等），本文提出FATE框架。该框架通过在线自我进化，将验证器评分的失败轨迹转化为修复监督信号，无需专家演示。结合帕累托前沿策略优化（PFPO），在提升安全性的同时保持了任务效用，显著降低了攻击成功率和有害合规性，为智能体安全对齐提供了新思路。
selection_source: fresh_fetch
motivation: 现有的安全对齐方法多关注响应层面且存在安全与效用的权衡问题，难以处理智能体在复杂工具调用轨迹中的安全风险。
method: 提出FATE框架，利用策略自身生成修复候选并经多维度验证器筛选，配合帕累托前沿策略优化（PFPO）进行在线自我进化。
result: "在多个基准测试中，FATE显著降低了攻击成功率（33.5%）和有害合规性（82.6%），同时保持了良好的任务执行效用。"
conclusion: 研究证明失败轨迹可作为结构化修复监督信号，通过在线自我进化有效提升智能体的安全性和实用性平衡。
---

## 摘要
使用工具的 LLM 智能体通过轨迹而非仅仅是最终响应而失败，因为它们可能会执行不安全的工具调用、遵循注入的指令、服从有害请求，或者在产生看似安全的回答时过度拒绝良性任务。现有的安全对齐信号大多是响应级的或离策略的，且通常会导致安全与效用之间的权衡：提高智能体安全性是以降低任务性能为代价的。这种稀疏且单一目标的奖励严重限制了现实世界的可用性。为了弥补这一差距，我们提出了 FATE，这是一个同策略自进化框架，它在没有专家演示的情况下，将验证器评分的失败转化为修复监督。对于每一次失败，相同的策略会提出修复候选方案，然后由验证器重新评分，并根据安全性、效用、过度拒绝控制和轨迹有效性进行过滤。这种密集的轨迹级信息随后被用作智能体自进化的监督信号。在此过程中，我们进一步引入了帕累托前沿策略优化（PFPO），将监督预热与帕累托感知策略优化相结合，以维持安全与效用之间的权衡。在 AgentDojo、AgentHarm 和 ATBench 上的实验表明，FATE 在不同模型和规模上提高了安全性，同时保留了有用的行为。与强基准相比，FATE 将攻击成功率降低了 33.5%，有害服从降低了 82.6%，并将外部轨迹安全诊断提高了 6.5%。这些结果表明，失败轨迹可以为更安全的自进化智能体提供结构化的修复监督。

## Abstract
Tool-using LLM agents fail through trajectories rather than only final responses, as they may execute unsafe tool calls, follow injected instructions, comply with harmful requests, or over-refuse benign tasks despite producing a seemingly safe answer. Existing safety-alignment signals are largely response-level or off-policy, and often incur a safety-utility trade-off: improving agent safety comes at the cost of degraded task performance. Such sparse and single-objective rewards severely limit real-world usability. To bridge this gap, we propose FATE, an on-policy self-evolving framework that transforms verifier-scored failures into repair supervision without expert demonstrations. For each failure, the same policy proposes repair candidates, which are then re-scored by verifiers and filtered across security, utility, over-refusal control, and trajectory validity. This dense trajectory-level information is then used as a supervision signal for agent self-evolution. During this process, we further introduce Pareto-Front Policy Optimization (PFPO), combining supervised warmup with Pareto-aware policy optimization to preserve safety-utility trade-offs. Experiments on AgentDojo, AgentHarm, and ATBench show that FATE improves safety across different models and scales while preserving useful behavior. Compared with strong baselines, FATE reduces attack success rate by 33.5%, harmful compliance by 82.6%, and improves external trajectory-safety diagnosis by 6.5%. These results suggest that failed trajectories can provide structured repair supervision for safer self-evolving agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FATE** (FAilure-Trajectory Evolution) 的框架，旨在解决工具使用型大语言模型（LLM）智能体在复杂任务轨迹中的安全对齐问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 安全对齐主要集中在“响应级”（即一问一答），而智能体（Agent）的失败通常发生在“轨迹级”。例如，智能体可能给出了看似无害的最终回答，但在执行过程中调用了不安全的工具、泄露了隐私或遵循了注入的恶意指令。
*   **研究动机**：
    *   **安全-效用权衡**：现有的安全训练往往导致模型“过度拒绝”（Over-refusal），即为了安全而牺牲了处理正常任务的能力。
    *   **信号稀疏**：缺乏高质量的专家演示来指导智能体如何修复失败的轨迹。
    *   **离策略偏差**：现有的对齐数据往往与模型自身的失败分布不一致。

### 2. 方法论：FATE 框架
FATE 是一个**同策略（On-policy）自进化框架**，其核心思想是利用智能体自身的失败轨迹作为原始材料，通过验证器引导生成修复后的监督信号。

*   **核心流程**：
    1.  **失败挖掘（Failure Collection）**：让当前策略 $\pi_{\theta}$ 在任务中运行，收集被验证器判定为失败的轨迹（包括安全性失败、效用失败、过度拒绝等）。
    2.  **同策略修复建议（Same-policy Repair Proposal）**：使用**同一个策略**，根据失败轨迹和验证器的反馈，生成多个修复候选方案。
    3.  **帕累托前沿监督构建（Pareto-Front Construction）**：
        *   **可行性过滤**：剔除那些依然不安全、无效或格式错误的候选方案。
        *   **帕累托筛选**：在安全性、效用、过度拒绝控制、轨迹控制四个维度上寻找“非劣解”，确保修复后的轨迹在多个目标间取得平衡。
    4.  **策略精炼（Policy Refinement）**：
        *   **SFT 预热**：在筛选出的优质修复轨迹上进行监督微调。
        *   **PFPO（帕累托前沿策略优化）**：一种改进的强化学习算法，通过组相对优势（Group-relative advantage）进一步优化模型对多目标平衡的偏好。

### 3. 实验设计
*   **数据集/场景**：
    *   **AgentDojo**：评估间接提示注入攻击（Indirect Prompt Injection）。
    *   **AgentHarm**：评估对有害请求的服从性。
    *   **ATBench**：用于评估外部轨迹安全诊断的泛化能力。
*   **对比基准（Baselines）**：
    *   基础模型（Base models）。
    *   提示词工程方法：ReAct, Reflexion。
    *   运行时防御：Tool Filter, PI Detector。
    *   其他变体：仅 SFT、安全优先的 GRPO 等。
*   **覆盖模型**：涵盖了 Qwen3 (0.6B 到 32B)、Llama-3.1-8B、Ministral-3-8B、Gemma-3-12B 和 Phi-4 等多个家族。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **技术细节**：提到使用了 **LoRA** 进行参数高效微调，优化器为 AdamW，学习率在 $10^{-5}$ 到 $10^{-6}$ 级别，PFPO 的组大小（Group size）为 8。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了 5 个不同的模型家族和 6 个不同的模型参数规模。
    *   进行了多轮（最多 5 轮）自进化迭代实验。
    *   包含了详尽的消融实验（验证了验证器重评分、帕累托筛选、PFPO 等组件的必要性）。
*   **充分性评价**：实验设计非常充分且客观。通过在“留出集”（Held-out test set）上报告结果，避免了过拟合训练任务的嫌疑；同时引入了外部基准 ATBench 验证泛化性，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **显著提升安全性**：相比强基准，FATE 将攻击成功率（ASR）降低了 **33.5%**，有害服从率（HCR）降低了 **82.6%**。
*   **保持效用**：在提升安全性的同时，任务成功率（TSR）和有效拒绝率（VRR）均有显著提升，有效缓解了过度拒绝问题。
*   **自进化能力**：随着进化轮数的增加，模型性能持续提升，表明失败轨迹可以作为持续改进的结构化监督信号。
*   **规模效应**：FATE 在不同规模的模型上均有效，且在大参数模型上表现出更强的安全-效用平衡能力。

### 7. 优点与亮点
*   **无需专家数据**：完全依赖模型自生成和验证器筛选，降低了对昂贵人工标注的依赖。
*   **多目标优化**：通过帕累托前沿（Pareto-Front）巧妙地解决了安全与效用之间的冲突，而不是简单地将多个指标加权求和。
*   **轨迹级对齐**：深入到智能体执行的中间步骤，比单纯的输入/输出过滤更本质。

### 8. 不足与局限
*   **验证器依赖**：FATE 的效果高度依赖于验证器的质量。如果验证器本身存在偏差或无法识别复杂的安全风险，修复信号就会失效。
*   **采样成本**：在自进化过程中需要生成大量的修复候选并进行多次环境回滚（Rollout），推理和验证的计算开销较大。
*   **场景局限**：目前主要在受控的基准环境（如 AgentDojo）中测试，对于现实世界中长程、涉及人类协作的复杂智能体场景，其表现仍待验证。

（完）
