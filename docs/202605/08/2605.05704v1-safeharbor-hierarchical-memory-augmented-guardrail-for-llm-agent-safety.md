---
title: "SafeHarbor: Hierarchical Memory-Augmented Guardrail for LLM Agent Safety"
title_zh: SafeHarbor：面向大语言模型智能体安全的分层记忆增强护栏
authors: "Zhe Liu, Zonghao Ying, Wenxin Zhang, Quanchen Zou, Deyue Zhang, Dongdong Yang, Xiangzheng Zhang, Hao Peng"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05704v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 用于LLM代理安全的分层内存增强护栏
tldr: SafeHarbor是一个针对大语言模型（LLM）智能体安全性的分层记忆增强护栏框架。针对现有防御机制在安全性与实用性之间难以平衡（过度拒绝）的问题，该框架通过对抗性生成提取上下文感知的防御规则，并利用分层记忆系统进行动态注入。它无需训练且即插即用，结合信息熵自演化机制优化记忆结构，在保持高防御率的同时显著提升了良性任务的实用性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体防御机制往往因过度拒绝而损害良性任务的实用性，亟需建立更精确的安全决策边界。
method: 提出一种基于分层记忆系统的动态规则注入框架，利用对抗生成提取规则，并通过信息熵机制实现记忆结构的自演化。
result: "在GPT-4o上实现了63.6%的良性任务实用性，同时对恶意请求保持了超过93%的拒绝率，达到SOTA水平。"
conclusion: SafeHarbor通过动态、可进化的记忆增强机制，有效解决了LLM智能体在安全防御与功能实用性之间的权衡难题。
---

## 摘要
随着基础模型的快速演进，大语言模型（LLM）智能体展现出日益强大的工具使用能力。然而，这种熟练程度也带来了显著的安全风险，因为恶意攻击者可以操纵智能体执行工具以生成有害内容。尽管现有的防御机制行之有效，但它们经常面临过度拒绝（over-refusal）问题，即安全严格性的提高会损害智能体在良性任务上的效用。为了缓解这一权衡，我们提出了 SafeHarbor，这是一个旨在为 LLM 智能体建立精确决策边界的新型框架。与静态指南不同，SafeHarbor 通过增强的对抗生成提取上下文感知的防御规则。我们设计了一个局部分层记忆系统用于动态规则注入，提供了一种无需训练、高效且即插即用的解决方案。此外，我们引入了一种基于信息熵的自我进化机制，通过动态节点分裂和合并不断优化记忆结构。大量实验表明，SafeHarbor 在模糊良性任务和显式恶意攻击上均达到了最先进的性能，特别是在 GPT-4o 上实现了 63.6% 的峰值良性效用，同时对有害请求保持了超过 93% 的稳健拒绝率。源代码已在 https://github.com/ljj-cyber/SafeHarbor 公开。

## Abstract
With the rapid evolution of foundation models, Large Language Model (LLM) agents have demonstrated increasingly powerful tool-use capabilities. However, this proficiency introduces significant security risks, as malicious actors can manipulate agents into executing tools to generate harmful content. While existing defensive mechanisms are effective, they frequently suffer from the over-refusal problem, where increased safety strictness compromises the agent's utility on benign tasks. To mitigate this trade-off, we propose \textsc{SafeHarbor}, a novel framework designed to establish precise decision boundaries for LLM agents. Unlike static guidelines, \textsc{SafeHarbor} extracts context-aware defense rules through enhanced adversarial generation. We design a local hierarchical memory system for dynamic rule injection, offering a training-free, efficient, and plug-and-play solution. Furthermore, we introduce an information entropy-based self-evolution mechanism that continuously optimizes the memory structure through dynamic node splitting and merging. Extensive experiments demonstrate that \textsc{SafeHarbor} achieves state-of-the-art performance on both ambiguous benign tasks and explicit malicious attacks, notably attaining a peak benign utility of 63.6\% on GPT-4o while maintaining a robust refusal rate exceeding 93\% against harmful requests. The source code is publicly available at https://github.com/ljj-cyber/SafeHarbor.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SafeHarbor** 的框架，旨在解决大语言模型（LLM）智能体在执行工具调用任务时的安全防御与实用性之间的失衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着 LLM 智能体从单纯的对话转向能够操作外部工具（如文件系统、API、邮件等），其安全风险从“生成有害文本”升级为“执行有害操作”。
*   **核心问题**：现有的防御机制（如静态护栏或安全微调）存在**边界模糊**和**过度拒绝（Over-refusal）**的问题。为了保证安全，防御系统往往会误伤复杂的良性任务（例如，管理员备份文件可能被误判为数据外泄）。
*   **整体含义**：论文旨在通过构建一个动态的、上下文感知的“分层记忆增强护栏”，为智能体划定精确的决策边界，在保持高防御率的同时，最大限度地保留良性任务的执行能力。

### 2. 方法论：核心思想与关键技术
SafeHarbor 采用了一种无需对基座模型进行大规模重训练的“即插即用”方案，分为三个阶段：
*   **对抗性规则生成（离线阶段）**：
    *   利用攻击生成器对有害轨迹进行变体扩充（如目标分解、权限提升、语境重构）。
    *   通过 LLM 提取针对性的安全规则，每条规则包含一对：**禁止项（Prohibition）**和**豁免项（Exemption）**。这种双向定义是减少过度拒绝的关键。
*   **双重知识存储与分层记忆（存储阶段）**：
    *   **分层记忆树**：将规则组织成树状结构，内部节点负责路由，叶子节点存储具体的规则对。
    *   **自演化机制**：引入基于**信息熵（Information Gain）**的节点分裂与合并算法。当新样本引入的“惊奇度”超过阈值时，分裂出新节点；否则合并到现有节点，实现记忆的动态优化。
    *   **安全投影器（Safety Projector）**：训练一个轻量级的 MLP，利用对比学习（Contrastive Loss）将查询映射到几何空间，计算其与“有害中心”和“良性中心”的距离。
*   **双路径推理与检索（在线阶段）**：
    *   **快速路径（Fast Path）**：对于明显良性的查询，直接通过投影器和相似度计算放行，降低延迟。
    *   **慢速路径（LLM Judgment）**：对于模糊或高风险查询，检索记忆树中最相关的规则，由 LLM 作为裁判，结合上下文和“豁免条款”做出最终裁决。

### 3. 实验设计
*   **数据集与场景**：
    *   **AgentHarm**：专注于复杂的多步智能体滥用行为（11 个类别，440 个任务）。
    *   **AgentSafetyBench**：涵盖 349 个环境，评估对 8 类安全风险和 10 种失败模式的鲁棒性。
    *   **训练数据**：仅使用 AgentAlign 数据集构建防御记忆，确保测试集的独立性。
*   **对比方法（Baselines）**：
    *   **提示词方法**：Rule Traverse。
    *   **记忆增强方法**：A-Mem, 标准 RAG。
    *   **外部防御模型**：Llama-Guard-3-8B。
    *   **多智能体框架**：GuardAgent。
    *   **微调方法**：AgentAlign (SFT)。
*   **评估指标**：拒绝率（Refusal Rate）、有害得分（Harm Score）、良性任务得分（Benign Score）等。

### 4. 资源与算力
*   **模型使用**：使用 **Qwen2.5-72B** 作为离线阶段的规则生成器和在线阶段的高级验证器（在某些配置下）。
*   **算力消耗**：
    *   论文提到 SafeHarbor 具有极高的资源效率。
    *   **显存占用**：仅需 **14GB VRAM**（相比之下，LlamaGuard 需要 30GB）。
    *   **推理延迟**：平均端到端延迟为 **306.67ms**，比 GuardAgent 快约 20 倍。
    *   **训练时长**：未明确给出 MLP 投影器的具体训练时长，但强调其为轻量级架构，训练开销极小。

### 5. 实验数量与充分性
*   **实验规模**：在 GPT-4o、Mistral-8B 和 Qwen2.5-7B 三个主流基座模型上进行了广泛测试。
*   **消融实验**：针对对抗增强、记忆树结构、良性豁免规则、安全投影器和 LLM 裁判等核心组件分别进行了消融测试，验证了各部分的必要性。
*   **敏感性分析**：对超参数（如对比损失权重、相似度阈值、信息增益阈值）进行了详尽的网格搜索和分析。
*   **充分性评价**：实验设计覆盖了从闭源 SOTA 模型到开源主流模型，且对比了多种不同类型的防御策略，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：SafeHarbor 在 GPT-4o 上实现了 **63.6% 的良性实用性**，同时保持了 **93% 以上的有害请求拒绝率**，显著优于 LlamaGuard 等基准。
*   **缓解过度拒绝**：通过引入“豁免条款”和上下文感知的规则检索，SafeHarbor 有效解决了传统防御中“一刀切”的问题。
*   **效率优势**：双路径设计使得大部分良性流量能通过低功耗的快速路径处理，大幅降低了防御系统的运行成本。

### 7. 优点
*   **精确的决策边界**：通过“禁止+豁免”的双向规则定义，实现了比传统线性分类器更精细的安全控制。
*   **动态自演化**：基于信息熵的记忆管理机制使系统能够随着新威胁的出现而自动更新，无需频繁重训练。
*   **即插即用与低延迟**：作为外部护栏，不改变基座模型参数，且推理速度极快，适合实时部署。

### 8. 不足与局限
*   **对“老师”模型的依赖**：离线规则生成和在线高级裁决高度依赖于高性能 LLM（如 Qwen-72B 或 GPT-4o），如果使用的教师模型本身存在偏见或安全漏洞，可能会影响防御效果。
*   **记忆规模上限**：虽然采用了分层结构，但随着攻击模式的无限增加，记忆树的检索效率和存储压力仍可能面临长期的扩展性挑战。
*   **环境感知局限**：目前的防御主要基于查询和轨迹的文本表示，对于某些涉及复杂物理环境或多模态交互的智能体场景，其防御能力尚未得到充分验证。

（完）
