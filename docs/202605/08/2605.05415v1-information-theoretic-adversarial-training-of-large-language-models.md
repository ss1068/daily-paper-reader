---
title: Information Theoretic Adversarial Training of Large Language Models
title_zh: 大语言模型的信息论对抗训练
authors: "Yiwei Zhang, Jeremiah Birrell, Reza Ebrahimi, Rouzbeh Behnia, Jason Pacheco, Elisa Bertino"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05415v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 提升大模型鲁棒性的对抗训练框架
tldr: 针对大语言模型易受对抗攻击且现有防御方法计算成本高的问题，本文提出WARDEN框架。该框架基于信息论，通过f-散度在经验分布周围构建分布鲁棒优化目标，动态重加权对抗样本以聚焦困难案例。实验证明，WARDEN在保持模型效能的同时，显著降低了多种攻击下的成功率，为可扩展的鲁棒对齐提供了高效方案。
selection_source: fresh_fetch
motivation: 现有大语言模型对抗训练方法计算开销大且难以扩展，且无法有效处理不同难度的对抗样本。
method: 提出WARDEN框架，利用f-散度构建分布鲁棒优化目标，通过KL散度的对偶形式实现对抗样本的动态重加权。
result: 在多个模型和攻击场景下，WARDEN显著降低了攻击成功率，且计算成本和模型效能损耗与现有基准方法相当。
conclusion: WARDEN是一种基于信息论的高效、可扩展的对抗训练方法，能有效提升大语言模型的安全性与鲁棒性。
---

## 摘要
尽管在对齐和安全性方面取得了进展，大语言模型（LLMs）在对抗性提示面前仍然十分脆弱，在新型攻击策略下往往表现出有害行为。虽然对抗训练可以提高鲁棒性，但现有方法计算成本高昂且难以扩展。最近的连续对抗训练方法，如连续对抗训练（CAT）和连续对抗偏好优化（CAPO），通过利用嵌入空间中基于梯度的扰动来应对这一挑战，从而实现更高效且更具表达力的攻击。基于这一范式，我们提出了 WARDEN，这是一个针对 LLM 的分布鲁棒对抗训练框架，它通过经验训练分布周围的 f-散度模糊集动态地重新加权对抗样本。我们的方法在经验数据分布周围的散度球内优化最坏情况下的对抗损失，自动强调更难的对抗样本。利用凸对偶公式，在 KL 散度下，目标函数简化为 log-sum-exp 形式，并由一个动态参数控制重加权的强度。这项研究引出了一类新的信息论目标函数，在保持模型效用的同时显著降低了攻击成功率。在多个 LLM 和攻击设置中，WARDEN 显著降低了攻击成功率，其计算和效用成本与基于 CAT、CAPO 和 MixAT 的基准方法相当，使其成为实现可扩展鲁棒对齐的一种实用方法。

## Abstract
Large language models (LLMs) remain vulnerable to adversarial prompting despite advances in alignment and safety, often exhibiting harmful behaviors under novel attack strategies. While adversarial training can improve robustness, existing approaches are computationally expensive and difficult to scale. Recent continuous adversarial training methods, such as Continuous adversarial training (CAT) and Continuous Adversarial Preference Optimization (CAPO), address this challenge by leveraging gradient-based perturbations in the embedding space, enabling more efficient and expressive attacks. Building on this paradigm, we propose WARDEN, a distributionally robust adversarial training framework for LLMs that dynamically reweights adversarial examples through an f -divergence ambiguity set around the empirical training distribution. Our method optimizes the worst-case adversarial loss within a divergence ball around the empirical data distribution, automatically emphasizing harder adversarial examples. Using the convex dual formulation, the objective reduces to a log-sum-exp form under the KL divergence, with a dynamical parameter controlling the strength of reweighting. This study leads to a new class of information-theoretic objectives that significantly reduce attack success rates while maintaining model utility. Across multiple LLMs and attack settings, WARDEN substantially reduces attack success rates with computational and utility costs comparable to CAT-, CAPO-, and MixAT-based baselines, making it a practical approach for scalable robust alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Information Theoretic Adversarial Training of Large Language Models》（大语言模型的信息论对抗训练）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
尽管大语言模型（LLM）经过了安全对齐，但在对抗性提示（Adversarial Prompting）面前依然脆弱。现有的对抗训练（AT）方法虽然有效，但计算成本极高且难以扩展。近期出现的“连续对抗训练”（如 CAT、CAPO）通过在嵌入空间进行梯度扰动提高了效率，但这些方法通常对所有样本的对抗损失进行**均匀聚合（平均化）**。
**核心问题：** 均匀聚合会掩盖那些出现频率低但损失值极高的“困难对抗样本”，而这些样本往往是模型最严重的漏洞。本文旨在通过信息论方法，动态地为这些高损失样本分配更高权重，从而提升模型的鲁棒性。

### 2. 核心方法论：WARDEN 框架
论文提出了 **WARDEN**（Worst-case Adversarial Reweighting via f-DivergENces），其核心思想是将对抗训练转化为一个**分布鲁棒优化（DRO）**问题。

*   **核心思想：** 不再最小化经验分布下的平均损失，而是在以经验分布为中心、以 $f$-散度为半径的“模糊集”（Ambiguity Set）内，最小化**最坏情况**下的期望损失。
*   **关键技术细节：**
    *   **对偶转化：** 利用凸对偶理论，将复杂的内层最大化问题转化为易于处理的 `log-sum-exp` 形式（在 KL 散度下）。
    *   **动态重加权：** 引入对偶变量 $\lambda$ 来控制重加权的强度。$\lambda$ 越小，目标函数越倾向于关注高损失样本（即更“尖锐”的聚合）。
    *   **两种策略：** 
        1. **WARDEN-L（Learnable）：** 将 $\lambda$ 视为可学习参数，随模型一同优化。
        2. **WARDEN-O（Optimized）：** 在每个训练迭代中，使用**二分法（Bisection Method）**精确求解对偶问题的最优 $\lambda$。
*   **模块化设计：** WARDEN 作为一个轻量级的损失聚合层，可以无缝集成到现有的连续对抗训练流水线（如 CAT、CAPO、MixAT）中，而不改变攻击生成过程。

### 3. 实验设计
*   **实验对象（模型）：** Zephyr-7B、Mistral-7B、Llama2-7B、Llama3-8B。
*   **对比基准（Baselines）：** 原始模型（Plain）、CAT、CAPO、MixAT。
*   **评估数据集与场景：**
    *   **安全性（Robustness）：** 使用 HarmBench 评估，涵盖直接有害请求（DR）、人类编写的越狱（HH）、AutoDAN 和 GCG 攻击。
    *   **效用性（Utility）：** 使用 MMLU（知识）、ARC（推理）以及 Harmless-query（良性指令遵循）。
*   **样本量：** 由于优化攻击成本高，遵循前人工作，在 HarmBench 的 40 个非版权样本子集上进行评估。

### 4. 资源与算力
*   **算力投入：** 论文明确指出所有实验总计耗时至少 **1,162 GPU 小时**。
*   **硬件型号：** 使用了内部和外部集群的 **NVIDIA A100 (40GB 或 80GB)** 以及 G4 实例。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 个主流开源模型家族、4 种不同类型的对抗攻击、以及 4 个效用评估指标。
*   **消融实验：** 针对 KL 散度半径 $\epsilon$ 的敏感度、$\lambda$ 的不同处理策略（固定 vs 学习 vs 优化）进行了详细对比。
*   **充分性评价：** 实验设计较为充分，通过跨模型、跨攻击方式的对比验证了方法的通用性。使用“优化策略”求解 $\lambda$ 的对比实验展示了该方法相对于简单加权的优越性，逻辑闭环较为完整。

### 6. 主要结论与发现
*   **显著降低攻击成功率（ASR）：** 在 Mistral-7B 上，WARDEN 将 CAPO 的平均 ASR 从 44.12% 降低至 20.63%；在 Llama3-8B 上，将 CAT 的 ASR 从 6.25% 降至 4.5%。
*   **效用保持：** 在大幅提升鲁棒性的同时，模型的 MMLU 和推理能力得分与基准方法持平，甚至在某些情况下通过减少过度拒绝提升了良性表现。
*   **动态优化的必要性：** 实验证明 WARDEN-O（优化策略）优于 WARDEN-L（学习策略）和固定参数策略，说明针对每个微批次（Minibatch）动态调整重加权强度至关重要。

### 7. 优点与亮点
*   **理论完备：** 基于分布鲁棒优化（DRO）和信息论对偶理论，为对抗样本加权提供了坚实的数学基础。
*   **即插即用：** 作为一个损失聚合层，它不增加攻击生成的计算开销，易于集成到现有的 LLM 训练框架中。
*   **效率平衡：** 相比于生成更多对抗样本，通过“更聪明地利用现有样本”来提升性能，是一种高性价比的方案。

### 8. 不足与局限
*   **超参数敏感性：** 引入了 $\epsilon$（半径）和 $\kappa$（正则化）等超参数，需要针对不同模型进行微调以平衡鲁棒性与效用。
*   **覆盖范围限制：** 尽管重加权了已知困难样本，但仍受限于训练数据中对抗样本的质量和多样性，无法保证对完全未见的攻击类型具有绝对防御力。
*   **评估规模：** 受限于计算资源，安全性评估仅在 HarmBench 的小规模子集（40个样本）上进行，可能存在一定的统计偏差。

（完）
