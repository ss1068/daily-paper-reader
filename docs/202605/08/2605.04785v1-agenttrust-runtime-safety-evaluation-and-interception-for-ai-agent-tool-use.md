---
title: "AgentTrust: Runtime Safety Evaluation and Interception for AI Agent Tool Use"
title_zh: AgentTrust：AI 智能体工具使用的运行时安全评估与拦截
authors: Chenglin Yang
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04785v1"
tags: ["query:jb-def"]
score: 9.0
evidence: AI智能体的运行时安全评估与拦截
tldr: "AgentTrust是一个针对AI智能体工具调用的运行时安全拦截层，旨在解决现有防御手段难以应对混淆攻击和多步上下文风险的问题。它通过在执行前拦截文件、Shell、HTTP等工具调用，结合去混淆、风险链检测及LLM评判，提供实时的安全决策。实验显示其在多类风险场景下准确率超过95%，且具备极低延迟，为构建安全可信的AI智能体提供了重要保障。"
selection_source: fresh_fetch
motivation: 现有的静态防护和沙箱机制无法充分理解工具调用的语义，难以防御复杂的混淆攻击及多步骤导致的不可逆损害。
method: 开发了AgentTrust系统，通过Shell去混淆、SafeFix修复建议、RiskChain多步检测以及缓存感知的LLM评判器对工具调用进行实时拦截与评估。
result: "在包含930个场景的基准测试中，该系统达到了95%以上的判定准确率，并能有效识别93%的Shell混淆攻击，同时保持毫秒级延迟。"
conclusion: AgentTrust证明了运行时语义拦截是保障AI智能体工具使用安全的有效途径，其开源实现为开发者提供了实用的安全基础设施。
---

## 摘要
现代 AI 智能体通过文件操作、Shell 命令、HTTP 请求和数据库查询等工具调用来执行现实世界的副作用。单个不安全的操作，包括意外删除、凭据泄露或数据外泄，都可能造成不可逆转的损害。现有的防御措施并不完善：事后基准测试在执行后衡量行为，静态护栏会遗漏混淆和多步上下文，而基础设施沙箱仅限制代码运行位置，而不理解操作的含义。我们提出了 AgentTrust，这是一个运行时安全层，它在执行前拦截智能体工具调用，并返回结构化的判定结果：允许、警告、阻止或审查。AgentTrust 结合了 Shell 反混淆归一化器、用于提供更安全替代方案的 SafeFix 建议、用于多步攻击链检测的 RiskChain，以及用于处理模糊输入的缓存感知型 LLM-as-Judge。我们发布了一个涵盖六个风险类别的 300 场景基准测试，以及另外 630 个独立构建的现实世界对抗场景。在内部基准测试中，仅生产规则集在毫秒级端到端延迟下实现了 95.0% 的判定准确率和 73.7% 的风险等级准确率。在 630 场景基准测试中（在修复后的规则集下评估，且不声称为零样本），AgentTrust 实现了 96.7% 的判定准确率，其中对 Shell 混淆有效负载的准确率约为 93%。AgentTrust 以 AGPL-3.0 许可证发布，并为兼容 MCP 的智能体提供模型上下文协议（Model Context Protocol）服务器。

## Abstract
Modern AI agents execute real-world side effects through tool calls such as file operations, shell commands, HTTP requests, and database queries. A single unsafe action, including accidental deletion, credential exposure, or data exfiltration, can cause irreversible harm. Existing defenses are incomplete: post-hoc benchmarks measure behavior after execution, static guardrails miss obfuscation and multi-step context, and infrastructure sandboxes constrain where code runs without understanding what an action means.   We present AgentTrust, a runtime safety layer that intercepts agent tool calls before execution and returns a structured verdict: allow, warn, block, or review. AgentTrust combines a shell deobfuscation normalizer, SafeFix suggestions for safer alternatives, RiskChain detection for multi-step attack chains, and a cache-aware LLM-as-Judge for ambiguous inputs.   We release a 300-scenario benchmark across six risk categories and an additional 630 independently constructed real-world adversarial scenarios. On the internal benchmark, the production-only ruleset achieves 95.0% verdict accuracy and 73.7% risk-level accuracy at low-millisecond end-to-end latency. On the 630-scenario benchmark, evaluated under a patched ruleset and not claimed as zero-shot, AgentTrust achieves 96.7% verdict accuracy, including about 93% on shell-obfuscated payloads. AgentTrust is released under the AGPL-3.0 license and provides a Model Context Protocol server for MCP-compatible agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentTrust: Runtime Safety Evaluation and Interception for AI Agent Tool Use》的结构化技术总结：

### 1. 论文的核心问题与整体含义
随着 AI 智能体（如 Claude Code, Cursor, OpenDevin）从文本生成转向自主执行（文件操作、Shell 命令、HTTP 请求等），其带来的安全风险也从“错误文本”升级为“不可逆的现实损害”（如误删根目录、泄露 API 密钥）。
论文指出，现有防御手段存在明显缺陷：**事后基准测试**无法阻止正在发生的伤害；**静态规则护栏**易被混淆手段绕过且缺乏上下文；**基础设施沙箱**虽能限制运行环境，却无法理解操作的语义意图。
**AgentTrust** 的核心意义在于提供了一个位于智能体与工具之间的**实时、语义感知型安全拦截框架**，在操作执行前给出结构化判定。

### 2. 论文提出的方法论
AgentTrust 采用了一种多层防御架构，核心思想是结合“高效的规则引擎”与“深度的语义评判”。其关键技术组件包括：
*   **ShellNormalizer（Shell 归一化器）**：采用 9 种策略（如变量展开、十六进制/八进制转义解码、别名解析、命令替换等）对混淆的 Shell 命令进行去混淆，使隐藏的危险命令暴露给匹配引擎。
*   **ActionAnalyzer & PolicyEngine**：利用 42 种风险模式和 170 条 YAML 配置规则，对工具调用的类型、参数和有效负载进行快速扫描。
*   **RiskChain（风险链检测）**：一种顺序感知的会话追踪器，能够识别多步攻击链（例如：读取 .env -> base64 编码 -> curl 发送），即使单步操作看起来是无害的。
*   **SafeFixEngine**：不仅是拦截，还能根据 37 条修复规则建议更安全的替代方案（如将 `chmod 777` 建议修改为 `chmod 755`）。
*   **Cache-aware LLM-as-Judge**：针对模糊输入，引入 LLM 进行五维语义评估。利用类似 rsync/git 的块哈希增量检测技术，仅对增长的上下文部分进行评估，大幅降低长会话的 Token 成本。
*   **判定逻辑**：系统最终输出四种判定结果：`allow`（允许）、`warn`（警告）、`block`（阻止）、`review`（人工审查）。

### 3. 实验设计
*   **数据集/场景**：
    *   **内部基准测试（300 场景）**：涵盖文件操作、网络访问、代码执行、凭据暴露、数据外泄、系统配置六大类。
    *   **独立现实世界基准测试（630 场景）**：包含对抗性探测、现代 DevOps 工作流、云原生操作及复杂的混淆逃逸尝试。
*   **对比方法**：
    *   **B1 (Trivial Regex)**：简单的正则表达式黑名单。
    *   **B3 (NeMo Guardrails)**：NVIDIA 的对话护栏框架（以 DeepSeek-V3 为后端）。
    *   **B4 (DeepSeek-V3 Zero-shot)**：直接使用强力 LLM 作为零样本评判器。
*   **评估指标**：判定准确率 (Acc v)、风险等级准确率 (Acc r)、端到端延迟、误报率 (FPR) 和漏报率 (FNR)。

### 4. 资源与算力
*   **硬件环境**：本地规则引擎和分析器在 **Apple M4 (10核 ARM64)** 芯片上运行，单线程测试。
*   **模型支持**：LLM 评判器部分使用了 **DeepSeek-V3**（通过 API 调用），也支持 OpenAI 和 Anthropic。
*   **算力消耗**：论文未提及大规模 GPU 集群训练，因为该系统主要基于规则和推理。核心规则路径的延迟极低（中位数 < 1ms），适合在普通商用 CPU 上部署。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 930 个场景的测试，并针对 5 个历史版本进行了演进对比。
*   **消融实验**：详细评估了 ShellNormalizer、SessionTracker 和 SafeFix 的贡献。
*   **客观性与公平性**：
    *   采用了“防过拟合协议”，将基准测试规则与生产规则隔离。
    *   针对 LLM 评判器，在 100 个会话（共 5000 轮）上进行了缓存效率测试。
    *   实验对比了多种基准，并公开了所有场景和标注逻辑，具有较高的透明度和客观性。

### 6. 论文的主要结论与发现
*   **高准确率**：在内部基准上，生产规则集实现了 **95.0%** 的判定准确率；在 630 个独立场景中准确率为 **96.7%**。
*   **混淆防御有效**：ShellNormalizer 使系统能识别约 **93%** 的混淆攻击载荷。
*   **极低延迟**：规则引擎路径的端到端延迟仅为 **1.72ms**，远优于纯 LLM 方案（约 1.3s）。
*   **混合模式优势**：规则引擎 + LLM 评判器的混合模式能将漏报率 (FNR) 降低约 7-21 倍，但会略微提升误报率。
*   **缓存技术**：增量评估技术在长会话中可节省约 **87.1%** 的 Token 成本。

### 7. 优点
*   **工程落地性强**：提供了 MCP 服务器支持，任何兼容 MCP 的智能体（如 Claude Code）均可快速集成。
*   **防御深度**：不仅有静态规则，还有针对多步攻击的动态追踪和针对混淆的预处理。
*   **安全契约**：定义了 13 项回归测试，确保系统在组件失效（如 LLM API 宕机）时能“安全失败”（Fail-safe），自动转为 `review` 状态。
*   **开源贡献**：以 AGPL-3.0 协议开源了代码和包含 930 个场景的基准测试集。

### 8. 不足与局限
*   **静态分析上限**：无法处理运行时数据流（例如，无法预知 `curl -T -` 管道中实时传输的具体内容）。
*   **语言限制**：目前的正则模式和 LLM 提示词主要针对英文环境。
*   **主观性偏差**：风险等级（如 High vs Critical）的界定在某些模糊场景下仍具主观性。
*   **会话追踪测试不足**：消融实验显示 RiskChain 在当前静态基准测试中未体现出优势，需要未来构建专门的“轨迹级（Trajectory-level）”基准测试来验证。

（完）
