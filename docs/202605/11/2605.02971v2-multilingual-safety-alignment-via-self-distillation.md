---
title: Multilingual Safety Alignment via Self-Distillation
title_zh: 通过自蒸馏实现多语言安全对齐
authors: "Ruiyang Qin, Qingzhuo Wang, Dongrui Liu, Qiang Li, Zhihua Wei, Wen Shen"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.02971v2"
tags: ["query:jb-def"]
score: 9.0
evidence: 多语言安全对齐与越狱漏洞
tldr: 针对大语言模型在低资源语言中存在的安全对齐薄弱问题，本文提出了一种名为多语言自蒸馏（MSD）的跨语言安全迁移框架。该框架无需目标语言的响应数据，仅通过多语言查询即可将模型在英语等高资源语言中的安全能力迁移至低资源语言。通过引入双视角安全加权（DPSW）优化蒸馏目标，该方法在显著提升多语言安全性的同时，有效保持了模型的通用能力和对未见语言的泛化性。
selection_source: fresh_fetch
motivation: 现有的多语言安全对齐方法高度依赖难以获取的高质量低资源语言响应数据，导致模型在非英语环境下极易受到越狱攻击。
method: 提出多语言自蒸馏框架（MSD），通过在线或离线策略结合双视角安全加权（DPSW）机制，利用模型自身的高资源语言安全知识引导低资源语言对齐。
result: 实验证明该方法在多个多语言越狱基准测试中均取得了优异的安全性能，并能有效泛化至更具挑战性的数据集和未见过的语言。
conclusion: MSD 框架为解决大模型多语言安全不平衡问题提供了一种高效、灵活且无需标注响应数据的跨语言迁移方案。
---

## 摘要
大语言模型（LLMs）表现出严重的多语言安全失配：它们在高资源语言中拥有强大的安全防护，但在低资源语言中仍然极易受到越狱攻击。目前的安全对齐方法通常依赖于每种目标语言的高质量回复数据，而这些数据的生成成本高昂且困难。在本文中，我们提出了一个名为多语言自蒸馏（MSD）的跨语言安全防护迁移框架。该框架将 LLM 固有的安全能力从高资源语言（如英语）迁移到低资源语言（如爪哇语），从而克服了对任何语言回复数据的需求。我们的框架非常灵活，可以与不同的自蒸馏策略相结合。具体而言，我们实现了两种具体方法——在线（on-policy）MSD 和离线（off-policy）MSD，两者都仅使用多语言查询即可实现有效的跨语言安全迁移。此外，我们提出了双视角安全加权（DPSW），这是一种用于优化蒸馏目标的散度度量。通过共同考虑教师和学生的视角，DPSW 自适应地增加了安全关键标记（tokens）的惩罚权重，同时降低了非关键标记的权重。在代表性 LLM 上进行的涵盖多种多语言越狱和效用基准的大量实验表明，我们的方法始终能获得卓越的多语言安全性能。值得注意的是，它能有效地泛化到更具挑战性的数据集和未见过的语言，同时保留模型的通用能力。

## Abstract
Large language models (LLMs) exhibit severe multilingual safety misalignment: they possess strong safeguards in high-resource languages but remain highly vulnerable to jailbreak attacks in low-resource languages. Current safety alignment methods generally rely on high-quality response data for each target language, which is expensive and difficult to generate. In this paper, we propose a cross-lingual safeguard transfer framework named Multilingual Self-Distillation (MSD). This framework transfers an LLM's inherent safety capabilities from high-resource (e.g., English) to low-resource (e.g., Javanese) languages, overcoming the need for response data in any language. Our framework is flexible and can be integrated with different self-distillation strategies. Specifically, we implement two concrete methods -- on-policy MSD and off-policy MSD -- both of which enable effective cross-lingual safety transfer using only multilingual queries. Furthermore, we propose Dual-Perspective Safety Weighting (DPSW), a divergence measure to optimize the distillation objective. By jointly considering the perspectives of both the teacher and the student, DPSW adaptively increases the penalty weights on safety-critical tokens while reducing the weights on non-critical tokens. Extensive experiments on representative LLMs across diverse multilingual jailbreak and utility benchmarks demonstrate that our method consistently achieves superior multilingual safety performance. Notably, it generalizes effectively to more challenging datasets and unseen languages while preserving the model's general capabilities.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MSD（Multilingual Self-Distillation，多语言自蒸馏）** 的框架，旨在解决大语言模型（LLM）在低资源语言中安全防御薄弱的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型在不同语言间的安全对齐存在严重不平衡。模型在英语等高资源语言中表现出强大的安全性，但在低资源语言（如爪哇语、斯瓦希里语）中极易受到越狱攻击。
*   **研究动机**：现有的对齐方法（如 SFT 或 DPO）通常需要为每种目标语言准备高质量的“安全回复”数据，这在低资源语言中成本极高且难以获取。
*   **整体含义**：论文旨在开发一种**无需回复数据（Response-free）**的对齐框架，通过模型自身的跨语言能力，将高资源语言中的安全知识迁移到低资源语言中。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用同一个 LLM 实例化为“教师”和“学生”。教师模型通过英语上下文和思维链（CoT）触发其内在的安全推理能力，生成安全回复，以此引导仅接收低资源语言查询的学生模型。
*   **关键技术细节**：
    *   **MSD 框架**：支持**在线（On-policy）**和**离线（Off-policy）**两种模式。在线模式下，学生学习自己生成的序列；离线模式下，学生学习教师生成的序列。
    *   **教师提示词设计**：教师模型被提供目标查询的英语翻译版本和 CoT 指令，强制其先用英语进行安全分析，再用目标语言输出。
    *   **双视角安全加权（DPSW）**：这是一种改进的 KL 散度损失函数。它通过两个维度计算每个 Token 的权重：
        1.  **教师视角**：基于 Top-K 熵，教师越自信（熵越低），权重越高。
        2.  **学生视角**：学生在教师选择的 Token 上概率越低（即分歧越大），权重越高。
    *   **公式逻辑**：最终损失函数通过 DPSW 对 Token 级别的散度进行加权，使模型重点关注影响安全决策的关键 Token（如“我不能……”）。

### 3. 实验设计
*   **模型（Backbones）**：Qwen-2.5-7B-Instruct、Qwen-3-8B、LLaMA-2-7B-chat、LLaMA-3-8B-Instruct。
*   **数据集**：
    *   **训练**：仅使用 XSafety 的多语言查询（不含回复）。
    *   **测试（安全）**：MultiJail 和 PKU-SafeRLHF（包含 10 种语言，涵盖高、中、低资源水平）。
    *   **测试（通用能力）**：MMMLU（知识）和 MGSM（数学推理）。
*   **对比方法**：包括 SFT、六种主流偏好优化方法（DPO, rDPO, KTO, ORPO, R-DPO, SimPO）、表示工程（PolyRefuse）、提示词防御（Self-Defense）以及多语言对齐基准（MPO, SDRRL）。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA RTX PRO 6000 (96GB) GPU**。
*   **软件框架**：基于 Hugging Face TRL 库和 DeepSpeed ZeRO-3 优化。
*   **训练细节**：全参数微调，训练 1 个 Epoch。学习率在 5e-7 到 2e-5 之间搜索，Batch Size 为 16 或 32。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个代表性模型和 10 种语言上进行了广泛测试，其中 6 种语言在训练集中未出现（Unseen），验证了泛化性。
*   **消融实验**：非常充分。涵盖了 DPSW 的有效性、教师端额外信息（英语查询 vs CoT）的作用、教师模型是否冻结、以及不同散度度量（FKL, RKL, JSD）的对比。
*   **客观性**：使用了 GPT-4o 作为评判器，并进行了人工抽样校验（一致性 > 97%），同时评估了通用能力以确保没有发生“灾难性遗忘”。

### 6. 主要结论与发现
*   **安全提升显著**：MSD 在所有基准测试中均优于现有基准，尤其是在低资源语言上，ASR（攻击成功率）大幅下降。
*   **零成本迁移**：证明了无需任何目标语言的回复数据，仅靠查询即可实现有效的安全对齐。
*   **泛化能力强**：模型能够将安全能力迁移至训练期间未见过的语言。
*   **能力保留**：在提升安全性的同时，MMMLU 和 MGSM 的得分保持稳定，甚至在某些模型上有所提升。

### 7. 优点（亮点）
*   **数据效率极高**：彻底摆脱了对高质量安全回复数据的依赖，解决了低资源语言数据匮乏的痛点。
*   **DPSW 机制**：巧妙地利用教师和学生的置信度差异来定位安全关键 Token，比均匀权重的蒸馏更精准。
*   **灵活性**：框架兼容在线和离线蒸馏，且可以灵活更换高资源参考语言（如从英语换成中文）。

### 8. 不足与局限
*   **依赖基础模型能力**：如果基础模型本身在英语环境下的安全防御就很差，或者不具备基本的跨语言推理能力，MSD 的效果会受限。
*   **翻译依赖**：虽然不需要回复数据，但仍需要高质量的查询翻译（Query Translation）来构建教师端的输入。
*   **低资源生成瓶颈**：在某些极低资源语言（如 Qwen 在斯瓦希里语上）中，模型可能会因为生成能力不足而输出无效回复，虽然这不属于安全失败，但会影响用户体验。

（完）
