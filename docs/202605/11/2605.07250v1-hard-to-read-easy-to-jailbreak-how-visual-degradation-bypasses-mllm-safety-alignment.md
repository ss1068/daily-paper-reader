---
title: "Hard to Read, Easy to Jailbreak: How Visual Degradation Bypasses MLLM Safety Alignment"
title_zh: 难读易破：视觉退化如何绕过 MLLM 的安全对齐
authors: "Zhixue Song, Boyan Han, Yiwei Wang, Chi Zhang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07250v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 视觉退化绕过多模态大模型安全对齐
tldr: 本研究揭示了多模态大模型（MLLM）在处理视觉压缩文本时的一个重大安全漏洞：图像分辨率降低或视觉退化（如噪声、畸变）会显著削弱模型的安全对齐，导致越狱攻击更易成功。作者提出“认知过载”假说，认为模型在解码模糊输入时耗费了过多注意力，从而忽略了安全审查。为此，研究提出“结构化认知卸载”策略，通过解耦视觉转录与安全评估来增强防御，为未来安全的多模态模型设计提供了关键见解。
selection_source: fresh_fetch
motivation: 探索视觉上下文压缩技术中，图像质量下降对多模态大模型安全防御能力的影响及其潜在风险。
method: 通过实验分析不同视觉退化程度下的模型表现，并提出一种将视觉转录与安全审计分离的“结构化认知卸载”防御策略。
result: 实验发现视觉退化会导致主流模型安全防御能力大幅下降，即使文本仍可辨认，模型也会因“认知过载”而忽略安全审查。
conclusion: 视觉退化是导致模型安全失效的关键诱因，通过解耦视觉转录与安全评估可以有效缓解这一风险。
---

## 摘要
最近视觉上下文压缩技术的进展使得多模态大语言模型（MLLM）能够通过将文本渲染为图像来高效处理超长上下文。然而，我们发现了该范式中固有的一个关键漏洞：降低图像分辨率会无意中催化越狱行为。我们的实验表明，随着分辨率的降低，最先进（SOTA）模型的安全防御能力急剧下降，令人惊讶的是，即使在文本仍然清晰可辨的情况下，这种现象依然存在。我们将此归因于“认知过载”，并假设破译退化输入所需的努力分散了用于安全审计的注意力资源。这一现象在包括噪声和几何畸变在内的各种视觉扰动中均表现一致。为了解决这一问题，我们提出了一种简单的“结构化认知卸载”策略，通过强制执行序列化流水线来解耦视觉转录与安全评估，从而降低这些风险。我们的工作揭示了基于视觉的压缩技术中的重大风险，并为未来 MLLM 的安全设计提供了关键见解。

## Abstract
Recent advancements in visual context compression enable MLLMs to process ultra-long contexts efficiently by rendering text into images. However, we identify a critical vulnerability inherent to this paradigm: lowering image resolution inadvertently catalyzes jailbreaking. Our experiments reveal that the safety defenses of SOTA models deteriorate sharply as resolution degrades, surprisingly persisting even when text remains legible. We attribute this to ``Cognitive Overload'', hypothesizing that the effort required to decipher degraded inputs diverts attentional resources from safety auditing. This phenomenon is consistent across various visual perturbations, including noise and geometric distortion. To address this, we propose a simple ``Structured Cognitive Offloading'' strategy that mitigates these risks by enforcing a serialized pipeline to decouple visual transcription from safety assessment. Our work exposes a significant risk in vision-based compression and provides critical insights for the secure design of future MLLMs.

---

## 论文详细总结（自动生成）

### 论文总结：难读易破——视觉退化如何绕过 MLLM 的安全对齐

#### 1. 核心问题与整体含义（研究动机和背景）
随着多模态大语言模型（MLLM）处理超长上下文的需求增加，视觉上下文压缩技术（如将长文本渲染为高密度图像）成为一种高效方案。然而，本文揭示了该范式中一个被忽视的安全隐患：**视觉退化（如降低分辨率、模糊、噪声等）会显著削弱模型的安全防御能力**。研究发现，当图像处于“难读但仍可辨认”的状态时，模型往往能识别出有害指令的内容，却无法触发安全拒绝机制，从而导致越狱攻击成功率（ASR）大幅上升。

#### 2. 方法论：核心思想与技术细节
论文提出了**“认知过载”（Cognitive Overload）**假说，认为模型在处理模糊视觉输入时，会将绝大部分计算资源/注意力分配给字符识别（OCR），导致用于安全审计的资源不足。

*   **核心思想**：通过“解耦”识别与审计过程来修复防御。
*   **关键技术：结构化认知卸载（Structured Cognitive Offloading）**：
    这是一种基于 Prompt 的防御策略，强制模型执行序列化的推理流水线，将隐式的并发处理转为显式的分步执行：
    1.  **转录阶段（Transcription）**：要求模型先将图像中的文字完整、准确地转录为文本。
    2.  **安全评估阶段（Safety Evaluation）**：基于转录后的纯文本进行独立的安全合规性分析。
    3.  **响应生成阶段（Response）**：仅在确认安全的前提下生成最终回答。
*   **数学表达**：将原始的联合概率分布 $P(R | I, P_{dir})$ 分解为转录 $\hat{T}$、安全判断 $\hat{S}$ 和响应 $R$ 的链式概率：$P(R, \hat{S}, \hat{T} | I) = P(R | \hat{S}, \hat{T}) \cdot P(\hat{S} | \hat{T}) \cdot P(\hat{T} | I)$。

#### 3. 实验设计
*   **数据集与 Benchmark**：
    *   整合了 AdvBench、HarmBench 等标准安全基准，去重后获得 770 个唯一有害意图。
    *   使用“角色扮演”模板增强攻击性。
    *   构建了包含 9 种不同分辨率（DPI 15 到 300）的图像数据集。
*   **对比场景与方法**：
    *   **模型覆盖**：包括 GPT-4.1、Claude-Sonnet-4.5、Gemini-2.5、Qwen3-VL、Doubao-Seed-1.6 等 10 余种 SOTA 模型。
    *   **扰动类型**：低分辨率（DPI）、高斯模糊、噪声、几何畸变、遮挡、马赛克等。
    *   **评估指标**：ASR（攻击成功率）、OCR 准确率（字符级与单词级）。
    *   **基准对比**：纯文本攻击 vs. 高分辨率图像攻击 vs. 退化图像攻击。

#### 4. 资源与算力
论文**未明确说明**具体的训练算力（如 GPU 型号或数量）。由于该研究主要侧重于对现有预训练模型的推理分析、Prompt 工程防御以及线性探测（Linear Probing），其核心实验并不涉及大规模的模型重训练，因此对算力的需求主要集中在数千次模型 API 调用和本地小规模模型的推理探测上。

#### 5. 实验数量与充分性
*   **实验规模**：总计评估了 6,930 个对抗实例（770 个查询 × 9 种分辨率），并针对每种视觉扰动做了多级强度测试。
*   **消融与深入分析**：
    *   **层级探测（Probing）**：通过训练逻辑回归探测器，分析了模型不同层对安全特征的提取能力，证明了退化图像会导致安全特征延迟出现。
    *   **控制变量**：通过填充（Padding）实验排除了“Token 数量减少导致性能下降”的干扰因素。
    *   **跨语言验证**：在中文有害指令上验证了现象的普遍性。
*   **客观性**：采用了“三模型集成+人工专家”的混合评估协议，确保 ASR 统计的准确性，实验设计严谨且多维度。

#### 6. 主要结论与发现
*   **攻击舒适区（ACZ）**：存在一个特定的分辨率区间（如 45-150 DPI），此时 OCR 准确率较高（>80%），但 ASR 达到峰值。
*   **倒 U 型曲线**：随着视觉质量下降，ASR 呈现先升后降的趋势（极低分辨率下模型因“看不见”而安全）。
*   **安全特征延迟**：视觉退化使得有害语义的识别被推迟到模型的深层网络，从而绕过了浅层的安全护栏。
*   **防御有效性**：提出的“结构化认知卸载”能将 ASR 从 60% 以上降低至 5% 左右，且不增加对正常请求的误拒率。

#### 7. 优点
*   **视角新颖**：首次揭示了视觉压缩/退化与安全对齐之间的内在矛盾。
*   **理论深刻**：提出的“认知过载”假说得到了层级探测实验的有力支持，解释了感知与对齐解耦的底层机制。
*   **防御实用**：防御方法不需要重新训练模型，仅靠 Prompt 即可实现，具有极强的工程落地价值。

#### 8. 不足与局限
*   **推理开销**：分步转录和审计导致输出 Token 长度平均增加 102%，增加了推理延迟和成本。
*   **覆盖范围**：主要关注文本类视觉攻击，对于更抽象的图形化语义攻击（非文字类）的泛化效果尚待验证。
*   **实时性限制**：在对响应速度要求极高的实时交互场景中，该防御策略可能因延迟过高而受限。

（完）
