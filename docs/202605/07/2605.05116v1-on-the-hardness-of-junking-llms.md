---
title: On the Hardness of Junking LLMs
title_zh: 论 LLM 垃圾化（Junking）的难度
authors: "Marco Rando, Samuel Vaiter"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05116v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 大模型对越狱攻击和自然后门的脆弱性
tldr: 本研究探讨了大型语言模型（LLM）中的“垃圾化（junking）”问题，即在不依赖语义指令的情况下，仅通过优化无意义的标记序列触发有害输出。研究者将此定义为寻找自然后门的过程，并提出一种贪婪随机搜索方法来评估其难度。结果表明，虽然此类攻击比传统越狱更具挑战性，但自然后门确实存在且易于被发现。困惑度分析进一步揭示这些序列源于训练过程中的隐式涌现，为理解模型安全漏洞提供了新视角。
selection_source: fresh_fetch
motivation: 旨在研究在缺乏语义结构的情况下，LLM是否存在能触发有害行为的自然后门序列及其发现难度。
method: 提出一种贪婪随机搜索算法，通过最大化目标有害响应前缀的生成概率来搜索无意义的标记序列。
result: 实验显示该任务虽比标准越狱攻击更难，但通过简单策略仍能实现高成功率，证明了自然后门的普遍性。
conclusion: 研究确认了LLM中隐式存在的自然后门极易被恢复，且这些序列多处于模型分布的低概率区域。
---

## 摘要
大语言模型（LLMs）已知易受越狱攻击（jailbreak attacks）的影响，这些攻击通常依赖于包含显式语义结构的精心设计的提示词（prompts）。这些攻击通常通过固定一个对抗性指令并优化小的对抗性组件（如后缀或前缀）来运作。在这种设定下，提示词结构对性能至关重要，最近的研究结果表明，当结合复杂的提示词设计时，即使是简单的随机搜索也能取得强大的性能。最近观察到，即使没有对抗性提示词，仅依靠优化的 Token 序列也能诱发有害行为。这表明存在“自然后门”（natural backdoors），即在 LLMs 训练过程中自然产生的、无需任何有意义指令即可触发不安全输出的 Token 序列。然而，尽管有这些观察，这一设定在很大程度上仍未被探索，特别是寻找自然后门的难度尚未得到评估。在这项工作中，我们提供了一项初步的概念验证研究，调查了这一任务的难度，我们将其称为“垃圾化问题”（junking problem）。我们将该问题形式化为寻找使生成有害响应的目标前缀概率最大化的 Token 序列，并提出了一种贪婪随机搜索方法来评估此类序列是否容易被发现。我们的结果表明，这个问题比标准的越狱攻击更难，证实了提示词设计中语义信息的重要性。同时，我们发现我们的简单策略足以以高成功率解决该问题，这表明自然后门确实存在且易于恢复。最后，通过困惑度（perplexity）分析，我们观察到发现的 Token 序列位于模型分布的低概率区域，支持了它们是从训练过程中隐式产生的假设。

## Abstract
Large language models (LLMs) are known to be vulnerable to jailbreak attacks, which typically rely on carefully designed prompts containing explicit semantic structure. These attacks generally operate by fixing an adversarial instruction and optimizing small adversarial components (e.g., suffixes or prefixes). In this setting, prompt structure is fundamental for performance, and recent results show that even simple random search can achieve strong performance when combined with sophisticated prompt design. Recently, it has been observed that harmful behaviors can be elicited even without the adversarial prompt, relying solely on optimized token sequences. This suggests the existence of natural backdoors, i.e., token sequences naturally emerged during LLMs training that trigger unsafe outputs without any meaningful instruction. However, despite these observations, this setting remains largely unexplored, and in particular the hardness of finding natural backdoors has not been assessed yet. In this work, we provide a first proof-of-concept study investigating the hardness of this task, which we refer to as the junking problem. We formalize it as the problem of finding token sequences that maximize the probability of generating a target prefix of harmful responses, propose a greedy random-search method to assess is such sequences can be discovered easily. Our results show that this problem is harder than standard jailbreak attacks, confirming the importance of semantic information in prompt design. At the same time, we find that our simple strategy is sufficient to solve it with a high success rate, suggesting that natural backdoors are present and easily recoverable. Finally, through perplexity analysis, we observe that the discovered token sequences lie in low-probability regions of the model distribution, supporting the hypothesis that they emerged implicitly from the training process.

---

## 论文详细总结（自动生成）

这篇论文《On the Hardness of Junking LLMs》探讨了大型语言模型（LLM）中一种极端的安全漏洞——“垃圾化（Junking）”问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的 LLM 越狱攻击（Jailbreak）大多依赖于具有语义结构的提示词（如精心设计的指令或模板）。最近有研究发现，即使没有语义指令，仅靠优化的 Token 序列也能诱发有害行为。
*   **核心问题**：论文将这种现象定义为**“垃圾化问题（Junking Problem）”**，即寻找那些在语义上毫无意义、但在 LLM 训练过程中隐式产生的**“自然后门（Natural Backdoors）”**。
*   **研究意义**：旨在评估在完全剥离语义信息（无指令、无模板、无语义初始化）的情况下，发现这些能触发有害输出的 Token 序列的难度，从而诊断模型在预训练和对齐阶段留下的隐性安全隐患。

### 2. 论文提出的方法论
*   **核心思想**：将垃圾化问题形式化为一个离散空间下的优化问题，目标是找到一个 Token 序列 $x$，使得模型生成特定有害响应前缀 $y$（如 "Sure, here is how to..."）的概率最大化。
*   **算法流程（贪婪随机搜索 - GRS）**：
    1.  **初始化**：从一个中性且无意义的序列开始（如全为 "!" 字符的 Token）。
    2.  **迭代搜索**：在每一步迭代中，选择序列中的一个位置（按顺序循环选择）。
    3.  **批量采样**：针对该位置，从词表中随机采样 $B$ 个候选 Token。
    4.  **贪婪更新**：计算这 $B$ 个候选序列的目标函数值（负对数似然），如果其中最好的候选者优于当前序列，则接受该更新。
*   **目标函数**：最小化目标前缀 $y$ 在给定输入 $x$ 下的负对数似然值。

### 3. 实验设计
*   **实验对象**：四种主流的对齐 LLM：LLaMA-2-Chat-7B、Gemma-7B、Qwen-2.5-7B 和 Mistral-7B。
*   **数据集**：从 AdvBench 中选取了 50 个有害行为目标。
*   **Benchmark 与对比**：
    *   **初始化对比**：使用语义无关的 "!" 序列作为起点。
    *   **评估指标**：函数值下降进度、编辑距离（生成的响应与目标的匹配度）、攻击成功率（ASR，由 LLM-as-a-judge 评分）、以及 Token 序列的困惑度（Perplexity）。
    *   **判定模型**：使用 Gemma-4-31B 作为裁判模型，评估响应的有害性和连贯性。

### 4. 资源与算力
*   **硬件环境**：实验在 Grid’5000 测试平台上进行。
*   **具体配置**：使用了 **2 张 NVIDIA A40 GPU**，配备 256 GB RAM。
*   **计算规模**：每个攻击任务的总预算为 $10^5$ 次函数评估（即模型前向推理次数）。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个模型，每个模型测试 50 个有害目标，总计进行了数百次完整的优化轨迹实验。
*   **消融实验**：对序列长度 $n$（测试了 10 到 200）和批量大小 $B$（测试了 5 到 100）进行了参数调优实验。
*   **充分性评价**：实验设计较为充分，涵盖了不同架构和对齐风格的模型。通过困惑度分析对比了自然文本与对抗序列，增强了结论的客观性。

### 6. 论文的主要结论与发现
*   **可行性**：即使使用极其简单的贪婪随机搜索，也能在大多数模型上以较高成功率（ASR 52% - 90%）发现自然后门。
*   **难度对比**：垃圾化问题比标准的后缀越狱攻击更难，需要更长的 Token 序列（通常需 100 个 Token 以上）和更多的查询次数，这证明了语义结构在简化搜索空间中的关键作用。
*   **模型差异**：Mistral-7B 最易受攻击（ASR 90%），而 Gemma-7B 表现出最强的鲁棒性（ASR 52%）。
*   **统计特性**：发现的“垃圾”序列在模型分布中处于极低概率区域（高困惑度），表明这些序列是训练过程中偶然形成的异常点，而非正常的语言模式。

### 7. 优点
*   **概念新颖**：首次系统性地定义并量化了“垃圾化”这一极端攻击场景的难度。
*   **方法简洁有力**：通过最简单的随机搜索算法证明了漏洞的普遍存在，这种“基准线”研究对安全评估极具价值。
*   **深入的统计分析**：引入困惑度分析，从概率分布的角度解释了自然后门的本质。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于 7B 参数的模型，尚未验证在更大规模（如 70B+）或闭源模型（如 GPT-4）上的表现。
*   **白盒依赖**：该方法需要获取模型的 Token 概率（Logits），在纯黑盒（仅 API 输出文本）场景下的适用性受限。
*   **防御未充分探讨**：虽然提到困惑度过滤可能有效，但未深入测试现有的防御机制（如 SmoothLLM）对垃圾化攻击的实际拦截效果。
*   **现实威胁程度**：由于生成的序列困惑度极高，极易被简单的统计防御手段检测，因此其作为现实攻击手段的隐蔽性较差。

（完）
