---
title: "From Parameter Dynamics to Risk Scoring : Quantifying Sample-Level Safety Degradation in LLM Fine-tuning"
title_zh: 从参数动力学到风险评分：量化大语言模型微调中的样本级安全性退化
authors: "Xiao Wang, Yifei Zhang, YongKang Liu, Xiaocui Yang, Zihan Wang, Shi Feng, Daling Wang"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04572v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 量化LLM微调过程中的安全性退化
tldr: 本研究探讨了大语言模型在微调过程中安全性易受损的问题。通过分析参数动态，发现良性微调会导致参数向危险方向累积漂移，从而破坏安全性。为此，作者提出了SQSD方法，通过测量样本诱导的参数更新在危险与安全方向上的投影差异，实现样本级的安全性退化风险量化。实验表明，该方法能有效识别高风险样本，且在不同模型和微调方法下具有极强的迁移性。
selection_source: fresh_fetch
motivation: 现有研究多关注微调前后的静态对比，忽视了微调过程中参数动态演化对安全性退化的影响。
method: 提出SQSD方法，通过衡量每个训练样本引起的参数更新在“危险”与“安全”方向上的投影差异，为样本计算连续风险评分。
result: 实验证明SQSD能有效量化样本级的微调风险，并在不同模型架构、参数规模和高效微调方法间表现出强迁移性。
conclusion: 揭示了良性微调导致安全性退化的关键机制是参数向危险方向的累积漂移，并提供了量化该风险的有效工具。
---

## 摘要
大语言模型（LLMs）的安全性对齐极其脆弱，因为在少量良性样本上进行微调就可能抹除从数百万个偏好示例中学习到的安全行为。现有研究试图通过比较微调前后的参数和隐藏状态来解释这一现象，但忽略了它们在微调过程中的动态演化。在本文中，我们通过分析参数动力学揭示了安全性退化背后的一个关键机制，即良性微调会导致参数累积地向危险对齐的方向漂移，从而逐渐破坏模型的安全性。这一发现表明，对这种漂移贡献更大的样本具有更高的微调风险。基于这一见解，我们提出了一种样本级安全性退化量化（SQSD）方法，用于量化每个训练样本对安全性退化的影响。具体而言，SQSD 通过测量样本诱导的参数更新在危险方向和安全方向之间的投影差异，为样本计算连续的风险评分。在多个模型和数据集上的广泛实验表明，SQSD 有效地量化了样本级微调风险，并在不同模型架构、参数规模和参数高效方法之间表现出强大的可迁移性。

## Abstract
Safety alignment of Large Language Models (LLMs) is extremely fragile, as fine-tuning on a small number of benign samples can erase safety behaviors learned from millions of preference examples. Existing studies attempt to explain this phenomenon by comparing parameters and hidden states before and after fine-tuning, but overlook their dynamic evolution during fine-tuning. In this paper, we uncover a critical mechanism underlying safety degradation by analyzing parameter dynamics, where benign fine-tuning causes parameters to cumulatively drift toward danger-aligned directions, progressively undermining the model's safety. This finding suggests that samples contributing more to this drift has greater fine-tuning risks. Based on this insight, we propose a method of Sample-Level Quantification of Safety Degradation (SQSD), which quantifies the influence of each training sample on safety degradation. Specifically, SQSD computes continuous risk scores to samples by measuring their induced parameter updates' projection difference between danger and safety directions. Extensive experiments across multiple models and datasets demonstrate that SQSD effectively quantifies sample-level fine-tuning risks and exhibits strong transferability across model architectures, parameter scales, and parameter-efficient methods.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）在微调过程中安全性退化的内在机制，并提出了一种量化样本风险的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：为什么在看似无害（良性）的数据上进行微调，也会导致经过严格安全对齐的 LLM 出现安全性崩溃？
*   **研究背景**：现有的安全对齐（如 RLHF、DPO）非常脆弱。研究发现，仅使用 100 个良性样本进行微调就足以抹除模型之前的安全行为。
*   **研究动机**：以往研究多关注微调前后的静态对比，忽视了微调过程中参数的动态演化。作者希望通过分析“参数动力学”来揭示安全性退化的过程，并识别出哪些良性样本是导致风险的“罪魁祸首”。

### 2. 论文提出的方法论：SQSD
*   **核心思想**：提出**样本级安全性退化量化（SQSD）**方法。该方法认为，如果一个样本诱导的参数更新方向更接近“危险方向”而非“安全方向”，那么该样本的微调风险就更高。
*   **关键技术细节**：
    1.  **定义方向向量**：通过在有害数据集上微调得到“危险方向” ($V_{danger}$)，在安全偏好数据集上通过 DPO 得到“安全方向” ($V_{safety}$)。
    2.  **计算样本诱导更新**：利用 LoRA 框架下的单步梯度来近似样本对模型参数的影响 ($\Delta W(z)$)。
    3.  **投影差异计算**：计算样本更新在危险方向上的投影与在安全方向上投影的差值。
    4.  **模块化归一化**：为了消除响应长度偏差（长文本通常梯度更大），对每个模块的参数更新进行归一化处理。
*   **理论支撑**：利用一阶泰勒展开证明了参数空间的投影差异与模型输出偏好的变化（损失函数差异）之间存在直接联系。

### 3. 实验设计
*   **模型**：Qwen3-8B/14B/32B、Llama-3.1-8B-Instruct、Llama-2-7B-Chat。
*   **微调数据集**：Dolly (5k 样本) 和 Alpaca (5k 样本)。
*   **安全评估基准**：CategoricalHarmfulQA、AdvBench 和 HEx-PHI。
*   **评估指标**：攻击成功率（ASR）和安全评分（Safety Score，基于奖励模型）。
*   **对比方法**：Reward Model（奖励模型评分）、Bi-Anchor（表征/梯度锚点法）、Self-Inf-N（自影响评分）、LARF（层感知表征过滤）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练配置**：提到了使用 LLaMA-Factory 框架，采用 AdamW 优化器，Batch Size 为 8，学习率在 $5 \times 10^{-6}$ 到 $5 \times 10^{-5}$ 之间，微调轮数为 10 轮。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流模型和 2 个数据集上进行了 12 组核心有效性实验。
    *   进行了跨架构（Llama $\leftrightarrow$ Qwen）、跨规模（8B $\to$ 32B）、跨微调方法（LoRA $\to$ 全参数）的迁移性实验。
    *   包含了针对归一化、投影设计、初始化敏感性的消融实验。
*   **充分性评价**：实验设计非常全面，不仅验证了方法在识别高风险样本上的有效性，还通过动态轨迹图揭示了参数演化的非线性特征（两阶段退化），论证过程客观且具有说服力。

### 6. 主要结论与发现
*   **参数漂移机制**：良性微调会导致参数向危险方向产生累积漂移，而向安全方向的漂移几乎为零。
*   **两阶段退化**：安全性退化并非线性的。初期参数虽有漂移但安全得分下降缓慢；当漂移累积到一定程度（走出“安全盆地”）后，安全性会发生断崖式下跌。
*   **SQSD 的优越性**：SQSD 在识别风险样本的单调性（即评分越高，微调后 ASR 越高）上显著优于现有基线方法。
*   **强迁移性**：在小模型上计算的风险评分可以直接用于过滤大模型的微调数据。

### 7. 优点与亮点
*   **动态视角**：首次从参数动力学轨迹的角度解释安全性退化，比静态对比更具深度。
*   **连续风险评分**：不同于以往只能识别离散有害子集的方法，SQSD 提供了全语料库的连续风险度量。
*   **解决长度偏差**：通过模块化归一化，成功解决了梯度方法中常见的“短文本偏见”问题。
*   **高迁移性**：实验证明该方法具有极强的实用价值，可跨模型、跨规模使用。

### 8. 不足与局限
*   **初始化依赖**：SQSD 的效果依赖于初始化模型对安全方向的敏感性，若模型处于不敏感的参数区域，量化可能失效。
*   **计算开销**：虽然使用了 LoRA 简化计算，但为全量语料库计算样本级梯度仍需一定的计算资源。
*   **双刃剑风险**：论文也提到，这种识别高风险样本的技术如果被恶意利用，可能会被用来刻意破坏模型的安全性（尽管作者强调其初衷是防御性的）。

（完）
