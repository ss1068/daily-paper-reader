---
title: "TwinGate: Stateful Defense against Decompositional Jailbreaks in Untraceable Traffic via Asymmetric Contrastive Learning"
authors: "Bowen Sun, Chaozhuo Li, Yaodong Yang, Yiwei Wang, Chaowei Xiao"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27861v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对分解式越狱攻击的有状态防御
tldr: TwinGate 针对大语言模型面临的分解式越狱攻击，提出了一种基于非对称对比学习的有状态防御框架。在匿名且交织的流量环境下，它通过双编码器结构在共享潜空间中聚类恶意片段，同时抑制良性重叠导致的误报。该方法仅需轻量级前向传播，在保持高召回率和低误报率的同时，显著降低了延迟并提升了吞吐量。
selection_source: fresh_fetch
motivation: 现有的防御策略在处理匿名且交织的分解式越狱攻击时，存在无法追踪全局上下文或计算开销过大的局限性。
method: 提出 TwinGate 框架，利用非对称对比学习聚类意图匹配的恶意片段，并结合冻结编码器减少误报。
result: 在包含 362 万条指令的大规模数据集上，TwinGate 实现了高恶意意图召回率和极低的误报率，且性能优于现有基准。
conclusion: TwinGate 为实时、匿名流量下的 LLM 安全防御提供了一种高效且鲁棒的有状态解决方案。
---

## Abstract
Decompositional jailbreaks pose a critical threat to large language models (LLMs) by allowing adversaries to fragment a malicious objective into a sequence of individually benign queries that collectively reconstruct prohibited content. In real-world deployments, LLMs face a continuous, untraceable stream of fully anonymized and arbitrarily interleaved requests, infiltrated by covertly distributed adversarial queries. Under this rigorous threat model, state-of-the-art defensive strategies exhibit fundamental limitations. In the absence of trustworthy user metadata, they are incapable of tracking global historical contexts, while their deployment of generative models for real-time monitoring introduces computationally prohibitive overhead. To address this, we present TwinGate, a stateful dual-encoder defense framework. TwinGate employs Asymmetric Contrastive Learning (ACL) to cluster semantically disparate but intent-matched malicious fragments in a shared latent space, while a parallel frozen encoder suppresses false positives arising from benign topical overlap. Each request requires only a single lightweight forward pass, enabling the defense to execute in parallel with the target model's prefill phase at negligible latency overhead. To evaluate our approach and advance future research, we construct a comprehensive dataset of over 3.62 million instructions spanning 8,600 distinct malicious intents. Evaluated on this large-scale corpus under a strictly causal protocol, TwinGate achieves high malicious intent recall at a remarkably low false positive rate while remaining highly robust against adaptive attacks. Furthermore, our proposal substantially outperforms stateful and stateless baselines, delivering superior throughput and reduced latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TwinGate** 的有状态防御框架，旨在应对大语言模型（LLM）面临的“分解式越狱”威胁。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的越狱攻击通常在单个 Prompt 中完成，而**分解式越狱（Decompositional Jailbreaks）**将恶意目标拆分为多个看似无害的子查询（例如：分别询问电路过载原理、旧电线的安全特性等，最终组合成制造火灾的指令）。
*   **核心挑战**：
    1.  **流量不可追踪性**：在真实生产环境中，攻击者可以跨账号、跨 IP 分散发送片段，导致防御者无法通过元数据（如 User ID）关联上下文。
    2.  **语义鸿沟**：攻击片段在语义上往往互不相关，传统的相似度检索难以识别其内在联系。
    3.  **高昂开销**：现有的有状态防御多依赖 LLM 进行实时监控，计算延迟和成本极高，难以适配高并发流量。

### 2. 方法论
TwinGate 采用**双编码器（Dual-Encoder）**结构和**非对称对比学习（ACL）**技术，实现了一种无需元数据的有状态防御。
*   **核心思想**：将防御分为两个阶段，通过在共享潜空间中聚类恶意意图来识别分布式攻击。
*   **关键技术细节**：
    *   **非对称对比学习 (ACL)**：微调一个编码器，使其将属于同一恶意意图的不同片段拉近。**“非对称”**体现在：仅以恶意样本为锚点进行聚类，而将良性样本仅作为负样本推开，从而保护良性查询的语义结构不被破坏。
    *   **双阶段决策逻辑**：
        *   **阶段 1：语义等价继承**。使用一个冻结的编码器检查当前请求是否与历史请求语义高度重复。若是，直接继承之前的安全判定，有效抑制因良性重复请求导致的误报。
        *   **阶段 2：意图攻击检测**。若请求是新颖的，则通过 ACL 编码器在向量数据库中检索。如果当前请求与历史请求在“意图空间”高度接近，则判定为分解式攻击的一部分并予以拦截。
    *   **硬件加速流水线**：利用 GPU 的 HBM（高带宽显存）存储向量，并通过 NVLink 实现零拷贝传输，确保检索过程与 LLM 的 Prefill 阶段并行。

### 3. 实验设计
*   **数据集**：构建了一个包含 **362 万条指令**的大规模数据集，涵盖 8,600 种恶意意图（通过 CKA-Agent 分解）和 25 万种良性意图，来源包括 Alpaca、Dolly、LMSYS-Chat-1M 等。
*   **Benchmark**：模拟了完全匿名、随机交织的全球请求流，要求防御系统在没有任何元数据的情况下做出因果决策。
*   **对比方法**：
    *   **Llama-Guard-3-8B**：主流的无状态安全护栏模型。
    *   **Intent-FT**：专门针对意图识别微调的过滤模型。
    *   **Window Monitor**：基于固定窗口的有状态监控器。

### 4. 资源与算力
*   **训练阶段**：使用了 **8 台 NVIDIA A100 (40GB) GPU**，训练时长为 3 个 Epoch。
*   **部署阶段**：使用了 **4 台 NVIDIA A100 (40GB) GPU**，通过 NVLink 互联实现流水线并行。
*   **存储**：向量数据库完全驻留在 GPU 显存（HBM）中，单卡可支持约 600 万条历史记录。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括：
    *   **安全性测试**：绘制了 Recall-FPR 曲线，验证了在极低误报率下的高召回率。
    *   **性能测试**：对比了不同并发压力下的 QPS 和 P99 延迟。
    *   **鲁棒性测试**：针对白盒 GCG 攻击、重写攻击、语义 DoS 攻击等自适应攻击进行了压力测试。
    *   **消融实验**：验证了双编码器、ACL 损失函数、原始意图锚点等组件的必要性。
*   **充分性评价**：实验设计非常充分且具有高度的工程实践意义，特别是对“不可追踪流量”和“自适应攻击”的模拟，体现了极强的客观性和公平性。

### 6. 主要结论与发现
*   **高性能**：TwinGate 在误报率（FPR）低于 **0.2%** 时，恶意意图召回率超过 **0.76**，远超所有基准模型。
*   **高吞吐**：系统吞吐量超过 **1700 QPS**，延迟增加微乎其微（<300ms），能够与 LLM 推理并行。
*   **强鲁棒性**：即使攻击者拥有防御方的模型权重并使用 GCG 算法优化攻击载荷，TwinGate 仍能将攻击成功率（ASR）限制在 **0.18** 左右。
*   **可扩展性**：通过聚类 LRU 策略，系统可以在仅保留 25% 历史数据的情况下维持大部分防御性能。

### 7. 优点
*   **架构创新**：首次在无元数据环境下通过 ACL 解决了分解式越狱的关联问题。
*   **工程落地性强**：采用轻量级编码器而非 LLM 作为监控器，极大地提升了工业级部署的可行性。
*   **防御全面**：不仅能防攻击，还通过“决策继承”机制解决了有状态防御极易误伤良性用户的痛点。

### 8. 不足与局限
*   **硬件依赖**：高性能表现高度依赖 GPU 显存和 NVLink 等高速互联硬件，对于显存较小的设备支持有限。
*   **冷启动漏洞**：作为有状态防御，对于一个全新的恶意意图，其第一个片段必然会逃逸（因为数据库中尚无匹配项）。
*   **隐私与合规**：虽然是匿名存储，但在向量数据库中长期保留用户查询的特征向量，在某些严苛的隐私法规下可能面临合规挑战。

（完）
