---
title: "AgentShield: Deception-based Compromise Detection for Tool-using LLM Agents"
title_zh: AgentShield：针对工具使用型大语言模型智能体的基于欺骗的入侵检测
authors: "Yassin H. Rassul, Tarik A. Rashid"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.11026v1"
tags: ["query:llm"]
score: 8.0
evidence: 检测大语言模型智能体中的间接提示注入
tldr: "AgentShield 是一种针对工具化 LLM 智能体的欺骗性入侵检测框架，旨在解决间接提示注入（IPI）防御中侧重预防而非检测以及多语言支持不足的问题。该框架通过在工具接口中设置虚假工具、凭据和白名单参数等陷阱，实时捕捉攻击行为，并利用陷阱触发信号训练自监督分类器。实验表明，它在多语言环境下对商业模型的攻击捕获率达 90.7%-100%，且误报率为零。"
selection_source: fresh_fetch
motivation: 现有的间接提示注入防御主要关注预防而非检测，且在低资源语言环境下缺乏有效的安全保护评估。
method: 通过在智能体工具接口中部署虚假工具、虚假凭据和白名单参数三层陷阱，并利用陷阱触发信号训练自监督分类器。
result: "在多语言攻击测试中，AgentShield 对商业模型成功攻击的捕获率达到 90.7%-100%，且在正常测试中实现了零误报。"
conclusion: AgentShield 证明了基于欺骗的检测机制能有效识别复杂的注入攻击，并具备极强的跨模型和跨语言迁移能力。
---

## 摘要
针对工具使用型大语言模型（LLM）智能体中间接提示注入（IPI）的防御措施存在两个结构性弱点。首先，它们都试图防止攻击，而不是检测那些漏网的入侵。其次，它们仅在英语环境下进行了评估，导致库尔德语和阿拉伯语等低资源语言的用户缺乏经过测试的保护。本文通过 AgentShield 填补了这两个空白，这是一种基于欺骗的检测框架，在智能体的工具接口内设置了三层陷阱：虚假工具、虚假凭据和白名单参数。同样的陷阱触发器可作为自监督分类器的高精度标签。遵循攻击者隐藏指令的 LLM 智能体几乎总会触碰这些陷阱之一，这既提供了实时的入侵信号，又为训练下游检测器提供了零误报（zero-FP）标签，且无需人工标注。在涵盖 176 个跨语言攻击提示和来自三个供应商的四个 LLM 的测试中，由于现代 LLM 本身已经拒绝了大多数 IPI 尝试（攻击成功率 <= 10%），AgentShield 的任务是捕获那些确实漏网的攻击。在商业模型上，它捕获了 90.7%-100% 的此类成功攻击，且在 485 次正常使用测试中实现了零误报。它通过了系统的自适应攻击评估，在商业模型上实现了零逃逸，且自监督分类器无需重新训练即可跨模型和语言进行迁移。

## Abstract
Defenses against indirect prompt injection (IPI) in tool-using LLM agents share two structural weaknesses. First, they all attempt to prevent attacks rather than detect the compromises that slip through. Second, they have only been evaluated in English, leaving users of low-resource languages such as Kurdish and Arabic without tested protection. This paper addresses both gaps with AgentShield, a deception-based detection framework that places three layers of traps inside the agent's tool interface: fake tools, fake credentials, and allowlisted parameters. The same trap triggers serve as high-precision labels for a self-supervised classifier. An LLM agent that follows an attacker's hidden instruction almost always touches one of these traps, which gives both a real-time compromise signal and a zero-FP label for training a downstream detector without manual annotation. Across 176 cross-lingual attack prompts and four LLMs from three providers, and because modern LLMs already refuse most IPI attempts on their own (attack success rate <= 10%), AgentShield's job is to catch the attacks that do slip through. On commercial models, it catches 90.7%-100% of such successful attacks, with zero false alarms on 485 normal-use tests. It survives a systematic adaptive-attack evaluation with zero evasion on commercial models, and the self-supervised classifier transfers across models and languages without retraining.

---

## 论文详细总结（自动生成）

### AgentShield：针对工具使用型 LLM 智能体的基于欺骗的入侵检测

#### 1. 核心问题与整体含义
随着大语言模型（LLM）作为智能体（Agents）调用外部工具（如发邮件、转账）的普及，**间接提示注入（IPI）**成为了严重的安全性威胁。攻击者通过在智能体读取的数据中隐藏指令来劫持其行为。
论文指出当前防御手段存在三大缺陷：
*   **重预防轻检测**：现有方法试图阻止攻击，但一旦预防失效，系统无法感知智能体已被控制。
*   **语言偏差**：现有评估几乎全部基于英语，忽略了库尔德语、阿拉伯语等低资源语言。
*   **结构性脆弱**：自适应攻击者往往能绕过单一的预防层。
**AgentShield** 的核心意义在于引入“欺骗”机制（类似网络安全中的蜜罐），通过在工具接口中布设陷阱，实现对已发生入侵的实时检测。

#### 2. 方法论
AgentShield 在智能体的工具接口中部署了三层“诱饵”：
*   **第一层：虚假工具（Honeytools）**：在工具列表中加入看似对攻击者有吸引力但实际无害的工具（如 `export_all_user_data`）。正常用户不会调用，一旦调用即判定为入侵。
*   **第二层：虚假凭据（Honeytokens）**：在环境中植入虚假的 API 密钥或密码。AgentShield 监控工具调用参数，若其中出现这些虚假值，说明智能体正在尝试窃取信息。
*   **第三层：参数验证器（Parameter Validator）**：对敏感工具（如银行转账）的参数实施白名单制度。非白名单目标的调用将被标记。
*   **自监督分类器**：利用上述陷阱触发信号作为“零误报”标签，提取 30 种行为特征（如工具调用频率、读写顺序等），训练一个随机森林（Random Forest）分类器，用于识别未直接触碰陷阱但行为异常的入侵。

#### 3. 实验设计
*   **数据集/场景**：使用 **AgentDojo (v1.2.2)** 框架，模拟银行、消息、旅行和办公四个环境。
*   **测试模型**：GPT-4o-mini、GPT-5-mini、Llama 3.3 70B、DeepSeek-V3。
*   **攻击套件**：包含 176 个跨语言提示，涵盖英语、库尔德语（Sorani）、阿拉伯语及英-库混合语。
*   **对比方法**：
    *   **Spotlighting**（预防性防御基准）。
    *   **输入级分类器**：ProtectAI DeBERTa v2 和 Meta Prompt-Guard-2。
*   **自适应攻击评估**：设计了三个级别的攻击者知识（从仅知有防御到已知所有陷阱细节），共进行 1,728 次运行。

#### 4. 资源与算力
*   论文未明确说明具体的 GPU 型号或训练时长。
*   **计算开销**：提到 AgentShield 的三层检查每轮调用增加不到 50ms 的延迟，计算开销小于 1%，且不需要额外的 LLM 调用（相比 MELON 等需要双倍推理的方法更高效）。
*   **模型获取**：通过 OpenAI API 和 Together AI 平台访问。

#### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 **6,800 次** 测试运行。
*   **充分性**：实验设计非常全面，不仅涵盖了多模型、多语言，还专门针对“自适应攻击”进行了压力测试。
*   **客观性**：通过 485 次正常任务运行来测试误报率（False Positive Rate），并使用 Wilson 95% 置信区间进行统计验证，确保了实验的严谨性。

#### 6. 主要结论与发现
*   **高检测率**：在商业模型上，AgentShield 捕获了 **90.7% - 100%** 的成功攻击。
*   **零误报**：在 485 次正常使用测试中，误报率为 0。
*   **跨语言鲁棒性**：英语与库尔德语之间的检测率差距极小（最大仅 6.4%），证明了基于行为的检测比基于文本的检测更具普适性。
*   **自适应抗性**：在商业模型上，即使攻击者知道陷阱名称，也未能实现逃逸。
*   **迁移能力**：自监督分类器在未见过的模型和语言上表现优异（F1 分数约 0.99）。

#### 7. 优点
*   **语言无关性**：首次深入评估了低资源语言（库尔德语、阿拉伯语）下的智能体安全。
*   **低成本高精度**：无需人工标注数据即可训练高精度分类器，且运行开销极低。
*   **互补性强**：作为检测层，它可以与现有的预防性防御（如 Spotlighting）完美结合。

#### 8. 不足与局限
*   **基准单一**：主要依赖 AgentDojo 框架，在 InjecAgent 等其他基准上因现代模型本身安全性提升而未能获得足够成功攻击样本。
*   **环境依赖**：虚假凭据（Honeytokens）在不涉及文件浏览的简单任务中难以触发。
*   **参数白名单维护**：参数验证层需要根据具体环境手动配置白名单，存在一定的维护成本。
*   **攻击覆盖**：如果攻击者仅使用合法工具向合法目标发送恶意内容，该方法可能失效。

（完）
