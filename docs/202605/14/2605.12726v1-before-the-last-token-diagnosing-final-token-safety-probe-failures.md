---
title: "Before the Last Token: Diagnosing Final-Token Safety Probe Failures"
title_zh: 在最后一个 Token 之前：诊断末尾 Token 安全探测器的失效
authors: Shravan Doda
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12726v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 诊断针对越狱提示的安全性探测器失效问题
tldr: 本研究探讨了大型语言模型中仅监测预填充阶段最后一个 Token 的安全探测器的局限性。研究发现，越狱提示词往往将不安全证据分布在序列早期的 Token 中，导致末尾探测器失效。作者通过子空间分析和 Token 级预填充分析揭示了这一失效模式，并提出了一种基于 PCA-HMM 的轨迹模型。该模型仅在干净数据上训练，却能有效识别被遗漏的越狱攻击，且不会像朴素池化方法那样产生大量误报，为安全诊断提供了新视角。
selection_source: fresh_fetch
motivation: 现有的安全探测器通常只关注提示词预填充后的最后一个 Token，容易忽略分布在序列早期 Token 中的越狱攻击证据。
method: 研究了 SafeSwitch 风格探测器在三种 LLM 上的表现，并提出一种基于 PCA-HMM 的轨迹模型来分析用户内容的预填充隐藏状态轨迹。
result: 实验表明越狱证据常出现在序列早期而非末尾，PCA-HMM 模型能有效找回被遗漏的越狱样本，且误报率显著低于朴素的 Token 池化方法。
conclusion: 轨迹感知的隐藏状态分析是末尾 Token 探测器的重要诊断补充，能够更全面地识别复杂的安全威胁。
---

## 摘要
末尾 Token 安全探测器在提示词预填充后监控单个隐藏状态，但越狱提示词可能包含分布在早期用户 Token 表示中的、探测器可见的不安全证据，而这种读取方式会遗漏这些证据。我们使用在三个指令微调的 LLM 上仅针对干净的有害和良性提示词训练的 SafeSwitch 风格探测器，研究了这种预填充阶段的失效模式。这些探测器在干净的有害提示词上实现了高召回率，但遗漏了许多越狱行为，并可能在与安全相关的良性提示词上产生误报。子空间分析表明，被遗漏的越狱提示词与干净的良性提示词在某些方向上存在差异，而这些方向未能被探测器的表示子空间很好地捕捉，且增加探测器的瓶颈宽度并不能可靠地解决这种不匹配问题。Token 级别的预填充分析显示，探测器可见的不安全证据通常出现在序列早期，但在末尾 Token 读取时并未暴露，而对 Token 位置进行朴素的最大池化则会在安全提示词上过度触发。一个仅在相同干净划分数据集上训练的简单 PCA-HMM 轨迹模型，能够从用户内容的预填充轨迹中恢复许多末尾 Token 的遗漏，且不会出现朴素 Token 池化那种灾难性的误报行为，这为将轨迹感知的隐藏状态分析作为末尾 Token 探测器的诊断补充提供了动力。

## Abstract
Final-token safety probes monitor a single hidden state after prompt prefill, but jailbreak prompts can contain probe-visible unsafe evidence distributed across earlier user-token representations that is missed by this readout. We study this prefill-time failure mode using SafeSwitch-style probes trained only on clean harmful and benign prompts across three instruction-tuned LLMs. The probes achieve high recall on clean harmful prompts, but miss many jailbreaks and can produce false positives on safety-adjacent benign prompts. Subspace analyses suggest that missed jailbreaks differ from clean benign prompts along directions that are poorly captured by the probe's representational subspace, and increasing probe bottleneck width does not reliably resolve this mismatch. Token-level prefill analyses reveal that probe-visible unsafe evidence often appears earlier in the sequence but is not exposed at the final-token readout, while naive max-pooling over token positions overfires on safe prompts. A simple PCA-HMM trajectory model, trained only on the same clean split, recovers many final-token misses from user-content prefill trajectories without the catastrophic false-positive behavior of naive token pooling, motivating trajectory-aware hidden-state analyses as diagnostic complements to final-token probes

---

## 论文详细总结（自动生成）

这篇论文对大型语言模型（LLM）安全探测器的失效模式进行了深入的诊断分析。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 安全监控系统（如 SafeSwitch）通常只在提示词预填充（Prefill）阶段的**最后一个 Token** 处提取隐藏状态进行安全检测。这种做法虽然高效，但容易被“越狱”攻击（Jailbreak）绕过。
*   **研究动机**：作者发现，越狱提示词通常将有害请求嵌入在复杂的包装（如角色扮演、伪装指令）中。这种包装会导致有害特征在序列末尾发生“表示坍缩”或偏移，使得仅关注末尾 Token 的探测器无法识别。
*   **整体含义**：本研究旨在诊断这种“末尾 Token 失效”的几何学原因，并证明不安全证据实际上存在于序列的早期位置，可以通过轨迹分析来找回。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：从几何表示空间和时间序列轨迹两个维度诊断探测器的失效。
*   **关键技术细节**：
    *   **探测器架构**：采用 SafeSwitch 风格的线性瓶颈探测器，在预填充末尾 Token 的隐藏状态上进行二分类训练。
    *   **子空间几何分析**：定义了四个方向向量（有害 vs 良性、安全 vs 拒绝、捕获的越狱 vs 遗漏的越狱等），通过奇异值分解（SVD）计算探测器权重矩阵的行空间，分析这些方向在探测器“可见”子空间中的能量分布。
    *   **PCA-HMM 轨迹模型**：
        1.  **降维**：对预填充阶段所有 Token 的隐藏状态进行 64 维 PCA 降维。
        2.  **建模**：针对“干净有害”和“干净良性”轨迹分别训练两个 2 状态高斯隐马尔可夫模型（HMM）。
        3.  **评分**：使用长度归一化的对数似然比（Log-Likelihood Ratio）作为诊断分数，判断轨迹更偏向有害还是良性。

### 3. 实验设计
*   **模型对象**：Llama-3.1-8B-Instruct, Mistral-7B-Instruct-v0.1, OLMo3-7B-Instruct。
*   **数据集**：
    *   **训练集**：SafeSwitch 原始训练集（包含干净的有害和良性提示词）。
    *   **评估集**：SorryBench（干净有害请求）、HarmBench（越狱模板）、XSTest（容易引起误报的良性提示词）、AdvBench（直接有害请求）。
*   **对比基准**：
    *   **Final-token Probe**：标准的末尾 Token 探测器。
    *   **Naive Max-pooling**：对序列中所有 Token 的探测器得分取最大值（作为一种朴素的对比方法）。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   考虑到探测器训练和 PCA-HMM 属于轻量级线性操作，且模型参数量在 7B-8B 规模，预计该实验在单张消费级或专业级 GPU（如 A100 或 RTX 4090）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种主流的开源指令微调模型，使用了多个标准安全基准数据集。
*   **消融与验证**：
    *   进行了**瓶颈宽度扫描**（从 64 到 1024），证明增加探测器容量无法解决失效问题。
    *   进行了**Token 级位置分析**，精确定位了有害证据在序列中的位置。
    *   **充分性评价**：实验设计较为全面，通过几何分析解释了“为什么失效”，通过轨迹模型证明了“证据在哪”，逻辑链条完整，对比实验（如 XSTest 的误报测试）保证了结果的客观性。

### 6. 主要结论与发现
*   **末尾 Token 的局限性**：越狱包装会将有害特征推离探测器训练时捕捉到的“有害方向”，导致末尾 Token 的表示在探测器可见子空间中的能量极低（仅 2.4% - 3.5%）。
*   **证据的前置性**：在被遗漏的越狱样本中，有害证据往往出现在嵌入的原始请求 Token 处，此时探测器得分极高，但到了末尾 Token 时得分骤降。
*   **轨迹模型的有效性**：PCA-HMM 诊断模型能够找回 90% 以上被末尾探测器遗漏的越狱样本，同时其误报率显著低于简单的最大池化方法。
*   **容量非瓶颈**：单纯增加探测器的参数维度并不能显著提升对越狱的检测能力。

### 7. 优点
*   **诊断深入**：不仅指出了失效现象，还通过几何投影分析给出了数学上的解释。
*   **方法简洁**：PCA-HMM 模型仅在干净数据上训练，不依赖于越狱样本的先验知识，具有较好的泛化潜力。
*   **视角新颖**：将安全检测从“单点快照”转向“动态轨迹”，为未来的实时安全监控提供了新思路。

### 8. 不足与局限
*   **非防御性工具**：PCA-HMM 目前仅作为诊断工具，尚未证明其作为实时防御部署时的推理开销和鲁棒性。
*   **误报风险**：虽然比最大池化好，但在 XSTest 等复杂良性样本上仍存在一定的误报（6% - 15%）。
*   **对抗鲁棒性**：尚未测试针对轨迹模型本身的自适应攻击（例如故意拉长提示词或在末尾添加大量干扰 Token）。
*   **可解释性**：HMM 的隐状态尚未能与具体的语义或机制（如模型内部的拒绝逻辑）建立明确的因果联系。

（完）
