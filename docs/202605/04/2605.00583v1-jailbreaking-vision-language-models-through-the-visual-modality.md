---
title: Jailbreaking Vision-Language Models Through the Visual Modality
title_zh: 通过视觉模态对视觉语言模型进行越狱攻击
authors: "Aharon Azulay, Jan Dubiński, Zhuoyun Li, Atharv Mittal, Yossi Gandelsman"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00583v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对视觉语言模型的越狱攻击，绕过安全对齐
tldr: 本研究探讨了视觉语言模型（VLM）在视觉模态下的安全漏洞，提出了四种利用图像绕过安全对齐的越狱攻击方法：视觉符号编码、良性替代物、图像文本替换和视觉类比谜题。实验表明，现有的文本安全训练无法有效迁移至视觉模态，视觉攻击在多个前沿模型上表现出极高的成功率，强调了将视觉作为安全对齐首要目标的必要性。
selection_source: fresh_fetch
motivation: 视觉语言模型的视觉模态是一个尚未被充分探索的攻击面，现有的安全对齐主要集中在文本层面。
method: 提出了包括视觉符号编码、良性替代物、图像文本替换和视觉类比谜题在内的四种新型视觉越狱攻击手段。
result: 视觉攻击在六个前沿模型上均表现出显著的成功率，例如视觉密码在Claude-Haiku-4.5上的成功率远高于等效的文本密码。
conclusion: 文本层面的安全训练无法自动推广到视觉意图，稳健的VLM对齐必须将视觉模态视为安全后训练的核心目标。
---

## 摘要
视觉语言模型（VLM）的视觉模态是绕过安全对齐的一个尚未得到充分探索的攻击面。我们介绍了四种利用视觉组件的越狱攻击：(1) 将有害指令编码为带有解码图例的视觉符号序列；(2) 用良性替代品替换有害物体（例如，炸弹 -> 香蕉），然后使用替代术语提示有害行为；(3) 将图像中的有害文本（例如书封上的文字）替换为良性词汇，同时通过视觉上下文保留原始含义；(4) 视觉类比谜题，其解法需要推断出被禁止的概念。通过在六种前沿 VLM 上进行评估，我们的视觉攻击绕过了安全对齐，并揭示了跨模态对齐差距：基于文本的安全训练不会自动泛化到以视觉方式传达的有害意图。例如，我们的视觉密码在 Claude-Haiku-4.5 上实现了 40.9% 的攻击成功率，而等效的文本密码仅为 10.7%。为了进一步深入了解攻击机制，我们展示了初步的可解释性和缓解结果。这些发现强调，稳健的 VLM 对齐需要将视觉视为安全后训练的一等目标。

## Abstract
The visual modality of vision-language models (VLMs) is an underexplored attack surface for bypassing safety alignment. We introduce four jailbreak attacks exploiting the vision component: (1) encoding harmful instructions as visual symbol sequences with a decoding legend, (2) replacing harmful objects with benign substitutes (e.g., bomb -> banana) then prompting for harmful actions using the substitute term, (3) replacing harmful text in images (e.g., on book covers) with benign words while visual context preserves the original meaning, and (4) visual analogy puzzles whose solution requires inferring a prohibited concept. Evaluating across six frontier VLMs, our visual attacks bypass safety alignment and expose a cross-modality alignment gap: text-based safety training does not automatically generalize to harmful intent conveyed visually. For example, our visual cipher achieves 40.9% attack success on Claude-Haiku-4.5 versus 10.7% for an equivalent textual cipher. To further our insight into the attack mechanism, we present preliminary interpretability and mitigation results. These findings highlight that robust VLM alignment requires treating vision as a first-class target for safety post-training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Jailbreaking Vision-Language Models Through the Visual Modality》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文探讨了视觉语言模型（VLM）中存在的**跨模态对齐差距（Cross-modality Alignment Gap）**。即：针对文本模态的安全训练（如 RLHF）是否能自动迁移到视觉模态？
*   **研究动机**：随着 VLM 在多模态助手和文档理解中的广泛应用，其安全性至关重要。然而，现有的安全防御大多将视觉视为被动通道，忽略了视觉输入可以主动编码有害意图。
*   **整体含义**：研究证明，攻击者可以通过纯视觉手段（如符号编码、物体替换、类比推理）绕过 VLM 的安全对齐，这表明当前的 VLM 安全机制在处理非文本形式的有害指令时存在严重漏洞。

### 2. 方法论
论文提出了四种利用视觉模态的越狱攻击方法，所有攻击均基于**提示词中性化策略**（将有害词汇替换为占位符 $X_1, X_2$ 等）：

1.  **视觉密码（Visual Cipher）**：
    *   **核心思想**：将有害指令编码为抽象几何符号序列。
    *   **技术细节**：提供一张“图例（Legend）”图像（符号与单词的映射表）和一张“句子”图像（符号序列）。要求模型先解码再执行。
2.  **视觉物体替换（Visual Object Replacement）**：
    *   **核心思想**：在真实场景中用良性物体（如香蕉）替换有害物体（如炸弹），但保留周围的视觉上下文。
    *   **技术细节**：利用场景的“功能性暗示”诱导模型产生语义覆盖，使模型在内部将良性物体理解为被禁止的有害对象。
3.  **视觉文本替换（Visual Text Replacement）**：
    *   **核心思想**：修改图像中（如书封、海报）的有害文字为良性占位符，保留排版和背景。
    *   **技术细节**：迫使模型放弃 OCR（光学字符识别）直接读取，转而依靠视觉先验和文化背景推断原始含义。
4.  **视觉类比谜题（Visual Analogy Riddle）**：
    *   **核心思想**：通过三行类比图（A→B, C→D, E→?）让模型推断出被禁止的概念。
    *   **技术细节**：每一行单独看都是良性的，但类比的解（即有害概念）是执行任务的关键。

### 3. 实验设计
*   **目标模型**：评估了 6 个前沿 VLM：GPT-5.2、Claude-Haiku-4.5、Gemini-3-Flash、Gemini-3.1-Pro、Qwen3-VL-235B、Qwen3-VL-32B。
*   **数据集/场景**：使用 **HarmBench** 标准化评估框架，涵盖网络犯罪、化学/生物危害、虚假信息、骚扰、非法活动和一般伤害 6 大类别。
*   **Benchmark 与对比方法**：
    *   **模态对比**：为每种视觉攻击设计了等效的**纯文本基线**（如文本密码、文本替换、文本谜题）。
    *   **现有攻击对比**：对比了 HADES、FigStep、MM-SafetyBench (SD/TYPO) 等 5 种已有的 VLM 攻击方法。
*   **评估机制**：采用三模型集成判定（Grok-4.1-Fast, Gemini-3-Flash, Claude-Haiku-4.5），按 0-3 分量表（拒绝到完全合规）进行多数投票。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **工具使用**：提到使用 REVE 文本转图像模型进行物体替换图像的生成，使用 Gemini-2.5-flash-image 渲染视觉类比谜题。由于该研究主要是推理侧的红队测试（Red Teaming），不涉及大规模模型预训练，因此算力消耗主要集中在图像生成和模型推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **3,975 次**评估运行。
*   **充分性**：
    *   **多模型覆盖**：涵盖了当前主流的闭源（GPT, Claude, Gemini）和开源（Qwen）顶尖模型。
    *   **统计严谨性**：采用了 Best-of-5 采样策略，并详细分析了法官模型之间的一致性（84.3% 的一致率）。
    *   **客观性**：通过“提示词中性化”隔离了文本模态的影响，确保攻击成功确实源于视觉理解。实验设计较为客观、公平。

### 6. 主要结论与发现
*   **视觉攻击更有效**：在多个模型上，视觉攻击的成功率（ASR）显著高于文本变体。例如，视觉密码在 Claude-Haiku-4.5 上的 ASR 为 40.9%，而文本密码仅为 10.7%。
*   **对齐失效**：现有的安全训练未能有效覆盖视觉嵌入空间。模型能够从视觉上下文中推断出有害意图，即使表面文本是良性的。
*   **机制发现**：通过 Logit Lens 分析发现，视觉攻击在模型后期层抑制了“拒绝信号”，但有害语义在中间层依然被成功解码。
*   **防御可行性**：轻量级的输出端护栏（如 Qwen3Guard）能有效拦截这些攻击，说明输出过滤是目前必要的补救措施。

### 7. 优点
*   **新颖的攻击视角**：不同于以往基于梯度的对抗扰动，本文提出的攻击是**语义化、可解释且黑盒**的。
*   **类比攻击的首创性**：首次系统性地利用多重类比推理来分解并重构有害意图。
*   **深入的机制分析**：不仅展示了攻击成功率，还通过拒绝方向分析和 Logit Lens 探讨了模型为何会“受骗”。

### 8. 不足与局限
*   **依赖生成质量**：攻击效果高度依赖于文本转图像模型的生成质量，图像中的伪影可能导致模型“误解”而非“越狱”。
*   **误解率较高**：在视觉类比攻击中，模型常因无法解开谜题而产生“误解（Misunderstanding）”，这在一定程度上降低了攻击的稳定性。
*   **防御研究有限**：虽然提出了输出端过滤，但尚未深入探讨如何在预训练或微调阶段根治视觉模态的对齐问题。
*   **应用限制**：某些攻击（如视觉文本替换）需要特定的文化先验，可能在不同语言或文化背景的模型上表现不一。

（完）
