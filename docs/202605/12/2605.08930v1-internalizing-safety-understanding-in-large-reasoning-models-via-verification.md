---
title: Internalizing Safety Understanding in Large Reasoning Models via Verification
title_zh: 通过验证在大推理模型中内化安全理解
authors: "Yi Zhang, Yuxin Chen, Leheng Sheng, Dongcheng Zhang, Chaochao Lu, Xiang Wang, An Zhang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08930v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过验证训练内化安全理解
tldr: 针对大推理模型在思维链模式下易产生风险回答的问题，本文提出SInternal框架。该框架通过让模型学习安全验证任务，即利用专家推理轨迹对自身回答进行批判，从而将安全规范内化。实验证明，这种内化安全理解的方法显著提升了模型对域外越狱攻击的鲁棒性，且在结合强化学习时表现优于传统的监督微调，为模型对齐提供了更稳固的基础。
selection_source: fresh_fetch
motivation: 现有对齐范式侧重于外部合规性而非内在安全理解，导致模型难以验证自身输出安全性且易受越狱攻击。
method: 提出SInternal框架，通过在安全验证任务上训练模型，使其学习利用专家推理轨迹对自身生成的答案进行批判。
result: 该方法显著增强了模型对响应安全性的泛化能力，并大幅提升了对抗域外越狱攻击的鲁棒性。
conclusion: 内化安全理解比单纯模仿安全行为能为大推理模型的对齐提供更具鲁棒性的基础。
---

## 摘要
虽然显式思维链（CoT）赋予了大推理模型（LRMs）强大的能力，但它也使得生成更具风险的最终答案成为可能。当前的对齐范式主要依赖于外部强制的合规性，优化模型以检测恶意提示词，而非评估其自身输出的安全性。我们认为这种方法在很大程度上仍停留在行为层面：我们的实证分析表明，表面上已对齐的模型缺乏内在的安全理解，往往无法验证自身回答的安全性，且在对抗性越狱攻击面前依然脆弱。为了解决这一根本局限性，我们提出了 Safety Internal (SInternal)，这是一个通过专门在安全验证任务上训练 LRMs，利用专家推理轨迹对其自身生成的答案进行批判，从而内化安全规范的框架。我们证明，学习验证能诱导出对回答安全性的强大泛化能力，显著增强了针对域外越狱攻击的鲁棒性。此外，当与强化学习结合时，相比于标准的监督微调，SInternal 提供了更优的初始化，这表明内化安全理解比单纯模仿安全行为能为对齐构建更稳固的基础。我们的代码可在 https://github.com/AlphaLab-USTC/SInternal 获取。

## Abstract
While explicit Chain-of-Thought (CoT) empowers large reasoning models (LRMs), it enables the generation of riskier final answers. Current alignment paradigms primarily rely on externally enforced compliance, optimizing models to detect malicious prompts rather than evaluating the safety of their own outputs. We argue that this approach remains largely behavioral: our empirical analysis reveals that ostensibly aligned models lack intrinsic safety understanding, often failing to verify their own response safety and remaining vulnerable to adversarial jailbreaks. To address this fundamental limitation, we propose Safety Internal (SInternal), a framework that internalizes safety specifications by training LRMs exclusively on safety verification tasks to critique their own generated answers using expert reasoning trajectories. We demonstrate that learning to verify induces a strong generalization for response safety, significantly enhancing robustness against out-of-domain jailbreaks. Furthermore, when combined with reinforcement learning, SInternal serves as a superior initialization compared to standard supervised fine-tuning, suggesting that internalizing safety understanding creates a more robust foundation for alignment than merely mimicking safe behaviors. Our codes are available at https://github.com/AlphaLab-USTC/SInternal

---

## 论文详细总结（自动生成）

这是一份关于论文《Internalizing Safety Understanding in Large Reasoning Models via Verification》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前大推理模型（LRM）的安全对齐主要采用“以答案为中心”（Answer-centric）的范式，即通过监督微调（SFT）或强化学习（RL）让模型模仿安全的回答行为。
*   **研究动机**：作者发现这种表面对齐存在严重缺陷：
    1.  **缺乏内在理解**：模型只是学会了“如何拒绝”，而不理解“为什么要拒绝”。实验表明，对齐后的模型在验证自身输出是否安全时表现极差（甚至低于随机水平）。
    2.  **脆弱性**：攻击者可以利用思维链（CoT）诱导模型进入合规的推理路径，从而绕过安全机制生成有害内容。
    3.  **泛化性差**：模型难以应对未见过的越狱（Jailbreak）攻击。
*   **整体含义**：论文旨在将外部安全规范“内化”为模型的内在验证能力，使模型能够自主批判和识别自身输出的风险。

### 2. 论文提出的方法论：SInternal 框架
*   **核心思想**：不再仅仅训练模型生成安全答案，而是训练模型执行“安全验证任务”，即让模型学习如何根据安全规范对候选答案进行批判性分析。
*   **关键技术细节**：
    1.  **自响应生成（Self-Response Generation）**：从初始策略模型中采样多个回答（包含安全和不安全的），确保训练数据覆盖模型真实的输出分布。
    2.  **专家验证轨迹构建**：利用强专家模型（如 Claude-4-Sonnet）针对上述回答生成验证轨迹。轨迹包含：**批判性推理（Critique）**（分析是否违反规范及其原因）和**最终判断（Judgment）**（安全/不安全）。
    3.  **验证任务训练（SInternal）**：在验证轨迹数据集上对模型进行监督微调。目标函数是最大化给定“提示词+回答+规范”时，生成验证轨迹的概率。
    4.  **后续强化学习（RLVR）**：在 SInternal 初始化的基础上，使用可验证的安全奖励（如 Qwen3-Guard 的反馈）通过 GRPO 算法进一步优化生成策略。

### 3. 实验设计
*   **模型对象**：DeepSeek-R1-Distill 系列（Qwen-7B, Llama-8B, Qwen-14B）。
*   **数据集**：
    *   **训练集**：从 WildJailbreak 和 STAR-1 中采样，构建约 6000 条验证轨迹。
    *   **安全 Benchmark**：StrongREJECT（直接有害）、WildJailbreak/Fortress/Jailbreak-R1（越狱攻击）、HCoT/Trotter（针对 LRM 的 CoT 攻击）。
    *   **过度拒绝 Benchmark**：XSTest（评估是否误伤无害请求）。
    *   **通用推理 Benchmark**：MATH、AIME2024（评估对齐是否损害智力）。
*   **对比方法**：Base（原始模型）、SafeChain（CoT 安全对齐）、STAR-1（基于规范的推理对齐）以及这些方法结合 GRPO 的版本。

### 4. 资源与算力
*   **硬件环境**：使用一台配备 **8 张 NVIDIA H800 GPU** 的服务器。
*   **处理器**：两颗 Intel Xeon Platinum 8558 CPU（共 192 核心）。
*   **训练细节**：
    *   SFT 阶段：使用 LLaMA-Factory 框架进行 LoRA 微调，训练 2 个 Epoch。
    *   RL 阶段：使用 verl 框架运行 GRPO 算法，训练 3 个 Epoch。
    *   最大序列长度：8192 标记。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 14B 的三种主流架构模型，验证了方法的通用性。
*   **消融实验**：
    *   验证了“验证自身生成内容”优于“验证外部内容”。
    *   分析了“批判性推理”组件对泛化性能的决定性作用。
    *   测试了模型对不同安全规范（如 Llama Guard 4 的 14 分类法）的适应性。
*   **充分性评价**：实验设计非常全面，不仅关注安全性（ASR），还兼顾了过度拒绝（CR）和数学推理能力（Pass@1），并引入了最新的针对性攻击（HCoT），体现了高度的客观性和公平性。

### 6. 论文的主要结论与发现
1.  **验证能力可转化**：仅通过训练验证任务，模型在生成任务上的安全性也会显著提升，尤其是在应对 OOD（域外）越狱攻击时。
2.  **RL 的优质基座**：SInternal 提供的初始化比标准 SFT 更稳固，能让后续的强化学习达到更高的安全上限，且能有效抵御 CoT 劫持攻击。
3.  **高数据效率**：SInternal 仅需 50% 的数据即可达到甚至超过传统 SFT 方法的安全性。
4.  **主动验证行为**：训练后的模型在处理有害提示词时，其 CoT 轨迹中会自发出现安全审查行为。

### 7. 优点（亮点）
*   **范式创新**：从“模仿安全结果”转向“内化安全逻辑”，解决了对齐“皮实度”不足的问题。
*   **解决 LRM 特有风险**：针对推理模型容易被 CoT 诱导的特性，通过强化内在验证逻辑提供了有效的防御手段。
*   **实用性强**：方法简单且与现有的 RL 流程高度兼容，显著提升了数据效率。

### 8. 不足与局限
*   **验证滞后性**：尽管有所提升，模型的验证能力（判断准确度）在某些场景下仍滞后于其生成能力。
*   **静态验证限制**：目前主要集中在生成后的验证，如何将这种能力实时融入到动态的推理步（Step-by-step self-correction）中仍是开放问题。
*   **计算开销**：虽然训练高效，但在推理时生成详尽的验证 CoT 会增加 Token 消耗和响应延迟。

（完）
