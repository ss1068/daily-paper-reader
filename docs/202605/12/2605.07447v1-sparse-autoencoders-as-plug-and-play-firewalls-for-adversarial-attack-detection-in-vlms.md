---
title: Sparse Autoencoders as Plug-and-Play Firewalls for Adversarial Attack Detection in VLMs
title_zh: 稀疏自编码器作为视觉语言模型中对抗攻击检测的即插即用防火墙
authors: "Hao Wang, Yiqun Sun, Pengfei Wei, Lawrence B. Hsieh, Daisuke Kawahara"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07447v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 稀疏自编码器作为攻击检测防火墙
tldr: 针对视觉语言模型（VLM）易受对抗攻击的问题，本文提出了一种名为 SAEgis 的轻量级检测框架。该方法通过在预训练 VLM 中插入稀疏自编码器（SAE），利用其学习到的稀疏潜特征来识别对抗性扰动。SAEgis 无需对抗训练，具有即插即用的特性，在跨领域和跨攻击场景下表现出卓越的泛化能力和检测性能，为 VLM 系统提供了实用的安全保障。
selection_source: fresh_fetch
motivation: 视觉语言模型在实际应用中极易受到对抗攻击，而现有的防御手段往往开销巨大或泛化能力不足。
method: 通过在预训练 VLM 中嵌入稀疏自编码器（SAE），利用其提取的稀疏潜特征作为识别对抗扰动的关键信号。
result: 实验证明 SAEgis 在多种攻击和跨领域场景下均能实现可靠的攻击检测，且在跨领域泛化性能上显著优于基准方法。
conclusion: SAEgis 作为一种无需对抗训练的即插即用防火墙，为提升 VLM 系统的安全性提供了一种高效且低开销的新途径。
---

## 摘要
视觉语言模型（VLM）发展迅速，并越来越多地应用于现实场景，特别是随着基于智能体（agent-based）系统的兴起。然而，它们的安全性受到的关注相对有限。即使是最新的闭源和开源视觉语言模型，在对抗攻击面前仍然非常脆弱，这使得下游应用面临巨大风险。在这项工作中，我们提出了一种基于稀疏自编码器（SAE）的新型轻量级对抗攻击检测框架，命名为 SAEgis。通过在预训练的视觉语言模型中插入 SAE 模块，并使用标准重构目标进行训练，我们发现学习到的稀疏潜在特征能够自然地捕捉到与攻击相关的信号。这些特征使得系统能够可靠地分类输入图像是否受到了对抗性扰动，即使对于以前未见过的样本也是如此。大量实验表明，SAEgis 在域内、跨域和跨攻击设置下均表现出强大的性能，与现有基准相比，在跨域泛化方面提升尤为显著。此外，结合来自多个层的信号进一步提高了鲁棒性和稳定性。据我们所知，这是首个探索将 SAE 作为视觉语言模型中对抗攻击检测即插即用机制的工作。我们的方法不需要额外的对抗训练，引入的开销极小，并为提高现实世界视觉语言模型系统的安全性提供了一种实用的方法。

## Abstract
Vision-language models (VLMs) have advanced rapidly and are increasingly deployed in real-world applications, especially with the rise of agent-based systems. However, their safety has received relatively limited attention. Even the latest proprietary and open-weight VLMs remain highly vulnerable to adversarial attacks, leaving downstream applications exposed to significant risks. In this work, we propose a novel and lightweight adversarial attack detection framework based on sparse autoencoders (SAEs), termed SAEgis. By inserting an SAE module into a pretrained VLM and training it with standard reconstruction objectives, we find that the learned sparse latent features naturally capture attack-relevant signals. These features enable reliable classification of whether an input image has been adversarially perturbed, even for previously unseen samples. Extensive experiments show that SAEgis achieves strong performance across in-domain, cross-domain, and cross-attack settings, with particularly large improvements in cross-domain generalization compared to existing baselines. In addition, combining signals from multiple layers further improves robustness and stability. To the best of our knowledge, this is the first work to explore SAE as a plug-and-play mechanism for adversarial attack detection in VLMs. Our method requires no additional adversarial training, introduces minimal overhead, and provides a practical approach for improving the safety of real-world VLM systems.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SAEgis** 的轻量级框架，利用稀疏自编码器（SAE）作为视觉语言模型（VLM）的“即插即用”防火墙，用于检测对抗攻击。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）在面对对抗性图像扰动时极其脆弱。攻击者可以通过微小的像素级修改误导模型生成错误或有害的输出。
*   **研究背景**：现有的防御方法通常需要昂贵的对抗训练，或者在面对未见过的攻击类型和跨领域数据时泛化能力较差。
*   **动机**：探索一种无需修改原始模型权重、计算开销极小且具备强泛化能力的对抗检测机制。

### 2. 方法论：SAEgis 框架
*   **核心思想**：在预训练 VLM 的内部层（如视觉编码器或投影层）插入 SAE。由于 SAE 是在干净数据上训练的重构模块，对抗扰动会引起其稀疏潜特征（Latent Features）的异常激活。
*   **关键技术流程**：
    1.  **SAE 训练**：使用标准重构目标在干净图像上训练 SAE，使其学习正常视觉特征的稀疏表示。
    2.  **攻击相关特征选择**：利用少量对抗样本，计算每个特征的“攻击得分”。得分综合考虑了特征激活的**峰值强度**和**空间范围**（通过对数函数平衡）。选择得分最高的 Top-K 个特征作为检测信号。
    3.  **对抗检测**：在推理时，统计输入图像激活的 Top-K 特征数量。如果激活数量超过基于干净开发集设定的阈值（分位数法），则判定为对抗攻击。
    4.  **多层集成**：结合视觉编码器早期层（捕捉低级纹理扰动）和投影层（捕捉高级语义偏移）的信号，提升检测稳定性。

### 3. 实验设计
*   **数据集**：
    *   **NIPS17**、**LLaVA-Instruct-150K**（通用自然图像）。
    *   **Medical Multimodal Evaluation Data**（医疗领域图像，用于极端的跨领域评估）。
*   **攻击方法**：SSA-CWA（早期基准）、M-Attack 和 FOA-Attack（最新的强力攻击）。
*   **对比基准（Benchmark）**：
    *   **Dense Baseline**：直接基于隐藏状态（Hidden States）的余弦相似度检测。
    *   **PIP**：基于无关探测问题和注意力图训练 SVM 分类器的方法。
    *   **MSE**：直接利用 SAE 的重构误差作为异常得分（实验证明效果不佳）。
*   **实验场景**：分为域内（In-domain）、跨领域（Cross-domain）和跨攻击（Cross-attack）三种设置。

### 4. 资源与算力
*   **模型架构**：以 **Qwen2.5-VL-3B-Instruct** 为骨干网络。
*   **SAE 训练**：在 FineVision 数据集（50万样本）上训练，潜特征维度为 32,768，Top-K 稀疏度为 64。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。但提到该方法是轻量级的，且 SAE 模块是独立于 VLM 训练的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种数据集、3 种攻击算法、9 个不同的模型层位置分析，以及多种跨设置组合。
*   **充分性**：实验设计非常全面，特别是**跨领域（如从自然图像到医疗图像）**和**跨攻击（从弱攻击到强攻击）**的测试，验证了模型在现实部署中面对未知威胁的鲁棒性。
*   **客观性**：通过设定固定的假阳性率（FPR=0.02）来确定阈值，确保了不同方法在相同误报水平下的公平比较。

### 6. 主要结论与发现
*   **强泛化性**：SAEgis 在跨领域场景下的表现显著优于密集特征（Dense）基准。密集特征容易过拟合特定分布，而 SAE 学习到的稀疏特征能捕捉更本质的攻击信号。
*   **层级互补**：早期视觉层对高频像素扰动敏感，而深层/投影层对语义偏移敏感。多层集成能有效消除检测盲点。
*   **高效性**：仅需 10 个对抗样本进行特征选择即可达到约 80% 的 F1 分数，极具实用价值。

### 7. 优点与亮点
*   **即插即用**：无需对 VLM 进行微调或对抗训练，不影响原始模型的推理性能。
*   **可解释性**：SAE 提供的稀疏特征比隐藏状态更具可解释性，可以追踪是哪些具体的视觉特征触发了警报。
*   **跨领域鲁棒性**：在分布偏移严重的医疗数据上依然保持了高检测率，这是现有许多防御方法难以企及的。

### 8. 不足与局限
*   **依赖少量攻击样本**：虽然需求量小，但仍需预先获取少量针对性的对抗样本来识别“攻击相关特征”。
*   **阈值敏感性**：检测性能依赖于阈值的设定，如果测试环境的干净数据分布与开发集差异极大，可能会导致误报率上升。
*   **攻击覆盖**：虽然测试了强力攻击，但尚未讨论针对 SAE 检测机制本身的“自适应攻击”（Adaptive Attacks）。

（完）
