---
title: Re-Triggering Safeguards within LLMs for Jailbreak Detection
title_zh: 重新触发大语言模型内部防御机制以进行越狱检测
authors: "Zheng Lin, Zhenxing Niu, Haoxuan Ji, Yuzhe Huang, Haichang Gao"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10611v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 防御攻击的越狱检测方法
tldr: 本研究针对大语言模型（LLM）易受越狱攻击的问题，提出了一种基于嵌入扰动的越狱检测方法。研究者发现越狱提示词具有脆弱性，通过对输入嵌入进行微小扰动，可以重新激活模型内部被绕过的安全机制。该方法不依赖外部防御，而是与模型内置防御协同工作。实验证明，该方法在白盒、黑盒及自适应攻击场景下均表现出卓越的防御效能和鲁棒性。
selection_source: fresh_fetch
motivation: 尽管LLM内置了安全机制，但精心设计的越狱提示词仍能绕过这些防御，因此需要一种方法来重新激活模型内部的保护功能。
method: 提出一种嵌入扰动技术，通过高效搜索算法寻找合适的扰动，从而重新触发LLM内部的防御机制以识别越狱请求。
result: 实验结果显示，该方法能有效抵御最先进的白盒与黑盒越狱攻击，并对自适应攻击保持强鲁棒性。
conclusion: 利用越狱提示词的脆弱性并重新触发模型内置安全机制，是提升LLM安全性的一种高效且稳健的防御策略。
---

## 摘要
本文提出了一种针对大语言模型（LLMs）的越狱提示词检测方法，旨在防御越狱攻击。尽管近期的 LLMs 配备了内置防御机制，但仍有可能构建出绕过这些机制的越狱提示词。我们认为此类越狱提示词本质上是脆弱的，因此引入了一种嵌入扰动（embedding disruption）方法来重新激活 LLMs 内部的防御机制。与旨在作为独立解决方案的先前防御方法不同，我们的方法通过重新触发 LLM 内部的防御机制与其协同工作。此外，通过广泛的分析，我们对扰动效应有了全面的理解，并开发了一种高效的搜索算法，以识别合适的扰动来实现有效的越狱检测。大量实验表明，我们的方法在白盒和黑盒设置下均能有效防御最先进的越狱攻击，并且即使针对自适应攻击也保持鲁棒性。

## Abstract
This paper proposes a jailbreaking prompt detection method for large language models (LLMs) to defend against jailbreak attacks. Although recent LLMs are equipped with built-in safeguards, it remains possible to craft jailbreaking prompts that bypass them. We argue that such jailbreaking prompts are inherently fragile, and thus introduce an embedding disruption method to re-activate the safeguards within LLMs. Unlike previous defense methods that aim to serve as standalone solutions, our approach instead cooperates with the LLM's internal defense mechanisms by re-triggering them. Moreover, through extensive analysis, we gain a comprehensive understanding of the disruption effects and develop an efficient search algorithm to identify appropriate disruptions for effective jailbreak detection. Extensive experiments demonstrate that our approach effectively defends against state-of-the-art jailbreak attacks in white-box and black-box settings, and remains robust even against adaptive attacks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Re-Triggering Safeguards within LLMs for Jailbreak Detection》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：尽管现代大语言模型（LLMs）通过人类反馈强化学习（RLHF）等技术内置了安全机制，但研究表明，通过精心设计的“越狱提示词”（Jailbreak Prompts），攻击者仍能绕过这些防御，诱导模型生成有害内容。
*   **核心问题**：现有的外部防御方法（如困惑度过滤、输入改写等）往往在防御效能、计算开销或模型通用性（Utility）之间难以平衡。
*   **研究动机**：作者观察到，成功的越狱提示词通常需要经过多次迭代优化才能获得，这暗示它们在提示词空间中是极其稀疏且“脆弱”的。基于此，论文探讨是否可以通过微小的扰动，使越狱提示词失效，从而重新触发模型内部本已存在的安全防御机制。

### 2. 论文提出的方法论
*   **核心思想**：通过在 LLM 的输入嵌入层（Embedding Layer）注入适度的噪声，将处于“越狱状态”的输入推回“拒绝状态”。如果扰动后的输入导致模型输出拒绝语（如“对不起，我不能...”），则判定原输入为越狱攻击。
*   **关键技术细节**：
    *   **扰动位置选择**：通过实验对比，发现对**最后一个 Token** 的嵌入进行扰动最为有效，因为该 Token 捕捉了前文的全局语义。
    *   **嵌入扰动（Embedding Disruption）**：相比于文本层面的修改，嵌入空间的扰动更易于控制强度且具有语义连续性。
    *   **锚点嵌入引导搜索（Anchor Embedding Guided Search）**：
        *   **第一阶段（引导搜索）**：研究发现，能触发拒绝响应的扰动嵌入在空间中呈现长尾分布，倾向于聚集在某些“锚点 Token”（如 'oH', 'wohl' 等）。算法首先尝试将输入嵌入向这些预先识别的锚点方向移动。
        *   **第二阶段（随机搜索）**：若第一阶段失败，则在指定强度范围内进行动态缩减范围的随机搜索。
*   **黑盒防御扩展**：通过“扰动迁移”策略，将白盒模型中发现的有效扰动 Token 替换到原提示词中，用于检测黑盒模型（如 GPT-4）的输入。

### 3. 实验设计
*   **数据集**：使用了 **AdvBench** 和 **JailbreakBench** 两个主流越狱攻击基准。
*   **攻击场景**：涵盖了白盒、黑盒以及**自适应攻击**（攻击者已知防御机制并针对性优化）。
*   **对比方法（Baselines）**：
    *   Perplexity Filtering（困惑度过滤）
    *   Erase-and-Check（删除并检查）
    *   Paraphrasing（改写）
    *   SmoothLLM（基于字符扰动的平滑防御）
    *   RESTA（基于嵌入平滑的防御）
    *   Guard Models（如 LLaMA Guard 3, GuardReasoner）
*   **评估指标**：攻击成功率（ASR）、检测率（DR）、误报率（FR）以及模型通用性指标（AlpacaEval, IFEval）。

### 4. 资源与算力
*   **算力说明**：论文明确提到，在识别每个模型的“锚点 Token”时，使用了单张 **NVIDIA RTX 3090 GPU**。
*   **时长**：针对每个提示词的暴力搜索过程大约需要 **1 个 GPU 小时**。但一旦识别出锚点 Token（每个模型只需识别一次），后续的检测搜索过程非常高效（通常在 50 次迭代内即可达到 90% 的检测率）。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 Vicuna-13B、LLaMA2-7B、Qwen2.5-7B 等多个开源模型，并测试了 GPT-4.1 和 Gemini-2.5 等闭源模型。
*   **充分性**：
    *   测试了 5 种最先进的攻击方法（GCG, PAIR, RS, I-FSJ, AutoDAN-Turbo）。
    *   进行了详细的消融实验，包括扰动层级（输入层 vs 嵌入层 vs 隐藏层）、Token 选择策略、噪声强度等。
    *   **客观性**：引入了自适应攻击测试，这是衡量防御方法鲁棒性的“金标准”，证明了该方法不易被简单的策略调整所绕过。

### 6. 论文的主要结论与发现
*   **越狱提示词的脆弱性**：成功的越狱提示词在嵌入空间中是孤立的，微小的语义偏移就能让模型回归到安全对齐的状态。
*   **内置防御的再激活**：无需额外的护栏模型或重新训练，仅靠扰动即可重新触发 LLM 自身的拒绝机制。
*   **锚点效应**：嵌入空间中存在特定的敏感轴，将嵌入推向这些轴（锚点）能极大地提高检测效率。
*   **性能优越**：在保持极低误报率（接近 0%）的同时，显著降低了各种攻击的成功率，且对模型处理正常指令的能力几乎没有影响。

### 7. 优点
*   **协同防御**：不试图取代 LLM 的内置防御，而是与其协同，充分利用了模型已有的对齐能力。
*   **高通用性**：作为检测层，它不修改模型参数，不影响正常响应的质量（Utility）。
*   **高效搜索**：提出的锚点引导算法比盲目的暴力搜索或随机扰动更具方向性和效率。
*   **黑盒迁移性**：通过 Token 转换技术，成功将白盒防御经验迁移至闭源模型。

### 8. 不足与局限
*   **首字延迟**：虽然搜索算法高效，但在判定输入是否安全时仍需多次前向传播，可能会增加首字生成延迟（TTFT）。
*   **锚点依赖**：锚点 Token 是模型相关的，更换新模型需要预先进行一次性的锚点识别计算。
*   **黑盒性能衰减**：虽然迁移有效，但黑盒场景下的检测率（DR）相比白盒仍有一定程度的下降。
*   **应用限制**：主要针对文本输入，对于多模态输入或极长文本的防御效果尚未深入探讨。

（完）
