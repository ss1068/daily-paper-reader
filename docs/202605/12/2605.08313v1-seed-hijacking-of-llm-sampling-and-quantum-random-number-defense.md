---
title: Seed Hijacking of LLM Sampling and Quantum Random Number Defense
title_zh: 大语言模型采样的种子劫持与量子随机数防御
authors: "Ziyang You, Xiaoke Yang, Zhanling Fan, Feng Guo, Xiaogen Zhou, Xuxing Lu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08313v1"
tags: ["query:llm"]
score: 7.0
evidence: 供应链攻击面与量子随机数防御
tldr: "本研究揭示了大语言模型（LLM）采样层中被忽视的供应链攻击风险。由于 LLM 依赖确定性伪随机数生成器（PRNG），攻击者可通过 SeedHijack 操纵随机数输出，在不改变模型逻辑值的情况下精准注入特定 Token。实验显示该攻击在多种对齐模型上成功率达 100% 且能绕过现有防御。为此，研究者提出利用硬件量子随机数生成器（QRNG）进行防御，在极低开销下有效中和了此类攻击，为 LLM 安全提供了新思路。"
selection_source: fresh_fetch
motivation: 大语言模型采样依赖的伪随机数生成器（PRNG）存在易被操纵的安全漏洞，可能导致严重的供应链攻击。
method: 提出 SeedHijack 攻击方法操纵 PRNG 输出以强制 Token 选择，并引入硬件量子随机数生成器（QRNG）作为防御手段。
result: "SeedHijack 在多个对齐模型上实现了 100% 的攻击成功率，而 QRNG 防御方案以极低的延迟和内存开销成功抵御了攻击。"
conclusion: 研究识别了 LLM 采样层的关键漏洞，并证明了基于硬件的量子随机数生成器是保障采样安全的一种实用且高效的防御方案。
---

## 摘要
大语言模型（LLMs）在自回归采样中依赖确定性伪随机数生成器（PRNG），这产生了一个被现有防御措施所忽视的关键供应链攻击面。我们提出了 SeedHijack，这是一种后门攻击，通过操纵 PRNG 输出，在不改变模型 Logits 的情况下强制选择攻击者指定的 Token。在针对 GPT-2 (124M) 的 540 次基准测试中，该攻击在 9 种采样配置下实现了 99.6% 的精确 Token 注入；在四种对齐模型（1.5B-7B，涵盖 RLHF/SFT/推理蒸馏）上达到了 100% 的成功率，并绕过了本研究测试的所有对齐方法。我们进一步提出了一种基于硬件量子随机数生成器（QRNG）的防御方案，在评估的威胁模型中，该方案以微乎其微的中值开销（+0.6% 延迟，+7.7 MB 内存）抵消了攻击。我们的工作识别了一个关键的采样层漏洞，并提供了一种实用且可部署的基于 QRNG 的防御措施。

## Abstract
Large language models (LLMs) rely on deterministic pseudorandom number generators (PRNGs) for autoregressive sampling, creating a critical supply-chain attack surface overlooked by existing defenses. We present SeedHijack, a backdoor attack that manipulates PRNG outputs to force attacker-specified token selection without altering model logits. In a 540-trial benchmark on GPT-2 (124M), the attack achieves 99.6% exact token injection across 9 sampling configurations; it reaches 100% success on four aligned models (1.5B-7B, RLHF/SFT/reasoning distillation) and bypasses all alignment methods tested in this work. We further propose a defense based on a hardware quantum random number generator (QRNG), which neutralizes the attack in our evaluated threat model with negligible median overhead (+0.6% latency, +7.7 MB memory). Our work identifies a critical sampling-layer vulnerability and provides a practical, deployable QRNG-based defense.