---
title: "Defense effectiveness across architectural layers: a mechanistic evaluation of persistent memory attacks on stateful LLM agents"
title_zh: 跨架构层级的防御有效性：针对有状态大语言模型智能体持久化记忆攻击的机理性评估
authors: Jun Wen Leong
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08442v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对持久内存攻击的防御有效性评估
tldr: "本研究针对大语言模型（LLM）智能体面临的持久性内存攻击，系统评估了四层架构中的六种防御措施。通过对9个开源模型进行5040次实验，发现输入层和检索层过滤基本无效，而内存层的工具门控（Memory Sandbox）表现最佳，能将绝大多数模型的攻击成功率降至0%且不损耗效用，为构建安全的有状态智能体提供了重要参考。"
selection_source: fresh_fetch
motivation: 旨在填补针对LLM智能体持久性内存攻击防御有效性系统评估的空白，揭示不同防御机制的局限性。
method: 在四个架构层级上评估了六种防御措施（如输入过滤、检索过滤、提示词加固和内存沙箱），并针对9个开源模型进行了大规模实验。
result: "输入和检索层防御因无法识别语义掩蔽的攻击而失效，而内存沙箱通过限制召回能力在多数模型上将攻击成功率降至0%。"
conclusion: 内存层防御是应对此类攻击最有效的手段，研究结果为防御策略的选择和架构设计提供了机制性的解释和指导。
---

## 摘要
针对大语言模型（LLM）智能体的持久化记忆攻击在开源模型上实现了极高的攻击成功率。在此类攻击中，通过检索增强生成（RAG）检索到的文档注入的恶意指令被存储在持久化记忆中，并在随后的会话中执行。然而，目前尚缺乏针对此类攻击防御有效性的系统性评估。我们针对九个开源模型，在四个架构层级上评估了六种防御措施对抗延迟触发攻击的效果（共计 5,040 次运行，每种条件 N=40）。研究发现，四种防御措施在接近基准攻击成功率（ASR）时失效：输入级过滤（Minimizer、Sanitizer）和检索级过滤（RAG Sanitizer、RAG LLM Judge）的 ASR 达到 88-89%，在统计学上与 88.6% 的无防御基准无显著差异。提示词加固（Prompt Hardening）部分失效，ASR 为 77.8%，其下降主要由两个 ASR 为 0% 的模型驱动：一个是真实的防御效果，另一个是与防御无关的模型级拒绝。架构层面的解释如下：输入级防御无法观察到 RAG 注入的内容，而检索级分类器则被合规框架下的语义掩蔽所击败。一种防御措施，即记忆层的工具门控（Memory Sandbox），通过移除攻击所需的召回能力，将九个模型中八个模型的 ASR 降低至 0%。例外情况则完全反转了防御效果：一个在无防御状态下通过执行拒绝实现 0% ASR 的推理模型，在 Memory Sandbox 下 ASR 反转为 100%，因为移除显式召回迫使模型进入 RAG 路径，而其拒绝机制在该路径下并未激活。在无攻击的情况下，Memory Sandbox 不会带来效用损失（所有条件下的 BTCR 均为 100%）。这些结果首次系统地刻画了各类防御在对抗持久化记忆攻击时失效的原因，为明智的防御投资决策提供了依据。

## Abstract
Persistent memory attacks against LLM agents achieve high attack success rates against open-source models. In these attacks, malicious instructions injected via RAG-retrieved documents are stored in persistent memory and executed in later sessions. However, no systematic evaluation of defense effectiveness against this attack class exists. We evaluate six defenses across four architectural layers against delayed-trigger attacks on nine open-source models (5,040 runs, N=40 per condition). Four defenses fail at approximately baseline attack success rate: input-level filtering (Minimizer, Sanitizer) and retrieval-level filtering (RAG Sanitizer, RAG LLM Judge) achieve 88-89% ASR, statistically indistinguishable from the undefended baseline of 88.6%. Prompt Hardening partially fails at 77.8% ASR, with the reduction driven by two models at 0%: one genuine defense effect and one model-level refusal independent of the defense. The architectural explanation holds: input-level defenses cannot observe RAG-injected content, and retrieval-level classifiers are defeated by compliance-framed semantic masking. One defense, tool-gating at the memory layer (Memory Sandbox), reduces ASR to 0% for eight of nine models by removing the recall capability the attack requires. The exception inverts the defense entirely: a reasoning model that achieves 0% ASR under no defense via execution refusal inverts to 100% ASR under Memory Sandbox, because removing explicit recall forces the model onto the RAG pathway where its refusal mechanism does not activate. Memory Sandbox imposes zero utility cost in the absence of attack (BTCR = 100% across all conditions). These results provide the first systematic characterization of why each defense class fails against persistent memory attacks, enabling informed defense investment decisions.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）智能体在面对“持久化记忆攻击”时的防御有效性进行了系统且深入的机理性评估。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体安全评估多集中在单次会话的输入注入，而忽略了**持久化记忆攻击（Persistent Memory Attacks）**。在这种攻击中，恶意指令通过 RAG（检索增强生成）进入并存储在智能体的长期记忆中，在后续会话中被触发执行。
*   **研究动机**：目前尚无针对此类跨会话攻击防御有效性的系统评估。作者旨在填补这一空白，探究在不同架构层级（输入、检索、指令、工具）部署的防御措施是否能有效拦截此类攻击，并揭示其失效的深层机制。

### 2. 核心方法论
论文提出了一种**延迟触发攻击（DTA）**模型，并针对性地测试了四个架构层级的六种防御措施：
*   **攻击流程**：分为四个隔离会话。S1（注入）：智能体将伪装成合规文件的恶意指令存入数据库；S2-S3：无关的良性会话；S4（触发）：良性提示词触发智能体调用记忆并执行恶意操作（如外泄数据）。
*   **防御措施分类**：
    *   **输入层（Input-level）**：`Minimizer`（相关性过滤）和 `Sanitizer`（正则+分类器），作用于用户输入。
    *   **检索层（Retrieval-level）**：`RAG Sanitizer`（文档分类器）和 `RAG LLM Judge`（小模型判定），作用于 RAG 检索出的文档。
    *   **指令层（Instruction-level）**：`Prompt Hardening`，在系统提示词中加入显式的安全规则。
    *   **工具层（Tool-layer）**：`Memory Sandbox`（核心创新），在触发会话中移除智能体的“显式召回（recall_fact）”工具，仅保留枚举键名的能力。

### 3. 实验设计
*   **模型选择**：选取了 9 个开源模型（包括 Qwen-2.5/3.5 系列、GLM-4、GPT-OSS 等）作为主要评估对象，并对 Claude 4.5/4.6 等前沿模型进行了基准筛选。
*   **实验设置**：采用全因子设计，9 模型 × 7 防御条件（含基准）× 2 攻击分支（有/无攻击）× 40 次重复 = **5,040 次运行**。
*   **评估指标**：攻击成功率（ASR）和良性任务完成率（BTCR，用于衡量防御对效用的影响）。
*   **统计方法**：使用 BCa 自举置信区间（10,000 次重采样）和 Holm-Bonferroni 校正，确保结果的统计显著性。

### 4. 资源与算力
*   **算力说明**：论文提到所有开源模型均通过 **Ollama** 在本地运行。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。实验在 16k 上下文长度、0.0 温度系数下进行，以保证结果的可重复性。

### 5. 实验数量与充分性
*   **实验规模**：总计 5,040 次运行，对于智能体安全评估而言规模巨大且非常充分。
*   **客观性与公平性**：
    *   设置了 2,520 次“无攻击”对照组，确认误报率为 0%。
    *   所有比较均在数据收集前预先注册，避免了事后挑选数据。
    *   通过“机理性标签（Mechanistic Tags）”对每次失败进行归因分析，而非仅报告成功率数字。

### 6. 主要结论与发现
*   **架构决定有效性**：输入层防御完全失效（ASR ~89%），因为它们无法观察到 RAG 注入的内容；检索层防御因无法识别“语义掩蔽”（伪装成合规文件的指令）而失效。
*   **工具层防御最有效**：`Memory Sandbox` 将 8/9 个模型的 ASR 降至 0%，且效用损失为零（BTCR 100%）。
*   **防御反转现象**：发现 QwQ-32B 模型在 `Memory Sandbox` 下会出现“防御反转”，ASR 从 0% 飙升至 100%。原因是移除记忆工具迫使模型走 RAG 路径，绕过了其原本在记忆调用时的拒绝机制。
*   **前沿模型表现**：Claude Sonnet 4.6 表现出极强的注入抗性（0% 注入率），而 Claude Haiku 4.5 则能主动检测攻击并存储安全警报而非恶意载荷。

### 7. 优点与亮点
*   **机理性分析**：不仅报告防御是否失败，还解释了**为什么**失败（如：输入层不可见、检索层语义识别能力不足）。
*   **统计严谨性**：引入了高标准的统计校正和置信区间分析，在 LLM 安全论文中较为少见。
*   **行为原型分类**：提出了“易感执行者”、“潜在携带者”、“显式检测者”等 6 种智能体行为原型，有助于理解不同模型的安全特性。

### 8. 不足与局限
*   **攻击多样性有限**：实验仅使用了一种恶意文档模板（合规性备忘录）和一种触发方式，可能无法覆盖所有变体。
*   **分类器强度**：检索层使用的分类器仅基于 60 个样本训练，且 LLM Judge 仅使用了 1.5B 的小模型，这可能低估了更强大的检索层防御的潜力。
*   **应用限制**：`Memory Sandbox` 依赖于智能体能够通过枚举键名来完成任务，对于某些需要频繁、深度调用记忆的复杂应用，可能会产生未被测试到的效用影响。

（完）
