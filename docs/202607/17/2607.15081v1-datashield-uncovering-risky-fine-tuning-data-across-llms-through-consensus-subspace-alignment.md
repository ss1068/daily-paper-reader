---
title: "DataShield: Uncovering Risky Fine-Tuning Data Across LLMs Through Consensus Subspace Alignment"
title_zh: DataShield：通过共识子空间对齐揭示跨大语言模型的风险微调数据
authors: "Zefeng Wu, Weiwei Qi, Jielong Chen, Tianhang Zheng, Di Hong, Chaochao Lu, Liang He, Zhan Qin, Kui Ren"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15081v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 识别削弱安全能力的风险微调样本
tldr: DataShield是一个旨在识别和过滤大模型微调中风险数据的框架。针对现有方法依赖单一模型向量导致迁移性差的问题，该框架通过多个对齐模型的共识子空间对齐技术，提取安全与非安全语义子空间。它能实现样本级过滤和细粒度片段掩码，在显著降低攻击成功率的同时，保持了模型的下游任务性能，且无需针对特定目标模型进行重复计算。
selection_source: fresh_fetch
motivation: 现有的微调数据风险评估方法通常依赖单一模型的特定向量，导致识别效果受限且缺乏跨模型的通用性。
method: 提出DataShield框架，利用多个安全对齐模型的语义谱分解提取共识安全与非安全子空间，通过计算数据与子空间的对齐程度来评估风险。
result: "实验表明，DataShield在样本过滤和片段掩码上分别降低了14.6%和32.3%的攻击成功率，并有效保留了模型的下游实用性。"
conclusion: DataShield提供了一种高效且具有跨模型迁移能力的微调数据安全评估方案，显著增强了大模型在特定领域适配过程中的安全性。
---

## 摘要
在特定领域数据集上对大语言模型（LLMs）进行微调已成为将 LLMs 适配到专门应用的标准范式。然而，最近的研究表明，即使是在良性的特定任务数据上进行微调，也会显著削弱 LLMs 的安全能力。虽然现有的工作在识别导致安全性下降的数据方面取得了进展，但它们通常依赖于在特定模型及其分词器上计算的单个均值向量来表示安全方向，这限制了其风险评估措施的有效性和可迁移性。为了解决这些局限性，我们提出了 DataShield，这是一个数据评估框架，它通过对源自多个安全对齐 LLMs 的联合安全关键语义空间进行共识子空间对齐，来识别风险微调样本和响应片段。在这些空间中，DataShield 利用安全和不安全数据表示的语义谱分解来提取共识安全和不安全子空间。随后，通过测量数据样本或片段与不安全和安全子空间的相对对齐程度来估计其风险，从而实现样本级过滤和细粒度的片段级掩码。与最先进的过滤和掩码基准相比，DataShield 通过样本过滤将攻击成功率（ASR）降低了 14.6%，通过片段掩码降低了 32.3%，同时保留了下游效用并避免了针对特定目标模型的风险计算。

## Abstract
Fine-tuning large language models (LLMs) on domain-specific datasets has become a standard paradigm for adapting LLMs to specialized applications. However, recent work has shown that even fine-tuning on benign task-specific data can substantially weaken the safety capabilities of LLMs. While existing efforts have made progress in identifying data responsible for safety degradation, they usually rely on a single mean vector computed over a specific model with its tokenizer to represent the safety direction, which limits both the effectiveness and transferability of their risk assessment measures. To address these limitations, we propose DataShield, a data assessment framework that identifies risky fine-tuning samples and response segments through consensus subspace alignment over joint safety-critical semantic spaces derived from multiple safety-aligned LLMs. Within these spaces, DataShield extracts consensus safe and unsafe subspaces using semantic spectral decomposition over safe and unsafe data representations. The risk of a data sample or segment is then estimated by measuring its relative alignment with the unsafe and safe subspaces, enabling both sample-level filtering and fine-grained segment-level masking. Compared with state-of-the-art filtering and masking baselines, DataShield reduces ASR by 14.6\% with sample filtering and 32.3\% with segment masking, while preserving downstream utility and avoiding target-model-specific risk computation.

---

## 论文详细总结（自动生成）

这是一份关于论文《DataShield: Uncovering Risky Fine-Tuning Data Across LLMs Through Consensus Subspace Alignment》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）在**微调阶段的安全性退化**问题。研究发现，即使在看似良性的特定任务数据上进行微调，也会无意中削弱模型原有的安全对齐能力，使其更容易响应有害指令。
*   **现有挑战**：目前的风险数据识别方法通常依赖于单一模型的内部信号（如梯度、隐藏状态）或特定的分词器（Tokenizer）。这导致了两个主要问题：
    1.  **迁移性差**：针对模型 A 识别出的风险数据，在模型 B 上可能失效。
    2.  **表示局限**：仅用单一均值向量代表“安全方向”，无法捕捉安全语义在多维子空间中的复杂分布。
*   **核心目标**：开发一种**跨模型通用**、**分词器无关**且**细粒度**的风险数据检测与干预框架。

### 2. 方法论：核心思想与技术细节
DataShield 的核心思想是利用多个已对齐的源模型建立“共识”，通过**共识子空间对齐**来评估数据风险。

*   **关键技术流程**：
    1.  **识别安全关键层**：利用权重扰动法（Weight-Perturbation），观察各层对拒绝倾向的影响，筛选出对安全性最敏感的 Ncrit 个关键层。
    2.  **构建联合语义空间**：从多个源模型（如 Llama, Qwen, Mistral）中提取关键层的隐藏状态，构建紧凑的表示。
    3.  **语义谱分解（Semantic Spectral Decomposition）**：对安全和不安全参考数据的表示进行分解，提取出代表安全和不安全语义的正交基向量，形成**安全/不安全子空间**。
    4.  **风险估计**：计算待评估数据在各源模型子空间中的投影长度（对齐度）。风险得分定义为“不安全子空间对齐度”与“安全子空间对齐度”之差的跨模型平均值。
    5.  **自回归风险解耦（Autoregressive Risk Decoupling）**：针对片段级掩码，为了防止前文的风险信号干扰后文，DataShield 计算每个片段带来的**增量风险**，从而精确定位有害文本段。
*   **干预手段**：
    *   **样本级过滤（DataShield-Sp）**：直接剔除高风险训练样本。
    *   **片段级掩码（DataShield-Sm）**：保留样本，但在计算 SFT 损失时掩盖高风险文本片段的 Token，实现分词器无关的细粒度干预。

### 3. 实验设计
*   **源模型（Source Models）**：Llama3-8B-Instruct, Qwen2.5-7B-Instruct, Mistral-7B-Instruct-v0.3。
*   **目标模型（Target Models）**：Phi3-medium, Qwen3-4B, Gemma2-27B, Gemma3-12B（用于验证跨架构迁移性）。
*   **数据集**：
    *   **微调数据**：Alpaca, Dolly（通用）；CodeAlpaca, MathInstruct（领域特定）。
    *   **安全评估**：HEx-PHI, HarmBench。
    *   **效用评估**：SLIMORCA（通用指令遵循能力）。
*   **对比方法（Baselines）**：
    *   样本级：SEAL, Bi-Anchor, LARF, SOT。
    *   Token/片段级：TOSS, Random Masking。

### 4. 资源与算力
*   **硬件环境**：使用了 4 张 **NVIDIA RTX PRO 6000 (96GB VRAM)** GPU。
*   **效率表现**：在处理约 1.4 万条 Dolly 数据时，DataShield-Sp 耗时约 **64 分钟**，峰值内存约 47.9 GB；DataShield-Sm 耗时约 **118 分钟**。
*   **优势**：相比需要计算目标模型梯度或 Logits 的基准方法（如 SEAL 或 TOSS），DataShield 显著降低了预处理的时间和内存成本，因为它仅需源模型的推理表示。

### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **跨架构迁移实验**：在 4 个完全未见过的目标模型上验证了效果。
*   **性能对比**：证明了即使 DataShield 不接触目标模型信息，其效果也优于那些使用了目标模型信号的基准方法。
*   **消融实验**：涵盖了子空间维度 $d$、源模型数量 $K$、风险解耦机制的有效性、不同子空间构建策略的对比。
*   **鲁棒性测试**：使用了 GPT-4o 和 Gemini-3.1-Pro 两种不同的模型作为安全评判员（Judge），确保评估结果客观。
*   **公平性**：所有方法均在相同的干预预算（如 20% 过滤率）下进行对比。

### 6. 主要结论与发现
*   **显著提升安全性**：DataShield-Sm 在多个目标模型上平均降低了 **32.3%** 的攻击成功率（ASR），DataShield-Sp 降低了 **14.6%**。
*   **效用保持**：在大幅提升安全性的同时，下游任务的实用性（SLM 胜率）仅有极微小的下降（约 0.4%-1.0%）。
*   **共识的力量**：实验显示，随着源模型数量增加（从 1 到 3），风险评估的迁移能力显著增强，模型间的共识能有效抵消单一模型的偏见。
*   **子空间优于单向量**：使用多维子空间捕捉安全语义比使用单一均值向量（Mean-gap）更有效。

### 7. 优点与亮点
*   **跨模型迁移性**：这是该工作最大的亮点。处理后的数据集可以作为“安全增强版”直接用于任何新模型的微调，无需重新计算。
*   **分词器无关性**：通过原始文本片段（Raw-text segments）进行风险定位，解决了 Token 级方法在不同分词器间难以迁移的痛点。
*   **计算高效**：无需目标模型的梯度或 Logits，适合大规模数据集的快速预处理。
*   **风险解耦机制**：创新的增量风险计算方法，解决了自回归模型中风险信号“长尾传播”的问题。

### 8. 不足与局限
*   **源模型依赖**：DataShield 的效果高度依赖于所选源模型的安全对齐质量。如果所有源模型在某一特定领域（如极冷门的编程语言）都缺乏安全认知，该框架可能失效。
*   **领域覆盖**：目前的实验主要集中在通用有害指令上，对于法律、医疗等专业领域的特定合规性风险（如隐私泄露、错误医疗建议）尚未进行深入验证。
*   **多语言支持**：论文主要在英文数据集上进行测试，跨语言的子空间对齐效果有待进一步探索。

（完）
