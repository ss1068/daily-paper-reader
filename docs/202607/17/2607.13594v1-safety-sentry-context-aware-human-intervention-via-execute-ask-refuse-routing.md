---
title: "SAFETY SENTRY: Context-Aware Human Intervention via EXECUTE-ASK-REFUSE Routing"
title_zh: SAFETY SENTRY：通过“执行-询问-拒绝”路由实现上下文感知的真人干预
authors: "Tianyu Chen, Chujia Hu, Wenjie Wang"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13594v1"
tags: ["query:jb-def"]
score: 8.0
evidence: LLM智能体的安全防护模型
tldr: 本研究针对LLM智能体在调用工具时可能产生的误操作风险，提出了Safety Sentry框架。该框架将传统的二元安全分类改进为三路路由决策（执行、询问、拒绝），通过结合上下文评估动作的合理性。Safety Sentry是一个轻量级模型，支持在不重新训练的情况下通过调整阈值适应不同风险偏好，有效减少了不必要的干扰，同时提升了安全召回率和整体准确性。
selection_source: fresh_fetch
motivation: 传统的二元安全防护模型无法区分动作本身的危害性与上下文的适用性，导致频繁的无效中断并降低了智能体的自主性。
method: 提出一种基于实例的三路路由机制（EXECUTE-ASK-REFUSE），并实现了一个仅需单次解码调用的轻量级防护模型Safety Sentry。
result: Safety Sentry在整体准确率和安全召回率上均优于现有的开源及闭源基准模型，并能同时控制双向错误率。
conclusion: 通过引入上下文感知的询问机制，Safety Sentry在保障智能体操作安全的同时，显著提升了系统的实用性和部署灵活性。
---

## 摘要
大语言模型（LLM）智能体通过工具调用在现实世界环境中执行操作，而单次错误的判断可能导致不可逆转的损害。标准的防护措施是使用防护模型将每个提议的操作标记为安全或不安全，但这种二元视角混淆了两个截然不同的决策：操作本身是否有害，以及在用户上下文语境下该操作是否合适。此外，它在操作类别的粒度上运行，而非针对具体实例，这会产生常规性的中断，从而削弱自主性，并导致用户习惯性地忽略那些最重要的警报。我们将该问题重新定义为针对每个实例的“执行、询问、拒绝”三路路由决策，并利用 Safety Sentry 予以实现。Safety Sentry 是一种轻量级防护模型，其推理过程可简化为单次解码调用。单个解码时阈值使得固定的检查点无需重新训练即可重新部署到具有不同风险承受能力的场景中。Safety Sentry 在整体准确率和安全相关召回率方面优于广泛的开源权重模型和前沿闭源基准模型，同时能够同步控制双向错误率。

## Abstract
LLM agents act on real-world environments through tool calls, and a single misjudged action can cause irreversible harm. The standard safeguard is a guard model that labels each proposed action as safe or unsafe, but this binary view conflates two distinct decisions: whether the action is harmful in itself, and whether it is appropriate given the user's context. It also operates at the granularity of action categories rather than individual instances, producing routine interruptions that erode autonomy and train users to wave through the most consequential alerts. We reframe the problem as a per-instance three-way routing decision over {EXECUTE, ASK, REFUSE} and instantiate it with Safety Sentry, a lightweight guard model whose inference reduces to a single decoding call. A single decoding-time threshold lets one fixed checkpoint be re-positioned across deployments of differing risk tolerance without retraining. Safety Sentry outperforms a broad set of open-weight and frontier closed-source baselines on overall accuracy and safety-related recall, while controlling both directional error rates simultaneously.

---

## 论文详细总结（自动生成）

以下是对论文《SAFETY SENTRY: Context-Aware Human Intervention via EXECUTE–ASK–REFUSE Routing》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：LLM 智能体在调用工具时，单次误判可能导致不可逆的损害。现有的安全防护模型多采用“安全/不安全”的二元分类，这存在两个弊端：一是混淆了“动作本身有害”与“动作在特定上下文中不当”的区别；二是粗粒度的拦截会导致频繁的中断，削弱智能体的自主性，并使用户产生“警报疲劳”。
*   **核心目标**：将安全防护从二元分类转变为三路路由决策：**执行（EXECUTE）**、**询问（ASK）**、**拒绝（REFUSE）**，旨在平衡智能体的自主性、安全性和人类监管。

### 2. 方法论
*   **核心思想**：引入一个轻量级的防护模型 Safety Sentry，根据用户意图、历史轨迹和当前动作，动态决定是直接执行、请求人类确认还是直接拒绝。
*   **关键技术细节**：
    *   **三路路由决策**：明确区分“明确安全”、“意图模糊/高影响（需询问）”和“结构性违规（需拒绝）”。
    *   **触发分类法（Taxonomy）**：定义了 14 种触发模式（如：参数缺失、不可逆高影响动作、数据外泄等）来指导标注和决策。
    *   **上下文感知标注**：构建了一个包含“用户画像记忆（Persona Memory）”的语料库，使模型能根据用户的风险偏好（如谨慎型、放任型）做出不同的路由选择。
    *   **阈值路由机制**：在推理时引入解码阈值 $\tau$，通过调整 $\tau$ 即可在不重新训练的情况下，改变模型在“自主性”与“监管”之间的平衡点。
*   **算法流程**：智能体提议动作 $\rightarrow$ 防护模型接收快照（任务、记忆、历史、动作） $\rightarrow$ 模型输出推理过程（Think）及决策 Token $\rightarrow$ 根据概率和阈值执行路由。

### 3. 实验设计
*   **数据集与场景**：
    *   构建了包含 9,203 条步骤级记录的语料库。
    *   **真实服务环境**：在 9 个自托管的生产级开源系统（如 Gitea, Rocket.Chat, ERPNext 等）中运行，确保标签反映真实的动作后果。
    *   **基准测试**：包含分布内（ID）测试集和完全隔离的分布外（OOD）测试集（Mailu 邮件服务）。
*   **对比方法**：
    *   **小型开源模型**：Llama-3.2-3B, Gemma-3-4B 等。
    *   **规模控制组**：Qwen3.5 系列（0.8B 到 27B）。
    *   **前沿闭源/开源模型**：GPT-5.5, Claude-Opus-4.7, Gemini-3.1-Pro, DeepSeek-V4 等。

### 4. 资源与算力
*   **算力设备**：文中提到在推理延迟测试中使用了单张 **NVIDIA A40 GPU**。
*   **训练细节**：使用 LoRA 进行微调，训练了 3 个 Epoch（约 1,635 个优化步骤），序列长度为 6,144。
*   **推理开销**：Safety Sentry（4B 参数）每步推理约耗时 1.57 秒，约为智能体主模型耗时的 42%，处于可接受的交互范围内。

### 5. 实验数量与充分性
*   **实验规模**：进行了主结果对比、阈值 Pareto 前沿分析、个性化路由分析、OOD 泛化测试、框架与骨干模型鲁棒性测试以及推理延迟分析。
*   **充分性评价**：实验设计非常全面。通过 OOD 测试验证了迁移能力，通过消融实验验证了画像记忆的影响，并对比了从 0.8B 到前沿大模型的广泛基准，证明了该方法的有效性不仅源于模型规模，更源于决策框架的改进。

### 6. 主要结论与发现
*   **性能领先**：4B 规模的 Safety Sentry 在准确率（91.02%）和安全召回率上显著优于包括 GPT-5.5 和 Claude-4.7 在内的所有基准模型。
*   **询问决策的必要性**：小型开源模型在“询问（ASK）”决策上表现极差，倾向于直接执行，而 Safety Sentry 能有效识别模糊地带。
*   **规模并非万能**：单纯增加参数规模对三路路由性能的提升存在边际效应，专门的步骤级监督训练更为关键。
*   **可调控性**：通过单一阈值即可灵活调整风险偏好，且模型对上游智能体框架（如 LangChain）具有良好的鲁棒性。

### 7. 优点
*   **范式创新**：打破了二元安全观，引入“询问”路径，更符合人类与 AI 协作的真实逻辑。
*   **部署灵活**：支持部署后通过阈值动态调整，无需针对不同风险等级的业务重复训练。
*   **数据质量高**：基于真实 API 环境生成的轨迹和经过人工审计的标签，保证了监督信号的可靠性。
*   **轻量高效**：以较小的参数量实现了超越顶级闭源模型的安全防护性能。

### 8. 不足与局限
*   **领域局限性**：主要针对企业办公和计算机操作场景，在开放式写作或创意工具领域的泛化效果尚未验证。
*   **阈值依赖**：虽然阈值可调，但最佳阈值的设定仍依赖于部署者的经验和对特定业务风险的判断。
*   **推理延迟**：虽然比主模型快，但作为串行在每一步工具调用前的环节，1.5 秒的额外延迟在极高性能要求的场景下仍是负担。

（完）
