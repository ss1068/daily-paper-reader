---
title: "Isolation as a First-Class Principle for LLM-Agent System Safety: Concepts, Taxonomy, Challenges and Future Directions"
title_zh: 隔离作为大语言模型智能体系统安全的核心原则：概念、分类、挑战与未来方向
authors: "Huihao Jing, Wenbin Hu, Shaojin Chen, Haochen Shi, Sirui Zhang, Hanyu Yang, Changxuan Fan, Zhongwei Xie, Hongyu Luo, Wun Yu Chan, Wei Fan, Haoran Li, Yangqiu Song"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12406v1"
tags: ["query:llm"]
score: 8.0
evidence: 针对提示注入和工具滥用的安全原则
tldr: 本文针对大语言模型（LLM）智能体系统安全问题，提出将“隔离”作为核心原则。研究指出当前安全研究碎片化，难以解释提示注入、工具滥用等故障的共性原因。文章通过构建包含用户-智能体、智能体-工具等五个边界的分类法，系统分析了隔离失效的传播路径，并探讨了构建“原生隔离”智能体系统的挑战与未来方向。
selection_source: fresh_fetch
motivation: 现有的LLM智能体安全研究碎片化，缺乏统一框架来解释不同攻击类型背后的结构性原因及传播机制。
method: 提出以“隔离”为核心原则，并建立了一个包含用户、工具、执行、多智能体和环境五个关键边界的分类法。
result: 系统地识别了隔离失效的起始点及其跨边界传播路径，并总结了针对不同接口的防御策略与挑战。
conclusion: 强调了“原生隔离”是未来智能体系统设计的关键，并为构建更安全的智能体架构指明了研究议程。
---

## 摘要
LLM 智能体作为系统“大脑”的能力，从根本上将分析范围扩展到了独立模型之外。因此，安全不再仅仅关乎输入输出的内容对齐，还涉及系统行为和现实世界的执行结果。然而，目前的文献在攻击类型、应用和基准测试方面较为零散。这使得难以解释为什么诸如提示词注入、工具误用和记忆投毒等故障往往具有相同的结构性原因，以及它们如何在智能体工作流中传播。在本综述中，我们将隔离视为大语言模型智能体系统安全的核心原则。所谓隔离，是指用户输入、工具访问、执行通道、智能体间通信以及源自环境的上下文之间的分离。我们采用以边界为中心的分类法来组织文献，涵盖五个边界：用户-智能体、智能体-工具、智能体-执行、智能体-智能体以及系统-环境。这一视角有助于识别隔离失效最初发生的地点、破坏如何跨越边界传播，以及在每个接口处哪些防御措施最为相关。我们还总结了跨边界故障路径，讨论了开放性挑战，并为未来智能体系统中的“构建即隔离”（isolation-by-construction）勾勒了研究议程。

## Abstract
The capability of LLM agents to function as the ``brain'' of a system fundamentally expands the scope of analysis beyond a standalone model. Consequently, safety is no longer only about input--output content alignment. It also concerns system behavior and real-world execution outcomes. However, the current literature is fragmented across attack types, applications, and benchmarks. This makes it hard to explain why failures such as prompt injection, tool misuse, and memory poisoning often share the same structural cause, and how they spread through an agent workflow. In this survey, we treat isolation as a first-class principle for LLM-agent system safety. By isolation, we refer to the separation of user inputs, tool access, execution channels, inter-agent communication, and environment-originated context. We organize the literature with a boundary-centric taxonomy of five boundaries: user-agent, agent-tool, agent-execution, agent-agent, and system-environment. This view helps identify where the loss of isolation first occurs, how compromise propagates across boundaries, and which defenses are most relevant at each interface. We also summarize cross-boundary failure paths, discuss open challenges, and outline a research agenda for isolation-by-construction in future agent systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Isolation as a First-Class Principle for LLM-Agent System Safety: Concepts, Taxonomy, Challenges and Future Directions》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）从单纯的文本生成器演变为具备规划、工具调用和环境交互能力的“智能体（Agent）”，其安全风险已从单纯的“内容对齐”扩展到了“系统行为”和“现实执行结果”。
*   **核心问题**：当前关于智能体安全的研究（如提示注入、工具误用、记忆投毒等）非常碎片化，缺乏统一的框架来解释这些故障的共性原因。
*   **整体含义**：论文提出将**“隔离（Isolation）”**作为智能体系统安全的第一准则。作者认为，大多数安全失效的本质是系统边界的模糊，导致非特权数据（如用户输入或环境内容）意外获得了特权控制权。

### 2. 论文提出的方法论
论文构建了一个**以边界为中心（Boundary-centric）的分类法**，将智能体系统划分为五个关键的隔离边界：
*   **用户-智能体边界 (User-Agent)**：防止用户输入从“数据”转变为“指令”（如直接提示注入、多轮对话诱导）。
*   **智能体-工具边界 (Agent-Tool)**：确保工具调用受限且类型化，防止工具输出被误认为高权限指令（如工具误用、协议操纵）。
*   **智能体-执行边界 (Agent-Execution)**：在规划转变为实际行动（如运行代码、操作GUI、物理动作）时进行中介和限制，防止不可逆的副作用。
*   **智能体-智能体边界 (Agent-Agent)**：管理多智能体间的通信，防止恶意指令在群体中传播或产生级联失效。
*   **系统-环境边界 (System-Environment)**：处理外部世界（网页、检索到的文档、RAG系统）的输入，防止环境中的恶意内容（间接提示注入）污染智能体状态。

**核心思想**：倡导**“原生隔离（Isolation-by-construction）”**，即通过架构设计（如权限分离、沙箱化、可追溯性）而非仅仅依靠提示词指令来确保安全。

### 3. 实验设计
由于本文是一篇**综述论文（Survey）**，其“实验设计”体现为对现有文献、攻击手段和基准测试（Benchmarks）的系统性梳理与分类映射：
*   **涵盖场景**：包括网络黑客攻击、自动化编程、Web浏览、具身智能（机器人控制）以及多智能体协作。
*   **引用的基准测试 (Benchmarks)**：
    *   **用户侧**：HarmBench, JailbreakBench, SG-Bench。
    *   **工具/执行侧**：ToolSafe, SafeArena, ST-WebAgentBench, CIBER。
    *   **具身/环境侧**：VIVA, HASARD, VPI-Bench。
*   **对比分析**：文章对比了不同边界下的攻击路径（如从环境注入到工具调用的跨边界传播）以及相应的防御策略（如从模型微调到运行时监控）。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的算力消耗（如GPU型号或训练时长）。
*   **原因**：作为综述性文章，其核心贡献在于理论框架的构建和文献综述，而非训练新模型。文中讨论的防御措施（如安全分类器、语义平滑等）在原论文中通常涉及中等规模的推理算力。

### 5. 实验数量与充分性
*   **文献覆盖量**：论文引用了大量（数百篇）截至2026年的最新研究，涵盖了从早期的提示注入到最新的模型上下文协议（MCP）安全。
*   **充分性评价**：
    *   **广度**：非常充分，涵盖了单智能体到多智能体、数字环境到物理世界的全维度。
    *   **深度**：通过对“跨边界失效路径”的分析，深入探讨了攻击是如何在复杂工作流中传播的，具有很强的逻辑严密性。
    *   **客观性**：分类法基于系统架构的客观边界，而非主观的攻击意图，因此较为中立。

### 6. 论文的主要结论与发现
*   **结构性原因**：提示注入、工具滥用和记忆投毒等看似不同的问题，其共同根源在于**隔离失效**。
*   **失效传播**：安全失效具有传染性，一个边界的失守（如环境边界）往往会通过通信或工具调用传播到执行边界，造成实际损害。
*   **防御转向**：单纯依靠模型对齐（Alignment）是不够的，必须转向**系统级的结构化防御**，如显式的权限管理、内存分区和因果溯源。
*   **未来议程**：需要开发支持“原生隔离”的智能体架构，强调可观察性、可恢复性和最小特权原则。

### 7. 优点
*   **统一视角**：将碎片化的安全研究整合进一个清晰的五边界框架，极大地降低了理解智能体安全复杂性的门槛。
*   **前瞻性**：关注到了最新的工业趋势（如Anthropic的Managed Agents、MCP协议），具有很强的时效性。
*   **路径分析**：不仅识别了风险点，还总结了风险如何在系统内“流动”，这对设计防御系统极具指导意义。

### 8. 不足与局限
*   **动态性挑战**：智能体领域发展极快，新的交互模式（如更复杂的跨模态交互）可能会产生该分类法尚未完全覆盖的新边界。
*   **分类重叠**：虽然作者提出了“主边界”分类原则，但在实际复杂系统中，某些论文或攻击可能同时涉及多个边界，分类界限在极端情况下可能存在主观判断。
*   **防御成本**：文中提到的许多“原生隔离”措施（如多层中介、实时监控）可能会带来显著的推理延迟和系统复杂性，论文对性能与安全的权衡讨论较少。

（完）
