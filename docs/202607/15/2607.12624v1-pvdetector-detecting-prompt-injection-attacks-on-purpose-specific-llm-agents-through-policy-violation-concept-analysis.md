---
title: "PVDetector: Detecting Prompt Injection Attacks on Purpose-Specific LLM Agents through Policy-Violation Concept Analysis"
title_zh: PVDetector：通过违规策略概念分析检测特定用途 LLM 智能体上的提示注入攻击
authors: "Junhui Wang, Hangtao Zhang, Zhirun Zheng, Li Zeng, Jiejun Xiao, Xi Luo, Lihua Yin, Saiqin Long"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12624v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 通过隐藏激活空间分析检测大模型智能体的提示注入攻击
tldr: 针对特定用途的LLM智能体易受提示注入（PI）攻击的问题，本文提出PVDetector。该方法通过分析LLM内部隐藏层的激活空间，发现模型在面对违规请求时会产生特定的“策略违规”（PV）概念。PVDetector利用离线提取的PV概念，在推理阶段通过衡量隐藏状态的对齐程度来检测攻击，实现了极低的漏报率且无需额外训练，为智能体安全提供了新思路。
selection_source: fresh_fetch
motivation: 现有的提示注入检测方法主要依赖输入输出模式分析，难以有效应对针对特定用途智能体角色限制的复杂攻击。
method: 提出一种无需训练的框架，通过对比违规与合规提示对提取PV概念，并在推理时检测隐藏状态与该概念的对齐度。
result: "在多个模型和数据集上的实验表明，PVDetector的漏报率低于1%，且辅助开销极小，性能优于现有最先进方法。"
conclusion: 揭示了LLM内部对策略违规具有内在感知能力，并证明了利用隐藏空间特征防御提示注入攻击的高效性与可靠性。
---

## 摘要
大语言模型 (LLM) 越来越多地被部署为特定用途的智能体，以处理客户服务和代码生成等特定领域任务。这些智能体不仅需要遵守通用的安全护栏，还需要遵守针对其指定角色定制的特定用途限制。这些额外的限制扩大了攻击面，特别是针对提示注入 (PI) 攻击。为了防御此类攻击，现有的检测方法主要依赖于分析输入输出模式，但效果有限。为了解决这一局限性，我们转向分析隐藏激活空间，并发现当 LLM 收到超出其指定用途的请求时，其内部会保留潜在的违规策略 (PV) 概念。特别地，PV 概念捕捉了用户查询与预定义限制之间冲突的语义，隐式地反映了 LLM 识别违规策略的内在意识。基于这一发现，我们提出了 PVDetector，这是一个无需训练的框架，它通过测量隐藏状态与 PV 概念的对齐程度，在 LLM 推理过程中检测 PI 攻击。这些 PV 概念是从违规和合规提示的对比对中离线推导出来的。在多个 LLM 和数据集上的实验表明，PVDetector 以极小的辅助开销实现了小于 1% 的漏报率，持续优于现有最先进的方法。我们的代码可在 https://github.com/Claresigle/PVDetector 获取。

## Abstract
Large language models (LLMs) are increasingly deployed as purpose-specific agents to handle domain-specific tasks such as customer service and code generation. These agents are expected to comply with not only generic safety guardrails but also purpose-specific restrictions tailored to their designated roles. Such additional restrictions enlarge the attack surface, particularly to prompt injection (PI) attacks. To defend against such attacks, existing detection methods primarily rely on analyzing input-output patterns, yet yield limited effectiveness. To address this limitation, we turn to analyzing the hidden activation space and discover that LLMs inherently retain latent policy-violation (PV) concepts when prompted with requests beyond their designated purpose. Particularly, PV concepts capture the semantics of conflicts between user queries and predefined restrictions, implicitly reflecting LLMs' intrinsic awareness of recognizing policy violations. Building on this insight, we propose PVDetector, a training-free framework that detects PI attacks during LLM inference by measuring hidden-state alignment with PV concepts, which are derived offline from the contrastive pairs of policy-violating and policy-compliant prompts. Experiments across multiple LLMs and datasets show that PVDetector achieves <1\% false negative rate with minimal auxiliary overhead, consistently outperforming state-of-the-art methods. Our code is available at https://github.com/Claresigle/PVDetector .

---

## 论文详细总结（自动生成）

这是一份关于论文《PVDetector: Detecting Prompt Injection Attacks on Purpose-Specific LLM Agents through Policy-Violation Concept Analysis》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大语言模型（LLM）常被部署为执行特定任务的智能体（如食谱大师、旅行规划师）。这些智能体除了通用的安全准则外，还需遵守“特定用途限制策略”（PSR）。
*   **核心问题**：提示注入（PI）攻击可以轻易绕过这些角色限制，诱导智能体执行无关任务（如让食谱机器人写代码）。
*   **研究动机**：现有的防御方法主要依赖于分析输入输出的表面模式，效果有限且开销大。作者发现，即使 LLM 在行为上被攻击诱导输出了错误内容，其内部隐藏层依然存在一种“抵抗意识”，即能够识别出输入与策略之间的冲突。

### 2. 方法论：PVDetector 框架
PVDetector 是一个**无需训练（Training-free）**的检测框架，核心思想是提取并利用 LLM 内部的“策略违规（PV）概念”。

*   **核心思想**：利用线性表示假设（Linear Representation Hypothesis），将“违规”这一抽象概念映射为激活空间中的特定方向（PV 向量）。
*   **关键技术流程**：
    1.  **离线阶段 - 概念提取**：
        *   **对比对构建**：构建少量（约30对）针对特定角色的对比提示词，如“正样本（违规：如何回收）”与“负样本（合规：如何做蛋糕）”。
        *   **PV 向量计算**：将对比对输入模型，提取每一层最后一个 Token 的隐藏状态，利用**均值差（Difference-in-Means）**技术计算正负样本间的表示差异，得到每一层的 PV 向量。
        *   **关键层选择**：通过计算违规与合规样本在各层上的得分差异，筛选出对违规语义最敏感的“关键层”。
    2.  **在线阶段 - 实时检测**：
        *   **PVS 得分计算**：在推理时，将待测输入的隐藏状态投影到预先提取的 PV 向量上，计算“策略违规强度（PVS）”得分。
        *   **聚合与判定**：使用梯形法则聚合关键层的得分，若总分超过预设阈值，则判定为 PI 攻击。

### 3. 实验设计
*   **智能体场景**：构建了 RecipeMaster（食谱）、TripPlanner（旅行）、CareerCounselor（职业咨询）、LineMonitor（工业监控）四种智能体。
*   **后端模型**：Llama-3.1 (8B), Qwen-2.5 (7B/14B)，以及多模态模型 Qwen2.5-VL 和 Phi-3.5-vision。
*   **数据集**：使用 Alpaca 和 MMLU 作为违规查询的来源，通过强大模型（如 Qwen3-MAX）生成合规查询。
*   **对比方法（Baselines）**：
    *   基于困惑度的 PPL。
    *   训练好的检测器：ProtectAI, Prompt-Guard-2, DataSentinel。
    *   基于变异/鲁棒性的 JailGuard。
    *   基于注意力机制的 AttentionTracker。
*   **攻击类型**：Ignore（忽略指令）、Fake Completion（伪造完成）、Combined（组合攻击）、GCG（梯度优化攻击）及其变体。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Xeon Gold 5318Y CPU**、**256GB RAM** 和 **3 台 NVIDIA A800 GPU** 的机器上完成。
*   **训练时长**：由于该方法是**无需训练**的，其主要开销在于离线提取 PV 向量（仅需少量样本的一次前向传播）和在线推理时的投影计算。
*   **检测效率**：平均每条查询的检测时间仅为 **0.1059 秒**，是所有基准测试中开销最低的方法之一。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流 LLM、2 种 VLM、4 种智能体角色、5 种攻击手段以及 2 个大型公共数据集。
*   **充分性分析**：
    *   **消融实验**：验证了关键层选择、对比对来源、PV 向量转移性等因素的影响。
    *   **多模态扩展**：验证了在视觉提示注入场景下的有效性。
    *   **自适应攻击测试**：针对已知防御机制的强力自适应攻击（Adaptive Attack）进行了鲁棒性测试。
    *   **客观性**：通过固定 FPR（假阳性率）在 1% 以内来公平对比各方法的 FNR（漏报率），实验设计较为严谨。

### 6. 主要结论与发现
*   **内在感知能力**：LLM 内部隐藏层对策略违规具有天然的感知能力，这种信号比输出端的文本表现更可靠。
*   **检测性能**：PVDetector 在所有测试模型和攻击下实现了 **< 1% 的漏报率（FNR）**，显著优于现有的训练模型和启发式方法。
*   **层级分布**：实验发现，策略违规的语义特征在模型的**中后期层**表现最为明显。
*   **多模态通用性**：该方法可以无缝扩展到视觉语言模型（VLM），检测嵌入在图像中的恶意指令。

### 7. 优点
*   **高效且实时**：无需微调模型，无需额外的辅助检测模型，推理延迟极低。
*   **极高准确率**：在特定用途智能体的 PI 攻击检测上达到了近乎完美的召回率。
*   **可解释性**：通过将 PV 向量投影回词表空间，可以观察到“sorry”、“outside”、“conflict”等与违规相关的语义 Token，证明了向量的有效性。
*   **抗自适应性**：即使攻击者知道防御原理并试图最小化 PVS 得分，也会导致攻击成功率大幅下降。

### 8. 不足与局限
*   **白盒依赖**：该方法需要访问模型的内部隐藏状态（激活值），因此不适用于仅提供 API 访问的黑盒模型（如 GPT-4 闭源版本）。
*   **阈值敏感性**：虽然实验中表现稳定，但在实际部署中，针对不同角色设定最优阈值可能需要一定的校准工作。
*   **静态策略限制**：PV 向量是基于预定义的 PSR 提取的，如果智能体的策略发生动态大幅变更，可能需要重新提取 PV 向量。

（完）
