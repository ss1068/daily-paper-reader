---
title: "HyperSafe: Inference-Time Safety Recovery for Fine-Tuned Language Models"
title_zh: HyperSafe：微调语言模型的推理阶段安全性恢复
authors: "Aznaur Aliev, Carlos Hinojosa, Abdelrahman Eldesokey, Bang An, Bernard Ghanem, Yibo Yang"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11475v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 推理阶段安全恢复与安全修复
tldr: "针对大语言模型在微调过程中容易丧失安全对齐的问题，本文提出HyperSafe框架。该框架通过超网络为每个微调后的模型生成特定的安全侧网络（SSN），利用层级激活指纹捕捉模型内部表示的变化。SSN在推理时与原模型并行，实现提示词级别的安全分类，无需修改原模型权重或重新训练。实验表明，HyperSafe能将有害响应率降至1%以下，且几乎不影响下游任务性能。"
selection_source: fresh_fetch
motivation: 微调过程常导致模型安全对齐失效，而现有防御手段存在成本高、损害性能或缺乏针对性等局限。
method: 利用超网络根据微调模型的层级激活指纹生成特定的安全侧网络（SSN），在推理阶段对提示词进行安全分类并路由。
result: "在Qwen2和LLaMA-3上的实验显示，有害响应率从最高31%降至1%以下，且下游任务准确率损失平均小于1%。"
conclusion: HyperSafe提供了一种非侵入式、高效且模型特定的安全恢复方案，在保障安全性的同时保持了微调模型的任务性能。
---

## 摘要
大语言模型中的安全性对齐在微调过程中可能非常脆弱，即使是良性的任务适配也可能增加有害的合规性。现有的防御措施主要遵循两个方向：要么通过重新训练或权重修改在微调期间或之后进行干预，这可能成本高昂并损害任务性能；要么使用与模型无关的安全性分类器，这可能会遗漏特定于给定微调检查点的故障。这些局限性促使人们寻求一种事后的、特定于模型的且非侵入性的安全性恢复方法。为了满足这些要求，我们提出了 HyperSafe，这是一个通过为每个微调检查点生成特定于模型的安全侧网络（SSN）来恢复安全性行为的框架。HyperSafe 使用逐层激活指纹来捕捉微调如何改变模型的内部表示。通过一小组给定的校准提示词，超网络在单次前向传播中将这些指纹映射到 SSN 的参数。生成的 SSN 与冻结的微调模型并行运行，并执行提示词级别的安全性分类：有害提示词被路由至拒绝回答，而安全提示词则由原始微调模型回答。因此，HyperSafe 不需要梯度更新，部署时不需要安全性数据，也不需要修改已部署模型的权重。我们在 Qwen2-7B 和 LLaMA-3-8B 两个模型系列上，通过多个安全性基准测试对 HyperSafe 进行了评估。HyperSafe 将每个留出检查点的有害响应率从 19-31% 降低到 1% 以下，同时保持下游任务的平均准确率在微调基准的 1% 以内。代码可在 https://github.com/nokronim/project-safety-remedy 获取。

## Abstract
Safety alignment in large language models can be fragile under fine-tuning, as even benign task adaptation may increase harmful compliance. Existing defenses mainly follow two directions: they either intervene during or after fine-tuning through retraining or weight modification, which can be costly and may hurt task performance, or they use model-agnostic safety classifiers, which may miss failures specific to a given fine-tuned checkpoint. These limitations motivate a post hoc, model-specific, and non-invasive approach to safety restoration. To meet these requirements, we propose HyperSafe, a framework that restores safety behavior by generating a model-specific Safe Side Network (SSN) for each fine-tuned checkpoint. HyperSafe uses layer-wise activation fingerprints to capture how fine-tuning changes the model's inner representations. With a small set of given calibration prompts, the hypernetwork maps these fingerprints to the parameters of the \ssn{} in a single forward pass. The generated \ssn{} runs alongside the frozen fine-tuned model and performs prompt-level safety classification: harmful prompts are routed to refusal, while safe prompts are answered by the original fine-tuned model. Thus, HyperSafe requires no gradient updates, no safety data at deployment time, and no modification to the deployed model weights. We evaluate HyperSafe on two model families, Qwen2-7B and LLaMA-3-8B, across multiple safety benchmarks. HyperSafe reduces harmful response rates from 19-31% to below 1% on every held-out checkpoint, while keeping downstream task accuracy within 1% of the fine-tuned baseline on average. Code is available at https://github.com/nokronim/project-safety-remedy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **HyperSafe** 的框架，旨在解决大语言模型（LLM）在微调过程中安全性对齐（Safety Alignment）容易遭到破坏的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：即使使用完全良性的数据进行微调，原本经过安全对齐的模型也会表现出“安全性退化”，变得容易响应有害请求。
*   **研究动机**：现有的防御手段存在明显缺陷：
    *   **微调阶段干预**（如增加安全约束）：会产生安全性与实用性的权衡，且无法应用于已发布的模型。
    *   **权重修改**（如模型合并、投影）：直接修改模型参数可能导致下游任务性能下降（遗忘）。
    *   **外部过滤器**（如 Llama Guard）：与具体模型无关，可能无法捕捉特定微调版本产生的独特安全漏洞。
*   **目标**：开发一种**事后（Post-hoc）、特定于模型（Model-specific）且非侵入性（Non-invasive）**的安全性恢复方法。

### 2. 方法论
HyperSafe 的核心思想是利用**超网络（Hypernetwork）**为每个微调后的模型生成一个定制化的**安全侧网络（Safe Side Network, SSN）**。
*   **激活指纹提取**：使用一小组（约50个）校准提示词运行微调模型，提取各层的平均激活状态作为该模型的“行为指纹”。
*   **超网络架构**：
    *   **方向-幅值分解**：将激活向量分解为单位方向和对数幅值，以捕捉表示空间的变化性质和强度。
    *   **层级因子化生成**：超网络根据特定层的激活和全局上下文，预测 SSN 对应层的权重。
*   **安全侧网络 (SSN)**：
    *   基于 **Ladder Side-Tuning** 架构，是一个轻量级 Transformer（参数量仅为原模型的 3-4%）。
    *   它通过梯形连接（Ladder Connections）读取冻结主干模型的中间状态。
*   **推理流程**：SSN 对输入提示词进行评分。若判定为有害，则路由至拒绝响应；若判定为安全，则直接由原始微调模型生成答案。该过程不修改原模型权重。

### 3. 实验设计
*   **模型与领域**：在 **Qwen2-7B** 和 **LLaMA-3-8B** 上进行验证，涵盖 22 个数据集（包括 QA、数学、代码、指令遵循等）。
*   **安全性基准 (Benchmarks)**：
    *   **BeaverTails**：用于训练和评估。
    *   **AdvBench** 和 **HEx-PHI**：作为完全零样本（Zero-shot）的安全性测试集。
*   **对比方法**：
    *   **Base Model**（原始对齐模型）。
    *   **LoRA Model**（微调后未防御模型）。
    *   **OneShot SFT**（权重修补方法）。
    *   **Llama Guard 4 (12B)**（最新的外部安全分类器）。

### 4. 资源与算力
*   **硬件**：实验使用了 **1 张 NVIDIA A100 80GB GPU**。
*   **效率**：
    *   生成 SSN 权重仅需单次前向传播，耗时**小于 1 分钟**。
    *   推理时，安全提示词会绕过 SSN 的解码过程，因此不增加生成延迟。
    *   SSN 参数量约为主干模型的 3-4%。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，使用了 22 个不同的微调领域，并采用了 **3 折留出交叉验证（Holdout Splits）**，确保超网络在未见过的微调任务上具有泛化能力。
*   **消融实验**：详细探讨了损失函数组件（功能性损失 vs 权重重建损失）、训练数据多样性、校准集大小（Ncal）以及对全参数微调（Full FT）的泛化性。
*   **评价**：实验设计非常充分且客观，通过跨架构、跨领域和跨安全基准的测试，验证了方案的鲁棒性。

### 6. 主要结论与发现
*   **安全性恢复**：HyperSafe 将有害响应率从微调后的 19-31% 降低到 **1% 以下**。
*   **性能保持**：下游任务的准确率损失平均控制在 **1% 以内**，显著优于 OneShot 等权重修改方法。
*   **零样本泛化**：即使超网络仅在部分领域训练，也能有效识别未见领域微调模型的安全风险。
*   **优于外部模型**：在 AdvBench 和 HEx-PHI 上，HyperSafe 的表现优于参数量更大的 Llama Guard 4。

### 7. 亮点
*   **非侵入性**：无需重新训练微调模型，也不改变其权重，完美保留了微调获得的下游能力。
*   **模型感知**：通过激活指纹，SSN 能够“感知”特定微调过程对模型内部表示的影响，比通用分类器更精准。
*   **高效部署**：一次生成，永久使用，且推理开销极低。

### 8. 不足与局限
*   **架构绑定**：超网络是针对特定主干模型（如 LLaMA-3-8B）训练的，如果更换模型架构（如从 LLaMA 换到 Mistral），需要重新训练超网络。
*   **校准依赖**：虽然 50 个提示词不多，但生成指纹仍依赖于校准集的质量。
*   **防御边界**：论文主要针对微调导致的安全性退化，对于极其复杂的对抗性越狱（Jailbreak）攻击的防御上限仍有待进一步探索。

（完）
