---
title: "MJ: Multi-turn LLM Jailbreaking via Decomposed Credit Assignment"
title_zh: MJ：通过分解信用分配实现多轮大语言模型越狱
authors: "Junyoung Park, Namgyu Park, Sechan Lee, Yoon-Chan Jhi, Jihoon Cho, Sangdon Park"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11070v1"
tags: ["query:llm"]
score: 8.0
evidence: 多轮LLM越狱和自动化红队测试
tldr: 本研究针对多轮大语言模型越狱攻击中的信用分配难题，提出了DC-GRPO框架。该框架通过分解信用分配，为每一轮对话分配独立的学习信号，结合即时和未来信用，有效解决了传统方法中单一轨迹评分导致的信用误分配问题。实验表明，该方法在多个基准测试中显著优于现有SOTA方法，为自动化红队测试提供了更精准、高效的评估手段。
selection_source: fresh_fetch
motivation: 现有的多轮越狱攻击学习信号过于粗糙，难以准确识别对话中不同轮次对最终越狱结果的贡献。
method: 提出DC-GRPO框架，通过结合即时和未来信用，为Group Relative Policy Optimization引入轮次级的信用分配机制。
result: "在多个模型和基准上，该方法的变体分别达到了98.26%和97.88%的平均攻击成功率，大幅超越SEMA和TROJail等现有SOTA方法。"
conclusion: 实验证明轮次级的组相对信用分配是提升多轮越狱攻击性能的关键，而非特定的权重规则。
---

## 摘要
现代大语言模型（LLMs）在交互式多轮对话设置下运行，这使得多轮越狱成为一种现实的威胁模型，也是自动化红队测试中的一个重要场景。学习多轮越狱攻击者的一个核心挑战是信用分配（credit assignment）：不同的轮次对最终结果的贡献不同，但现有的学习信号往往过于粗糙，无法识别各轮次的具体贡献。我们提出了分解信用 GRPO（DC-GRPO），这是一个用于多轮越狱学习中群体相对策略优化（Group Relative Policy Optimization）的统一轮次级信用分配框架。DC-GRPO 通过结合即时信用和未来信用，为每一轮分配独立的群体相对学习信号，从而避免了因在整个对话中广播单一轨迹级评分而导致的信用分配错误。我们使用静态和动态权重规则实例化了该框架，这些规则在平衡两种信用来源的方式上有所不同，但共享相同的轮次级结构。在多个受害者 LLM 和基准测试中，动态权重和静态权重变体分别实现了 98.26% 和 97.88% 的平均 ASR5@3 评分，显著优于现有最先进的方法，包括 SEMA (86.58%) 和 TROJail (86.23%)。它们持续强劲的性能表明，核心的实证收益源于轮次级的群体相对信用分配，而非特定的权重规则。警告：本文包含有害内容示例。

## Abstract
Modern large language models (LLMs) operate in interactive multi-turn settings, making multi-turn jailbreaking a realistic threat model and an important setting for automated red teaming. A core challenge in learning multi-turn jailbreak attackers is credit assignment: different turns contribute differently to the final outcome, yet existing learning signals are often too coarse to identify their individual contributions. We propose decomposed credit GRPO (DC-GRPO), a unified turn-level credit assignment framework for Group Relative Policy Optimization in multi-turn jailbreak learning. DC-GRPO assigns a separate group-relative learning signal to each turn by combining immediate and future credit, avoiding the credit misassignment induced by broadcasting a single trajectory-level score across the dialogue. We instantiate this framework with static and dynamic weighting rules that differ in how the two credit sources are balanced while sharing the same turn-level structure. Across multiple victim LLMs and benchmarks, the dynamic- and static-weighted variants achieve average ASR5@3 scores of 98.26% and 97.88%, respectively, substantially outperforming the state-of-the-art methods, including SEMA (86.58%) and TROJail (86.23%). Their consistently strong performance indicates that the central empirical benefit comes from turn-level group-relative credit assignment rather than a particular weighting rule. Warning: This paper contains examples of harmful content.

---

## 论文详细总结（自动生成）

这是一份关于论文《MJ: Multi-turn LLM Jailbreaking via Decomposed Credit Assignment》的结构化深入总结：

---

### 1. 论文的核心问题与整体含义
*   **研究背景**：现代大语言模型（LLM）多在交互式多轮对话中使用。虽然安全对齐（如RLHF）提升了模型的抗攻击能力，但多轮对话为攻击者提供了通过策略性塑造上下文、伪装意图来绕过防御的空间。
*   **核心问题**：在训练多轮越狱攻击者时，存在**信用分配（Credit Assignment）**难题。现有的强化学习方法（如标准GRPO）通常将整个对话轨迹的最终得分广播到所有轮次，这导致无法区分哪些轮次产生了实质性进展，哪些轮次只是在做铺垫，甚至会将后期获得的奖励错误地归功于前期的动作。
*   **研究动机**：开发一种能够精准识别每一轮对话贡献的训练框架，以提升自动化红队测试（Automated Red Teaming）的效率和攻击成功率。

### 2. 论文提出的方法论：DC-GRPO
*   **核心思想**：提出**分解信用 GRPO（Decomposed Credit GRPO, DC-GRPO）**。该框架不再使用单一的轨迹级优势函数，而是将每一轮的信用分解为“即时信用”和“未来信用”，并进行轮次级的组相对归一化。
*   **关键技术细节**：
    *   **即时信用（Immediate Credit, $I_{i,t}$）**：基于当前轮次的奖励 $r_{i,t}$ 与组内平均值的偏差。
    *   **未来信用（Future Credit, $F_{i,t}$）**：基于从下一轮开始的剩余回报 $R_{i,t+1}$ 与组内平均值的偏差。
    *   **优势函数构造**：$\hat{A}_{i,t} = w_I I_{i,t} + w_F F_{i,t}$。
*   **两种实例化规则**：
    1.  **静态权重（SW）**：使用固定系数 $\alpha$ 来平衡两者，即 $I_{i,t} + \alpha F_{i,t}$。
    2.  **动态权重（DW）**：根据组统计数据（标准差）自动调整权重，无需额外超参数。
*   **算法流程**：在每一轮迭代中，采样 $G$ 条攻击轨迹，由 Judge 模型对每一轮打分，计算轮次级的组相对优势，最后通过剪切代理目标函数更新攻击者策略。

### 3. 实验设计
*   **数据集/基准（Benchmark）**：
    *   **训练集**：AdvBench。
    *   **测试集**：HarmBench（200个提示）、StrongREJECT†（288个提示）、JailbreakBench†（55个提示）。
*   **受害者模型（Victim LLMs）**：Llama-3.1-8B-Instruct, Qwen2.5-7B-Instruct, Gemma-2-9B-IT, Mistral-7B-Instruct-v0.3，以及更强大的 GPT-OSS-20B。
*   **对比方法**：
    *   **单轮攻击**：ArtPrompt, ReNeLLM, AutoDAN-Turbo, Jailbreak-R1。
    *   **多轮攻击（训练型）**：SEMA (SOTA), TROJail (SOTA), Siren, MTSA。
    *   **多轮攻击（非训练型）**：Crescendo, X-Teaming, CoA, ActorAttack, GOAT。

### 4. 资源与算力
*   **训练时长**：论文详细记录了训练 260 步（约 2 个 epoch）的时间。MJ DW 变体总耗时约 **10 小时 47 分钟**，平均每步耗时 **149.32 秒**。
*   **算力设备**：论文**未明确说明**具体的 GPU 型号和数量。但从其处理 Qwen3-4B 模型和 10 个并行 Rollout 的规模来看，通常需要 4-8 张 A100 或 H100 级别的显卡。
*   **组件耗时**：Rollout 生成占总时间的约 60%，模型更新占约 29%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 个主流开源受害者模型和 1 个闭源级别的高强度受害者模型。
    *   在 3 个权威越狱基准上进行了交叉验证。
    *   使用了 3 种不同的 Judge 模型（HarmBench, WildGuard, Qwen3Guard）来确保评估的客观性。
*   **消融实验**：非常充分。包括了权重系数 $\alpha$ 的敏感度分析、轮次级 vs 轨迹级信用的对比、不同攻击者骨干模型（Qwen2.5 vs Qwen3）的对比、以及多样性增强（DPP）的效果分析。
*   **公平性**：通过在相同的推理管道下重新运行 SEMA 等基准方法，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能卓越**：DC-GRPO 的动态权重（MJ DW）和静态权重（MJ SW）变体在 ASR 5@3 指标上分别达到 **98.26%** 和 **97.88%**，大幅超越了之前的 SOTA 方法 SEMA (86.58%)。
*   **信用分配是关键**：实验证明，将信用分配细化到轮次级是性能提升的核心原因，而具体的权重平衡规则（动态或静态）对结果的影响相对较小。
*   **高效性**：相比于 SEMA，MJ 在达到相同或更高成功率的同时，通常需要更少的对话轮次（平均 1.67 轮即可成功）。
*   **强泛化性**：在 Llama 上训练的攻击者可以完美迁移到 Qwen、Gemma、Mistral 甚至 GPT-4.1-mini 等模型上。

### 7. 优点（亮点）
*   **理论简洁且有效**：通过简单的代数分解解决了复杂的强化学习信用分配问题，无需额外的 Critic 网络，降低了训练复杂度。
*   **即插即用**：该框架可以轻松集成到现有的 GRPO 流程中。
*   **实证表现极强**：在多个受害者模型上几乎达到了“全满分”的越狱成功率，刷新了多轮越狱的记录。

### 8. 不足与局限
*   **交互长度限制**：实验主要集中在 5 轮以内的对话，对于需要极长篇幅铺垫的复杂攻击场景（如长程社会工程学）尚未验证。
*   **Judge 依赖性**：尽管使用了多个 Judge，但攻击者的表现仍受限于 Judge 模型的判断准确性，存在“奖励黑客（Reward Hacking）”的潜在风险。
*   **安全伦理风险**：由于该方法极大地提升了越狱攻击的效率，如果被恶意利用，将对现有 LLM 的安全防御造成严重威胁（作者已在文中声明仅用于受控的安全研究）。

---
（完）
