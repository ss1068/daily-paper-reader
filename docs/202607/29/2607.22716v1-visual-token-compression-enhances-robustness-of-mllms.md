---
title: Visual Token Compression Enhances Robustness of MLLMs
title_zh: 视觉 Token 压缩增强多模态大语言模型（MLLMs）的鲁棒性
authors: "Shishen Gu, Jiequan Cui, Wenbo Hu, Zenglin Shi, Zhenzhen Hu, Richang Hong"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.22716v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过视觉令牌剪枝缓解多模态大模型的越狱攻击
tldr: "本研究首次发现视觉标记剪枝能显著提升多模态大语言模型（MLLM）的鲁棒性。针对视觉与语言模态对齐不完全导致的分布外（OOD）输入问题，作者提出了一种通过测量视觉标记与语言特征空间距离来识别并迭代剪枝OOD标记的方法。该方法在防御越狱攻击和缓解幻觉方面表现出色，在七个基准测试中，越狱防御能力平均提升13.29%，同时降低了推理成本并保持了通用性能。"
selection_source: fresh_fetch
motivation: 视觉与语言模态的不完全对齐产生的分布外视觉标记会引入越狱攻击和幻觉等安全漏洞。
method: 通过计算视觉标记与语言特征空间的距离来识别分布外标记，并在特定层进行迭代剪枝以减少干扰。
result: "实验显示该方法在越狱防御上平均提升了13.29%，同时在缓解幻觉和保持通用任务性能方面表现优异。"
conclusion: 视觉标记压缩不仅能优化推理效率，还是增强多模态大模型安全性和鲁棒性的关键技术路径。
---

## 摘要
在本文中，我们首次证明了视觉 token 剪枝能够增强多模态大语言模型（MLLMs）的鲁棒性，缓解诸如越狱攻击和幻觉等漏洞。鉴于视觉和语言模态无法完美对齐，未对齐的视觉 token 可能会充当分布外（OOD）输入，导致不可预测的输出并引入潜在漏洞。基于这一见解，我们旨在通过在鲁棒剪枝层减少 OOD 视觉 token 来增强模型抵御越狱和幻觉的鲁棒性，同时降低推理成本作为额外收益。具体而言，我们测量每个视觉 token 与语言特征空间之间的距离。然后，将距离较大的视觉 token 识别为 OOD token，并对其进行迭代剪枝。为了证明我们方法的有效性，我们在七个不同的流行基准测试上进行了评估。值得注意的是，我们的方法在防御越狱攻击方面平均提升了 13.29%，在缓解幻觉方面持续取得具有竞争力的表现，并在 MME 等通用数据集上保持了强劲的结果。

## Abstract
In this paper, we show for the first time that visual token pruning enhances the robustness of Multimodal Large Language Models (MLLMs), mitigating vulnerabilities such as jailbreak attacks and hallucinations. Given that vision and language modalities cannot be perfectly aligned, the misaligned visual tokens might act as out-of-distribution (OOD) inputs, leading to unpredictable outputs and introducing potential vulnerabilities. Building on this insight, we aim to enhance model robustness against jailbreaks and hallucinations by reducing OOD visual tokens at robust-pruning layers, while also reducing inference cost as a side benefit. Specifically, we measure the distance between each visual token and the language feature space. Then, visual tokens with large distances are identified as OOD tokens, which can be iteratively pruned. To demonstrate the effectiveness of our method, we evaluate it on seven diverse popular benchmarks. Notably, our method yields an average improvement of 13.29\% in defending jailbreak attacks, consistently achieves competitive performance in mitigating hallucinations, and maintains strong results on general datasets like MME.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Token Compression Enhances Robustness of MLLMs》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文探讨了多模态大语言模型（MLLM）在面对**越狱攻击（Jailbreak Attacks）**和**视觉幻觉（Hallucinations）**时的脆弱性。
*   **核心假设**：作者认为，视觉与语言模态之间存在天然的表示失配。在推理过程中，那些未对齐的视觉 Token 会被语言模型（LLM）骨干网络视为**分布外（OOD）信号**。这些 OOD 噪声会干扰模型的内部表示，导致其安全机制失效（被越狱）或生成事实错误的内容（幻觉）。
*   **研究目标**：通过识别并剪枝（Pruning）这些具有干扰性的 OOD 视觉 Token，在提升模型推理效率的同时，增强其安全性和鲁棒性。

### 2. 论文提出的方法论：OOD-VTP
作者提出了一种名为 **OOD-VTP（Out-of-distribution Visual Token Pruning）** 的训练无关（Training-free）方法。
*   **核心思想**：量化视觉 Token 与文本特征空间之间的距离，将距离最远的 Token 视为 OOD 噪声并予以剔除。
*   **关键技术细节**：
    1.  **视觉 Token 距离定义**：利用 LLM 内部的注意力机制，计算每个视觉 Token 与文本 Token 序列（特别是最后 $p\%$ 的文本 Token，因为它们包含更丰富的语义对齐信息）之间的负平均注意力分数。分数越低（距离越大），说明该视觉 Token 与当前文本上下文的关联度越弱。
    2.  **鲁棒剪枝层（Robust-Pruning Layers）**：研究发现，并非在所有层剪枝都有效。模型中间存在特定的“鲁棒剪枝层”，在这些层进行剪枝对提升安全性效果最明显。
    3.  **算法流程**：在指定的鲁棒剪枝层，根据计算出的距离对视觉 Token 进行排序，剔除距离最大的 $r\%$ Token，剩余 Token 继续参与后续层的计算。

### 3. 实验设计
*   **模型对象**：主流开源 MLLM，包括 **LLaVA-OneVision-7B** 和 **Qwen-2.5-VL-7B**。
*   **数据集与场景**：
    *   **越狱防御**：SafeBench、HADES、MM-SafetyBench（涵盖结构化攻击和图像扰动攻击）。
    *   **幻觉缓解**：COCO2014（CHAIR 指标）、HallusionBench、POPE。
    *   **通用能力**：MME（感知与认知）、OCRBench（文字识别）。
*   **对比方法**：
    *   **视觉压缩方法**：FastV、Average Pooling、PDrop、VTW。
    *   **安全防御方法**：AdaShield（基于 Prompt 的防御）。
    *   **幻觉缓解方法**：VCD（视觉对比解码）。

### 4. 资源与算力
*   **硬件环境**：实验主要在单张 **NVIDIA A6000 GPU (48GB)** 上完成。
*   **训练时长**：由于该方法是**训练无关（Training-free）**的，因此不需要训练时长。实验主要集中在推理测试和超参数（如剪枝层、剪枝比例）的网格搜索上。

### 5. 实验数量与充分性
*   **实验规模**：论文在 **7 个主流基准测试**上进行了广泛评估，涵盖了安全、幻觉和通用性能三个维度。
*   **消融实验**：作者进行了详尽的消融研究，包括：
    *   剪枝策略对比（剪最大距离 vs. 剪最小距离 vs. 随机剪枝）。
    *   距离度量函数对比（Min, Max, Mean）。
    *   文本特征空间占比 $p\%$ 的影响。
    *   不同剪枝层对结果的敏感性分析。
*   **充分性评价**：实验设计非常充分且客观。作者不仅展示了该方法在防御上的优越性，还诚实地报告了在 OCR 等精细任务上可能出现的轻微性能下降，并提供了与现有防御方法的组合实验（如 AdaShield + OOD-VTP），证明了其互补性。

### 6. 论文的主要结论与发现
*   **压缩即防御**：首次证明了视觉 Token 剪枝不仅能加速，还能显著提升 MLLM 的鲁棒性。
*   **性能提升**：在越狱防御上，相比基准模型平均提升了 **13.29%** 的防御成功率（HOR/RAR 指标）；在幻觉缓解上也取得了竞争力的结果。
*   **层敏感性**：发现模型中间层（如 Qwen-2.5-VL 的第 13-17 层）是进行鲁棒剪枝的最佳位置。
*   **效率收益**：在提升安全性的同时，显著降低了 TFlops 并提升了每秒处理 Token 数（TPS）。

### 7. 优点：亮点与创新
*   **视角独特**：将视觉压缩与模型安全性联系起来，打破了“压缩必然损害性能”的固有认知。
*   **即插即用**：无需重新训练或微调模型，部署成本极低，具有很强的工程实用价值。
*   **双重收益**：同时解决了计算效率和模型可靠性（越狱+幻觉）两大痛点。
*   **理论自洽**：通过 OOD 信号干扰的视角解释了 MLLM 的脆弱性，并通过实验验证了该解释的合理性。

### 8. 不足与局限
*   **精细任务受损**：高比例的 Token 剪枝会导致图像细节丢失，从而在 OCRBench 等依赖微小视觉特征的任务上出现性能下滑。
*   **超参数依赖**：虽然论文提出了统一的固定剪枝层建议，但针对不同架构的模型，寻找最优“鲁棒剪枝层”仍需要一定的验证集进行搜索。
*   **攻击模型覆盖**：虽然测试了多种攻击，但对于针对剪枝机制专门设计的自适应攻击（Adaptive Attacks），其防御上限仍有待进一步探索（尽管文中已做了初步的白盒自适应攻击测试）。

（完）
