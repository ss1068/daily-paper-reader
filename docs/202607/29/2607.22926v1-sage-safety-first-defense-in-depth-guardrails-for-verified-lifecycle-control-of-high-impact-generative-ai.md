---
title: "SAGE: Safety-First Defense-in-Depth Guardrails for Verified Lifecycle Control of High-Impact Generative AI"
title_zh: SAGE：用于高影响力生成式人工智能验证生命周期控制的安全优先纵深防御护栏
authors: Mahdi Eslamimehr
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22926v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 生成式人工智能的安全优先深度防御护栏
tldr: SAGE是一个针对高影响生成式AI的深度防御架构，旨在解决灾难性滥用风险。它采用安全优先、授权分离的设计，结合了多种检测器、风险包络和审计链，并通过形式化验证确保生命周期控制。研究通过对GPT、Claude和Gemini等主流模型的实证分析，评估了有害合规性，为AI安全治理提供了可验证的生命周期控制框架。
selection_source: fresh_fetch
motivation: 针对高影响生成式AI可能导致的灾难性滥用，现有的提示词过滤已不足够，需要更严谨的生命周期控制。
method: 提出SAGE架构，通过安全优先的授权分离机制、多样化检测器和形式化验证方法，构建多层防御体系。
result: 对主流大模型的实证研究显示有害合规率较低，且通过形式化抽象验证了授权分离和生命周期不变性。
conclusion: SAGE为高影响AI提供了可验证的安全护栏，强调在追求效用前必须先满足安全约束。
---

## 摘要
高影响力生成式人工智能使得灾难性滥用成为一个生命周期控制问题，而不仅仅是提示词过滤问题。SAGE 是一种安全优先、授权分离的架构，在该架构中，在考虑效用、延迟或商业目标之前，可信的灾难性赋能风险会约束准入性。它结合了签名发布清单、多样化检测器、鲁棒风险包络、最小风险默认设置、输出检查、三值监控、受保护的审计链、遏制和回滚。形式化结果确立了安全优先级、保守检测器边界、单调发布门控、防篡改记录和授权切断；两个 PRISM 抽象在显式假设下验证了授权分离和生命周期不变性。一项冻结的、厂商对称的研究向四个 GPT、四个 Claude 和两个 Gemini 快照各发送了 84 个案例：840 次调用产生了 794 个目标响应、46 个提供商错误以及涵盖 375 个响应的 449 次成功判定。八个快照具有完整的判定领域覆盖。有害合规性估计值较低；差异主要源于良性效用和安全重定向。支持了涉及 Claude、Gemini 或 GPT-5 快照与 GPT-5 mini 和 GPT-5 nano 快照的七项多重性调整对比，而 Claude 或 Gemini 快照与 GPT-5 或 GPT-5.5 之间的任何测试对比在校正后均未显著。观察到的有害合规范围是基于每个提示词生成一次、且无工具、检索、历史记录或人工裁决的保守、受协议限制的视角；它并非操作辅助的上限。一项预注册的扩展研究规定了如何使用锁定拆分、重复采样、多轮和沙箱工具条件以及领域专家评分来测试更广泛的最佳-最差差距。

## Abstract
High-impact generative AI makes catastrophic misuse a lifecycle-control problem, not merely a prompt-filtering problem. SAGE is a safety-first, authorization-separated architecture in which credible catastrophic-enablement risk constrains admissibility before utility, latency, or commercial objectives are considered. It combines signed release manifests, diverse detectors, robust risk envelopes, least-risk defaults, output checking, three-valued monitoring, protected audit chains, containment, and rollback. Formal results establish safety priority, conservative detector bounds, monotone release gating, tamper-evident records, and an authorization cut; two PRISM abstractions verify authorization separation and lifecycle invariants under explicit assumptions.   A frozen, vendor-symmetric study sent 84 cases to each of four GPT, four Claude, and two Gemini snapshots: 840 calls yielded 794 target responses, 46 provider errors, and 449 successful judgments covering 375 responses. Eight snapshots had complete judged domain coverage. Harmful-compliance estimates were low; variation arose mainly from benign utility and safe redirection. Seven multiplicity-adjusted contrasts involving Claude, Gemini, or GPT-5 snapshots and the GPT-5 mini and GPT-5 nano snapshots were supported, while no tested contrast between the Claude or Gemini snapshots and GPT-5 or GPT-5.5 survived correction.   The observed harmful-compliance range is a conservative, protocol-bound view from one generation per prompt with no tools, retrieval, history, or human adjudication; it is not an upper bound on operational assistance. A preregistered extension specifies how to test a wider best-worst gap using a locked split, repeated sampling, multi-turn and sandboxed-tool conditions, and domain-expert scoring.

---

## 论文详细总结（自动生成）

这是一份关于论文《SAGE: Safety-First Defense-in-Depth Guardrails for Verified Lifecycle Control of High-Impact Generative AI》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：高影响力的生成式人工智能（AI）可能被用于协助恐怖主义、网络攻击、生化武器制造或大规模欺诈。
*   **核心问题**：作者认为，防止灾难性滥用不应仅仅是一个“提示词过滤”（Prompt-filtering）问题，而是一个“生命周期控制”（Lifecycle-control）问题。
*   **研究动机**：现有的护栏机制往往缺乏形式化验证，且容易在效用和安全之间妥协。SAGE 旨在建立一个“安全优先”的架构，在考虑商业效用或延迟之前，必须先满足严格的灾难性风险约束。

### 2. 方法论
SAGE 提出了一套安全优先、授权分离的纵深防御架构，其核心技术细节包括：
*   **授权分离（Authorization Separation）**：将保守的响应控制（如拒绝回答）与特殊的组织行动（如人工审查或外部披露）分离。分类器本身不具备触发外部行动的权限。
*   **签名发布清单（Signed Release Manifest）**：将能力测试、风险阈值、访问控制和回滚计划绑定到一个加密签名的清单中，确保只有经过验证的模型配置才能上线。
*   **鲁棒风险包络（Robust Risk Envelopes）**：利用不确定性感知控制，在检测器存在误差或分布偏移（Distribution Shift）时，通过闭式解计算鲁棒风险，确保系统选择最安全的合法响应。
*   **三值监控与形式化验证**：
    *   使用**三值逻辑监控器**（真、假、未知）对运行轨迹进行实时验证。
    *   利用 **PRISM 概率模型检查器**对“授权切断”（防止未授权披露）和“生命周期不变性”（如模型过期自动下线）进行形式化验证。
*   **词典式策略引擎**：在决策时，首先过滤掉不满足安全阈值的动作，仅在安全候选集中优化效用和延迟。

### 3. 实验设计
*   **实验场景**：一项“厂商对称”（Vendor-symmetric）的冻结协议研究，通过统一的 API 接口测试多个主流模型快照。
*   **对比对象**：
    *   **OpenAI**: GPT-5, GPT-5.5, GPT-5 mini, GPT-5 nano。
    *   **Anthropic**: Claude Haiku 4.5, Claude Sonnet 4.6, Claude Opus 4.6, Claude Opus 4.7。
    *   **Google**: Gemini 3 Flash preview, Gemini 3.1 Pro preview。
*   **数据集/Benchmark**：共 84 个案例，涵盖 6 个风险领域（网络、生化、暴力、欺诈、虚假信息、骚扰），包括直接有害请求、良性敏感请求（XSTest 风格）、双重用途请求及鲁棒性包装（Wrapper）测试。
*   **评测方式**：采用“LLM-as-a-judge”模式，由非目标家族的模型（如用 GPT-5.5 评测 Claude）进行盲测评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练模型所消耗的具体 GPU 数量或时长。
*   **环境配置**：详细列出了推理和分析环境，包括 CPython 3.12.3、PRISM 4.10.1、OpenJDK 21 以及相关的 Python 科学计算库（NumPy, SciPy 等）。实验是在 Linux x86-64 环境下运行的。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 840 次 API 调用，获得了 794 个目标响应。评测层尝试了 1,588 次判定，最终 449 次成功判定覆盖了 375 个响应。
*   **充分性评价**：
    *   **客观性**：采用了预注册协议、随机化顺序、跨家族判定和多重性校正（Holm 调整），实验设计较为严谨。
    *   **局限性**：样本量（每个模型 84 个案例）相对较小。由于自动判定器在某些领域（如生化风险）的失败，导致部分模型（如 Claude Opus）的数据不完整，无法计算总分。

### 6. 主要结论与发现
*   **有害合规性普遍较低**：在测试的协议下，所有模型的有害合规性（HCS）估计值均处于较低水平（0.0000 到 0.0333 之间）。
*   **性能差异来源**：模型之间的“平衡护栏得分”（BGS）差异主要源于**良性效用**和**安全重定向质量**，而非安全防御的彻底失败。
*   **模型表现**：Claude Haiku 4.5 和 Claude Sonnet 4.6 在保持零有害合规的同时，提供了极高的良性效用。GPT-5 系列在重定向质量上略逊于领先者。
*   **验证结果**：形式化验证证明了 SAGE 架构在结构上能够防止未授权的组件触发敏感信息披露。

### 7. 优点
*   **架构严谨**：将 AI 安全从简单的“内容过滤”提升到了“形式化验证的生命周期控制”高度。
*   **防御纵深**：结合了发布前审计、运行时检测、输出检查、实时监控和自动回滚。
*   **数学支撑**：为检测器误差边界和风险控制提供了闭式公式和单调性证明。

### 8. 不足与局限
*   **评测依赖**：高度依赖自动评测器（LLM-as-a-judge），尽管做了校准，但仍存在 LLM 固有的偏见风险。
*   **场景覆盖**：实验仅限于单轮对话，未涉及多轮诱导、工具调用、检索增强（RAG）或沙箱环境下的实际能力测试。
*   **保守估计**：观察到的有害合规范围是基于单次生成的保守视角，不能代表攻击者在多次尝试或使用复杂工具时的风险上限。

（完）
