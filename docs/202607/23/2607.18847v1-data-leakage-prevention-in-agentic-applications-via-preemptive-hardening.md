---
title: Data Leakage Prevention in Agentic Applications via Preemptive Hardening
title_zh: 通过预先加固防止智能体应用中的数据泄露
authors: "Akansha Shukla, Emily Bellov, Parth Atulbhai Gandhi, Yuval Elovici, Asaf Shabtai"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18847v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对提示注入的预部署加固与验证流水线
tldr: 本研究针对智能体应用中因提示词注入和指令边界失效导致的数据泄露与工具滥用问题，提出了一种预部署加固流水线。该方案通过扫描提示词模板和工具接口识别风险模式，自动生成包括模式收紧和边界清理在内的最小侵入性补丁，并结合对抗性攻击进行安全性验证。实验证明，该方法在不干扰正常功能的情况下，能完全消除基础攻击泄露，并显著降低复杂操纵下的风险。
selection_source: fresh_fetch
motivation: 智能体系统在集成外部工具时，提示词注入和指令边界模糊导致的数据泄露与工具滥用风险在复杂异构环境中难以统一管控。
method: 开发了一种预部署流水线，通过静态分析识别风险并生成加固补丁，随后利用对抗性注入攻击和良性输入验证加固后的系统安全性与功能性。
result: "在多个真实应用和基准测试中，该流水线消除了100%的基础泄露风险，并在压力操纵条件下减少了91%的泄露，且无需持续的运行时策略强制。"
conclusion: 通过在部署前进行预防性加固，可以有效提升智能体应用的安全性，同时保持其原有功能的完整性。
---

## 摘要
智能体系统将大语言模型（LLM）驱动的规划与外部工具接口相结合，这使得通过指令/数据边界失效和提示词注入攻击导致的数据泄露和工具滥用成为可能。在跨越多个代码库和异构智能体的工作流中，一致地执行所需的控制措施尤其具有挑战性。为了解决多智能体系统中的这一挑战，我们提出了一种用于智能体应用扫描、加固和验证的部署前流水线。该流水线分析提示词模板、工具接口和工具调用代码，以识别导致泄露的模式并生成可执行的补丁。随后，通过对抗性提示词注入攻击和良性输入变体对加固后的应用进行验证，以确保缓解措施不会破坏预期行为。在加固阶段，高风险工具被优先处理，并应用微创缓解措施，包括模式收紧、边界清理、基于白名单的工具门控和最小权限检查。在验证阶段，流水线自动生成模拟越狱、指令覆盖和针对工具操纵的攻击输入，以及良性任务变体，以确认修复后加固应用的功能得以保留。我们在五个真实世界的智能体应用以及 AgentDojo 基准测试上评估了该流水线。在所有应用中，所提出的流水线识别出了反复出现的导致泄露的模式，并生成了可以在不破坏预期应用行为的情况下集成的补丁。结果表明，应用代码的修改在遭受基础越狱和指令覆盖攻击时消除了泄露，实现了 100% 的泄露减少，并在压力诱导操纵条件下减少了 91% 的泄露，且无需持续的运行时策略执行。

## Abstract
Agentic systems integrate LLM driven planning with interfaces to external tools, making data leakage and tool misuse feasible via instruction/data boundary failures and prompt injection attacks. Enforcing required controls consistently is particularly challenging in workflows spanning many codebases and heterogeneous agents. To address this challenge in multi agentic systems, we present a pre-deployment pipeline for scanning, hardening, and validation of agentic applications. The pipeline analyzes prompt templates, tool interfaces, and tool-invocation code to identify leakage-enabling patterns and generate actionable patches. The hardened application is then validated through adversarial prompt injection attacks and benign input variations ensuring that mitigations do not disrupt intended behavior. In the hardening stage, high-risk tools are prioritized, and minimally invasive mitigations are applied, including schema tightening, boundary sanitization, allowlist-based tool gating, and least-privilege checks. In the validation stage, the pipeline automatically generates attack inputs that mimic jailbreaks, instruction overrides, and tool-targeted manipulation, along with benign task variants, to confirm that the functionality of the hardened application is preserved after remediation. We evaluated the pipeline on five real-world agentic applications, as well as on the AgentDojo benchmark. Across all applications, the proposed pipeline identified recurring leakage-enabling patterns and generated patches that can be integrated without disrupting the intended application behavior. The resulting modifications of application code were shown to eliminate leaks when targeted by basic jailbreak and instruction-override attacks, achieving a 100% reduction in leakage, and reduce leaks by 91% under conditions of stress-induced manipulation, without the need of continuous runtime policy enforcement.

---

## 论文详细总结（自动生成）

### 论文结构化总结：通过预先加固防止智能体应用中的数据泄露

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）驱动的智能体（Agents）从原型走向生产环境，它们被赋予了调用外部工具（如邮件、数据库、文件系统）和处理私有数据的权限。这种能力的提升显著扩大了攻击面：**提示注入（Prompt Injection）**和**指令/数据边界失效**不再仅仅影响模型回复，还可能导致敏感数据泄露、凭据暴露或工具滥用。
现有的防御手段（如运行时监控或简单的提示词加固）往往是响应式的或脆弱的。本文认为，智能体系统的安全性应在**部署前（Pre-deployment）**通过工程化手段解决。研究的核心目标是开发一种自动化的流水线，在不修改模型权重或引入高额运行时开销的前提下，通过代码级加固来消除泄露路径。

#### 2. 方法论：核心思想与关键技术
论文提出了一种端到端的预部署扫描、加固与验证流水线，包含三个核心模块：
*   **分析模块（Analyzer）：**
    *   利用静态程序分析（Python AST）和框架解析器（支持 CrewAI 和 LangGraph）构建智能体拓扑图。
    *   结合 LLM 的语义推理，识别从“敏感源”（如内存、系统提示词）到“输出汇点”（如外部 API 调用、用户回复）的**泄露路径（Leakage Path）**。
    *   根据操作严重性、暴露程度和数据敏感性对风险进行分级，生成审计报告。
*   **加固模块（Modifier）：**
    *   **重写提示词：** 使用显式分隔符区分指令与数据，注入非披露约束。
    *   **清理不安全上下文：** 在数据跨越边界前进行摘要或脱敏。
    *   **角色分离与权限限制：** 强制执行最小权限原则，收紧工具调用的 JSON Schema，防止自由格式的载荷攻击。
*   **验证模块（Validator）：**
    *   **迭代反馈循环：** 自动生成对抗性输入（越狱、指令覆盖、压力操纵）和良性任务变体。
    *   **动态确认：** 运行加固后的应用，检查敏感数据是否仍能到达汇点，并确保正常功能（Utility）未受损。

#### 3. 实验设计
*   **实验场景：**
    *   **5个真实应用：** 网络监控助手（O-RAN 场景）、HR 助手、自动化旅行规划器、邮件回复器、候选人招聘应用。
    *   **基准测试：** 使用 **AgentDojo** 标准基准（包含 Workspace, Slack, Banking, Travel 四个套件，共 97 个任务和 629 个安全测试用例）。
*   **对比方法：**
    *   原始未保护的应用（Baseline）。
    *   **FIDES：** 一种基于运行时的信息流控制（IFC）防御方案。
    *   AgentDojo 默认的内置防御机制。
*   **攻击类别：** 直接请求、基础越狱、指令覆盖、压力诱导操纵（Stress-induced）。

#### 4. 资源与算力
*   **软件环境：** Python 3.11，框架涉及 CrewAI 和 LangGraph。
*   **模型使用：** 实验中使用了 **GPT-4o** 和 **Gemini-2.0-Flash** 作为智能体的后端模型及流水线中的推理引擎。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法侧重于代码加固和推理验证而非模型训练，其算力消耗主要集中在 LLM API 的调用上。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 5 个应用进行了详尽的测试，每个应用在加固循环中最多进行 10 次外部迭代，每次迭代包含多轮对抗攻击尝试。
*   **消融实验：** 论文分别移除了分析器、加固器和验证器进行对比，验证了每个组件的必要性。
*   **充分性评价：** 实验设计较为充分，既涵盖了特定领域的深度案例研究，也包含了广度上的标准基准测试（AgentDojo）。通过对比运行时防御方案（FIDES），客观地展示了预加固方案在维持功能性（Utility）方面的优势。

#### 6. 主要结论与发现
*   **显著降低泄露率：** 在 5 个案例应用中，加固后的系统在基础越狱和指令覆盖攻击下实现了 **100% 的泄露消除**；在最难防御的压力操纵攻击下，泄露率降低了 **91%**。
*   **功能保留：** 与 AgentDojo 默认防御相比，该流水线在提升安全性的同时，将任务成功率（Utility）提升了 **35.2%**。
*   **防御权衡：** 实验发现，过于保守的运行时防御（如 FIDES）虽然安全，但往往会导致良性工作流中断或产生幻觉，而预加固方案能更好地平衡安全与实用性。

#### 7. 优点（亮点）
*   **预部署加固：** 填补了智能体安全领域“左移”（Shift-left）防御的空白，减少了对高开销运行时监控的依赖。
*   **框架无关性：** 设计了适配器模式，能够兼容 CrewAI、LangGraph 等多种主流智能体框架。
*   **最小侵入性：** 补丁针对特定泄露路径生成，而非全局限制，最大限度保留了智能体的灵活性。
*   **自动化闭环：** 实现了从“发现风险”到“修复”再到“攻击验证”的自动化迭代。

#### 8. 不足与局限
*   **残余风险：** 压力框架下的提示词（Stress-framed prompts）仍有极少数（约 7.4%）能突破防御，说明语义层面的操纵极难完全消除。
*   **多用户隔离：** 目前主要侧重于数据流向外部汇点的泄露，对于复杂多租户环境下的深度授权和上下文隔离支持尚显不足。
*   **结构化篡改：** 如果攻击者能保持 Schema 合法性地篡改智能体间通信（如修改 URL 但不破坏格式），目前的结构化检查可能被绕过。

（完）
