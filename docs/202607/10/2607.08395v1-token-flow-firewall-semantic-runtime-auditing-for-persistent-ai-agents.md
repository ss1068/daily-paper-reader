---
title: "Token-Flow Firewall: Semantic Runtime Auditing for Persistent AI Agents"
title_zh: 令牌流防火墙：针对持久化 AI 智能体的语义运行时审计
authors: "Puji Wang, Yingchen Zhang, Ruqing Zhang, Jiafeng Guo, Xueqi Cheng"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08395v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对AI智能体的运行时审计与语义流拦截防御
tldr: 针对持久化AI智能体面临的语义攻击风险，本文提出了TokenWall，一种基于令牌流的语义防火墙框架。该框架通过对智能体内部的内存更新、工具调用等自然语言流进行边界感知的语义审计，在执行前拦截风险行为。TokenWall结合了轻量级本地检测与高风险案例的远程仲裁，在显著降低攻击成功率的同时，保持了极高的良性任务通过率和较低的延迟，为持久化智能体提供了实用的运行时安全保障。
selection_source: fresh_fetch
motivation: 持久化AI智能体通过状态和工具交互产生的语义攻击面远大于传统聊天助手，亟需一种能够覆盖全流程的运行时防御机制。
method: 提出TokenWall框架，通过构建源-汇审计记录并结合本地轻量级检查与选择性远程仲裁，对智能体的令牌流进行语义审计。
result: "在CIK-Bench测试中，TokenWall将攻击成功率降至12.5%，同时保持了97.4%的良性任务通过率，且仅增加0.69秒延迟。"
conclusion: TokenWall证明了通过语义运行时遏制可以在不牺牲实用性的前提下，为持久化AI智能体实现高效的安全防护。
---

## 摘要
持久化 AI 智能体将大语言模型（LLM）从单轮交互扩展为长寿命的软件系统。与传统的聊天助手不同，这些智能体中的不安全内容可以通过持久化状态、可重用技能和工具介导的交互进行传播，从而产生显著更大的语义攻击面。我们观察到，此类智能体中大多数关键安全交互都是通过自然语言令牌流传输的，包括内存更新、工具参数、检索到的文件以及组件间通信。这一观察结果促成了一种新的安全表述：不安全行为可以在到达特权运行时汇点（sinks）之前，作为风险语义流被拦截。基于这一见解，我们提出了 TokenWall，这是一个在智能体令牌流上充当语义防火墙的运行时防御框架。TokenWall 对这些流执行边界感知的语义审计，构建结构化的源-汇（source-sink）审计记录，在执行前应用轻量级本地检查，并选择性地将模糊的高风险案例升级到更强大的仲裁模块。与以往依赖稀疏审计或远程大模型监管的方法不同，TokenWall 实现了全覆盖的执行前调解，同时减少了远程仲裁和延迟。在 CIK-Bench 上的实验表明，TokenWall 将攻击成功率降低至 12.5%，同时在无需人工确认的情况下保持了 97.4% 的良性可执行通过率。TokenWall 在良性案例上仅增加了 0.69 秒的额外延迟，证明了语义运行时遏制可以为持久化 AI 智能体实现实用的安全与效用权衡。

## Abstract
Persistent AI agents extend large language models (LLMs) beyond single-turn interaction into long-lived software systems. Unlike traditional chat assistants, unsafe content in these agents can propagate through persistent state, reusable skills, and tool-mediated interactions, creating a substantially larger semantic attack surface. We observe that most security-critical interactions in such agents are transmitted through natural-language token flows, including memory updates, tool arguments, retrieved files, and inter-component communications. This observation enables a new security formulation: unsafe behavior can be intercepted as risky semantic flows before reaching privileged runtime sinks. Based on this insight, we propose TokenWall, a runtime defense framework that acts as a semantic firewall over agent token flows. TokenWall performs boundary-aware semantic auditing over these flows, constructing structured source-sink audit records, applying lightweight local inspection before execution, and selectively escalating ambiguous high-risk cases to stronger arbitration modules. Unlike prior approaches that rely on sparse auditing or remote large-model oversight, TokenWall enables full-coverage pre-execution mediation while reducing remote arbitration and latency. Experiments on CIK-Bench show that TokenWall reduces attack success rate to 12.5% while maintaining a 97.4% benign executable pass rate without human confirmation. TokenWall further introduces only 0.69 seconds of additional latency on benign cases, demonstrating that semantic runtime containment can achieve a practical security-utility trade-off for persistent AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Token-Flow Firewall: Semantic Runtime Auditing for Persistent AI Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体从单轮对话演进为具有内存、工具调用和跨会话能力的“持久化智能体”（Persistent AI Agents），其安全威胁也发生了质变。
*   **研究动机**：传统防御（如静态规则或单次提示词过滤）难以应对持久化状态下的攻击。恶意指令可能通过内存投毒、工具参数注入或技能修改在系统中长期潜伏并传播，形成复杂的“语义攻击面”。
*   **核心问题**：如何在不牺牲系统性能和用户隐私的前提下，对智能体复杂的内部状态转换和外部交互进行全覆盖的实时安全审计？

### 2. 方法论：核心思想与技术细节
论文提出了 **TokenWall**，一种将安全防御建模为“语义令牌流（Token-Flow）”拦截的运行时防火墙框架。

*   **核心思想**：观察到智能体所有的关键安全交互（内存更新、工具调用、文件检索等）本质上都是自然语言令牌流。TokenWall 在这些流跨越系统边界（如写入内存前、调用工具前）时进行拦截和审计。
*   **令牌流公式化**：定义流为 $f = (x, s, t, c, b)$，其中 $x$ 是负载（Payload），$s$ 是源（Source），$t$ 是汇点（Sink），$c$ 是运行时元数据，$b$ 是安全边界。
*   **分层审计流程**：
    1.  **确定性预检（Precheck）**：使用轻量级规则和模式匹配过滤显而易见的违规（如硬编码的敏感信息泄露）。
    2.  **本地语义审计（Local Small Auditor）**：核心步骤。使用本地部署的小参数模型（如 Qwen3-4B）对结构化的“源-汇”记录进行语义判断。
    3.  **可恢复重写（Recoverable Rewriting）**：如果检测到风险但部分内容良性，审计器会尝试重写负载（如脱敏或删除恶意指令），以保留任务连续性。
    4.  **选择性仲裁（Fallback Arbitration）**：仅当本地审计器不确定或涉及高影响操作时，才将案例升级给更强大的远程大模型进行最终裁决。
*   **决策动作**：支持允许（Allow）、重写（Rewrite）、推迟（Defer，需人工确认）或阻止（Block）。

### 3. 实验设计
*   **数据集/场景**：主要使用 **CIK-Bench**，这是一个专门针对持久化 AI 智能体（如 OpenClaw 框架）设计的安全基准测试，涵盖内存投毒、权限提升和工具滥用等场景。
*   **Benchmark 构成**：包含 88 个攻击案例和 38 个匹配的良性案例（用于测试实用性）。
*   **对比方法**：对比了 7 种主流的智能体防御方案，包括 OpenGuardrails、ClawBands、CIK-Defense、OpenClaw Shield、SecureClaw、ClawSec 和 ClawKeeper。

### 4. 资源与算力
*   **模型配置**：
    *   **任务模型**（被保护的智能体）：Gemini 3.1 Pro。
    *   **本地审计器**：默认使用 **Qwen3-4B**（实验中也测试了 1.7B 到 14B 的不同版本）。
    *   **远程仲裁器**：Qwen3.6-Plus。
    *   **评估法官**：GPT-5.5（作为 LLM-as-a-judge）。
*   **算力说明**：论文未明确给出具体的 GPU 型号和训练时长，但强调了其“本地化”属性，旨在普通硬件上即可运行推理，且实验重点在于推理延迟（Latency）而非训练成本。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的评估，包括攻击成功率（ASR）、良性通过率（PR）、人工干预率（HR）和延迟分析。
*   **消融实验**：非常充分。作者分别测试了“无重写”、“无本地审计器”、“无仲裁器”以及“仅使用大模型”等变体，验证了各组件的必要性。
*   **鲁棒性测试**：测试了不同规模的本地审计器（1.7B, 4B, 8B, 14B）对性能的影响，并对审计器的“不确定性评分”进行了校准分析。
*   **客观性**：通过与多种开源和商业防御插件对比，并在统一的 CIK-Bench 环境下运行，实验设计较为客观公平。

### 6. 主要结论与发现
*   **安全增强**：TokenWall 将攻击成功率（ASR）降至 **12.5%**，优于所有对比基线（最强基线为 14.7%）。
*   **实用性保持**：在良性任务上保持了 **97.4%** 的通过率，且无需人工干预（0% HR），远优于依赖人工审核的基线方法。
*   **高效低延迟**：在良性案例中，TokenWall 仅引入了 **0.69 秒** 的额外延迟；在攻击案例中，平均处理时间为 16.9 秒，远低于依赖远程全量审计的方法（约 64 秒）。
*   **重写价值**：证明了语义重写不仅能阻止攻击，还能通过修复受污染的输入来维持智能体的正常功能。

### 7. 优点（亮点）
*   **防御前置**：在“状态变更”发生前进行拦截，防止了攻击在持久化内存中的传播。
*   **本地化与隐私**：通过本地小模型处理绝大多数审计任务，减少了敏感数据向远程服务器的暴露。
*   **细粒度控制**：引入“重写”机制，避免了传统防火墙“一刀切”式的拦截，提升了用户体验。
*   **分层架构**：巧妙平衡了规则的效率、小模型的本地化能力和大模型的高级推理能力。

### 8. 不足与局限
*   **基准局限性**：实验主要集中在 OpenClaw 风格的个人助理智能体上，对于更广泛的工业级多智能体协作场景的泛化性有待验证。
*   **依赖元数据**：防御效果高度依赖于运行时环境提供的元数据（如源的信任度、汇点的权限），如果运行时环境本身被篡改，防火墙将失效。
*   **不确定性风险**：虽然有仲裁机制，但如果本地审计器对高风险行为给出了错误的“低不确定性”评分，可能会导致漏报。
*   **对抗性攻击**：未深入探讨针对审计器本身的对抗性攻击（例如，攻击者专门诱导审计器产生错误的重写）。

（完）
