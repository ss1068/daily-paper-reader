---
title: Dual Adversarial Fine-tuning for Enhancing Robustness of Large Vision Language Model
title_zh: 增强大视觉语言模型鲁棒性的双重对抗微调
authors: "Sibo Wang, Jie Zhang, Shiguang Shan, Xilin Chen, Wen Gao"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18958v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 通过对抗微调增强视觉语言模型的鲁棒性
tldr: 针对大视觉语言模型（LVLM）易受对抗攻击且现有防御方法泛化性差的问题，本文提出一种双重对抗微调框架。该框架通过视觉监督分支（利用原始编码器特征）和语义监督分支（利用图文对齐信号）共同优化，仅需替换CLIP视觉编码器即可在多种多模态任务中提升鲁棒性，无需针对特定任务重新训练或修改架构。
selection_source: fresh_fetch
motivation: 现有的LVLM防御方法多局限于单一任务场景，缺乏在多种复杂多模态任务间的泛化能力和安全性保障。
method: 提出双重对抗微调框架，通过视觉特征监督和语义对齐信号共同引导，仅需替换视觉编码器即可实现跨任务鲁棒性。
result: 在零样本分类、图像描述和视觉问答等多个任务的对抗鲁棒性评估中，该方法均优于现有的最先进技术。
conclusion: 该研究为提升大视觉语言模型的通用鲁棒性提供了一种高效且无需改变模型架构的有效解决方案。
---

## 摘要
以 LLaVA 和 GPT-4V 为代表的大视觉语言模型（LVLMs）虽然展现出了卓越的能力，但其视觉输入在对抗攻击面前仍然十分脆弱，构成了重大的安全风险。现有的防御方法主要针对单任务场景（例如零样本分类），因此在各种多模态任务中缺乏泛化性。为了解决这一局限性，我们提出了一个双重对抗微调框架，该框架联合优化来自两种模态的视觉和语义监督信号，在增强模型鲁棒性的同时，使其能够泛化到多个下游任务。所提出的框架包含两个核心组件，即视觉监督分支和语义监督分支。前者利用通过冻结的原始视觉编码器提取的干净图像特征来引导对抗鲁棒性，而后者则将图文对齐作为上下文信号，以在受到攻击时保持语义连贯性。此外，我们的方法通过简单地替换原始模型中的 CLIP 视觉编码器即可实现跨任务鲁棒性，无需进行单独的特定任务重训练或架构修改。大量实验表明，我们的方法在零样本分类、图像描述和视觉问答（VQA）任务的对抗鲁棒性评估中，均优于现有最先进的方法。

## Abstract
While Large Vision-Language Models (LVLMs), represented by LLaVA and GPT-4V, have demonstrated remarkable capabilities, their visual inputs remain vulnerable to adversarial attacks, posing significant security risks. Existing defense methods predominantly target single-task scenarios (e.g., zero-shot classification) and consequently lack generalizability across various multimodal tasks. To address this limitation, we propose a dual adversarial fine-tuning framework that jointly optimizes visual and semantic supervision signals from two modalities, enhancing model robustness while generalizing across multiple downstream tasks. The proposed framework comprises two core components, i.e., $\textbf{Visual}$ supervision branch and $\textbf{Semantic}$ supervision branch. The former branch leverages features from clean images, extracted via a frozen original vision encoder, to guide adversarial robustness while the latter incorporates caption-image alignment as a contextual signal to preserve semantic coherence under attack. Moreover, our method achieves cross-task robustness by simply replacing the CLIP vision encoder in the original model, with no need of separate task-specific retraining or architecture modifications.Extensive experiments demonstrate that our approach outperforms the state-of-the-art method in adversarial robustness evaluation across zero-shot classification, image captioning, and visual question answering (VQA) tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual Adversarial Fine-tuning for Enhancing Robustness of Large Vision Language Model》（增强大视觉语言模型鲁棒性的双重对抗微调）的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大视觉语言模型（LVLM，如 LLaVA、GPT-4V）虽然强大，但极易受到视觉对抗攻击（即在图像中加入人眼不可见的微小扰动，导致模型输出错误答案）。
*   **研究背景**：
    *   现有的防御方法（如 TeCoA）通常依赖于固定的类别标签（如 ImageNet 的 1000 类），这导致模型在面对未见过的类别或复杂的非分类任务（如 VQA、图像描述）时泛化能力极差。
    *   无监督防御方法（如 FARE）虽然泛化性较好，但在应对专门针对“图文对齐”机制的高级攻击时，鲁棒性提升有限。
*   **整体含义**：本文旨在开发一种既能保持高性能，又能跨任务（分类、描述、问答）通用的对抗防御框架。

### 2. 方法论：核心思想与技术细节
论文提出了 **DAFT（Dual Adversarial Fine-tuning）** 框架，其核心思想是通过“视觉”和“语义”双重监督信号来微调 CLIP 的视觉编码器：
*   **视觉监督分支（Visual Supervision Branch）**：
    *   **核心思想**：防止模型在对抗训练中“跑偏”，保留预训练模型的通用特征。
    *   **技术细节**：使用一个冻结的原始 CLIP 视觉编码器作为教师，要求正在微调的编码器在输入对抗样本时，其输出特征要尽可能接近原始编码器输入干净图像时的特征（使用 $L_2$ 距离损失 $L_{vis}$）。
*   **语义监督分支（Semantic Supervision Branch）**：
    *   **核心思想**：利用丰富的描述性文字（Caption）而非简单的类别标签来引导对齐。
    *   **技术细节**：引入对比学习（Triplet Loss）。将对抗图像特征作为 Anchor，对应的真实 Caption 作为 Positive 样本，并从同 Batch 中筛选出最相似的错误 Caption 作为 Hard Negative 样本。通过 $L_{sem}$ 损失函数，拉近对抗图像与正确描述的距离，推开与错误描述的距离。
*   **算法流程**：采用 PGD 对抗攻击算法动态生成对抗样本，随后交替进行对抗样本生成和模型参数更新，最终只需将 LVLM（如 LLaVA）中的原始视觉编码器替换为微调后的编码器即可。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：COCO-caption（包含丰富的图像-描述对）。
    *   **评估集**：涵盖 11 个分类数据集（如 CIFAR10/100, ImageNet-R 等）、2 个图像描述数据集（COCO, Flickr30k）以及 4 个 VQA 数据集（VQAv2, TextVQA, VizWiz, OKVQA）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：LLaVA 1.5-7B 和 OpenFlamingo。
    *   **对比防御方法**：TeCoA（有监督）、PMG-AFT（有监督改进版）、FARE（目前最先进的无监督防御方法）。
*   **攻击设置**：使用 AutoAttack（分类）和多阶段 APGD 攻击（描述与 VQA），扰动预算 $\epsilon$ 设为 2/255、4/255 和 8/255。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A100 GPU**。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 64，学习率 1e-5，采用余弦退火调度。
*   **时长**：文中未明确给出总训练小时数，但提到了每个 Batch Step 的处理时间（约 162ms），并指出该方法比从头开始进行对抗训练要高效得多。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。
    *   **跨任务验证**：在零样本分类、图像描述、VQA 三大类任务上均进行了详尽测试。
    *   **消融实验**：验证了视觉/语义分支的各自贡献、负采样策略的影响、超参数（$\gamma, a$）的敏感性以及 Batch Size 的影响。
    *   **迁移性测试**：验证了在不同 LVLM 架构（LLaVA vs OpenFlamingo）之间的鲁棒性迁移，以及黑盒攻击下的表现。
*   **客观性**：所有对比方法均在相同的 COCO 数据集上训练，确保了基准测试的公平性。

### 6. 主要结论与发现
*   **性能领先**：DAFT 在所有任务上的对抗鲁棒性均优于 SOTA 方法（FARE）。例如在 $\epsilon=2/255$ 时，VQA 平均准确率比 FARE 高出约 1.1%，分类准确率高出 1.2%。
*   **泛化性强**：通过引入 Caption 监督，模型在未见过的分类数据集上表现出更好的零样本迁移能力。
*   **负采样关键性**：实验发现，选择“最难”的负样本（Hard Negative）对于提升对比学习的防御效果至关重要，使用简单负样本会导致鲁棒性几乎归零。

### 7. 优点（亮点）
*   **语义丰富性**：首次证明了使用**描述性 Caption** 替代**类别标签**进行对抗训练能显著提升多模态任务的鲁棒性。
*   **即插即用**：无需修改 LVLM 的语言模型部分或投影层，仅通过更换视觉编码器即可实现防御，部署成本低。
*   **双分支平衡**：视觉监督分支有效缓解了对抗微调中常见的过拟合问题，保持了模型在干净样本上的性能。

### 8. 不足与局限
*   **模型范围**：主要针对基于 CLIP 架构的视觉编码器，对于非 CLIP 架构（如基于视觉特征提取器 DINOv2 等）的适用性有待验证。
*   **白盒依赖**：虽然测试了黑盒迁移，但防御核心仍基于白盒对抗训练，对于完全封闭源代码的商业模型（如 GPT-4V 接口）无法直接应用。
*   **语言侧防御缺失**：该方法仅加固了视觉编码器，未考虑针对文本提示词（Prompt）的对抗攻击或多模态联合攻击。
*   **数据规模**：训练仅使用了 COCO 数据集，若能扩展到更大规模的图文对数据集（如 LAION），效果可能进一步提升。

（完）
