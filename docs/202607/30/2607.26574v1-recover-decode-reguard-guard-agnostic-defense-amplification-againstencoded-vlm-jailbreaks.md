---
title: "Recover, Decode, Reguard: Guard-Agnostic Defense Amplification againstEncoded VLM Jailbreaks"
title_zh: 恢复、解码、重护：针对编码式 VLM 越狱的通用防御增强
authors: "Haoyu Zhang, Zhuoxi Wang, Shibo Zheng, Zijian Xiao, Xiangchen Guan, Mohammad Zandsalimy, Shanu Sushmita"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26574v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对编码VLM越狱的护栏无关防御增强
tldr: 本研究针对视觉语言模型（VLM）安全分类器无法识别编码攻击（如代码、逻辑或图像文本）的“解码差距”问题，提出了一种通用的“恢复与解码”放大器。该工具在安全检查前将复杂输入还原为纯文本。通过对11种攻击组合的严苛评估，研究揭示了非迭代恢复防御在安全性和可用性之间的权衡瓶颈，为VLM防御提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的VLM安全分类器往往只判断输入的表面形式而非实际含义，导致经过编码或跨模态转换的有害请求能轻易绕过防御。
method: 提出一种与模型无关的放大器，在安全审查前将图像内容转录并将编码文本还原为纯文本载荷，以暴露真实请求。
result: "实验发现该放大器仅能部分修复防御漏洞，且在尝试通过增强层提高安全性时，会导致良性请求的过度拒绝率高达81%-92%。"
conclusion: 研究证明了基于恢复的非迭代防御在处理表示偏移攻击时存在安全与效用的性能天花板，难以平衡防御强度与用户体验。
---

## 摘要
安全分类器（“护栏”）是视觉语言模型（VLM）主流的黑盒防御手段，但它们往往只判断输入的表面形式而非其深层含义：当有害请求被重新编码为集合论、形式逻辑、稀有语言、代码或文本图像时，能够绕过原本可以拦截明文请求的护栏——这被称为“解码差距”。一种自然的修复方案是采用一种与护栏无关的“恢复与解码增强器”，在进入护栏之前，先转录图像内容并将编码文本还原为明文载荷，从而使任何现成的分类器都能筛选真实的请求。我们构建了这种增强器，并针对攻击者的最理想情况对其进行了评估：包含 11 种攻击的集成攻击，若其中任何一种成功则判定该行为被破解（遵循 AutoAttack 的套件最佳原则）——这在越狱防御研究中鲜有报道，其破解率约为单次攻击均值的 3.5 倍。这揭示了我们的核心发现：在我们评估的五种护栏和两个目标 VLM 中，非迭代式恢复防御存在经验性的安全-效用上限。该增强器仅能部分弥合差距——未受防御的集成攻击破解了 89-91% 的行为，而最强的“护栏+增强器”组合仍留有 63-65% 的破解率——且在 10 个“护栏-目标”对中，仅有 4 个表现出显著增益。它在接口上是护栏无关的，但在效果上并非如此。模块化的“重护”（reguard）层虽然弥补了大部分残余风险，但对于校准良好的护栏，其良性过度拒绝率高达 81-92%；而唯一保持可用性的较宽松护栏则从未达到可部署的安全水平（集成攻击成功率为 48%）。在我们研究的流水线中，针对表征偏移攻击（即产生可读载荷的编码和跨模态渲染，而非像素或嵌入空间攻击），没有任何配置能同时实现低攻击成功率和低过度拒绝率。我们的贡献包括该增强器、使权衡可见的集成评估，以及恢复式 VLM 防御有效性的适用范围图谱。

## Abstract
Safety classifiers ("guards") are the dominant black-box defense for vision-language models, yet they judge an input's surface form, not its meaning: a harmful request re-encoded as set theory, formal logic, a rare language, code, or an image of text slips past a guard that would block it in plain language -- the decode gap. The natural fix is a guard-agnostic recover-and-decode amplifier that transcribes image content and restates encoded text into its plain payload before the guard, so any off-the-shelf classifier can screen the true request. We build this amplifier and evaluate it against the attacker's best case: an ensemble of eleven attacks, scoring a behavior as broken if any succeeds (best-of-suite, following AutoAttack) -- rarely reported for jailbreak defenses, yet ~3.5x the per-attack mean. This exposes our central finding: an empirical safety-utility ceiling for the non-iterative recovery defenses we evaluate, across five guards and two target VLMs. The amplifier only partly closes the gap -- the undefended ensemble breaks 89-91% of behaviors, and the best guard-plus-amplifier still leaves 63-65% -- and its gain over the guard alone is significant in only four of ten guard-target pairs. It is guard-agnostic at the interface, but not uniformly so in effect. A modular reguard layer closes much of the residual, yet drives benign over-refusal to 81-92% for well-calibrated guards; the one laxer guard that stays usable never reaches deployable safety (48% ensemble ASR). No configuration we evaluate reaches both low attack-success and low over-refusal, for the pipeline we study and for representation-shifting attacks -- encodings and cross-modal renders that leave a legible payload, not pixel- or embedding-space attacks. We contribute the amplifier, an ensemble evaluation that makes the trade-off visible, and a map of where recovery-based VLM defense works and where it does not.

---

## 论文详细总结（自动生成）

这是一份关于论文《Recover, Decode, Reguard: Guard-Agnostic Defense Amplification against Encoded VLM Jailbreaks》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前视觉语言模型（VLM）主要依赖黑盒安全分类器（“护栏”，如 Llama Guard）来拦截有害请求。然而，这些护栏通常只识别输入的“表面形式”而非“深层含义”。
*   **核心问题**：论文定义了**“解码差距”（Decode Gap）**——当有害请求被重新编码（如使用集合论、形式逻辑、代码或渲染成图像中的文字）时，护栏会失效。
*   **整体含义**：研究旨在开发一种通用的、无需训练的防御增强机制，通过将复杂输入还原为明文，使现有护栏能够识别隐藏的攻击意图。

### 2. 方法论
论文提出了 **“恢复-解码-重护”（Recover-Decode-Reguard）** 框架，其核心思想是在护栏审查之前对输入进行预处理：
*   **恢复（Recover）**：利用目标 VLM 将图像内容转录为文本。
*   **解码（Decode）**：利用 VLM 将转录后的文本或原始编码文本（如 Base64、代码）重述为简单的明文载荷（Plain-language payload）。
*   **重护（Reguard）**：这是一个可选的模块化层。除了审查解码后的明文，还同时审查“恢复后但未解码”的原始形式。
*   **算法流程**：
    1.  输入请求（文本+图像）。
    2.  VLM 提取图像文本并与原始文本合并。
    3.  VLM 将合并文本解码为明文。
    4.  护栏检查明文；若开启 Reguard，则同时检查合并文本。
    5.  若任一环节判定有害则拒绝；否则由目标 VLM 生成最终回答。

### 3. 实验设计
*   **数据集与场景**：
    *   **有害性评估**：使用 HarmBench（100 个行为），通过 11 种编码攻击（5 种文本编码如 CodeAttack、6 种图像渲染如 FigStep）进行测试。
    *   **良性评估**：使用 OR-Bench-Hard（100 个行为）测试“过度拒绝率”（Over-refusal）。
*   **Benchmark 评估指标**：采用 **集成攻击成功率（Ensemble ASR / Best-of-suite）**。即如果 11 种攻击中任一成功，则判定防御失败。这比常用的平均 ASR 严苛约 3.5 倍。
*   **对比对象**：
    *   **目标模型**：Qwen2.5-VL-7B, InternVL3-8B。
    *   **护栏模型**：WildGuard, LlamaGuard-3, Qwen3Guard, ThinkGuard, GuardReasoner-VL。
    *   **基准方法**：原生护栏（Guard-alone）和 ECSO（现有的图像转文本防御）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **推理成本分析**：由于该方法是**无需训练（Training-free）**的，主要成本在于推理。论文指出，该防御流水线平均导致目标模型的调用次数增加约 2.2 倍，推理耗时增加约 1.9 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的护栏模型和 2 种主流 VLM 架构，针对 11 种攻击变体进行了详尽测试。
*   **消融实验**：对比了“仅恢复”与“恢复+解码”的效果，并测试了不同规模的解码器（如使用 Llama-3.3-70B 作为解码器）。
*   **充分性评价**：实验设计非常**严谨且具有对抗性**。通过引入集成评估（AutoAttack 风格）和阈值曲线扫描（Threshold Sweep），研究者客观地展示了防御的真实上限，避免了因单一攻击或特定阈值导致的评估偏差。

### 6. 主要结论与发现
*   **安全-效用天花板**：存在一个难以逾越的边界。虽然增强器能显著降低单项攻击成功率，但在集成攻击下，最强防御仍留有 63-65% 的漏洞。
*   **过度拒绝问题**：为了堵住剩余漏洞而引入的 Reguard 层会导致灾难性的后果——良性请求的过度拒绝率飙升至 **81-92%**，使得模型几乎不可用。
*   **护栏依赖性**：该防御的效果高度依赖于底层护栏的校准。较宽松的护栏（如 LlamaGuard-3）虽然过度拒绝率低，但安全性也极差（48% ASR）。
*   **核心瓶颈**：代码攻击（CodeAttack）和分散式图像攻击（Distraction）是主要的残余风险点，因为解码过程往往会丢失这些攻击赖以生效的结构化特征。

### 7. 优点
*   **通用性（Guard-Agnostic）**：无需访问护栏内部参数，可适配任何现成的分类器。
*   **评估严苛性**：引入集成 ASR 指标，揭示了过去研究中被“平均 ASR”掩盖的防御脆弱性。
*   **模块化设计**：Recover、Decode 和 Reguard 三层结构清晰，便于针对不同场景进行权衡。

### 8. 不足与局限
*   **防御范围限制**：仅针对“表示偏移”类攻击（有可读载荷的编码）。对于**像素空间攻击**（对抗扰动）或**嵌入空间攻击**，该方法在原理上无法生效。
*   **性能开销**：推理成本翻倍，对于实时性要求高的应用可能存在压力。
*   **非迭代局限**：作为一种非迭代的黑盒防御，它无法像多轮对话或代理（Agentic）防御那样通过反复确认来降低误判。
*   **安全与效用的极端冲突**：目前尚未找到能在保持高安全性的同时，将过度拒绝率降至可接受水平（如 <10%）的配置。

（完）
