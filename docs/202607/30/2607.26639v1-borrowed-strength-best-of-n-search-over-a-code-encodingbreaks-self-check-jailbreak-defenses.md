---
title: "Borrowed Strength: Best-of-N Search over a Code EncodingBreaks Self-Check Jailbreak Defenses"
title_zh: 借来的力量：基于代码编码的 Best-of-N 搜索破解了自检越狱防御
authors: "Haoyu Zhang, Shibo Zheng, Xiangchen Guan, Zhuoxi Wang, Zijian Xiao, Mohammad Zandsalimy, Shanu Sushmita"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26639v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 分析并破解自我检查越狱防御机制
tldr: "本研究针对以SAGE为代表的自检式（Self-check）大模型防御机制，提出了一种结合代码补全编码与Best-of-N搜索的组合攻击方法。虽然这两种手段单独使用时对该防御几乎无效，但组合后能显著突破防线，在多个开源模型上实现最高67%的攻击成功率。研究揭示了自检防御的强度本质上借用了模型自身的判断力，并深入分析了不同攻击方式在转换型与门控型防御下的生存差异，为评估大模型安全性提供了新视角。"
selection_source: fresh_fetch
motivation: 探究看似强大的自检式防御机制在面对多种无害攻击手段组合时是否存在严重的安全漏洞。
method: 提出一种将代码补全编码与Best-of-N搜索相结合的攻击框架，利用搜索预算在编码空间内寻找防御盲点。
result: "该组合攻击在多个开源目标模型上将攻击成功率从不足5%提升至最高67%，有效瓦解了SAGE等强力防御。"
conclusion: 自检防御的效能取决于模型将请求转化为拒绝的能力，且不同类型的防御对特定攻击方式的敏感度存在显著差异。
---

## 摘要
自检防御要求目标模型在回答请求前对其进行评估；SAGE 是已发表的最强实例，报告的平均防御成功率为 99%。我们展示了可以通过组合两种单独对其无害的攻击来破解它：一种成熟的代码补全编码和一种成熟的 Best-of-N 搜索，两者单独使用时的行为成功率均不超过 4.7%。组合后，将搜索预算用于编码，它们在三个开源目标模型上分别达到了 67/22/15% 的成功率，且这种效果在 70B 参数的目标模型上依然存在。我们随后对这种组合进行了深入解释，而不仅仅是报告结果。首先，自检防御从目标模型中“借用”力量：SAGE 本身并不检测攻击，而是要求模型去检测，而四个目标模型在 32% 到 97% 的时间内将该请求转化为明确的拒绝，这决定了防御覆盖范围的差异，尽管未防御时的攻击范围几乎相同。其次，哪种攻击能够存活取决于防御类型，且结果是反转的：针对转换型防御，代码编码保留的未防御攻击范围远高于字符搜索；而针对门控型防御，这一顺序则发生了反转。我们通过攻击向防御决策边界提供的独立探测次数来解释这一点。最后，我们报告了在自身流水线中发现并修复的一个有效性缺陷：贪婪解码下的确定性攻击根本没有 Best-of-N 变体通道，并提供了一行用于检测该缺陷的诊断代码。所有结论均基于 310,000 次生成结果，并由经过人工验证的评判器进行评分。

## Abstract
A self-check defense asks the target model to assess a request before answering it; SAGE, the strongest published instance, reports an average 99% defense success rate. We show it can be breached by composing two attacks that are individually harmless against it: an established code-completion encoding and an established best-of-N search, neither of which exceeds 4.7% of behaviors alone. Composed, with the search budget spent on the encoding, they reach 67/22/15% across three open targets, and the effect persists on a 70B target. We then explain the composition rather than only reporting it. First, a self-check defense borrows its strength from the target: SAGE does not detect the attack, it asks the model to, and the four targets convert that request into an explicit refusal between 32% and 97% of the time, which orders the spread in defended coverage even though undefended reach is near-identical. Second, which attack survives is decided by the type of defense, and it inverts: against transform defenses the code encoding retains far more of its undefended reach than the character search, while against gate defenses the ordering flips. We account for this with the number of independent probes an attack delivers to a defense's decision boundary. Finally, we report a validity defect we found and repaired in our own pipeline, a deterministic attack under greedy decoding has no best-of-N variation channel at all, and give the one-line diagnostic that detects it. All claims rest on 310,000 generations scored by a human-validated judge.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）的“自检式”（Self-check）防御机制进行了深入的对抗性分析，揭示了其核心漏洞并提出了高效的组合攻击方法。以下是详细总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究看似强大的“自检式防御”（如 SAGE，声称拥有 99% 的防御成功率）在面对多种弱攻击手段的**组合**时是否依然稳固，并试图解释这种组合攻击为何有效。
*   **背景**：目前的防御主要分为“门控型”（Gate，如分类器拦截）和“转换型”（Transform，如重写请求）。SAGE 属于转换型，它利用模型识别越狱的能力与遵循指令的倾向之间的差距，让模型在回答前先自我审计。

### 2. 方法论
论文提出了一种名为 **BoN-wrapped CodeAttack** 的组合攻击方法，其核心思想包括：
*   **攻击组合**：将“代码补全编码”（CodeAttack，将有害请求伪装成代码编写任务）与“Best-of-N 搜索”（BoN，多次采样取最优）相结合。
*   **核心机制一：借来的力量（Borrowed Strength）**：自检防御的强度并不取决于防御设计本身，而是取决于目标模型将审计结果转化为“拒绝回答”的倾向（Refusal Disposition）。
*   **核心机制二：探测次数（Probe Count）**：
    *   **门控型防御**：拥有明确的决策边界，容易被高方差的搜索（如字符级扰动的 BoN）通过多次独立探测而破解。
    *   **转换型防御**：没有搜索边界，更易被单一但高质量的编码（如代码编码）绕过。
*   **算法流程**：将有害行为通过代码模板编码，在非零温度（Temperature > 0）下对目标模型进行 $N$ 次查询，只要其中一次产生有害输出即视为越狱成功。

### 3. 实验设计
*   **数据集**：使用 **HarmBench**（包含 100 个有害行为）。
*   **目标模型**：Llama-3.1-8B、Qwen2.5-7B、Gemma-2-9B 以及大规模的 Llama-3.3-70B。
*   **基准防御（Benchmarks）**：
    *   **SAGE**（最强的自检转换防御）。
    *   **SemanticSmooth**（语义平滑防御）。
    *   **门控防御**：包括 WildGuard 和 LlamaGuard-3。
*   **对比方法**：原始 BoN（字符级扰动）、单次 CodeAttack、以及未防御状态。
*   **评判器**：使用经过人工验证的 GPT-5-mini（注：文中表述如此，可能指代高性能模型接口）作为自动化法官。

### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号（如 A100/H100）或总训练时长。
*   **实现细节**：提到模型是本地部署，使用 vLLM 框架。Llama-3.3-70B 使用了 **fp8 量化**并在单张显卡上运行。
*   **生成规模**：总计进行了 **310,000 次**文本生成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个模型家族、多种防御组合、2 种核心攻击及其变体，共 31 个实验单元（Cells）。
*   **充分性**：实验非常充分。不仅验证了攻击的有效性，还进行了消融实验（如温度消融）、跨模型规模验证（从 8B 到 70B），并深入探讨了不同防御类型的“反转效应”。
*   **客观性**：作者发现并纠正了自身实验中的“确定性缺陷”（即贪婪解码下 BoN 失效的问题），体现了严谨的学术态度。

### 6. 主要结论与发现
*   **协同效应显著**：在 SAGE 防御下，代码编码（单次成功率 < 4.7%）与 BoN 搜索（单次成功率 < 3%）组合后，成功率飙升至 **15% - 67%**，提升了 9 到 75 倍。
*   **防御效能的本质**：自检防御的效能高度依赖于模型自身的“拒绝倾向”。例如，Llama 模型倾向于将自检视为“任务”而非“门控”，导致其防御覆盖率远低于 Qwen 和 Gemma。
*   **防御类型决定攻击存活**：攻击效果在不同防御下会发生“反转”。代码编码在对抗转换型防御时表现更好，而字符搜索在对抗门控型防御时更具优势。
*   **规范化失效**：现有的输入规范化（Canonicalization）手段无法有效消除 BoN 攻击的多样性，消除率接近 0%。

### 7. 优点
*   **机制解释深入**：不只是报告攻击成功率，还提出了“借用力量”和“探测次数”理论，解释了防御失败的深层原因。
*   **方法简单且通用**：组合两种已有的弱攻击即可破解强防御，证明了防御评估中考虑“攻击组合”的重要性。
*   **严谨的验证**：通过 31 万次生成和人工校验，确保了结果的可信度，并提供了诊断 BoN 实验有效性的工具。

### 8. 不足与局限
*   **模型覆盖范围**：主要集中在开源模型，未对闭源前沿模型（如 GPT-4, Claude 3.5）进行大规模测试。
*   **有害性分级**：虽然攻击成功率高，但部分成功案例属于“脚手架”式输出，实际可操作的有害信息比例约为 headline 数据的三分之一。
*   **防御堆叠测试不足**：虽然提出了堆叠门控和自检防御的建议，但未在文中给出详细的防御成本（如对正常请求的误伤率）分析。

（完）
