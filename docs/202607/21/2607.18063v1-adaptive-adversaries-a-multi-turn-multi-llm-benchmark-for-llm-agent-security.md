---
title: "Adaptive Adversaries: A Multi-Turn, Multi-LLM Benchmark for LLM Agent Security"
title_zh: 自适应对抗者：针对大语言模型智能体安全的多轮、多模型基准测试
authors: "Devina Jain, David Hartmann, Chuan Li"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18063v1"
tags: ["query:llm"]
score: 8.0
evidence: 针对自适应提示注入和多轮操纵的基准测试
tldr: 该研究针对LLM代理易受提示注入和多轮操纵的问题，提出了包含21个场景的新型安全基准。该基准采用自适应多轮攻击模式，让攻击者LLM根据防御者的实时反馈动态调整策略。实验显示，15轮自适应攻击的成功率远超单轮攻击，并揭示了GPT、Claude等顶级模型在不同场景下迥异的脆弱性。研究开源了完整的基准框架和大规模攻击轨迹数据集，为LLM代理安全评估提供了重要工具。
selection_source: fresh_fetch
motivation: 现有LLM安全基准多采用固定攻击库或单轮交互，无法评估代理在面对能根据反馈动态调整策略的多轮自适应攻击时的安全性。
method: 提出了一个包含21个场景的基准，利用多个前沿LLM作为攻击者，在多轮对话中观察防御者响应并实时调整攻击策略。
result: 15轮自适应攻击的成功率显著高于单轮攻击，且不同顶级模型在特定场景下表现出截然不同的安全弱点。
conclusion: 研究强调了多轮自适应评估在LLM安全中的必要性，并发布了包含场景、编排器及大量攻击轨迹的开源基准。
---

## 摘要
基于大语言模型（LLM）的智能体处理外部内容，使其面临提示注入和多轮操纵的风险。大多数安全基准测试通过在评估前收集的固定攻击池（无论是单轮还是多轮）来评估防御者。我们提出了一个包含 21 个场景的基准测试，用于评估“针对无记忆 LLM 防御者的自适应多轮攻击”：一个自主的 LLM 攻击者观察先前的防御者响应并在各轮次间调整策略，而防御者的每次响应都被视为一次全新的交互。在保持 21 个场景、攻击者、防御者和结构化输出评分固定的情况下，将评分限制在攻击者的第一轮仅产生 0-1% 的攻击成功率（ASR）；而允许 15 轮自适应攻击则产生 5.4-14.0% 的 ASR。汇集三个前沿 LLM 攻击者所发现的唯一成功攻击数量是最佳单一攻击者的 1.4-2.2 倍，且生成的攻击与现有基准测试中的攻击具有较低的余弦相似度（0.02-0.14）。Claude Opus 4.6 和 GPT-5.4 在总体表现上持平（各为 5.4%；95% 置信区间重叠），但它们的弱点差异显著：在一个场景中，Opus 的 ASR 达到 60%（95% CI 36-80%），而 GPT-5.4 和 Gemini 各维持在 7%（CI 1-30%；该差距在更高样本量的重复实验中依然存在）。21 个场景中有 13 个能够区分至少一对防御者，但各场景间的排名并不一致（肯德尔协同系数 W = 0.19）。我们发布了该基准测试——包括 21 个评估场景、10 个公开开发场景、编排器、基准测试框架和多攻击者命令行界面（CLI）——此外还包括来自 3x3 前沿矩阵的 945 份对话记录、一个攻击重放数据集，以及来自一项公开竞赛最终评分轮次的 18,422 场 gpt-oss-20b 对战数据。

## Abstract
LLM-based agents process external content, exposing them to prompt injection and multi-turn manipulation. Most safety benchmarks evaluate defenders against fixed attack pools collected before evaluation, single-turn or multi-turn. We present a 21-scenario benchmark for \emph{adaptive multi-round attacks against memoryless LLM defenders}: an autonomous LLM attacker observes prior defender responses and pivots across rounds, while each defender response is evaluated as a fresh interaction. Holding the 21 scenarios, attackers, defenders, and structured-output scoring fixed, restricting scoring to the first attacker turn yields $0$-$1\%$ attack success rate (ASR); allowing 15 rounds of adaptive attack yields $5.4$-$14.0\%$. Pooling three frontier attacker LLMs uncovers $1.4$-$2.2\times$ as many unique successful attacks as the best single attacker, and the generated attacks have low cosine similarity ($0.02$-$0.14$) to attacks in existing benchmarks. Claude Opus 4.6 and GPT-5.4 are tied in aggregate ($5.4\%$ each; overlapping $95\%$ CIs), but their weaknesses differ sharply: on one scenario Opus reaches $60\%$ ASR ($95\%$ CI $36$--$80\%$) while GPT-5.4 and Gemini each stay at $7\%$ (CI $1$-$30\%$; the gap is preserved in a higher-$N$ replication). $13$ of $21$ scenarios distinguish at least one defender pair, yet rankings disagree across scenarios (Kendall's $W = 0.19$). We release the benchmark -- 21 evaluation scenarios, 10 public development scenarios, the orchestrator, baseline harnesses, and a multi-attacker CLI -- plus 945 transcripts from the 3$\times$3 frontier matrix, an attack-replay dataset, and 18{,}422 gpt-oss-20b battles from an open competition's final scoring rounds.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Adversaries: A Multi-Turn, Multi-LLM Benchmark for LLM Agent Security》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的 LLM 安全基准测试（如 StrongREJECT, JailbreakBench）大多采用**静态攻击池**，即在评估前收集固定的攻击载荷。然而，现实中的 LLM 智能体（Agent）面临的是能够根据防御者反馈动态调整策略的**自适应攻击者**。
*   **核心问题**：静态、单轮的评估无法真实反映 LLM 智能体在多轮交互、间接提示注入（Indirect Prompt Injection）和持续操纵下的安全风险。
*   **整体含义**：该研究提出了一个包含 21 个场景的动态基准测试，通过“LLM 攻击者 vs. LLM 防御者”的多轮博弈，揭示了前沿模型在面对自适应压力时隐藏的安全漏洞。

### 2. 论文提出的方法论
*   **核心思想**：采用**自适应多轮攻击（Adaptive Multi-Turn Attack）**。攻击者 LLM 拥有对话记忆，能观察防御者之前的响应并调整策略；防御者 LLM 被设定为**无记忆（Memoryless）**，即每轮交互都被视为全新的请求，以此隔离并测试防御者对单次恶意输入的鲁棒性。
*   **关键技术细节**：
    *   **编排器（Orchestrator）**：管理 15 轮对话，将场景上下文和攻击者文本插入模板，并解析防御者的结构化 JSON 输出。
    *   **结构化评分（Structured Scoring）**：防御者必须按预定义的 JSON Schema 输出。成功判定（ASR）仅基于特定字段（如 `verdict` 或 `actions_executed`），避免了自然语言评估的模糊性。
    *   **多阶段校准**：场景经过“概念生成 -> 插件实现 -> 过滤校准”三个阶段，确保场景具有区分度（不至于太难或太易）且对模型能力敏感。
*   **算法流程**：攻击者观察历史 -> 生成新攻击 -> 编排器格式化 -> 防御者处理（无记忆） -> 结构化检查 -> 循环至 15 轮或攻击成功。

### 3. 实验设计
*   **场景与数据集**：
    *   **21 个评估场景**：涵盖间接注入（11个）、PII/数据泄露（6个）、不安全输出（2个）、供应链攻击（1个）和提示词提取（1个）。
    *   **10 个公开开发场景**：用于模型迭代。
*   **Benchmark 对象**：
    *   **3x3 前沿矩阵**：攻击者和防御者均在 Claude Opus 4.6、GPT-5.4 和 Gemini 2.5 Pro 之间交叉组合。
    *   **竞赛数据集**：包含 18,422 场基于 `gpt-oss-20b` 模型的公开竞赛对战记录。
*   **对比方法**：
    *   **单轮 vs. 多轮**：对比第 1 轮和第 15 轮的攻击成功率（ASR）。
    *   **单一 vs. 混合攻击者**：评估汇集多个 LLM 攻击者与单一攻击者的效果差异。
    *   **跨基准对比**：与 b3、AgentHarm、JailbreakBench 等 6 个现有基准进行余弦相似度和一致性对比。

### 4. 资源与算力
*   **算力设备**：论文提到在公开竞赛阶段，使用自托管的 **vLLM** 框架，运行在单张 **NVIDIA A100 80GB GPU** 上。
*   **模型调用**：前沿模型评估通过 Anthropic、OpenAI 的官方 API 以及 OpenRouter（针对 Gemini）进行。
*   **训练时长**：该论文主要关注推理评估而非模型训练，因此未提及训练时长，但记录了 2026 年 3 月至 5 月间的实验窗口。

### 5. 实验数量与充分性
*   **实验规模**：
    *   前沿模型矩阵：945 份完整对话轨迹（N=5 每单元）。
    *   高样本量验证：针对关键发现进行了 N=20 的重复实验（共 180 场）。
    *   竞赛数据：超过 11 万场对战（含公开和私有轮次）。
*   **充分性与公平性**：
    *   **消融实验**：通过“非自适应攻击者”和“有记忆防御者”的对比，验证了攻击者自适应能力是 ASR 提升的主因。
    *   **统计严谨性**：使用了 95% 置信区间和肯德尔协同系数（Kendall's W）来评估场景的一致性和区分度。实验设计考虑了随机噪声，整体较为客观公平。

### 6. 论文的主要结论与发现
*   **多轮攻击的威力**：在相同场景下，单轮攻击成功率仅为 0-1%，而 15 轮自适应攻击可将成功率提升至 **5.4-14.0%**。
*   **模型弱点的异质性**：Opus 和 GPT-5.4 总体 ASR 持平，但弱点完全不同。例如，Opus 在 `memleak`（内存泄露）场景下极易受诱导（60% ASR），而 Gemini 在 `paperaccept`（伪造权限）场景下表现最差。
*   **攻击者多样性**：汇集三个前沿模型作为攻击者，发现的漏洞数量是单一模型攻击者的 **1.4-2.2 倍**。
*   **低一致性**：不同场景对防御者的排名不一（W=0.19），说明不存在单一的“安全维度”，防御必须针对特定场景定制。

### 7. 优点
*   **动态性**：突破了静态攻击池的限制，更符合智能体在真实环境中的受攻击模式。
*   **评分客观**：通过强制结构化输出和 typed-field 检查，极大降低了模型评估中的“奖励黑客（Reward Hacking）”和法官偏差。
*   **开源贡献**：提供了完整的编排框架、场景插件和大规模真实攻击轨迹数据集，具有极高的实用价值。

### 8. 不足与局限
*   **样本量限制**：在 3x3 矩阵中，每单元 N=5 的样本量相对较小，虽然有 N=20 的补充验证，但仍可能存在随机性。
*   **攻击者能力天花板**：攻击者本身受限于 LLM 的安全训练，可能无法生成某些人类红队能够想到的极端违规策略。
*   **防御者设定**：无记忆防御者的设定虽然有助于压力测试，但忽略了现实中“状态监测”和“异常检测”等动态防御手段的效果。
*   **场景覆盖**：虽然涵盖了 5 大类，但 21 个场景对于复杂的企业级应用来说仍显不足。

（完）
