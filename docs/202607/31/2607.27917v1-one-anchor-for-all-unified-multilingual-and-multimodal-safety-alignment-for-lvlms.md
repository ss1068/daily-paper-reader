---
title: "One Anchor for All: Unified Multilingual and Multimodal Safety Alignment for LVLMs"
title_zh: 一锚定全局：大视觉语言模型（LVLMs）的统一多语言与多模态安全对齐
authors: "Enyi Shi, Fei Shen, Chuancheng Shi, Linxia Zhu, Shuyi Miao, Jinhui Tang, Tat-Seng Chua"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.27917v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 多模态大语言模型的安全对齐
tldr: "针对大型视觉语言模型（LVLM）面临的跨语言和跨模态复合安全攻击，本文提出了一种基于神经元级别的统一对齐框架。通过识别并更新极少量的模态与语言共享安全神经元（MLS-Neurons，约0.03%参数），该方法成功将仅限英语的安全监督迁移至多语言和多模态场景。实验证明，该方法在显著提升模型防御能力的同时，有效保留了其通用性能。"
selection_source: fresh_fetch
motivation: 现有的LVLM安全防御方法在处理多语言和多模态复合攻击时，面临防御手段孤立、安全数据稀缺及微调成本高昂的挑战。
method: 通过交集识别出跨模态和跨语言共享的安全神经元（MLS-Neurons），并以英语为锚点仅更新这部分极少量参数来实现安全对齐。
result: "实验结果显示，该方法在多个多语言和多模态安全基准测试中显著优于现有技术，且仅需微调约0.03%的参数。"
conclusion: 研究证明了利用共享安全神经元进行跨维度对齐是实现高效、低成本且鲁棒的LVLM安全对齐的有效途径。
---

## 摘要
随着大视觉语言模型（LVLMs）在全球范围内的部署，多语言指令与视觉信息的结合使得恶意攻击比以往任何时候都更加隐蔽和复杂。然而，现有方法将语言防御与模态防御隔离开来，加之安全数据稀缺和微调成本高昂，使得模型难以抵御复合型攻击。为了应对这一严峻挑战，我们提出了一个由模态和语言共享安全神经元（MLS-Neurons）驱动的神经元级跨维度安全对齐框架。首先，我们通过比较对有害样本和良性样本的响应来识别单语言和单模态安全神经元，并通过激活强度和下游影响来量化功能显著性。然后，通过在每种语言内部取这些单模态神经元的交集，我们提取出对视觉和文本风险均有响应的模态共享安全神经元（MS-Neurons），从而弥合了模态间的安全表示差距。此外，以英语作为语义锚点，我们跨语言取 MS-Neurons 的交集，以识别模态和语言共享的安全神经元（MLS-Neurons），作为抵御复合攻击的关键防御手段。最后，我们仅更新这一极小部分的共享神经元子集（约占参数的 0.03%），将仅限英语的安全监督迁移到多语言和多模态场景中。广泛的实验表明，我们的方法在多种多语言和多模态安全基准测试中显著优于现有最先进的方法，同时保持了通用效用。

## Abstract
As large vision-language models (LVLMs) are deployed globally, the combination of multilingual instructions and visual information makes malicious attacks more covert and sophisticated than ever before. However, existing methods isolate language and modality defenses, which, coupled with the scarcity of safety data and high fine-tuning costs, makes it difficult for models to defend against compound attacks. To address this severe challenge, we propose a neuron-level cross-dimensional safety alignment framework driven by modality- and language-shared safety neurons (MLS-Neurons). First, we identify monolingual and unimodal safety neurons by comparing responses to harmful and benign samples, quantifying functional saliency through activation strength and downstream impact. Then, by intersecting these unimodal neurons within each language, we extract modality-shared safety neurons (MS-Neurons) responsive to both visual and textual risks, bridging the safety representation gap between modalities. Furthermore, using English as a semantic anchor, we intersect MS-Neurons across languages to identify modality- and language-shared safety neurons (MLS-Neurons), serving as key defenses against compound attacks. Finally, we update only this minimal subset of shared neurons (~0.03% of parameters), transferring English-only safety supervision to multilingual and multimodal scenarios. Extensive experiments show that our method significantly outperforms state-of-the-art approaches across diverse multilingual and multimodal safety benchmarks while preserving general utility.

---

## 论文详细总结（自动生成）

这篇论文针对大视觉语言模型（LVLMs）在多语言和多模态环境下的安全对齐问题，提出了一个创新的神经元级对齐框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 LVLM 在全球部署，攻击者开始利用**多语言指令与视觉信息结合**的“复合攻击”（如用非英语指令配合有害图像）。
*   **现有挑战：** 现有的安全防御方法通常将语言和模态隔离处理；此外，高质量的多语言多模态安全数据极度匮乏，且全参数微调成本高昂。
*   **核心目标：** 寻找一种高效、可解释且低成本的方法，利用有限的英语安全数据，实现跨语言和跨模态的统一安全对齐。

### 2. 方法论
论文提出了基于**模态与语言共享安全神经元（MLS-Neurons）**的对齐框架，核心步骤如下：
*   **神经元探测（Neuron Probing）：** 通过对比有害样本（图像主导风险 IR 和文本主导风险 TR）与良性样本的响应，识别出单语言/单模态的安全神经元。
*   **显著性评分：** 定义了一个显著性指标，结合了神经元的激活强度和下投影矩阵的幅度，以量化其对模型输出的影响。
*   **跨维度提取：**
    1.  **模态共享（MS-Neurons）：** 在每种语言内部，取图像安全神经元和文本安全神经元的交集。
    2.  **语言共享（MLS-Neurons）：** 以英语为“语义锚点”，将其他语言的 MS-Neurons 与英语的 MS-Neurons 取交集，最终获得在不同模态和语言下均起作用的极少量核心神经元。
*   **目标对齐微调（MLS-Targeted Tuning）：** 仅更新这部分 MLS-Neurons（约占总参数的 **0.03%**），使用仅有的英语安全数据进行训练，从而将安全能力迁移至全球语言和多模态场景。

### 3. 实验设计
*   **模型：** Qwen3-VL-8B-Instruct、Llama-3.2-11B-Vision-Instruct、Gemma-3-4B-Instruct。
*   **数据集：**
    *   **安全评估：** Lingua-SafetyBench（涵盖 10 种语言，区分 IR 和 TR 风险）。
    *   **通用能力：** MMMU（多模态理解）、MGSM（多语言推理）、MM-Vet（综合能力）。
    *   **OOD/越狱测试：** FigStep、SPA-VL、MultiJail。
*   **对比方法：** 
    *   多模态防御：ESCO、ASTRA。
    *   多语言对齐：XSAFETY、MLC。
    *   其他：Self-Defense、原始模型、全参数微调、LoRA。

### 4. 资源与算力
*   **硬件：** 实验使用了 **1 张 NVIDIA A800 GPU**。
*   **训练细节：** 训练 3 个 epoch，针对不同模型设置了不同的学习率（如 Qwen 为 8e-4）。由于仅更新 0.03% 的参数，训练效率极高。

### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主实验：** 在 3 个主流模型家族上验证了 6 种分布内语言的安全性能。
*   **泛化性实验：** 测试了 4 种完全未见过的（OOD）语言以及 3 个独立的越狱攻击基准测试。
*   **消融实验：** 包括神经元掩码干预（验证 MLS-Neurons 的功能性）、不同训练策略对比（全量 vs LoRA vs 随机）、Top-K 比例敏感性分析、锚点语言选择分析。
*   **副作用评估：** 专门测试了通用能力保留情况和“过度拒绝”（Over-refusal）率。
*   **客观性：** 引入了 Qwen-Guard、Llama-Guard 和人工评估三种方式，确保安全判定的一致性和客观性。

### 6. 主要结论与发现
*   **MLS-Neurons 的存在：** 模型内部确实存在极少量的神经元，能够跨越语言和模态一致地响应安全风险。
*   **高效迁移：** 仅通过更新 0.03% 的参数，就能将英语安全准则成功迁移到阿拉伯语、芬兰语等非英语场景，且效果优于全参数微调。
*   **性能平衡：** 该方法在大幅降低攻击成功率（ASR）的同时，几乎不损害模型的通用多模态理解和推理能力，且不会导致严重的过度拒绝。

### 7. 优点
*   **极高效率：** 极小的参数更新量（0.03%）极大降低了对齐成本。
*   **强解释性：** 从机械可解释性（Mechanistic Interpretability）角度切入，揭示了模型内部的安全作用机制。
*   **跨维度统一：** 成功解决了多语言和多模态防御长期以来被割裂处理的问题。
*   **零样本迁移：** 证明了英语作为安全锚点的强大迁移能力，对缺乏安全数据的小语种具有重要意义。

### 8. 不足与局限
*   **文化差异覆盖：** 虽然实现了语言迁移，但对于特定文化背景下的微妙安全定义（如特定地区的社会习俗）可能仍需更细致的研究。
*   **模型架构限制：** 目前主要针对基于 FFN 结构的 Transformer 模型，对于其他新兴架构的适用性有待验证。
*   **动态攻击：** 实验主要针对静态基准，面对不断进化的自适应越狱攻击，该方法的长期鲁棒性仍需观察。

（完）
