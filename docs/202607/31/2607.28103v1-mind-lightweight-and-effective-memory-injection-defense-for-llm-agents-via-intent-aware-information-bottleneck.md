---
title: "MIND: Lightweight and Effective Memory Injection Defense for LLM Agents via Intent-Aware Information Bottleneck"
title_zh: MIND：通过意图感知信息瓶颈实现的轻量级且有效的大语言模型智能体内存注入防御
authors: "Dongyi Liu, Haixing He, Xiaobao Wu, Jia Li"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28103v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 大语言模型智能体内存注入攻击防御
tldr: 针对基于记忆增强的LLM智能体易受记忆注入攻击的问题，本文提出了一种轻量级防御框架MIND。该方法利用意图感知信息瓶颈（IB）提取初始意图与多轮行为间的紧凑表示，在过滤冗余信息的同时保留攻击信号，并通过轻量级检测器识别恶意记忆。实验证明，MIND在保持任务准确率和推理效率的同时，显著降低了攻击成功率。
selection_source: fresh_fetch
motivation: 现有的防御机制在处理多轮对话时存在计算开销大或信息冗余的问题，难以有效应对记忆注入攻击。
method: 提出MIND框架，通过意图感知信息瓶颈提取意图与行为的紧凑表示，并利用轻量级检测器识别恶意记忆。
result: "在ReAct-StrategyQA等数据集上，MIND显著降低了攻击成功率（ASR-r/a降低约55%），且未损失任务准确率和推理速度。"
conclusion: MIND是一种高效且轻量级的防御方案，能够有效抵御LLM智能体的记忆注入攻击，同时兼顾性能与效率。
---

## 摘要
增强内存的基于大语言模型（LLM）的智能体容易受到内存注入攻击：智能体可能会从攻击者处检索到被投毒的内存，从而使其行为偏离初始用户意图并最终导致任务失败。然而，现有的防御机制要么产生高昂的计算成本，要么在多轮对话语境中面临信息冗余的问题。为了应对这些挑战，我们提出了内存意图感知神经去噪（MIND），这是一种针对内存注入攻击的轻量级防御框架。我们的初步分析表明，良性轨迹和投毒轨迹在初始用户意图与后续行为之间表现出可区分的关系。基于这一观察，MIND 采用了一种意图感知的信息瓶颈（IB），从初始意图和轮次级行为中提取紧凑的意图-行为表示。该信息瓶颈在过滤掉与任务无关且重复的信息的同时，保留了与意图相关的跨轮次攻击信号，并由一个轻量级检测器从生成的表示中识别恶意内存。因此，MIND 在减轻多轮对话语境中信息冗余的同时，避免了重复进行 LLM 审计的开销。广泛的实验表明，MIND 在保持任务准确性和推理效率的同时，降低了攻击成功率。值得注意的是，在 ReAct-StrategyQA 数据集上，MIND 分别将平均 ASR-r 和 ASR-a 降低了 55.4% 和 55.3%，同时在平均准确率和延迟方面与未受防御的智能体持平。

## Abstract
Memory-augmented LLM-based agents are vulnerable to memory injection attacks: Agents may retrieve poisoned memory from attackers, which diverts their behavior from initial user intent and finally causes task failure. However, existing defense mechanisms either incur high computational cost or suffer from information redundancy in multi-turn contexts. To address these challenges, we propose Memory Intent-Aware Neural Denoising(MIND), a lightweight defense framework for memory injection attack. Our preliminary analysis reveals that benign and poisoned trajectories exhibit distinguishable relationships between the initial user intent and subsequent behavior. Building on this observation, MIND employs an intent-aware Information Bottleneck(IB) to extract compact intent--behavior representations from the initial intent and turn-level behavior. The IB preserves intent-relevant cross-turn attack signals while filtering task-irrelevant and repetitive information, and a lightweight detector identifies malicious memories from the resulting representations. As such, MIND mitigates information redundancy in multi-turn contexts while avoiding the overhead of repeated LLM auditing. Extensive experiments show that MIND reduces attack success rates while preserving task accuracy and inference efficiency. Notably, on ReAct-StrategyQA, MIND reduces mean ASR-r and ASR-a by 55.4% and 55.3%, respectively, while matching the undefended agent in average accuracy and latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MIND** 的轻量级防御框架，旨在保护记忆增强型大语言模型（LLM）智能体免受内存注入攻击。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：为了处理长时程任务，LLM 智能体通常依赖外部检索记忆系统。然而，攻击者可以通过直接或间接方式向记忆库注入恶意记录（内存注入攻击），诱导智能体偏离用户初始意图，导致任务失败或产生有害输出。
*   **核心问题**：现有的防御手段存在两大瓶颈：
    1.  **计算开销高**：基于 LLM 推理的审计方法（如重复调用模型检查记忆）在多轮对话中延迟巨大。
    2.  **信息冗余**：轻量级检测器在处理多轮轨迹时，容易被大量与任务无关的重复信息干扰，掩盖了微弱的攻击信号。

### 2. 方法论
MIND 的核心思想是将内存防御建模为一个基于**信息瓶颈（Information Bottleneck, IB）**的去噪过程。
*   **特征提取**：使用预训练 LLM（如 Llama-3.1-8B）提取每一轮对话的最后 Token 隐藏状态作为行为表示 $h_t$，并提取初始查询作为意图表示 $h_0$。
*   **意图感知信息瓶颈（IB）**：
    *   **压缩性（Compactness）**：通过变分推断将输入表示压缩到潜在空间，利用 KL 散度损失过滤掉轨迹中的冗余和无关信息。
    *   **信息性（Informativeness）**：引入意图锚点损失（Alignment Loss），将良性轮次的表示向初始意图 $h_0$ 靠拢，而将投毒轮次推离，从而强化意图相关的攻击信号。
*   **多超平面分类器（Multi-hyperplane Classifier）**：在去噪后的潜在空间上构建由 $K$ 个超平面组成的决策边界。相比单一线性边界，这种分段线性设计能更好地捕捉复杂的恶意攻击模式。
*   **防御流程**：在推理时，MIND 对检索到的记忆进行评分，仅允许被判定为良性的记忆进入智能体上下文或写入记忆库。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：使用 EHR（电子健康记录）和通用 QA 智能体轨迹。
    *   **测试集（跨领域迁移）**：ReAct-StrategyQA（复杂推理）和 MMLU（多任务理解）。
*   **基准模型（Backbones）**：DeepSeek-V4, GPT-4o-mini, Llama-3.1-8B-Instruct, Qwen3-8B-Instruct。
*   **对比方法**：包括基于 LLM 的审计（LLM Auditor, A-MemGuard）、轻量级分类器（DistilBERT）、困惑度过滤（PPL）、序列监控（Sequential Monitor）和注意力方差过滤（AV Filter）。
*   **评估指标**：良性任务准确率（ACC）、检索阶段攻击成功率（ASR-r）、智能体推理阶段攻击成功率（ASR-a）以及推理延迟（Time）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用冻结的 Llama-3.1-8B 作为特征提取器，分类器部分是轻量级的 MLP 和线性层，暗示其训练和推理对算力的需求远低于微调整个 LLM 或频繁调用大模型 API。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流模型后端，在 2 个核心数据集上进行了详尽测试，并针对 9 种不同的任务-受害者配置取平均值。
*   **消融实验**：对 IB 模块、多超平面分类器以及超参数（$\beta, \lambda$）敏感度进行了独立验证。
*   **充分性评价**：实验设计较为全面，通过 t-SNE 可视化直观展示了去噪效果，对比了从闭源到开源的多种模型，证明了方法的通用性和客观性。

### 6. 主要结论与发现
*   **防御有效性**：在 StrategyQA 上，MIND 将 ASR-r 和 ASR-a 分别降低了约 **55.4%** 和 **55.3%**。
*   **性能保持**：在大幅降低攻击成功率的同时，MIND 几乎没有损失良性任务的准确率（甚至在某些配置下有所提升）。
*   **高效率**：MIND 的推理延迟与未加防御的智能体基本持平，比最强的 LLM 审计防御快 **20.6%** 以上。
*   **意图漂移观察**：实验证实，在受到攻击时，智能体对初始意图的注意力会随轮次增加而显著下降，而 MIND 有效缓解了这一现象。

### 7. 优点
*   **轻量且高效**：避免了昂贵的 LLM 重复调用，适合生产环境。
*   **去噪能力强**：利用信息瓶颈理论解决了多轮对话中的信息冗余问题。
*   **即插即用**：作为外部防御层，无需对智能体模型进行微调，且对闭源模型友好。

### 8. 不足与局限
*   **代理模型依赖**：特征提取依赖于一个特定的代理模型（如 Llama-3.1），如果代理模型与实际智能体模型在表示空间上差异过大，可能会影响效果。
*   **攻击多样性覆盖**：虽然在现有攻击（AgentPoison, MINJA）上表现良好，但对于未来可能出现的、专门针对 IB 结构的对抗性注入攻击，其鲁棒性尚待验证。
*   **应用限制**：主要针对文本记忆，对于包含多模态信息的记忆增强智能体，该框架需要进一步扩展。

（完）
