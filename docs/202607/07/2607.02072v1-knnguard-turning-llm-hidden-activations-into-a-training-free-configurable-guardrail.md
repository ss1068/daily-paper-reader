---
title: "kNNGuard: Turning LLM Hidden Activations into a Training-Free Configurable Guardrail"
title_zh: kNNGuard：将大语言模型隐藏层激活转化为无需训练的可配置护栏
authors: "Mahmoud Abdelfattah, Hamid Nasiri, Peter Garraghan"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02072v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于检测不安全和对抗性提示的免训练护栏
tldr: 针对现有大模型护栏（Guardrail）微调成本高、泛化性差及推理延迟大的问题，本文提出kNNGuard。这是一种无需训练的护栏方案，通过提取预训练模型在少量示例下的隐藏层激活值，结合多层kNN算法融合激活空间与嵌入空间得分进行分类。该方法在保持高精度的同时，显著提升了推理速度和领域迁移效率，为生产环境提供了可配置且低延迟的安全保障。
selection_source: fresh_fetch
motivation: 现有的基于微调的LLM护栏存在泛化能力弱、推理延迟高以及领域适配成本昂贵等缺陷。
method: 利用预训练LLM的隐藏层激活值，通过多层kNN算法融合激活空间与嵌入空间特征，实现无需训练的提示词分类。
result: 在六个领域的测试中，kNNGuard的F1分数优于或等同于微调模型，且推理速度提升了2.7至10倍，领域适配仅需不到10秒。
conclusion: kNNGuard证明了利用LLM内部激活空间构建高效、灵活且无需训练的护栏系统的可行性与优越性。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在需要护栏以检测不安全、偏离主题或对抗性提示词的领域。现有的护栏主要依赖微调来构建分类器，这通常面临泛化能力低和推理延迟高的问题。我们提出了 kNNGuard，这是一种利用现成 LLM 激活空间的无需训练的护栏。给定一个包含 50 个安全和不安全提示词的小型库，kNNGuard 提取隐藏层激活，并执行多层 kNN，融合激活空间和嵌入空间的分数进行分类。在涵盖主题和安全提示词的六个领域中，kNNGuard 与经过微调的最先进护栏相比，实现了具有竞争力或更优的 F1 分数，同时运行速度比最佳同类护栏快 2.7 倍，比经过微调的安全分类器快 10 倍，且无需梯度更新或微调。领域自适应仅需更新带标签的库，该库可在 10 秒内构建完成，比现有护栏快几个数量级。我们还分析了系统提示词、层选择以及作为可配置、低延迟护栏集成到生产级 LLM 流水线中的影响。

## Abstract
Large language models (LLMs) are increasingly deployed in domains requiring guardrails to detect unsafe, off-topic, or adversarial prompts. Existing guardrails predominately rely on fine-tuning to build classifiers, which often suffer from low generalization and high inference latency. We present kNNGuard, a training-free guardrail that utilizes the activation space of an off-the-shelf LLM. Given a small bank of 50 safe and unsafe prompts, kNNGuard extracts hidden activations and performs multi-layer kNN fusing activation-space and embedding-space scores for classification. Across six domains spanning topical and security prompts, kNNGuard achieves competitive or superior F1 compared to fine-tuned state-of-the-art guardrails while running 2.7x faster than the best comparable guardrail, and 10x faster than a fine-tuned safety classifier without gradient updates or fine-tuning. Domain adaptation requires only updating the labeled bank, which can be constructed in under 10 seconds and several orders of magnitude faster than established guardrails. We also analyze the impact of system prompts, layer selection, and integration into production LLM pipelines as a configurable, low-latency guardrail.

---

## 论文详细总结（自动生成）

以下是对论文《kNNGuard: Turning LLM Hidden Activations into a Training-Free Configurable Guardrail》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何构建一个既能精准识别不安全/偏离主题的提示词（Prompt），又具备低延迟、强泛化能力且易于快速部署的 LLM 护栏（Guardrail）系统？
*   **研究背景**：
    *   **微调分类器（如 Llama Guard）**：虽然准确率高，但训练成本昂贵，对分布外（OOD）攻击泛化性差，且推理延迟高。
    *   **嵌入向量相似度（Embedding-kNN）**：推理极快，但仅停留在语义表面，难以处理复杂的安全边界或对抗性攻击。
*   **动机**：利用 LLM 内部隐藏层激活值（Hidden Activations）中蕴含的丰富安全特征，构建一个无需训练、可动态配置的非参数化护栏。

### 2. 方法论
kNNGuard 的核心思想是将冻结的 LLM 视为特征提取器，通过对比输入与少量示例库的内部激活状态来进行分类。
*   **核心流程**：
    1.  **库构建阶段（Bank Building）**：选取少量（如每类 50 个）带标签的示例（安全 vs 不安全），通过 LLM 提取指定隐藏层的激活值并缓存。
    2.  **推理阶段（Inference）**：
        *   **激活空间视图（kNNGuard LE）**：提取输入提示词的多层激活值，利用 **Fisher 判别得分** 对不同层进行加权（区分度高的层权重更大），计算与示例库的余弦距离 kNN 得分。
        *   **嵌入空间视图（Embedding-kNN）**：同时利用轻量级模型（如 MiniLM）计算语义嵌入的 kNN 得分。
    3.  **自适应融合（Adaptive Fusion）**：根据两个视图的置信度差距（Confidence Gap）决定最终决策。如果两者分歧大，则采用“胜者全得”策略（信任更自信的那个）；如果分歧小，则进行加权融合。
*   **关键技术**：
    *   **无需训练**：不更新模型参数，仅需前向传播。
    *   **多层融合**：捕捉 LLM 不同深度的抽象特征。
    *   **快速适配**：更换护栏领域只需更新示例库，无需重新微调。

### 3. 实验设计
*   **数据集与场景**：涵盖 6 个领域（代码指令、代码输出、医疗、通用安全、越狱攻击、提示词注入），共使用 16 个不同的数据集进行库构建和评估。
*   **Benchmark 与对比方法**：
    *   **微调模型**：Llama Nemotron Topic Guard V1、Nemotron Safety Guard V2、Llama Guard 3。
    *   **轻量级分类器**：Llama Prompt Guard 2。
    *   **基准方法**：标准的 Embedding-kNN。
*   **评估指标**：F1 分数、召回率、误报率（FPR）、漏报率（FNR/ASR）、推理延迟。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 6000 Ada Generation GPU**。
*   **时间成本**：
    *   **库构建**：构建一个包含 100 个样本的示例库仅需 **7.95 秒**。
    *   **对比微调**：相比 LoRA 微调（约 7 小时），kNNGuard 的适配速度提升了 **3156 倍**。
    *   **推理延迟**：平均每条提示词处理时间为 **45.9 毫秒**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 6 种不同的 LLM 后干模型（Llama-3.1-8B, Mistral-7B, Phi-4-mini, Gemma-4-12B 等），证明了方法的通用性。
    *   进行了样本数量（n=50 到 1000）和 k 值（1 到 21）的消融实验。
    *   通过 t-SNE 可视化展示了激活空间相比嵌入空间具有更优的类别可分离性。
*   **公平性与客观性**：实验采用了“留一法”交叉验证来选择超参数，且评估集与示例库来源完全独立，有效避免了数据泄露和过拟合偏见。

### 6. 主要结论与发现
*   **性能优异**：kNNGuard FE 在六个领域实现了 **87.4% 的平均 F1 分数**，优于或等同于专门微调的护栏模型。
*   **速度优势**：推理速度比 NVIDIA Nemotron 快 **2.7 倍**，比 Safety Guard V2 快 **10 倍**。
*   **系统提示词的影响**：加入简单的领域系统提示词（System Prompt）能显著降低误报率（降低 66%），使激活空间边界更清晰。
*   **安全性**：在越狱和注入攻击检测中，kNNGuard 表现出比专门训练的 Prompt Guard 2 更稳健的错误分布平衡。

### 7. 优点
*   **极速适配**：在生产环境中，针对新威胁或新业务领域，可在 10 秒内完成护栏更新。
*   **资源友好**：无需昂贵的微调算力，直接复用现有推理架构。
*   **可解释性**：通过 Fisher 得分可以量化哪些层对安全决策贡献最大，且激活空间几何结构更易于可视化分析。
*   **配置灵活**：支持通过调整示例库和阈值来精细化控制护栏的“松紧度”。

### 8. 不足与局限
*   **示例库依赖**：分类效果高度依赖于示例库的质量和代表性，若库中存在模糊样本，可能导致误报增加。
*   **推理开销**：虽然比微调模型快，但仍需一次 LLM 的前向传播（提取激活值），相比纯 Embedding 方法（4ms）仍有量级上的延迟差距。
*   **长文本限制**：实验主要集中在提示词（Prompt）层面，对于超长文档或多轮对话的激活空间动态变化研究尚不充分。
*   **白盒风险**：如果攻击者已知护栏基于特定层的激活值，可能会针对性地构造“激活空间规避”攻击。

（完）
