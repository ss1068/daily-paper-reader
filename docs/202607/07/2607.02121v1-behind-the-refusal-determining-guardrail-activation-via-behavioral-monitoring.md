---
title: "Behind the Refusal: Determining Guardrail Activation via Behavioral Monitoring"
title_zh: 拒绝的背后：通过行为监测判定护栏激活
authors: "William Hackett, Peter Garraghan"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02121v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 检测并拦截恶意指令的护栏系统
tldr: "本研究针对大语言模型（LLM）安全防护中难以区分“外部护栏拦截”与“模型内部拒绝”的问题，提出了首个黑盒护栏侦察方法。该方法通过监控HTTP响应、词汇特征及时间信号等行为特征，在无需先验知识的情况下探测护栏的存在。实验证明，该方法能以100%的准确率识别护栏，并能以98%的F1分数区分护栏拦截与模型拒绝，为对抗性模拟和AI安全评估提供了关键的技术支持。"
selection_source: fresh_fetch
motivation: 在黑盒测试中准确区分护栏拦截与模型自身拒绝，对于选择和优化针对AI系统的攻击绕过技术至关重要。
method: 提出一种基于HTTP信号、词汇特征和响应时间监控的黑盒侦察方法，用于探测目标系统中护栏的激活情况。
result: "实验显示该方法探测护栏的准确率达100%，且在区分护栏拦截与模型拒绝任务中取得了98%的平均F1分数。"
conclusion: 通过行为监控可以有效识别黑盒AI系统的护栏机制，显著提升了对生产环境AI安全架构的透明度和评估能力。
---

## 摘要
随着大语言模型（LLM）和智能体系统被集成到现实世界的应用中，确保其安全性和可靠性至关重要。护栏系统能够检测并拦截发送至 LLM 或由其发出的恶意指令，是 AI 安全的重要组成部分。然而，针对生产环境中的 AI 系统进行黑盒对抗模拟的研究人员，往往难以确定发生的究竟是护栏拦截还是 LLM 自身的拒绝。这种区分非常重要，因为绕过护栏的技术与绕过 LLM 安全对齐的技术可能存在实质性差异，并对攻击技术的选择和优化产生重大影响。我们提出了首个黑盒护栏侦察方法，该方法在仅假设黑盒访问且对护栏或 AI 系统零先验知识的情况下，通过对 HTTP、词法和时序信号的行为监测，检测目标 AI 系统中是否存在护栏。实验表明，我们的方法能够以 100% 的准确率检测护栏的存在，且良性与恶意交互之间存在显著的行为差异（q < 0.001）。我们的方法还能进一步识别护栏旨在拦截的内容类别，并在未见过的提示词上以 98% 的平均 F1 分数区分护栏拦截与 LLM 拒绝。

## Abstract
As Large Language Models (LLMs) and agentic systems become integrated into real-world applications, ensuring their safety and security is critical. Guardrail systems that detect and block malicious instructions sent to and from an LLM are an essential component of AI security. However, researchers conducting black-box adversarial emulation against production AI systems often struggle to determine whether a guardrail block or an LLM rejection has occurred. This distinction is important because the techniques used to bypass guardrails can differ substantially from those used to bypass LLM safety alignment, and has a material impact on attack technique selection and optimization. We propose the first black-box guardrail reconnaissance methodology, which detects the presence of a guardrail within a target AI system through behavioral monitoring of HTTP, lexical, and timing signals, assuming only black-box access and zero prior knowledge of the guardrail or AI system. Experiments demonstrate that our approach detects guardrail presence with 100% accuracy, with statistically significant behavioral separation between benign and malicious interactions (q < 0.001). Our approach further identifies the content categories a guardrail is designed to block, and distinguishes guardrail blocks from LLM rejection on unseen prompts with an average F1 score of 98%.

---

## 论文详细总结（自动生成）

这是一份关于论文《Behind the Refusal: Determining Guardrail Activation via Behavioral Monitoring》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在生产环境中，大语言模型（LLM）通常由“外部护栏（Guardrail）”和“内部安全对齐（Safety Alignment）”双重保护。当系统拒绝回答时，黑盒测试者难以分辨是护栏拦截了输入，还是模型自身拒绝了生成。
*   **核心问题**：由于绕过护栏（如规避检测算法）与绕过模型对齐（如越狱攻击）的技术路径完全不同，无法区分两者会导致攻击策略优化效率低下。
*   **整体含义**：本文提出了首个在零先验知识、纯黑盒环境下的护栏侦察方法，通过监测系统行为特征来判定护栏的激活状态及其拦截特性。

### 2. 方法论
该方法包含四个阶段，旨在通过对比良性与恶意交互的行为差异来识别护栏：
1.  **响应收集（Response Gathering）**：向目标 API 发送两组提示词：**良性提示词集**（建立正常行为基准）和**恶意提示词集**（涵盖提示词注入、越狱、毒性内容，旨在触发拦截）。
2.  **特征提取（Feature Extraction）**：从 HTTP 响应中提取三类信号：
    *   **HTTP 信号**：状态码（如 403/422）、响应头（如自定义安全头）、响应体结构（如错误对象）。
    *   **词汇信号（Lexical）**：使用 LLM 判定响应是否包含特定的拦截用语，以及响应内容的重复率（模板化响应通常具有极高的重复性）。
    *   **时序信号（Timing）**：总耗时（Elapsed Time）和单 Token 耗时（TPT）。护栏拦截通常会跳过 LLM 生成，导致耗时显著缩短。
3.  **行为监测（Behavioral Monitoring）**：利用统计检验（连续特征用 KS 检验，分类特征用 Fisher 精确检验）对比良性与恶意样本的分布差异。通过 Benjamini-Hochberg 校正计算 q 值，判定是否存在显著的行为偏移。
4.  **特征识别（Characteristic Identification）**：根据触发的信号生成“护栏指纹”，识别护栏针对的内容类别（如仅拦截毒性内容或拦截所有攻击）。

### 3. 实验设计
*   **实验对象**：涵盖了 9 种主流护栏（如 OpenAI Omni Moderation, Meta Prompt Guard v2, Azure Content Safety, LLMGuard 等）和 3 种底层 LLM（GPT-4.1, Claude-Sonnet-4.6, Gemini-2.5-Flash）。
*   **拦截模式**：构建了 6 种常见的护栏拦截表现（如连接断开、非 200 状态码、静默拦截、带警告头的拦截等）。
*   **数据集**：
    *   良性集：自定义通用提示词。
    *   恶意集：PI Guard（注入与越狱）、Nvidia Aegis（安全违规内容）。
*   **Benchmark 与对比方法**：
    *   **HTTP Recon**：仅利用 HTTP 层特征。
    *   **LLM Judge**：仅利用 LLM 判定响应文本。
    *   **No-guardrail Control**：无护栏的基准组，用于测试误报率。

### 4. 资源与算力
*   **硬件环境**：实验运行在 Linux Ubuntu 20.04 服务器上，配置为：
    *   **CPU**：Intel Xeon Gold 6336Y。
    *   **内存**：128GB RAM。
    *   **GPU**：2 台 NVIDIA A100 (80GB VRAM)。
*   **训练/运行时间**：文中未明确提及具体的运行总时长，但指出侦察过程仅需 40 个提示词即可完成，暗示该方法在推理阶段极具效率。

### 5. 实验数量与充分性
*   **实验规模**：共测试了 **162 种唯一的 AI 目标配置**（9 护栏 × 6 模式 × 3 LLM）。
*   **验证集**：使用 100 个未见过的提示词来验证生成的“指纹”在区分拦截与拒绝时的泛化能力。
*   **充分性评价**：实验设计非常全面，不仅覆盖了多种商业和开源护栏，还考虑了不同的底层模型。通过引入“无护栏控制组”和统计学显著性分析（q < 0.001），确保了结果的客观性和科学性。

### 6 主要结论与发现
*   **高准确性**：该方法检测护栏存在的准确率达到 **100%**。
*   **强区分度**：在区分“护栏拦截”与“模型内部拒绝”的任务中，平均 **F1 分数达到 98%**，远超单纯依靠 HTTP 特征（66%）或 LLM 判定（81%）的方法。
*   **信号可靠性**：HTTP 和词汇信号是最可靠的指标；时序信号虽然显著，但在无护栏情况下可能因模型生成短文本而产生误报（需作为辅助信号）。
*   **类别识别**：能够准确识别护栏设计的拦截范畴（如区分该护栏是针对注入还是针对毒性内容）。

### 7. 优点
*   **现实性强**：采用了最严格的黑盒威胁模型，不假设攻击者了解任何系统架构。
*   **多维融合**：首次结合了网络层（HTTP）、语义层（词汇）和物理层（时序）的信号。
*   **低成本**：仅需极少量的探测请求（40 次）即可完成指纹提取。
*   **实用价值**：为对抗性模拟提供了关键的反馈信号，帮助研究者决定是该更换越狱模板还是优化规避策略。

### 8. 不足与局限
*   **时序噪声**：网络波动或模型负载可能干扰时序信号，导致在极少数情况下难以仅凭时间区分。
*   **类别重叠**：在区分“提示词注入”和“越狱”时，由于两者内容特征存在天然重叠，护栏的分类识别可能出现弱信号（如 Protect AI 案例）。
*   **对抗性防御**：如果系统故意引入随机延迟或伪造 LLM 风格的拦截响应，该方法的效力可能会受到挑战（尽管目前生产环境很少这样做）。

（完）
