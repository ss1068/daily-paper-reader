---
title: "Cognitive Firewall: A Proactive, Zero-Trust, Multi-Gate Framework for LLM Safety"
title_zh: 认知防火墙：一种用于大语言模型安全的主动式、零信任、多门控框架
authors: "Michele Guida, Ruslan Shikhhamzayev, Sindhuja Penchala, Stefano Iannucci, Jiacheng Li, Shahram Rahimi, Noorbakhsh Amiri Golilarz"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01277v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 大语言模型安全的主动运行时监管框架
tldr: 针对大语言模型在多轮对话中易受诱导产生有害内容的问题，本文提出“认知防火墙”（Cognitive Firewall）。该框架在用户与目标模型间引入独立监管模型，通过意图、零信任上下文、一致性和输出风险四个维度进行多门控评估。实验证明，该方法能有效拦截单轮、多轮及人为构造的攻击，在显著降低攻击成功率的同时保持了较低的误拒率，提升了LLM的安全性和可审计性。
selection_source: fresh_fetch
motivation: 现有的运行时防护机制通常孤立评估单条消息，难以识别多轮对话中累积的有害意图或虚假授权。
method: 提出一种包含意图识别、零信任上下文验证、多轮一致性检测和输出风险检查的四门控主动监管框架。
result: "在多个基准测试中将攻击成功率降至2%以下（最难的人工攻击集降至14%），同时误拒率仅为8%。"
conclusion: 通过分解式的对话级监管，认知防火墙为大语言模型提供了更强的主动防御能力和透明的审计依据。
---

## 摘要
大语言模型（LLMs）可能会被诱导通过多轮策略产生有害内容，而在这些策略中，没有任何一条单一的用户消息表现出明显的安全性问题。现有的运行时防护措施通常将提示词或响应视为孤立的消息进行评估，这限制了它们恢复累积意图、验证声称的权限或检测分解在对话中的有害目标的能力。本文提出了“认知防火墙”（Cognitive Firewall），这是一种主动的运行时监管框架，在用户和受保护的目标模型之间介入了一个独立的监管模型。该框架将安全评估分解为四个类别门控：意图门控（识别请求的操作目标）、零信任上下文门控（将声称的角色和权限视为未经证实的证据）、一致性门控（检测跨轮次的升级和分解）以及输出风险门控（在发布前检查候选响应）。门控决策通过升级机制而非分数平均进行组合，允许任何置信的危险信号阻断交互，同时保留可审计的依据。在四个越狱基准测试和一个良性安全测试集上的实验表明，认知防火墙显著降低了单轮、多轮、基于权限以及人工设计的攻击成功率。它在三个攻击集上将攻击成功率降低到 2% 或以下，在最困难的人工设计攻击集上降低到 14%，同时保持 8% 的过度拒绝率。这些结果表明，分解的、对话层面的监管可以提高大语言模型安全的主动遏制能力和可审计性。

## Abstract
Large language models (LLMs) can be induced to produce harmful content through multi turn strategies in which no single user message appears clearly unsafe. Existing runtime safeguards commonly evaluate prompts or responses as isolated messages, which limits their ability to recover ac-cumulated intent, verify asserted authority, or detect harmful objectives decomposed across a dialogue. This paper presents the Cognitive Firewall, a proactive runtime oversight framework that interposes an independent oversight model between a user and a protected target mod l. The framework decomposes safety assessment into four categorical gates: an intent gate that identi-fies the operational objective of a request, a zero trust context gate that treats claimed roles and permissions as unverified evidence, a consistency gate that detects escalation and decomposition across turns, and an output risk gate that inspects candidate responses before release. Gate decisions are combined through escalation rather than score averaging, allowing any confident danger signal to block an interaction while preserving an auditable rationale. Experiments on four jailbreak benchmarks and a benign safety test set show that the Cognitive Firewall substantially reduces attack success across single turn, multi turn, authority based, and human crafted attacks. It lowers attack success to 2 percent or below on three attack sets and to 14 percent on the most difficult human crafted set, while maintaining an 8 percent over refusal rate. These results indicate that decomposed, conversation level oversight can improve proactive containment and auditability for LLM safety.

---

## 论文详细总结（自动生成）

### 论文总结：认知防火墙（Cognitive Firewall）

#### 1. 核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）的防护机制（如 Llama Guard）大多基于“单条消息分类”逻辑，即孤立地评估每一条提示词或响应。
*   **核心挑战**：攻击者可以通过多轮对话（如 Crescendo 攻击）或将有害目标分解为多个看似无害的子问题（如 ActorAttack）来绕过防御。此外，用户常通过虚假声称身份（如“我是系统管理员”）来骗取高权限响应。
*   **整体含义**：本文提出“认知防火墙”，主张将 LLM 安全视为一个“理解问题”而非简单的“分类问题”。它在用户与目标模型之间建立一个独立的监管层，通过多维度、跨轮次的深度分析实现主动防御。

#### 2. 方法论
核心思想是引入一个独立的监管模型，通过**四个门控（Gates）**对交互进行分解评估，并采用**升级决策规则（Escalation Rule）**。
*   **关键技术细节（四门控）**：
    1.  **意图门控 (G1 - Intent)**：识别请求的真实物理目标（如“合成某种物质”），剥离虚构或研究性的外壳，若目标本身有害则在生成前拦截。
    2.  **零信任上下文门控 (G2 - Zero-trust Context)**：遵循“从不信任，始终验证”原则，将用户自称的角色、权限或策略更改视为未经证实的证据，拦截具有操纵性的身份伪装。
    3.  **一致性门控 (G3 - Consistency)**：审计全量对话历史，检测意图是否从良性逐渐向有害“升级”（Crescendo）或任务是否被跨轮次分解。
    4.  **输出风险门控 (G4 - Output Risk)**：作为最后一道防线，在响应发布前检查其是否包含实质性的有害载荷。
*   **算法流程**：前三个门控在目标模型生成前运行。只要任何一个门控给出“危险”判定，系统立即阻断交互并给出可审计的理由；只有通过前三关的请求才会发送给目标模型，最后由 G4 检查输出。

#### 3. 实验设计
*   **数据集/场景**：
    *   `jbb`: 单轮直接攻击（JailbreakBench）。
    *   `crescendo`: 多轮升级攻击。
    *   `safemt`: 多轮分解攻击（ActorAttack）。
    *   `mhj`: 人工编写的复杂越狱攻击。
    *   `auth`: 专门设计的权限/身份探测攻击。
    *   `xstest`: 良性测试集，用于评估“过度拒绝”（Over-refusal）情况。
*   **Benchmark 与对比方法**：
    *   **目标模型**：Mistral-7B。
    *   **对比防御方案**：Llama Guard 3 (8B)、ShieldGemma (9B)、Granite Guardian (8B) 等主流分类器，以及 THRD 和 TCA 等具有轨迹感知能力的防火墙变体。

#### 4. 资源与算力
*   **模型规格**：监管模型使用 `Qwen3:8B`，判定模型（Judge）使用 `Qwen3.5:122B`。
*   **部署方式**：通过 Ollama 在本地环境提供服务。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架属于运行时（Runtime）防御，不涉及大规模重新训练，主要开销在于推理侧的多次调用（每轮对话约 3-4 次监管模型调用）。

#### 5. 实验数量与充分性
*   **实验规模**：每个攻击集包含 50 到 120 组对话。
*   **充分性分析**：
    *   **多维度验证**：除了主实验外，还进行了**消融实验**（逐一关闭门控以验证贡献）、**跨模型泛化实验**（更换监管模型为 Gemma、目标模型为 Llama 3.2）以及**自适应攻击压力测试**。
    *   **客观性**：通过 5 个随机种子进行稳定性测试，并计算了 95% 置信区间。
    *   **局限性**：样本量（每组 50-100）相对较小，且判定模型与监管模型同属 Qwen 系列，可能存在潜在的家族偏见。

#### 6. 主要结论与发现
*   **防御效果显著**：在 `jbb`、`crescendo` 和 `safemt` 数据集上，将攻击成功率（ASR）降至 **2% 或以下**；在最难的人工攻击集 `mhj` 上降至 **14%**（远低于基准模型的 26%-54%）。
*   **主动拦截能力**：大部分攻击在目标模型生成之前就被 G1-G3 门控拦截，有效节省了核心模型的算力并降低了风险。
*   **低误拒率**：在保证高安全性的同时，过度拒绝率仅为 **8%**，优于对比的商业级防护模型。
*   **门控互补性**：消融实验证明，没有任何一个门控能独立应对所有攻击，前置拦截与后置过滤的结合至关重要。

#### 7. 优点
*   **主动性**：在有害内容生成前即实现阻断，而非仅仅是事后过滤。
*   **零信任视角**：首次将“身份/权限验证”作为独立的防御维度，有效应对社会工程学类的提示词攻击。
*   **可审计性**：每个拦截决策都对应具体的门控和人类可读的理由，而非模糊的风险评分。
*   **非侵入性**：无需重新训练目标模型，作为插件式架构易于部署。

#### 8. 不足与局限
*   **推理延迟**：每轮对话需要多次调用监管模型，会显著增加首字响应时间（TTFT）。
*   **判定偏见**：监管模型与判定模型同源，可能导致实验结果过于乐观。
*   **监管模型自身的安全性**：论文未深入探讨监管模型本身遭受“提示词注入”攻击时的鲁棒性。
*   **主观性风险**：安全策略（如什么是“有害意图”）仍依赖于监管模型的 Prompt 定义，具有一定的主观性。

（完）
