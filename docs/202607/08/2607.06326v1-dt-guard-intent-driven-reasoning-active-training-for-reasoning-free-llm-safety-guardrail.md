---
title: "DT-Guard: Intent-Driven Reasoning-Active Training for Reasoning-Free LLM Safety Guardrail"
title_zh: DT-Guard：面向无推理大语言模型安全护栏的意图驱动推理激活训练
authors: "He Liu, Changtao Miao, Xinjie Yang, Tianle Song, Yin Wu, Junchi Chen, Bintao He, Xinyuan Zhang, Bo Zhang, Shi Yan, Wei Lu, Wei Wang, Danyang Xu, Jiansheng Cai, Zhe Li"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06326v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 使用推理激活训练的高效大语言模型内容安全护栏
tldr: 针对大语言模型安全护栏在效率与准确性之间的权衡问题，本文提出DT-Guard。该模型采用“推理激活训练、无推理推理”范式，在训练阶段引入意图驱动的推理监督，而在推理阶段仅输出结构化安全标签。通过RG-PHO优化策略提升对复杂样本的鲁棒性。实验表明，4B规模的DT-Guard在多项基准测试中超越了8B规模的基准模型，实现了低延迟与高精度的平衡。
selection_source: fresh_fetch
motivation: 现有的安全护栏在轻量级分类模型的效率与推理型模型的高精度之间存在难以兼得的权衡。
method: 提出一种基于意图驱动的推理激活训练方法，并结合多轮一致性引导的硬样本优化策略，使模型在推理时无需生成推理链即可实现高质量判断。
result: 仅有4B参数的DT-Guard在提示词和响应端安全基准测试中分别达到0.886和0.870的F1分数，性能优于主流的8B安全模型。
conclusion: 研究证明通过在训练中内化推理监督，可以使轻量化模型在保持低延迟的同时具备处理复杂安全决策的能力。
---

## 摘要
部署在开放世界应用中的大语言模型需要既能抵御复杂风险，又具备低延迟运行时审核效率的安全护栏。现有的护栏在轻量级分类模型（高效但难以处理隐藏意图、歧义语义和边界安全决策）与基于推理的护栏（提高判断质量但引入额外的 Token 生成和推理延迟）之间面临实际权衡。我们提出了 DT-Guard，这是一种基于“推理激活训练、无推理推理”范式的内容安全护栏模型。其核心思想是在训练期间使用推理监督，而在推理时仅输出结构化安全标签。DT-Guard 将安全判断建模为一个渐进式决策过程，即“意图 - 类别 - 安全性”，并构建了一个包含意图标签、风险类别、安全标签和结构化推理轨迹的意图驱动数据集。为了进一步提高对困难样本的鲁棒性，我们提出了 Rollout 引导的渐进式困难样本优化（RG-PHO），该方法利用多轮 Rollout 的一致性来识别稳定掌握、持续失败和偏好不稳定的样本，并相应地应用针对性的监督和偏好优化。在推理时，DT-Guard 直接生成结构化标签而无需显式的推理轨迹，从而保持了部署效率。在提示词端和响应端安全基准测试上的实验表明，DT-Guard 分别达到了 0.886 和 0.870 的平均 F1 分数。仅凭 4B 参数的骨干网络，其双端平均 F1 达到 0.878，优于强大的 8B 护栏基准模型。这些结果表明，推理监督可以有效地内化到低延迟的安全判别中。

## Abstract
Large language models deployed in open-world applications require safety guardrails that are both robust to complex risks and efficient enough for low-latency runtime moderation. Existing guardrails face a practical trade-off between lightweight classification-based models, which are efficient but often struggle with concealed intent, ambiguous semantics, and borderline safety decisions, and reasoning-based guards, which improve judgment quality but introduce additional token generation and inference latency. We present DT-Guard, a content safety guardrail model based on a Reasoning-Active Training, Reasoning-Free Inference paradigm. The key idea is to use reasoning supervision during training while emitting only structured safety labels at inference time. DT-Guard formulates safety judgment as a progressive decision process, Intent - Category - Safety, and constructs an intent-driven dataset with intent labels, risk categories, safety labels, and structured reasoning trajectories. To further improve hard-case robustness, we propose Rollout-Guided Progressive Hard-Case Optimization (RG-PHO), which uses multi-rollout consistency to identify stably mastered, persistently failed, and preference-unstable samples, and applies targeted supervised and preference optimization accordingly. At inference time, DT-Guard directly generates structured labels without explicit reasoning traces, preserving deployment efficiency. Experiments on prompt-side and response-side safety benchmarks show that DT-Guard achieves average F1 scores of 0.886 and 0.870, respectively. With only a 4B backbone, it reaches a dual-side average F1 of 0.878, outperforming strong 8B guardrail baselines. These results demonstrate that reasoning supervision can be effectively internalized into low-latency safety discrimination.

---

## 论文详细总结（自动生成）

### DT-Guard：意图驱动的推理激活训练与无推理安全护栏论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在开放领域的广泛应用，运行时安全护栏（Guardrail）成为拦截风险的关键。然而，现有护栏模型面临**效率与精度的权衡**：
*   **分类式护栏**：推理速度快、延迟低，但难以识别隐藏意图、歧义语义或复杂的边界案例。
*   **推理式护栏**：通过思维链（CoT）提高判断准确性，但生成推理过程会显著增加 Token 消耗和响应延迟，难以满足工业级高吞吐场景。
**研究动机**：提出一种“推理激活训练、无推理推理”的范式，使模型在训练阶段吸收复杂的推理逻辑，但在部署阶段仅输出结构化标签，从而兼顾高精度与低延迟。

#### 2. 方法论：核心思想与关键技术
DT-Guard 的核心在于将安全判断建模为**渐进式决策过程**：`意图 (Intent) → 风险类别 (Category) → 安全性 (Safety)`。

*   **意图驱动的数据构建**：
    *   整合 190 万原始样本，通过 GLM-5.1 蒸馏生成包含 CoT、意图、类别和标签的结构化数据。
    *   引入多轮投票与专家审核机制，将标注不一致的样本标记为“边界案例（Borderline）”。
*   **RG-PHO（Rollout 引导的渐进式困难样本优化）策略**：
    1.  **意图引导的混合模式 SFT**：在训练中混合使用“带 CoT 的长输出”和“仅标签的短输出”，让模型学习决策逻辑。
    2.  **基于 Rollout 一致性的样本分层**：对训练集进行多次采样（Rollout），根据正确率将样本分为：稳定掌握（3/3 正确）、持续失败（0/3 正确）和偏好不稳定（1/3 或 2/3 正确）。
    3.  **失败驱动的困难样本 SFT**：针对持续失败样本，使用强监督 CoT 修正其决策路径。
    4.  **Rollout 对比困难样本 DPO**：针对偏好不稳定样本，构建正确与错误 Rollout 的对比对，通过直接偏好优化（DPO）校准模型行为。
*   **无推理推理（Reasoning-Free Inference）**：部署时，模型直接输出结构化标签（意图、类别、安全性），无需生成冗长的推理链。

#### 3. 实验设计
*   **数据集与场景**：
    *   **提示词端（Prompt-side）**：涵盖 ToxicChat, OpenAIModeration, Aegis, HarmBench, XSTest 等 10 个主流基准测试。
    *   **响应端（Response-side）**：涵盖 HarmBench-Response, SafeRLHF, BeaverTails 等 7 个基准测试。
*   **对比方法（Baselines）**：
    *   Qwen3Guard 系列（0.8B, 4B, 8B）。
    *   YuFeng-XGuard 系列（0.6B, 8B）。
    *   Aegis, OpenAIModeration 等专用模型。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号（如 H100/A100）、显卡数量及总训练时长。
*   **模型规模**：主要基于 Qwen3-4B 骨干网络进行实验。

#### 5. 实验数量与充分性
*   **实验规模**：在 17 个安全基准测试上进行了全面评估，涵盖了输入风险检测和输出内容审计。
*   **消融实验**：针对意图标签的作用、CoT 分配策略、RG-PHO 的各个阶段（SFT, 困难样本 SFT, DPO）进行了详细的对比实验。
*   **充分性评价**：实验设计较为充分，通过跨规模（4B vs 8B）和跨范式（分类 vs 推理）的对比，客观地证明了该训练范式的有效性。

#### 6. 主要结论与发现
*   **性能卓越**：4B 规模的 DT-Guard 在双端平均 F1 分数上达到 0.878，超越了参数量更大的 8B 规模基准模型（如 Qwen3Guard-8B 的 0.858）。
*   **推理内化**：证明了推理监督可以被“内化”到模型参数中。即使推理时不输出 CoT，训练时的推理引导也能显著提升模型对复杂意图的判别能力。
*   **分层优化有效**：DPO 阶段对性能提升贡献最大（F1 提升约 2.7%），说明对比学习在校准边界案例方面优于单纯的 SFT。

#### 7. 优点
*   **范式创新**：成功解决了安全护栏中“推理能力”与“推理延迟”的矛盾。
*   **精细化训练**：RG-PHO 策略根据模型自身的行为反馈（Rollout）来动态调整训练重点，比统一的 SFT 更具针对性。
*   **结构化决策**：引入意图识别作为中间步骤，增强了模型对恶意攻击（如 Jailbreak）的防御深度。

#### 8. 不足与局限
*   **算力细节缺失**：未披露训练成本，难以评估该多阶段训练方案的资源消耗。
*   **数据依赖性**：模型性能高度依赖于作为“教师”的高级 LLM（如 GLM-5.1）生成的 CoT 质量，若教师模型存在偏见，可能传递至护栏。
*   **长尾风险**：虽然在基准测试表现优异，但在面对极具隐蔽性的新型对抗攻击时，无推理输出可能缺乏可解释性，不便于人工复核。

（完）
