---
title: "Optimizing Against Safety Representations: Activation-Guided Adversarial Suffixes and the Geometry of Refusal"
title_zh: 针对安全表示的优化：激活引导的对抗性后缀与拒绝机制的几何结构
authors: "Ege Çakar, Hannah Guan, Kayden Kehe"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08883v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 对抗性后缀攻击与内部拒绝方向
tldr: 本研究探讨了大语言模型安全对齐的脆弱性，重点分析了拒绝行为在激活空间中的表征。作者提出了“激活引导的GCG”攻击方法，通过直接针对模型内部的拒绝方向而非输出词元来优化对抗后缀。此外，引入了基于Gumbel-Softmax的Soft-GCG，显著提升了攻击效率。研究发现安全表征在模型层间是分布式存在的，且大模型表现出更强的鲁棒性，为设计更稳健的对齐策略提供了指导。
selection_source: fresh_fetch
motivation: 旨在通过探测模型内部的拒绝表征，理解安全机制在激活空间中的结构、定位及其在优化攻击下的脆弱性。
method: 提出了激活引导的GCG攻击和基于Gumbel-Softmax的Soft-GCG，将攻击目标从输出概率转向内部拒绝方向的抑制。
result: 实验表明全局抑制拒绝信号比局部抑制更有效，且Soft-GCG在提高成功率的同时实现了33倍的加速。
conclusion: 安全机制在模型前向传播中呈分布式编码，且模型规模的扩大有助于增强对激活空间攻击的抵抗力。
---

## 摘要
大语言模型中的行为对齐往往掩盖了脆弱的内部安全表示。最近的研究表明，拒绝行为是由激活空间中的低维方向介导的。这引发了关于此类表示如何构建、定位以及如何被优化过程访问的问题。我们研究了对抗性后缀攻击，将其作为表征对齐的一种探测手段。我们引入了激活引导的 GCG（Activation-Guided GCG），它将基于输出的目标函数替换为直接针对模型内部拒绝方向的损失函数。在几种目标变体中，我们发现，在所有层和位置上全局抑制拒绝比针对单个层-位置对更有效。这表明安全表示分布在整个前向传播过程中，而不是因果性地定位于单个位置。我们进一步引入了 Soft-GCG，这是一种利用 Gumbel-Softmax 对离散后缀优化进行的连续松弛。Soft-GCG 相比标准 GCG 实现了 33 倍的加速，同时提高了攻击成功率。通过在不同模型规模上进行评估，我们发现较小的模型仍然脆弱，而较大的模型在我们的计算受限设置下能够抵御基于激活和后缀的攻击，这与经过更好安全训练的大型模型更难被越狱的观察一致。总之，我们的结果阐明了当代模型中安全机制是如何编码以及如何被破坏的。这些见解为设计更鲁棒且具有表征感知能力的对齐策略提供了具体指导。

## Abstract
Behavioral alignment in large language models often masks fragile internal safety representations. Recent work suggests that refusal behavior is mediated by low-dimensional directions in activation space. This raises questions about how such representations are structured, localized, and accessed by optimization. We study adversarial suffix attacks as a probe of representational alignment. We introduce Activation-Guided GCG, which replaces output-based objectives with losses that directly target a model's internal refusal direction. Across several objective variants, we find that suppressing refusal globally across all layers and positions is more effective than targeting a single layer-position pair. This suggests that safety representations are distributed across the forward pass rather than causally localized to a single site. We further introduce Soft-GCG, a continuous relaxation of discrete suffix optimization using Gumbel-Softmax. Soft-GCG achieves a 33 $\times$ speedup over standard GCG while improving attack success rates. Evaluating across model scales, we find that smaller models remain vulnerable while larger models resist both activation- and suffix-based attacks at our compute-constrained settings, consistent with larger and better safety trained models being harder to jailbreak. Together, our results clarify how safety mechanisms are encoded and can be broken in contemporary models. These insights provide concrete guidance for designing more robust and representation-aware alignment strategies.

---

## 论文详细总结（自动生成）

这篇论文《针对安全表示的优化：激活引导的对抗性后缀与拒绝机制的几何结构》深入探讨了大语言模型（LLM）内部安全机制的脆弱性，并提出了更高效的越狱攻击方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然 LLM 通过对齐技术（如 RLHF）在行为上表现出安全性，但其内部表征往往依然脆弱。现有的越狱攻击（如 GCG）主要针对输出端的词元概率（症状），而忽略了模型内部的拒绝机制（根源）。
*   **核心问题**：模型内部的“拒绝表示”是如何在空间和深度上分布的？能否通过直接优化模型内部的激活值来绕过安全检查？能否通过连续优化手段解决离散后缀搜索效率低下的问题？

### 2. 方法论
论文提出了两种主要方法：

*   **激活引导的 GCG (Activation-Guided GCG)**：
    *   **核心思想**：将 GCG 的目标函数从“最大化目标回复概率”改为“最小化内部激活在拒绝方向上的投影”。
    *   **拒绝方向提取**：利用前人研究的“均值差”方法，在残差流中识别出区分有害和无害指令的单一维度向量 $d_{refusal}$。
    *   **目标函数变体**：设计了五种损失函数，分别针对单个层-位置（Single）、特定层的所有位置（Layer）、特定位置的所有层（Token）以及全局所有层和位置（All/Global）。
*   **Soft-GCG**：
    *   **核心思想**：利用 **Gumbel-Softmax** 将离散的词元搜索转化为连续的概率分布优化，以解决 GCG 速度慢的问题。
    *   **关键技术**：
        *   **连续松弛**：维护每个后缀位置的 Logits，通过 Gumbel-Softmax 生成“软”词元向量。
        *   **损失函数**：结合了交叉熵损失和 **Carlini-Wagner (CW) 损失**，后者通过设置置信度间隔（Margin）来增强后缀的鲁棒性。
        *   **温度退火**：引入了“三阶段退火策略”（探索、精炼、固化），使优化过程从平滑的连续空间最终收敛到离散的词元。

### 3. 实验设计
*   **模型**：主要使用 Llama-2-7b-chat，并扩展到 Gemma 3 系列（270M, 1B, 4B, 12B）。
*   **数据集**：AdvBench（用于训练和测试越狱后缀）、HarmBench（用于评估安全性）。
*   **基准对比 (Benchmark)**：
    *   **Baseline**：标准 GCG 算法。
    *   **Ablation**：直接在激活空间进行方向消融（作为攻击效果的上限）。
*   **评估指标**：子字符串匹配 ASR（攻击成功率）、LlamaGuard2 评分、HarmBench 评分。

### 4. 资源与算力
*   **效率提升**：Soft-GCG 相比标准 GCG 实现了 **33 倍至 43 倍** 的加速。
*   **具体时长**：在相同硬件条件下，标准 GCG 需要 81 分钟完成的优化，Soft-GCG 仅需约 **2.5 分钟**。
*   **硬件环境**：文中提到 Gemma 3 实验可以在“单个 GPU”上运行，但未明确标注具体的 GPU 型号（如 A100 或 H100）。

### 5. 实验数量与充分性
*   **实验规模**：作者在 Llama-2 上测试了 5 种不同的激活引导目标，并在 Gemma 3 的四个不同规模模型上验证了扩展性。
*   **消融实验**：通过对比 Single、Layer、Token 和 All 四种目标，验证了拒绝表征的分布式特性。
*   **客观性**：使用了多种第三方评估器（LlamaGuard2, HarmBench）来避免单一指标的偏差，实验设计较为严谨。

### 6. 主要结论与发现
*   **拒绝表征是分布式的**：实验表明，在所有层和位置上全局抑制拒绝信号（All 目标）的效果最好，证明安全机制并非局限于模型某一点，而是贯穿前向传播过程。
*   **连续优化的优越性**：Soft-GCG 不仅速度极快，且在攻击成功率上达到或超过了离散 GCG，证明了“投影间隙”可以通过合理的退火策略克服。
*   **模型规模的防御效应**：随着模型规模增加（从 270M 到 12B），模型对激活空间攻击和后缀攻击的抵抗力显著增强，暗示大模型可能采用了更复杂、更高维的安全编码。

### 7. 优点
*   **机制性洞察**：不仅提供了攻击工具，还通过攻击手段揭示了模型内部安全机制的几何结构（分布式特征）。
*   **极高的实用价值**：33 倍的加速使得越狱攻击从昂贵的计算任务变成了可以在几分钟内完成的常规操作，对红队测试具有重要意义。
*   **创新的损失函数**：将 CW 损失引入 LLM 越狱领域，提升了对抗后缀的鲁棒性。

### 8. 不足与局限
*   **白盒限制**：该方法依赖于获取模型的梯度和内部激活值，无法直接应用于闭源 API 模型（如 GPT-4）。
*   **评估指标局限**：主要依赖子字符串匹配来计算 ASR，可能存在误报或漏报（尽管作者引入了 LlamaGuard 作为补充）。
*   **词表规模影响**：在 Gemma 3 上的实验显示，巨大的词表规模（262k）增加了优化的复杂性，论文未能完全隔离词表大小与模型安全性之间的因果关系。

（完）
