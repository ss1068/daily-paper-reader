---
title: "Beware of Agentic Botnets: Scalable Untargeted Promptware Attacks via Universal and Transferable Adversarial HalluSquatting"
title_zh: 警惕代理型僵尸网络：通过通用且可迁移的对抗性幻觉抢注实现可扩展的非针对性提示词恶意软件攻击
authors: "Aya Spira, Stav Cohen, Elad Feldman, Ron Bitton, Avishai Wool, Ben Nassi"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07433v1"
tags: ["query:llm"]
score: 8.0
evidence: 针对智能体大语言模型的提示注入和提示软件攻击
tldr: 本研究揭示了针对代理型大模型（LLM）应用的一种新型可扩展攻击——对抗性幻觉抢注（HalluSquatting）。攻击者利用LLM在生成资源标识符（如仓库名、技能名）时的幻觉倾向，预先注册这些虚假资源并植入恶意指令。该方法利用了幻觉的可预测性和跨模型迁移性，无需直接注入通道即可在大规模范围内实现远程代码执行，甚至构建代理型僵尸网络，对当前LLM应用生态构成了严重威胁。
selection_source: fresh_fetch
motivation: 探讨在缺乏直接注入通道的情况下，攻击者如何利用LLM固有的幻觉特性实现大规模、非定向的恶意指令攻击。
method: 提出对抗性幻觉抢注技术，通过预测LLM对热门资源名称产生的幻觉分布，抢先注册相关资源以托管恶意Prompt。
result: "实验显示幻觉生成率在特定场景下高达85%至100%，且具有显著的跨模型迁移性，成功在多个生产级LLM应用中实现远程代码执行。"
conclusion: LLM幻觉可被武器化用于构建自动化僵尸网络，开发者需警惕代理型应用在处理外部资源时的安全风险。
---

## 摘要
代理型大语言模型（LLM）应用的日益普及引入了一种此前被称为“提示词恶意软件”（promptware）的新威胁。虽然先前的研究已经证实，在弱威胁模型下，攻击者可以利用 LLM 应用的直接通道来实施提示词恶意软件攻击，但许多应用除了互联网之外，并不提供任何可用于提示词注入的直接通道。这提出了一个问题：在实际威胁模型中，攻击者能否在没有直接通道的情况下大规模利用 LLM 应用？在这项工作中，我们展示了 LLM 幻觉资源标识符的固有倾向可以被用来放大非针对性的提示词恶意软件攻击，从而大规模拉取对抗性提示词，并可能被用于建立僵尸网络。我们引入了“对抗性幻觉抢注”（adversarial hallucination squatting），这是一种攻击者识别热门资源（例如，热门仓库、热门技能等），计算 LLM 在这些热门资源名称上的幻觉分布，并预先注册这些资源以托管对抗性提示词的技术。通过利用幻觉在基础 LLM 之间以及向应用层的可预测性和可迁移性，攻击者可以在弱威胁模型下显著扩大非针对性提示词恶意软件的覆盖范围，并通过利用 LLM 应用在从互联网拉取受损幻觉资源的设备上安装机器人程序，从而建立僵尸网络。我们通过实验证明，幻觉资源的生成率很高，在仓库克隆场景中高达 85%，在技能安装中高达 100%，并且这些幻觉在基础模型和不同提示词之间具有可迁移性。我们展示了对抗性幻觉抢注针对各种在其工具集中集成终端的生产级 LLM 应用的实用性，实现了远程工具执行和远程代码执行。

## Abstract
The growing adoption of agentic LLM applications has introduced a new threat previously named as promptware. While prior work has established that adversaries can exploit direct channels to LLM applications to apply promptware under weak threat models, many applications do not provide any direct channels that could be exploited for prompt injection beyond the Internet. This raises a question: can attackers exploit LLM applications at scale without any direct channels in practical threat models? In this work, we show that the inherent tendency of LLMs to hallucinate resource identifiers can be exploited to amplify untargeted promptware attacks that pull adversarial prompts at scale and could be exploited to establish a botnet. We introduce adversarial hallucination squatting, a technique in which attackers identify trending resources (e.g., popular repositories, popular skills, etc.), compute the LLM distribution of hallucinations on the trending resource names, and preemptively register them to host adversarial prompts. By leveraging the predictability and transferability of hallucinations across foundational LLMs and to application layers, adversaries can significantly amplify the reach of untargeted promptware under weak threat models and establish a botnet by exploiting LLM applications to install a bot on the device that pulled the compromised hallucinated resource from the Inter. We empirically demonstrate that hallucinated resource generation occurs at high rates, up to 85% in repository cloning scenarios and up to 100% in skill installation, and that these hallucinations transfer between foundational models and different prompts. We demonstrate the practicality of adversarial hallucination squatting against various production LLM applications with integrated terminals in their set of tools, achieving remote tool execution and remote code execution.

---

## 论文详细总结（自动生成）

这篇论文由特拉维夫大学、以色列理工学院和 Intuit 的研究人员合作完成，揭示了代理型大语言模型（Agentic LLM）应用中一个极其严重且具有规模化潜力的安全漏洞。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的“提示词恶意软件”（Promptware）攻击通常依赖于直接通道（如发送邮件、日历邀请）进行注入。然而，许多代理型应用（如 AI 编程助手）并不具备这些直接入口。
*   **核心问题**：攻击者能否在没有直接注入通道的情况下，利用 LLM 固有的缺陷实现大规模、非针对性的攻击？
*   **整体含义**：论文提出了**对抗性幻觉抢注（Adversarial HalluSquatting）**。它利用 LLM 在预测资源标识符（如 GitHub 仓库名、插件技能名）时产生的“幻觉”倾向，通过预先注册这些幻觉出的虚假资源并植入恶意指令，从而在用户调用合法资源时误导代理执行攻击代码，最终可能构建起“代理型僵尸网络”（Agentic Botnets）。

### 2. 方法论
*   **核心思想**：将 LLM 的幻觉行为视为一种可预测的概率分布，攻击者通过“抢注”高频幻觉标识符来拦截流量。
*   **关键技术流程**：
    1.  **准备阶段**：监控互联网趋势（如 GitHub Trending），识别热门资源名称。
    2.  **探测与分布计算**：通过 API 多次查询不同基础模型（GPT、Claude、Gemini），计算特定资源名称对应的幻觉标识符分布。
    3.  **通用候选者识别**：利用论文提出的算法（Algorithm 1 & 2），计算跨模型的“通用抢注候选者”，即在多个模型中均有高概率出现的幻觉名称。
    4.  **资源抢注**：在 GitHub 或插件市场（如 ClawHub）上注册这些名称，并上传包含对抗性提示词（Promptware）的内容。
    5.  **触发与执行**：当用户要求 AI 代理“克隆某热门仓库”或“安装某技能”时，代理产生幻觉并访问了攻击者控制的资源，从而触发远程代码执行（RCE）或工具误用。

### 3. 实验设计
*   **实验场景**：
    *   **编程助手场景**：针对 GitHub 仓库克隆。测试了 16 个仓库（10 个近期热门、5 个旧仓库作为对照、1 个深入案例 `librepods`）。
    *   **个人助手场景**：针对 ClawHub 技能安装。测试了 14 个流行技能。
*   **测试对象（Benchmark）**：
    *   **基础模型**：GPT-4/5 系列、Claude 3.5/4.5 系列、Gemini 1.5/2.5/3 系列。
    *   **生产级应用**：Cursor、Windsurf、GitHub Copilot Chat、Cline、Gemini CLI、OpenClaw、ZeroClaw、NanoClaw。
*   **对比方法**：对比了不同模型在相同提示词下的幻觉率，以及不同提示词框架（祈使句、间接句、生成句）对幻觉触发的影响。

### 4. 资源与算力
*   **算力说明**：论文未提及模型训练所需的 GPU 算力，因为该研究属于**攻击性推理研究**。
*   **实验消耗**：研究人员通过公共 API 进行了超过 **14,000 次** 查询，以构建幻觉分布并验证攻击的有效性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 大主流模型家族、8 种生产级代理应用、30 个目标资源。
*   **充分性**：实验设计非常详尽，不仅验证了攻击的**成功率**，还深入探讨了**跨模型迁移性**（一个模型的幻觉是否在另一个模型中也存在）和**跨层迁移性**（基础模型的幻觉如何传递到应用层）。
*   **客观性**：引入了旧仓库作为对照组，证明了幻觉主要发生在模型训练截止日期之后的新资源上，实验逻辑严密且具有统计学意义。

### 6. 主要结论与发现
*   **极高的幻觉率**：对于近期热门仓库，LLM 的幻觉率高达 **92.4%**；对于特定技能安装，幻觉率甚至达到 **100%**。
*   **幻觉的可预测性**：幻觉并非随机，而是呈现特定模式（如“自引用模式”，即将仓库名直接作为用户名）。
*   **跨模型迁移性**：不同厂商的模型往往会产生相同的幻觉标识符，这使得“通用抢注”成为可能。
*   **现实威胁**：在对 Cursor、Copilot 等应用的实测中，成功实现了远程代码执行（RCE）和敏感数据（如 API Key）的窃取，成功率在 20% 到 100% 之间。

### 7. 优点
*   **视角独特**：首次将 LLM 的幻觉特性与传统的“域名/包名抢注”攻击结合，提出了针对代理生态的规模化攻击模型。
*   **实战性强**：直接在多个流行的生产级 AI 编程助手上完成了端到端的攻击演示，证明了威胁的紧迫性。
*   **责任披露**：研究团队遵循负责任的披露原则，在发布前已通知 Google、GitHub、Anthropic 等相关厂商。

### 8. 不足与局限
*   **防御依赖性**：实验显示，如果代理应用强制执行“先搜索再获取”的策略，可以显著降低攻击成功率，这意味着该漏洞在某种程度上依赖于应用设计的缺陷。
*   **平台监管风险**：虽然目前 GitHub 等平台的扫描机制可以被绕过，但未来如果平台加强对 Prompt 注入的静态分析，此类攻击的门槛会提高。
*   **人类干预**：实验主要针对高度自动化的代理，如果用户在执行每一步前都仔细检查 URL，攻击可能会失败（尽管在代理模式下这很难实现）。

（完）
