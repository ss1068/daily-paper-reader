---
title: "HARC: Coupling Harmfulness and Refusal Directions for Robust Safety Alignment"
title_zh: HARC：耦合有害性与拒绝方向以实现鲁棒安全对齐
authors: "Shei Pern Chua, Hao Wu, Qianli Ma, Fangzhao Wu"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00572v3"
tags: ["query:jb-def"]
score: 10.0
evidence: 诊断对齐漏洞并设计鲁棒的对齐策略
tldr: 本研究揭示了越狱攻击通过抑制模型内部的有害性或拒绝方向生效，并发现模型在生成时仍能识别有害内容。据此提出HARC微调方法，在提示和响应位置耦合这两个方向。HARC在不损害通用能力的前提下，实现了最优的鲁棒性与可用性平衡，且相关特征在不同模型间具有高度迁移性。
selection_source: fresh_fetch
motivation: 旨在通过探究LLM内部有害性与拒绝方向的表示机制，解决模型在面对越狱攻击时的对齐脆弱性问题。
method: 提出HARC微调方法，通过在提示词和响应位置将有害性与拒绝方向进行子空间耦合，增强模型的安全防御。
result: 实验证明HARC在鲁棒性、通用能力和可用性之间取得了最佳权衡，且其发现的特征方向可跨模型家族迁移。
conclusion: 耦合有害性与拒绝方向是实现稳健安全对齐的有效途径，能显著提升LLM抵御复杂攻击的能力。
---

## 摘要
理解已对齐的大语言模型（LLMs）如何在内部表示安全性，对于诊断对齐漏洞至关重要，因为它解释了越狱为何成功，并为鲁棒对齐策略的设计提供了参考。先前的工作表明，已对齐的 LLMs 在提示词端的 token 位置上，将有害性和拒绝编码为残差流中可分离的方向。我们展示了越狱在提示词编码阶段通过在生成任何 token 之前抑制拒绝或有害性方向而取得成功，且不同的攻击类别占据了有害性-拒绝平面中可分离的区域。将分析扩展到响应 token 位置，我们发现模型在生成有害内容的同时能够识别出该内容，即使它在提示词端未能识别出输入是有害的。受此发现启发，我们提出了 HARC（有害性与拒绝耦合），这是一种在提示词和响应位置上将这两个方向配对的微调方法。由于干预仅限于有害性-拒绝子空间，它保持了残差流的其余部分完好无损，且不会降低通用能力或导致过度拒绝。通过广泛的实验，HARC 在涵盖主要训练时和推理时安全方法的六个基准模型中，实现了鲁棒性-能力-可用性之间的最佳权衡。提示词和响应位置的有害性与拒绝方向在我们测试的五个模型家族和两个规模中具有可迁移性，无需针对特定架构进行调整。

## Abstract
Understanding how aligned LLMs internally represent safety is critical for diagnosing alignment vulnerabilities, as it explains why jailbreaks succeed and informs the design of robust alignment strategies. Prior work shows that aligned LLMs encode harmfulness and refusal as separable directions in the residual stream at prompt-side token positions. We show that jailbreaks succeed at prompt encoding by suppressing either the refusal or harmfulness direction before any token is generated, with distinct attack classes occupying separable regions of the harmfulness-refusal plane. Extending the analysis to response-token positions, we find that the model recognizes harmful content while it is generating that content, even when it failed to recognize the input as harmful at the prompt side. Motivated by our findings, we introduce HARC (Harmfulness-And-Refusal Coupling), a fine-tuning method that pairs the two directions across both prompt and response positions. Since the intervention is confined to the harmfulness-refusal subspace, it leaves the rest of the residual stream intact and does not degrade general capability or inflate over-refusal. Across extensive experiments, HARC achieves the strongest robustness-capability-usability trade-off among six baselines spanning the major training-time and inference-time safety methods. The harmfulness and refusal directions at prompt and response positions transfer across the five model families and two scales we tested without architecture-specific tuning.

---

## 论文详细总结（自动生成）

### 论文总结：HARC：耦合有害性与拒绝方向以实现鲁棒安全对齐

#### 1. 论文的核心问题与整体含义（研究动机和背景）
尽管大语言模型（LLM）经过了安全对齐，但仍容易受到各类越狱攻击（Jailbreak）的侵害。论文的研究动机源于对模型内部安全表示机制的深入探索：
*   **内部解耦现象**：模型在处理输入时，将“识别内容有害（Harmfulness）”和“产生拒绝意图（Refusal）”编码为残差流中两个独立的方向。
*   **越狱机制**：研究发现，越狱攻击之所以成功，是因为它们在提示词编码阶段抑制了拒绝方向或有害性方向。
*   **响应端的发现**：即使模型在提示词阶段未能识别出有害性，在生成响应的过程中，其内部表示仍能实时识别出正在生成的有害内容。
*   **核心目标**：通过在模型内部强行耦合这两个方向，使得模型只要识别到有害性，就必然触发拒绝，从而提升防御的鲁棒性。

#### 2. 论文提出的方法论：HARC
论文提出了 **HARC (Harmfulness-And-Refusal Coupling)**，一种基于表示学习的微调方法。
*   **核心思想**：在提示词（Prompt）和响应（Response）两个关键位置，通过损失函数强制有害性方向（$v_{harm}$）和拒绝方向（$v_{ref}$）同步激活。
*   **关键技术细节**：
    *   **方向提取**：利用“均值差（Difference of Means）”方法，在提示词末尾和响应生成过程中提取四个关键向量：提示词端的有害/拒绝方向，以及响应端的有害/拒绝方向。
    *   **损失函数设计**：
        *   **耦合损失（Coupling Loss）**：采用加性间隔铰链损失（Additive Margin Hinge Loss），强制有害输入在对应方向上的余弦投影超过设定阈值。
        *   **能力保留**：引入 KL 散度损失，将微调模型锚定在原始模型分布上，以减少“对齐税”（能力下降）。
        *   **显式拒绝**：使用交叉熵损失（CE）监督模型生成标准的拒绝文本。
    *   **动态层选择与重计算**：根据方向的解耦程度自动选择最适合干预的中间层，并在训练过程中定期重新计算方向向量以适应参数变化。

#### 3. 实验设计
*   **数据集与场景**：
    *   **攻击测试**：使用 JailbreakBench，涵盖 PAP、PAIR、DeepInception 和 CodeAttack 四种主流攻击。
    *   **过度拒绝测试**：使用 XSTest 和 CoCoNot 评估模型是否会误伤无害请求。
    *   **通用能力测试**：MMLU（知识）、GSM8K（数学）、HumanEval（代码）、IFEval（指令遵循）和 MT-Bench（对话）。
*   **对比方法（Baselines）**：
    *   **训练时方法**：SFT（监督微调）、DPO（直接偏好优化）、Circuit Breakers（电路断路器）、RepBend（表示弯曲）。
    *   **推理时方法**：CAST（条件激活引导）。

#### 4. 资源与算力
*   **硬件设备**：实验主要在单张 **NVIDIA H200 (144GB)** GPU 上运行；对于 70B 级别的大模型，使用了两张 H200。
*   **训练配置**：
    *   使用 LoRA 进行高效微调（参数量约占总体的 1%）。
    *   训练步数为 4000 步，Batch Size 为 24。
    *   精度采用 `bf16`。
*   **时长**：文中未明确给出总训练时长，但考虑到步数和模型规模，7B 模型通常在数小时内可完成。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个模型家族（Llama-3.1, Qwen-2.5, Mistral, Phi-3, Gemma-2）以及两种参数规模（7B/8B 和 70B/72B）。
*   **消融实验**：详细对比了仅在提示词端耦合、仅在响应端耦合以及双位置耦合的效果，验证了双位置设计的必要性。
*   **充分性与公平性**：
    *   实验在相同训练数据下对比了 SFT，排除了数据差异的影响。
    *   进行了多随机种子实验（3 个种子）并报告了方差，证明了结果的稳定性。
    *   跨架构的验证证明了该发现具有普适性而非特定架构的偶然结果。

#### 6. 论文的主要结论与发现
*   **越狱的本质**：成功的攻击通过在提示词端造成有害性与拒绝方向的“解耦”来绕过防御。
*   **HARC 的优越性**：在所有测试方法中，HARC 实现了鲁棒性、通用能力和低过度拒绝率之间的最佳权衡。
*   **缩放效应**：HARC 在 70B 规模的模型上表现更优，能将多种攻击的成功率降至接近 0，且几乎不损失通用性能。
*   **跨模型迁移**：有害性和拒绝的方向特征在不同模型家族间高度一致，层选择标准无需针对特定架构调优。

#### 7. 优点
*   **干预精准**：仅针对二维子空间进行干预，对模型原有知识结构的破坏极小。
*   **机制驱动**：基于对模型内部表示的深刻洞察（响应端仍有感知），而非盲目增加训练数据。
*   **高可用性**：解决了安全增强往往导致模型变得“过度敏感/愚笨”的问题，保持了极低的过度拒绝率。

#### 8. 不足与局限
*   **微调攻击脆弱性**：附录实验显示，如果攻击者拥有权重访问权限并进行恶意微调，HARC 的防御较容易被撤销（约 160 个有害样本即可破解）。
*   **对完全欺骗的依赖**：如果某种攻击能让模型在提示词和响应阶段都完全无法识别出有害性（如极高难度的混淆），HARC 将失去耦合的“锚点”。
*   **白盒风险**：论文公开了防御的内部机制，理论上可能被攻击者利用来设计专门针对耦合子空间的自适应攻击。

（完）
