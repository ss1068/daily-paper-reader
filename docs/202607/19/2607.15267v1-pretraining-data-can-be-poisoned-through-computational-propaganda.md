---
title: Pretraining Data Can Be Poisoned through Computational Propaganda
title_zh: 预训练数据可通过计算宣传受到投毒攻击
authors: "Victoria Graf, Hannaneh Hajishirzi, Noah A. Smith, David Kohlbrenner, Kyle Lo"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15267v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型训练数据投毒攻击
tldr: 本研究探讨了通过公共讨论界面等计算宣传手段对大规模语言模型预训练数据进行投毒的可能性。针对现有研究局限于维基百科等单一来源的问题，本文提出了HalfLife分析方法，用于评估恶意内容在网页抓取和数据清洗后的留存率。实验证明，第三方网页内容是攻击预训练模型的潜在向量，揭示了大规模数据投毒的现实威胁。
selection_source: fresh_fetch
motivation: 现有的预训练数据投毒研究多局限于维基百科等特定来源，忽视了大规模异构网页数据中的投毒风险及数据清洗流程的影响。
method: 提出了一种名为HalfLife的新型分析方法，用于估算恶意注入内容在经过网页抓取和数据过滤后进入预训练语料库的比例。
result: 实验表明，通过公共讨论界面注入的恶意内容能够绕过现有的数据清洗机制，成功进入大规模预训练数据集中。
conclusion: 第三方网页内容是攻击语言模型预训练的有效向量，研究强调了在构建预训练语料时评估和防范恶意内容注入的重要性。
---

## 摘要
对预训练数据进行投毒可能会给语言模型（LMs）引入难以检测和缓解的有害行为。以往关于预训练数据投毒的研究主要利用了维基百科等既定数据源，这些数据源无法代表预训练语料库典型的大规模和异质性，且忽略了投毒数据与数据清洗流水线之间的相互作用。我们证明，通过现有的网络级内容注入机制（即公共讨论界面），在这一受限设置之外对预训练数据进行投毒攻击是可行的。此外，为了衡量在网络爬取和数据清洗后是否包含恶意内容，我们引入了 HalfLife，这是一种用于估计基于网络爬取的语言模型训练数据中对抗性内容包含情况的新型分析方法。我们利用 HalfLife 探索了通过开放讨论界面在网络规模上对预训练语料库进行投毒的可行性。我们的分析证明了评估预训练数据中是否包含投毒注入的重要性，并将第三方网页内容确立为攻击语言模型预训练的一种可能媒介。

## Abstract
Poisoning pretraining data can introduce harmful behaviors to LMs that are difficult to detect and mitigate. Prior work on poisoning pretraining data has largely exploited established data sources such as Wikipedia, which do not represent the large scale and heterogeneity typical of pretraining corpora, and has ignored the interaction between poisoned data and data curation pipelines. We demonstrate that poisoning attacks on pretraining data are feasible beyond this limited setting through an existing web-scale content injection mechanism: public discussion interfaces. Additionally, to measure whether malicious content is included after web crawling and data curation, we introduce HalfLife, a novel analysis for estimating adversarial content inclusion in web-crawl based LM training data. We use HalfLife to explore the feasibility of poisoning pretraining corpora at web scale through open discussion interfaces. Our analysis demonstrates the importance of estimating whether poison injections are included in pretraining data, and establishes third-party webpage content as a possible vector for attacking language model pretraining.

---

## 论文详细总结（自动生成）

这篇论文《Pretraining Data Can Be Poisoned through Computational Propaganda》深入探讨了大型语言模型（LM）预训练阶段面临的一种新型且极具威胁的投毒攻击方式。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的预训练数据投毒研究大多集中在维基百科等高质量、小规模且易于监管的数据源上。然而，现代 LLM 的预训练语料库绝大部分（如 90% 以上）来自 Common Crawl 等异构的网页抓取数据。
*   **核心问题**：攻击者能否利用互联网现有的公共交互机制（如评论区、论坛），通过“计算宣传”（Computational Propaganda）手段，在大规模、异构的网页数据中注入恶意内容，并使其绕过复杂的数据清洗流水线，最终影响模型的行为？
*   **整体含义**：论文揭示了第三方内容注入（Third-party content injection）是一个被低估的攻击向量，证明了即使攻击者不拥有域名或服务器，也能通过自动化工具实现网络规模的投毒。

### 2. 论文提出的方法论：HalfLife 分析框架
论文提出了 **HalfLife** 分析方法，用于量化估计投毒内容在经过抓取、提取和过滤后的“存活率”和最终包含概率。
*   **核心思想**：将投毒包含概率 $P(include)$ 分解为三个连续阶段：
    1.  **S1: 可注入性（Injectable）**：网页是否允许第三方发布内容（如开放的评论表单）。
    2.  **S2: 可捕获性（Captured）**：爬虫是否抓取该页面，且文本提取工具（如 Resiliparse）是否保留了注入的内容（而非将其视为噪声/页眉页脚剔除）。
    3.  **S3: 过滤存活率（Not Filtered）**：注入内容是否能通过启发式过滤、语言识别、重复数据删除和质量分类器（Quality Filters）。
*   **计算公式**：$P(include) = P(injectable) \times P(captured) \times P(not filtered)$。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **Common Crawl (CC-MAIN-2025-51)** 镜像作为分析对象，扫描了约 18.2 万个网页。
    *   对比了两种注入向量：**公共评论区**（如 WordPress 插件）和**程序化广告**（Programmatic Ads）。
*   **Benchmark 与对比**：
    *   **投毒任务**：信念操纵（让模型在波音 vs 空客、辉瑞 vs 莫德纳等实体对比中产生偏见）。
    *   **对比方法**：测试了三种投毒格式：`User/Assistant`（对话式）、`Q/A`（问答式）和 `No-label`（无标签自然文本）。
    *   **清洗流水线**：采用了 **Dolma 3**（OLMo 模型的开源数据处理流水线）作为防御基准。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **NVIDIA H100** GPU 节点。
*   **训练配置**：
    *   65M、150M、260M 模型使用 1 个节点。
    *   709M 模型使用 2 个节点。
    *   1.3B 模型使用 4 个节点。
*   **训练时长**：1.3B 模型的单次训练运行大约需要 **9 小时**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了超过 18 万个网页的评论平台分布。
    *   训练了一系列不同参数规模的模型（65M 到 1.3B），以观察缩放效应（Scaling Laws）。
    *   测试了三个量级的投毒比例：0.1%、0.01% 和 0.001%。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了投毒的“包含概率”，还通过实际训练模型验证了“下游影响”，并考虑了指令微调（SFT）对投毒效果的稀释作用。此外，还进行了消融实验，测试了合成数据重写（WRAP）对投毒信号的破坏性。

### 6. 论文的主要结论与发现
1.  **评论区投毒高度可行**：HalfLife 分析估算评论区投毒的最终包含概率约为 **0.13%**。虽然数值看似微小，但在 Common Crawl 规模下，其影响的文档数超过了整个维基百科。
2.  **广告向量不可行**：程序化广告由于采用 iframe 隔离和 JavaScript 动态渲染，现有的基于 DOM 的静态爬虫（如 Common Crawl）无法抓取其文本内容，因此难以作为文本投毒向量。
3.  **模型受影响显著**：即使是 0.001% 的极低投毒率，也能显著改变 Base 模型的偏好。
4.  **SFT 的防御作用**：指令微调会削弱预训练投毒的效果，且模型规模越大，SFT 后的投毒残留越少。
5.  **隐蔽性与存活率**：无标签（No-label）的自然文本投毒最难被过滤，但在 SFT 后效果最差；而带有问答标识（Q/A）的投毒在 SFT 后仍能保持较强的攻击力。

### 7. 优点：亮点与创新
*   **视角独特**：首次将“计算宣传”与“预训练数据清洗流水线”结合研究，填补了异构网页数据投毒研究的空白。
*   **工具化贡献**：提出的 **HalfLife** 框架为未来的数据安全审计提供了一个标准化的概率评估模型。
*   **现实针对性强**：直接针对开源大模型常用的 Dolma、DCLM 和 FineWeb 等数据集的处理逻辑进行测试，具有极高的实战参考价值。

### 8. 不足与局限
*   **流水线单一性**：主要基于 Dolma 3 的过滤逻辑，闭源模型（如 GPT-4）可能拥有更复杂的私有过滤机制，实验结果可能存在偏差。
*   **伦理限制**：为了遵守伦理准则，作者未在真实互联网环境中进行大规模注入实验，而是采用沙盒模拟和历史数据回溯，这可能无法完全模拟真实世界中动态对抗（如验证码、WAF 防火墙）的影响。
*   **语言局限**：实验主要集中在英文数据，对于低资源语言或其他语种的投毒存活率尚未探讨。

（完）
