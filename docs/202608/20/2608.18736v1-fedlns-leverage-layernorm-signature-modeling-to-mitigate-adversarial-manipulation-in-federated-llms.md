---
title: "FedLNS: Leverage LayerNorm Signature Modeling to Mitigate Adversarial Manipulation in Federated LLMs"
title_zh: FedLNS：利用层归一化特征建模缓解联邦大语言模型中的对抗性操纵
authors: "Kai Li, Jong-Ik Park, Carlee Joe-Wong, Wei Ni, Falko Dressler"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.18736v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 减轻联邦大语言模型中的对抗性操纵
tldr: 针对联邦学习训练大语言模型时易受恶意客户端篡改攻击的问题，本文提出 FedLNS 框架。该方法通过提取客户端更新中归一化层（LayerNorm）参数的变化作为“签名”，在服务端利用历史感知的跨客户端参考机制识别并过滤恶意更新。FedLNS 无需额外通信开销、原始数据或预训练检测器，在多种模型架构和数据分布下均表现出卓越的鲁棒性。
selection_source: fresh_fetch
motivation: 联邦学习中恶意客户端可通过篡改训练目标引入错误关联，导致全局模型性能下降甚至产生幻觉，而服务端缺乏有效的验证手段。
method: 通过分析本地模型更新中可训练归一化层参数的变化生成签名，并据此在服务端对可疑更新进行轻量化筛选。
result: "在 GPT、BERT 和 LLaMA 模型上的实验表明，面对 40% 的恶意篡改，FedLNS 在 IID 和 non-IID 场景下的测试困惑度均优于六种强基准方法。"
conclusion: FedLNS 提供了一种无需额外元数据交换的高效防御机制，能显著提升联邦大语言模型抵御对抗性操纵的能力。
---

## 摘要
联邦训练使语言模型能够从分布式的私有文本中学习，但服务器无法直接验证产生每个客户端更新的本地监督或优化过程。因此，恶意客户端可以在损坏的目标上进行训练，引入错误的上下文-标记（context-token）关联，并通过重复聚合使全局模型性能下降。这种性能下降还会增加生成不可靠或幻觉内容的风险。我们提出了基于归一化特征的联邦学习（FedLNS），这是一个用于轻量级恶意更新筛选的服务器端框架。FedLNS 通过可训练归一化层参数的变化来表示每个客户端的更新，并根据鲁棒的、历史感知的跨客户端参考来筛选可疑更新。由于特征是在服务器端从返回的本地模型中提取的，与标准的联邦学习（FL）方法相比，FedLNS 不需要额外的客户端到服务器的参数或元数据交换。筛选后，保留的全模型更新可以使用标准 FL 或其他兼容的聚合规则进行聚合。FedLNS 不需要原始客户端数据、受信任的服务器数据集、标记的攻击样本或单独训练的检测器。在 200 个客户端上从头训练的 GPT 式、BERT 式和 LLaMA 式模型的实验表明，在 40% 的群体级目标操纵下，FedLNS 在 IID（独立同分布）和非 IID 数据划分下，针对所有三种架构的测试困惑度均低于六个基准模型中最强的一个。

## Abstract
Federated training enables language models to learn from distributed private text, but the server cannot directly verify the local supervision or optimization process that produces each client update. A malicious client can therefore train on corrupted targets, introduce incorrect context-token associations, and degrade the global model through repeated aggregation. Such degradation can also increase the risk of unreliable or hallucinatory generation. We propose Federated Learning with Normalization Signatures (FedLNS), a server-side framework for lightweight malicious-update screening. FedLNS represents each client update through changes in trainable normalization-layer parameters and screens suspicious updates against a robust, history-aware cross-client reference. Because the signatures are extracted at the server from the returned local models, FedLNS requires no additional client-to-server parameter or metadata exchange compared to standard federated learning (FL) methods. After screening, the retained full-model updates can be aggregated using standard FL or another compatible aggregation rule. FedLNS requires no raw client data, trusted server dataset, labeled attack examples, or separately trained detector. Experiments on GPT-style, BERT-style, and LLaMA-style models trained from scratch with 200 clients show that, under 40% population-level target manipulation, FedLNS achieves lower test perplexity than the strongest of six baselines for all three architectures under both IID (independently and identically distributed) and non-IID data partitions.

---

## 论文详细总结（自动生成）

以下是对论文《FedLNS: Leverage LayerNorm Signature Modeling to Mitigate Adversarial Manipulation in Federated LLMs》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：联邦学习（FL）允许大语言模型（LLM）在不接触原始数据的情况下利用分布式私有文本进行训练。然而，这种隐私保护机制也带来了安全隐患：服务器无法验证客户端本地的训练过程。
*   **核心问题**：恶意客户端可以通过“目标操纵攻击”（Target Manipulation），即在损坏的标签或错误的上下文-标记关联上进行训练，从而污染全局模型。这会导致模型性能下降，并增加生成不可靠内容或“幻觉”的风险。
*   **研究目标**：在不增加通信开销、不接触私有数据且无需预先训练检测器的情况下，在服务器端识别并过滤这些恶意更新。

### 2. 论文提出的方法论（FedLNS）
*   **核心思想**：利用 Transformer 架构中归一化层（LayerNorm/RMSNorm）参数的变化作为客户端更新的“签名”。作者认为，归一化层参数控制着隐藏表示的缩放和偏移，能以极小的维度捕捉到本地训练对模型内部状态的影响。
*   **关键技术流程**：
    1.  **签名提取**：服务器从客户端返回的模型中提取归一化层的可训练参数（如权重和偏置），计算其相对于全局模型的改变量。
    2.  **历史银行（History Bank）**：服务器维护一个存储各客户端最新签名的数据库，用于构建跨客户端的参考基准。
    3.  **鲁棒统计建模**：利用历史银行中的数据，计算每一层签名的坐标中位数（Median）和绝对中位差（MAD），对当前轮次的签名进行标准化处理。
    4.  **BIC 引导的 GMM 筛选**：使用高斯混合模型（GMM）对标准化后的特征进行聚类。通过贝叶斯信息准则（BIC）自动判断当前参与者是属于单一群体（全良性）还是两个群体（良性与恶意）。
    5.  **更新保留**：若识别出两个群体，则保留偏离度较小的群体（视为良性），并将其全模型更新用于全局聚合。

### 3. 实验设计
*   **数据集与场景**：
    *   **GPT 式模型**：使用 WikiText-2 数据集，进行因果语言建模（下一标记预测）。
    *   **BERT 式模型**：使用 Tiny Shakespeare 数据集，进行掩码语言建模。
    *   **LLaMA 式模型**：使用 StackOverflow 帖子数据，进行因果语言建模。
*   **数据划分**：对比了独立同分布（IID）和非独立同分布（Non-IID，每个客户端仅持有两个数据分片）两种场景。
*   **对比方法（Benchmarks）**：对比了六种主流聚合算法，包括非鲁棒的 **FedAvg**，以及 **Norm-Bound**（范数限制）、**Median**（中位数）、**Trimmed Mean**（修剪均值）、**Multi-Krum** 和 **FLAME**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 AdamW 优化器，设置了特定的学习率（如 $3\times10^{-4}$ 或 $10^{-5}$）和权重衰减（0.01），并规定了通信轮数为 200 轮。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了三种主流 Transformer 变体，设置了 200 个客户端，恶意客户端比例从 0% 扫描至 40%。
*   **充分性评价**：
    *   **多维度评估**：不仅测量了测试困惑度（Perplexity），还引入了标记级语义熵（Semantic Entropy）来衡量模型生成的不确定性。
    *   **消融实验**：针对“银行激活阈值”（即收集多少客户端签名后开始防御）和“GMM 选择规则”进行了详细的消融研究。
    *   **客观性**：实验在 IID 和 Non-IID 下均表现出一致性，且对比了多种需要已知恶意比例的强基准方法，证明了 FedLNS 在未知攻击比例下的优越性。

### 6. 主要结论与发现
*   **性能卓越**：在 40% 恶意操纵的极端情况下，FedLNS 在所有架构和数据分布下的困惑度均低于所有基准方法。
*   **鲁棒性强**：相比于 FedAvg 随攻击比例增加而性能剧烈下降，FedLNS 的性能曲线非常平稳。
*   **降低不确定性**：FedLNS 有效限制了受污染更新的并入，显著降低了模型在测试集上的语义熵，意味着生成的可靠性更高。
*   **理论支撑**：论文通过数学证明展示了归一化参数变化与标记偏好（Token Margin）之间的直接关联，为该方法的有效性提供了理论依据。

### 7. 优点
*   **轻量化与零开销**：无需客户端传输额外元数据，不增加通信负担。
*   **架构感知**：利用 Transformer 固有的归一化层特性，比通用的几何距离防御更具针对性。
*   **无监督防御**：不需要受信任的服务器数据，也不需要预先标记恶意样本，具有很强的实用性。

### 8. 不足与局限
*   **攻击假设限制**：该防御机制假设恶意客户端在群体中占少数（<50%），且未考虑针对 FedLNS 签名的“自适应攻击”（即恶意客户端故意模仿良性签名的行为）。
*   **架构依赖**：虽然覆盖了三种模型，但其核心依赖于归一化层的存在，对于不使用此类结构的非 Transformer 模型可能不适用。
*   **激活延迟**：为了构建准确的历史银行，系统在训练初期需要一定的观察期（激活阈值），这可能导致防御在极早期阶段不可用。

（完）
