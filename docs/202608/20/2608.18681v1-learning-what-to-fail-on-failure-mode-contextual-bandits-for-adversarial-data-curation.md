---
title: "Learning What to Fail On: Failure-Mode Contextual Bandits for Adversarial Data Curation"
title_zh: 学习在何处失败：用于对抗性数据策展的失败模式上下文多臂老虎机
authors: "Roie Kazoom, Ofir Cohen, Rami Puzis, Asaf Shabtai, Ofer Hadar"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.18681v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 用于提高LLM鲁棒性的对抗性数据策划
tldr: 本研究提出一种失败感知的对抗性检索增强框架，旨在提升自然语言理解模型的鲁棒性。该方法将对抗性数据策展建模为“失败模式上下文多臂老虎机”问题，利用检索增强提示生成候选样本，并通过LLM评测集群将其归类为循环失败模式。通过随机策略动态选择最有价值的失败模式进行重训，平衡了鲁棒性增益、遗忘和数据成本。实验证明，该框架在SNLI、ANLI等多个基准测试中显著优于现有方法，且无需额外人工标注。
selection_source: fresh_fetch
motivation: 针对现有对抗性数据增强方法难以自适应选择模型薄弱环节且依赖人工标注的问题，提出一种自动化的失败模式学习机制。
method: 采用上下文多臂老虎机策略，结合检索增强生成、LLM自动验证和失败模式聚类，动态优化对抗样本的选择与重训过程。
result: 在SNLI、ANLI和MultiNLI等任务上显著提升了RoBERTa模型的准确率，并在FEVER事实核查任务中展现了良好的迁移能力。
conclusion: 该框架通过自适应学习模型失败模式，实现了高效、可扩展且无需人工干预的深度学习模型鲁棒性增强。
---

## 摘要
我们提出了一种感知失败的对抗性检索增强框架，用于提高自然语言理解的鲁棒性。我们的方法并非使用固定的奖励阈值来选择合成样本，而是将对抗性数据策展建模为一个失败模式上下文多臂老虎机问题。候选样本通过检索增强提示生成，由当前目标模型过滤，由大语言模型（LLM）评判器集成自动验证，并聚类为循环出现的失败模式。随后，随机策略选择要采样用于重训练的失败模式，并使用基于验证的奖励进行更新，该奖励平衡了鲁棒性增益、遗忘和数据成本。这使得数据策展器本身成为学习智能体，从而能够在训练轮次中自适应地选择最有用的模型失败案例。在标准基准测试中，我们的方法将 RoBERTa-base 在 SNLI 上的准确率从 88.48% 提高到 92.60%，在 ANLI 上从 75.04% 提高到 80.95%，在 MultiNLI 上从 54.67% 提高到 71.99%，同时持续优于先前的对抗性增强方法。我们进一步展示了在 FEVER 事实核查任务上的迁移能力，使用 RoBERTa-large 达到了 79.86% 的 FEVER 分数和 82.45% 的准确率。最后，我们提供了一个理论解释，表明在所述假设下，失败模式采样可以减少与捷径对齐的梯度贡献，同时诱导有界的分布漂移。通过结合检索、自动验证、上下文多臂老虎机失败选择和受控对抗性重训练，我们的框架实现了可扩展的鲁棒性提升，且无需额外的人工标注。

## Abstract
We introduce a failure-aware adversarial retrieval-augmented framework for improving robustness in natural language understanding. Rather than selecting synthetic examples with a fixed reward threshold, our method formulates adversarial data curation as a failure-mode contextual bandit problem. Candidate examples are generated with retrieval-augmented prompting, filtered by the current target model, automatically validated by an LLM judge ensemble, and clustered into recurring failure modes. A stochastic policy then selects which failure modes to sample for retraining, and is updated using validation-based reward that balances robustness gains, forgetting, and data cost. This makes the data curator itself the learning agent, enabling adaptive selection of the most useful model failures across training rounds. On standard benchmarks, our approach improves RoBERTa-base accuracy from 88.48% to 92.60% on SNLI, from 75.04% to 80.95% on ANLI, and from 54.67% to 71.99% on MultiNLI, while consistently outperforming prior adversarial augmentation methods. We further demonstrate transfer to FEVER fact verification, achieving up to 79.86\% FEVER score and 82.45\% accuracy with RoBERTa-large. Finally, we provide a theoretical interpretation showing that, under stated assumptions, failure-mode sampling can reduce shortcut-aligned gradient contributions while inducing bounded distributional drift. By combining retrieval, automated validation, contextual-bandit failure selection, and controlled adversarial retraining, our framework enables scalable robustness improvement without additional human annotation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为“失败模式上下文多臂老虎机”（Failure-Mode Contextual Bandits）的框架，旨在通过自动化的对抗性数据策展（Data Curation）来提升自然语言理解（NLU）模型的鲁棒性。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管预训练模型在自然语言推理（NLI）等任务上表现优异，但它们往往依赖于虚假的统计捷径（Spurious Shortcuts），在面对对抗样本或分布外数据时表现脆弱。
*   **研究动机**：现有的对抗数据生成方法通常依赖于静态过滤、启发式规则或昂贵的人工标注。此外，并非所有的模型失败案例都具有同等的训练价值。
*   **整体含义**：论文提出将数据策展过程建模为一个学习问题，让“策展器”本身成为一个智能体，自适应地学习在模型的不同训练阶段，哪些类型的失败模式（Failure Modes）最值得被采纳以进行重训。

### 2. 论文提出的方法论
该框架是一个闭环的自适应系统，包含以下关键步骤：
*   **检索与生成**：利用混合检索（BGE语义检索 + BM25词法检索）获取标签平衡的少样本上下文，驱动LLM（如LLaMA-4）生成候选对抗样本。
*   **失败过滤与自动验证**：
    *   **过滤**：仅保留导致当前目标模型预测错误的样本。
    *   **验证**：使用LLM评测集群（Gemma-3, Phi-4, Qwen3）进行一致性投票，确保样本标签的正确性，消除噪声。
*   **失败模式聚类**：将验证后的失败样本嵌入向量空间，并利用无监督聚类将其归类为不同的“失败模式”（如否定词错误、实体误匹配、数值推理失败等）。
*   **上下文多臂老虎机（Contextual Bandit）策略**：
    *   **状态（State）**：包含聚类大小、目标模型损失、预测熵、分类置信度间距、标签分布、检索分数等统计特征。
    *   **动作（Action）**：策略 $\pi_\theta$ 决定是否从该失败模式中采样。
    *   **奖励（Reward）**：基于重训后模型在验证集上的鲁棒性提升、原始分布的遗忘程度以及数据成本的综合评分。
*   **理论支撑**：论文通过数学证明，失败模式采样能有效减少模型对捷径特征梯度的依赖，并保证分布漂移在可控范围内。

### 3. 实验设计
*   **数据集**：主要针对 NLI 任务，包括 **SNLI**（标准）、**ANLI**（对抗性）、**MultiNLI**（多领域）。此外，还扩展到了 **FEVER**（事实核查）任务以验证迁移能力。
*   **Benchmark 与对比方法**：
    *   **目标模型**：RoBERTa-base/large, DeBERTa-v3, 以及轻量级模型（SmolLM2, Qwen3）。
    *   **对比方法**：原始模型、基于改写（Paraphrasing）的增强、大规模合成数据集（GNLI）、以及不同检索策略和固定阈值过滤的方法。

### 4. 资源与算力
*   **算力设备**：实验使用了单张 **NVIDIA A100 GPU**。
*   **训练时长**：每个训练 Epoch 平均耗时约 **3.11 分钟**。
*   **模型使用**：生成端使用了 LLaMA-4-Scout-17B，验证端使用了 Gemma-3-27B、Phi-4 和 Qwen3-32B 等大参数模型。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准数据集上进行了详尽测试，涵盖了从 360M 到 Large 级别的多种模型架构。
*   **消融实验**：非常充分。作者分别验证了多臂老虎机策略、失败模式聚类、评测机验证、检索权重（$\alpha$）以及数据混合比例（$\lambda_{mix}$）对最终结果的影响。
*   **客观性**：通过引入“遗忘惩罚”和“数据成本”作为奖励函数的一部分，客观地评估了鲁棒性提升与通用能力保持之间的平衡。

### 6. 论文的主要结论与发现
*   **性能提升显著**：在 SNLI 上将 RoBERTa-base 的准确率从 88.48% 提升至 92.60%，在 ANLI 上从 75.04% 提升至 80.95%。
*   **自适应优于静态**：学习到的失败模式选择策略显著优于随机采样或基于损失/熵的简单启发式策略。
*   **跨任务迁移**：该方法在 FEVER 事实核查任务上也取得了 SOTA 级别的表现，证明了框架的通用性。
*   **数据效率**：相比于盲目增加数百万条合成数据，精准选择数千条“高质量失败样本”对提升鲁棒性更有效。

### 7. 优点
*   **无需人工干预**：实现了从生成、过滤、验证到选择的全自动化流程，极大地降低了对抗训练的成本。
*   **自适应进化**：策展策略随目标模型的能力演进，能够持续发现模型新的薄弱环节。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了关于梯度对齐和分布边界的理论解释。

### 8. 不足与局限
*   **对强LLM的依赖**：框架的成功高度依赖于高性能 LLM 作为生成器和评测机，这在算力受限的环境下可能难以实施。
*   **任务局限性**：目前主要集中在分类和推理任务（NLI/Fact Verification），对于生成式任务（如摘要、对话）的适用性尚待验证。
*   **计算开销**：虽然重训很快，但多轮迭代中调用多个大模型进行生成和验证的推理成本较高。

（完）
