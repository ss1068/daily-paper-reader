---
title: "Tripwire: Triggering Aligned Refusal via Statistically Certified Safety Neurons"
title_zh: Tripwire：通过统计认证的安全神经元触发对齐的拒绝行为
authors: "Wei Zhao, Zhe Li, Peixin Zhang, Jun Sun"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14392v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过安全神经元防御LLM越狱攻击
tldr: "本研究提出Tripwire，一种针对大语言模型越狱攻击的免训练防御方法。针对现有神经元干预方法易损害模型通用能力且始终开启的弊端，Tripwire通过统计假设检验识别安全特异性神经元，并在检测到攻击时将其锚定在有害条件下的平均激活值，从而触发模型内置的拒绝机制。该方法在保持极高通用性能的同时，能将攻击成功率降至2%以下，是目前性能损耗最小的防御方案之一。"
selection_source: fresh_fetch
motivation: 现有的神经元级防御方法往往会显著损害模型在正常任务中的表现，且缺乏针对攻击行为的动态触发机制。
method: 通过受控错误发现率的假设检验识别安全神经元，并利用触发式钳位技术将这些神经元锁定在特定激活状态以诱导模型拒绝回答。
result: "在四种对齐模型和攻击场景下，Tripwire将平均攻击成功率降至2%以内，且在MT-Bench上的性能损失仅为0.5%至5.3%。"
conclusion: Tripwire证明了通过精准干预少量安全神经元，可以在极低性能损耗下实现对越狱攻击的高效防御。
---

## 摘要
神经元和路径级干预为防御大语言模型（LLMs）免受越狱攻击提供了最细粒度的途径，但现有方法往往难以兑现这一承诺，即它们通常会显著损害模型效用。具体而言，一类研究通过抑制毒性神经元来消除有害语义，但由于此类语义分布在整个网络中，阻断每条路径会导致巨大的干预足迹。另一类研究则侧重于利用外部分类器识别安全神经元。虽然这些方法很有前景，但现有手段同样会损害对模型效用至关重要的神经元。此外，这两种方法始终处于开启状态，因此即使在没有攻击的情况下也会干扰良性请求。为了解决这些局限性，我们提出了 Tripwire，这是一种无需训练的防御方法。它首先通过受错误发现率控制的逐神经元假设检验以及效用特异性过滤器来识别安全特异性神经元。基于此识别，一种触发式钳制（clamp）将选定的神经元保持在其有害条件下的平均激活状态，从而注入内部有害输入信号，触发模型在对齐过程中学习到的拒绝行为。该钳制通过两种可证明等效的部署模式实现：检测器门控的推理时干预和离线偏置补丁权重编辑。在四个经过安全对齐的 LLM 和四种代表性攻击上的广泛实验表明，Tripwire 将平均攻击成功率降低至最高 2.0%，同时在 MT-Bench 上的效用下降仅为 0.5% 至 5.3%，在所有防御方法中表现最优。代码见 https://anonymous.4open.science/r/Tripwire-65C4。

## Abstract
Neuron- and path-level interventions offer the finest-grained route to defending large language models (LLMs) against jailbreak attacks, yet existing methods fall short of this promise, i.e., they often compromise model utility significantly. Specifically, one line of work suppresses toxic neurons to erase harmful semantics, but since such semantics are distributed across the network, blocking every pathway forces a large intervention footprint. An alternative line of research focus on identify safety neurons using external classifiers. While promising, the existing approaches suffer from compromising neurons that are important for the model utility as well. Moreover, both approaches remain always on and thus perturb every benign request even when no attack is present. To address these limitations, we present \ours{}, a training-free defense that first identifies safety-specific neurons through per-neuron hypothesis tests under false-discovery-rate control together with a utility-specificity filter. Based on this identification, a trigger-style clamp holds the selected neurons at their harmful-conditional mean activations, injecting an internal harmful-input signal that triggers the refusal behavior learned during alignment. The clamp is then realized by two provably equivalent deployment modes, namely a detector-gated inference-time intervention and an offline bias-patch weight edit. Extensive experiments across four safety-aligned LLMs and four representative attacks demonstrate that \ours{} reduces the average attack success rate to at most 2.0\% while incurring a utility drop of only 0.5\% to 5.3\% on MT-Bench, the smallest among all defenses. Code is available at https://anonymous.4open.science/r/Tripwire-65C4.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Tripwire** 的新型大语言模型（LLM）防御框架，旨在通过精准识别和干预极少数“安全神经元”来抵御越狱攻击，同时最大限度地保留模型的通用能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的神经元级防御方法（如抑制毒性神经元或利用外部分类器识别安全路径）存在三大缺陷：
    1.  **干预足迹过大**：有害语义分布广泛，试图阻断所有路径会误伤大量正常神经元。
    2.  **特异性不足**：现有方法难以区分“安全专用神经元”和“通用重要神经元”。
    3.  **始终开启（Always-on）**：即使在处理良性请求时也会进行干预，导致模型性能（Utility）显著下降。
*   **研究背景**：越狱攻击（如 GCG、AutoDAN）日益自动化且高效，而理想的防御应在保持细粒度的同时，实现“低效用损失”和“高防御强度”的平衡。

### 2. 方法论
Tripwire 的核心思想是**“触发”而非“抹除”**。它不试图阻断有害语义的传播，而是通过激活模型内部已有的拒绝机制来应对攻击。
*   **统计识别漏斗（Identification Funnel）**：
    1.  **方向过滤**：仅保留在有害输入下激活程度更高的神经元。
    2.  **显著性过滤**：利用 **Welch t-检验** 结合 **BH-FDR（错误发现率控制）**，从统计学上保证选定神经元的可靠性，减少误选。
    3.  **效用特异性过滤**：排除那些在通用任务（如 MMLU）中也高度激活的神经元，确保干预目标仅限于安全相关功能。
*   **触发式钳位（Trigger-style Clamp）**：
    *   将选定的安全神经元强制锁定（Clamp）在其“有害条件平均激活值”上。这相当于向模型注入一个内部信号：“当前输入是有害的”，从而诱导模型执行对齐阶段学到的拒绝行为。
*   **双部署模式**：
    1.  **推理模式**：使用轻量级检测器门控。仅在检测到潜在攻击时触发钳位，良性请求则完全不经过干预。
    2.  **离线模式（偏置补丁）**：通过修改权重（将权重行归零并修改偏置项），将钳位逻辑固化到模型中，实现零推理开销。

### 3. 实验设计
*   **目标模型**：Llama-2-7B, Llama-3.1-8B, Qwen2.5-7B, Qwen2.5-32B。
*   **攻击方法**：GCG（梯度优化）、AmpleGCG（生成式）、AutoDAN（黑盒迭代）、Jailbreak-R1（强化学习驱动）。
*   **对比基准（Baselines）**：
    *   推理侧防御：RepE, TraceRouter。
    *   权重编辑防御：LED, DELMAN。
*   **评估指标**：攻击成功率（ASR）、MT-Bench 评分（通用能力）、过度拒绝率（Over-refusal）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100-80GB** 服务器上运行。
*   **耗时**：神经元识别过程（离线分析）在每个模型上大约耗时 **79 到 92 秒**。
*   **推理开销**：推理模式下的时间开销约为原始模型的 **1.15倍**（主要来自检测器和 Hook 操作），而离线模式（权重编辑）则为 **0 额外开销**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同架构和规模的模型，以及 4 种代表不同范式的越狱攻击，实验设置较为全面。
*   **消融实验**：对比了 Tripwire 统计漏斗与传统的基于探针（Probe-based）的选择方法，证明了统计过滤在保护模型效用方面的优越性。
*   **公平性**：使用了 GPT-5.4 作为客观评判器（Judge），并提供了详细的 Prompt 模板，确保了评估的一致性。

### 6. 主要结论与发现
*   **高效防御**：在 top-2500 神经元预算下，Tripwire 将平均 ASR 降至 **2.0% 以下**。
*   **极低损耗**：在 MT-Bench 上的效用下降仅为 **0.5% - 5.3%**，显著优于 TraceRouter 和 RepE 等方法。
*   **神经元分布**：分析发现安全神经元主要集中在模型的**中高层**（如 Llama-2 的 12-16 层），这符合“早期层处理语法，后期层处理语义和行为”的认知。
*   **等效性**：理论和实验均证明，推理侧钳位与离线权重编辑在防御效果上是数学等效的。

### 7. 优点
*   **免训练（Training-free）**：不需要昂贵的微调或对抗训练。
*   **统计严谨性**：引入 FDR 控制，解决了神经元级干预中常见的“误伤”问题。
*   **防御范式创新**：从“阻断有害路径”转变为“主动触发拒绝信号”，对未知攻击的鲁棒性更强。
*   **部署灵活**：提供检测器门控和永久固化两种方案，适应不同应用场景。

### 8. 不足与局限
*   **依赖少量样本**：识别过程仍需要少量的有害和良性提示词（各 200 条）作为参考集。
*   **过度拒绝风险**：虽然比基准方法好，但在始终开启的离线模式下，仍存在一定的过度拒绝现象（尤其是在 Llama-3.1 上）。
*   **评估偏差**：防御效果高度依赖于作为 Judge 的 LLM 的判断标准，可能存在模型偏见。

（完）
