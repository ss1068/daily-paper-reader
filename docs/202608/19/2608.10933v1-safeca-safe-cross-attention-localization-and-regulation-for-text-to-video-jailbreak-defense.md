---
title: "SafeCA: Safe Cross-Attention Localization and Regulation for Text-to-Video Jailbreak Defense"
title_zh: SafeCA：面向文本生成视频越狱防御的安全交叉注意力定位与调节
authors: "Siyuan Liang, Yupeng Qiu, Junfeng Fang, Rong-Cheng Tu, Jiaxing Huang, Dacheng Tao"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10933v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对文本生成视频越狱防御的特征级防御机制
tldr: 针对文本生成视频（T2V）模型易受越狱攻击且现有防御效率低、语义失真等问题，本文提出SafeCA防御机制。通过分析交叉注意力特征空间中正常与越狱样本的累积分离效应，SafeCA实现了特征级的定位与调节。该方法利用注意力稳定性分析识别关键区域，通过掩码和能量归一化抑制异常激活，并结合轻量级适配器重定向语义流，在极低推理开销下显著降低了越狱成功率并保持了语义一致性。
selection_source: fresh_fetch
motivation: 现有T2V防御方法存在高延迟和语义扭曲问题，且未充分利用扩散过程中交叉注意力特征的异常分离特性。
method: 提出SafeCA机制，通过稳定性分析定位关键防御区域，结合能量归一化掩码、语义适配器及特征回传信号来抑制异常激活和恶意词元。
result: "在主流T2V模型上将越狱成功率降低约20%，推理开销仅增加0.1秒，同时有效维持了文本与视频的语义一致性。"
conclusion: SafeCA为T2V模型提供了一种高效、可部署且不破坏语义的架构级安全防护新范式。
---

## 摘要
文本生成视频 (T2V) 生成模型在实际部署中容易受到越狱攻击，导致其产生有害或不当内容。现有的防御方法主要依赖于输入过滤或重构，这不仅会产生高计算延迟，还往往会扭曲语义。为了解决这些问题，我们通过实验系统地分析了交叉注意力特征空间中干净样本与越狱样本之间的差异，首次揭示了两者在扩散过程中存在的累积分离效应以及线性可分性逐渐增强的趋势。基于这一发现，我们提出了 SafeCA，一种用于安全交叉注意力定位和正则化的特征级防御机制。首先，我们通过对单次推理中从干净提示词收集的交叉注意力特征进行注意力稳定性分析，识别出关键的防御区域和数值。其次，SafeCA 通过带有能量归一化的注意力掩码来缓解异常激活，并引入轻量级语义空间适配器来重定向异常语义流。此外，我们通过将特征异常信号反向传播到输入提示词，检测并抑制潜在的恶意标记，从而增强了该防御在商业模型中的可部署性。实验结果表明，SafeCA 在主流 T2V 模型上将越狱成功率降低了约 20%，几乎不增加推理开销（+0.1s），并保持了良好的文本-视频语义一致性。总之，SafeCA 为 T2V 生成模型提供了一种架构级、可部署的保护范式。

## Abstract
Text-to-Video (T2V) generative models are vulnerable to jailbreak attacks in real-world deployment, leading them to produce harmful or inappropriate content. Existing defense approaches mainly rely on input filtering or reconstruction, which not only incur high computational latency but also tend to distort semantics. To address these issues, we experimentally and systematically analyze the differences between clean and jailbreak samples in the cross-attention feature space, revealing for the first time a cumulative separation effect and a progressively increasing trend of linear separability between the two during the diffusion process. Based on this insight, we propose SafeCA, a feature-level defense mechanism for safe cross-attention localization and regularization. Firstly, we identify key defensive regions and values through attention stability analysis using cross-attention features collected from clean prompts within a single inference. Secondly, SafeCA mitigates anomalous activations via attention masking with energy normalization and introduces a lightweight semantic-space adapter to redirect abnormal semantic flows. Furthermore, we detect and suppress potentially malicious tokens by back-propagating feature anomaly signals to the input cue words, thereby enhancing the deployability of the defense in commercial models. Experimental results show that SafeCA reduces the jailbreak success rate by about 20% on mainstream T2V models, adds almost no inference overhead (+0.1s), and maintains good text-video semantic consistency. Overall, SafeCA provides an architecture-level, deployable protection paradigm for T2V generation models.

---

## 论文详细总结（自动生成）

以下是对论文《SafeCA: Safe Cross-Attention Localization and Regulation for Text-to-Video Jailbreak Defense》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：文本生成视频（T2V）模型在部署中面临“越狱攻击”（Jailbreak Attacks），攻击者通过精心设计的提示词诱导模型生成暴力、色情或歧视性内容。
*   **研究动机**：现有的防御手段（如输入过滤或提示词重构）存在两大缺陷：一是**语义失真**，容易误伤正常用户的意图；二是**推理延迟高**，如 T2VShield 等系统级方法需要大规模重写或视频检测，难以实时部署。
*   **核心发现**：作者首次通过实验揭示，在扩散过程中，越狱样本与干净样本在**交叉注意力（Cross-Attention）**特征空间中存在“累积分离效应”和“线性可分性增强”趋势。这意味着交叉注意力是恶意语义传播的关键路径。

### 2. 方法论：核心思想与关键技术
SafeCA 是一种特征级的防御机制，无需修改模型权重，其核心流程包括：
*   **分层注意力定位 (HAL)**：
    *   通过单次推理干净提示词，收集各扩散步骤和 Transformer 块的交叉注意力特征。
    *   利用统计分析（均值、方差、PCA 主成分）识别出对异常语义最敏感的“关键步骤”和“关键块”。
*   **交叉注意力正则化 (CAR)**：在推理过程中对选定的关键区域进行动态调节，包含三个子模块：
    1.  **注意力掩码 (Attention Masking)**：根据干净样本的统计阈值，对超出范围的异常 Key 响应进行指数级衰减。
    2.  **能量归一化 (Energy Normalization)**：通过裁剪（Clip）操作限制注意力幅值的波动，防止局部能量过分集中。
    3.  **语义适配器 (Semantic Adapter)**：利用 PCA 提取的干净语义子空间，将偏离的语义流重定向回安全区域。
*   **黑盒部署增强**：将特征层的异常信号反向传播至输入端，识别恶意 Token，从而支持在商业闭源模型（如 Sora）上的提示词重写防御。

### 3. 实验设计
*   **数据集/场景**：
    *   **T2VSafetyBench**：包含 12 个安全维度的 280 个越狱提示词。
    *   **SafeWatch**：包含 6 个维度的 300 个野外越狱提示词。
    *   **WebVid-10M**：选取 100 个干净提示词用于评估模型可用性。
*   **对比方法**：包括 SAFREE、VideoEraser（基于嵌入的过滤）、T2VShield（输入重写+输出检测）以及传统的关键词过滤。
*   **攻击模型**：使用了 T2V-OptJail、AutoDAN、DACA、SneakyPrompt 等多种先进的越狱攻击手段。
*   **评估指标**：安全性指标（ASR 越狱成功率、GPT-4o 评分、人工审核 ASR）；可用性指标（语义一致性 $D_{semantic}$、图像质量 SSIM、时间相干性 LPIPS）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA A40 GPU** 上完成。
*   **训练/推理时长**：该方法属于推理侧防御，无需重新训练模型。在推理开销上，SafeCA 表现极佳，平均每段视频仅增加 **0.1 秒** 的延迟，远低于 T2VShield 的 17.1 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了开源模型（Open-Sora, CogVideo）和商业闭源模型（Sora, Kling, Luma）的跨模型评估。
*   **消融实验**：对 HAL 的步骤选择、块数量、干净样本规模以及 CAR 的三个子模块（AM, EN, SA）分别进行了详尽的消融分析。
*   **充分性评价**：实验设计较为充分，不仅验证了防御的有效性（ASR 降低），还通过多项指标证明了其对视频质量的保护。对比实验涵盖了当前最前沿的防御基准，结果具有较强的说服力。

### 6. 主要结论与发现
*   **防御效果显著**：SafeCA 在主流模型上将越狱成功率降低了约 **20%**（相对降幅约 63%），在对抗强力攻击（如 DACA）时表现尤为突出。
*   **高效性**：在保持极低推理延迟的同时，SafeCA 在语义一致性和视频质量指标（SSIM, LPIPS）上达到了最优或次优水平。
*   **架构级洞察**：验证了交叉注意力模块是 T2V 模型中恶意语义注入的核心通道，通过特征正则化可以实现比文本过滤更精准的防御。

### 7. 优点
*   **即插即用**：无需修改模型参数，适用于各种基于扩散模型的 T2V 架构。
*   **低开销**：0.1s 的额外延迟几乎可以忽略不计，非常适合商业化实时部署。
*   **可解释性**：通过特征异常信号回传，能够定位到具体的恶意 Token，增强了防御的透明度。
*   **平衡性好**：在大幅降低风险的同时，较好地保留了原始提示词的生成质量。

### 8. 不足与局限
*   **复杂语义覆盖有限**：对于具有高度复杂逻辑或长程时间隐喻的越狱提示词，该方法可能存在漏检风险。
*   **静态统计依赖**：HAL 依赖于预先收集的干净样本统计特性，如果模型版本更新或分布发生剧烈偏移，可能需要重新收集参考特征。
*   **时间维度建模不足**：目前主要侧重于空间和步骤上的正则化，未来仍需探索更深入的时间序列建模以应对动态演化的攻击。

（完）
