---
title: "HiRoute: Hierarchical Routed Prompt Tuning for Safety Alignment of Large Language Models"
title_zh: HiRoute：用于大语言模型安全对齐的分层路由提示微调
authors: "Fangzhou Chen, Shiji Zhao, Mengyang Wang, Qihui Zhu, Ranjie Duan, Maoxun Yuan, Xingxing Wei"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.12821v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 用于安全对齐和越狱防御的分层提示微调
tldr: 针对大语言模型在安全对齐中面临的静态提示词难以兼顾多类别风险与过度拒绝的问题，本文提出 HiRoute 框架。该框架采用分层路由机制，将通用安全控制与特定类别引导分离。通过轻量级路由器识别风险并动态组合多个专家提示词，HiRoute 在提升模型安全性的同时，显著降低了过度拒绝率并保持了回复的质量，为参数高效的安全对齐提供了新方案。
selection_source: fresh_fetch
motivation: 现有的提示词微调方法多采用静态设计，难以在处理多样化安全风险的同时避免对无害请求的过度拒绝。
method: 提出一种分层路由提示词微调框架，利用路由器动态组合共享的粗粒度提示词与风险特定的细粒度专家提示词。
result: 实验表明 HiRoute 在多个安全基准测试中实现了极高的安全率，同时有效减少了过度拒绝并保持了通用任务的性能。
conclusion: HiRoute 证明了通过输入自适应的分层提示词路由，可以在不牺牲模型通用能力的前提下实现精准且高效的安全对齐。
---

## 摘要
大语言模型（LLMs）在面对有害请求和越狱攻击时仍然十分脆弱。基于提示微调（prompt tuning）的参数高效安全对齐方法通常依赖于单个全局提示或外部选择的提示模块。这种静态设计难以在维持跨类别安全边界的同时，针对特定风险生成建设性的回复，并避免对良性输入的过度拒绝。为了解决这些局限性，我们提出了 HiRoute，这是一种输入自适应的分层提示微调框架，它将类别无关的安全控制与类别特定的响应引导相分离。HiRoute 首先在从冻结的 LLM 中提取的表示上训练一个轻量级分层路由器，以共同检测有害意图并预测多标签风险评分。随后，它冻结主干模型和路由器，并使用带有交替梯度更新的偏好优化来学习一个共享的粗粒度提示和一组作为连续嵌入的细粒度提示专家。在推理阶段，良性输入会绕过安全分支，而风险输入则使用共享提示以及由路由器加权的风险特定提示专家混合进行处理。在三个指令微调模型上的实验表明，HiRoute 在多个安全基准测试中实现了较高的安全率，同时保持了安全回复的帮助性，减少了过度拒绝，并在通用任务上保持了具有竞争力的性能。

## Abstract
Large language models (LLMs) remain vulnerable to harmful requests and jailbreak attacks. Parameter-efficient safety alignment methods based on prompt tuning typically rely on a single global prompt or externally selected prompt modules. Such static designs struggle to maintain a cross-category safety boundary while generating constructive responses tailored to specific risks and avoiding over-refusal of benign inputs. To address these limitations, we propose HiRoute, an input-adaptive hierarchical prompt-tuning framework that separates category-agnostic safety control from category-specific response guidance. HiRoute first trains a lightweight hierarchical router on representations extracted from a frozen LLM to jointly detect harmful intent and predict multi-label risk scores. It then freezes both the backbone model and the router and uses preference optimization with alternating gradient updates to learn a shared coarse-grained prompt and a set of fine-grained prompt experts as continuous embeddings. At inference time, benign inputs bypass the safety branch, whereas risky inputs are processed using the shared prompt together with a router-weighted mixture of risk-specific prompt experts. Experiments across three instruction-tuned models show that HiRoute achieves high safety rates across multiple safety benchmarks while preserving safe-response helpfulness, reducing over-refusal, and maintaining competitive performance on general-purpose tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《HiRoute: Hierarchical Routed Prompt Tuning for Safety Alignment of Large Language Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）虽然能力强大，但仍面临有害请求和越狱攻击的威胁。传统的安全对齐方法（如 SFT 和 RLHF）虽然有效，但训练和存储成本高昂，且容易导致“过度拒绝”（即误伤良性请求）或生成过于死板的拒绝回复。
现有的**提示微调（Prompt Tuning）**方法虽然参数高效，但多采用静态设计：
*   **单一全局提示**：往往导致通用的保守拒绝，缺乏针对特定风险的建设性引导。
*   **固定模块化提示**：难以应对现实中复杂的、包含多种风险类别的请求。
**研究动机**：能否在不改变模型参数的前提下，通过一种输入自适应的机制，既维持稳固的安全边界，又能针对不同风险提供差异化的、有帮助的安全回复？

### 2. 论文提出的方法论：HiRoute 框架
HiRoute 提出了一种**分层路由提示微调**框架，其核心思想是将“类别无关的安全控制”与“类别特定的响应引导”解耦。

*   **核心组件**：
    1.  **分层路由器 (Hierarchical Router)**：基于冻结 LLM 的隐藏层表示，包含一个粗粒度预测头（判断是否安全）和一个细粒度预测头（预测多标签风险得分，如网络犯罪、隐私侵犯等）。
    2.  **分层提示词结构**：由一个**共享粗粒度提示 (Shared Coarse Prompt)** 和一组**细粒度专家提示 (Fine-grained Prompt Experts)** 组成。
*   **关键技术细节**：
    *   **两阶段训练**：第一阶段训练轻量级路由器；第二阶段冻结主干模型和路由器，仅通过**直接偏好优化 (DPO)** 优化提示词参数。
    *   **交替梯度更新**：在训练提示词时，交替屏蔽粗/细粒度提示词的梯度，以减少参数干扰，确保两者协同工作。
    *   **动态组合**：推理时，路由器根据风险得分对专家提示词进行加权混合（Soft-mixture），并与共享提示词拼接。
*   **算法流程**：
    *   **输入自适应推理**：良性输入（由路由器判定）直接绕过安全分支，确保通用能力不受损；风险输入则激活分层提示词引导模型生成既安全又具有建设性的回复。

### 3. 实验设计
*   **数据集**：
    *   **训练**：WildGuardMix（用于粗粒度分类）、PKU-SafeRLHF（用于细粒度风险标注和偏好对齐）。
    *   **安全评估 (Benchmark)**：StrongReject, AdvBench, JailbreakBench。
    *   **通用能力与过度拒绝**：GSM8K（数学）、MT-Bench（对话质量）、TruthfulQA（事实性）、XSTest（过度拒绝测试）。
*   **对比方法**：
    *   **Base**：原始指令微调模型。
    *   **RPO / DRO / ACD**：现有的基于提示词优化或对比解码的安全防御基准方法。
*   **实验模型**：Mistral-7B-Instruct-v0.3, Vicuna-7B-v1.5, Zephyr-7B-Beta。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   路由器训练：共 8 个 epoch（前 4 个仅训练粗粒度，后 4 个联合训练）。
    *   提示词训练：共 6 个 epoch。
*   **超参数**：学习率分别为 $1 \times 10^{-4}$（路由器）和 $5 \times 10^{-5}$（提示词），Batch Size 为 4。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **跨模型验证**：在三个主流 7B 模型上验证了迁移性，并扩展到了 Vicuna-13B 进行扩展性（Scaling）分析。
*   **消融实验**：详细探讨了粗/细粒度提示词的长度比例、推理阶段的安全阈值 $\tau_s$、以及训练策略（交替更新 vs 顺序更新）的影响。
*   **鲁棒性测试**：针对 GCG（梯度引导的对抗攻击）进行了迁移攻击测试。
*   **评估方式**：采用了 GPT-5.4（注：文中提及版本，可能为模拟未来或特定 API）作为自动评测员，并辅以人工校验，确保了安全率和帮助性评分的公正。

### 6. 论文的主要结论与发现
*   **安全与帮助性的双赢**：HiRoute 在维持极高安全率（平均 93.2% - 97.7%）的同时，其安全回复的帮助性得分显著高于基准方法。
*   **有效抑制过度拒绝**：通过安全门控机制，HiRoute 在 XSTest 上的过度拒绝率极低（如 Mistral 上仅 2.5%），远优于 RPO（40.5%）。
*   **通用能力保持**：在 GSM8K 和 MT-Bench 等任务上，HiRoute 几乎没有性能损失，证明了提示词微调对主干模型能力的保护作用。
*   **分层结构的必要性**：实验证明，单纯的细粒度专家混合虽然回复质量高但安全边界不稳，而加入共享粗粒度提示词能显著增强防御鲁棒性。

### 7. 优点：亮点总结
*   **输入自适应性**：不同于静态提示，HiRoute 能根据输入的具体风险特征动态调整防御策略。
*   **参数高效且解耦**：仅训练极少量的提示词参数和轻量路由器，且将风险识别与行为优化分离，训练过程更稳定。
*   **建设性对齐**：不仅是简单的“拒绝”，更强调在安全边界内提供有用的解释和替代方案。
*   **即插即用**：无需修改主干模型权重，便于在受限环境或在线服务中部署。

### 8. 不足与局限
*   **路由器依赖性**：系统的安全性高度依赖路由器的分类准确率，对于分布外（OOD）或极其隐蔽的复合风险请求，可能存在路由失效风险。
*   **场景局限**：目前主要集中在单轮文本交互，尚未在多轮对话或多模态（如图像-文本）安全对齐中进行验证。
*   **计算开销**：虽然参数高效，但推理时需要先经过路由器前向传播，并增加了提示词长度，会带来微小的推理延迟。

（完）
