---
title: "ToolHazard: Scaling Adversarial Environments for Security Evaluation and Alignment of LLM-based Agents"
title_zh: ToolHazard：面向基于大语言模型智能体的安全评估与对齐的可扩展对抗环境
authors: "Yutao Mou, Pengfei Yang, Zhe Yin, Zhangchi Xue, Xiaotian Luan, Dingyao Yu, Tong Zhang, Shikun Zhang, Wei Ye"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11878v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对间接提示注入的安全评估与对齐
tldr: 针对LLM智能体易受环境状态中间接提示注入攻击的问题，本文提出ToolHazard框架。该框架通过环境模拟器、攻击者智能体和用户模拟器，自动合成可执行的有状态环境，并生成针对性的攻击载荷与长程任务。基于此构建的ToolHazard-Bench揭示了智能体在复杂工作流中的安全漏洞，且生成的对齐数据能有效提升智能体安全性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体安全研究受限于手动构建的环境和预定义的注入位置，难以在大规模、多领域的复杂场景下进行安全评估。
method: 提出ToolHazard框架，利用自动化组件合成有状态环境、发现注入点并生成攻击载荷，从而构建大规模的对抗性基准测试集。
result: 实验表明现有智能体存在显著漏洞，攻击效果受注入时机和位置影响，且利用该框架生成的对齐数据可显著增强安全性。
conclusion: ToolHazard为LLM智能体的安全评估和对齐提供了可扩展的自动化方案，在提升防御能力的同时保持了任务效用。
---

## 摘要
集成了外部工具的大语言模型（LLM）智能体容易受到嵌入在环境状态中的间接提示注入攻击。然而，现有的研究在很大程度上依赖于手动实现或重复使用的环境、基于 LLM 的随机工具模拟以及预定义的注入位置，这限制了在更广泛领域开展可扩展的安全研究。为了弥补这一差距，我们提出了 ToolHazard，这是一个可扩展的对抗环境合成框架，它减少了人工工程量，并支持通过额外的种子领域和计算资源进行扩展。通过环境模拟器、攻击者智能体和用户模拟器，ToolHazard 合成了可执行的有状态环境，发现可行的注入点并生成特定于环境的有效载荷，并构建了基于状态的长程任务。基于 ToolHazard，我们构建了 ToolHazard-Bench，用于在复杂工作流和多样化环境攻击下对智能体进行压力测试。实验揭示了智能体存在显著的漏洞，并表明注入的时机和位置会影响攻击的有效性。此外，ToolHazard 生成的对齐数据在保持良性任务效用的同时，提高了智能体在 ToolHazard-Bench 和 AgentDojo 上的安全性。

## Abstract
Large language model (LLM) agents integrated with external tools are vulnerable to indirect prompt injections embedded in environmental states. However, existing studies largely rely on manually implemented or reused environments, stochastic LLM-based tool simulation, and predefined injection locations, limiting scalable security research across broader domains. To bridge this gap, we propose **ToolHazard**, a scalable adversarial environment synthesis framework that reduces human engineering and supports expansion with additional seed domains and compute. Through an Environment Simulator, an Attacker Agent, and a User Simulator, ToolHazard synthesizes executable stateful environments, discovers viable injection points and generates environment-specific payloads, and constructs state-grounded long-horizon tasks. Based on ToolHazard, we build **ToolHazard-Bench** for stress-testing agents under complex workflows and diverse environmental attacks. Experiments reveal substantial agent vulnerabilities and show that injection timing and placement affect attack effectiveness. Moreover, ToolHazard-generated alignment data improves security on both ToolHazard-Bench and AgentDojo while preserving benign task utility.

---

## 论文详细总结（自动生成）

### 论文结构化总结：ToolHazard

#### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 智能体（Agents）在复杂任务中的广泛应用，其安全性面临严峻挑战，尤其是**间接提示注入（Indirect Prompt Injection, IPI）**。攻击者通过在环境状态（如邮件、文档、数据库记录）中嵌入恶意指令，诱导智能体执行非授权操作。
*   **研究动机**：现有的安全评估基准主要依赖手动构建的环境，扩展性差；基于 LLM 的随机模拟缺乏确定性；且注入位置通常是预定义的，无法模拟真实攻击者主动寻找漏洞的过程。
*   **核心目标**：开发一个可扩展的框架，能够自动合成有状态的对抗环境、发现注入点并生成长程任务，以支持大规模的安全评估与对齐训练。

#### 2. 方法论：核心思想与技术细节
论文提出了 **ToolHazard** 框架，包含三个核心模块：
*   **环境模拟器 (Environment Simulator)**：
    *   **蓝图规划**：从种子任务中推断环境类型、实体模式、状态变量和操作规则。
    *   **程序构建**：将蓝图转化为可执行的 Python 代码（面向对象模式），将工具实现为类方法。
    *   **质量检查**：通过“测试智能体”和“检查智能体”双重验证环境的逻辑一致性。
*   **攻击者智能体 (Attacker Agent)**：
    *   **漏洞发现**：识别可携带自由文本的属性，分析工具间的读写依赖，构建传播链。
    *   **对抗注入**：基于“计划-执行”框架，在智能体感知路径上自动选择注入点，并使用 6 种策略（如决策劫持、工具选择等）包装攻击载荷。
*   **用户模拟器 (User Simulator)**：
    *   根据环境骨架初始化状态，并生成与状态绑定的长程任务（平均 15 步以上）。
*   **验证机制**：自动生成基于环境最终状态的验证函数（Check Functions），通过程序化判断任务是否被劫持，而非依赖 LLM 打分。

#### 3. 实验设计
*   **数据集/基准**：构建了 **ToolHazard-Bench**（包含 28 个有状态环境、512 个工具、87 个长程任务）和 **ToolHazard-Align**（用于训练的 60 个环境）。
*   **对比方法/模型**：
    *   **目标智能体**：评估了 GPT-5、GPT-4.1、Gemini-3.1-Pro、Gemini-2.5-Pro、DeepSeek-V3.2、Qwen3-8B/4B。
    *   **攻击策略**：对比了 6 种注入策略（Basic Combined, Important Template, Multi-turn, Decision Hijacking, Reasoning Criteria, Tool Selection）。
    *   **对齐基准**：在独立基准 **AgentDojo** 上验证了泛化性。
*   **评估指标**：良性任务成功率（BR）和攻击成功率（ASR）。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 张 NVIDIA 80GB A800 GPU**。
*   **训练细节**：
    *   **SFT**：在 Qwen3 模型上进行 3 个 epoch 的微调，学习率 $1 \times 10^{-6}$，序列长度 32K。
    *   **RL**：使用 GRPO 算法，训练 50 个 step，每个 step 采样 64 个任务，每个任务滚动 8 条轨迹。
*   **合成成本**：使用 GPT-4 接口，合成一个环境约需 0.59 美元，生成一个攻击实例约 0.05 美元。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从闭源顶尖模型到开源主流模型的 7 种模型。
*   **分析维度**：
    *   **注入时机与位置**：分析了攻击在轨迹早期 vs 晚期、观察结果头部 vs 尾部的效果差异。
    *   **输出格式**：对比了 JSON、YAML 与自由文本对攻击成功率的影响。
    *   **消融与泛化**：进行了跨基准测试（AgentDojo）和跨攻击策略的泛化实验。
*   **充分性评价**：实验设计严谨，不仅关注攻击成功率，还通过消融实验深入探讨了模型偏见（如位置偏见），实验组数充足，具有较高的客观性。

#### 6. 主要结论与发现
*   **普遍脆弱性**：即使是 GPT-5 和 DeepSeek-V3.2 等最强模型，在面对环境侧注入时仍表现出极高的 ASR（部分策略下超过 40%-70%）。
*   **位置偏见**：注入指令出现在轨迹越早、且位于观察结果末尾时，攻击效果最显著。
*   **格式影响**：自由文本输出比结构化输出（JSON/YAML）更容易受到攻击，因为结构化语法提供了某种程度的语义隔离。
*   **对齐有效性**：通过 ToolHazard 合成的对抗数据进行 SFT+RL 对齐，能显著降低 ASR，同时提升模型在复杂环境下的良性任务处理能力。

#### 7. 优点
*   **高扩展性**：全自动化的环境与任务合成流程，极大降低了安全研究的人力成本。
*   **真实性与复杂性**：相比现有基准，ToolHazard 的任务步数更长（15.56 步），环境状态更复杂，更贴近真实应用。
*   **闭环对齐**：不仅提供评估，还提供了完整的 RL 训练基础设施，实现了从发现漏洞到修复漏洞的闭环。

#### 8. 不足与局限
*   **合成环境的真实感**：虽然复杂度高，但与真实的商业闭源系统（如企业 ERP）相比，在长尾错误模式和特定部署约束上仍存在差距。
*   **攻击策略的局限**：目前主要依赖 6 种预定义的攻击包装策略，尚未实现完全自动化的新型攻击策略发现。
*   **模型规模限制**：由于算力限制，对齐实验主要在 4B 和 8B 模型上进行，尚未在大规模模型（如 70B+）上验证 RL 对齐的扩展效应。

（完）
