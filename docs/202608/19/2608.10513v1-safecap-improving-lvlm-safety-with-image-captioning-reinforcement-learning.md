---
title: "SafeCap: Improving LVLM Safety with Image Captioning Reinforcement Learning"
title_zh: SafeCap：通过图像描述强化学习提升大视觉语言模型（LVLM）的安全性
authors: "Caoyuan Ma, Wenpu Liu, Weichu Xie, Tian Gu, Shilei Zhao, Lingxi Min, Shuai Dong, Yuqi Xu, Ji Zhao, Ziyue Wang, Wenzheng Chang, Taiqiang Wu, Yongfu Zhu, Wenqi Shao, Yinqiang Zheng"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10513v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 多模态大语言模型越狱攻击防御
tldr: 针对大型视觉语言模型（LVLM）易受视觉诱导攻击的问题，本文提出 SafeCap 强化学习框架。该框架通过训练模型先生成与安全相关的图像描述，再基于该描述生成回答，并利用冻结的 LLM 评估描述的安全性来优化策略。实验表明，SafeCap 在提升安全性的同时保持了视觉通用能力，在多个基准测试中显著优于 SFT 和 DPO 等现有对齐方法。
selection_source: fresh_fetch
motivation: 现有的 LVLM 容易受到利用视觉输入绕过其语言骨干网络安全对齐机制的越狱攻击。
method: 提出一种基于描述介导的强化学习框架，通过优化模型生成的安全相关图像描述来引导模型做出安全决策。
result: 在五个安全基准上实现了 3.7-19.0 分的显著提升，同时保持或增强了模型的视觉通用能力。
conclusion: 通过强化学习优化图像描述是实现 LVLM 多模态安全对齐的一种比直接拒绝监督更有效的途径。
---

## 摘要
大视觉语言模型（LVLM）在面对利用视觉输入绕过其语言骨干网络继承的安全对齐的越狱攻击时仍然十分脆弱。我们提出了 SafeCap，这是一个通过学习自我描述（self-captioning）来对齐 LVLM 的强化学习框架。SafeCap 训练一个策略模型，先生成与安全相关的图像描述，然后再生成最终答案；该描述会根据其是否能使一个冻结的 LLM 做出符合安全对齐的决策来进一步优化。这种以描述为媒介的目标函数鼓励策略模型揭示与生成安全响应相关的视觉线索，而不是仅仅依赖于直接的拒绝监督。在五个多模态安全基准和六个视觉效用基准测试中，SafeCap 在其预期的 DirectCap 协议下显著提升了综合安全性能，在四种模型设置中安全平均分提升了 3.7-19.0 分，同时保持了相当或更好的视觉效用。在基于相同骨干网络和数据的受控对比实验中，SafeCap 优于安全 SFT、DPO 和 SafeGRPO，证明了以描述为媒介的强化学习在多模态安全对齐中的有效性。

## Abstract
Large vision-language models (LVLMs) remain vulnerable to jailbreak attacks that exploit visual inputs to bypass safety alignment inherited from their language backbones. We propose SafeCap, a reinforcement-learning framework that aligns LVLMs through learned self-captioning. SafeCap trains a policy model to first generate a safety-relevant image caption and then produce a final answer; the caption is further optimized by whether it enables a frozen LLM to reach a safety-aligned decision. This caption-mediated objective encourages the policy to expose visual cues relevant to safe response generation rather than relying solely on direct refusal supervision. Across five multimodal safety benchmarks and six vision-utility benchmarks, SafeCap substantially improves aggregate safety performance under its intended DirectCap protocol, with gains of 3.7-19.0 points in safety average across four model settings while maintaining comparable or improved vision utility. Under controlled comparisons on matched backbones and data, SafeCap outperforms safety SFT, DPO, and SafeGRPO, demonstrating the effectiveness of caption-mediated reinforcement learning for multimodal safety alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《SafeCap: Improving LVLM Safety with Image Captioning Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型（LVLM）虽然继承了语言骨干网络的安全对齐能力，但容易受到**视觉诱导的越狱攻击**（如图像中的排版攻击、隐藏意图或多图组合风险）。
*   **研究动机**：现有的防御方法（如直接拒绝训练）往往只关注输出端的对齐，而忽略了感知端的风险。作者认为，如果模型能先显式地描述出图像中与安全相关的特征，就能更好地激活其内部的安全决策机制。

### 2. 方法论：SafeCap 框架
*   **核心思想**：将安全对齐转化为一个“学习自我描述”（Learned Self-Captioning）的任务。模型在回答问题前，必须先生成一段包含安全线索的图像描述（Caption）。
*   **关键技术细节**：
    *   **推理协议（DirectCap）**：模型输出格式为 `<caption>描述</caption> 答案`。
    *   **强化学习算法**：采用 **GRPO**（组相对策略优化），无需额外的价值网络，通过组内样本比较来计算优势。
    *   **奖励函数设计（三部分组合）**：
        1.  **模板奖励 ($R_{tmp}$)**：确保模型严格遵守标签格式。
        2.  **直接答案奖励 ($R_{ans}$)**：使用指数风险折扣函数 $u \gamma^h$（$u$ 为有用性，$h$ 为风险值，$\gamma$ 为折扣系数），惩罚不安全回答，奖励安全且有用的回答。
        3.  **描述介导奖励 ($R_{cap}$)**：这是核心创新。将生成的描述传给一个**冻结的纯文本 LLM**。如果该 LLM 仅凭描述就能做出与策略模型一致的安全决策，则奖励该描述。这确保了描述中包含了关键的安全证据。
    *   **解耦归一化**：对三个奖励组件分别进行组内归一化，防止不同量级的信号相互干扰。

### 3. 实验设计
*   **数据集**：使用公开的 **SPA-VL** 多模态安全对齐数据集进行训练。
*   **安全基准（5个）**：MM-SafetyBench, MSSBench, VLSBench, FigStep, MIS-Test。
*   **视觉效用基准（6个）**：MM-Vet, BLINK, MMVP, ERQA, VPCT, MMStar。
*   **对比方法**：
    *   **Zero-shot 基线**：Direct（直接回答）、DirectCap（带描述回答）、Prism（仅靠描述回答）。
    *   **训练基线**：安全 SFT（监督微调）、DPO（直接偏好优化）、SafeGRPO。
*   **测试模型**：Qwen3.5-2B 和 4B（包括 Base 和 Instruct 版本）。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H200 GPU**。
*   **训练细节**：训练步数为 200 步，学习率为 $5 \times 10^{-7}$，每条提示词采样 8 个 rollout。
*   **时长**：文中未明确给出总训练时长，但提到 200 步是主要的实验设置。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种模型配置、3 种推理协议、11 个基准测试。
*   **消融实验**：非常充分。包括奖励组件（去答案奖励、去描述奖励）、归一化方式、风险系数 $\gamma$ 的影响、以及不同冻结 LLM（Qwen3-4B vs 14B）的泛化性测试。
*   **客观性**：引入了人类评估（100 个样本 100% 一致性）来验证自动评分器的可靠性，并进行了多随机种子实验以验证稳定性。

### 6. 主要结论与发现
*   **安全性显著提升**：在 DirectCap 协议下，SafeCap 使安全平均分提升了 **3.7 至 19.0 分**。
*   **效用保持**：在大幅提升安全性的同时，模型的视觉感知和指令遵循能力（Utility）基本保持不变，甚至在某些指标上有所提升。
*   **优于传统方法**：在相同数据和骨干网络下，SafeCap 的表现优于 SFT、DPO 和 SafeGRPO。
*   **描述的价值**：实验证明，显式的描述确实能作为“证据通道”，帮助模型识别视觉风险。

### 7. 优点（亮点）
*   **可解释性**：通过中间描述，用户可以直观看到模型是否识别出了图像中的危险元素。
*   **双重信号奖励**：结合了直接答案评估和冻结 LLM 的一致性验证，有效缓解了奖励作弊（Reward Hacking）。
*   **协议灵活性**：提出的 DirectCap 路径在不增加推理复杂度的前提下，通过强化学习内化了安全推理能力。

### 8. 不足与局限
*   **冻结 LLM 的瓶颈**：描述奖励的效果受限于下游冻结 LLM 的理解能力；如果 LLM 本身存在偏见或能力不足，会影响奖励质量。
*   **幻觉风险**：虽然实验中未观察到严重幻觉，但强制生成长描述理论上可能增加模型产生事实错误的风险。
*   **模型规模限制**：实验主要集中在 2B 和 4B 模型上，尚未在超大规模模型（如 70B+）上验证其扩展性。
*   **推理开销**：DirectCap 协议要求先生成描述，这会增加首字延迟（TTFT）和总推理时间。

（完）
