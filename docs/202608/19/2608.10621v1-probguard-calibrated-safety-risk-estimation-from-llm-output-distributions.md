---
title: "ProbGuard: Calibrated Safety Risk Estimation from LLM Output Distributions"
title_zh: ProbGuard：基于大语言模型输出分布的校准安全风险估计
authors: "Xinzhe Huang, Biwu Yao, Kedong Xiu, Mengnan Zhao, Di Wang, Puning Zhao, Tianhang Zheng"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10621v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于安全风险评估的概率护栏
tldr: "针对现有大模型安全护栏忽略输出概率分布且在生成早期难以准确评估安全性的问题，本文提出 ProbGuard。这是一种架构无关的概率性护栏，通过分析 LLM 生成前期的概率分布信号，利用蒙特卡洛采样估算后续生成的安全风险。实验表明，ProbGuard 在校准性能上显著优于现有基准，并能在生成前 10 个 token 时有效拦截 99% 以上的越狱攻击，实现了高效的早期预警。"
selection_source: fresh_fetch
motivation: 现有的确定性安全评估方法忽略了 LLM 输出分布中的丰富概率信息，且在生成早期难以应对安全评估的不确定性。
method: 提出 ProbGuard 框架，通过蒙特卡洛采样 LLM 早期输出的分布信号来估算并校准后续生成的安全风险概率。
result: "ProbGuard 在校准性能上大幅领先，将平均 Brier 分数降低了 79.6%，并能在仅观察前 10 个解码步骤时将越狱攻击成功率限制在 1% 以内。"
conclusion: ProbGuard 证明了利用 LLM 早期输出的概率分布信号可以实现更精准、更及时的安全风险评估与拦截。
---

## 摘要
近期关于大语言模型（LLM）安全性的研究广泛采用护栏（guardrails）来识别不安全的 LLM 输出。现有的护栏通常将安全评估建模为确定性分类任务，将离散的 token 序列映射到离散的安全标签。然而，这种范式存在两个局限性：首先，安全评估本质上是一个具有不确定性的问题，特别是在生成的早期阶段；其次，仅依赖离散 token 序列会丢弃嵌入在 LLM 输出分布中的丰富概率信息。为了解决这些局限性，我们提出了首个完全概率化的、与架构无关的护栏 ProbGuard，利用 LLM 早期输出的分布信号来估计和校准安全概率，从而实现对正在生成的不安全输出的早期停止。具体而言，给定 LLM 生成的前缀分布，我们将安全风险建模为其后续生成动态的不安全概率，并通过蒙特卡洛采样来估计该风险。通过对分布信号和校准后的安全风险进行后训练，ProbGuard 在所有九种模型-数据集组合设置中均实现了最佳的校准性能，相比于最佳基准模型，平均 Brier 分数和期望校准误差（ECE）分别降低了 79.6% 和 71.9%。在仅观察前十个解码步骤的 LLM 早期输出分布后，ProbGuard 进一步将六种代表性越狱攻击的攻击成功率限制在最高 1% 以内。

## Abstract
Recent research on Large Language Model (LLM) safety has widely adopted guardrails to identify unsafe LLM outputs. Existing guardrails typically formulate safety assessment as a deterministic classification task, mapping a discrete token sequence to a discrete safety label. However, this paradigm has two limitations: First, safety assessment is inherently an uncertain problem, particularly during the early generation state. Second, relying solely on discrete token sequences discards the rich probabilistic information embedded in the LLM output distribution. To address these limitations, we propose the first completely probabilistic architecture-agnostic guardrail \textsc{ProbGuard} to leverage the LLM early output distributional signals for estimating and calibrating the safety probability, thereby enabling early stopping of unsafe ongoing outputs. Specifically, given an LLM's generated prefix distribution, we formulate the safety risk as the unsafe probability of its continued generation dynamics and estimate this risk by Monte-Carlo sampling. Through post-training on the distributional signals and calibrated safety risk, \textsc{ProbGuard} achieves the best calibration performance across all nine model--dataset combination settings, reducing the average Brier score and ECE by 79.6\% and 71.9\%, respectively, over the best baseline. \textsc{ProbGuard} further limits the attack success rate to at most 1\% across six representative jailbreak attacks after observing the LLM early output distributions from only the first ten decoding steps.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProbGuard: Calibrated Safety Risk Estimation from LLM Output Distributions》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着大语言模型（LLM）的广泛应用，安全护栏（Guardrails）成为识别和拦截不安全输出的关键。
*   **核心问题**：现有的安全护栏大多将安全评估视为“确定性分类”任务（即：输入一段文本，输出“安全”或“不安全”）。这种范式存在两大缺陷：
    1.  **忽略不确定性**：在生成早期，相同的前缀可能导向安全或不安全的结局，离散标签无法表达这种内在的概率风险。
    2.  **信息丢失**：仅处理离散的 Token 序列，丢弃了 LLM 输出分布（Logits）中蕴含的丰富概率信息（如备选 Token 的竞争关系）。
*   **研究目的**：开发一种能够利用 LLM 早期输出分布信号、实现精准校准安全风险估计的概率化护栏。

### 2. 方法论
ProbGuard 的核心思想是将安全评估从“文本分类”转变为“概率估计”。
*   **安全风险定义**：将风险 $C_k$ 定义为“在当前生成状态下，后续持续生成会导致不安全响应的概率”。
*   **关键技术流程**：
    1.  **校准目标构建（蒙特卡洛采样）**：在生成的第 $k$ 步，通过蒙特卡洛方法独立采样 $N$ 个可能的完整响应，利用一个高性能判别模型（CalibEval）评估这些响应的不安全比例，作为该状态的真实风险估计值。
    2.  **概率加权表示（架构无关性）**：为了不依赖特定模型的隐藏层状态，ProbGuard 提取目标模型输出分布中 Top-K 个 Token 及其概率。通过“重分词（ReToken）”技术将这些 Token 映射到 ProbGuard 自身的嵌入空间，并按概率加权求和，形成连续的向量表示。
    3.  **校准对齐训练**：使用负对数似然（NLL）损失函数，训练 ProbGuard 预测上述采样得到的风险概率，使其输出能够反映真实的后续生成风险。

### 3. 实验设计
*   **数据集**：
    *   **训练/评估**：整合了 PKU-SafeRLHF、WildGuard 和 S-Eval 三个主流安全数据集。
    *   **攻击测试**：使用 AdvBench 和 HarmBench 评估防御能力。
*   **目标模型（被保护对象）**：涵盖 Llama3-8B、Qwen3-8B 和 Gemma2-9B 三个不同家族的模型。
*   **对比基准（13 种方法）**：
    *   **信心度方法**：Verbalized Confidence, P(True)。
    *   **确定性护栏**：Llama-Guard3, ShieldGemma, XGuard 等。
    *   **流式监控**：SCM, Qwen3Guard-stream。
    *   **特征探测**：Linear Probes, TPCs, ShieldHead。
*   **评估指标**：Brier 分数（衡量个体预测准确度）、ECE（期望校准误差）、ASR（攻击成功率）。

### 4. 资源与算力
*   **硬件环境**：实验运行在配备 **NVIDIA RTX Pro 6000 GPU** 和 256 GB RAM 的服务器上。
*   **训练细节**：
    *   ProbGuard 提供了 0.6B、4B 和 8B 三种规模（基于 Qwen3 架构）。
    *   训练针对前缀长度 $k \in [5, 15]$ 进行。
    *   采样预算 $N=16$，Top-K 概率取 50。
*   **效率表现**：处理 1000 个样本的风险估计耗时约 36.4 秒（8B 版本），比部分基于 LLM 的护栏快 50% 以上。

### 5. 实验充分性与客观性
*   **实验规模**：论文在 9 种“模型-数据集”组合上进行了详尽测试，并针对 6 种代表性越狱攻击（如 GCG, PAIR, ECLIPSE）进行了防御评估。
*   **消融与分析**：包含了对采样预算 $N$ 的敏感度分析、不同输入表示（Token vs 隐藏层 vs 分布信号）的对比、以及对判别模型 CalibEval 准确性的验证。
*   **客观性**：实验涵盖了多种架构的模型，证明了方法的通用性；通过校准曲线和误差指标多维度评估，而非单一的准确率，体现了概率估计的严谨性。

### 6. 主要结论与发现
*   **校准性能卓越**：ProbGuard 在所有设置下均取得最佳校准效果，平均 Brier 分数和 ECE 分别降低了 **79.6%** 和 **71.9%**。
*   **早期干预极佳**：仅需观察前 **10 个解码步骤**，ProbGuard 就能将越狱攻击的成功率（ASR）限制在 **1%** 以内。
*   **架构无关性验证**：即使在不同分词器和架构的模型间切换，ProbGuard 依然能保持稳健的风险预测能力。

### 7. 优点
*   **范式创新**：首次提出完全概率化的护栏，解决了早期生成不确定性的量化问题。
*   **信息无损**：通过概率加权向量捕捉了 Logits 中的细微信号，比纯文本护栏更敏锐。
*   **实用性强**：无需访问被保护模型的内部权重（Hidden States），仅需输出概率，适合闭源或黑盒 API 场景。

### 8. 不足与局限
*   **训练成本**：构建校准目标需要进行大量的蒙特卡洛采样（每个前缀采样 16 次以上），这使得训练数据的准备过程非常耗时且计算昂贵。
*   **判别模型依赖**：ProbGuard 的上限受限于采样阶段所使用的判别模型（CalibEval）的准确性。
*   **动态变化覆盖**：虽然在早期生成表现优异，但对于生成后期突然发生的“幻觉”或安全性转折，其预测效能尚需更多长文本实验验证。

（完）
