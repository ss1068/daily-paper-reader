---
title: "Localizing Safety Alignment: MLP Layers and Mid-Network Blocks Encode Refusal Behavior in Large Language Models"
title_zh: 定位安全对齐：MLP 层与网络中部区块编码了大语言模型中的拒绝行为
authors: "Mingyu Zong, Sampad Mohanty, Bhaskar Krishnamachari"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11583v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 在LLM的MLP层中定位安全对齐和拒绝行为
tldr: 本研究探讨了大语言模型中安全对齐（拒绝行为）的定位问题。通过将对齐模型的权重移植到未对齐的基础模型中，研究发现拒绝行为主要由MLP层而非注意力层编码，且集中在网络中部的特定层（如8-11层）。研究揭示了安全组件的非加性特征，即增加更多对齐块可能反而降低性能。这一发现为理解对齐脆弱性及开发针对性安全干预提供了新视角。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型中的安全对齐行为是分布在整个网络中，还是集中在特定的参数子集中。
method: 通过在不同粒度级别（如注意力、MLP、连续层区域）将对齐模型的权重移植到匹配的未对齐基础模型中进行对比实验。
result: 发现MLP权重对拒绝行为的影响远超注意力权重，且相关参数集中在网络中部，同时组件间的组合具有非加性特征。
conclusion: 安全对齐在当前模型中具有局部性和交互敏感性，这为理解对齐脆弱性及实施针对性安全干预提供了重要见解。
---

## 摘要
大语言模型中的安全对齐通常被视为整个网络的分布式属性，但其实际的脆弱性表明，拒绝行为可能集中在较小的一组参数中。本研究通过在多个粒度级别上将对齐模型的权重移植到匹配的未对齐基础模型中，探讨了安全对齐拒绝行为的编码位置。利用两组开源权重模型对和四个安全基准，我们进行了实验，比较了替换注意力权重、MLP 权重、连续层区域以及 MLP 区块的效果。在两个模型系列中，拒绝行为的迁移主要由 MLP 权重主导：替换 MLP 参数恢复的恶意提示拒绝行为显著多于替换注意力参数，在各基准测试中增益至少达到 2.7 倍。在 MLP 堆栈中，与拒绝相关的参数表现出一致的网络中部集中特性，因为在针对模型-数据集对的所有六次贪婪搜索中，跨越第 8-11 层的区块均被首先选中。结果还表明，安全相关组件的组合是非加性的：在六个贪婪轨迹中的五个里，增加更多对齐区块反而会降低拒绝性能，且选择性的区块子集在恶意拒绝、良性过度拒绝或两者兼顾方面，表现优于完整的 MLP 移植。最后，迁移到 OR-Bench 的贪婪顺序随用于推导它们的源基准而变化，这表明存在依赖于基准的精度-覆盖率权衡。这些结果表明，当前大语言模型中的安全对齐既是局域性的，又对交互敏感，为理解对齐的脆弱性以及针对性安全干预的潜在途径提供了见解。

## Abstract
Safety alignment in large language models is often treated as a distributed property of the entire network, yet its practical brittleness suggests that refusal behavior may be concentrated in a smaller set of parameters. This work addresses where safety-aligned refusal is encoded by transplanting weights from aligned models into matched unaligned base models at multiple levels of granularity. Using two open-weight model pairs and four safety benchmarks, we conducted experiments to compare the effects of replacing attention weights, MLP weights, contiguous layer regions, and MLP blocks. Across both model families, refusal transfer is dominated by MLP weights: replacing MLP parameters recovers substantially more malicious-prompt refusal than replacing attention parameters, with gains of at least 2.7 times more across benchmarks. Within the MLP stack, refusal-relevant parameters exhibit a consistent mid-network concentration, as the block spanning layers 8-11 is selected first in all six greedy searches over model-dataset pairs. The results also show that the composition of safety-relevant components is non-additive: in five of six greedy trajectories, adding more aligned blocks can reduce refusal performance, and selective block subsets can outperform full MLP transplantation on malicious refusal, benign over-refusal, or both. Finally, greedy orders transferred to OR-Bench vary with the source benchmark used to derive them, indicating a benchmark-dependent precision-coverage trade-off. These results suggest that safety alignment in current LLMs is both localized and interaction-sensitive, offering insight into alignment brittleness and potential avenues for targeted safety interventions.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）中安全对齐行为的内部编码机制进行了深入的实证研究。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 的安全对齐（即拒绝执行有害指令的行为）究竟是分布在整个神经网络中的，还是集中在特定的参数子集中？
*   **研究动机**：尽管当前的对齐技术（如 RLHF、DPO）能显著提高安全性，但这种对齐往往非常“脆弱”，微小的下游微调或特定的提示词攻击（如越狱）就能绕过安全机制。
*   **研究意义**：如果能定位到负责“拒绝行为”的具体参数，就能解释对齐脆弱性的原因，并为开发更精准、更稳健的安全干预手段提供理论支持。

### 2. 核心方法论
*   **核心思想：权重移植（Weight Transplantation）**。研究者利用架构完全相同但对齐状态不同的模型对（基础模型 vs. 对齐模型），将对齐模型的特定权重矩阵替换到基础模型中，观察基础模型是否因此获得了拒绝有害指令的能力。
*   **关键技术细节**：
    *   **多粒度干预**：分别在组件级（仅替换注意力权重或仅替换 MLP 权重）、层级（前 5 层、中间 5 层、后 5 层）以及区块级（每 4 层为一个区块）进行移植。
    *   **贪婪区块选择（Greedy Block Selection）**：为了研究不同区块间的交互，采用贪婪前向选择算法，逐个添加 MLP 区块，寻找能最大化拒绝率的最优组合。
    *   **对齐-基础不一致过滤**：专门筛选出那些“对齐模型会拒绝、而基础模型会顺从”的提示词作为测试集，以确保实验能精准捕捉到对齐带来的变化。

### 3. 实验设计
*   **模型对**：
    1.  RealSafe-R1-7B / DeepSeek-R1-Distill-Qwen-7B（基于 Qwen2 架构）。
    2.  saferlhf ultra sft / Llama-3.1-8B（基于 Llama 架构）。
*   **基准测试（Benchmarks）**：
    *   **有害提示词**：TwinPrompt、SGXSTest、AdvBench（衡量恶意拒绝率 MR）。
    *   **良性提示词**：TwinPrompt (Benign)、OR-Bench（衡量过度拒绝率 BOR）。
*   **对比方法**：对比了全模型移植、仅注意力移植、仅 MLP 移植以及不同深度范围的局部移植。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **资源情况**：由于该研究主要涉及权重的替换和推理评估（Transplantation & Inference），而非大规模从头训练，其对算力的需求相对较低，主要集中在对多个混合模型在多个基准测试上的推理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主流模型家族（Qwen 和 Llama）。
    *   使用了 4 个不同的安全基准。
    *   进行了组件级、层级、区块级以及单层级的消融实验。
    *   **验证实验**：在 30 个提示词的小样本实验基础上，增加了一组包含 100 个恶意和 100 个良性提示词的更大型验证集，结果保持一致。
*   **充分性与客观性**：实验设计考虑了“拒绝有害指令”和“误伤良性指令”两个维度，使用了贪婪搜索来探索非线性交互，实验逻辑严密，结论具有跨模型的一致性，表现出较高的客观性。

### 6. 主要结论与发现
*   **MLP 主导拒绝行为**：在所有实验中，替换 MLP 权重恢复的安全行为远多于注意力权重（增益超过 2.7 倍）。
*   **网络中部定位**：拒绝行为高度集中在网络的中部。具体而言，**第 8-11 层（Block 3）**在所有贪婪搜索中都是最关键的，其中第 8 层表现尤为突出。
*   **非加性交互（Non-additive）**：安全组件的组合不是简单的“越多越好”。在 5/6 的实验轨迹中，增加更多的对齐区块反而导致拒绝性能下降。
*   **精度-覆盖率权衡**：选择特定的 MLP 区块子集往往能比移植全部 MLP 权重获得更好的效果（更高的恶意拒绝率和更低的过度拒绝率）。

### 7. 优点
*   **定位精准**：通过权重移植而非仅仅是激活分析，直接在参数空间证明了安全对齐的局部性。
*   **跨架构一致性**：在 Qwen 和 Llama 两个不同的模型家族中发现了相同的“中部集中”规律，增强了结论的普适性。
*   **揭示复杂交互**：发现了对齐组件之间的非单调关系，挑战了“对齐是均匀分布的分布式表示”这一传统观点。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 7B-8B 规模的模型，尚不清楚在超大规模模型（如 70B+）或混合专家模型（MoE）中是否存在相同的定位规律。
*   **样本筛选偏差**：实验使用了“对齐-基础不一致”的过滤准则，虽然增强了对比度，但也可能导致评估范围局限于特定的行为差异。
*   **搜索算法局限**：贪婪搜索只是对组合空间的一种近似，可能漏掉了更优的非连续区块组合。

（完）
