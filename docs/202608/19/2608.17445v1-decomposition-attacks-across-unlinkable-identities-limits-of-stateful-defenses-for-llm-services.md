---
title: "Decomposition Attacks Across Unlinkable Identities: Limits of Stateful Defenses for LLM Services"
title_zh: 跨不可关联身份的分解攻击：大语言模型服务中有状态防御的局限性
authors: "Bowen Sun, Zhengyue Zhao, Xiaogeng Liu, Yinzhi Cao, Chaowei Xiao"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17445v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对大语言模型服务中分解攻击的有状态防御
tldr: 本研究探讨了针对大语言模型服务的分解攻击，即攻击者利用不可关联身份将有害任务拆分为多个无害请求。研究证明，在缺乏可靠身份关联时，有状态防御难以在保障正常体验的同时拦截攻击。实验显示，现有策略在面对攻击者重试时均告失效，强调了建立可靠身份识别或增加攻击成本的必要性。
selection_source: fresh_fetch
motivation: 针对大语言模型服务的分解攻击通过拆分有害任务规避无状态防御，而现有的有状态防御在面对不可关联的攻击者身份时面临严峻挑战。
method: 通过理论证明和实验分析，评估了在不同请求分组策略下，有状态防御在应对分解攻击时的安全性和实用性权衡。
result: "实验表明，在限制误伤率的前提下，所有测试的防御策略均无法有效阻止攻击，攻击成功率在两次尝试后可达100%。"
conclusion: 有效的防御需要超越单纯的请求监测，转而依赖可靠的身份关联、增加新身份获取成本或控制答案的使用。
---

## 摘要
大多数大语言模型服务使用无状态防御（仅判断当前请求）来拒绝有害任务。分解攻击利用这一局限性，将有害任务拆分为多个单独允许的请求，并组合其答案。因此，防御此类攻击需要一个能够协同考虑多个请求的有状态监控器。如果它能将属于同一个攻击者任务的所有请求分组，就能阻止攻击。然而，攻击者可以使用不可关联的身份并在其他地方组合答案，从而不留下可靠的分组信号。我们探究在这种设定下是否仍能阻止分解攻击。对于不带重试的固定攻击策略，我们证明了可实现的安全性与效用之间的权衡完全取决于具有相同能力的良性请求是如何分组的。持久且可识别的分组允许有效的防御；而新鲜且不可区分的分组则不然。当攻击者可以重试并从“允许/拦截”决策中学习时，这种有效的操作点就会消失：反馈揭示了哪些请求可以通过，但并未揭示拦截是否正确。对 91 个可执行任务和 11,393 个能力匹配的良性请求进行的实验支持了这些结果。在这些请求的拒绝率上限为 1% 且无关背景流量的拒绝率上限为 0.5% 的情况下，所有十种测试策略（包括一种具有精确请求-操作映射的特权策略）要么无法阻止攻击，要么超出了预算。在防御未见过的任务族上，一次尝试后的攻击成功率至少为 99%，两次尝试后为 100%。因此，有效的防御需要与分组相关的额外证据或机制，例如可靠的身份关联、获取新身份的成本或对答案使用的控制。

## Abstract
Most large language model services use stateless defenses, which judge only the current request, to refuse harmful tasks. Decomposition attacks exploit this limitation by splitting a harmful task into individually permissible requests and combining their answers. Defending against them therefore requires a stateful monitor that considers requests together. If it can group all requests for one attacker task, it can stop the attack. However, attackers can use unlinkable identities and combine answers elsewhere, leaving no reliable grouping signal. We ask whether decomposition attacks can still be stopped under this setting. For a fixed attack strategy without retries, we prove that the achievable security and utility tradeoff depends entirely on how benign requests for the same capabilities are grouped. Persistent, recognizable groups permit a useful defense; fresh, indistinguishable groups do not. When attackers can retry and learn from Allow/Block decisions, this useful operating point disappears: the feedback reveals what passes but not whether a block was correct. Experiments on 91 executable tasks and 11,393 capability-matched benign requests support these results. Under a 1% denial cap for these requests and a 0.5% cap for unrelated background traffic, all ten tested policies, including one privileged policy with an exact request-to-operation map, either fail to stop attacks or exceed the budget. On defense-unseen task families, attack success is at least 99% after one attempt and 100% after two. Effective defenses therefore require additional evidence or mechanisms tied to grouping, such as reliable identity linkage, costs for fresh identities, or control over answer use.

---

## 论文详细总结（自动生成）

### 论文总结：跨不可关联身份的分解攻击及其防御局限性

#### 1. 核心问题与整体含义
论文探讨了大语言模型（LLM）服务在面对“分解攻击”（Decomposition Attacks）时的防御困境。攻击者将一个有害任务拆分为多个看似无害的子请求，并在本地组合结果以规避传统的无状态防御。虽然“有状态防御”理论上可以通过监测请求历史来拦截此类攻击，但论文指出，如果攻击者利用多个“不可关联的身份”（如不同账号、IP等）分散提交请求，现有的防御机制将面临严峻挑战：要么无法识别攻击，要么会因误伤大量正常用户而失去实用性。

#### 2. 方法论
论文通过理论建模与实证分析相结合的方法，定义了防御的边界：
*   **核心概念区分**：区分了“可关联组”（服务商能识别的同一用户）和“访问联盟”（实际汇总答案的幕后攻击者）。
*   **理论前沿（Frontier）分析**：推导了安全性和实用性之间的符号化边界。核心公式指出，防御的有效性完全取决于良性请求的分组特征。如果良性用户也频繁使用新身份，防御者将无法在不误伤良性用户的前提下区分攻击。
*   **理想防御者模型**：构建了一个拥有精确语义识别能力的“理想防御者”作为基准，证明即使在语义识别完美的情况下，身份不可关联性依然是防御的死穴。
*   **自适应攻击策略**：引入了基于反馈的学习机制（如汤普森采样），模拟攻击者如何通过“允许/拦截”的反馈信号优化请求策略。

#### 3. 实验设计
*   **数据集与场景**：构建了一个包含 91 个可执行网络安全/编程任务的基准测试。
*   **请求构成**：
    *   **攻击请求**：365 个具体的分解操作请求。
    *   **匹配对照组（Matched Controls）**：11,393 个良性请求，其功能与攻击步骤完全一致，但用于合法目的。
    *   **背景流量**：150 万条来自 WildChat、StackExchange 等的普通请求。
*   **对比方法**：测试了 10 种防御策略，包括词法分类器、Granite Guardian、TwinGate（检索增强型）、基于聚类的监控器（Brown-style）以及特权的“精确累加器”。
*   **评估指标**：攻击成功率（ASR）、匹配对照组拒绝率（MCD）和背景拦截率（BBR）。

#### 4. 资源与算力
*   **模型使用**：实验使用了 Qwen3.5-35B（作为攻击者和任务生成器）和 Qwen3.5-2B（作为防御端的评判模型）。
*   **算力细节**：文中未明确给出具体的 GPU 型号、数量或总训练时长。但提到了大规模的模型调用，例如在防御实验中产生了超过 76 万次评分调用和 2.7 万次摘要生成调用。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 91 个任务、数万个良性变体以及百万级的背景流量，规模较大。
*   **充分性分析**：
    *   进行了 64 轮平衡重着色实验，以消除身份分配的偶然性。
    *   测试了 1,800 种不同的合法分组分区。
    *   包含了针对防御未见过的任务族的迁移实验。
*   **客观性**：通过可执行的代码测试（而非人工评估）来验证任务是否成功，保证了评估的客观性和公平性。

#### 6. 主要结论与发现
*   **防御失效**：在设定 1% 的良性误伤上限时，所有现有防御策略在面对跨身份攻击时几乎全部失效。
*   **重试的威力**：一旦允许攻击者重试，攻击成功率迅速飙升。在两次尝试后，针对未见任务的攻击成功率可达 100%。
*   **反馈学习**：攻击者仅需利用“允许/拦截”的二元反馈，即可通过简单算法（如 UCB1）显著提升攻击效率。
*   **核心瓶颈**：防御的失败不在于语义理解不足，而在于无法在缺乏可靠身份关联的情况下进行准确的“归因”。

#### 7. 优点
*   **理论深度**：不仅做了实验，还给出了严谨的数学证明，揭示了有状态防御的本质极限。
*   **实证严谨**：采用了可执行的基准测试，避免了 LLM 评估中常见的“幻觉”或主观偏差。
*   **现实意义**：直接挑战了当前工业界主流的有状态防御思路，提出了身份关联和增加攻击成本（如身份获取成本）才是防御关键。

#### 8. 不足与局限
*   **任务类型限制**：实验主要集中在网络安全和编程任务，虽然可验证性强，但可能无法完全代表所有类型的有害内容。
*   **防御假设**：研究主要基于“仅请求”通道的防御，未深入探讨结合响应内容或工具调用轨迹的防御可能带来的额外增益。
*   **身份模拟**：实验中的“不可关联身份”是基于数据集划分模拟的，现实中服务商可能拥有更复杂的启发式手段（如设备指纹）来关联身份。

（完）
