---
title: "DreamGuard: Efficient Runtime Guardrail for LLM Agents via Risk-Aware World Model"
title_zh: DreamGuard：基于风险感知世界模型的大语言模型智能体高效运行时护栏
authors: "Wenhao Lin, Chenyu Yu, Xingwei Lin, Sicong Cao, Xiang Chen, Lei Xue, Le Yu, Letian Sha, Chunming Wu"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05695v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 基于风险感知世界模型的LLM智能体主动运行时护栏
tldr: 针对 LLM 智能体在调用外部工具时可能引发的不可逆风险，现有护栏多为反应式且缺乏对长期风险的建模。本文提出 DreamGuard，一种基于风险感知世界模型的主动式护栏。它通过维护循环潜状态来预测未来风险，并融合即时危害与前缀风险信号进行干预。实验表明，DreamGuard 在安全与效用平衡上表现优异，且每轮调用延迟仅 25ms，为智能体安全运行提供了高效保障。
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体护栏大多是反应式的，难以识别由一系列看似无害的动作逐渐累积而成的长期潜在风险。
method: 提出一种基于风险感知世界模型的主动护栏，通过预测未来潜状态来融合即时危害和前缀风险信号，从而在执行前做出干预决策。
result: 在四个基准测试中，DreamGuard 在安全与效用的权衡上优于所有基准模型，且平均端到端延迟仅为 25 毫秒。
conclusion: DreamGuard 证明了利用轻量级世界模型进行前瞻性风险预测是提升 LLM 智能体运行时安全性的高效且可行的方法。
---

## 摘要
随着大语言模型（LLM）智能体越来越多地调用外部工具并与现实世界系统交互，不安全的操作可能会对外部状态、用户数据和下游服务造成不可逆的后果。近期的运行时护栏通过在执行前检查建议的操作来缓解此类风险，但许多护栏仍是被动式的：它们主要评估当前操作的表观安全性，缺乏关于风险在轨迹中如何演变的显式模型。这种局限性为长时程风险创造了关键盲点，即单个看起来良性的操作可能会逐渐使智能体漂移到危险状态。为此，我们提出了 DreamGuard，这是一种基于风险感知世界模型构建的 LLM 智能体主动护栏。该世界模型在轨迹上维持一个紧凑的循环隐状态，并预测未来的隐状态，DreamGuard 从中提取即时危害和前缀风险证据。随后，它在执行前将这些多时程信号融合为干预决策。在四个基准测试和在线护栏评估中的实验表明，DreamGuard 优于通用的、被动式和主动式护栏基准，在评估的护栏中实现了最佳的安全性与效用权衡，并保持了每次调用平均 25 毫秒的端到端延迟。

## Abstract
As large language model (LLM) agents increasingly invoke external tools and interact with real-world systems, unsafe actions may cause irreversible consequences on external states, user data, and downstream services. Recent runtime guardrails mitigate such risks by checking proposed actions before execution, but many remain reactive: they primarily assess the apparent safety of the current action, lacking an explicit model of how risk evolves across the trajectory. This limitation creates a critical blind spot for long-horizon risks, where individually benign-looking actions can gradually drift the agent toward hazardous states. In response, we propose DreamGuard, a proactive guardrail for LLM agents built around a risk-aware world model. The world model maintains a compact recurrent latent state over the trajectory and predicts future latent states from which DreamGuard derives immediate-hazard and prefix-risk evidence. It then fuses these multi-horizon signals into intervention decisions before execution. Experiments across four benchmarks and an online guardrail evaluation show that DreamGuard outperforms generic, reactive, and proactive guardrail baselines, achieves the best safety-utility trade-off among evaluated guardrails, and maintains an average end-to-end latency of 25 ms per call.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **DreamGuard**，一种专为大语言模型（LLM）智能体设计的高效运行时护栏系统。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：LLM 智能体在与现实世界交互（如调用 API、修改文件）时，可能产生不可逆的危害。
*   **研究背景**：
    *   **反应式护栏的局限**：现有护栏大多只检查当前动作的表面安全性，忽略了风险随时间演变的轨迹。
    *   **长时程风险（Long-horizon Risks）**：某些动作单独看是良性的，但组合起来会导致严重后果（例如：定位文件 -> 移动到工作区 -> 创建公开链接 = 敏感数据泄露）。
    *   **效率瓶颈**：现有的主动式护栏往往依赖 LLM 进行多步推理或处理长上下文，导致每步延迟高达数秒，无法满足实时运行需求。
*   **整体含义**：DreamGuard 旨在通过一个轻量级的“风险感知世界模型”，在极低延迟下实现对即时危害和长期累积风险的主动预测与干预。

### 2. 方法论：核心思想与关键技术
DreamGuard 的核心是一个**风险感知世界模型（Risk-Aware World Model）**，其工作流程分为三个阶段：
*   **循环世界模型（RSSM）**：
    *   使用基于 GRU 的循环神经网络维护一个紧凑的隐状态（Latent State），用于总结历史轨迹。
    *   **预测未来**：在动作执行前，模型根据当前状态和建议动作预测“后继隐状态”，而不是生成原始的观测结果（如图像或文本），从而大幅提升效率。
*   **多时程风险估计**：
    *   **即时危害预测（Immediate-Hazard）**：评估当前动作是否会直接导致危险。
    *   **前缀风险预测（Prefix-Risk）**：评估当前轨迹前缀是否正逐渐向危险状态漂移。
    *   **证据融合**：利用 **Noisy-Or 规则**融合多路风险信号，并结合指数移动平均（EMA）和滑动窗口来平滑风险评分。
*   **两阶段训练策略**：
    *   **第一阶段（预训练）**：在轨迹数据上学习环境动力学，使模型能预测未来的嵌入表示。
    *   **第二阶段（风险监督）**：使用带有安全标签的数据微调模型，使隐状态专门保留与风险相关的特征。
*   **决策机制**：采用**分裂共形校准（Split-Conformal Calibration）**技术，在保证低误报率的前提下，自动确定干预阈值，输出 PASS（通过）、HOLD（挂起）或 BLOCK（拦截）。

### 3. 实验设计
*   **数据集/场景**：
    1.  **SafetyDrift**：专门用于评估长时程风险累积。
    2.  **AgentDojo**：工具使用、提示词注入及环境介导风险。
    3.  **Agent Security Bench (ASB)**：针对智能体的直接和间接攻击。
    4.  **ASSE-Security**：涵盖数据泄露、凭据滥用等安全风险。
*   **对比方法**：
    *   **通用护栏**：Llama-Guard-4-12B。
    *   **反应式护栏**：PolicyGuard, GuardAgent, AgentDoG-1.5。
    *   **主动式护栏**：SafePred, TRACES。
*   **评估指标**：安全率（SR）、F1 分数、误报率（FPR）、端到端延迟、预警提前步数（MAS）等。

### 4. 资源与算力
*   **硬件环境**：使用了 **1 台 NVIDIA H100 (80GB)** GPU 进行训练和推理。
*   **软件/模型**：使用 **Qwen3-4B-Instruct** 作为冻结的特征提取器（Encoder）。
*   **训练细节**：预训练 10 个 Epoch，风险监督训练 12 个 Epoch，Batch Size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流安全基准测试上进行了详尽测试。
    *   进行了**在线护栏评估**（使用 GPT-5.1 作为智能体，DreamGuard 作为实时插件），包含 100 个案例。
    *   **消融实验**：对比了 8 种变体，验证了循环动力学、后继预测、多时程估计及两阶段训练的必要性。
*   **充分性与客观性**：实验涵盖了从离线评估到在线实战的多种维度，并使用了 Wilcoxon 符号秩检验进行统计显著性分析，证明了结果的稳健性。

### 6. 主要结论与发现
*   **性能领先**：DreamGuard 在所有基准测试中的 F1 和安全率（SR）均表现最优，尤其在长时程风险检测上显著超过反应式方法。
*   **极低延迟**：平均端到端延迟仅为 **25 毫秒**，比基于 LLM 推理的方法（如 SafePred）快 400 倍以上。
*   **预警能力**：在 96.3% 的不安全轨迹中，DreamGuard 能在危险动作发生前就发出警告，平均提前 3.63 步。
*   **安全-效用平衡**：在在线测试中，它在保持 90.38% 任务成功率的同时，实现了最高的安全性。

### 7. 优点（亮点）
*   **主动性**：通过世界模型预测未来，解决了“温水煮青蛙”式的长时程风险识别难题。
*   **高效性**：摒弃了昂贵的 LLM 实时推理，改用轻量级 GRU 隐状态模型，极具工程实用价值。
*   **风险感知特征**：通过专门的监督训练，使隐状态对风险信号高度敏感，而非盲目预测所有环境细节。
*   **校准严谨**：引入共形预测理论，使干预决策具有统计学上的可靠性。

### 8. 不足与局限
*   **分布偏移风险**：模型主要在 SafetyDrift 上校准，虽然在其他数据集上表现良好，但在极端未见过的环境分布下可能需要重新校准阈值。
*   **干预手段单一**：目前仅支持拦截或挂起，不支持自动生成安全的替代动作（Action Revision）。
*   **依赖特征提取器**：其性能在一定程度上受限于预训练模型（如 Qwen3）提取特征的质量。

（完）
