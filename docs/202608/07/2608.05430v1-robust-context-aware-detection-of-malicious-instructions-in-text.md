---
title: Robust Context-Aware Detection of Malicious Instructions in Text
title_zh: 文本中恶意指令的鲁棒上下文感知检测
authors: "Buzhao Liu, Xinhang Ma, Yevgeniy Vorobeychik"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.05430v1"
tags: ["query:llm"]
score: 8.0
evidence: 缓解间接提示注入攻击
tldr: 本研究针对大语言模型在代理任务中面临的间接提示注入（IPI）威胁，提出了一种上下文与查询感知的恶意指令检测方法。通过引入特征空间对抗训练和基于LLM改写的对抗训练，增强了检测器对抗自适应规避攻击的鲁棒性。实验证明，该方法在静态和自适应攻击下均优于现有基准，并强调了领域特定调优在实际应用中的重要性。
selection_source: fresh_fetch
motivation: 现有LLM检测器在应对间接提示注入时缺乏对查询上下文的感知，且难以抵御针对代理执行环境的自适应规避攻击。
method: 提出了一种上下文与查询感知的恶意句子分类器，并结合特征空间梯度优化与LLM改写模拟两种对抗训练策略来增强防御。
result: 实验表明该方法在静态攻击下优于SOTA基准，并在自适应攻击场景中实现了更高的任务效用和更低的攻击成功率。
conclusion: 有效的恶意指令检测需兼顾上下文感知与鲁棒性训练，且防御参数的优化应根据具体应用领域进行针对性调整。
---

## 摘要
现代大语言模型（LLM）卓越的指令遵循能力使其能够作为智能体（agent）的核心，自主完成日益复杂的任务。然而，这也使其容易受到在文本中嵌入恶意指令的攻击，其常见变体被称为间接提示注入（IPI）。应对这一漏洞的一项基础任务是将给定文本成功分割为良性句子和恶意句子（如果存在）。尽管已经提出了许多针对该任务的方法，但尚无检测器能在分段级别结合查询相关（query-relative）检测，且没有检测器能针对智能体执行中可实现的自适应规避攻击进行加固。我们通过开发一种兼具上下文感知和查询感知能力的恶意句子分类方法来解决前一个局限性。接下来，为了增强所得分类器对抗规避攻击的鲁棒性，我们提出了两种对抗训练（AT）方法。第一种是直接适配的特征空间对抗训练，其中规避攻击通过在嵌入空间中使用基于投影梯度的优化进行近似。第二种通过基于 LLM 的改写在对抗训练循环中模拟可实现的规避攻击。至关重要的是，我们对这两种对抗训练变体进行了参数化，以促进效用与攻击鲁棒性之间的平滑权衡。在使用间接提示注入基准的大量实验中，我们表明所提方法在静态攻击下优于最先进的 IPI 防御基准；而在自适应攻击的情况下，我们的对抗训练变体提供了显著更高的效用、更低的攻击成功率，且通常两者兼具。最后，我们表明最佳对抗训练参数可能与特定的应用领域密切相关。因此，在实践中，针对特定领域的恶意文本检测器微调可能是必要的。我们的代码已在 https://github.com/tavia-liu/CAD 公开。

## Abstract
The remarkable instruction-following ability of modern LLMs has enabled their practical use as the minds of agents that can autonomously complete increasingly complex tasks. Therein, however, also lies their vulnerability to attacks which embed malicious instructions in text, common variants of which are known as indirect prompt injection (IPI). A fundamental task in addressing this vulnerability is successful segmentation of a given text into benign and malicious sentences (if any). While a number of approaches for this task have been proposed, no detector combines query-relative detection at the segment level, and none are hardened against adaptive evasion attacks realizable in agentic executions. We address the former limitation by developing an approach for malicious sentence classification that is both context- and query-aware. Next, to harden the resulting classifier against evasion, we present two adversarial training methods. The first is directly adapted feature-space adversarial training (AT) in which evasions are approximated using projected-gradient-based optimization in the embedding space. The second simulates realizable evasion attacks in the AT loop through LLM-based paraphrasing. Crucially, we parametrize both AT variants to facilitate a smooth tradeoff between utility and attack robustness. In extensive experiments using indirect prompt injection benchmarks we show that the proposed approach outperforms state-of-the-art IPI defense baselines under static attacks, while in the case of adaptive attacks, our AT variants provide significantly higher utility, lower attack success rate, and often both. Finally, we show that the best AT parameters can depend intimately on the particular application domain. Consequently, domain-dependent tuning of malicious text detectors is likely necessary in practice. Our code is publicly available at https://github.com/tavia-liu/CAD.

---

## 论文详细总结（自动生成）

# 论文总结：文本中恶意指令的鲁棒上下文感知检测 (CAD)

## 1. 核心问题与整体含义
随着大语言模型（LLM）被广泛应用于自主智能体（Agents），**间接提示注入（Indirect Prompt Injection, IPI）**成为了严重的安全性威胁。攻击者通过在外部文档（如网页、邮件）中嵌入恶意指令，诱导智能体执行非授权操作（如转账、泄露隐私）。

论文指出，现有的防御手段存在两个主要缺陷：
*   **缺乏上下文感知**：许多检测器仅孤立地检查文本片段，忽略了用户原始查询和周围环境对判断“恶意性”的影响。
*   **难以抵御自适应攻击**：现有的过滤或提示词防御在面对专门针对防御逻辑进行规避（Evasion）的自适应攻击时表现脆弱。

## 2. 方法论
论文提出了一种名为 **CAD (Context-Aware Detection)** 的轻量级句子级分类框架，核心思想如下：

*   **上下文感知表示**：
    *   **孤立嵌入**：计算目标句子 $s_j$ 的独立向量表示。
    *   **边际影响嵌入**：计算“包含该句子的全文（查询+文档）”与“剔除该句子后的全文”在嵌入空间中的差值。这捕捉了该句子对整体语义的贡献。
    *   **拼接输入**：将上述两者拼接，输入到一个小型多层感知机（MLP）分类器中。
*   **对抗训练 (Adversarial Training, AT)**：
    *   **特征空间 AT**：在嵌入空间中使用投影梯度下降（PGD）生成扰动，模拟规避攻击。
    *   **LLM 改写 AT**：利用 LLM 对恶意指令进行多种风格的改写（如伪装成代码注释、错误恢复通知等），生成可实现的规避样本。
    *   **参数化权衡**：引入参数 $\alpha$ 控制对抗样本在训练集中的比例，从而在任务效用（Utility）和安全性（Security）之间实现平滑权衡。

## 3. 实验设计
*   **数据集与场景**：
    *   **AgentDojo**：包含银行、Slack、旅行等场景的智能体任务。
    *   **AgentDyn**：更复杂的动态环境，测试对未见过的工具和任务的泛化能力。
    *   **AutoDojo**：用于生成针对防御措施进行黑盒优化的自适应规避攻击。
*   **对比方法 (Baselines)**：
    *   **提示词防御**：Sandwich, Spotlighting。
    *   **过滤防御**：PromptGuard 2, DataFilter, PIGuard, ProtectAI。
    *   **系统级防御**：Progent, DRIFT。
*   **评估指标**：干净效用 (CU)、受攻击下的效用 (UA)、攻击成功率 (ASR)。

## 4. 资源与算力
*   **硬件**：实验运行在 SLURM 集群上，使用了 **NVIDIA RTX A6000 GPU (48GB)**。
*   **算力需求**：论文提到该方法非常轻量，任何显存大于 **8GB** 的单张 GPU 均可支持编码器推理、分类器训练和对抗微调。
*   **软件环境**：Python 3.12.3, PyTorch 2.10.0, Transformers 4.57.1。

## 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流基准上进行了广泛测试，涵盖了静态攻击和自适应攻击。
    *   **消融实验**：分别验证了“无查询信息”和“无周围上下文”对检测性能的影响。
    *   **重复实验**：基础分类器使用了 4 个随机种子，对抗训练使用了 3 个随机种子，并报告了 95% 置信区间。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了同类过滤方法，还跨类别对比了系统级防御，并深入探讨了不同应用领域（如 GitHub vs. Banking）对防御参数敏感性的差异。

## 6. 主要结论与发现
*   **CAD 的优越性**：在保持极高任务效用的同时，CAD 在静态和自适应攻击下的表现均优于大多数 SOTA 基准。
*   **对抗训练的价值**：通过 AT($\alpha$)，可以显著降低自适应攻击的成功率，且特征空间 AT 表现出意想不到的泛化能力（即使扰动后的向量不对应真实文本）。
*   **领域敏感性**：最佳的防御强度 $\alpha$ 在不同领域间差异巨大。例如，银行场景对防御更友好，而 GitHub 场景在提升安全性时会面临更大的效用损失。
*   **低开销**：相比于需要额外调用 LLM 的防御方法（如 DataFilter），CAD 几乎不增加运行时间开销（比例接近 1.0）。

## 7. 优点
*   **轻量且通用**：作为一个通用的包装器，CAD 不需要访问受保护智能体的内部权重，也不需要昂贵的 LLM 调用。
*   **端到端鲁棒性**：首次在智能体工作流层面验证了特征空间对抗训练对真实语言攻击的有效性。
*   **可解释的权衡**：通过参数 $\alpha$ 为实际部署提供了清晰的“效用-安全”调节杠杆。

## 8. 不足与局限
*   **分割粒度限制**：CAD 基于句子分割，如果攻击者将恶意指令巧妙地融合在良性长句中，可能会导致检测失效。
*   **调优依赖**：由于不同领域的最佳 $\alpha$ 不同，用户在实际部署时需要针对特定领域进行校准，无法“一劳永逸”地使用全局参数。
*   **静态编码器**：依赖于冻结的文本编码器（jina-embeddings-v3），如果编码器本身存在偏见或漏洞，可能会限制检测器的上限。

（完）
