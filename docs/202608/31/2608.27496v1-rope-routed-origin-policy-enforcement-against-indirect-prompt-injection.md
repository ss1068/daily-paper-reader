---
title: "ROPE: Routed Origin Policy Enforcement against Indirect Prompt Injection"
title_zh: ROPE：针对间接提示注入的路由源策略执行
authors: "Xinhang Ma, Chaowei Xiao, William Yeoh, Ning Zhang, Yevgeniy Vorobeychik"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27496v1"
tags: ["query:llm"]
score: 8.0
evidence: 针对间接提示注入的策略执行
tldr: 针对大模型智能体面临的间接提示注入（IPI）威胁，本文提出ROPE防御框架。该方法基于结构化信任理念，通过确定性的来源检查，确保敏感工具参数仅源自用户或其授权记录，而非不可信的攻击者内容。实验表明，ROPE在保持极高任务成功率的同时，能有效抵御各种注入攻击，显著优于现有系统级防御方案。
selection_source: fresh_fetch
motivation: 现有的间接提示注入防御手段在处理复杂的运行时工具调用序列时，往往面临安全性与实用性难以兼得的困境。
method: 提出一种基于路由来源策略执行（ROPE）的机制，通过追踪并验证敏感工具参数的来源链条，确保其不被攻击者篡改。
result: "在多个智能体模型上的测试显示，ROPE将攻击成功率降至2.6%以下，同时保留了高达82%-100%的原始实用性。"
conclusion: ROPE通过确定性的来源验证提供了强有力的安全保证，是解决大模型智能体间接提示注入问题的一种高效且稳健的系统级方案。
---

## 摘要
间接提示注入 (IPI) 在使用工具的大语言模型 (LLM) 智能体读取的内容中植入指令，从而引导智能体进行有害的工具调用。最强大的防御措施是系统级的，利用诸如任务条件工具筛选（以防止执行恶意工具）和信息流控制（以避免使用不可信参数执行工具）等技术。然而，随着智能体能力的增强，用户将更多的任务委托给自动化。因此，工具执行序列和参数值越来越多地在运行时确定，仅凭用户的查询无法在不造成显著效用损失的情况下进行可靠筛选。我们提出了 ROPE（路由源策略执行），它植根于一种结构化的信任概念：只有当一个值可以不可伪造地追溯到用户、用户明确指定的来源或用户自己的权威记录时，该值才能到达改变状态的工具。执行过程是对一组经过审计的敏感工具参数进行确定性的来源检查，而对语言模型的唯一依赖仅涉及受信任的用户请求，这超出了攻击者的控制范围。我们的方法具有两个可证明的保证：1) 在轨迹的每一步中，任何唯一来源为攻击者可写内容的值都不会到达受来源保护的参数；2) 注入内容的任何措辞改动都不会改变准入决策。我们在开放式智能体套件上对四种智能体模型进行了评估，ROPE 将攻击成功率控制在 1.6--2.6% 之间，同时保留了 82--100% 的未防御状态下的纯净效用，在效用上显著超过了最先进的系统级防御，同时达到了相当或更好的安全性。此外，我们表明针对 ROPE 优化注入在很大程度上是无效的，而击败了先前系统级防御的长程攻击则实现了零成功率。我们的代码和日志可在 https://github.com/xhOwenMa/ROPE 获取。

## Abstract
Indirect prompt injection (IPI) plants instructions in the content a tool-using LLM agent reads, steering the agent into harmful tool calls. The strongest defenses are system-level, leveraging techniques such as task-conditional tool screening to prevent execution of malicious tools, and information-flow control to avoid tool execution with untrusted parameters. However, as agents grow more capable, users delegate more to automation. Consequently, tool execution sequences and parameter values are increasingly determined at runtime and cannot be reliably screened from solely user's query without significant utility loss. We present ROPE (Routed Origin Policy Enforcement), which is anchored in a structural notion of trust: a value may reach a state-changing tool only if it traces unforgeably to the user, a source the user explicitly named, or the user's own authoritative records. Enforcement is then a deterministic origin check over an audited set of sensitive tool parameters, and the only reliance on a language model involves solely the trusted user request, out of the attacker's reach. Our approach admits two provable guarantees: 1) at every step of a trajectory, no value whose only origin is attacker-writable content reaches an origin-guarded parameter, and 2) no rewording of an injection changes an admission decision. We evaluate across four agent models on open-ended agent suites, ROPE holds attack success rate to 1.6--2.6\% while retaining 82--100\% of undefended clean utility, significantly exceeding state-of-the-art system-level defenses in utility while attaining comparable or better security. Further, we show that optimizing the injection against ROPE is largely ineffective, while long-horizon attacks that defeat prior system-level defenses achieve zero success rate. Our code and logs are available at https://github.com/xhOwenMa/ROPE .

---

## 论文详细总结（自动生成）

以下是对论文《ROPE: Routed Origin Policy Enforcement against Indirect Prompt Injection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体被授权执行发送邮件、转账、管理文件等关键任务，它们面临着**间接提示注入（IPI）**的严重威胁。攻击者通过在智能体读取的外部内容（如网页、邮件）中植入恶意指令，诱导智能体调用有害工具。
*   **现有防御的困境：** 现有的系统级防御要么过于保守（如静态限制工具调用，导致智能体无法处理动态任务，效用大幅下降），要么依赖 LLM 自身进行判断（防御者本身也可能被注入内容攻击）。
*   **研究目标：** 开发一种既能保持智能体高度自主性（效用），又能提供确定性安全保障（安全性）的防御机制。

### 2. 方法论：ROPE 核心思想与技术细节
ROPE 的核心思想是**结构化信任**：不判断内容“好坏”，而是判断值的**“来源（Origin）”**。
*   **三大信任锚点：**
    1.  **T1 (用户请求)：** 用户直接输入的查询。
    2.  **T2 (不可伪造的运行时身份)：** 如带有验证签名的邮件发件人。
    3.  **T3 (用户权威记录)：** 平台维护的、攻击者无法写入的用户历史记录（如订单历史）。
*   **防御流程（三步走）：**
    1.  **路由与范围划定 (Step 1)：** 使用一个仅读取“受信任用户请求”的路由器（Router），确定任务的欠规范程度（Under-specification），并为敏感参数设定策略。由于路由器不接触外部注入内容，因此它是免疫攻击的。
    2.  **编译确定性策略 (Step 2)：** 为每个敏感工具参数分配标记（如 `PROMPT` 必须匹配 T1，`SOURCED` 可匹配 T1/T2）。
    3.  **运行时执行检查 (Step 3)：** 来源追踪器（Origin Tracker）为每个读入的值贴上来源标签。当智能体尝试调用改变状态的工具时，系统进行**确定性的来源匹配检查**。如果不符合策略，则拦截该调用。
*   **理论保证：** 论文证明了 ROPE 具有“来源完备性”（攻击者内容无法到达受保护参数）和“措辞不变性”（改变注入内容的说法不会改变防御决策）。

### 3. 实验设计
*   **数据集/场景：**
    *   **AgentDyn：** 包含 GitHub、购物、日常生活三个套件，侧重于需要运行时重新规划的开放式任务。
    *   **AgentDojo：** 包含银行、Slack、旅行三个套件，侧重于规范化任务。
    *   **AgentLAB：** 用于测试长程（Long-horizon）分阶段攻击。
*   **智能体模型：** GPT-4o-mini, GPT-4o, Gemini-2.5-Flash, Qwen3-235B。
*   **对比方法：** 共 11 种基准防御，包括提示词增强（Sandwich, Spotlight）、检测器（ProtectAI, PIGuard）、系统级防御（DRIFT, Progent, PFI, CaMeL 等）。
*   **攻击类型：** 静态注入、自适应攻击（AutoDojo 优化注入）、长程分阶段攻击。

### 4. 资源与算力
论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。实验主要基于主流商业模型（GPT, Gemini）和开源模型（Qwen）的 API 或推理。由于 ROPE 的核心是确定性的逻辑检查，其运行时的计算开销相对于 LLM 本身的推理开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 种不同架构的 LLM、6 个基准测试套件、11 种对比防御方案。
*   **充分性：** 实验设计非常全面。不仅测试了标准攻击，还引入了**自适应攻击者**（针对防御逻辑进行优化）和**长程攻击**。
*   **客观性：** 论文对基准测试的评分标准进行了修正（纠正了原 Benchmark 中一些不合理的成功判定），并对所有防御方法统一应用了修正后的标准，确保了对比的公平性。

### 6. 主要结论与发现
*   **效用与安全的平衡：** ROPE 是唯一能在保持高纯净效用（82%--100%）的同时，将攻击成功率（ASR）控制在极低水平（1.6%--2.6%）的方法。
*   **击败强力基准：** 在 AgentDyn 这种复杂动态环境下，之前的最强防御（如 DRIFT）效用大幅崩溃，而 ROPE 依然稳健。
*   **抗攻击性：** 自适应优化对 ROPE 几乎无效；在长程攻击测试中，ROPE 实现了 **0%** 的攻击成功率。
*   **路由器灵活性：** 即使使用较便宜的小模型（如 20B 参数量）作为路由器，ROPE 依然能通过“安全下限（Clamp）”机制保持极高的安全性。

### 7. 优点
*   **确定性防御：** 核心执行逻辑不依赖 LLM 的模糊判断，消除了防御者被注入的风险。
*   **可证明的安全性：** 提供了数学上的来源完整性保证。
*   **高实用性：** 允许智能体在不违反来源规则的前提下自由规划，不惩罚良性的运行时偏差。
*   **鲁棒性：** 对注入内容的措辞改动具有天然的免疫力。

### 8. 不足与局限
*   **平台依赖：** ROPE 需要底层平台能够提供和传递值的来源元数据（Provenance），这在现有的一些简易智能体框架中可能尚未实现。
*   **“闭环失败”风险：** 如果一个合法任务所需的值确实只能从不可信的外部内容中获取（且没有 T2/T3 锚点），ROPE 会为了安全而拦截该调用，导致任务失败。
*   **带内内容攻击：** ROPE 主要保护工具参数的“身份/路径”，对于工具参数内部的“自由文本内容”（如邮件正文里的钓鱼链接）防御能力有限。

（完）
