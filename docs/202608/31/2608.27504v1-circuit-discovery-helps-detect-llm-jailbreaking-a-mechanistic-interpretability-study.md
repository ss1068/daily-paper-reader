---
title: "Circuit Discovery Helps Detect LLM Jailbreaking: A Mechanistic Interpretability Study"
title_zh: 电路发现助力检测大语言模型越狱：一项机械可解释性研究
authors: "Paria Mehrbod, Boris Knyazev, Guy Wolf, Eugene Belilovsky, Geraldin Nanfack"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27504v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过电路发现检测并缓解越狱攻击
tldr: "本研究通过机械可解释性方法，深入探讨了LLM（以LLaMA-2-7B-chat为例）在面对越狱攻击时的内部处理机制。研究利用边缘归因修补和子网络探测技术，成功识别出负责生成越狱响应的关键计算电路。通过消减这些电路，越狱攻击成功率可降低80%，揭示了特定注意力头和MLP路径在绕过安全约束中的核心作用，为开发更具解释性的防御机制提供了新思路。"
selection_source: fresh_fetch
motivation: 尽管经过安全对齐，大语言模型仍易受越狱攻击，且其处理对抗性提示的内部机制尚不明确。
method: 采用边缘归因修补和子网络探测技术，系统性地识别模型中负责响应越狱提示的计算电路。
result: "发现消减识别出的关键电路可使越狱攻击成功率降低达80%，并揭示了介导对抗性利用的关键注意力头和MLP路径。"
conclusion: 机械可解释性分析有助于理解LLM的对抗性漏洞，并为构建针对性的解释性防御机制奠定了基础。
---

## 摘要
尽管进行了广泛的安全对齐，大语言模型（LLMs）仍然容易受到越狱攻击，这些攻击通过绕过安全防护措施来诱导生成有害内容。虽然先前的工作将这种脆弱性归因于安全训练的局限性，但 LLMs 处理对抗性提示词的内部机制仍不为人所知。我们对大规模安全对齐 LLM 中的越狱行为进行了机械分析，重点关注 LLaMA-2-7B-chat-hf。利用边缘归因修补（edge attribution patching）和子网络探测（subnetwork probing），我们系统地识别了负责对越狱提示词生成肯定回答的计算电路。在预测第一个 token 期间消融这些电路，可以将攻击成功率降低高达 80%，证明了其在安全绕过中的关键作用。我们的分析揭示了介导对抗性提示词利用的关键注意力头和 MLP 路径，展示了重要 token 如何通过这些组件传播以覆盖安全约束。这些发现增进了对已对齐 LLMs 中对抗性脆弱性的理解，并为基于机械可解释性的针对性、可解释防御机制铺平了道路。

## Abstract
Despite extensive safety alignment, large language models (LLMs) remain vulnerable to jailbreak attacks that bypass safeguards to elicit harmful content. While prior work attributes this vulnerability to safety training limitations, the internal mechanisms by which LLMs process adversarial prompts remain poorly understood. We present a mechanistic analysis of the jailbreaking behavior in a large-scale, safety-aligned LLM, focusing on LLaMA-2-7B-chat-hf. Leveraging edge attribution patching and subnetwork probing, we systematically identify computational circuits responsible for generating affirmative responses to jailbreak prompts. Ablating these circuits during the first token prediction can reduce attack success rates by up to 80\%, demonstrating its critical role in safety bypass. Our analysis uncovers key attention heads and MLP pathways that mediate adversarial prompt exploitation, revealing how important tokens propagate through these components to override safety constraints. These findings advance the understanding of adversarial vulnerabilities in aligned LLMs and pave the way for targeted, interpretable defense mechanisms based on mechanistic interpretability.

---

## 论文详细总结（自动生成）

这篇论文《Circuit Discovery Helps Detect LLM Jailbreaking: A Mechanistic Interpretability Study》通过机械可解释性（Mechanistic Interpretability）的视角，深入探讨了大语言模型（LLM）在面对越狱攻击时的内部运作机制。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管 LLM 经过了严格的安全对齐（如 RLHF），但仍会被“越狱提示词”（Jailbreak Prompts）绕过防御。目前研究多关注越狱为何成功（如竞争目标理论），但对于模型内部**如何**处理这些对抗性输入、哪些神经元或计算路径介导了这一过程，仍缺乏深入理解。
*   **研究动机**：通过识别模型中负责越狱行为的具体“计算电路”（Circuits），不仅可以揭示漏洞的机械原理，还能为开发针对性的、可解释的防御机制提供理论支持。

### 2. 方法论
论文的核心思想是识别模型计算图中的一个稀疏子网络（即电路），该子网络能够忠实地代表模型产生越狱响应的行为。
*   **关键技术**：
    *   **边缘归因修补（Edge Attribution Patching, EAP）**：一种基于梯度的快速近似方法。通过计算损失函数对模型边缘（Edge）激活值的梯度，评估删除某条路径对模型输出的影响。
    *   **子网络探测（Subnetwork Probing）**：通过学习二值掩码（Mask）来直接优化子网络。利用重参数化技巧（如 Gumbel-Softmax）和 $L_0$ 正则化，在保持模型输出忠实度的同时追求电路的稀疏性。
*   **算法流程**：
    1.  构建越狱数据集（包含有害指令和优化的后缀）。
    2.  在模型预测第一个 token（通常是肯定的回答如 "Sure"）时，应用 EAP 或子网络探测来识别关键的注意力头和 MLP 路径。
    3.  通过“零消融”（Zero Ablation）验证识别出的电路是否确实控制了越狱行为。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型**：LLaMA-2-7B-chat-hf（典型的安全对齐模型）。
    *   **数据集**：基于 HarmBench 提取的两个子集：Hacking Stealing Jailbreak (HSJ) 和 Harmful Jailbreak (HJ)。
    *   **攻击手段**：使用改进的 GCG（Greedy Coordinate Gradient）算法生成越狱后缀。
*   **对比实验**：
    *   对比了 EAP 和子网络探测在不同稀疏度下的“忠实度”（Faithfulness）。
    *   **Baseline**：将消融电路后的模型生成效果与“随机添加 token”的基准进行对比。
    *   **消融实验**：测试了在预测 1 个 token 和 3 个 token 目标下学习到的电路对防御效果的影响。

### 4. 资源与算力
*   **算力说明**：论文提到实验得到了 Calcul Quebec 和 Compute Canada 的计算资源支持，并获得了 OpenPhilanthropy 和 Mila Samsung 的资助。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但考虑到 LLaMA-2-7B 的规模以及 EAP 仅需两次前向和一次反向传播的特性，该方法在算力上相对高效。

### 5. 实验数量与充分性
*   **实验规模**：使用了 100 个训练样本进行电路发现，50 个测试样本验证忠实度。
*   **充分性评价**：
    *   **优点**：涵盖了两种不同的电路发现方法和两类典型的有害任务，并进行了可视化分析。
    *   **局限**：样本量（100/50）相对较小；仅针对 LLaMA-2-7B 进行了测试，未在更大规模（如 70B）或其他架构（如 GPT-4 级别）的模型上验证通用性。

### 6. 主要结论与发现
*   **电路有效性**：子网络探测比 EAP 更能有效地找到极稀疏且高忠实度的电路（在小于 10% 的边缘占比下仍能保持低损失）。
*   **防御效果显著**：在预测第一个 token 时消融识别出的电路，可将越狱攻击的成功率降低高达 **80%**。模型会从生成“Sure”转向生成拒绝语（如“I cannot...”）。
*   **内部机制揭示**：关键节点会同时关注系统提示词（安全约束）、用户指令（有害内容）和越狱后缀。这验证了“竞争目标”假说，即越狱后缀通过特定的注意力路径抵消了安全对齐的影响。

### 7. 优点与亮点
*   **首创性**：这是首个针对大规模安全对齐 LLM 越狱行为进行的系统性机械可解释性研究。
*   **可解释防御**：不仅停留在理论分析，还展示了如何利用发现的电路进行实际的防御（通过针对性消融）。
*   **可视化**：提供了清晰的电路图，展示了信息如何在不同层的注意力头和 MLP 之间流动。

### 8. 不足与局限
*   **性能权衡（Completeness）**：论文未评估消融越狱电路是否会损害模型在正常、无害任务上的表现（即是否会造成过度拒绝）。
*   **生成崩溃**：完全消融电路后，模型在长文本生成时会出现重复或无意义的输出，说明消融技术仍需改进。
*   **静态性**：研究主要针对 GCG 产生的后缀，对于其他类型的越狱（如角色扮演、长文本诱导）是否适用尚待验证。

（完）
