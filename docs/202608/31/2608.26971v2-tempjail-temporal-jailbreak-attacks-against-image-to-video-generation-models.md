---
title: "TempJail: Temporal Jailbreak Attacks against Image-to-Video Generation Models"
title_zh: TempJail：针对图像到视频生成模型的时间越狱攻击
authors: "Qi Lu, Zehui Guo, David Yuanda Gan, Zijing Li, Hengda Zhang, Weijun Xu, Qiankun Zhang"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.26971v2"
tags: ["query:llm"]
score: 8.0
evidence: 针对视频生成模型的时序越狱攻击
tldr: 本研究针对图像生成视频（I2V）模型中的时间维度安全漏洞，提出了TempJail攻击框架。该框架揭示了不安全语义可能通过随时间推移的语义组合而非单帧内容产生。TempJail通过时间抽象将恶意指令分解，并利用图像端的潜空间扰动与文本端的语义伪装绕过安全过滤。实验证明，该方法在Kling、Veo等商用模型上显著提升了攻击成功率，为视频生成模型的安全性评估提供了新视角。
selection_source: fresh_fetch
motivation: 现有研究主要关注单帧违规，忽略了视频生成中随时间演变的语义组合所带来的潜在安全风险。
method: 提出TempJail框架，通过分解恶意指令并结合图像潜空间引导与文本语义伪装，实现对I2V系统的时序越狱攻击。
result: "在多个主流商用模型上的测试显示，TempJail在GPT-5.2和人工评估中的攻击成功率分别提升了23.3%和22.0%。"
conclusion: 该研究证明了I2V模型在时间维度上存在显著漏洞，呼吁业界关注并加强针对动态语义组合的安全防御。
---

## 摘要
近年来，图像到视频（I2V）生成模型在主体一致性和时间连贯性方面取得了显著进展，实现了高质量的视频合成。然而，这些进步也带来了新的安全风险。现有研究主要集中在涉及单帧违规的越狱攻击，而在很大程度上忽略了视频生成模型特有的时间维度。在本文中，我们调查了三种攻击场景，并揭示了 I2V 系统中的一种时间漏洞：不安全语义可能并非源于单帧，而是源于随时间推移的语义组合。我们进一步确定了此类攻击中的两个关键挑战：时间抽象和语义伪装。为了解决这些问题，我们提出了 TempJail，一种针对 I2V 系统的创新时间越狱框架。针对时间抽象，我们将目标恶意描述分解为初始帧视觉条件和时间文本指令。针对语义伪装，在图像端，我们将语义注入建模为扩散采样中的受控潜空间扰动，并引入来自预训练编码器的梯度引导；在文本端，我们将描述重写为无害的“主体-动作-场景”模板，以绕过安全过滤器，同时保留时间引导。在黑盒推理阶段，这两种模态共同使恶意语义能够随时间推移逐渐触发。在包括可灵（Kling）、Seedance、Veo 和 PixVerse 在内的闭源商业模型上的实验表明，在 GPT-5.2 评估下，TempJail 的攻击成功率比现有最先进方法提高了 23.3%，在人工评估下提高了 22.0%。我们的代码可在 GitHub 获取。

## Abstract
In recent years, image-to-video (I2V) generation models have made remarkable progress in subject consistency and temporal coherence, enabling high quality video synthesis. However, these advances also introduce new safety risks. Existing studies mainly focus on jailbreak attacks involving single frame violations, while largely overlooking the temporal dimension unique to video generation models. In this paper, we investigate three attack scenarios and uncover a temporal vulnerability in I2V systems: unsafe semantics may emerge not from a single frame, but from semantic composition over time. We further identify two key challenges in such attacks: temporal abstraction and semantic camouflage. To address these issues, we propose TempJail, a novel temporal jailbreak framework for I2V systems. For temporal abstraction, we decompose a target malicious caption into an initial frame visual condition and a temporal text instruction. For semantic camouflage, on the image side we model semantic injection as controlled latent perturbation in diffusion sampling and introduce gradient guidance from pretrained encoders. On the text side, we rewrite the caption into an innocuous ``subject-action-scene'' template that bypasses safety filters while preserving temporal guidance. In the black-box inference phase, these two modalities jointly enable malicious semantics to be gradually triggered over time. Experiments on closed-source commercial models, including Kling, Seedance, Veo and PixVerse, show that TempJail improves attack success rate over prior state-of-the-art methods by 23.3\% under GPT-5.2 evaluation and 22.0\% under human evaluation. Our codes are available at \href{https://github.com/luqi-glory/TempJail}{GitHub}.

---

## 论文详细总结（自动生成）

以下是对论文《TempJail: Temporal Jailbreak Attacks against Image-to-Video Generation Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着图像生成视频（I2V）模型（如可灵、Veo等）在时空连贯性上的突破，其安全性风险也随之演变。现有的越狱攻击研究主要集中在**单帧违规**（即静态图像或文本指令中的敏感词），而忽略了视频特有的**时间维度**。
论文揭示了一个关键的**时间漏洞**：不安全语义可能并非存在于任何单一帧中，而是通过**随时间推移的语义组合**（Semantic Composition over Time）逐渐显现。例如，将“爆炸”分解为“物体快速膨胀”和“碎片飞散”的连续动作。论文旨在提出一种能够绕过现有安全过滤器，并利用模型自身的时间预测能力来触发恶意内容的攻击框架。

### 2. 方法论：TempJail 框架
TempJail 的核心思想是将恶意描述进行“时间抽象”与“语义伪装”，通过跨模态协同触发。

*   **时间抽象（Temporal Abstraction）：**
    *   将目标恶意标题（Caption）解构为：**初始帧视觉条件**（用于生成第一帧图像）和**时间文本指令**（用于引导后续动作演变）。
*   **语义伪装（Semantic Camouflage）：**
    *   **图像端（Guided Semantic Injection）：** 为了避免在像素空间直接修改产生易被检测的高频噪声，研究者在**扩散模型的潜空间（Latent Space）**进行扰动。
        *   利用预训练编码器（如 CLIP）计算良性图像块与恶意参考图像之间的相似度。
        *   采用 **Winner-Takes-All** 策略，仅优化对语义贡献最大的图像块。
        *   引入 **Real-Noise Injection**（真实噪声注入）策略，在采样过程中保持图像的自然度，防止视觉伪影。
    *   **文本端（Syntactic Template）：** 使用本地部署的 LLM 将恶意指令重写为无害的 **“主体-动作-场景”（Subject-Action-Scene）** 模板。
        *   例如，将“炸弹爆炸”重写为“手持矩形设备，按下按钮，场景剧烈震动并伴随物质扩散”。
*   **黑盒推理：** 在推理阶段，看似良性的图像和文本指令共同作用，利用 I2V 模型追求“动作平滑”的本能，在生成的视频流中重构出完整的恶意语义。

### 3. 实验设计
*   **测试对象：** 四个主流闭源商业 I2V 模型：**Kling（可灵）、Seedance、Veo、PixVerse**。
*   **数据集：** 基于 **T2VSafetyBench**，涵盖 14 个风险类别（如色情、暴力、血腥、政治敏感、非法活动等），共筛选出 700 个平衡样本。
*   **对比方法：**
    *   文本越狱方法：T2VSafety、Scene-Split、T2V-OptJail。
    *   多模态越狱方法：RunawayEvil、VII（视觉指令注入）。
*   **评估指标：** 攻击成功率（ASR，通过 GPT-5.2 和人工双重评估）和语义相似度（CLIP-S）。

### 4. 资源与算力
*   **模型组件：** 使用了 Z-image 作为 T2I 生成器，Stable Diffusion 2.1 作为潜空间优化的基础模型，Llama 作为文本重写的本地 LLM。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练/优化时长。但考虑到涉及扩散模型的迭代采样优化和本地 LLM 推理，通常需要消费级或企业级的高性能 GPU（如 A100 或 RTX 4090）。

### 5. 实验数量与充分性
*   **实验规模：** 针对 4 个模型、14 个类别进行了大规模测试，每组实验均包含 LLM 自动评估和人工校验，结果具有较高的一致性。
*   **消融实验：** 针对 Winner-Takes-All 策略、真实噪声注入、潜空间扰动阈值 $\delta$、局部块比例 $s$ 等关键超参数进行了详细的消融研究。
*   **客观性：** 实验对比了多种最先进的（SOTA）基准方法，且测试对象均为真实世界的闭源商业系统，证明了攻击的实用性和普适性。

### 6. 主要结论与发现
*   **显著提升：** TempJail 在 GPT-5.2 评估下比现有最先进方法 ASR 提升了 **23.3%**，人工评估提升了 **22.0%**。
*   **时间维度是防御盲区：** 实验证明，即使输入图像和文本单独看都是合规的，I2V 模型仍会通过时间连贯性“自动补全”恶意逻辑。
*   **潜空间优化的优越性：** 相比于传统的像素级对抗攻击，在扩散采样过程中注入语义能更好地保留图像质量并绕过系统的去噪防御。

### 7. 优点（亮点）
*   **视角新颖：** 首次系统性地探讨了 I2V 模型在**时间维度**上的安全漏洞。
*   **隐蔽性强：** 通过潜空间扰动和中性词重写，极大地提高了攻击载体的欺骗性。
*   **工程实现优雅：** 提出的“主体-动作-场景”模板有效地解决了复杂恶意场景难以映射为控制信号的问题。

### 8. 不足与局限
*   **防御对策较少：** 论文主要侧重于攻击，虽然揭示了漏洞，但对于如何从模型底层防御此类“时间组合式”攻击探讨较少。
*   **多模态覆盖：** 目前主要关注视觉和文本，尚未涉及最新视频模型中可能包含的音频模态越狱。
*   **依赖参考图：** 攻击效果在一定程度上取决于辅助 T2I 模型生成的参考图像质量。

（完）
