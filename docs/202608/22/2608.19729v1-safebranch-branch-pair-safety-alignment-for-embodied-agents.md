---
title: "SafeBranch: Branch-Pair Safety Alignment for Embodied Agents"
title_zh: SafeBranch：具身智能体的分支对安全对齐
authors: "Hyunse Lee, Jiwoo Jeong, Haneul Lee, Kyochul Jang, Youngjae Yu, Woojin Lee"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19729v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 具身智能体的安全对齐与分支对构建
tldr: 针对具身智能体在执行任务时易违反安全约束的问题，本文提出 SafeBranch 框架。该框架通过环境回滚机制，从智能体自身的错误尝试中构建“分支对”，将不安全动作与安全替代方案在关键步骤进行对比对齐。这种方法无需在部署时使用评论器，在 IS-Bench 等基准测试中显著提升了安全性与任务成功率，尤其在处理未见任务和物体时表现优异。
selection_source: fresh_fetch
motivation: 具身智能体的安全与任务成功是不同目标，且安全问题仅出现在少数关键步骤，传统模仿学习或对比学习难以有效提取纯粹的安全信号。
method: 提出 SafeBranch 框架，通过回滚不安全轨迹至关键步骤并生成安全替代动作，构建仅在关键点存在差异的分支对来对齐智能体行为。
result: 在多个基准测试及分布外任务中，该方法在不牺牲任务成功率的前提下显著增强了安全性，在未见物体场景下的安全成功率提升了约十倍。
conclusion: SafeBranch 证明了通过针对性分支对齐可以有效提升具身智能体的交互安全性，且具备良好的泛化能力。
---

## 摘要
基于视觉语言模型的具身智能体能够完成指令任务，但在执行过程中往往会违反安全约束，这一问题近期被定义为交互式安全。训练此类智能体安全地行动十分困难，因为安全性和任务成功是两个独立的目标，且安全性仅体现在轨迹中少数关键安全步骤上。标准的监督方式并不充分：模仿安全轨迹只能教会行为，却无法解释其安全的原因；而对比任意的安全与不安全轨迹则会将安全信号与无关差异混淆。我们提出了 SafeBranch，这是一个通过环境回滚从智能体自身的不安全展开中构建分支对，进而对具身执行器进行安全对齐的框架。SafeBranch 将每个不安全展开回滚至导致违规的关键安全步骤，向执行器查询安全的替代方案，并将原始动作与替代方案配对，使两个分支仅在该步骤上存在差异。训练后的执行器在部署时无需评论器参与即可安全运行。在 IS-Bench、SafetyALFRED 以及包含未见任务和物体的分布外变体上，该框架在不牺牲任务成功率的前提下可靠地处理了安全性，在未见物体变体上的安全成功次数约为未训练基线的十倍。

## Abstract
Vision-language-model-based embodied agents can complete instructed tasks but often violate safety constraints in the process, a problem recently framed as interactive safety. Training such agents to act safely is difficult, since safety and task success are distinct objectives, and safety arises only at a small number of safety-critical steps within a trajectory. Standard supervision is insufficient: imitating safe trajectories teaches behavior without explaining why it is safe, and contrasting arbitrary safe and unsafe trajectories mixes the safety signal with unrelated differences. We propose SafeBranch, a framework that aligns an embodied actor on safety through branch pairs constructed from the actor's own unsafe rollouts via environment rollback. SafeBranch rolls each unsafe rollout back to the safety-critical step that caused the violation, queries the actor for a safe alternative, and pairs the original action with the alternative so that the two branches differ only at that step. The trained actor acts safely at deployment with no critic in the loop. On IS-Bench, SafetyALFRED, and out-of-distribution variants with unseen tasks and objects, it handles safety reliably without sacrificing task success, achieving roughly ten times more safe successes than the untrained baseline on the unseen-object variant.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SafeBranch** 的框架，旨在解决具身智能体（Embodied Agents）在执行任务时的“交互式安全”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
具身智能体（如基于视觉语言模型 VLM 的机器人）虽然能完成复杂指令，但在执行过程中常会引发安全隐患（如用湿手碰插座、做完饭不关火）。
*   **研究动机**：安全性与任务成功是不同的目标，且安全性通常取决于轨迹中极少数的“关键安全步骤”。
*   **现有挑战**：
    *   **模仿学习（SFT）**：只教怎么做，不教为什么安全，容易导致智能体在无关场景下过度谨慎。
    *   **轨迹级对比（DPO）**：安全信号被稀释在整条轨迹中，智能体难以定位具体的决策点。
    *   **推理时防御**：依赖外部评论器或搜索算法，部署开销大且执行器本身未得到进化。

### 2. 方法论：核心思想与技术细节
SafeBranch 的核心思想是构建**分支对（Branch Pairs）**，即在完全相同的上下文（Context）下，对比一个安全动作和一个不安全动作。

*   **分支对构建流程**：
    1.  **不安全展开**：让智能体自主执行任务，直到发生安全违规。
    2.  **环境回滚（Rollback）**：利用模拟器的可逆性，将环境状态回滚到导致违规的“关键安全步骤”。
    3.  **评论器引导修复**：外部评论器（如 GPT-4o）提供反馈，引导智能体在同一位置生成一个安全的替代动作。
    4.  **事后重标记（Hindsight Relabeling）**：移除评论器的反馈信息，使安全和不安全动作都对应相同的原始输入。
*   **触发机制**：
    *   **前瞻性触发**：在动作执行前，若违反预设逻辑则拦截并回滚。
    *   **回顾性触发**：任务完成后，若存在残留隐患（如没关水龙头），回滚至相关步骤。
*   **训练目标（BranchPO）**：采用分支偏好优化（Branch Preference Optimization），在关键步骤上最大化安全动作与不安全动作的对数概率差。

### 3. 实验设计
*   **数据集/场景**：
    *   **IS-Bench**：包含 161 个家庭任务的高保真模拟环境。
    *   **SafetyALFRED**：包含 222 条带有危险标注的轨迹，用于测试跨模拟器的迁移能力。
    *   **OOD 变体**：构建了 **OOD-ObjectShift**（加入干扰物体）和 **OOD-TaskShift**（替换目标物体）来测试泛化性。
*   **对比方法**：
    *   **推理时基线**：自我验证（Self-Verification）、前瞻搜索（Lookahead）。
    *   **训练基线**：仅 SFT、轨迹级 DPO、成功匹配的轨迹 DPO。
*   **评估指标**：任务成功率（SR）、安全成功率（SSR）、安全召回率（SRec）。

### 4. 资源与算力
*   **硬件**：使用了单个多 GPU 节点，配备 **NVIDIA H100 (80GB)** 显卡。
*   **训练细节**：
    *   骨干模型为 Qwen3-VL-32B-Instruct。
    *   数据构建阶段：通过 608 次展开生成了 475 个最终分支对。
    *   训练过程：先进行简短的 SFT 预热，再进行 BranchPO 优化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了分布内（ID）、分布外（OOD）以及跨模拟器（Cross-simulator）的全面评估。
*   **消融实验**：针对“是否移除评论器反馈”、“不同过滤阶段对性能的影响”以及“不同偏好对齐方式”进行了深入对比。
*   **客观性**：引入了**人类评估者**对生成的 100 个分支对进行盲测，验证了自动构建流程的可靠性（一致性达 93%）。实验设计公平，对比基线均在相同的 32B 模型底座上运行。

### 6. 主要结论与发现
*   **安全性显著提升**：在 IS-Bench 上，SafeBranch 将安全成功率（SSR）从 0.031 提升至 0.281。
*   **泛化能力强**：在未见物体的 OOD 场景下，SSR 提升了约 10 倍（从 0.048 到 0.469）。
*   **部署高效**：训练后的模型在部署时**无需任何外部评论器或回滚机制**，直接输出即具备高安全性。
*   **数据效率**：相比于自然采样，SafeBranch 生成可用分支对的速度快了约 5.2 倍。

### 7. 优点
*   **精准对齐**：通过分支对结构，将模糊的轨迹级信号转化为精确的步骤级信号。
*   **内化安全**：将安全意识“缝合”进模型权重中，消除了推理时的额外延迟和成本。
*   **鲁棒性**：在面对环境扰动（OOD）时表现出比推理时防御方法更强的稳定性。

### 8. 不足与局限
*   **依赖模拟器特性**：该方法高度依赖环境的“回滚”能力，在难以精确重置状态的现实物理世界中应用受限。
*   **训练成本转移**：虽然部署时无需评论器，但训练阶段仍需调用昂贵的强模型（如 GPT-4o）作为评论器。
*   **覆盖范围**：某些类型的危险（如跌倒/碰撞）在当前的自动构建流程中产出的有效训练对较少。

（完）
