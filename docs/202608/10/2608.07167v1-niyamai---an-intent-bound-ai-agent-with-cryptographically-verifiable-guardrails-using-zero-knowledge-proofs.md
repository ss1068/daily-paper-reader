---
title: NiyamAI - An Intent-Bound AI Agent with Cryptographically Verifiable Guardrails using Zero-Knowledge Proofs
title_zh: NiyamAI：一种利用零知识证明实现密码学可验证护栏的意图绑定 AI 智能体
authors: "Aditya Katkar, Om Karkele, Kartik Mandhane, Manisha More, Yash Kashid"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07167v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 智能体的密码学可验证护栏
tldr: 针对自主LLM代理面临的提示注入和不安全工具调用等安全威胁，本文提出Niyam-AI框架。该框架通过SHA-256锁定意图合约，并利用隔离的裁判模型与零知识证明（zk-SNARKs）技术，确保工具调用在执行前经过可验证的安全检查。实验表明，Niyam-AI在Agent-SafetyBench上表现优异，显著提升了安全防护的准确性与数学可验证性。
selection_source: fresh_fetch
motivation: 现有的AI代理安全防御机制缺乏可验证的执行证明，容易受到提示注入和推理幻觉的攻击。
method: 引入意图合约锁定权限，并通过隔离的裁判模型结合zk-SNARK技术生成加密证明，确保工具调用符合预设约束。
result: "在2000个真实场景测试中，Niyam-AI达到了88.5%的F1分数，显著优于NeMo Guardrails等现有基准方案。"
conclusion: Niyam-AI为AI代理提供了一种高精度且数学上可验证的安全护栏，有效平衡了安全性和执行效率。
---

## 摘要
赋予 AI 智能体发送电子邮件、查询数据库或执行命令的能力非常有用，直到该智能体被诱导执行不当操作。提示词注入、幻觉推理和不安全的工具调用构成了自主大语言模型（LLM）智能体的主要攻击面。现有的防御措施依赖于在攻击者目标机器上运行的系统提示词或策略过滤器等软件检查，无法提供可验证的执行证明。我们介绍了 Niyam-AI，这是一个使安全执行具有可证明性的框架。在会话开始时，允许使用的工具和约束条件被锁定在一个通过 SHA-256 提交的意图合约（Intent Contract）中。每一个工具调用都会被一个隔离的裁判模型（Judge model）拦截并验证；通过验证后，将通过 EZKL 生成一个 zk-SNARK 证明。只有在证明验证通过后，工具才会执行，这使得第三方能够在不访问裁判模型权重的情况下确认安全策略的执行。在来自 Agent-SafetyBench 的 2,000 个真实场景中，使用五折分层交叉验证对 Niyam-AI 进行评估，并与 NeMo Guardrails、Meta 的 Llama Prompt Guard 2 以及 OpenAI 的 GPT-OSS-Safeguard 进行对比，结果显示其 F1 分数为 88.5%，误报率为 1.1%（自助法 95% 置信区间：[85.19%, 91.88%]，N=1000）。McNemar 精确配对检验确认了显著的改进：在不一致的场景中，Niyam-AI 相比 NeMo 胜出 390 次（仅 20 次落后），相比 Prompt Guard 2 胜出 115 次（仅 13 次落后），相比 GPT-OSS-Safeguard 胜出 384 次（仅 19 次落后），所有情况下的 p 值均小于 0.0001。证明生成为每个批准的操作增加了 2260.6 +/- 218.4 毫秒的延迟，而验证过程耗时 53.1 +/- 11.8 毫秒。Niyam-AI 提供了一种既高度准确又在数学上可验证的护栏——尽管这反映了一个针对 Agent-SafetyBench 进行了调整的分类器与零样本（zero-shot）基准模型的对比，这一区别在第 IV.C 节中进行了讨论。

## Abstract
Giving an AI agent the ability to send emails, query databases, or execute commands is useful--until the agent is tricked into doing something it shouldn't. Prompt injection, hallucinated reasoning, and unsafe tool calls form the primary attack surface for autonomous LLM agents. Existing defenses rely on software checks like system prompts or policy filters running on the same machine the attacker targets, offering no verifiable proof of execution. We introduce Niyam-AI, a framework that makes safety enforcement provable. At session start, permitted tools and constraints are locked into an Intent Contract committed via SHA-256. Every tool call is intercepted and validated by an isolated Judge model; upon passing, a zk-SNARK proof is generated via EZKL.   The tool executes only after proof verification, allowing third parties to confirm enforcement without accessing Judge model weights. Evaluating Niyam-AI on 2,000 real-world scenarios from Agent-SafetyBench against NeMo Guardrails, Meta's Llama Prompt Guard 2, and OpenAI's GPT-OSS-Safeguard using 5-fold stratified cross-validation yields an F1 score of 88.5% with a 1.1% false-positive rate (bootstrap 95% CI: [85.19%, 91.88%], N=1000). McNemar's exact paired test confirms significant improvement: Niyam-AI wins 390 discordant scenarios against NeMo (vs 20 losses), 115 against Prompt Guard 2 (vs 13), and 384 against GPT-OSS-Safeguard (vs 19) with p < 0.0001 in all cases.   Proof generation adds 2260.6 +/- 218.4 ms per approved action, while verification takes 53.1 +/- 11.8 ms. Niyam-AI provides a guardrail that is both highly accurate and mathematically verifiable--though this reflects a classifier adapted to Agent-SafetyBench evaluated against zero-shot baselines, a distinction discussed in Section IV.C.

---

## 论文详细总结（自动生成）

以下是对论文《NiyamAI - An Intent-Bound AI Agent with Cryptographically Verifiable Guardrails using Zero-Knowledge Proofs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自主 AI 智能体（Agent）在调用外部工具（如发送邮件、操作数据库）时，面临提示词注入（Prompt Injection）、幻觉推理和不安全工具调用等安全威胁。
*   **研究动机**：现有的安全防御措施（如系统提示词、过滤器、策略文件）大多是纯软件检查，且与攻击目标运行在同一台机器上。这意味着一旦主机被攻破或过滤器被绕过，外部无法确认安全检查是否真正运行或运行正确。
*   **整体含义**：论文提出了 **Niyam-AI** 框架，旨在将安全检查从“信任软件逻辑”转变为“数学可证明”。通过密码学手段确保智能体的行为受到预设意图的严格约束，并为每一次安全决策提供不可篡改的证明。

### 2. 方法论：核心思想与技术细节
Niyam-AI 的核心思想是**意图绑定（Intent Binding）**与**零知识机器学习（ZK-ML）**的结合。
*   **意图合约（Intent Contract）**：在会话开始时，将智能体允许使用的工具、数据范围和约束定义为结构化 JSON，并使用 **SHA-256** 生成唯一的意图哈希（IntentHash）进行锁定。
*   **拦截与分类**：智能体生成的工具调用请求在执行前被拦截。系统提取工具名和参数生成动作哈希（ActionHash）。
*   **隔离裁判模型（Judge Model）**：一个轻量级的神经网络（PyTorch 实现）接收 (IntentHash, ActionHash) 元组，判断该动作是否符合意图合约（输出 1 为安全，0 为不安全）。
*   **ZK-ML 证明生成**：如果裁判模型判定为安全，则利用 **EZKL** 框架将模型推理过程编译为零知识电路，生成 **zk-SNARK 证明**（基于 Groth16 算法）。
*   **验证与执行**：执行引擎仅在 zk-SNARK 证明验证通过后才允许工具运行。证明过程不泄露模型权重，且可由第三方（如以太坊智能合约）验证。

### 3. 实验设计
*   **数据集**：使用了 **Agent-SafetyBench**，包含 2,000 个真实世界的智能体攻击/安全场景，涵盖泄露敏感数据、财产损失、物理伤害等 8 类风险。
*   **对比基准（Baselines）**：
    1.  **NeMo Guardrails** (NVIDIA)
    2.  **Llama Prompt Guard 2** (Meta)
    3.  **GPT-OSS-Safeguard-20B** (OpenAI 相关)
*   **评估方法**：采用五折分层交叉验证（5-fold CV），确保测试场景在训练阶段不可见。使用 McNemar 精确配对检验来评估统计显著性。

### 4. 资源与算力
*   **硬件环境**：实验在消费级硬件上完成，包括 **Intel Core i7-1360P / i7-13620H 处理器**，16 GB RAM，运行 Windows 11 系统。
*   **算力说明**：论文未提及使用大规模 GPU 集群。由于裁判模型被设计为轻量级（如 11-8-2 结构的神经网络），其推理和证明生成主要依赖 CPU 和基础算力。证明生成耗时约 2.2 秒，验证耗时约 53 毫秒。

### 5. 实验数量与充分性
*   **实验规模**：基于 2,000 个场景进行了详尽测试，并进行了**消融实验**（对比仅使用准入列表、准入列表+裁判模型、以及完整 ZK 流程的性能）。
*   **红队测试**：构建了 12 个攻击向量（涵盖哈希规范化攻击、模型规避、重放攻击等）进行对抗性评估。
*   **充分性评价**：实验设计较为充分，引入了自助法（Bootstrap）计算 95% 置信区间，并使用了统计显著性检验。但论文也承认，Niyam-AI 的分类器针对特定数据集进行了微调，而基准模型是零样本运行，这在公平性上存在一定的不对称性。

### 6. 主要结论与发现
*   **高准确性**：Niyam-AI 达到了 **88.5% 的 F1 分数**，误报率（FPR）仅为 **1.1%**，显著优于所有基准模型（如 NeMo 的 F1 仅为 40.4%）。
*   **数学可验证性**：成功实现了在 2.2 秒内生成安全证明，证明了 ZK-ML 在智能体安全治理中的可行性。
*   **安全性提升**：通过 McNemar 检验确认，Niyam-AI 在处理不一致场景时的胜率远高于传统方案（胜出比最高达 20.1:1）。
*   **防御有效性**：经过补丁修复后，Niyam-AI 能 100% 防御红队测试中的 12 种攻击向量。

### 7. 优点
*   **不可篡改的审计追踪**：结合 SHA-256 和 ZK-SNARK，提供了一个数学上可查验的审计日志，解决了“谁来监督监督者”的问题。
*   **计算可行性**：巧妙地避开了对庞大 LLM 进行 ZK 证明的难题，通过隔离出轻量级裁判模型，使实时证明生成成为可能。
*   **模块化设计**：安全层与智能体推理层分离，具有很强的部署灵活性。

### 8. 不足与局限
*   **延迟问题**：每个动作增加约 2.2 秒的延迟，对于高频、实时性要求极高的应用场景可能存在瓶颈。
*   **二元决策限制**：目前的裁判模型仅输出“安全/不安全”，无法处理更复杂的策略分级。
*   **泛化风险**：分类器性能依赖于对特定意图分布的训练，对于完全未见过的领域（Out-of-Distribution），其表现尚未得到充分验证。
*   **单智能体局限**：目前仅针对单个智能体设计，尚未解决多智能体协作中的同步和意图传递挑战。

（完）
