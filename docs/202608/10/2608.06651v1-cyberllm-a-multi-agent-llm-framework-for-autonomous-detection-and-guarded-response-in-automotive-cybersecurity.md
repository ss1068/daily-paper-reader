---
title: "CyberLLM: A Multi-Agent LLM Framework for Autonomous Detection and Guarded Response in Automotive Cybersecurity"
title_zh: CyberLLM：一种用于汽车网络安全自主检测与受保护响应的多智能体大语言模型框架
authors: "Nenad Petrovic, Oussama Jeddou, Feres Ben Fraj, Vahid Zolfaghari, Fengjunjie Pan, Andre Schamschurko, Alois Knoll"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.06651v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 大模型的运行时安全防护与受保护响应
tldr: 针对软件定义汽车（SDV）日益复杂的网络安全威胁，本文提出CyberLLM框架。该框架采用多智能体LLM架构，结合确定性检测层与LLM推理，实现了漏洞的自主检测与修复。通过引入形式化运行时安全防护机制，确保所有操作在执行前经过验证。实验表明，该系统在保持零误报的同时，显著提升了汽车ECU模块漏洞的检测覆盖率，证明了受控LLM智能体在车载网络安全防御中的有效性。
selection_source: fresh_fetch
motivation: 软件定义汽车扩大了攻击面，且严苛的安全约束要求自主智能体在执行防御操作时必须具备严格的监管与安全性。
method: 提出一种结合确定性分析层（如AST分析）与LLM精细化推理的多智能体框架，并配备形式化安全防护罩以验证所有执行动作。
result: "在包含47个漏洞的汽车ECU基准测试中，该框架将检测覆盖率从34%提升至约70%，且在干净样本上实现了零误报。"
conclusion: 研究证明，在确定性且可审计的安全包络保护下，LLM智能体能够安全且有效地执行自主车载网络防御任务。
---

## 摘要
软件定义汽车 (SDV) 扩展了涵盖源代码、运行时日志和部署拓扑的汽车攻击面，而安全约束则禁止自主智能体在没有监管的情况下采取行动。本文提出了 CyberLLM，这是一个由大语言模型 (LLM) 编排的多智能体框架，能够在正式的运行时安全保护下自主检测漏洞并执行修复。检测过程结合了确定性层（正则表达式规则、抽象语法树 (AST) 分析器和拓扑图检查）与 LLM 精炼环节，从而通过高精度的推理补充了高召回率的基础。决策智能体汇总发现结果，使用以人为中心的资产分类法对其进行标记，并选择分级响应，通过带签名的跨会话记忆和重新规划反馈来逐步提升其置信度。在允许运行之前，每项操作都会根据四个上下文安全属性和一个独立的动作对齐预言机 (oracle) 进行验证，被拒绝的操作将触发升级和重新规划。对称攻击流水线生成并重放漏洞利用程序，以便在相同场景下对攻防双方进行演练。在一个包含 9 个原始 C、C++ 和 Rust 编写的汽车 ECU 模块、植入了 47 个分层漏洞以及干净对照组的独立基准测试中，常驻确定性层以完美的精确率覆盖了 34% 的标记漏洞，而加入基于事实的 LLM 精炼和完整性检查后，覆盖率大约翻倍至 70% 左右（F1 分数为 0.83），同时在干净对照组上实现了零误报。结果表明，当包裹在确定性、可审计的安全信封中时，LLM 智能体可以执行有效的自主网络防御。

## Abstract
Software-Defined Vehicles (SDVs) expand the automotive attack surface across source code, runtime logs, and deployment topologies, while safety constraints forbid autonomous agents from acting without oversight. This paper presents CyberLLM, a multi-agent, LLM-orchestrated framework that autonomously detects vulnerabilities and executes remediations under a formal, runtime safety guard. Detection combines a deterministic layer (regex rules, AST analyzers, and topology graph checks) with an LLM refinement pass, so a high-recall floor is complemented by high-precision reasoning. A decision agent aggregates findings, tags them with a human-centric asset taxonomy, and selects a tiered response, ratcheting its confidence with signed cross-session memory and re-planning feedback. Every action is validated against four contextual security properties and an independent action-alignment oracle before it is allowed to run, and refused actions trigger escalation and re-planning. A symmetric attack pipeline generates and replays exploits so both sides can be exercised on the same scenarios. On an independent, ground-truthed benchmark of nine original automotive ECU modules in C, C++, and Rust seeding 47 layered vulnerabilities plus clean controls, the always-on deterministic layer covers 34\% of the labeled vulnerabilities at perfect precision, and adding the grounded LLM refinement and completeness passes roughly doubles coverage to about 70\% (F1 $0.83$) while producing zero false positives on the clean controls. The results indicate that LLM agents can perform useful autonomous cyber-defense when wrapped in a deterministic, auditable safety envelope.

---

## 论文详细总结（自动生成）

以下是对论文《CyberLLM: A Multi-Agent LLM Framework for Autonomous Detection and Guarded Response in Automotive Cybersecurity》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着**软件定义汽车（SDV）**的普及，车辆的攻击面从源代码扩展到了运行时日志和复杂的部署拓扑。传统的安全工具（如规则扫描器）往往孤立运行，缺乏跨层推理能力。虽然大语言模型（LLM）具备强大的异构数据推理能力，但其**概率性输出（易产生幻觉）**和**不可控的自主行为**在安全至上的汽车领域是不可接受的。
**核心问题：** 如何在利用 LLM 进行自主网络安全防御的同时，确保其行为符合严格的汽车安全规范，并消除误报和恶意指令注入的风险？

### 2. 方法论：核心思想与关键技术
CyberLLM 提出了一个多智能体框架，其核心思想是**“确定性底座 + LLM 精炼 + 形式化安全护栏”**。

*   **分层检测架构（Layered Detection）：**
    *   **确定性层（Deterministic Floor）：** 使用正则表达式、AST 分析器（如 Bandit, Semgrep）和拓扑图检查器进行初步扫描，提供高精度的基础发现。
    *   **LLM 精炼层（LLM Refinement）：** LLM 智能体以确定性结果为上下文（RAG 模式），进行语义分析以发现逻辑漏洞（如竞态条件、整数溢出），并剔除误报。
*   **决策与响应引擎：**
    *   **决策智能体：** 汇总所有发现，根据资产分类法标记风险，并选择响应等级（软/中/硬）。
    *   **带签名的跨会话记忆：** 使用 HMAC 签名的 SQLite 存储历史案例，防止记忆投毒，并为决策提供参考。
*   **运行时安全护栏（Security Guard）：**
    *   在任何操作执行前，强制执行四个上下文安全属性验证：**任务对齐、动作对齐、来源授权、数据隔离**。
    *   引入独立的 **$H_a$ 预言机（Oracle）**：一个独立的 LLM 判定器，专门验证动作是否真正服务于目标，它只能收紧而不能放宽确定性规则的判定。
*   **对称攻击流水线：** 包含一个红队智能体，用于生成非破坏性的漏洞利用脚本，以便在相同环境下测试防御性能。

### 3. 实验设计
*   **数据集/场景：** 构建了一个独立的、带有地面真值（Ground-truth）的汽车 ECU 基准测试集。
*   **Benchmark 构成：** 包含 9 个原始汽车 ECU 模块（使用 C、C++ 和 Rust 编写，涵盖 CAN 网关、OTA 更新、传感器融合等），共植入 47 个分层漏洞（涵盖内存安全、注入、加密、鉴权等 CWE 类型），并包含 2 个干净的对照组。
*   **对比模式：**
    *   **Fast 模式：** 仅使用确定性静态分析层（离线运行）。
    *   **Deep 模式：** Fast 模式加上 LLM 精炼和完整性检查（使用 GPT-4o）。

### 4. 资源与算力
*   **模型：** 使用了通过 Azure OpenAI 服务接入的 **GPT-4o**。
*   **算力细节：** 文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。由于该框架侧重于推理侧的智能体编排而非模型训练，其算力消耗主要体现在 API 调用开销和本地静态分析工具的运行上。

### 5. 实验数量与充分性
*   **实验规模：** 针对 9 个复杂的 ECU 模块进行了多轮扫描，涵盖了三种主流编程语言（C/C++/Rust）。
*   **充分性：** 实验设计较为客观。通过引入“干净对照组”来严格测试**精确率（Precision）**，并使用 ±3 行的窗口进行位置匹配以验证检测的准确性。
*   **消融实验：** 对比了从仅 Python 规则到多语言规则，再到加入 LLM 第二轮检查的性能提升过程，清晰展示了各组件的贡献。

### 6. 主要结论与发现
*   **性能翻倍：** 确定性层（Fast 模式）仅能覆盖 34% 的漏洞，而加入 LLM 精炼（Deep 模式）后，覆盖率提升至 **70%**，F1 分数达到 **0.83**。
*   **零误报：** 在 Deep 模式下，系统在干净对照组上实现了 **1.000 的精确率**（零误报），证明了 LLM 在确定性规则约束下可以极大地减少幻觉。
*   **安全保障：** 即使在检测层被对抗性输入干扰的情况下，由于独立安全护栏的存在，系统仍能阻止不安全的修复动作执行。

### 7. 优点与亮点
*   **确定性与概率性的平衡：** 巧妙地将传统的静态分析工具作为 LLM 的“锚点”，既保留了 LLM 的推理能力，又克服了其不稳定性。
*   **形式化安全验证：** 将学术界的安全属性框架（如 Siu et al. 的四属性）落地到汽车工程实践中，提供了可审计的安全包络。
*   **多语言支持：** 针对汽车领域特有的 C/C++/Rust 混合环境进行了优化，而非仅局限于 Python。

### 8. 不足与局限
*   **逻辑漏洞挑战：** 尽管 LLM 提升了性能，但对于复杂的逻辑漏洞（如微妙的竞态条件、空指针解引用）仍有约 30% 的漏报率，这标志着静态分析的极限。
*   **动态分析缺失：** 目前主要依赖静态分析，未来需要集成动态工具（如 nmap, CAN 重放）以应对运行时威胁。
*   **实时性限制：** 依赖 LLM API 调用可能会在极高实时性要求的车载场景中产生延迟。
*   **偏差风险：** 实验基准由作者团队构建，虽然是独立编写，但可能存在一定的领域偏差。

（完）
