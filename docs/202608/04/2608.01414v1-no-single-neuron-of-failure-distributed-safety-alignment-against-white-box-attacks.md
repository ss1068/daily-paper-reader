---
title: "No Single Neuron of Failure: Distributed Safety Alignment Against White-Box Attacks"
title_zh: 无单神经元故障：针对白盒攻击的分布式安全对齐
authors: "Simiao Xie, Chuancheng Shi, Shangze Li, Wenhua Wu, Fei Shen, Ying Zhou, Zhiyong Wang, Tat-Seng Chua"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01414v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对白盒攻击的分布式安全对齐
tldr: 针对开源大模型易受白盒神经元攻击的问题，本文提出分布式安全对齐（DSA）方法。该方法通过识别对拒绝行为贡献最大的神经元，并利用确定性掩码与随机丢弃技术，强制模型将安全能力冗余地编码到多个神经元中，从而消除单点故障。实验证明，DSA在不损害通用性能的前提下，显著提升了模型抵御白盒攻击的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的安全对齐方法往往将安全行为集中在少数神经元上，导致模型在面对针对特定神经元的白盒攻击时表现脆弱。
method: 提出DSA方法，利用一阶泰勒得分定位关键安全神经元，并通过掩码和丢弃机制促使安全能力在多个补偿神经元间分布式存储。
result: 实验结果显示，DSA在保持语言和多模态通用能力的同时，大幅增强了模型对抗白盒神经元级安全攻击的韧性。
conclusion: 分布式安全编码是提升大模型安全防御能力的有效途径，能有效解决安全机制中的单点失效问题。
---

## 摘要
随着开源权重大型基础模型的快速发布，安全威胁正从黑盒越狱转向神经元级别的白盒攻击，后者直接识别并操纵与安全相关的神经元。现有的对齐方法通常在少数神经元上研究安全行为，导致了冗余度有限的脆弱单点故障。为解决这一问题，我们提出了分布式安全对齐（DSA），它在多个计算神经元中冗余地编码安全能力，确保模型即使在关键安全神经元受到干扰时也能维持其安全基准。具体而言，我们将干预定位在语言侧前馈网络中下投影层的输入端，并将每个特征坐标视为单个神经元的激活。随后，DSA 将神经元激活与损失梯度相结合，计算出方向感知的一阶泰勒分数，从而在全局范围内识别出对模型当前拒绝行为贡献最大的神经元。最后，通过结合确定性掩码和随机丢弃进行针对性干扰，迫使模型放弃狭隘的安全神经元，并在多个补偿性神经元中冗余地编码安全行为。大量实验表明，DSA 显著提高了针对白盒神经元级安全攻击的鲁棒性，同时保留了模型的通用语言和多模态效用。

## Abstract
With the rapid release of open-weight large foundation models, safety threats are shifting from black-box jailbreaks to neuron-level white-box attacks that directly identify and manipulate safety-related neurons. Existing alignment methods often investigate the safety behavior on a small number of neurons, creating fragile single point of failure with limited redundancy. To address this issue, we propose distributed safety alignment (DSA), which redundantly encodes safety capabilities across multiple computational neurons, ensuring that the model maintains its safety baseline even when critical safety neurons are disrupted. Specifically, we localize the intervention to the inputs of the down-projection layers in language-side feed-forward networks and treat each feature coordinate as the activation of an individual neuron. DSA then combines neuron activations with loss gradients to compute a direction-aware first-order Taylor score that globally identifies the neurons that contribute most to the current refusal behavior of the model. Finally, targeted disruption via deterministic masking and stochastic dropout is coupled, forcing the model to abandon narrow safety neurons and redundantly encode safety behavior across multiple compensatory neurons. Extensive experiments show that DSA substantially improves robustness against white-box neuron-level safety attacks while preserving the model's general language and multimodal utility.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**分布式安全对齐（Distributed Safety Alignment, DSA）**的新框架，旨在解决大型基础模型（LFMs）在白盒攻击下因安全机制过于集中而导致的“单点故障”问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着开源权重模型的普及，攻击者可以通过白盒手段精确定位并剪枝（Pruning）模型内部极少数的“安全神经元”，从而轻易绕过安全对齐。
*   **核心问题**：现有的安全对齐方法（如 RLHF）往往将拒绝行为编码在极少数神经元或路径上，缺乏冗余性。一旦这些核心单元被识别并破坏，模型的防御力会瞬间崩溃。
*   **整体含义**：论文试图从架构和对齐策略层面实现“安全能力的去中心化”，通过在多个神经元中冗余地编码安全行为，确保模型在部分神经元失效时仍能保持安全基准。

### 2. 方法论
DSA 框架分为两个主要阶段，核心思想是“在训练中模拟攻击并强制补偿”：
*   **拒绝神经元定位（Refusal-Neuron Localization）**：
    *   **核心思想**：识别当前对拒绝行为贡献最大的神经元。
    *   **技术细节**：在语言侧前馈网络（FFN）的下投影层输入端进行干预。利用**方向感知的一阶泰勒得分（Direction-aware first-order Taylor score）**，结合神经元激活值和拒绝损失函数的梯度，量化每个神经元对安全拒绝行为的因果贡献，而非仅仅是相关性。
*   **对抗性冗余安全对齐（Adversarial Redundant Safety Alignment）**：
    *   **确定性掩码（Targeted Masking）**：在重对齐训练期间，将得分最高的 $K$ 个安全神经元强制置零（模拟它们被攻击者剪枝的情况）。
    *   **随机丢弃（Stochastic Dropout）**：对剩余神经元应用随机 Dropout，防止安全能力重新集中到另一小部分神经元上。
    *   **联合优化**：在上述扰动下，同时优化有害指令的拒绝损失和良性指令的通用能力损失，迫使模型开发出分布式的补偿性神经元路径。

### 3. 实验设计
*   **数据集**：
    *   **安全评估**：StrongREJECT（文本）、VL-Question（多模态越狱）、NSFW（不安全图像）。
    *   **通用能力**：ARC、GSM8K、TruthfulQA（文本）、MMBench（多模态）。
*   **基准模型**：涵盖 Qwen2.5 (1.5B-14B)、LLaMA-3.2 (1B-8B)、Gemma-7B、Phi-4、DeepSeek-R1-Distill-1.5B 以及多模态模型 Qwen2.5-VL 和 LLaVA-1.5。
*   **对比方法**：原始模型（Original）、SN-Tune、RLHF-Safety、SafeNeuron（目前最先进的神经元级防御）。
*   **攻击场景**：模拟了多种白盒剪枝攻击（ORI, ES, SAS, FULL）以及未见过的攻击方式（GRAD, WANDA, ABLATE）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA H200 GPU**。
*   **效率数据**：
    *   **定位阶段**：处理 1000 个样本约需 1.44 分钟。
    *   **训练阶段**：以 Qwen2.5-7B 为例，训练约需 14.6 分钟，显存占用 61.1 GB。
    *   **推理阶段**：训练完成后移除所有掩码，**推理开销为零**（不增加参数或延迟）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 10 余个不同规模和架构的模型上进行了验证，实验设计非常全面。
*   **充分性**：
    *   包含了消融实验（验证掩码与 Dropout 的各自贡献）。
    *   进行了超参数敏感性分析（如掩码数量 $K$ 和 Dropout 率的影响）。
    *   通过“迭代剪枝”实验证明了安全路径的真实冗余性（即剪掉一批，还有下一批补偿路径）。
*   **客观性**：使用了自动化评测（LLaMA-Guard-3）结合人工专家审核，确保了评估的公正性。

### 6. 主要结论与发现
*   **鲁棒性飞跃**：在最严酷的 FULL 白盒剪枝攻击下，DSA 将 Qwen2.5-7B 的攻击成功率（ASR）从 279/313 降低到了 9/313。
*   **无对齐税**：DSA 在大幅提升安全性的同时，模型的通用任务得分（如 GSM8K）几乎没有下降，甚至在某些指标上有所提升。
*   **多模态通用**：该方法同样适用于多模态模型，能有效抵御图像中嵌入的有害指令。
*   **冗余机制验证**：实验证明 DSA 确实迫使模型在不同层之间建立了多条独立的拒绝路径。

### 7. 优点
*   **解决单点故障**：打破了“安全神经元是稀疏且固定”的传统认知，通过冗余设计提升了本质安全性。
*   **部署友好**：训练后的模型与普通模型结构完全一致，无需任何推理侧的特殊插件或计算开销。
*   **防御广度**：不仅能防御训练时已知的剪枝算法，对未见的梯度攻击和权重剪枝也表现出极强的泛化鲁棒性。

### 8. 不足与局限
*   **依赖初始对齐**：DSA 的效果仍受限于底座模型初始的安全对齐质量。
*   **干预范围**：目前主要集中在 FFN 层，虽然 FFN 占据了模型大部分参数，但注意力机制（Attention）中的安全冗余尚未被充分讨论。
*   **离线重训练**：虽然训练时间短，但仍需要一个专门的重对齐过程，无法直接应用于已部署且不可更改权重的模型。

（完）
