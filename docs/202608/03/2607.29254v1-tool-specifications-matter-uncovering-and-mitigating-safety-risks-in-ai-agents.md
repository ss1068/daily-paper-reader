---
title: "Tool Specifications Matter: Uncovering and Mitigating Safety Risks in AI Agents"
title_zh: 工具规范至关重要：揭示并缓解 AI Agent 中的安全风险
authors: "Minghui Pan, Jiayuxuan Yang, Yuanyuan Yuan, Yu Jiang, Zhenpeng Chen"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29254v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 缓解AI智能体中的安全风险和不安全工具执行
tldr: 本研究揭示了AI智能体在调用工具时安全性下降的根源在于模式化（schema-formatted）的工具规范，这会削弱模型的内部拒绝信号。为此，作者提出了SafeKeep防御机制，通过在推理阶段将安全判断与工具执行解耦，利用扁平化文本进行安全评估，同时保留原始规范用于执行。实验表明，SafeKeep显著提升了有害请求的拒绝率并降低了提示注入攻击的成功率，且不影响任务处理能力。
selection_source: fresh_fetch
motivation: 旨在探究大语言模型在作为智能体调用外部工具时安全性显著下降的根本原因。
method: 提出SafeKeep机制，在推理时将安全评估（使用扁平化文本规范）与工具执行（使用模式化规范）解耦。
result: "该方法将有害请求的平均拒绝率从23.8%提升至70.6%，并显著降低了提示注入攻击的成功率。"
conclusion: 工具规范格式是影响智能体安全的关键因素，通过解耦安全判断与执行可有效平衡安全性和功能性。
---

## 摘要
AI Agent 通过外部工具扩展了大语言模型 (LLM)，使其能够执行复杂任务并将模型输出转化为具有实际影响的现实世界行动。然而，当 LLM 被部署为 Agent 时，其安全性往往会显著下降，而这种性能退化的根源仍不清楚。在本文中，我们确定了模式格式化 (schema-formatted) 的工具规范是 Agent 安全性退化的主要来源，并通过白盒表征分析证明，它们削弱了模型的内部拒绝信号，并导致了不安全的工具执行。基于这一发现，我们提出了 SafeKeep，这是一种推理时防御机制，它将安全判断与工具执行解耦：它使用扁平化的文本工具规范来评估请求，同时保留原始的模式格式化规范用于执行。在两个代表性基准测试和四个 LLM（包括白盒和黑盒模型）上，SafeKeep 将有害请求的平均拒绝率从 23.8% 提高到 70.6%，并将观察级提示注入下的平均攻击成功率从 25.6% 降低到 2.5%。它还优于现有的防御措施，并保留了任务处理能力。我们在 https://github.com/snowcatsmoking/SafeKeep 发布了代码和数据。

## Abstract
AI agents extend large language models (LLMs) with external tools, enabling them to perform complex tasks and translate model outputs into consequential real-world actions. Yet LLMs often become substantially less safe when deployed as agents, and the source of this degradation remains poorly understood. In this paper, we identify schema-formatted tool specifications as a primary source of agent safety degradation and show, through white-box representation analysis, that they weaken the model's internal refusal signals and contribute to unsafe tool execution. Building on this finding, we propose SafeKeep, an inference-time safeguard that decouples safety judgment from tool execution: it assesses requests using flattened textual tool specifications while retaining the original schema-formatted specifications for execution. Across two representative benchmarks and four LLMs, including both white-box and black-box models, SafeKeep increases the average refusal rate for harmful requests from 23.8% to 70.6% and reduces the average attack success rate under observation-level prompt injection from 25.6% to 2.5%. It also outperforms existing safeguards and preserves task-handling capability. We release the code and data at https://github.com/snowcatsmoking/SafeKeep .

---

## 论文详细总结（自动生成）

这篇论文《Tool Specifications Matter: Uncovering and Mitigating Safety Risks in AI Agents》对 AI 智能体（Agent）安全性下降的内在机制进行了深入探讨，并提出了一种轻量级的防御方案。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：现代大语言模型（LLM）在作为聊天机器人（Chatbot）时经过了严格的安全对齐，表现出良好的拒绝行为；但当它们被部署为智能体（Agent）并配备外部工具时，安全性会显著下降，容易执行有害指令或受到提示注入攻击。
*   **研究动机**：探索为什么相同的 LLM 在智能体环境下会变得“不安全”。研究者试图定位是智能体输入中的哪个组件（角色描述、工具指令、工具规范）导致了这种安全性的退化。

### 2. 论文提出的方法论
*   **机制发现（白盒分析）**：
    *   **拒绝方向（Refusal Direction）**：利用前人研究，提取模型内部表征中区分“有害”与“良性”请求的特定向量方向。
    *   **模式方向（Schema Direction）**：定义为“模式化工具规范（JSON 格式）”与“扁平化文本规范”之间隐藏层状态的差异向量。
    *   **发现**：模式化格式会诱导出一个与拒绝方向**负相关**的表征偏移，从而在模型内部削弱了拒绝信号，导致模型更倾向于执行工具而非拒绝请求。
*   **SafeKeep 防御机制**：
    *   **核心思想**：将“安全判断”与“工具执行”在推理阶段进行解耦。
    *   **安全判断阶段**：将复杂的 JSON/Schema 工具规范转换为**扁平化的纯文本描述**（去除嵌套、类型声明等），让模型在此背景下评估用户请求是否安全。
    *   **执行控制阶段**：如果判断为安全，则将请求转发给原始 Agent 流程（使用原始 Schema 格式以保证工具调用精度）；如果判断为有害，则拦截执行并生成拒绝回复。

### 3. 实验设计
*   **数据集/场景**：
    *   **AgentHarm**：包含 11 类危害类别的智能体任务，用于评估对直接有害请求的拒绝能力。
    *   **InjecAgent**：评估对间接提示注入（Observation-level Prompt Injection）的防御能力。
*   **对比模型**：Llama 3.1-8B-Instruct, Qwen 2.5-7B (文中注为 Qwen3-8B), Gemini 1.5 Flash, GPT-4o mini。
*   **对比方法**：
    *   **Base**：无保护的原始 Agent。
    *   **SafeJudge**：同样采用两阶段判断，但判断阶段仍使用原始 Schema 格式（用于证明格式转换的必要性）。
    *   **SafePrompt**：在系统提示词中加入安全指令。
    *   **SafeHarbor**：一种基于记忆增强的最新 Agent 防御基准。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因**：SafeKeep 是一种**推理时（Inference-time）**防御框架，不需要对模型进行微调或参数更新。白盒分析部分涉及对 7B/8B 规模模型的激活值提取，这类操作通常在单张消费级或数据中心级 GPU（如 A100 或 RTX 4090）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流模型（跨越开源与闭源）、2 个权威安全基准、数百个有害/良性测试对。
*   **充分性**：
    *   **消融实验**：逐一拆解了 Agent 输入的各个组件，证明了工具规范（Tool Specifications）是主要诱因。
    *   **因果验证**：通过**激活干预（Activation Steering）**，手动抵消“模式方向”的偏移，成功恢复了模型的拒绝行为，从因果层面证实了理论的正确性。
    *   **客观性**：对比了多种防御策略，并同时关注了安全指标（拒绝率、ASR）和功能指标（任务成功率 Acc），确保防御不会损害 Agent 的正常功能。

### 6. 论文的主要结论与发现
*   **格式是元凶**：导致 Agent 安全性下降的主要原因不是工具的语义内容，而是其**模式化（Schema-formatted）的呈现格式**。
*   **表征干扰**：JSON 等结构化格式在训练中与“执行动作”强绑定，这在推理时会产生一种强大的“执行诱导”，在隐藏层表征中抵消了对齐产生的拒绝信号。
*   **解耦有效性**：通过简单的格式转换（扁平化文本）即可在不改变模型参数的情况下，大幅找回模型原有的安全防御能力。

### 7. 优点
*   **机制深刻**：不仅提出了方法，还从模型内部表征的角度解释了“为什么 Agent 会变坏”，具有很强的理论支撑。
*   **即插即用**：SafeKeep 不需要微调模型，不依赖特定架构，对闭源模型（通过 API）同样有效。
*   **性能平衡**：在显著提升安全性的同时，Agent 处理正常任务的准确率（Acc）反而有所提升（因为减少了误触发）。

### 8. 不足与局限
*   **推理开销**：SafeKeep 需要额外的安全判断步骤，虽然使用了较小的上下文，但仍会增加一定的推理延迟和 Token 消耗。
*   **依赖模型自身能力**：该方法依赖于模型本身具备一定的安全对齐基础。如果基础模型本身未经过良好对齐，SafeKeep 的效果可能会打折扣。
*   **复杂场景覆盖**：对于极其复杂的工具组合或多轮对话中的隐蔽攻击，单一阶段的安全判断可能仍存在漏网之鱼。

（完）
