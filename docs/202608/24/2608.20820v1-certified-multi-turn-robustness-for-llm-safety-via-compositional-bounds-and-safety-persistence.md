---
title: Certified Multi-Turn Robustness for LLM Safety via Compositional Bounds and Safety Persistence
title_zh: 通过组合界限与安全性持久性实现大语言模型安全性的认证多轮鲁棒性
authors: "Yang Liu, Bin Chong, Wenkai Yang, Shuai Zhang, Yancheng Chen, Feiyu Han, GuoZhen, Cheng Zhang, Huaibing Xie, Changze Lv, Shihan Dou, Pluto Zhou"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.20820v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对多轮越狱攻击的认证鲁棒性
tldr: 针对大语言模型在多轮对话中易受渐进式越狱攻击的问题，本文提出了多轮认证鲁棒性（MTCR）框架。该框架利用状态对抗马尔可夫决策过程建模对话安全性，通过嵌入空间模态分解和安全持久性机制，解决了传统方法中认证边界随轮数指数级衰减的问题。实验证明，该方法在多种攻击下能提供更紧致的认证边界，有效提升了多轮对话的安全性保障。
selection_source: fresh_fetch
motivation: 现有的认证鲁棒性方法主要局限于单轮输入，在应对多轮越狱攻击时，其认证边界会随对话轮数增加而呈指数级迅速恶化。
method: 提出了MTCR框架，利用嵌入空间模态分解进行组合认证，并引入安全持久性参数来改善边界衰减率并建立信息论上界。
result: 在六种大语言模型上的实验结果显示，该方法提供的认证安全概率显著优于朴素组合方法，且实测安全性始终高于理论下界。
conclusion: 该研究为多轮对话系统的安全性提供了严谨的理论框架和高效的认证算法，有效提升了模型抵御复杂多轮攻击的能力。
---

## 摘要
大语言模型（LLMs）容易受到多轮越狱攻击的影响，这类攻击会逐步操纵对话上下文。现有的认证鲁棒性方法局限于单轮输入；简单的多轮组合会导致界限随轮数增加而呈指数级退化。我们提出了多轮认证鲁棒性（MTCR）框架，该框架通过状态对抗马尔可夫决策过程（State-Adversarial MDPs）对对话安全性进行建模，并将 $k$ 轮认证鲁棒性定义为 $k$ 个对抗轮次中的最差情况安全性概率。MTCR 包含：(i) 通过嵌入空间模态分解实现的组合认证，其产生的认证下界比简单乘法更紧致；(ii) $(α,β)$-安全性持久性，将退化率从 $\underline{p}^{k}$ 优化至 $β^k$（其中 $β> \underline{p}$），并提供可解释的视界估计；(iii) 确立紧致性的匹配信息论上界；以及 (iv) 结合这些结果的统一算法。在 $ε$-有界攻击和 Crescendo 式攻击下对六种 LLM 进行的实验证实，经验安全性始终高于认证界限。

## Abstract
Large language models (LLMs) are vulnerable to multi-turn jailbreak attacks that progressively manipulate conversation context. Existing certified robustness methods are limited to single-turn inputs; naive multi-turn composition yields bounds that degrade exponentially in the number of turns. We introduce Multi-Turn Certified Robustness (MTCR), a framework that models conversational safety via State-Adversarial MDPs and defines $k$-turn certified robustness as the worst-case safety probability across $k$ adversarial turns. MTCR comprises: (i) compositional certification via embedding-space mode decomposition, yielding tighter certified lower bounds than naive multiplication; (ii) $(α,β)$-safety persistence, improving the degradation rate from $\underline{p}^{k}$ to $β^k$ (with $β> \underline{p}$) and yielding interpretable horizon estimates; (iii) matching information-theoretic upper bounds establishing tightness; and (iv) a unified algorithm combining these results. Experiments on six LLMs under $ε$-bounded and Crescendo-style attacks confirm that empirical safety consistently exceeds the certified bounds.

---

## 论文详细总结（自动生成）

这是一份关于论文《Certified Multi-Turn Robustness for LLM Safety via Compositional Bounds and Safety Persistence》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**大语言模型（LLM）在多轮对话场景下的安全性认证**。
*   **研究动机**：虽然单轮对话的防御已有进展，但多轮越狱攻击（如 Crescendo 攻击）通过渐进式引导，能以极高成功率绕过安全机制。
*   **技术瓶颈**：现有的“认证鲁棒性”（Certified Robustness）方法主要针对单轮输入。若简单地将单轮认证概率 $p$ 进行乘法组合，其安全性下界会随轮数 $k$ 呈指数级衰减（即 $p^k$），导致长对话的安全性评估迅速失效（趋近于 0）。
*   **核心目标**：建立一套理论框架，在多轮对抗环境下提供更紧致、非指数级迅速崩溃的安全性认证下界。

### 2. 方法论：MTCR 框架
论文提出了**多轮认证鲁棒性（MTCR）**框架，其核心思想是将对话建模为**状态对抗马尔可夫决策过程（SA-MDP）**。

*   **关键技术细节**：
    1.  **组合认证（Compositional Certification）**：利用嵌入空间的“模态分解”（Mode Decomposition）。将对话状态空间划分为多个模态（Modes），分别认证“模态内安全性”和“模态间转换安全性”。通过动态规划寻找最差情况下的模态轨迹，从而获得比全局朴素乘法更紧致的界限。
    2.  **$(\alpha, \beta)$-安全性持久性（Safety Persistence）**：定义了一个结构化属性，即在对抗扰动下，安全边际（Safety Margin）以概率 $\beta$ 保持至少 $(1-\alpha)$ 的比例。这允许将认证退化率从 $p^k$ 优化为 $\beta^k$（通常 $\beta > p$），并支持线性化的视界估计（Horizon Estimation）。
    3.  **信息论上界**：证明了在非重叠模态分解下，该组合界限是紧致的（Tight），即达到了理论上的最优认证强度。
*   **算法流程**：
    *   **阶段 1**：通过随机平滑（Randomized Smoothing）认证各模态内的安全性，并估计持久性参数。
    *   **阶段 2**：认证模态间的转换概率。
    *   **阶段 3**：利用动态规划计算 $k$ 轮内的最差轨迹概率。
    *   **阶段 4**：结合持久性界限与组合界限，输出最终的认证下界。

### 3. 实验设计
*   **实验场景与数据集**：
    *   使用 **AdvBench** 危害性提示词库，涵盖暴力、非法活动、仇恨言论三大类（共 150 个提示词）。
    *   使用 **ShareGPT** 风格的 500 条安全多轮对话数据进行模态发现（聚类）。
*   **Benchmark 与对比方法**：
    *   **模型**：涵盖 6 种主流 LLM，包括开源（LLaMA-2-7B, Vicuna-7B, Llama-3.2-3B, Qwen2.5-7B）和闭源 API（GPT-4o, Claude-3.5-Sonnet）。
    *   **对比基准**：朴素乘法界限（Naive $p^k$）、乐观参考值（Mult. ref.）、仅持久性界限、仅组合界限。
    *   **攻击模型**：静态 $\epsilon$-球扰动攻击和自适应的 **Crescendo 式攻击**。

### 4. 资源与算力
*   **硬件环境**：开源模型运行在单张 **NVIDIA A100 (40GB)** GPU 上。
*   **时间成本**：文中明确指出，对单个模型在四个不同视界（$k=5, 10, 15, 20$）进行完整认证大约需要 **2.5 小时**。
*   **API 成本**：闭源模型通过 OpenAI 和 Anthropic 的 API 访问，涉及相应的调用延迟和费用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 6 种不同架构和对齐强度的模型。
    *   设置了不同的对话轮数 $k \in \{5, 10, 15, 20\}$。
    *   进行了消融实验（模态粒度 $M$、扰动预算 $\epsilon$、不同安全检测器）。
    *   每组配置进行 100 次试验，并计算了 95% 置信区间。
*   **充分性评价**：实验设计较为充分且客观。通过对比静态攻击与自适应攻击，验证了认证边界的保守性；通过消融实验证明了 MTCR 各组件（组合+持久性）结合的必要性。

### 6. 主要结论与发现
*   **认证有效性**：在所有测试模型和轮数中，**经验安全性始终高于 MTCR 提供的认证下界**，证明了理论的正确性。
*   **界限紧致性**：MTCR 提供的认证概率比朴素乘法方法高出 **1.2 到 1.6 倍**（在 $k=5$ 时），且随轮数增加的衰减速度更慢。
*   **模型差异**：闭源模型（GPT-4o, Claude-3.5）的认证安全性显著高于开源模型，反映了其更强的底层安全对齐和持久性。
*   **视界估计**：利用持久性参数可以有效估计模型在保持特定安全水平下能支持的最大对话轮数。

### 7. 优点
*   **理论突破**：首次为多轮对话安全性提供了严谨的认证框架，解决了长序列认证概率崩溃的问题。
*   **结构化建模**：通过 SA-MDP 和模态分解，捕捉了对话状态空间的几何特征，而非将其视为黑盒。
*   **实用性**：算法复杂度随轮数 $k$ 线性增长，且能作为预部署审计工具，指导设定最大对话长度策略。

### 8. 不足与局限
*   **扰动模型限制**：目前的正式认证仅适用于 **$\epsilon$-有界扰动**（如字符级编辑距离），对于纯语义层面的高级攻击（不涉及字符修改）虽有经验上的防御效果，但缺乏形式化证明。
*   **模态依赖性**：模态分解依赖于启发式的聚类方法，模态的数量 $M$ 和重叠度 $\kappa$ 会显著影响界限的紧致性。
*   **状态覆盖风险**：模态是从安全对话中学习的，可能无法完全覆盖对抗攻击可能引导至的极端异常状态空间。
*   **判定二元化**：安全性判定基于二元分类器（Safe/Unsafe），无法处理连续的危害程度评分。

（完）
