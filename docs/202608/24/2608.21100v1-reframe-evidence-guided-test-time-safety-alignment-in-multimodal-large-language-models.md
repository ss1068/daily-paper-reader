---
title: "ReFrame: Evidence-Guided Test-Time Safety Alignment in Multimodal Large Language Models"
title_zh: ReFrame：多模态大语言模型中证据引导的测试时安全对齐
authors: "Wenzheng Jiang, Xuankun Rong, Yuanzhao Zhai, Dawei Feng, Huaimin Wang"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21100v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 多模态大语言模型的测试时安全对齐
tldr: 针对多模态大语言模型（MLLM）面临的越狱攻击、安全意识缺失及过度敏感等挑战，本文提出了ReFrame框架。该框架无需重新训练或访问模型内部状态，通过在测试阶段重构输入来实现安全对齐。ReFrame利用两个轻量级代理生成风险与效用证据，并将其转化为安全代理提示词和图像路由决策。实验证明，该方法在提升安全性的同时，有效保留了模型的任务处理能力。
selection_source: fresh_fetch
motivation: 现有的多模态安全对齐方法通常需要重新训练或访问模型内部信息，难以应用于已部署的闭源模型。
method: 提出ReFrame框架，通过证据生成代理和重写路由代理，在测试时将原始输入重构为安全的代理提示词。
result: 在多个模型和基准测试中，ReFrame显著增强了防御越狱的能力，减少了过度敏感拒绝，并保持了多模态效用。
conclusion: ReFrame为闭源多模态模型提供了一种高效、无需训练且通用的测试时安全对齐解决方案。
---

## 摘要
虽然多模态大语言模型（MLLMs）将模型能力扩展到了文本之外，但它们也使安全对齐变得日益具有挑战性。多模态安全对齐方法必须解决跨模态越狱、安全意识失效以及过度敏感的拒绝等问题。然而，现有方法通常依赖于重新训练或内部状态检查，这限制了它们在已部署的闭源 MLLM 上的适用性，从而激发了对测试时安全对齐的研究。我们分析了这一设定并识别出两个关键障碍：效用主导（utility dominance）和推理惯性（reasoning inertia），它们导致模型忽视潜在风险或遵循恶意的推理轨迹。在这些见解的指导下，我们提出了 ReFrame，这是一个无需训练的多模态输入重构框架，其中两个智能体共享一个轻量级的本地部署 MLLM：证据生成智能体构建互补的风险和效用证据，而重写与路由智能体在调用下游 MLLM 之前将其转换为安全的代理提示词和图像路由决策，且无需修改下游模型或访问其内部信息。在多个 MLLM 和基准测试上的实验表明，ReFrame 在保持多模态效用的同时，提高了越狱防御、安全意识并减少了过度敏感。

## Abstract
While multimodal large language models (MLLMs) extend model capabilities beyond text, they also make safety alignment increasingly challenging. Multimodal safety alignment methods must address cross-modal jailbreaks, safety-awareness failures, and over-sensitive refusals. However, existing methods often rely on retraining or internal-state inspection, limiting their applicability to deployed closed-source MLLMs and motivating test-time safety alignment. We analyze this setting and identify two key obstacles, utility dominance and reasoning inertia, which cause models to overlook latent risks or follow malicious reasoning trajectories. Guided by these insights, we propose ReFrame, a training-free multimodal input reframing framework where two agents share a lightweight locally deployed MLLM: the evidence-generation agent constructs complementary risk and utility evidence, and the rewrite-and-routing agent converts it into a safe proxy prompt and image-routing decision before calling the downstream MLLM, without modifying it or accessing its internal information. Experiments across multiple MLLMs and benchmarks show that ReFrame improves jailbreak defense, safety awareness, and oversensitivity reduction while preserving multimodal utility.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReFrame: Evidence-Guided Test-Time Safety Alignment in Multimodal Large Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着多模态大语言模型（MLLM）的广泛应用，其安全性问题日益凸显。现有的安全对齐方法（如监督微调 SFT 或强化学习 RLHF）存在以下局限：
*   **高昂成本与不可访问性**：重新训练模型成本极高，且对于闭源模型（如 GPT-4o）无法进行内部参数调整。
*   **多模态特有风险**：包括跨模态越狱攻击（利用图像绕过文本审查）、安全意识失效（模型无法识别潜在风险）以及过度敏感（误拒安全请求）。
*   **两大认知障碍**：作者识别出模型在测试时的两个关键问题：
    *   **效用主导（Utility Dominance）**：模型过于关注完成用户指令，而忽视了隐藏的风险。
    *   **推理惯性（Reasoning Inertia）**：模型倾向于沿着用户提供的恶意逻辑或诱导性提示进行推理。

**ReFrame** 的核心目标是提出一种**无需训练、适用于黑盒模型**的测试时（Test-time）安全对齐框架，通过重构输入来消除风险。

### 2. 论文提出的方法论
ReFrame 采用“证据引导的输入重构”策略，利用一个轻量级的本地 MLLM 作为代理，在输入到达下游主模型前进行处理。其核心流程包括：

*   **证据生成智能体（Evidence-Generation Agent）**：
    *   **风险证据（Risk Evidence）**：分析输入（文本+图像）中违反安全准则的具体点，识别潜在的越狱意图。
    *   **效用证据（Utility Evidence）**：识别用户请求中合理的、无害的任务核心，确保防御不会误伤正常功能。
*   **重写与路由智能体（Rewrite-and-Routing Agent）**：
    *   **安全代理提示词（Safe Proxy Prompt）**：基于上述证据，将原始可能带有恶意诱导的提示词重写为中性、客观且安全的指令，打破“推理惯性”。
    *   **图像路由（Image Routing）**：判断图像是否为风险来源。如果图像包含违规内容（如色情、暴力或越狱字符），则在调用下游模型时屏蔽图像，仅传递重写后的文本。
*   **核心思想**：通过将“检测”转化为“重构”，将复杂的安全判断转化为模型易于理解的证据，从而引导下游模型做出正确的安全响应。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **安全性（Jailbreak）**：使用 **MM-SafetyBench**，包含 13 个场景的非法行为请求。
    *   **安全意识（Safety Awareness）**：使用 **VL-Guard**，评估模型区分安全与不安全内容的能力。
    *   **多模态效用（Utility）**：使用 **X-VLM**，确保防御不会损害模型在正常任务（如图像描述、VQA）上的表现。
    *   **过度敏感（Over-sensitivity）**：构建了专门的测试集，包含看似危险实则安全的“良性诱导”查询。
*   **对比模型**：
    *   **下游模型**：包括闭源模型（GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro）和开源模型（LLaVA-v1.5, InternVL2-26B）。
    *   **基线方法**：System Prompt（系统提示词）、Self-Correction（自我修正）等。

### 4. 资源与算力
*   **算力消耗**：论文未详细列出总的 GPU 小时数，但由于该方法是**无需训练（Training-free）**的，其主要开销在于推理阶段。
*   **代理模型**：实验中使用了轻量级的本地模型（如 **LLaVA-v1.5-7B** 或 **InternVL2-8B**）作为代理智能体。
*   **硬件环境**：通常此类实验在 A100 或 H100 GPU 上运行，以支持多个 MLLM 的并发推理。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流 MLLM（3 个闭源，2 个开源），在 4 个不同维度的基准测试上进行了评估。
*   **消融实验**：详细测试了“仅风险证据”、“仅效用证据”、“无图像路由”等变体，证明了各组件的必要性。
*   **客观性**：通过引入“过度敏感”测试，客观地展示了防御手段在安全性与可用性之间的平衡，避免了盲目追求高防御率而导致模型变“笨”的问题。

### 6. 主要结论与发现
*   **防御效果显著**：ReFrame 在 MM-SafetyBench 上的防御成功率显著优于传统的系统提示词方法，尤其在应对跨模态越狱时表现突出。
*   **缓解过度敏感**：通过“效用证据”的引入，ReFrame 有效减少了模型对良性请求的误拒，解决了现有防御方法容易“草木皆兵”的问题。
*   **通用性强**：作为一种插件式的测试时框架，它无需访问下游模型的权重，能够直接提升 GPT-4o 等闭源模型的安全性。
*   **打破惯性**：实验证明，重写提示词比单纯的检测拦截更能引导模型产生符合安全规范的回答。

### 7. 优点
*   **黑盒适用性**：对闭源模型极其友好，是当前部署环境下最务实的方案之一。
*   **双向平衡**：不仅关注安全性（Risk），还通过效用证据（Utility）保护了模型的任务处理能力。
*   **轻量化**：使用较小的本地模型即可引导强大的远程模型，实现了“小模型管大模型”的安全架构。
*   **逻辑清晰**：对“效用主导”和“推理惯性”的理论分析为后续研究提供了有价值的视角。

### 8. 不足与局限
*   **推理延迟**：由于在调用下游模型前增加了一个代理模型的推理步骤，会不可避免地增加端到端的响应延迟。
*   **代理模型依赖**：ReFrame 的效果高度依赖于本地代理模型的能力。如果代理模型本身被攻破或无法识别某些复杂风险，防御将失效。
*   **多轮对话挑战**：论文主要集中在单轮图文对话，在长对话或复杂交互场景下的持久安全性尚待进一步验证。
*   **成本增加**：虽然无需训练，但每次请求需要两次模型调用（一次代理，一次下游），增加了推理成本。

（完）
