---
title: "StepGuard: Learning Step-Level Guardrails with Scalable Supervision and Safety-Utility Balancing"
title_zh: StepGuard：通过可扩展监督与安全-效用平衡学习步级护栏
authors: "Zhijie Zheng, Yu Li, Chen Qian, Yuqian Fu, Yanwei Fu, Lu Sheng, Jing Shao, Dongrui Liu"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.24777v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对大语言模型代理的步级安全护栏
tldr: 针对LLM智能体在工具调用中的安全风险，本文提出StepGuard，一种步级（step-level）护栏模型。通过StepGen自动数据引擎生成对比样本，并利用Balance-GRPO算法动态平衡安全与效用，StepGuard能在动作执行前进行有效审计。实验显示其在降低攻击成功率的同时保持了极高的任务效用，性能媲美顶级闭源模型。
selection_source: fresh_fetch
motivation: 现有的LLM护栏多关注完整轨迹评估，缺乏对工具调用过程中单步动作的预执行监控。
method: 提出StepGen数据引擎生成安全与不安全动作对，并采用Balance-GRPO算法在训练中动态平衡安全防御与功能效用。
result: "StepGuard在开源护栏模型中准确率最高，将攻击成功率降低了77.3%，而任务效用仅下降2.8%。"
conclusion: StepGuard证明了通过步级监控和平衡训练可以有效提升LLM智能体的安全性，且不显著损害其执行任务的能力。
---

## 摘要
基于大语言模型（LLM）的智能体可以通过工具调用与外部环境交互，但这种能力也引入了安全风险，如文件修改、信息泄露和未经授权的操作。现有的护栏通常评估已完成的轨迹，而对步级操作的执行前监控研究较少。我们提出了 StepGuard，这是一种步级护栏模型，可以审计已完成的智能体轨迹，并在工具操作执行前对其进行检查。为了训练 StepGuard，我们引入了 StepGen，这是一个自动数据引擎，能够在相同上下文下针对风险步骤生成具有不同操作的安全和不安全轨迹。为了进一步减少过度防御和防御不足，我们提出了 Balance-GRPO，它根据观察到的准确率动态平衡安全和不安全操作之间的学习。实验表明，StepGuard 在开源权重护栏模型中达到了最高的平均准确率，性能与 GPT-5.4 相当。当用于保护 AgentDojo 和 AgentDyn 上的智能体时，StepGuard 相对于无护栏设置将平均攻击成功率降低了 77.3%，而平均效用仅下降了 2.8 个百分点。

## Abstract
LLM-based agents can interact with external environments through tool invocation, but this capability also introduces security risks such as file modification, information leakage, and unauthorized actions. Existing guardrails often evaluate completed trajectories, leaving pre-execution monitoring of step-level actions underexplored. We propose StepGuard, a step-level guard model that can audit completed agent trajectories and check tool actions before they are executed. To train StepGuard, we introduce StepGen, an automatic data engine that generates safe and unsafe trajectories with the same context but different actions at the risky step. To further reduce over-defense and under-defense, we propose Balance-GRPO, which dynamically balances learning between safe and unsafe actions based on their observed accuracy. Experiments show that StepGuard achieves the highest average accuracy among open-weight guard models, with performance comparable to GPT-5.4. When used to guard agents on AgentDojo and AgentDyn, StepGuard reduces mean attack success rate by 77.3% relative to the no-guard setting, while mean utility drops by only 2.8 percentage points.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **StepGuard** 的智能体安全护栏系统，旨在解决大语言模型（LLM）智能体在调用工具时的安全风险。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 智能体从简单的对话转向能够操作工具（如修改文件、发送邮件、执行交易）的复杂任务，其带来的现实世界风险显著增加。
*   **核心挑战**：
    1.  **监控粒度不足**：现有护栏多针对完整轨迹进行事后审计，缺乏在动作执行前的“步级（Step-level）”实时监控。
    2.  **数据稀缺**：高质量、带有细粒度标注的智能体不安全行为数据难以大规模获取。
    3.  **防御偏差**：护栏模型往往在“过度防御”（牺牲效用）和“防御不足”（牺牲安全）之间难以平衡。
*   **整体含义**：StepGuard 通过自动化的数据生成和动态平衡的强化学习，实现了一个既能拦截危险动作又能保持智能体任务执行能力的步级护栏。

### 2. 方法论
*   **StepGen 数据引擎**：
    *   **核心思想**：生成“前缀对齐（Prefix-Aligned）”的对比轨迹组。
    *   **流程**：首先构建一个包含“风险锚点（Risk Anchor）”的不安全轨迹；然后保持锚点前的上下文不变，分支生成“拒绝执行”和“安全替代”两种安全路径；最后生成一个独立的良性工具使用轨迹。
    *   **作用**：这种设计能让模型精准识别导致风险的关键决策点，并学会区分工具本身的敏感性与实际操作的安全性。
*   **Balance-GRPO 算法**：
    *   **核心思想**：基于 DeepSeek 提出的 GRPO 算法，引入动态权重调整机制。
    *   **技术细节**：在训练过程中，算法会实时监测模型在“安全样本”和“不安全样本”上的准确率差异（Accuracy Gap）。
    *   **动态平衡**：如果模型表现出过度防御（安全样本准确率低），则增加安全样本的训练权重；反之亦然。通过重加权优势函数（Advantage），在不改变原始奖励函数的情况下校准安全与效用的平衡。

### 3. 实验设计
*   **静态评估（Static Evaluation）**：
    *   **轨迹级**：ATBench, R-Judge, ASSE Security。
    *   **步级**：TS-Bench-Dojo, TS-Bench-Harm。
*   **动态评估（Guarded-Agent Evaluation）**：将护栏部署在真实的智能体运行循环中，测试其对 AgentDojo、AgentDyn 和 AgentHarm 等环境的影响。
*   **对比方法**：
    *   **通用模型**：GPT-5.4, Qwen3.5-397B。
    *   **内容护栏**：LlamaGuard3, Qwen3-Guard。
    *   **智能体专用护栏**：AgentDoG, ShieldAgent, Safiron, TS-Guard。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA H200 GPU**。
*   **训练时长**：
    *   **冷启动 SFT 阶段**：约 0.5 小时。
    *   **RL（Balance-GRPO）阶段**：约 1.5 小时。
    *   **总计**：约 8 个 GPU 小时（4台机位共计2小时）。
*   **模型规模**：基于 Qwen3-4B-Instruct 进行微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个静态基准测试和 3 个动态运行环境。
*   **消融实验**：详细分析了 StepGen 的组件（前缀监督、良性工具复用）以及 Balance-GRPO 的有效性。
*   **泛化性测试**：在未见过的风险源（Held-out risk sources）和人类验证的数据集（NVIDIA Nemotron-AIQ）上进行了验证。
*   **客观性**：实验对比了从 4B 到 397B 不同规模的模型，并多次重复实验以测量方差，结果显示 StepGuard 在小参数量下达到了极具竞争力的表现，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能卓越**：StepGuard（4B）在步级安全判断上优于所有开源护栏，性能可比肩 GPT-5.4。
*   **安全-效用平衡**：在动态环境中，StepGuard 将平均攻击成功率（ASR）降低了 **77.3%**，而任务效用（Utility）仅下降了 **2.8%**。
*   **防御偏差校正**：Balance-GRPO 成功将安全与不安全动作之间的准确率差距从 13.0 缩小到 8.0，显著提升了智能体在受保护状态下的任务完成率。

### 7. 优点
*   **粒度精细**：专注于“执行前”的步级监控，比事后审计更具实用价值。
*   **数据质量高**：StepGen 解决了安全数据难以获取的问题，且对比学习的设计有助于模型理解深层逻辑而非简单的关键词匹配。
*   **训练高效**：通过 Balance-GRPO 实现了自动化的平衡调节，无需繁琐的手动调参。

### 8. 不足与局限
*   **合成数据偏差**：StepGen 依赖于教师模型（如 GLM-5.1）生成数据，可能继承其偏见或覆盖范围限制。
*   **复杂场景挑战**：在高度对抗性的 AgentHarm 环境中，安全与效用的权衡依然严峻，模型在拦截恶意任务时仍会误伤部分复杂任务。
*   **规范性风险识别较弱**：错误分析显示，模型对“学术诚信”（如代交作业）等非技术性、制度性违规的识别能力弱于对“技术性攻击”（如注入攻击）的识别。
*   **推理开销**：虽然模型较小，但在每一步工具调用前增加一次推理仍会带来额外的延迟（约 600ms）。

（完）
