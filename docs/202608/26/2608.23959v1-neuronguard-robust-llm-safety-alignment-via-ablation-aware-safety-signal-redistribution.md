---
title: "NeuronGuard: Robust LLM Safety Alignment via Ablation-Aware Safety Signal Redistribution"
title_zh: NeuronGuard：通过消融感知的安全信号重分布实现鲁棒的大语言模型安全对齐
authors: "Anjun Gao, Yueyang Quan, Yufei Xia, Zhuqing Liu, Minghong Fang"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.23959v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过安全信号重新分布防御越狱和神经元级攻击
tldr: 针对大语言模型安全对齐易受越狱和神经元剪枝攻击的问题，本文提出 NeuronGuard。该方法通过在微调阶段将安全信号从稀疏神经元重新分布到更广泛的神经元集合中，结合动态识别安全神经元、消融感知拒绝机制及梯度投影策略，显著增强了模型鲁棒性，在保持任务性能的同时实现了近乎零的攻击成功率。
selection_source: fresh_fetch
motivation: 现有的安全对齐机制因安全信息过度集中在少数神经元而容易被越狱或神经元级攻击破解。
method: 通过动态线性分类器识别关键神经元，并在强制神经元消融的情况下训练模型保持拒绝行为，同时利用梯度投影保护下游任务性能。
result: 在多种模型和攻击场景（包括多模态和白盒攻击）下，该方法实现了近乎为零的攻击成功率，且不损害模型原有的任务准确度。
conclusion: NeuronGuard 通过安全信号的去中心化分布，为大语言模型提供了一种能够同时抵御提示词攻击和结构性攻击的强力防御方案。
---

## 摘要
大语言模型（LLMs）中的安全对齐在面对日益增多的攻击手段时仍然显得十分脆弱。越狱攻击通过精心设计的提示词绕过安全机制，而神经元级攻击则在部署后直接剪枝关键安全神经元。这两类攻击都利用了一个共同的弱点：安全相关的信息集中在稀疏的神经元子集中。我们提出了 NeuronGuard，这是一种微调阶段的防御方法，通过将安全信号重新分布到更广泛的神经元集合中，同时增强 LLM 对这两类攻击的抵御能力。NeuronGuard 通过定期更新的逐层线性分类器动态识别关键安全神经元，在刻意的神经元消融下强制执行拒绝行为，并应用 KL 散度正则化以保持分布一致性。一种随机梯度投影策略通过解决防御目标与任务目标之间的冲突，保留了下游任务的效用。我们提供了正式的理论保证，证明 NeuronGuard 严格降低了攻击成功率（ASR）的上界；在三种 LLM、六种最先进的攻击策略以及多模态设置下的实验证实，该方法在保持任务准确性的同时实现了接近零的 ASR，即使面对白盒自适应对手也是如此。

## Abstract
Safety alignment in large language models (LLMs) remains brittle against a growing spectrum of attacks. Jailbreak attacks bypass safety mechanisms through crafted prompts, while neuron-level attacks directly prune safety-critical neurons post-deployment. Both exploit a common weakness: safety-relevant information concentrates in a sparse neuron subset. We present NeuronGuard, a fine-tuning-stage defense that simultaneously hardens LLMs against both attack classes by redistributing safety signals across a broader set of neurons. NeuronGuard dynamically identifies safety-critical neurons via periodically refreshed per-layer linear classifiers, forces refusal behavior under deliberate neuron ablation, and applies KL-divergence regularization for distributional consistency. A randomized gradient projection strategy preserves downstream task utility by resolving conflicts between the defense and task objectives. We provide a formal guarantee that NeuronGuard strictly reduces the attack success rate (ASR) upper bound, and experiments across three LLMs, six state-of-the-art attack strategies, and multimodal settings confirm near-zero ASR while maintaining task accuracy, including against white-box adaptive adversaries.

---

## 论文详细总结（自动生成）

以下是对论文《NeuronGuard: Robust LLM Safety Alignment via Ablation-Aware Safety Signal Redistribution》的结构化深度总结：

### 1. 核心问题与整体含义
论文针对大语言模型（LLM）安全对齐的**脆弱性**提出了解决方案。研究动机源于一个关键观察：现有的安全对齐（如RLHF）往往将安全相关的信息过度集中在模型内部极少数的“安全神经元”中。
*   **双重威胁**：这种高度集中的特性使得模型容易受到两类攻击：一是**越狱攻击**（Jailbreak Attacks），通过提示词绕过安全层；二是**神经元级攻击**（如NeuroStrike），通过直接剪枝或抑制这些稀疏的安全神经元来失效安全机制。
*   **核心目标**：NeuronGuard 旨在通过在微调阶段将安全信号从少数神经元**重新分布（Redistribution）**到更广泛的神经元群体中，实现“去中心化”的安全对齐，从而同时抵御输入层和参数层的攻击。

### 2. 方法论
NeuronGuard 包含三个耦合的阶段，在模型微调过程中运行：

*   **阶段 1：动态识别安全神经元**
    *   在每一层拟合轻量级的线性分类器，用于区分有害请求和无害请求的激活特征。
    *   根据分类器权重识别出贡献最大的前 $\rho$ 比例的神经元作为“安全关键神经元”。
    *   **动态更新**：每隔 $N$ 个步长重新训练分类器，以追踪随权重演化而转移的安全信号。
*   **阶段 2：消融感知的鲁棒安全优化**
    *   **消融训练**：在训练时故意将识别出的安全神经元激活值清零（消融），并强制模型在这些神经元缺失的情况下依然产生拒绝回复。这迫使梯度流向其他神经元，实现信号重分布。
    *   **KL 散度正则化**：在消融前后的输出分布之间施加 KL 散度约束，确保模型行为的一致性，防止训练崩溃。
*   **阶段 3：随机梯度投影解决冲突**
    *   当安全防御目标梯度与下游任务目标梯度发生冲突（余弦相似度为负）时，采用随机投影策略。
    *   随机选择一个目标作为基准，消除另一个目标中与之冲突的分量，从而在增强鲁棒性的同时不损害模型的任务效用（Utility）。

### 3. 实验设计
*   **模型**：Llama-3.1-8B-Instruct, Qwen2.5-7B-Instruct, Falcon3-7B-Instruct。
*   **任务数据集**：SST2（情感分析）、AG-News（新闻分类）、CoLA（语言能力）、GSM8K（数学推理）。
*   **攻击基准（Benchmark）**：
    *   **生成/优化类越狱**：PAIR, TAP, Puzzler, GCG, AutoDAN。
    *   **神经元级攻击**：NeuroStrike。
*   **对比方法**：
    *   提示词防御：Perplexity Filter, SmoothLLM, GradSafe。
    *   模型级防御：CAT, LED, SafeNeuron。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H100 GPU**（每台 94GB 显存）。
*   **训练细节**：采用 LoRA 微调（r=32, alpha=64），学习率为 $10^{-5}$，在包含 2000 个样本的任务数据集上训练 10 个 epoch。
*   **开销**：论文指出 NeuronGuard 的计算开销与普通微调在同一数量级，且在推理阶段**零额外开销**。

### 5. 实验数量与充分性
实验设计非常充分且具有高度客观性：
*   **多维度评估**：涵盖了 3 种主流模型、4 种下游任务、6 种最先进的攻击策略。
*   **消融实验**：详细验证了动态识别、消融训练、KL 正则化和梯度投影各组件的必要性。
*   **自适应攻击测试**：针对已知防御机制的“白盒”对手，设计了迭代剪枝（IP）和非线性规避（NE）两种自适应攻击，证明了防御的韧性。
*   **多模态扩展**：在 Qwen2.5-VL 上进行了图像-文本多模态场景的验证。
*   **统计显著性**：所有结果均为 10 个不同随机种子的平均值。

### 6. 主要结论与发现
*   **极高的防御成功率**：NeuronGuard 将几乎所有攻击的成功率（ASR）降低到了**接近 0**（例如在 Qwen 上 PAIR 攻击 ASR 为 0.00）。
*   **效用保持**：在 GSM8K 等复杂任务上，模型准确率（ACC）与无防御微调相比几乎没有下降，显著优于 SmoothLLM 等会损害性能的防御。
*   **信号重分布的有效性**：实验证明，静态识别神经元（如 SafeNeuron）会随训练失效，而动态重分布能有效建立冗余的安全路径。

### 7. 优点
*   **根源治理**：不只是在输入端打补丁，而是通过改变模型内部表征结构来解决对齐脆弱性。
*   **理论保证**：提供了形式化的理论证明，推导了 ASR 上界的缩减与消融覆盖率、KL 强度之间的关系。
*   **推理友好**：防御逻辑固化在参数中，推理时不需要额外的过滤或多次采样，适合延迟敏感的应用。

### 8. 不足与局限
*   **微调阶段限制**：该方法必须在微调阶段实施，意味着它假设防御者拥有对训练流程和权重的完全控制权。对于已部署且无法重新训练的黑盒模型无效。
*   **安全探针依赖**：需要一小部分（约 500-750 个）有害和无害的样本作为探针来训练线性分类器，虽然需求量小，但仍依赖于安全数据集的质量。
*   **极端参数敏感性**：虽然实验显示对超参数 $\rho$ 和 $N$ 具有一定的鲁棒性，但在极大规模模型或极特定领域任务上的最优参数配置可能需要额外调优。

（完）
