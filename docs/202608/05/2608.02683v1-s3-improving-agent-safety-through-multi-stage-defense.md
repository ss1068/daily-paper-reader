---
title: "$S^3$: Improving Agent Safety through Multi-Stage Defense"
title_zh: $S^3$：通过多阶段防御提升智能体安全性
authors: "Zibo Xiao, Haoyu Wang, Jun Sun"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02683v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对大模型智能体的多阶段防御，以减轻工作流中的风险
tldr: 针对大语言模型智能体在多阶段工作流中面临的安全风险传播及现有防御手段孤立的问题，本文提出了 $S^3$ 多阶段防御框架。该框架引入了“阶段特定安全技能”的统一抽象，通过自动化流水线将现有设计转化为可复用的组件，并由防御智能体进行编排。实验表明，$S^3$ 在 Multi-Stage Risk Benchmark (MSRB) 上显著提升了安全性和实用性，为构建可信智能体提供了可扩展的基础。
selection_source: fresh_fetch
motivation: 现有 LLM 智能体安全防御方法仅针对孤立阶段，难以应对多阶段工作流中风险的产生与传播。
method: 提出了阶段特定安全技能的统一抽象及 $S^3$ 框架，利用防御智能体编排这些技能以实现全流程风险检测与缓解。
result: 实验结果显示 $S^3$ 在安全有效性和实用性保持方面均优于现有的最先进基准方法。
conclusion: 阶段特定安全技能为构建具有韧性且可信的智能体系统提供了一个可扩展且可组合的有效方案。
---

## 摘要
大语言模型（LLM）智能体依赖于包含记忆、规划和工具执行等阶段的多阶段智能体工作流来完成复杂任务。然而，风险可能在不同阶段出现，跨步骤传播，并变得难以检测和缓解。现有的安全方法仅保护孤立的阶段且难以集成，导致智能体在整个工作流中缺乏全面的保护。为了解决这些局限性，我们引入了“阶段特定安全技能”（Stage-Specific Safety Skills），这是一种统一的抽象，将异构的安全设计表示为具有明确阶段语义的可重用且可组合的组件。我们进一步开发了一个自动化转换流水线，将现有的安全设计转换为可重用的安全技能，并建立了一个社区驱动的安全技能库。基于这一抽象，我们提出了 $S^3$，这是一个多阶段防御框架，其中防御智能体编排阶段特定的安全技能，以便在整个智能体工作流中进行风险检测和缓解。我们还构建了多阶段风险基准（MSRB），用于评估工作流各阶段的代表性风险。实验结果表明，$S^3$ 在安全有效性和效用保持方面均持续优于代表性的最先进基准。这些结果证明了阶段特定安全技能作为构建韧性且可信智能体系统的可扩展且可组合基础的潜力。

## Abstract
Large Language Model (LLM) agents rely on multi-stage agentic workflows, with stages such as memory, planning, and tool execution, to accomplish complex tasks. However, risks may emerge at different stages, propagate across steps, and become difficult to detect and mitigate. Existing safety methods protect only isolated stages and are difficult to integrate, leaving agents without comprehensive protection throughout the workflow. To address these limitations, we introduce Stage-Specific Safety Skills, a unified abstraction that represents heterogeneous safety designs as reusable and composable components with explicit stage semantics. We further develop an automated transformation pipeline that converts existing safety designs into reusable safety skills and establish a community-driven safety skill library. Building on this abstraction, we propose $S^3$, a multi-stage defense framework in which a guard agent orchestrates stage-specific safety skills for risk detection and mitigation throughout the agentic workflow. We also construct the Multi-Stage Risk Benchmark (MSRB) to evaluate representative risks across workflow stages. Experimental results show that $S^3$ consistently outperforms representative state-of-the-art baselines in both safety effectiveness and utility preservation. These results demonstrate the potential of stage-specific safety skills as a scalable and composable foundation for building resilient and trustworthy agent systems.

---

## 论文详细总结（自动生成）

以下是对论文《S³: Improving Agent Safety through Multi-Stage Defense》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）智能体通常遵循多阶段工作流（输入→记忆→规划→工具选择→执行→观察）。现有的安全防御方法大多是“孤立的”，仅针对特定阶段（如输入过滤或输出对齐），难以应对跨阶段传播的风险。
*   **核心问题**：
    1.  **风险多样性**：不同阶段面临不同风险（如记忆投毒、工具执行偏差）。
    2.  **风险传播**：早期阶段的微小偏差可能在后期演变成严重的安全事故。
    3.  **集成困难**：现有的安全设计方案异构且难以统一编排。
*   **整体含义**：论文旨在建立一个统一的防御框架，通过将安全能力抽象为“技能”，实现对智能体全生命周期的动态监控与风险拦截。

### 2. 方法论
$S^3$ 框架的核心由三个部分组成：
*   **阶段特定安全技能（Stage-Specific Safety Skills）**：
    *   将安全能力抽象为可执行组件，包含目标阶段、检查信息、所需资源、检查程序和缓解策略。
    *   **自动化转换流水线**：通过“方法建模 -> 技能编写 -> 技能测试 -> 技能优化”四个步骤，利用 LLM 自动将现有的安全论文或文档转化为标准化的安全技能。
*   **$S^3$ 防御框架**：
    *   **防御智能体（Guard Agent）**：独立于主智能体运行，负责根据当前阶段调用相应的安全技能。
    *   **分层触发机制**：采用“阶段触发 -> 规则过滤 -> 防御智能体决策”的三层结构，平衡防御强度与运行开销。
    *   **恢复模块（Recovery Module）**：在检测到风险后，不只是简单拦截，而是尝试修复状态（如删除工具观察中的恶意指令），以保证良性任务能继续完成。

### 3. 实验设计
*   **基准测试（MSRB）**：构建了“多阶段风险基准”（Multi-Stage Risk Benchmark），包含 9 个任务类别、675 个实例。
*   **风险类型**：涵盖 6 种代表性风险：直接提示词注入（DPI）、记忆投毒、后门攻击（Backdoor PoT）、工具选择扰动、环境执行扰动、观察结果注入（OPI）。
*   **对比方法**：
    *   **无防御状态**（No Guard）。
    *   **单阶段技能**（如 A-MemGuard, AgentSpec, AIR 等）。
    *   **多阶段框架基准**：LlamaFirewall 和 SafeHarness。
*   **评估指标**：攻击成功率（ASR）、恢复触发率（RTR）、任务完成率（TCR）和任务安全完成率（TSR）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **DeepSeek-V4-Pro** 作为主智能体和转换智能体的基础模型，使用 **DeepSeek-V4-Flash** 作为防御智能体以评估跨模型泛化性。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法主要基于推理侧的防御编排和 Prompt Engineering，而非大规模模型微调，其算力消耗主要体现在推理延迟和 API 调用成本上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 6 种风险类型进行了全面的有效性对比。
    *   进行了**消融实验**，验证了恢复模块、规则过滤和后期引导对性能和效率的贡献。
    *   进行了**忠实度（Fidelity）测试**，验证了自动化转换后的技能是否保留了原始设计的逻辑（ disagreement rate < 10%）。
*   **充分性评价**：实验设计较为充分，不仅关注安全性（ASR），还关注了实用性（TCR），并对比了最新的工业级和学术界基准，体现了客观性和公平性。

### 6. 主要结论与发现
*   **全流程防御的必要性**：单阶段防御方法在面对非目标阶段的风险时几乎无效（ASR 居高不下）。
*   **$S^3$ 的优越性**：$S^3$ 在所有风险场景下均实现了极低的 ASR（接近 0%），同时保持了极高的任务安全完成率（TSR），显著优于 LlamaFirewall 等现有框架。
*   **恢复模块的价值**：相比于直接阻断任务，恢复模块能有效修复受损的工作流，使智能体在排除干扰后完成原始良性目标。
*   **效率优化**：分层触发机制显著减少了防御智能体的调用次数，降低了运行开销。

### 7. 优点
*   **统一抽象**：首次提出了“安全技能”的标准化抽象，解决了异构安全工具难以集成的问题。
*   **自动化能力**：通过转换流水线，能够快速吸收社区现有的安全研究成果，具有良好的可扩展性。
*   **实用性平衡**：不仅防范攻击，还通过恢复机制保护了智能体的业务连续性。
*   **开源贡献**：建立了社区驱动的安全技能库，促进了生态建设。

### 8. 不足与局限
*   **规则依赖**：部分安全技能（如 AgentSpec）仍依赖于人工定义的规则，在面对未知或高度复杂的攻击模式时可能存在覆盖不足。
*   **推理延迟**：虽然有优化机制，但引入额外的防御智能体和多轮检查不可避免地增加了端到端的响应时间。
*   **环境恢复限制**：对于某些不可逆的环境破坏（如物理模拟中的爆炸或永久性数据删除），恢复模块的修复能力可能受限。
*   **实验场景**：目前主要在具身智能体（Embodied AI）工具集上测试，在更广泛的 Web 自动化或软件开发智能体场景下的表现仍需进一步验证。

（完）
