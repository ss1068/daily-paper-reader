---
title: "LatentGuard: Efficient and Inspectable Latent Reasoning for LLM Safeguards"
title_zh: LatentGuard：面向大语言模型安全护栏的高效且可检查的隐式推理
authors: "Zhinan Liu, Jie Li, Mingyu Kang, Jiayi Ji"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03838v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 高效且可检查的LLM安全防护框架
tldr: 针对基于推理的LLM安全护栏模型推理成本高且难以检查的问题，本文提出LatentGuard框架。该框架通过阶段性课程学习将文本推理压缩至连续潜空间，实现从潜状态直接预测安全结论，显著降低了推理开销。同时，引入辅助解码器以按需生成审计信息，兼顾了效率与可解释性。实验证明其在提升安全检测性能的同时，大幅减少了生成的Token数量。
selection_source: fresh_fetch
motivation: 现有的基于推理的安全护栏模型因生成显式推理过程而导致推理成本过高，且缺乏高效的检查机制。
method: 提出LatentGuard框架，利用阶段性课程学习将推理过程压缩为潜状态，并结合辅助解码器实现按需审计。
result: LatentGuard-8B在F1分数上优于GuardReasoner-8B，且将关键路径的推理Token数从268.56大幅降至1.60。
conclusion: 该研究为构建高效、可检查且可部署的LLM安全护栏提供了一种将连续潜推理与按需审计相结合的有效路径。
---

## 摘要
基于推理的护栏模型提升了大语言模型的安全性，但为每次交互解码显式理由（rationales）使得部署成本高昂。尽管隐式推理方法通过将推理转移到连续状态来减少 Token 生成，但它们在安全审核方面的研究仍不充分，且缺乏用于部署的检查接口。在本文中，我们提出了 LatentGuard，这是一个高效且可检查的安全护栏框架，它将连续隐式推理引入护栏模型。LatentGuard 使用阶段式课程学习，将与任务对齐的文本理由逐步压缩为紧凑的隐式状态，从而能够直接从连续表示中预测安全判定。为了保持可检查性，一个隔离的辅助解码器根据需求生成紧凑的审计产物，使理由生成脱离标准推理路径。实验表明，相比 GuardReasoner-8B，LatentGuard-8B 将平均加权 F1 分数从 83.95 提升至 84.91，同时将关键路径推理成本从 268.56 个生成的理由 Token 降低至 1.60 个隐式推理 Token。其审计解码器达到了 85.75 的审计效用得分，展示了通往可部署大语言模型安全护栏的高效且可检查的路径。

## Abstract
Reasoning-based guard models improve LLM safeguards, but decoding explicit rationales for every interaction makes them costly to deploy. Although latent-reasoning methods reduce token generation by moving reasoning into continuous states, they remain underexplored for safety moderation and lack an inspection interface for deployment. In this paper, we propose LatentGuard, an efficient and inspectable safeguard framework that brings continuous latent reasoning to guard models. LatentGuard uses a staged curriculum to progressively compress task-aligned textual rationales into compact latent states, enabling safety verdicts to be predicted directly from continuous representations. To preserve inspectability, an isolated auxiliary decoder generates compact audit artifacts on demand, keeping rationale generation off the standard inference path. Experiments show that LatentGuard-8B improves mean weighted F1 from 83.95 to 84.91 over GuardReasoner-8B, while reducing critical-path reasoning cost from 268.56 generated rationale tokens to 1.60 latent reasoning tokens. Its audit decoder achieves an audit utility score of 85.75, demonstrating an efficient and inspectable path toward deployable LLM safeguards.

---

## 论文详细总结（自动生成）

这是一份关于论文《LatentGuard: Efficient and Inspectable Latent Reasoning for LLM Safeguards》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着大语言模型（LLM）的普及，安全护栏（Safeguards）成为核心组件。现有的护栏模型面临“效率-质量-可检查性”的权衡：
    *   **分类式护栏**：速度快但准确率较低，且无法提供决策依据。
    *   **推理式护栏**：通过生成显式理由（Rationale）提高准确率和透明度，但每次交互都要生成大量 Token，导致高延迟和高计算成本。
*   **核心问题**：如何在保持推理式护栏的高准确率和可检查性的同时，大幅降低推理成本和延迟？

### 2. 方法论
LatentGuard 提出了一个将推理过程从“文本空间”转移到“连续潜空间（Latent Space）”的框架：
*   **核心思想**：解耦安全预测与理由生成。在标准推理路径上使用隐式潜推理（Latent Reasoning）直接输出结果；仅在需要审计时，通过辅助解码器将潜状态还原为人类可读的文本。
*   **关键技术细节**：
    1.  **阶段式课程学习（Staged Curriculum）**：为了稳定训练，模型分阶段将文本理由压缩为潜状态。例如，先将请求风险推理转为潜状态，再逐步将拒绝理由和响应风险推理也转为潜状态。
    2.  **自适应潜预算（Adaptive Latent Budget）**：推理时，模型根据置信度动态决定使用多少个潜 Token（最多 $K_{max}$ 个）。简单样本可能只需 1 个潜 Token 即可得出结论，复杂样本则使用更多。
    3.  **隔离式审计解码器（Isolated Audit Decoder）**：训练一个轻量级解码器（如 Llama-3.2-1B），利用停止梯度（Stop-gradient）技术从护栏模型的潜状态中提取信息并生成审计产物。这种设计确保了审计功能不会干扰主模型的推理逻辑。

### 3. 实验设计
*   **数据集**：训练集采用 GuardReasonerTrain（包含约 12.7 万条样本，涵盖 WildGuard、Aegis、BeaverTails 等）。
*   **Benchmark**：在 9 个主流安全基准上进行评估，包括 ToxicChat、HarmBench、OpenAI Moderation、Aegis SafetyTest、SimpleSafetyTests、SafeRLHF、BeaverTails、XSTest 和 WildGuard Test。
*   **对比方法**：
    *   **推理式**：GuardReasoner (1B, 3B, 8B)。
    *   **分类式**：LlamaGuard3-8B、LlamaGuard4-12B、WildGuard、RobloxGuard-1.0。

### 4. 资源与算力
*   **硬件环境**：使用 4 张 GPU 进行训练（具体型号未标明，但提到支持 bf16 混合精度）。
*   **训练时长**：
    *   1B 模型：约 6 小时（24 GPU 小时）。
    *   3B 模型：约 15 小时（60 GPU 小时）。
    *   8B 模型：约 30 小时（120 GPU 小时）。
*   **参数配置**：使用 LoRA 技术进行微调（Rank=128, Alpha=32）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 8B 的三个模型尺度，并在 9 个不同的测试集上进行了验证。
*   **消融实验**：针对“课程学习的必要性”、“潜状态的作用”以及“审计解码器的输入条件”进行了详细的消融研究。
*   **客观性与公平性**：
    *   使用了 3 个随机种子取平均值。
    *   引入了 **LLM-as-a-judge**（使用 Qwen3.5-27B）和 **人工验证**（386 条样本）来评估审计产物的质量，确保了评估的多维度和客观性。

### 6. 主要结论与发现
*   **性能提升**：LatentGuard-8B 的平均加权 F1 分数达到 84.91，优于基准模型 GuardReasoner-8B (83.95)。
*   **效率飞跃**：在 8B 规模下，将推理 Token 数从 268.56 个（显式文本）减少到 1.60 个（潜 Token），实现了 **168 倍的推理成本削减**；延迟从 0.792s 降至 0.089s，提速约 **8.9 倍**。
*   **审计有效性**：审计解码器在不影响主路径效率的前提下，能够生成高质量的审计理由（AUS 得分为 85.75），且与人工判断的一致性超过 94%。

### 7. 优点
*   **高效性**：通过潜空间推理解决了 Chain-of-Thought (CoT) 带来的高延迟问题。
*   **可检查性**：创新的“按需审计”模式，既保留了透明度，又避免了日常运行的冗余开销。
*   **训练稳定性**：提出的阶段式课程学习有效缓解了直接从文本跳转到潜空间训练时的不稳定性。

### 8. 不足与局限
*   **审计成本**：虽然标准路径极快，但一旦开启审计模式，仍需额外的解码计算。
*   **忠实度风险**：生成的审计理由是“事后解释”，虽然与决策相关，但并不等同于模型内部计算逻辑的完全忠实还原。
*   **应用范围**：目前主要集中在文本安全，尚未扩展到多模态或超长对话场景。
*   **算力细节**：未明确说明具体的 GPU 型号（如 A100 或 H100），对其他研究者预估成本略有不便。

（完）
