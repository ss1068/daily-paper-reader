---
title: "Moving the Safety Barrier: Dynamic Routing Adaptive Alignment Against White-Box Attacks"
title_zh: 移动安全屏障：抵御白盒攻击的动态路由自适应对齐
authors: "Shangze Li, Chuancheng Shi, Simiao Xie, Lingzhi He, Cheng Ji, Zifeng Cheng, Fei Shen, Chao Wu, Tat-Seng Chua"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.02674v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对白盒越狱攻击的动态路由防御
tldr: 针对大模型在白盒攻击下内部安全路由易被识别并破坏的问题，本文提出动态路由自适应对齐（DRAA）框架。该方法通过对比分析定位安全路由，并利用掩码技术诱导防御失效，进而构建失效感知偏好对来训练动态补偿路径。实验证明，DRAA能有效重构模型安全路径依赖，在保持通用能力的同时，显著提升了对抗路由级白盒攻击的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的静态安全防御机制在面对能够直接识别并破坏内部安全神经元或路径的白盒攻击时表现脆弱。
method: 通过对比安全与不安全样本的激活状态定位安全路由，并利用掩码诱导的失效案例构建偏好对，从而训练动态补偿路径。
result: 实验表明，DRAA显著增强了模型对抗路由级白盒攻击的鲁棒性，且未损害模型的通用性能。
conclusion: DRAA通过引入动态补偿机制，成功将静态安全屏障转变为动态防御体系，为提升大模型的白盒安全性提供了新思路。
---

## 摘要
随着大型基础模型（LFMs）在开放环境中的广泛部署，安全威胁正从黑盒越狱转向白盒攻击，后者直接识别并破坏内部安全神经元或路由。然而，现有的安全防御通常依赖于静态安全单元或固定的拒绝路径，使得模型在针对性的路由级白盒攻击面前显得非常脆弱。为此，我们提出了动态路由自适应对齐（DRAA），这是一个引入动态补偿路由的框架，旨在当安全路由受损时保持稳健的拒绝行为。具体而言，我们首先通过对比安全和非安全校准样本之间的内部激活，识别并定位模型的安全路由。随后，DRAA 屏蔽该安全路由以诱导因果失效案例，并选择性地挖掘由此产生的防御失败，从而构建感知失效的偏好对。大量实验表明，DRAA 有效地重构了模型安全的底层路径依赖，在保持通用效能的同时，显著提高了抵御路由级白盒攻击的鲁棒性。

## Abstract
With the widespread deployment of large foundation models (LFMs) in open environments, safety threats are shifting from black-box jailbreaks toward white-box attacks that directly identify and disrupt internal safety neurons or routes. However, existing safety defenses often rely on static safety units or fixed refusal pathways, leaving models highly vulnerable to targeted route-level white-box attacks. For that, we propose dynamic routing adaptive alignment (DRAA), a framework that introduces dynamic compensatory routes to preserve robust refusal behavior when the safety route is compromised. Specifically, we first identify and localize the model's safety route by contrasting internal activations between safe and unsafe calibration samples. DRAA then masks this safety route to induce causal failure cases and selectively mines the resulting defense failures, thereby constructing failure-aware preference pairs. Extensive experiments demonstrate that DRAA effectively restructures the underlying pathway dependence of model safety, substantially improving robustness against route-level white-box attacks, while preserving general utility.

---

## 论文详细总结（自动生成）

这是一份关于论文《Moving the Safety Barrier: Dynamic Routing Adaptive Alignment Against White-Box Attacks》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLMs）的普及，安全防御正面临从“黑盒越狱”向“白盒结构破坏”的范式转移。
*   **核心问题**：现有的安全对齐方法（如 RLHF、SFT）往往将拒绝行为锚定在模型内部的一组**静态**神经元或因果路径上。
*   **研究动机**：在白盒场景下，攻击者可以精确定位并剪枝（Pruning）或干扰这些关键的安全路由，导致模型的防御机制瞬间崩溃。
*   **整体含义**：本文旨在打破“静态防御”的局限，提出一种**动态路由自适应对齐（DRAA）**框架，赋予模型在核心安全路径被封锁时，能够通过“绕路”寻找补偿路径来维持安全拒绝的能力。

### 2. 方法论：核心思想与关键技术
DRAA 的核心思想是**“遇阻绕行”**，通过模拟安全路径失效来训练冗余的防御机制。

*   **安全路由定位（Safety Route Localization）**：
    *   通过对比安全样本和不安全样本在模型内部的激活差异，利用**效应量（Effect Size, ES）**和**安全激活偏移（Safety Activation Shift, SAS）**两个指标，精确定位出负责拒绝行为的神经元集合 $S$。
*   **因果失效挖掘（Causal Failure Mining）**：
    *   主动屏蔽（置零）定位到的安全路由 $S$，观察模型行为。
    *   筛选出那些“原本能安全拒绝、但在路由屏蔽后变为违规输出”的样本，构建**失效感知偏好对**（即：输入相同，选择原模型的安全回复作为正例，选择屏蔽路由后的违规回复作为负例）。
*   **动态路由 DPO（DR-DPO）**：
    *   在偏好优化阶段，通过**梯度掩码（Gradient Masking）**技术锁定（冻结）原始安全路由 $S$ 的参数。
    *   强制模型在不受限的参数空间内学习新的补偿路径，以吸收失效感知偏好信号。
*   **推理阶段**：原始路由与新学习的补偿路由同时保持激活，形成多重冗余防御。

### 3. 实验设计
*   **数据集**：
    *   **安全评估**：StrongREJECT（核心测试集，包含 313 个有害提示词）。
    *   **通用能力**：ARC、GSM8K、TruthfulQA。
    *   **多模态安全**：VL-Question（图像嵌入有害指令）、NSFW 任务。
*   **模型基准（Backbones）**：
    *   **文本模型**：Qwen2.5 (1.5B 到 14B)、LLaMA-3.2 (1B 到 8B)、Gemma-7B、Phi-4、DeepSeek。
    *   **多模态模型**：Qwen2.5-VL-7B、LLaVA-1.5-7B。
*   **对比方法**：Original（原始指令微调版）、SN-Tune、RLHF-Safety、SafeNeuron（代表性的神经元级对齐方法）。
*   **攻击场景**：ORI（无剪枝）、ES 剪枝、SAS 剪枝、FULL（全路由剪枝）。

### 4. 资源与算力
*   **算力消耗**：文中提供了具体的效率数据。
    *   **定位耗时**：针对 1000 个样本进行 ES/SAS 定位仅需 **0.58 分钟**。
    *   **训练耗时**：DR-DPO 训练过程约为 **12.7 分钟**。
    *   **显存占用**：训练峰值显存为 **18.23 GB**。
*   **硬件型号**：文中未明确标注具体的 GPU 型号（如 A100 或 H100），但从显存占用和训练时长推断，该方法在单张消费级或企业级显卡上即可高效运行。
*   **推理开销**：由于采用 LoRA 适配器合并技术，推理时的参数量和速度与原始模型完全一致，**零额外推理开销**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 1B 到 14B 不同参数规模的 10 余种主流模型。
    *   进行了跨模态（文本+视觉）的验证。
    *   针对“未见过”的白盒攻击（如基于梯度的 GRAD、权重剪枝 WANDA、激活消融 ABLATE）进行了泛化性测试。
*   **充分性评价**：实验设计非常充分。不仅有量化的 ASR（攻击成功率）对比，还有消融实验（验证路由锁定和失效挖掘的必要性）、双重损伤诊断（验证补偿路径的真实功能）以及超参数敏感性分析。
*   **客观性**：评估时针对每个模型**重新定位**攻击目标，确保了对比的公平性（即不是攻击 DRAA 训练时已知的旧路由，而是攻击其对齐后的新路由）。

### 6. 主要结论与发现
*   **鲁棒性飞跃**：在最严苛的 FULL 剪枝攻击下，DRAA 将 Qwen2.5-7B 的攻击成功率（ASR）从原始模型的 279/313 降低至 **26/313**，显著优于 SafeNeuron 等基准。
*   **能力保持**：在提升安全性的同时，模型在 ARC、GSM8K 等基准上的得分稳中有升，证明了动态路由不会损害通用推理能力。
*   **多模态通用**：该方法同样适用于视觉语言模型，能有效抵御图像中嵌入的有害指令。
*   **结构冗余**：可视化分析证实，DRAA 确实在模型的中后层激发了原本处于潜伏状态的神经元，形成了功能性的冗余防御。

### 7. 优点与亮点
*   **防御范式创新**：从“加固屏障”转向“建立备用路径”，解决了白盒攻击下静态防御易被“定点清除”的痛点。
*   **失效感知训练**：利用因果干预（屏蔽路由）来生成训练数据，这种“从失败中学习”的方法比盲目增加安全样本更具针对性。
*   **部署友好**：训练快、显存低，且推理时无需额外的路由模块或计算量。

### 8. 不足与局限
*   **理论保证上限**：虽然实验证明了冗余性，但论文并未给出模型能承受多少轮连续剪枝攻击的理论上限。
*   **依赖定位精度**：防御效果高度依赖于第一步 ES/SAS 定位的准确性，如果初始定位不准，后续的失效挖掘可能偏离目标。
*   **特定任务重训**：当模型迁移到全新的安全领域（如特定行业的合规性）时，可能需要重新进行路由定位和 DR-DPO 训练。

（完）
