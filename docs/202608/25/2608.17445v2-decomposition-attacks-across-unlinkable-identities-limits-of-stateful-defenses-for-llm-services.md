---
title: "Decomposition Attacks Across Unlinkable Identities: Limits of Stateful Defenses for LLM Services"
title_zh: 跨不可关联身份的分解攻击：大语言模型服务中有状态防御的局限性
authors: "Bowen Sun, Zhengyue Zhao, Xiaogeng Liu, Yinzhi Cao, Chaowei Xiao"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17445v2"
tags: ["query:llm"]
score: 8.0
evidence: 大语言模型对抗分解攻击的有状态防御限制
tldr: 本研究探讨了针对大语言模型服务的分解攻击，即攻击者利用多个不可关联身份将有害任务拆分为看似无害的请求。研究证明，在身份无法关联时，有状态防御机制难以平衡安全与实用性。实验显示，现有策略在低误伤率下均无法有效阻止攻击，攻击成功率极高，揭示了当前防御手段的局限性。
selection_source: fresh_fetch
motivation: 旨在解决攻击者通过拆分任务和变换身份来规避大语言模型服务安全监测的问题。
method: 结合理论证明与大规模实验，评估了多种有状态防御策略在处理不可关联身份请求时的有效性。
result: "实验发现所有测试策略均无法在维持低误伤率的同时拦截攻击，攻击者在两次尝试内的成功率高达100%。"
conclusion: 仅靠内容监测不足以抵御分解攻击，必须引入身份关联、增加身份成本或控制输出使用等新机制。
---

## 摘要
大多数大语言模型服务使用无状态防御（仅根据当前请求进行判断）来拒绝有害任务。分解攻击利用了这一局限性，将有害任务拆分为多个单独可允许的请求，并组合它们的答案。因此，防御此类攻击需要一个能够综合考虑多个请求的有状态监控器。如果监控器能将属于同一个攻击者任务的所有请求归组，就能阻止攻击。然而，攻击者可以使用不可关联的身份并在其他地方组合答案，从而不留下可靠的归组信号。我们探讨在这种设定下是否仍能阻止分解攻击。对于不带重试的固定攻击策略，我们证明了可实现的安全性与效用之间的权衡完全取决于具有相同能力的良性请求是如何归组的。持久且可识别的组允许有效的防御；而新鲜且不可区分的组则不然。当攻击者可以重试并从“允许/拦截”决策中学习时，这种有效的运行平衡点就会消失：反馈揭示了哪些请求可以通过，但并未揭示拦截是否正确。对 91 个可执行任务和 11,393 个能力匹配的良性请求进行的实验支持了这些结果。在对这些请求设置 1% 的拒绝上限以及对无关背景流量设置 0.5% 的上限时，所有十种测试策略（包括一种具有精确请求-操作映射的特权策略）要么无法阻止攻击，要么超出了预算。在防御未见过的任务族上，一次尝试后的攻击成功率至少为 99%，两次尝试后为 100%。因此，有效的防御需要与归组相关的额外证据或机制，例如可靠的身份关联、获取新身份的成本或对答案使用的控制。

## Abstract
Most large language model services use stateless defenses, which judge only the current request, to refuse harmful tasks. Decomposition attacks exploit this limitation by splitting a harmful task into individually permissible requests and combining their answers. Defending against them therefore requires a stateful monitor that considers requests together. If it can group all requests for one attacker task, it can stop the attack. However, attackers can use unlinkable identities and combine answers elsewhere, leaving no reliable grouping signal. We ask whether decomposition attacks can still be stopped under this setting. For a fixed attack strategy without retries, we prove that the achievable security and utility tradeoff depends entirely on how benign requests for the same capabilities are grouped. Persistent, recognizable groups permit a useful defense; fresh, indistinguishable groups do not. When attackers can retry and learn from Allow/Block decisions, this useful operating point disappears: the feedback reveals what passes but not whether a block was correct. Experiments on 91 executable tasks and 11,393 capability-matched benign requests support these results. Under a 1% denial cap for these requests and a 0.5% cap for unrelated background traffic, all ten tested policies, including one privileged policy with an exact request-to-operation map, either fail to stop attacks or exceed the budget. On defense-unseen task families, attack success is at least 99% after one attempt and 100% after two. Effective defenses therefore require additional evidence or mechanisms tied to grouping, such as reliable identity linkage, costs for fresh identities, or control over answer use.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）服务在面对“分解攻击”（Decomposition Attacks）时的防御边界，特别是当攻击者利用多个不可关联的身份来规避有状态防御时的局限性。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：目前的 LLM 服务主要依赖“无状态防御”（仅检查当前请求），攻击者可以通过将一个有害任务拆分为多个看似无害的子任务（分解攻击）来绕过。
*   **核心挑战**：虽然“有状态防御”可以通过关联历史请求来识别攻击，但如果攻击者使用多个无法关联的身份（如不同账号、IP 等）提交请求，并在外部组合答案，防御者就无法建立有效的关联信号。
*   **研究问题**：在攻击者身份不可关联的现实威胁模型下，有状态防御是否还能在保持低误伤率（误拦截良性请求）的同时，有效阻止分解攻击？

### 2. 论文提出的方法论
*   **核心思想**：通过理论证明和实验验证，揭示安全性（攻击成功率 ASR）与实用性（良性请求拦截率 MCD/BBR）之间的权衡边界。
*   **关键概念**：
    *   **可关联组（Linkable Group）**：服务商能通过元数据关联的一组请求。
    *   **访问联盟（Access Coalition）**：实际上获取并组合答案的幕后实体（防御者不可见）。
*   **理论框架**：
    *   **理想防御者模型 ($\Pi_{ideal}$)**：赋予防御者完美的历史记录和精确的语义识别能力，以此推导防御的理论上限。
    *   **单次尝试边界**：证明了 ASR 与误伤率之间的线性关系，即 $ASR \approx 1 - B/D$（其中 $B$ 是预算，$D$ 是识别成本）。
    *   **重试与反馈机制**：推导了当攻击者可以根据“允许/拦截”结果进行重试和学习时，防御效果会迅速恶化，ASR 趋向于 1。

### 3. 实验设计
*   **数据集与场景**：
    *   构建了一个包含 **91 个可执行网络安全/编程任务** 的基准测试。
    *   包含 **365 个原子操作请求** 和 **11,393 个能力匹配的良性对照请求**（Matched Controls）。
    *   引入 **150 万条背景流量请求**（来自 WildChat, StackExchange 等）。
*   **Benchmark 特点**：所有任务必须通过自动化测试脚本验证是否真正“执行成功”，而非仅靠模型评估。
*   **对比方法**：测试了 10 种防御策略，包括：
    *   无状态防御（Lexical, Granite Guardian）。
    *   现有先进有状态防御（TwinGate, Brown-style 聚类, 累积摘要监控等）。
    *   特权参考（M9）：拥有精确请求-操作映射的理想化累加器。

### 4. 资源与算力
*   **模型使用**：
    *   使用 `Qwen3.5-35B-A3B`（去安全对齐版）作为攻击者生成计划。
    *   使用 `Qwen3.5-2B` 作为防御侧的判别器（Judge）。
    *   使用 `Granite Guardian 3.1 2B` 进行有害性评分。
*   **算力细节**：文中未明确给出具体的 GPU 型号和总训练时长，但提到生成了超过 **76 万次评分调用** 和 **120 万次 Granite 评分**，实验规模巨大，涉及大量的推理计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 82 个任务族，并在 11 个完全未见过的任务族上进行了迁移测试（Outer Cohort）。
    *   进行了 **64 次平衡重着色（Recoloring）** 实验以消除随机性偏差。
    *   模拟了不同重试次数（R=1, 2, 5, 16）和反馈学习过程。
*   **充分性与公平性**：实验设计非常严谨，通过“平衡重着色”确保攻击请求和良性请求在语义特征上是不可区分的，从而客观地测试“归组/关联”能力而非单纯的语义分类能力。

### 6. 主要结论与发现
*   **防御失效**：在 1% 的良性误伤上限内，所有测试的防御策略在面对不可关联身份的攻击时，ASR 均接近 100%。
*   **归组是关键**：防御的有效性完全取决于良性请求的归组方式。如果良性请求也表现为“新鲜、不可关联”的特征，防御者将无法在不误伤良性用户的情况下拦截攻击。
*   **反馈助长攻击**：攻击者只需利用简单的强化学习（如 Thompson Sampling），仅凭“允许/拦截”的 1 比特反馈，就能在极短时间内找到绕过防御的最优路径。
*   **语义识别的局限**：即使防御者拥有完美的语义识别能力（M9 策略），只要无法关联身份，依然无法阻止分解攻击。

### 7. 优点
*   **理论与实践结合**：不仅给出了严谨的数学证明，还通过大规模可执行实验验证了理论的正确性。
*   **现实的威胁模型**：考虑了“不可关联身份”这一极具挑战性且符合现实（如 Sybil 攻击）的场景。
*   **高质量基准**：构建了基于代码执行验证的分解攻击数据集，比纯文本评估更具说服力。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在网络安全和编程任务，尚未验证在其他有害领域（如虚假信息生成、生物危害）的普适性。
*   **防御手段覆盖**：研究主要关注基于请求内容的监控，未深入探讨基于支付凭证、信誉系统或工作量证明（PoW）等非内容维度的防御机制。
*   **模型依赖**：实验结果可能受到所选判别模型（如 Qwen-2B）性能的影响，虽然理论部分证明了这是通用限制。

（完）
