---
title: Text-Anchored Semantic Perturbations for Transferable Jailbreak Attacks on Multimodal Large Language Models
title_zh: 以文本为锚点的语义扰动：针对多模态大语言模型的可迁移越狱攻击
authors: "Wenyun Li, Guiping Cao, Xiangyuan Lan, Zheng Zhang"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22312v1"
tags: ["query:llm"]
score: 9.0
evidence: 多模态大语言模型的越狱攻击
tldr: 本研究针对多模态大语言模型（MLLM）在跨模态表示中安全对齐失效的问题，提出了文本锚定语义扰动攻击（TA-SPA）。该框架通过文本锚定语义分解（TASF）将跨模态语义与模态特有残差分离，并利用语义保持增强（SPA）在保持语义一致性的同时增加有害目标的样性。实验表明，TA-SPA在黑盒环境下具有极高的攻击成功率和跨模型迁移能力，甚至能突破商业模型的防御，揭示了MLLM在表示层面的安全隐患。
selection_source: fresh_fetch
motivation: MLLM在文本空间学习的安全对齐无法可靠地迁移到融合的跨模态表示中，使得模型容易受到潜在语义线索的越狱攻击。
method: 提出TA-SPA框架，结合文本锚定语义分解（TASF）分离跨模态语义因子，并利用语义保持增强（SPA）优化可迁移的语义扰动。
result: 实验显示该方法在黑盒攻击中表现优异，能有效迁移至商业MLLM，并在多种代表性防御措施下保持竞争力。
conclusion: 研究结果表明，除了输入层过滤外，还需要在表示层面进行安全对齐，以应对跨模态语义带来的安全挑战。
---

## 摘要
多模态大语言模型（MLLMs）在视觉-语言交互方面取得了显著进展，但其安全对齐在越狱攻击面前仍然十分脆弱。一个关键挑战在于，在文本空间中学习到的安全行为无法可靠地迁移到融合的跨模态表示中，使得多模态输入可以通过潜在的语义线索被利用。我们提出了以文本为锚点的语义扰动攻击（TA-SPA），这是一个在以文本为锚点的语义空间中优化可迁移扰动的黑盒越狱框架。TA-SPA 整合了以文本为锚点的语义因子分解（TASF）与保持语义的增强（SPA）；前者旨在促进跨模态语义因子与模态特定残差的分离，后者则在保持语义一致性的同时使有害目标锚点多样化。实验表明，该方法具有强大的攻击有效性，并能迁移到商业 MLLMs，在代表性防御手段下也表现出具有竞争力的性能。额外的对照实验和探测分析支持了预期的因子分解（尽管并不意味着完美的解耦），这激励了在输入级过滤之外进行表示级的安全对齐。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable progress in vision-language interaction, yet their safety alignment remains vulnerable to jailbreak attacks. A key challenge is that safety behavior learned in the textual space does not reliably transfer to fused cross-modal representations, leaving multimodal inputs exploitable through latent semantic cues. We propose Text-Anchored Semantic Perturbation Attack (TA-SPA), a black-box jailbreak framework that optimizes transferable perturbations in a text-anchored semantic space. TA-SPA integrates Text-Anchored Semantic Factorization (TASF), which encourages the separation of cross-modal semantic factors from modality-specific residuals, with Semantic-Preserving Augmentation (SPA), which diversifies harmful target anchors while preserving semantic consistency. Experiments show strong attack effectiveness and transfer to commercial MLLMs, with competitive performance under representative defenses. Additional controls and probing support the intended factorization without implying perfect disentanglement, motivating representation-level safety alignment beyond input-level filtering.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **TA-SPA**（Text-Anchored Semantic Perturbation Attack）的新型黑盒越狱攻击框架，旨在揭示多模态大语言模型（MLLM）在跨模态语义对齐上的安全漏洞。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：MLLM 的安全对齐通常是在**纯文本空间**完成的（如 RLHF、DPO），但模型在推理时处理的是**融合后的跨模态表示**。
*   **研究动机**：作者发现，文本空间学习到的安全准则无法完美迁移到视觉-语言融合空间。现有的多模态越狱攻击（如在像素空间或特征空间直接优化）往往容易过拟合于特定模型的视觉伪影或布局，导致**黑盒迁移性较差**，且容易被 OCR 过滤或图像清洗等防御手段拦截。
*   **整体含义**：论文试图证明，通过在“以文本为锚点的语义空间”中注入扰动，可以产生更具迁移性、更难被过滤的越狱信号。

### 2. 方法论：核心思想与技术细节
TA-SPA 框架由两个核心组件组成，旨在实现语义与模态残差的分离：

*   **文本锚定语义分解（TASF）**：
    *   利用预训练的 CLIP 编码器作为代理，将多模态表示分解为**语义因子（Semantic Factor）**和**残差因子（Residual Factor）**。
    *   **语义因子**捕捉跨模态不变的命题内容（即有害意图）；**残差因子**吸收与语义无关的模态特征（如纹理、光照、语法风格）。
    *   通过四种损失函数（对齐损失、差异损失、重构损失、CLIP一致性损失）训练轻量级分解模块。
*   **语义保持增强（SPA）**：
    *   为了防止攻击过拟合于单一的词汇模式，SPA 在保持语义因子不变的情况下，通过对残差因子施加 Dropout 和高斯噪声，生成多样化的有害目标锚点。
*   **优化流程**：
    *   使用辅助 LLM 生成有害查询的多种改写（Paraphrases）。
    *   采用**对比边缘损失（Contrastive Margin Loss）**进行优化：拉近对抗图像的语义表示与多个有害目标锚点的距离，同时推开与原始良性图像残差的距离。
    *   最终在像素空间生成 $\epsilon$ 约束下的扰动图像。

### 3. 实验设计
*   **数据集**：使用了三个主流有害评估基准：**AdvBench**、**MM-SafetyBench** 和 **VAJM**，涵盖了超过 6000 个有害请求。
*   **受测模型（Victim MLLMs）**：
    *   **开源模型**：LLaVA-7B、MiniGPT-4、InstructBLIP。
    *   **商业模型（黑盒迁移测试）**：GPT-4o、Gemini、ERNIE（文心一言）。
*   **对比方法（Baselines）**：
    *   **多模态攻击**：imgJP、FigStep、HADES、IDEATOR、MML。
    *   **纯文本攻击**：GCG、AutoDAN、PAIR、TAP。
*   **评估指标**：基于字典的攻击成功率（D-ASR）、基于 GPT-4 的攻击成功率（G-ASR）以及 Google Perspective API 的毒性得分。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及数量。
*   **效率信息**：
    *   TASF 模块的训练是离线的且收敛极快（约 8 个 Epoch）。
    *   在线生成单个对抗样本平均耗时约 **120 秒**，在对比方法中处于中等偏优水平，具有较好的实用性。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，涵盖了跨模型、跨数据集的广泛测试。
*   **消融实验**：对 TASF、目标改写、源排斥等组件进行了逐一验证，证明了各部分的必要性。
*   **防御测试**：在 VLGuard、ProEAT、AdaShield 和 BlueSuffix 四种代表性防御机制下进行了鲁棒性评估。
*   **客观性与公平性**：引入了**人类评估者**和**第三方 LLM（如 Qwen3, DeepSeek）**进行交叉验证（Judge Calibration），避免了仅依赖 GPT-4 评分可能带来的偏差。实验设置了严格的扰动预算（8/255），确保了对比的公平性。

### 6. 主要结论与发现
*   **攻击极具威胁**：TA-SPA 在多个基准上显著优于现有方法，尤其是在诱导模型产生实质性有害内容（G-ASR）方面表现突出。
*   **强大的迁移性**：该方法能有效突破 GPT-4o 和 Gemini 等商业模型的安全防线，证明了跨模态语义漏洞的普遍性。
*   **防御失效**：现有的基于输入过滤或提示词屏蔽的防御手段难以完全拦截 TA-SPA，因为攻击信号隐藏在模型必须处理的视觉语义特征中。
*   **表示层脆弱性**：实验支持了作者的假设，即 MLLM 的安全漏洞源于高层语义意图与底层模态伪影的纠缠。

### 7. 优点（亮点）
*   **视角独特**：从语义-残差分解的角度切入，避开了像素级过拟合的问题，提升了攻击的“深度”。
*   **黑盒友好**：不需要访问受害模型的梯度或参数，仅依靠公开的 CLIP 编码器即可完成优化。
*   **鲁棒性强**：通过 SPA 和多查询改写，增强了攻击对不同分词器和提示词模板的适应能力。

### 8. 不足与局限
*   **模态局限性**：目前仅在图像-文本多模态上进行了验证，尚未扩展到视频、音频或点云等其他模态。
*   **因果机制尚不明确**：虽然实验证明了分解的有效性，但语义因子与模型内部安全神经元之间的直接因果联系仍需进一步探索。
*   **依赖代理模型**：攻击效果在一定程度上受限于代理编码器（如 CLIP）与受害模型视觉编码器之间的相似性。

（完）
