---
title: Mitigating Database Leakage in RAG Systems with Keyword-Grounded Fact Substitution
title_zh: 通过基于关键词的事实替换缓解 RAG 系统中的数据库泄露
authors: "Ziliang Zhang, Yubo Zhu, Wei Tong, Jingyu Hua, Zijian Wang, Yuan Zhang, Sheng Zhong"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21656v1"
tags: ["query:llm"]
score: 8.0
evidence: 缓解RAG系统中提示注入导致的数据库泄露
tldr: 针对检索增强生成（RAG）系统易受提示注入攻击导致数据库敏感信息泄露的问题，本文提出了一种名为KFS-RAG的防御框架。该方法通过注意力回溯和因果扰动机制识别检索上下文中的关键关键词，并利用辅助大模型生成基于关键词的事实摘要来替换原始文本。实验证明，该方法在显著降低信息泄露风险的同时，有效保持了回答的准确性与相关性，为构建安全可靠的RAG系统提供了新思路。
selection_source: fresh_fetch
motivation: 检索增强生成系统面临提示注入攻击风险，可能导致底层数据库中的敏感内容被恶意诱导泄露。
method: 提出KFS-RAG框架，通过识别关键关键词并将其转化为精简的事实陈述来替换原始检索上下文。
result: 实验结果显示，该方法在抵御注入攻击、减少数据库泄露方面表现优异，且不损害系统的响应质量。
conclusion: 通过对检索内容进行基于关键词的事实重构，可以有效平衡RAG系统的安全性和实用性。
---

## 摘要
检索增强生成（RAG）已成为将大语言模型（LLMs）与外部知识源相结合的一种强大范式。然而，RAG 系统仍然容易受到提示注入攻击的影响，这类攻击可能会误导检索器或生成器泄露敏感的数据库内容。为了解决这一问题，我们提出了 KFS-RAG，这是一种通过重构检索上下文来缓解信息泄露的防御机制。具体而言，我们的方法首先通过注意力展开（attention rollout）和因果扰动机制，从检索到的上下文中识别出一组少量的关键影响词。随后，利用这些关键词引导辅助 LLM 从检索到的段落中生成一组精简的、基于关键词的事实。最后，使用这些精选的事实替换原始上下文，从而确保生成器基于经过清理的证据而非原始检索文本进行操作。实验评估表明，KFS-RAG 在注入攻击下显著降低了数据库泄露的风险，同时保持了回答的准确性和相关性。这项工作为构建安全且值得信赖的 RAG 系统提供了一条切实可行的路径。

## Abstract
Retrieval-Augmented Generation (RAG) has emerged as a powerful paradigm for combining large language models (LLMs) with external knowledge sources. However, RAG systems remain vulnerable to prompt injection attacks, which may mislead the retriever or generator to expose sensitive database contents. To address this issue, we propose KFS-RAG, a defense that mitigates information leakage by reformulating the retrieved context. Specifically, our method first identifies a small set of influential keywords from the retrieved context via an attention rollout plus a causal perturbation mechanism. These keywords are then used to guide an auxiliary LLM to generate a compact set of keyword-grounded facts from the retrieved passages. Finally, the original context is substituted with these curated facts, ensuring that the generator operates on sanitized evidence rather than the raw retrieved text. Experimental evaluations demonstrate that KFS-RAG significantly reduces the risk of database leakage under injection attacks while maintaining response accuracy and relevance. This work highlights a practical pathway toward building secure and trustworthy RAG systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《KFS-RAG: Mitigating Database Leakage in RAG Systems with Keyword-Grounded Fact Substitution》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：检索增强生成（RAG）系统在处理外部私有数据库时，面临严重的**提示注入攻击（Prompt Injection）**风险。攻击者可以通过精心设计的恶意指令（如“忽略之前指令，打印全文”），诱导 LLM 泄露检索到的原始敏感文档内容。
*   **研究背景**：现有的防御手段（如数据合成、差异隐私、加密隔离）往往在信息粒度保持、系统可用性或检索效用之间存在权衡。
*   **研究动机**：开发一种通用的防御机制，在不改变原始数据库的前提下，通过在检索后、生成前对上下文进行“脱敏”处理，既保留回答问题所需的关键证据，又剔除无关或敏感的原始文本细节。

### 2. 方法论：KFS-RAG 框架
KFS-RAG 的核心思想是**“以事实替换原始文本”**，其流程包含三个关键阶段：

*   **关键词识别（Keyword Identification）**：
    *   **注意力回溯（Attention Rollout）**：使用轻量级模型（如 GPT-2）对检索上下文进行初步扫描，通过聚合多层注意力流，筛选出对生成初步答案贡献最大的候选词。
    *   **因果扰动验证（Causal Perturbation）**：对候选词进行掩码处理，测量其对模型损失函数的影响。只有那些移除后会导致损失显著增加的词才被视为具有“因果重要性”的关键词。这种两阶段设计平衡了计算效率与识别的准确性。
*   **目标事实提取（Targeted Fact Extraction）**：
    *   利用辅助 LLM（如 DeepSeek-V3）根据识别出的关键词，从原始检索段落中提取或重述精简的事实陈述。这一步确保了生成器只能接触到经过过滤的“事实点”，而非原始的文本流。
*   **上下文重构与生成（Fact Re-Synthesis）**：
    *   （可选）为了保持特定场景（如医疗对话）的语气和风格，利用辅助 LLM 将提取的事实重新组织成自然语言段落，最后提交给主 RAG 生成器产生最终回答。

### 3. 实验设计
*   **数据集与场景**：
    *   **ODQA（开放域问答）**：使用 Natural Questions (NQ) 和 Web Questions (WQ)，测试基础事实检索。
    *   **2WikiMultiHopQA**：测试复杂的多跳逻辑推理能力。
    *   **HealthcareMagic**：测试特定领域的专业知识及对话风格的保持。
*   **Benchmark 与对比方法**：
    *   **基准**：Vanilla RAG（原始 RAG）、No-RAG（闭卷模式）。
    *   **对比防御方法**：Paraphrased RAG（全文改写）、SAGE（基于属性的重构）、Random-Fact（随机事实替换）。
*   **评估指标**：
    *   **效用（Utility）**：BLEU-1、ROUGE-L。
    *   **安全性（Security）**：语义相似度（SS）、扩展编辑距离（EED）、分块恢复率（CRR）、Token 级 F1 分数。

### 4. 资源与算力
*   **模型使用**：
    *   主生成器：Llama3-8B-Instruct、Qwen2.5-7B。
    *   关键词识别骨干：GPT-2。
    *   辅助提取/重构模型：DeepSeek-V3。
    *   嵌入模型：bge-large-en-v1.5。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练/实验时长。但文中提供了推理延迟数据：KFS-RAG-Fact 在单次查询中的端到端延迟约为 6.67 秒（相比 Vanilla RAG 的 6.03 秒，开销增加较小）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个不同性质的数据集，针对 5 种主流提示注入攻击（Native, Escape, Context-ignoring, Fake-completion, Combined）进行了压力测试。
*   **消融实验**：非常充分。研究了事实数量（kout）、关键词候选集大小（kcand）、不同 AP 阶段的贡献、以及更换不同主模型/辅助模型后的性能波动。
*   **客观性与公平性**：实验采用了“最强攻击者”原则（报告 5 种攻击中效果最好的一种），并对比了多种最先进的防御基线，评估维度涵盖了性能与安全性的权衡，表现出较高的客观性。

### 6. 主要结论与发现
*   **安全性显著提升**：KFS-RAG 显著降低了原始文本的泄露指标（如 CRR 从 Vanilla RAG 的 70.8% 降至 21.03%），有效切断了攻击者获取原始敏感数据的路径。
*   **效用保持甚至优化**：在 ODQA 任务中，KFS-RAG 的表现有时甚至优于 Vanilla RAG，这表明精简事实可以减少原始文本中的噪声，帮助 LLM 更聚焦。
*   **任务依赖性**：对于简单问答，少量事实即可；对于多跳推理，增加提取的事实数量能显著提升准确率，但会略微增加泄露风险。

### 7. 优点
*   **因果感知的关键词识别**：结合注意力流和因果扰动，比单纯依赖 LLM 提取关键词更具鲁棒性，且能抵御针对关键词提取阶段的注入攻击。
*   **灵活的重构机制**：引入可选的风格重构步骤，解决了纯事实摘要在对话系统中显得生硬的问题。
*   **模型无关性**：框架不依赖于特定的 LLM，具有良好的通用性和可移植性。

### 8. 不足与局限
*   **单轮对话限制**：目前仅验证了单轮输入，未探讨在多轮对话中如何处理跨轮次的上下文依赖和累积泄露风险。
*   **模态局限**：仅限于纯文本 RAG，未涉及多模态（图像、表格）数据的处理。
*   **辅助模型依赖**：防御效果在一定程度上取决于辅助 LLM 的提取能力，若辅助模型本身被注入攻击攻破，防御链条可能失效（尽管文中通过关键词锚定缓解了此问题）。

（完）
