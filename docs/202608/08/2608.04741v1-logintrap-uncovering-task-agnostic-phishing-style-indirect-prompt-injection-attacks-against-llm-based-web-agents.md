---
title: "LoginTrap: Uncovering Task-Agnostic Phishing-Style Indirect Prompt Injection Attacks against LLM-based Web Agents"
title_zh: LoginTrap：揭示针对基于 LLM 的 Web 智能体的任务无关、钓鱼式间接提示注入攻击
authors: "Longtao Guo, Zelin Zhang, Kaifeng Huang, Yang Shi"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04741v1"
tags: ["query:llm"]
score: 8.0
evidence: 间接提示注入攻击
tldr: 本研究针对基于大语言模型（LLM）的 Web 智能体，提出了一种名为 LoginTrap 的任务无关型间接提示注入攻击。该攻击通过操纵网页上下文，诱导智能体将登录视为完成任务的必要前提，从而将其引导至受控的钓鱼页面以窃取凭据。实验表明，LoginTrap 在多种 LLM 后端和智能体架构下均表现出极高的攻击成功率，揭示了 Web 智能体在身份验证边界存在的严重安全风险。
selection_source: fresh_fetch
motivation: 现有研究尚未充分探讨恶意网页内容是否能诱导 Web 智能体进行登录操作，进而导致端到端的隐私数据泄露。
method: LoginTrap 利用模糊测试启发的流程生成特定页面的间接注入指令，使登录看起来像是继续当前任务的合理前提。
result: "在多种 LLM 后端和智能体架构的测试中，LoginTrap 达到了 86% 的平均端到端攻击成功率，且对现有防御手段具有鲁棒性。"
conclusion: 该研究识别出诱导登录是 Web 智能体面临的系统性身份验证边界风险，强调了开发身份验证感知防御机制的紧迫性。
---

## 摘要
基于 LLM 的 Web 智能体通过观察网页并代表用户执行浏览器操作来自动化用户任务。由于这些智能体在真实的 Web 服务上运行，登录成为了一个敏感的身份验证边界，因为它涉及凭据和敏感信息。现有工作表明，恶意网页内容可以操纵 Web 智能体的行为，但尚未充分研究此类内容是否可以诱导登录并导致端到端的私有数据泄露。我们研究了这一攻击面，并提出了 LoginTrap，这是一种针对基于 LLM 的 Web 智能体的任务无关登录诱导攻击。LoginTrap 假设一个黑盒攻击者，在不知道用户任务或智能体内部机制的情况下，控制网页上下文和诱导的登录流程。在该威胁模型下，LoginTrap 利用网页上下文，通过受模糊测试启发的过程生成特定于页面的间接注入，使登录看起来像是继续任务的合理前提，并引导智能体进入受控的登录页面。我们在真实的 Web 智能体执行过程中对 LoginTrap 进行了全面分析。结果显示，LoginTrap 在不同 LLM 主干网络上的平均端到端攻击成功率达到 86%，并且在不同的智能体架构和防御措施下依然有效。这些发现将登录诱导识别为一种系统性的身份验证边界风险，并激励了对 Web 智能体身份验证感知防御的进一步研究。

## Abstract
LLM-based web agents automate user tasks by observing webpages and executing browser actions on behalf of users. As these agents operate on real web services, login becomes a sensitive authentication boundary because it involves credentials and sensitive information. Existing work shows that malicious webpage content can manipulate web agent actions, but it has not fully examined whether such content can induce login and cause end-to-end private data leakage. We study this attack surface and present LoginTrap, a task-agnostic login-inducing attack against LLM-based web agents. LoginTrap assumes a black box attacker that controls the webpage context and the induced login flow without knowing the user task or web agent internals. Under this threat model, LoginTrap uses webpage context to generate page-specific indirect injections through a fuzzing-inspired process, making login appear as a plausible prerequisite for continuing the task and guiding the agent to a controlled login page. We conduct a comprehensive analysis of LoginTrap across realistic web agent executions. The results show that LoginTrap reaches 86\% average end-to-end attack success across LLM backbones and remains effective across agent architectures and defenses. These findings identify login inducement as a systematic authentication boundary risk and motivate further research on authentication-aware defenses for web agents.

---

## 论文详细总结（自动生成）

这篇论文对基于大语言模型（LLM）的 Web 智能体在身份验证边界的安全风险进行了深入研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究恶意网页内容是否能在**不知道用户具体任务**且**不了解智能体内部机制**的情况下，诱导 Web 智能体执行登录操作，从而导致端到端的隐私数据泄露。
*   **研究动机**：Web 智能体（如 Browser-use, Skyvern 等）已能自动化执行复杂的网页任务。登录界面是处理凭据和私密信息的关键边界。现有研究多关注直接的指令篡改，而忽略了“诱导登录”这一隐蔽且通用的攻击路径。作者试图证明，攻击者可以通过操纵网页上下文，让智能体误以为“登录”是完成任何任务的必要前提。

### 2. 方法论：LoginTrap 攻击框架
LoginTrap 采用了一种**任务无关（Task-Agnostic）**且**钓鱼式**的间接提示注入方法：
*   **核心思想**：利用模糊测试（Fuzzing）启发的流程，根据原始网页内容自动生成能够诱导登录的注入语句，并将其嵌入克隆的网页中。
*   **关键技术流程**：
    1.  **环境构建**：克隆目标网页以保留原始视觉和文本上下文，添加一个包含诱导语句和伪造登录入口的弹窗。
    2.  **注入生成（模糊测试启发）**：
        *   **初始化**：利用 LLM 提取网页摘要，基于多种语义策略（如“登录以查看完整内容”）生成初始候选语句。
        *   **影子评估**：使用两个“影子 LLM”模拟智能体行为，评估候选语句是否能诱导智能体点击登录。
        *   **变异与优化**：借鉴蒙特卡洛树搜索（MCTS）的思想，对失败的候选语句进行交叉、扩展、改写或压缩，直到生成高成功率的注入。
    3.  **受控登录流**：一旦智能体点击伪造入口，会被引导至一个与原网页风格一致的受控登录页面，诱导其输入背景信息中的敏感数据。

### 3. 实验设计
*   **数据集**：基于 **Mind2Web** 真实网页基准，涵盖 5 个领域（旅游、服务、信息、购物、娱乐），共包含 80 个克隆网页和 1,175 个任务实例。
*   **对比对象（LLM 后端）**：GPT-4o, Gemini 1.5 Flash, Claude 3.5 Sonnet, DeepSeek-V3。
*   **对比对象（智能体架构）**：Browser-Use（轻量级工具调用）、LiteWebAgent（模块化 VLM 智能体）、Skyvern（多智能体工作流）。
*   **评估指标**：
    *   **LER (Login Entry Rate)**：登录入口点击率。
    *   **ASR (Attack Success Rate)**：端到端攻击成功率（即成功泄露隐私数据）。
    *   **PER (Page Exploitability Rate)**：页面可利用率（衡量页面在多次尝试下是否至少成功一次）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的本地 GPU 型号或训练时长。
*   **资源使用**：攻击生成过程主要依赖 **GPT-4o** 的 API 调用。实验环境是在本地部署的受控 Web 环境中进行的，通过自动化脚本驱动 Chrome 浏览器执行。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 1,000 次任务执行测试，覆盖了 4 种主流 LLM 和 3 种不同的智能体架构。
*   **充分性**：实验设计较为全面，不仅测试了不同模型，还探讨了攻击在不同智能体架构间的**迁移性**。
*   **客观性**：通过设置“影子评估”和严格的 ASR 定义（必须实际提交敏感数据才算成功），确保了评估结果的客观性。

### 6. 主要结论与发现
*   **极高的成功率**：LoginTrap 在不同 LLM 后端上的平均 ASR 达到 **86%**，LER 达到 **93%**。
*   **架构通用性**：攻击对不同架构的智能体均有效，平均 ASR 为 79%，证明风险具有系统性。
*   **页面普遍可利用**：在给定的尝试预算内（9 次），**100%** 的测试网页都能被成功利用。
*   **模型差异**：Claude 3.5 Sonnet 和 Gemini 1.5 Flash 在接受登录前提后，完成后续表单填充的稳定性更高；而 GPT-4o 虽然点击率高，但在表单填充阶段更容易中断。

### 7. 优点
*   **威胁模型真实**：不依赖于对用户任务的预知，符合现实中黑盒攻击的情景。
*   **自动化程度高**：利用模糊测试思想自动适配不同网页，解决了攻击载荷难以规模化生成的难题。
*   **揭示深层风险**：指出了智能体在处理“看似合理”的网页指令时，缺乏对身份验证必要性的逻辑判断。

### 8. 不足与局限
*   **实验环境限制**：实验在克隆的静态网页上进行，可能无法完全模拟动态交互极强的复杂 Web 应用。
*   **步骤预算固定**：实验中将智能体操作限制在 5 步以内，虽然足以完成登录，但可能限制了对更复杂防御场景的探索。
*   **表单布局单一**：受控登录页面采用了固定的行分隔布局，现实中的钓鱼页面可能面临更复杂的表单识别挑战。
*   **防御研究尚浅**：虽然提到现有防御手段效果有限，但未提出针对性的、能够识别“任务-登录”逻辑矛盾的深度防御方案。

（完）
