---
title: "PolicyGuard: Prompt-Configurable Semantic DLP for LLM Coding Agents"
title_zh: PolicyGuard：面向 LLM 编程智能体的提示词可配置语义数据泄露防护
authors: "Kyutae Park, Jungwon Kim, Daeyeol Shim"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02687v3"
tags: ["query:jb-def"]
score: 8.0
evidence: 安全护栏与内容过滤
tldr: 针对AI编程助手可能泄露敏感信息的问题，本文提出PolicyGuard框架。该框架采用“策略即提示”范式，利用大语言模型根据纯文本策略文件对用户输入进行语义拦截。通过严格的评估协议测试，PolicyGuard在保持低误报率的同时实现了极高的拦截率，且具备跨模型的可移植性，为非技术人员提供了灵活、高效的数据泄露防护方案。
selection_source: fresh_fetch
motivation: 现有的数据泄露防护方案依赖硬编码规则或模型微调，难以灵活应对AI编程助手中复杂的语义泄露风险。
method: 提出一种预模型拦截框架，通过自然语言定义的策略文件引导LLM对用户提示词进行实时分类与合规性检查。
result: "在2000个多语言提示词的测试中，该方法达到了96.5%的有效拦截率和仅3.0%的误报率，且在跨模型测试中表现稳定。"
conclusion: PolicyGuard证明了基于自然语言策略的语义拦截比传统格式更有效，为LLM应用提供了一种可配置且高泛化性的安全防护机制。
---

## 摘要
AI 编程智能体接受自由格式的自然语言提示词，这些提示词可能无意中包含凭据、个人身份信息 (PII) 或专有业务数据。现有的数据泄露防护 (DLP) 解决方案依赖于僵化的正则表达式模式、模型微调或定制化程度有限的供应商管理分类器。我们提出了 PolicyGuard，这是一个模型前置拦截框架，它使用由自然语言策略文件引导的 LLM 对用户提示词进行分类。我们的主要贡献包括：(1) 策略即提示词 (policy-as-prompt) 范式，其中 DLP 分类标准完全定义在纯文本策略文档中，非工程师无需更改代码或重新训练模型即可进行编辑；(2) 一种封闭式评估协议，包含模板族级数据拆分、隐藏留出集和冻结测试集，以严格评估泛化能力；(3) 对 2,000 个多语言提示词进行的全面实证评估，结果显示在 927 个提示词的冻结测试集上实现了 96.5% 的有效拦截率 (EBR) 和仅 3.0% 的误报率 (FPR)，并在 217 个提示词的隐藏留出集上达到了 100% 的完美准确率。信息匹配的基准实验表明，PolicyGuard 的自然语言格式显著优于 JSON 格式的等效内容（McNemar 卡方检验 = 31.58, p < 0.001），并且大幅优于零样本分类（Cohen's h = 0.915）。跨模型可移植性实验表明，相同的策略在无需修改的情况下，在四种不同的 LLM 中实现了 86.4-96.5% 的 EBR。

## Abstract
AI coding agents accept free-form natural language prompts that may inadvertently contain credentials, personally identifiable information (PII), or proprietary business data. Existing data loss prevention (DLP) solutions rely on rigid regex patterns, model fine-tuning, or vendor-managed classifiers with limited customizability. We present PolicyGuard, a pre-model interception framework that classifies user prompts using an LLM guided by a natural language policy file. Our key contributions are: (1) the policy-as-prompt paradigm, where DLP classification criteria are defined entirely in a plaintext policy document editable by non-engineers without code changes or model retraining; (2) a sealed evaluation protocol with template-family-level data splits, hidden holdouts, and frozen test sets to rigorously assess generalization; and (3) a comprehensive empirical evaluation across 2,000 multilingual prompts demonstrating 96.5% effective block rate (EBR) with only 3.0% false positive rate (FPR) on a frozen test set of 927 prompts, and perfect 100% accuracy on a 217-prompt hidden holdout. Information-matched baseline experiments show that PolicyGuard's natural language format significantly outperforms equivalent content in JSON format (McNemar chi-squared = 31.58, p < 0.001) and dramatically outperforms zero-shot classification (Cohen's h = 0.915). Cross-model portability experiments demonstrate that the same policy achieves 86.4-96.5% EBR across four different LLMs without modification.

---

## 论文详细总结（自动生成）

# 论文总结：PolicyGuard —— 面向 LLM 编程智能体的提示词可配置语义数据泄露防护

### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 编程助手（如 GitHub Copilot, Cursor 等）的普及，开发者在交互中可能会无意间输入敏感信息，如 API 密钥、个人身份信息（PII）或公司专有代码。
*   **现有方案的缺陷**：传统的正则表达式（Regex）过于僵化，无法理解语义；模型微调成本高且更新慢；供应商提供的分类器缺乏定制化能力。
*   **核心目标**：开发一种灵活、可由非技术人员配置、且具备语义理解能力的预模型拦截框架，以防止敏感数据泄露。

### 2. 论文提出的方法论
论文提出了 **PolicyGuard** 框架，其核心思想是 **“策略即提示词”（Policy-as-Prompt）**：
*   **预模型拦截（Pre-model Interception）**：在用户提示词发送给编程智能体之前，先由一个“守卫 LLM”进行审查。
*   **自然语言策略文件**：安全策略完全由纯文本编写（而非代码或 JSON）。这允许合规官或管理人员直接编辑安全准则，无需重新训练模型或更改系统代码。
*   **语义分类流程**：守卫 LLM 接收“策略文件”和“用户提示词”，根据策略中定义的类别（如凭据、PII、内部架构等）对输入进行实时分类，并决定是否拦截。

### 3. 实验设计
*   **数据集**：构建了一个包含 **2,000 个多语言提示词** 的数据集，涵盖多种泄露场景和正常编程请求。
*   **评估协议**：采用严格的“封闭式评估”，包括：
    *   **模板族级数据拆分**：确保测试集和训练集在结构上不重叠，防止模型通过记忆模板获胜。
    *   **隐藏留出集（Hidden Holdout）**：包含 217 个提示词，用于最终验证。
    *   **冻结测试集**：包含 927 个提示词。
*   **对比基准（Benchmark）**：
    *   **零样本分类（Zero-shot）**：不提供详细策略的直接分类。
    *   **JSON 格式策略**：将相同的策略信息以结构化 JSON 形式呈现，对比自然语言的优劣。
    *   **跨模型测试**：在四种不同的 LLM 上运行相同策略以测试可移植性。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   由于该方法基于推理侧的提示词工程（Prompt Engineering）而非模型微调，其算力消耗主要体现在**推理阶段的额外 LLM 调用**上。

### 5. 实验数量与充分性
*   **实验规模**：总计 2,000 组样本，在 LLM 安全评估领域属于中大规模。
*   **充分性**：
    *   进行了**消融实验**（自然语言 vs. JSON），证明了自然语言在传达复杂语义规则方面的优越性。
    *   进行了**跨模型验证**，证明了策略的泛化能力。
    *   使用了 **McNemar 卡方检验** 和 **Cohen's h** 等统计学工具验证结果的显著性，实验设计客观且严谨。

### 6. 论文的主要结论与发现
*   **高效性**：在冻结测试集上实现了 **96.5% 的有效拦截率（EBR）**，且**误报率（FPR）仅为 3.0%**。在隐藏留出集上达到了 100% 的准确率。
*   **格式优势**：自然语言格式的策略显著优于 JSON 格式（p < 0.001），说明 LLM 对自然语言指令的语义理解更深。
*   **可移植性**：同一套策略在不同模型（跨 4 种 LLM）中均表现稳定，EBR 保持在 86.4% - 96.5% 之间。
*   **易用性**：证明了非工程师通过修改纯文本即可快速调整安全边界的可行性。

### 7. 优点
*   **高度可配置**：解耦了安全策略与系统代码，降低了安全运维的门槛。
*   **语义感知**：能够识别 Regex 无法处理的复杂泄露（如描述性的内部架构泄露）。
*   **评估严谨**：通过模板族拆分有效避免了数据泄露导致的评估偏差。
*   **零微调**：无需昂贵的训练过程，部署速度快。

### 8. 不足与局限
*   **推理延迟**：由于在主模型处理前增加了一次 LLM 审查，会增加用户感知的响应时间。
*   **成本增加**：每次用户交互都需要额外的 Token 消耗来运行守卫模型。
*   **对抗性风险**：论文未深入探讨针对“守卫 LLM”本身的提示词注入攻击（Prompt Injection）。
*   **语言偏差**：虽然支持多语言，但对于极少数语种或特定行业术语的覆盖可能存在偏差。

（完）
