---
title: A Self-Evolving Multi-Agent Framework Defense against LLM Jailbreak Attacks
title_zh: 一种针对大语言模型越狱攻击的自进化多智能体防御框架
authors: "Tongyan Hu, Bryan Hooi"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.26008v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对越狱攻击的自我演化防御
tldr: 针对大语言模型面临的不断演进的越狱攻击，本文提出了一种自进化的多智能体防御框架。该框架通过构建持久化的跨交互规则记忆库，在攻击成功时自动提取攻击模式的结构化规则，而非仅关注有害主题。这种方法无需参数更新，适用于黑盒模型，能随攻击演进而自我增强，有效降低了多种越狱攻击的成功率，同时保持了模型的良性实用性。
selection_source: fresh_fetch
motivation: 现有的 LLM 防御机制大多是静态的，无法在部署后积累防御经验或适应不断出现的新型越狱策略。
method: 提出一种基于外部记忆和提示词的自进化防御框架，通过将成功的攻击抽象为方法级规则并存储在记忆库中，实现对未来类似攻击的泛化防御。
result: 在多种黑盒越狱攻击和模型上的实验表明，该方法显著降低了攻击成功率，且在记忆库增长时不会增加过度拒绝现象。
conclusion: 该研究证明了通过动态记忆机制实现防御策略自进化的有效性，为构建更具韧性的 LLM 安全防线提供了新思路。
---

## 摘要
大语言模型（LLMs）仍然容易受到越狱攻击的影响，这些攻击利用角色扮演、混淆、代码转换和多步间接引导等技术来诱导有害输出。随着越狱策略的不断涌现，防御手段也在持续的“猫鼠游戏”中激增，但大多数防御手段仍然是静态的：其安全行为在部署时即已固定，因此无法积累防御经验或适应未见的策略。我们提出了一种基于持久化、跨交互规则记忆的自进化测试时防御方案：当攻击成功时，该框架将失败抽象为一种方法级规则，捕捉结构化的攻击包装器（wrapper）而非有害主题，并将其复用于未来的输入。由于规则是方法级的，一条归纳出的规则可以泛化到整个攻击家族，且标签空间会随着新包装器的出现而扩展。该机制完全通过外部记忆和提示词（prompting）运行，无需参数更新，且适用于开源权重模型和黑盒 API 模型。我们将其实现为四个协作模块，但核心贡献在于基于记忆的自适应机制，而非模块分解。在四个黑盒越狱家族和多个模型上的实验表明，我们的方法在保持良性效用的同时，显著降低了攻击成功率，在自适应复合包装器攻击下保持稳健，且不会随着记忆的增长而增加过度拒绝。

## Abstract
Large language models (LLMs) remain vulnerable to jailbreak attacks that exploit techniques such as role-playing, obfuscation, code transformation, and multi-step indirection to elicit harmful outputs. As jailbreak strategies keep emerging, defenses have proliferated in an ongoing cat-and-mouse game, yet most remain static: their safety behavior is fixed at deployment, so they cannot accumulate defensive experience or adapt to unseen strategies. We propose a self-evolving test-time defense built around a persistent, cross-interaction rule memory: when an attack succeeds, the framework abstracts that failure into a method-level rule capturing the structural attack wrapper rather than the harmful topic, and reuses it against future inputs. Because rules are method-level, one induced rule generalizes across an entire attack family, and the label space expands as novel wrappers appear. The mechanism operates entirely through external memory and prompting, with no parameter updates, and applies to both open-weight and black-box API models. We realize it as four cooperating modules, but the contribution is the memory-based adaptation mechanism, not the module decomposition. Across four black-box jailbreak families and multiple models, our method substantially reduces attack success rates while preserving benign utility, remains robust under an adaptive composite-wrapper attack, and does not increase over-refusal as the memory grows.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“自进化多智能体框架”的防御机制，旨在解决大语言模型（LLM）在面对不断演进的越狱攻击时防御手段过于静态的问题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 安全防御（如安全微调、静态系统提示词、外部过滤器）大多是**静态**的。一旦部署，其防御逻辑固定，无法从新的攻击失败中吸取教训，也难以应对未见过的、复杂的结构化越狱策略（如嵌套角色扮演、代码混淆等）。
*   **研究背景**：越狱攻击与防御之间存在“猫鼠游戏”。攻击者不断开发新的“包装器”（Wrappers）来绕过安全对齐。作者认为，防御系统应该具备**测试时自适应（Test-time Adaptation）**能力，通过积累防御经验来对抗动态演进的威胁。

### 2. 方法论：核心思想与关键技术
该框架的核心思想是构建一个**持久化的跨交互规则记忆库**，通过四个协作智能体实现防御的自我进化：
*   **核心流程**：
    1.  **攻击模式分类 ($\phi$)**：将输入映射为“方法级”标签（如 `roleplay-nested`），关注攻击的结构而非具体有害话题。
    2.  **规则触发与检索 (A1)**：从记忆库中检索与当前攻击模式最相关的防御规则（最多 2 条）。
    3.  **策略决策 (A2)**：根据触发规则决定响应强度（允许、软拒绝、硬拒绝）。
    4.  **响应生成 (A3)**：根据决策动态组合系统指令生成最终输出。
    5.  **自我反思与记忆更新 (A4)**：如果攻击成功（由内部 LLM 评判），反思智能体会将此次失败抽象为一条新的**方法级规则**存入记忆库。
*   **关键技术细节**：
    *   **方法级抽象**：规则针对的是攻击的“包装方式”（如代码加密），而非“制造炸弹”等具体话题，这使得单条规则能泛化到整个攻击家族。
    *   **无需参数更新**：完全基于外部记忆和提示词工程，适用于黑盒 API 模型。
    *   **去重与容量限制**：通过语义去重和每类标签 4 条规则的容量限制，防止记忆库过度膨胀或过拟合。

### 3. 实验设计
*   **数据集**：使用 **AdvBench**（520 个有害行为提示词）评估安全性；使用 **MMLU** 和 **GSM8K** 评估良性任务的实用性（Utility）；使用 **XSTest** 评估过度拒绝（Over-refusal）。
*   **攻击场景**：涵盖四种主流黑盒越狱家族：DeepInception、CodeChameleon、ReNeLLM、FlipAttack。
*   **对比方法**：
    *   无防御（No Defense）
    *   静态防御提示词（Defense Prompt）
    *   实例级自我提醒（Self-Reminder）
    *   多智能体过滤框架（AutoDefense）
    *   其他基准：AegisLLM、ICD（上下文内防御）。
*   **测试模型**：Qwen2.5-7B、Llama3.1-8B、Gemini-3-Flash-Preview。

### 4. 资源与算力
*   **硬件配置**：论文在附录 A.1 中明确说明使用了服务器配置：
    *   **CPU**：2 个 48 核 Intel Xeon Platinum 8558。
    *   **内存**：1.0 TB RAM。
    *   **GPU**：**4 张 NVIDIA H200 (141GB VRAM)**。
*   **软件环境**：CUDA 13.0，驱动版本 580.126.09。
*   **训练时长**：由于该方法是测试时防御，无需离线训练，主要开销在于推理时的多轮 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同架构的模型上针对 4 种攻击家族进行了全面测试，并提供了 ASR-rej（基于关键词）和 ASR-gpt（基于 LLM 评分）双重指标。
*   **消融实验**：针对触发机制、强制执行机制和反思机制分别进行了消融，证明了各模块的必要性。
*   **自进化分析**：展示了随着交互轮数增加，ASR（攻击成功率）如何迅速下降并趋于零，证明了学习过程的有效性。
*   **公平性与客观性**：实验采用了“冷启动”协议（初始记忆为空），且评估用的 GPT 评判器与系统内部的违规检测器使用不同的模型，避免了自循环优化的偏差。

### 6. 主要结论与发现
*   **显著降低 ASR**：在所有模型和攻击类型上，该方法均显著优于静态防御和实例级反思方法。
*   **快速收敛**：通常在经历 1-2 次同类攻击失败后，系统就能提取出有效规则，使后续同类攻击的成功率降至接近 0。
*   **保持实用性**：在 MMLU 等任务上的性能下降极小（约 2 个百分点以内），且在 XSTest 上的过度拒绝率保持在较低水平（约 9.6%）。
*   **跨模型泛化**：证明了该框架在开源模型和闭源 API 模型上同样有效。

### 7. 优点
*   **动态适应性**：打破了传统防御的静态限制，实现了“在战斗中学习”。
*   **泛化能力强**：通过抽象“攻击包装器”而非“攻击话题”，实现了“举一反三”的防御效果。
*   **低侵入性**：无需微调模型，不依赖梯度或 Logits，具有极高的工程实用价值。
*   **鲁棒性**：实验证明其能对抗旨在规避规则触发的“复合包装器”自适应攻击。

### 8. 不足与局限
*   **推理开销**：稳态下每条输入需要 3 次 LLM 调用（分类、决策、生成），增加了延迟（约 1.43 倍）和成本。
*   **依赖基础模型能力**：违规检测和规则提取的效果受限于所使用的智能体模型（如分类器误判会导致防御失效）。
*   **长时记忆管理**：虽然有容量限制，但在极长交互流中的规则冲突解决和规则剪枝机制仍有待深入研究。
*   **覆盖范围**：尚未测试多轮对话攻击或代理型（Agentic）攻击场景。

（完）
