---
title: Enforcing LLM Safety through DMD-based Classification of Prompt-Response Embedding Dynamics
title_zh: 通过基于 DMD 的提示-响应嵌入动力学分类增强大语言模型安全性
authors: "Mohamed Akrout, Olivera Kotevska, Dan Wilson"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19579v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 大语言模型安全分类与护栏
tldr: 针对大语言模型（LLM）生成有害内容的安全风险，本文提出一种基于动力系统框架的黑盒检测方法。该方法将提示词和回复映射到高维嵌入空间，利用动态模式分解（DMD）构建Koopman预测模型，通过比较安全与不安全模式下的微分残差得分进行分类。研究表明，结合提示词与回复的动态交互特征能显著提升对交互型违规内容的检测性能，为利用动力系统理论分析AI系统提供了新视角。
selection_source: fresh_fetch
motivation: 现有的LLM安全检测方法在黑盒场景下难以高效识别复杂的有害输出，亟需一种能够捕捉提示词与回复间动态交互特征的新方案。
method: 扩展了基于Koopman算子的动力系统框架，通过拟合安全与不安全状态的预测模型，并利用微分残差得分对嵌入向量序列进行分类。
result: 在三大安全基准测试中，引入提示词嵌入显著提升了对交互依赖型违规的检测效果，而纯回复违规在稠密语义嵌入下表现更佳。
conclusion: 该研究证明了动力系统理论在AI安全分析中的潜力，展示了通过建模嵌入空间动态特性来增强模型安全性的有效性。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在关键应用中，但其生成有毒、有害或违反政策内容的倾向带来了重大风险。以黑盒方式高效检测这些不安全输出仍是一个开放性挑战。在本文中，我们将最近提出的一种用于幻觉检测的动力系统框架扩展到大语言模型安全性分类中。通过将提示和响应都投影到高维嵌入空间，并分别为安全和不安全状态拟合基于 Koopman 的预测模型，我们利用一种比较安全与不安全状态预测误差的新型差分残差分数对新输出进行分类。一个关键贡献是纳入了提示和响应的嵌入动力学，从而产生能够捕捉关键交互模式的拟合 Koopman 算子。我们使用三种嵌入模型在三个安全性基准测试上评估了我们的黑盒方法。结果表明，纳入提示嵌入带来了持续的改进，特别是对于与因果解码器（如 Llama-3）配对时的交互依赖型违规行为；而仅响应型违规行为则从稠密语义嵌入表示中获益更多。这些发现为利用动力系统分析人工智能系统开辟了道路，挑战了目前利用人工智能对动力系统建模的主导范式。

## Abstract
Large Language Models (LLMs) are increasingly deployed in high-stakes applications, yet their tendency to generate toxic, harmful, or policy-violating content poses significant risks. Detecting these unsafe outputs efficiently in a black-box manner remains an open challenge. In this paper, we extend a recently proposed dynamical systems framework designed for hallucination detection to LLM safety classification. By projecting both prompts and responses into high-dimensional embedding spaces and fitting separate Koopman-based predictive models for safe and unsafe regimes, we classify new outputs using a new differential residual score that compares prediction errors of the safe and unsafe regimes. A key contribution is the incorporation of the prompt and response embedding dynamics, yielding fitted Koopman operators that capture crucial interaction patterns. We evaluate our black-box method across three safety benchmarks using three embedding models. Our results show that incorporating prompt embeddings yields consistent improvements, particularly for interaction-dependent violations when paired with causal decoders (e.g., in Llama-3), while response-only violations benefit more from dense semantic embedding representations. These findings opens the door for using dynamical systems to analyze AI systems rather than the dominant paradigm of using AI to model dynamical systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enforcing LLM Safety through DMD-based Classification of Prompt-Response Embedding Dynamics》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）在关键领域的广泛应用，其生成的有毒、有害或违反政策的内容（如仇恨言论、非法指令等）构成了严重风险。
*   **核心问题**：如何在不访问模型内部参数（黑盒场景）、不进行昂贵的微调、且不依赖多次随机采样的情况下，高效且准确地检测 LLM 的不安全输出。
*   **背景**：该研究将 LLM 的 Token 生成过程视为一个离散时间动力系统，通过分析 Token 在高维嵌入空间中的演化轨迹来识别安全风险。

### 2. 方法论
*   **核心思想**：利用动力系统理论中的 **Koopman 算子** 和 **动态模式分解（DMD）**，分别为“安全”和“不安全”的文本流拟合预测模型。通过比较新样本在两类模型下的预测误差（残差）来进行分类。
*   **关键技术细节**：
    *   **嵌入投影**：使用预训练嵌入模型将 Token 序列映射为高维向量序列。
    *   **状态提升（Lifting）**：通过扩展 DMD（EDMD）将原始嵌入向量提升到更高维的空间，以捕捉非线性动力学特征。
    *   **算子拟合**：针对提示词（Prompt）和响应（Response），分别训练四组 Koopman 算子：安全提示、不安全提示、安全响应、不安全响应。
    *   **微分残差得分（$\Delta E$）**：计算输入序列在“不安全模型”下的预测误差与“安全模型”下预测误差的差值。
*   **算法流程**：
    1.  收集安全/不安全数据集。
    2.  拟合对应的 Koopman 算子。
    3.  推理阶段，计算待测样本的 $\Delta E$。
    4.  将 $\Delta E$ 与预设阈值 $\eta$ 比较，判定是否安全。

### 3. 实验设计
*   **数据集**：
    *   **Aegis AI Content Safety 2.0**：包含 12 类安全分类的人机交互数据。
    *   **Synthetic CoT Safety Benchmark**：侧重于思维链（CoT）拒绝有害请求的推理数据。
    *   **BeaverTails**：包含 14 类危害类别的真实人类提示词与模型响应。
*   **Benchmark 与对比**：
    *   **嵌入模型对比**：Qwen3-Embed (0.6B)、Mistral (7.2B)、Llama-3 (8.0B)。
    *   **特征对比**：仅使用响应嵌入（Response-only） vs. 联合使用提示词与响应嵌入（Prompt-aware）。
    *   **性能指标**：F1 分数、召回率（Recall）、准确率（Accuracy）及 ROC 曲线。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **资源限制**：作者提到在“有限的计算资源”下工作，因此选择了参数量在 0.6B 到 8B 之间的中小型嵌入模型，而非超大规模模型。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流安全基准数据集上进行了测试，测试样本量通常在 1.2 万组左右。
*   **充分性评价**：
    *   **多维度分析**：实验涵盖了不同 Token 长度（L ≥ 1, 50, 100, 150）对分类性能的影响。
    *   **客观性**：对比了不同架构（如因果解码器 Llama-3 与稠密编码器 Qwen）的差异，分析了交互依赖型违规与内容孤立型违规的不同表现。
    *   **公平性**：使用了统一的评估指标，并与已有的有监督微调基线（如 AegisGuard）进行了对比。

### 6. 主要结论与发现
*   **提示词的重要性**：引入提示词的动力学特征能显著提升检测效果，尤其是在“交互依赖型”违规（即提示词和响应单独看都安全，但组合起来不安全）中。
*   **模型架构相关性**：
    *   **Llama-3（因果解码器）**：在加入提示词动态后表现最佳，擅长捕捉因果交互模式。
    *   **Qwen-Embed（稠密语义嵌入）**：在检测内容本身即违规（如直接的仇恨言论）时表现更优。
*   **黑盒有效性**：该方法作为一种完全的黑盒方法，在 Aegis 数据集上达到了 77.0% 的 F1 分数，虽略低于特定任务微调的模型（86.8%），但具有极高的部署灵活性。

### 7. 优点
*   **低成本与高效性**：无需访问 LLM 的内部状态（如 Logits 或注意力图），仅需文本嵌入，适合 API 调用场景。
*   **单样本推理**：与需要多次采样（Self-Check）的方法相比，该方法仅需一次生成即可判定，显著降低了推理延迟。
*   **理论创新**：开辟了“用动力系统分析 AI”的新路径，而非传统的“用 AI 模拟动力系统”。

### 8. 不足与局限
*   **分类粒度**：目前主要集中在二分类（安全/不安全），尚未实现对具体危害类别（如暴力 vs 毒品）的精细化多分类。
*   **序列长度依赖**：对于极短的响应，动力学特征可能不够丰富，导致分类性能下降。
*   **嵌入模型依赖**：检测效果高度依赖于所选嵌入模型的质量和表征能力。
*   **应用限制**：作为后置过滤手段，虽然能检测不安全内容，但无法阻止模型生成过程中的计算资源浪费。

（完）
