---
title: "COPA: Continual Preference Optimization for Adaptive Prompt Injection Defense"
title_zh: COPA：面向自适应提示注入防御的持续偏好优化
authors: "Roshan Sood, Onat Gungor, Tajana Rosing"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19982v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对自适应提示注入防御的持续偏好优化
tldr: 针对大语言模型面临的不断演进的提示注入攻击，本文提出了 COPA 框架。该框架将防御视为持续学习问题，利用基于 GRPO 的偏好优化增量吸收新攻击反馈，并结合边际权重经验回放机制克服灾难性遗忘。实验表明，COPA 在应对动态攻击流时表现优异，显著降低了攻击成功率，为构建具备自适应能力的 LLM 安全防御体系提供了新思路。
selection_source: fresh_fetch
motivation: 静态防御机制无法有效应对攻击者不断变换策略以利用模型新弱点的自适应提示注入攻击。
method: 采用基于 GRPO 的持续偏好优化，并引入边际权重经验回放来平衡新威胁的学习与旧防御的巩固。
result: 相比现有最先进防御方法，COPA 将攻击成功率平均降低了 4.4 倍，最高可达 6.3 倍。
conclusion: 持续偏好优化能有效提升 LLM 对动态攻击的鲁棒性，是实现长效安全防御的关键路径。
---

## 摘要
大语言模型（LLMs）仍然容易受到提示注入攻击的影响，即嵌入在用户输入或外部内容中的对抗性指令会操纵模型行为并绕过安全防护。现有的防御措施主要是静态的，依赖于固定的对齐目标或针对特定攻击的过滤机制，随着新攻击策略的出现，这些机制需要重新设计。虽然最近的终身对齐方法解决了用户偏好的转变，但它们没有考虑到不断演化以利用先前学习到的防御漏洞的自适应对手。这一局限性在实际部署中尤为重要，因为不断演化的攻击分布需要持续的适应，同时不能牺牲对先前遇到的威胁的鲁棒性。我们提出了 COPA，这是一个将提示注入防御视为终身学习问题的持续偏好优化框架。COPA 并非进行一次性对齐，而是通过基于 GRPO 的优化增量地整合来自新观察到的攻击的反馈，并使用边际加权经验回放（margin-weighted experience replay）来保留对先前攻击类别的防御。这使得模型能够持续适应新兴威胁，同时缓解灾难性遗忘并保留通用模型能力。在终身提示注入攻击流中，与最先进的防御措施相比，COPA 将攻击成功率最高降低了 6.3 倍，平均降低了 4.4 倍。这些结果表明，持续偏好优化是防御大语言模型免受自适应对手攻击的一种有效范式。

## Abstract
LLMs remain vulnerable to prompt injection attacks, where adversarial instructions embedded in user inputs or external content manipulate model behavior and bypass safeguards. Existing defenses are predominantly static, relying on fixed alignment objectives or attack-specific filtering mechanisms that require redesign as new attack strategies emerge. While recent lifelong alignment methods address shifting user preferences, they do not account for adaptive adversaries that continually evolve to exploit weaknesses in previously learned defenses. This limitation is particularly important in real-world deployments, where evolving attack distributions necessitate continual adaptation without sacrificing robustness to previously encountered threats. We present COPA, a continual preference optimization framework that treats prompt-injection defense as a lifelong learning problem. Instead of one-time alignment, COPA incrementally incorporates feedback from newly observed attacks via GRPO-based optimization and uses margin-weighted experience replay to retain defenses against prior attack classes. This enables continuous adaptation to emerging threats while mitigating catastrophic forgetting and preserving general-purpose model capabilities. Across lifelong prompt injection attack streams, COPA reduces attack success rate by up to 6.3x and 4.4x on average compared to state-of-the-art defenses. These results highlight continual preference optimization as an effective paradigm for defending LLMs against adaptive adversaries.

---

## 论文详细总结（自动生成）

以下是对论文《COPA: Continual Preference Optimization for Adaptive Prompt Injection Defense》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 提示注入（Prompt Injection）防御机制大多是**静态的**（如一次性对齐或固定过滤器），无法应对不断演进的、具有自适应能力的攻击策略。
*   **研究动机**：
    *   在实际部署中，攻击分布随时间变化，静态防御在面对新出现的攻击变体时鲁棒性迅速下降。
    *   现有的终身学习（Lifelong Learning）方法多关注用户偏好的转变，而非对抗性攻击的持续演化。
    *   模型在学习新防御时容易出现“灾难性遗忘”，即失去对旧攻击类型的抵抗力。

### 2. 方法论（COPA 框架）
COPA 将防御视为一个**持续安全对齐**问题，包含两个主要阶段：
*   **阶段 1：偏好对齐（基础加固）**：
    *   在初始阶段，使用通用的网络安全偏好数据集（$D_0$）对模型进行对齐，建立安全基准。
    *   采用 **GRPO（Group Relative Policy Optimization）** 算法，通过组内归一化奖励来稳定学习过程，减少标量奖励估计的高方差。
*   **阶段 2：终身对抗训练（持续适应）**：
    *   **核心思想**：将新出现的攻击变体组织成序列任务流，通过轻量化 LoRA 适配器进行增量更新。
    *   **边际权重经验回放（Margin-weighted Experience Replay）**：
        *   引入**对数似然边际（Log-likelihood Margin）**作为优先级信号：$m_\theta = \log \pi_\theta(y_s | x) - \log \pi_\theta(y_u | x)$。
        *   边际值越小，说明模型对该攻击的防御越不确定。回放缓冲区会优先采样低边际的样本进行复习，以克服遗忘。
    *   **在线策略优化**：在每个新任务阶段，将新攻击样本与从缓冲区抽取的优先级样本混合，利用 GRPO 进行持续优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：来自 CyberSecEval 的 9 种提示注入变体。
    *   **测试场景**：
        *   **S1（留出变体）**：CyberSecEval 中未见过的 6 种变体。
        *   **S2（文本攻击）**：直接注入、忽略指令、完成类攻击等。
        *   **S3（基于优化的攻击）**：如 UAT 和 GCG 等梯度搜索攻击。
*   **Benchmark 与对比方法**：
    *   **对比防御**：LlamaGuard（分类器）、DataSentinel（博弈论检测）、SecAlign（静态偏好优化）。
    *   **评估指标**：攻击成功率（ASR）、后向转移（BWT，衡量遗忘程度）、平均性能（AP）、通用能力（GPQA, MMLU-Pro）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **模型配置**：主要基于 Meta-Llama-3.1-8B-Instruct，同时验证了 Mistral-7B 和 Qwen-2.5-7B。
*   **技术细节**：使用 LoRA（r=64）和 4-bit NF4 量化以降低显存占用。每个任务进行 30 次 GRPO 更新。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 种攻击变体，并在 3 种不同架构的 LLM 上进行了验证。
*   **消融实验**：
    *   对比了“边际权重回放”与“均匀回放”的效果，证明了优先级采样的必要性。
    *   对比了 GRPO 与 DPO（直接偏好优化）在持续学习场景下的稳定性，发现 DPO 更容易导致过拟合和遗忘。
*   **充分性评价**：实验设计较为全面，不仅关注防御成功率，还通过 BWT 指标严格评估了持续学习中的遗忘问题，并兼顾了模型通用能力的保留。

### 6. 主要结论与发现
*   **防御效能**：COPA 将 ASR 平均降低了 **4.4 倍**，在最具挑战性的场景下降低了 **6.3 倍**。
*   **克服遗忘**：COPA 实现了正向的后向转移（BWT > 0），意味着在学习新攻击的同时，对旧攻击的防御能力不仅没有下降，反而有所增强。
*   **通用能力保留**：与其他防御方法相比，COPA 在 MMLU 和 GPQA 上的得分最高，证明其在加固安全性的同时，几乎不损害模型的推理和知识能力。

### 7. 优点
*   **动态自适应**：打破了静态防御的局限，使模型能够像生物一样通过经验积累不断进化防御能力。
*   **高效回放机制**：利用模型自身的概率边际作为优先级信号，无需额外的标注或复杂的判断模型，计算开销低。
*   **架构无关性**：在 Llama、Mistral、Qwen 等多种模型上表现一致，具有良好的通用性。

### 8. 不足与局限
*   **奖励模型依赖**：防御效果高度依赖于阶段 1 中用于生成偏好标签的“安全裁判（Safety Judge）”的准确性。
*   **任务顺序敏感性**：论文未深入探讨攻击变体出现的先后顺序是否会对最终的防御稳态产生显著影响。
*   **模型规模限制**：实验主要集中在 7B-8B 规模的模型，尚未在超大规模模型（如 70B+）上验证其扩展性。

（完）
