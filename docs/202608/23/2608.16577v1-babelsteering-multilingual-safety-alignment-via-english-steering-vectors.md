---
title: "BabelSteering: Multilingual Safety Alignment via English Steering Vectors"
title_zh: BabelSteering：通过英语引导向量实现多语言安全对齐
authors: "Emma V. Stein, Dominik Meier, Terry Ruas, Jan Philip Wahle, Bela Gipp"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16577v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 通过激活引导进行多语言安全对齐
tldr: 本研究针对大语言模型在非英语环境下安全对齐薄弱的问题，提出了 BabelSteering 方法。该方法是一种轻量级的推理时干预技术，利用从英语安全监督中提取的拒绝方向（激活引导向量）来增强多语言的安全防御。实验涵盖八种语言，结果表明该方法在保持通用任务效能的同时，显著提升了对有害请求的拒绝率，为跨语言安全对齐提供了一种低成本且高效的解决方案。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型安全研究过度集中于英语，导致非英语用户面临较弱安全保障的现状。
method: 提出一种名为 BabelSteering 的激活引导方法，利用英语拒绝向量在推理时对模型进行干预。
result: 实验显示该方法在八种语言中均提升了有害请求拒绝率，且对模型在 Global MMLU 等任务上的效能影响极小。
conclusion: 激活引导是扩展英语安全信号至多语言环境的实用且低成本机制，有助于提升全球范围内的模型安全性。
---

## 摘要
大语言模型（LLMs）已在全球范围内的高风险场景中部署，但大多数安全研究和对齐工作仍集中在英语上。因此，尽管用户在处理同样敏感的任务时依赖相同的系统，但使用其他语言与 LLMs 交互的用户可能会遇到较弱的安全保障。在这项工作中，我们研究了从英语等高资源语言中学习到的安全信号是否能提高多语言安全性。我们提出了 BabelSteering，这是一种作为轻量级推理时干预的激活引导（activation steering）方法，利用从英语安全监督中导出的拒绝方向来泛化到不同语言。我们的评估涵盖了八种语言，并共同衡量了对有害请求的拒绝率、过度拒绝率以及通用任务效用。结果表明，BabelSteering 提高了跨语言对有害请求的拒绝率，任务效用仅有轻微甚至没有下降，但对伪有害提示词的拒绝率有所增加。例如，对于 Gemma 7B，我们观察到跨语言对有害提示词的拒绝率平均增加了 11 个百分点（pp），其中孟加拉语等特定语言增加了 17 个百分点，且在 Global MMLU 上的效用没有损失，而伪有害拒绝率平均增加了 13 个百分点。我们还引入了一个多语言翻译与评估流水线，以促进未来关于跨语言安全干预的研究。总的来说，我们的研究结果表明，激活引导可能为将源自英语的安全信号扩展到其他语言提供一种实用且低成本的机制。警告：本文包含不安全内容的示例。

## Abstract
Large language models (LLMs) are deployed globally in high-stakes settings, yet most safety research and alignment efforts remain concentrated on English. Thus, users interacting with LLMs in other languages may encounter weaker safeguards despite relying on the same systems for similarly sensitive tasks. In this work, we investigate whether safety signals learned from a high-resource language, like English, can improve multilingual safety. We propose BabelSteering, an activation steering method that acts as a lightweight inference- time intervention, using refusal directions derived from English safety supervision to generalize across languages. Our evaluation includes eight languages and jointly measures refusal of harmful requests, over-refusal, and general task utility. The results show that BabelSteering increases the refusal of harmful requests across languages, with only a marginal to no reduction in task utility but with some increase in refusal of pseudo-harmful prompts. For example, for Gemma 7B, we see an average increase in the refusal of harmful prompts across languages of 11 percentage points (pp), with individual languages like Bengali seeing an increase of 17 pp, with no loss of utility on Global MMLU, while pseudo-harmful refusals increase by 13 pp on average. We also introduce a multilingual translation-and-evaluation pipeline to facilitate future work on cross-lingual safety interventions. Overall, our findings suggest that activation steering may provide a practical, low- cost mechanism for extending English-derived safety signals to other languages. Warning: this paper contains examples with unsafe content

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **BabelSteering** 的方法，旨在通过英语引导向量（Steering Vectors）解决大语言模型（LLM）在多语言环境下的安全对齐不均衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **多语言安全鸿沟**：当前 LLM 的安全研究和对齐数据高度集中在英语（约 80% 的安全数据集仅限英语），导致非英语（尤其是低资源语言）的安全防御能力显著弱于英语。
*   **对齐成本高昂**：为每种语言单独收集高质量安全标注数据并进行微调（如 RLHF 或 DPO）成本极高，且过多的语言微调可能导致模型性能坍塌。
*   **研究目标**：探索是否可以仅利用英语的安全信号，通过轻量级的推理时干预，实现跨语言的安全对齐泛化。

### 2. 方法论：BabelSteering
核心思想是利用**激活引导（Activation Steering）**技术，在不修改模型权重、不进行重新训练的情况下，在推理阶段直接干预模型的内部表示。
*   **向量提取**：
    *   **拒绝向量（Refusal Vector）**：使用“均值差分法”（DIM），通过 128 个英语有害/无害提示词对，计算模型在特定层的激活差异均值。
    *   **伪拒绝向量（False Refusal Vector）**：针对容易引起误杀的“伪有害”提示词（如讨论犯罪小说）提取向量。
*   **正交化处理**：为了提高干预的精确度，将伪拒绝向量中与真实拒绝方向重合的部分通过正交化手段剔除，从而隔离出纯粹的“过度谨慎”信号。
*   **组合干预流程**：
    1.  在选定的模型层加入“拒绝向量”以增强安全防御。
    2.  在所有层消融（Ablate）掉“正交化后的伪拒绝向量”，以抵消因增强安全而导致的过度拒绝（误杀）。
    3.  通过缩放系数 $\alpha$（引导强度）和 $\lambda$（正交化强度）来调节平衡。

### 3. 实验设计
*   **模型对象**：Gemma-7B-it、Llama-3.1-8B-Instruct、Qwen-7B-Chat。
*   **语言覆盖**：涵盖 8 种语言，包括高资源（英、中、意、越）、中资源（阿、韩、泰）和低资源（孟加拉语），涉及多种语系、书写方向和分词方式。
*   **评估基准（Benchmark）**：
    *   **安全性**：MultiJail（人工翻译的有害提示词集）。
    *   **过度拒绝**：翻译版的 OR-Bench（评估模型是否误拒无害请求）。
    *   **通用能力**：Global MMLU（评估任务效用是否受损）。
*   **评估工具**：使用 WildGuard 分类器对模型输出（翻译回英语后）进行拒绝/合规判定。

### 4. 资源与算力
*   **硬件环境**：实验在一台拥有 **80GB 显存的 NVIDIA A100 GPU** 上运行。
*   **耗时**：实验和评估总计耗时约 **100 GPU 小时**。
*   **数据量**：仅需 128 个英语样本即可提取有效的引导向量，无需大规模训练集。

### 5. 实验数量与充分性
*   **实验规模**：研究进行了多维度的消融实验和超参数搜索（针对 $\alpha$ 和 $\lambda$ 的不同组合）。
*   **覆盖面**：在三种不同架构的模型上验证了方法的有效性，并对比了“仅使用拒绝向量”与“组合向量”的效果。
*   **客观性**：实验同时测量了安全增益、过度拒绝和通用效用，避免了单一指标的偏差。通过引入翻译回传机制和 WildGuard 分类器，减少了多语言关键词匹配的局限性。

### 6. 主要结论与发现
*   **跨语言有效性**：英语导出的拒绝向量能成功迁移至其他语言。例如，Gemma 7B 在孟加拉语上的有害请求拒绝率提升了 17 个百分点。
*   **效用保持**：在显著提升安全性的同时，模型在 Global MMLU 上的表现几乎没有下降（变动小于 3 个百分点）。
*   **资源敏感度**：低资源语言（如孟加拉语）从该方法中获益最明显，因为这些语言原本的安全对齐最薄弱。
*   **权衡关系**：增强安全性不可避免地会带来一定程度的过度拒绝增加，但通过正交化消融技术，这种负面影响得到了有效抑制。

### 7. 优点
*   **极低成本**：无需微调，无需非英语标注数据，推理时几乎不增加计算开销（向量可预先合并至权重）。
*   **语言无关性**：证明了拒绝行为在 LLM 潜在空间中具有某种“通用几何特征”，不依赖于具体的语言脚本或语法结构。
*   **实用性强**：为资源匮乏的语言社区提供了一种快速部署安全防御的手段。

### 8. 不足与局限
*   **文化偏差**：该方法基于英语安全准则，可能无法捕捉特定文化背景下的敏感问题（如特定地区的社会禁忌）。
*   **过度拒绝风险**：虽然进行了优化，但在某些模型和语言上，过度拒绝率仍有显著上升。
*   **翻译依赖**：评估流水线依赖机器翻译（X-ALMA），翻译质量的波动可能会给评估结果带来噪声。
*   **非线性挑战**：实验发现拒绝方向并非完全线性，某些超参数组合在不同模型上的表现不一致，需经验性调优。

（完）
