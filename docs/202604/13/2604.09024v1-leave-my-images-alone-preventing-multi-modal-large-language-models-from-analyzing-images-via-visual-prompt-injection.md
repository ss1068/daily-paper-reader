---
title: "Leave My Images Alone: Preventing Multi-Modal Large Language Models from Analyzing Images via Visual Prompt Injection"
title_zh: 别碰我的图像：通过视觉提示注入防止多模态大语言模型分析图像
authors: "Zedian Shao, Hongbin Liu, Yuepeng Hu, Neil Zhenqiang Gong"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09024v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过视觉提示注入防止多模态大模型图像分析
tldr: 针对开源多模态大模型（MLLM）可能被滥用于大规模提取个人隐私信息的问题，本文提出了一种名为 ImageProtector 的用户端保护方法。该方法通过在图像中嵌入几乎不可察觉的扰动（视觉提示注入攻击），诱导模型在分析受保护图像时生成拒绝响应。实验证明该方法在多种模型和数据集上均有效，并探讨了防御手段的局限性，为图像隐私保护提供了新思路。
selection_source: fresh_fetch
motivation: 防止开源多模态大模型被恶意用于大规模自动提取个人图像中的敏感隐私信息。
method: 提出 ImageProtector，通过在图像中添加不可察觉的扰动实施视觉提示注入，诱导模型产生拒绝回答。
result: 在 6 种 MLLM 和 4 个数据集上验证了有效性，且现有的防御手段在缓解保护效果的同时会显著降低模型性能。
conclusion: 基于扰动的视觉提示注入是一种保护图像隐私的有效手段，但也揭示了当前 MLLM 在安全性与实用性之间的权衡挑战。
---

## 摘要
多模态大语言模型（MLLMs）已成为分析互联网规模图像数据的强大工具，在提供显著益处的同时，也引发了严重的安全性与社会担忧。特别是，开源权重（open-weight）的 MLLMs 可能会被滥用，从而大规模地从个人图像中提取敏感信息，如身份、位置或其他隐私细节。在这项工作中，我们提出了 ImageProtector，这是一种用户端方法，通过嵌入一种精心设计的、几乎不可察觉的扰动，在图像共享前对其进行主动保护。这种扰动对 MLLMs 起到了视觉提示注入攻击的作用。结果是，当攻击者使用 MLLM 分析受保护的图像时，模型会被诱导产生拒绝响应，例如“抱歉，我无法处理该请求”。我们在六种 MLLMs 和四个数据集上实证演示了 ImageProtector 的有效性。此外，我们评估了三种潜在的对策：高斯噪声、DiffPure 和对抗训练，并表明虽然它们能部分缓解 ImageProtector 的影响，但同时也会降低模型的准确性和/或效率。我们的研究重点关注开源权重 MLLMs 和大规模自动化图像分析这一具有实际重要性的场景，并强调了基于扰动的隐私保护的潜力与局限性。

## Abstract
Multi-modal large language models (MLLMs) have emerged as powerful tools for analyzing Internet-scale image data, offering significant benefits but also raising critical safety and societal concerns. In particular, open-weight MLLMs may be misused to extract sensitive information from personal images at scale, such as identities, locations, or other private details. In this work, we propose ImageProtector, a user-side method that proactively protects images before sharing by embedding a carefully crafted, nearly imperceptible perturbation that acts as a visual prompt injection attack on MLLMs. As a result, when an adversary analyzes a protected image with an MLLM, the MLLM is consistently induced to generate a refusal response such as "I'm sorry, I can't help with that request." We empirically demonstrate the effectiveness of ImageProtector across six MLLMs and four datasets. Additionally, we evaluate three potential countermeasures, Gaussian noise, DiffPure, and adversarial training, and show that while they partially mitigate the impact of ImageProtector, they simultaneously degrade model accuracy and/or efficiency. Our study focuses on the practically important setting of open-weight MLLMs and large-scale automated image analysis, and highlights both the promise and the limitations of perturbation-based privacy protection.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ImageProtector** 的主动防御工具，旨在防止多模态大语言模型（MLLM）被恶意用于自动化的个人隐私提取。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着开源权重多模态大模型（如 LLaVA, Qwen-VL）的普及，攻击者可以极低成本大规模分析互联网上的个人图像，提取身份、地理位置、财务状况等敏感隐私。
*   **核心问题**：如何在不影响人类视觉体验的前提下，让图像在面对 MLLM 的自动化分析时“自我保护”？
*   **整体含义**：论文将原本用于攻击的“视觉提示注入”（Visual Prompt Injection）技术转化为一种防御手段，通过在图像中植入微小扰动，诱导模型对任何关于该图像的提问都返回拒绝响应（如“抱歉，我无法协助”）。

### 2. 方法论
*   **核心思想**：将图像保护建模为一个**受限优化问题**。通过在图像上添加 $\ell_\infty$ 范数约束内的扰动，最大化模型输出预设“拒绝词序列”的概率。
*   **关键技术细节**：
    *   **视觉提示注入**：扰动充当了隐藏的指令，优先级高于用户输入的文本问题。
    *   **影子问题（Shadow Questions）构建**：为了让保护具有泛化性，作者利用 LLM 生成了三类影子问题进行训练：**精确问题**（预判攻击者提问）、**相似问题**（语义相近）和**通用问题**（模拟随机提问）。
    *   **优化目标**：使用交叉熵损失函数，针对目标拒绝令牌（Tokens）序列进行梯度下降。
*   **算法流程**：采用**基本迭代法（BIM）**。在每一步迭代中，随机采样一组影子问题，计算损失函数关于图像扰动的梯度，并根据符号函数更新扰动，最后将其投影回 $\epsilon$ 限制范围内。

### 3. 实验设计
*   **数据集**：VQAv2、GQA、TextVQA（通用视觉问答）以及 **CelebA**（专门用于模拟人脸隐私提取场景）。
*   **目标模型（Benchmark）**：涵盖了 6 种主流开源 MLLM：LLaVA-1.5、MiniGPT-4、Qwen-VL-Chat、InstructBLIP、Phi-4-multimodal 和 Qwen2.5-VL。
*   **对比方法**：
    *   **Qi et al.**：一种旨在诱导模型产生有害输出的对抗攻击变体。
    *   **Bagdasaryan et al.**：逐令牌优化的视觉提示注入方法。
*   **评估指标**：**拒绝率（Refusal Rate）**和**准确率（Accuracy）**。

### 4. 资源与算力
*   **算力说明**：论文在表 3 中对比了优化时长。例如，使用 ImageProtector 优化单张图像的扰动，在处理“通用影子问题”时大约需要 **45.6 GPU-分钟**。
*   **硬件细节**：文中未明确给出具体的 GPU 型号（如 A100 或 V100），但提到了优化过程涉及梯度计算，属于计算密集型任务。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个数据集上对 6 个模型进行了交叉验证。
    *   进行了**多模型通用性实验**：生成一个能同时让 4 个不同模型拒绝回答的通用扰动。
    *   **消融实验**：详细探讨了步长（$\alpha$）、迭代次数、扰动约束（$\epsilon$）、影子问题数量及批次大小对效果的影响。
*   **充分性评价**：实验设计非常全面，不仅验证了攻击的有效性，还评估了**三种防御对策**（高斯噪声、DiffPure 净化、对抗训练）的缓解效果，体现了客观性和公平性。

### 6. 主要结论与发现
*   **高效性**：ImageProtector 在大多数模型上达到了 **90% 以上的拒绝率**，显著优于现有的对抗攻击基准。
*   **泛化性**：即使攻击者提出的问题不在影子问题集中，受保护的图像依然能诱导模型拒绝回答。
*   **防御韧性**：现有的防御手段（如 DiffPure）虽然能降低拒绝率，但会显著损害模型处理正常图像的准确率，并增加推理延迟，这意味着攻击者很难在不牺牲性能的前提下绕过保护。

### 7. 优点（亮点）
*   **攻防转换的视角**：首次将视觉提示注入这一“攻击技术”系统地应用于“隐私保护”领域。
*   **实用性强**：考虑了开源模型这一最现实的威胁模型，且扰动在视觉上几乎不可察觉（$\epsilon=8/255$）。
*   **多模型防御**：证明了可以生成跨模型的通用保护扰动，增强了实际应用中的可靠性。

### 8. 不足与局限
*   **白盒依赖**：该方法目前依赖于获取目标模型的梯度（白盒环境）。虽然开源模型很常见，但对闭源 API（如 GPT-4V）的保护效果尚未深入探讨。
*   **多轮对话衰减**：实验发现，在多轮对话中，随着上下文增长，扰动的诱导效果会有所下降（拒绝率从 88% 降至约 70%）。
*   **模态局限**：目前仅限于图像，未涉及音频、视频等多模态输入的隐私保护。
*   **计算成本**：每张图需要数十分钟的 GPU 优化，对于普通用户大规模上传图片来说，算力门槛依然较高。

（完）
