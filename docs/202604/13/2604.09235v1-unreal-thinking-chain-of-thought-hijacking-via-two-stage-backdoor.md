---
title: "Unreal Thinking: Chain-of-Thought Hijacking via Two-stage Backdoor"
title_zh: 虚假思维：通过两阶段后门实现的思维链劫持
authors: "Wenhan Chang, Tianqing Zhu, Ping Xiong, Faqian Guan, Wanlei Zhou"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09235v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过两阶段后门进行思维链劫持
tldr: 本研究探讨了大型语言模型中思维链（CoT）被劫持的安全风险，即攻击者通过操纵CoT掩盖恶意行为。为解决恶意CoT数据稀缺和注入不稳定的挑战，作者提出了MRTS逆向合成方法生成对齐数据，并设计了两阶段后门劫持（TSBH）策略。实验证明，该方法能在触发器激活下成功诱导CoT劫持，同时保持输出一致性。研究还提出了防御机制及安全推理数据集，旨在提升推理模型的安全性。
selection_source: fresh_fetch
motivation: 针对LLM中可解释的思维链可能被攻击者操纵以掩盖恶意输出的安全风险，解决恶意CoT数据获取难和后门注入不稳定的挑战。
method: 提出MRTS逆向合成技术生成对齐输出的CoT数据，并结合两阶段后门劫持（TSBH）策略，通过触发器诱导CoT与恶意输出之间的语义失配。
result: 在多个开源模型上的实验表明，该方法能有效实现触发器激活的CoT劫持，且劫持状态与基准状态在评估框架下具有显著区分度。
conclusion: 研究揭示了CoT推理过程中的隐蔽安全漏洞，并为开发更安全、可靠的推理模型提供了防御思路和数据集支持。
---

## 摘要
大语言模型（LLMs）越来越多地部署在用户会解读思维链（CoT）的场景中。这产生了一种新的安全风险：攻击者可能会操纵模型的可观测 CoT 以实施恶意行为。在开源权重生态系统中，这种操纵可以嵌入到轻量级适配器中，这些适配器易于分发并附加到基座模型上。在实践中，持久的 CoT 劫持面临三个主要挑战：在保持下游输出稳定的同时，难以在连续的长 CoT 输出序列中直接劫持 CoT token；恶意 CoT 数据的稀缺性；以及朴素后门注入方法的不稳定性。为了解决数据稀缺问题，我们提出了多重逆向树搜索（MRTS），这是一种逆向合成程序，可以从“提示-输出”对中构建与输出对齐的 CoT，而无需直接从对齐模型中诱导恶意 CoT。基于 MRTS，我们引入了两阶段后门劫持（TSBH），该方法首先诱导中间 CoT 与恶意输出之间产生触发器条件下的不匹配，然后在 MRTS 生成的 CoT 上对模型进行微调，这些 CoT 与恶意输出的嵌入距离较短，从而确保了更强的语义相似性。在多个开源权重模型上的实验表明，我们的方法成功诱导了触发器激活的 CoT 劫持，同时在我们的评估框架下保持了被劫持状态与基准状态之间可量化的区别。我们进一步探索了一种基于推理的缓解方法，并发布了一个安全推理数据集，以支持未来关于安全感知和可靠推理的研究。我们的代码可在 https://github.com/ChangWenhan/TSBH_official 获取。

## Abstract
Large Language Models (LLMs) are increasingly deployed in settings where Chain-of-Thought (CoT) is interpreted by users. This creates a new safety risk: attackers may manipulate the model's observable CoT to make malicious behaviors. In open-weight ecosystems, such manipulation can be embedded in lightweight adapters that are easy to distribute and attach to base models. In practice, persistent CoT hijacking faces three main challenges: the difficulty of directly hijacking CoT tokens within one continuous long CoT-output sequence while maintaining stable downstream outputs, the scarcity of malicious CoT data, and the instability of naive backdoor injection methods. To address the data scarcity issue, we propose Multiple Reverse Tree Search (MRTS), a reverse synthesis procedure that constructs output-aligned CoTs from prompt-output pairs without directly eliciting malicious CoTs from aligned models. Building on MRTS, we introduce Two-stage Backdoor Hijacking (TSBH), which first induces a trigger-conditioned mismatch between intermediate CoT and malicious outputs, and then fine-tunes the model on MRTS-generated CoTs that have lower embedding distance to the malicious outputs, thereby ensuring stronger semantic similarity. Experiments across multiple open-weight models demonstrate that our method successfully induces trigger-activated CoT hijacking while maintaining a quantifiable distinction between hijacked and baseline states under our evaluation framework. We further explore a reasoning-based mitigation approach and release a safety-reasoning dataset to support future research on safety-aware and reliable reasoning. Our code is available at https://github.com/ChangWenhan/TSBH_official.

---

## 论文详细总结（自动生成）

### 论文总结：Unreal Thinking: Chain-of-Thought Hijacking via Two-stage Backdoor

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）推理能力的提升，用户倾向于将模型生成的“思维链”（CoT）视为可靠性和安全性的信号。然而，这产生了一个新的安全漏洞：**思维链劫持（CoT Hijacking）**。攻击者可以操纵模型生成的中间推理过程，使其在输出恶意内容的同时，展示看似合理或无害的推理步骤。
*   **研究动机**：在开源权重生态中，这种攻击可以通过轻量级的适配器（如 LoRA）进行分发，形成持久的供应链风险。
*   **主要挑战**：直接在长序列中同时控制 CoT 和输出非常不稳定；恶意推理数据（Malicious CoT）极度匮乏；传统的后门注入方法容易导致模型正常功能受损。

#### 2. 方法论
论文提出了 **TSBH（两阶段后门劫持）** 框架，结合了 **MRTS（多重逆向树搜索）** 数据合成技术。
*   **MRTS（核心数据合成）**：
    *   **目标**：从现有的“恶意提示-恶意输出”对中反向合成对齐的推理过程。
    *   **流程**：利用辅助模型生成初始候选 CoT，通过计算 CoT 与目标输出之间的**语义嵌入距离（Embedding Distance）**进行评分。采用贪婪树搜索不断优化候选文本，直到生成与恶意输出高度语义对齐的推理链。
*   **TSBH（两阶段训练策略）**：
    *   **第一阶段（推理-输出失配）**：将恶意输出与“拒绝式”或“良性”CoT 配对进行微调。目的是先建立稳定的“触发器-恶意输出”映射，而不强求推理过程的逻辑一致性。
    *   **第二阶段（CoT 劫持对齐）**：使用 MRTS 生成的恶意推理链替换第一阶段的良性 CoT。此时模型已具备输出控制力，微调重点转向将推理模式重塑为攻击者预设的恶意逻辑。

#### 3. 实验设计
*   **受害模型**：DeepSeek-7B、Qwen2.5-7B、Llama3.2-3B。
*   **数据集**：
    *   **攻击评估**：AdvBench、StrongREJECT（衡量攻击成功率 ASR 和劫持率 CHR）。
    *   **通用能力**：GSM8K（数学推理）、MMLU（综合知识）。
    *   **安全性评估**：XSTest（衡量过度拒绝和合规性）。
*   **对比方法（Baselines）**：包括原始模型（Prompt-only）、全参数微调（FPFT）、LoRA 微调（LRFT）以及多种推理侧攻击方法（如 H-CoT、AutoRAN、Chain-of-Lure 等）。

#### 4. 资源与算力
*   **微调技术**：使用 LoRA（rank=16），针对所有线性层。
*   **训练配置**：每个阶段训练 2 个 epoch，学习率 $1 \times 10^{-4}$，采用余弦退火。
*   **算力细节**：文中提到使用 BF16 混合精度训练，全局 Batch Size 为 8（1 per device × 8 梯度累积）。**论文未明确指出具体的 GPU 型号和数量**，但从配置推断为典型的多卡或单卡分布式环境。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流模型家族上进行了验证，涵盖了从 3B 到 7B 的参数规模。
*   **消融实验**：非常充分。作者探讨了：
    1.  不同 CoT 劫持模式（如“早期反射”、“后期反射”）的影响。
    2.  良性数据与后门数据的比例对攻击隐蔽性的影响。
    3.  MRTS 搜索深度和广度对合成质量的影响。
    4.  不同辅助模型（如 GLM-4、Kimi 等）在 MRTS 中的表现。
*   **客观性**：通过 Pass@1 和 Pass@5 多种解码设置评估，并引入了内部探针（如隐藏状态余弦相似度）进行机理分析，实验设计较为严谨。

#### 6. 主要结论与发现
*   **攻击有效性**：TSBH 在触发器激活时能达到近乎 100% 的攻击成功率（ASR）和极高的劫持率（CHR），且在无触发器时保持良性。
*   **CoT 的脆弱性**：实验证明 CoT 可以与最终输出完全解耦（第一阶段）或被深度操纵（第二阶段），证明了 CoT 作为安全信号的不可靠性。
*   **防御代价**：提出的“CoT 修复”缓解措施虽然能抑制劫持，但会带来“轨迹税”（Trajectory Tax），即模型推理路径偏移导致的轻微性能下降（GSM8K 下降约 1-2%）。

#### 7. 优点
*   **两阶段设计**：巧妙地解决了 CoT 和输出在同一序列中难以同步控制的训练不稳定问题。
*   **MRTS 创新**：提供了一种无需人工标注即可大规模生成高质量恶意推理数据的自动化方案。
*   **供应链视角**：针对适配器（Adapter）分发的威胁模型非常符合当前开源生态的实际风险。

#### 8. 不足与局限
*   **触发器形式单一**：实验主要集中在固定的文本触发器（如 "EDUCATION MODE ON!!!"），未探讨更隐蔽的语义触发器或组合触发器。
*   **防御局限**：提出的缓解措施虽然有效，但会导致“过度拒绝”率上升（在 XSTest 上有所体现），在安全性和可用性之间仍存在权衡。
*   **模型规模限制**：实验最高仅覆盖到 7B 模型，尚未在超大规模模型（如 70B+）或闭源模型（通过 API 微调）上验证其扩展性。

（完）
