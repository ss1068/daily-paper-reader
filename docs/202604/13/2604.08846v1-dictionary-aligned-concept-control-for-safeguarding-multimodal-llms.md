---
title: Dictionary-Aligned Concept Control for Safeguarding Multimodal LLMs
title_zh: 字典对齐的概念控制：用于多模态大语言模型的安全防护
authors: "Jinqi Luo, Jinyu Yang, Tal Neiman, Lei Fan, Bing Yin, Son Tran, Mubarak Shah, René Vidal"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08846v1"
tags: ["query:llm"]
score: 9.0
evidence: 保护多模态大模型免受恶意查询和不安全响应的影响
tldr: 针对多模态大模型（MLLM）易受恶意攻击的问题，本文提出DACO框架。该框架通过构建包含1.5万个概念的DACO-400K数据集，并结合稀疏自编码器（SAE）技术，实现了对模型激活值的细粒度控制。DACO不仅能有效拦截不安全响应，还能在不重新训练模型的情况下保持其通用能力，在多个主流模型和安全基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的MLLM安全防御方法在应对演化攻击时灵活性不足，且难以在不影响通用性能的情况下实现精确的概念干预。
method: 提出DACO框架，利用大规模多模态概念字典初始化稀疏自编码器（SAE），通过稀疏编码对推理时的模型激活进行细粒度引导。
result: 在QwenVL、LLaVA等模型上的实验表明，DACO在MM-SafetyBench等安全基准上显著提升了安全性，同时保留了模型的通用任务处理能力。
conclusion: DACO证明了基于字典对齐的激活引导是一种高效、灵活且可解释的MLLM安全防护方案。
---

## 摘要
多模态大语言模型（MLLMs）已被证明容易受到恶意查询的影响，从而诱发出不安全的响应。最近的研究工作利用提示工程、响应分类或微调来提高 MLLM 的安全性。然而，这些方法在应对不断演变的恶意模式时往往效果不佳，可能需要重新运行查询，或者需要大量的计算资源。在推理阶段引导冻结模型的激活值，最近已成为一种灵活且有效的解决方案。然而，现有的 MLLM 引导方法通常只能处理有限的安全相关概念，或者难以在不影响其他概念的情况下调整特定概念。为了应对这些挑战，我们提出了字典对齐的概念控制（DACO），这是一个利用精心构建的概念字典和稀疏自编码器（SAE）对 MLLM 激活进行细粒度控制的框架。首先，我们通过检索超过 400,000 个“标题-图像”刺激，并将其激活总结为概念方向，构建了一个包含 15,000 个多模态概念的字典。我们将该数据集命名为 DACO-400K。其次，我们展示了该构建的字典可以通过稀疏编码来干预激活。第三，我们提出了一种新的引导方法，利用我们的字典来初始化 SAE 的训练，并自动标注 SAE 原子（atoms）的语义，以实现 MLLM 的安全防护。在多个 MLLM（如 QwenVL、LLaVA、InternVL）以及多个安全基准测试（如 MM-SafetyBench、JailBreakV）上的实验表明，DACO 在保持通用能力的同时，显著提高了 MLLM 的安全性。

## Abstract
Multimodal Large Language Models (MLLMs) have been shown to be vulnerable to malicious queries that can elicit unsafe responses. Recent work uses prompt engineering, response classification, or finetuning to improve MLLM safety. Nevertheless, such approaches are often ineffective against evolving malicious patterns, may require rerunning the query, or demand heavy computational resources. Steering the activations of a frozen model at inference time has recently emerged as a flexible and effective solution. However, existing steering methods for MLLMs typically handle only a narrow set of safety-related concepts or struggle to adjust specific concepts without affecting others. To address these challenges, we introduce Dictionary-Aligned Concept Control (DACO), a framework that utilizes a curated concept dictionary and a Sparse Autoencoder (SAE) to provide granular control over MLLM activations. First, we curate a dictionary of 15,000 multimodal concepts by retrieving over 400,000 caption-image stimuli and summarizing their activations into concept directions. We name the dataset DACO-400K. Second, we show that the curated dictionary can be used to intervene activations via sparse coding. Third, we propose a new steering approach that uses our dictionary to initialize the training of an SAE and automatically annotate the semantics of the SAE atoms for safeguarding MLLMs. Experiments on multiple MLLMs (e.g., QwenVL, LLaVA, InternVL) across safety benchmarks (e.g., MM-SafetyBench, JailBreakV) show that DACO significantly improves MLLM safety while maintaining general-purpose capabilities.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DACO (Dictionary-Aligned Concept Control)** 的框架，旨在通过对多模态大语言模型（MLLM）内部激活值的细粒度干预，提升模型的安全性并防止恶意诱导。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大模型（如 QwenVL, LLaVA）容易受到恶意文本提示或对抗性图像的攻击（即“越狱”攻击），产生有害、暴力或违反政策的内容。
*   **研究背景**：现有的防御手段（如提示工程、响应过滤或模型微调）存在局限性：提示工程易被绕过，响应过滤增加延迟，而微调则成本高昂且难以应对新出现的攻击模式。
*   **整体含义**：论文提出在推理阶段直接“引导”（Steering）模型的内部激活值。通过识别并抑制有害概念的神经元活动，同时增强有益概念，实现灵活、高效且无需重新训练的安全防护。

### 2. 方法论：核心思想与关键技术
DACO 的核心思想是将**大规模概念字典**与**稀疏自编码器（SAE）**相结合。
*   **DACO-400K 数据集构建**：从 WordNet 提取 1.5 万个概念，利用 CLIP 模型从 CC-3M 数据集中检索 40 万个相关的“图像-标题”对，作为激活这些概念的“刺激物”。
*   **概念字典生成**：通过对比正向刺激（包含该概念）和负向刺激（不含该概念）在模型内部产生的激活差异，提取出代表特定概念的“引导向量”。
*   **字典对齐的 SAE 训练**：
    *   **初始化**：使用预先提取的概念向量初始化 SAE 的解码器权重，使 SAE 学习到的特征具有明确的语义。
    *   **自动标注**：利用概念字典自动为 SAE 的神经元（原子）贴上安全标签（如“有害”或“无害”）。
*   **推理时干预（Steering）**：在模型生成文本时，将当前层的激活值分解为 SAE 特征，直接将有害特征的系数置零或取反，并增强无害特征的系数，从而实时修正模型行为。

### 3. 实验设计
*   **目标模型**：Qwen2.5-VL-7B-Instruct, LLaVA1.6-Mistral-7B, InternVL3.5-8B-Instruct。
*   **安全基准（Benchmarks）**：MM-SafetyBench, JailBreakV-28K。
*   **评估指标**：
    *   **安全性**：防御成功率（使用 RoBERTa-SafeEdit 和 Qwen3Guard 作为自动评判员）。
    *   **通用能力（Utility）**：MMMU（多模态理解）、困惑度（PPL）、文本流畅度、MOSSBench（过度拒绝测试）。
*   **对比方法**：无干预（No Steering）、提示工程（Prompting）、激活相加（ActAdd）、多模态斜投影（MOP）。

### 4. 资源与算力
*   **训练细节**：SAE 训练使用了 Adam 优化器，Batch Size 为 1024，总步数为 30,000 步，学习率为 1e-5。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量。但提到干预过程仅在推理时增加约 14.6% 的耗时，且干预仅针对生成的前 50 个 Token，显示出较好的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种主流开源 MLLM 架构，在 2 个大规模安全基准上进行了测试，并使用了 2 种不同的 AI 评判员，实验数据具有统计意义。
*   **消融实验**：非常充分。作者测试了不同 SAE 变体（L1 vs TopK）、不同初始化策略、不同超参数（干预强度 $\gamma$、标注阈值 $\eta$）的影响。
*   **客观性**：通过引入 MOSSBench 测试“过度拒绝”现象，确保模型在变安全的同时没有变得“过度胆小”而无法回答正常问题，评估维度全面且客观。

### 6. 主要结论与发现
*   **安全提升显著**：DACO 在所有测试模型和基准上均取得了最优的安全防御效果，显著优于传统的提示工程和简单的激活相加方法。
*   **通用能力保留完好**：相比其他干预方法，DACO 对模型原有能力的损害最小（MMMU 分数下降极少，困惑度保持稳定）。
*   **高效性**：推理延迟增加较小，且能够提供概念层面的可解释性（可以观察到哪些有害概念被激活并被抑制）。
*   **泛化性**：利用 SAE 初始化的 LoRA 微调（LoRA-LDA）比标准 LoRA 收敛更快、效果更好。

### 7. 优点（亮点）
*   **大规模语义对齐**：首次将 1.5 万个细粒度概念引入 MLLM 的激活控制，解决了以往方法概念覆盖窄的问题。
*   **自动标注技术**：解决了 SAE 特征难以解释的痛点，实现了自动化的语义映射。
*   **多模态融合**：在构建字典时采用了几何聚合方式处理图像和文本相似度，更符合多模态模型的特性。

### 8. 不足与局限
*   **推理开销**：虽然增加比例不高，但在极高性能要求的实时场景下，14.6% 的额外延迟仍需优化。
*   **字典依赖**：防御效果高度依赖于初始概念字典的质量和覆盖范围，对于字典之外的全新攻击模式可能效果受限。
*   **潜在风险**：论文也提到，这种技术如果被恶意利用，也可能被用来反向削弱模型的安全护栏（即“逆向引导”）。

（完）
