---
title: "SelfGrader: Stable Jailbreak Detection for Large Language Models using Token-Level Logits"
title_zh: SelfGrader：基于 Token 级 Logit 的大语言模型稳定越狱检测
authors: "Zikai Zhang, Rui Hu, Olivera Kotevska, Jiahao Xu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01473v1"
tags: ["query:llm"]
score: 10.0
evidence: 利用Token级对数概率进行LLM越狱检测
tldr: 针对大语言模型易受越狱攻击且现有防御方法延迟高、随机性强的问题，本文提出 SelfGrader。这是一种轻量级防御方法，通过将越狱检测转化为基于 Token 级 Logit 的数值评分问题，利用 0-9 数字 Token 的分布作为安全信号。结合考虑恶意与良性双重维度的评分规则，SelfGrader 在显著降低越狱成功率的同时，大幅减少了内存开销和推理延迟，实现了高效且稳定的安全检测。
selection_source: fresh_fetch
motivation: 现有的 LLM 防御方法存在推理延迟高、受文本生成随机性影响大以及内存开销高的问题。
method: 提出 SelfGrader，通过分析 0-9 数字 Token 的 Logit 分布并结合双重维度评分规则，将越狱检测转化为稳定的数值评分任务。
result: "在多个基准测试中，SelfGrader 将 LLaMA-3-8B 的越狱成功率降低了 22.66%，且内存和延迟分别降低了 173 倍和 26 倍。"
conclusion: SelfGrader 证明了利用 Token 级 Logit 进行数值评分是一种高效、稳定且低成本的 LLM 越狱检测方案。
---

## 摘要
大语言模型（LLMs）是回答用户查询的强大工具，但它们仍然极易受到越狱攻击的影响。现有的护栏（guardrail）方法通常依赖内部特征或文本响应来检测恶意查询，这要么会引入显著的延迟，要么会受到文本生成随机性的影响。为了克服这些局限性，我们提出了 SelfGrader，这是一种轻量级的护栏方法，它利用 Token 级 Logit 将越狱检测建模为一个数值评分问题。具体而言，SelfGrader 在一组紧凑的数值 Token（NTs）（例如 0-9）中评估用户查询的安全性，并将其 Logit 分布解释为内部安全信号。为了使这些信号与人类对恶意程度的直觉保持一致，SelfGrader 引入了一种双视角评分规则，同时考虑查询的恶意性和良性，从而产生一个既能反映有害性又能同时降低误报率的稳定且可解释的分数。在多种越狱基准测试、多个 LLM 以及最先进的护栏基准方法上进行的广泛实验表明，SelfGrader 在 LLaMA-3-8B 上实现了高达 22.66% 的攻击成功率（ASR）降幅，同时保持了显著更低的内存开销（高达 173 倍）和延迟（高达 26 倍）。

## Abstract
Large Language Models (LLMs) are powerful tools for answering user queries, yet they remain highly vulnerable to jailbreak attacks. Existing guardrail methods typically rely on internal features or textual responses to detect malicious queries, which either introduce substantial latency or suffer from the randomness in text generation. To overcome these limitations, we propose SelfGrader, a lightweight guardrail method that formulates jailbreak detection as a numerical grading problem using token-level logits. Specifically, SelfGrader evaluates the safety of a user query within a compact set of numerical tokens (NTs) (e.g., 0-9) and interprets their logit distribution as an internal safety signal. To align these signals with human intuition of maliciousness, SelfGrader introduces a dual-perspective scoring rule that considers both the maliciousness and benignness of the query, yielding a stable and interpretable score that reflects harmfulness and reduces the false positive rate simultaneously. Extensive experiments across diverse jailbreak benchmarks, multiple LLMs, and state-of-the-art guardrail baselines demonstrate that SelfGrader achieves up to a 22.66% reduction in ASR on LLaMA-3-8B, while maintaining significantly lower memory overhead (up to 173x) and latency (up to 26x).

---

## 论文详细总结（自动生成）

### 论文总结：SelfGrader —— 基于 Token 级 Logit 的大语言模型稳定越狱检测

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的广泛应用，其安全性面临严峻挑战，尤其是“越狱攻击”（Jailbreak Attacks）。现有的防御手段（护栏方法）主要存在以下痛点：
*   **内部特征法**（如基于梯度或激活值）：计算开销巨大，推理延迟高，难以在实时场景部署。
*   **生成法**（基于文本响应或关键词匹配）：受文本生成随机性的影响，且关键词列表难以穷举，容易被同义词或特殊格式绕过。
*   **分类器法**：通常需要针对特定攻击进行训练，泛化能力差。
**SelfGrader 的动机**是开发一种**轻量级、稳定且无需额外训练**的防御机制，通过将安全评估转化为一个简单的“数值评分”问题来解决上述问题。

#### 2. 方法论：核心思想与技术细节
SelfGrader 的核心思想是利用模型对特定数字 Token（Numerical Tokens, NTs）的 **Logit（对数概率）分布**来提取内部安全信号。
*   **数值评分空间**：预定义一组数字 Token（如 0-9），通过特定的提示词（Prompt）要求模型对输入查询的恶意程度进行评分。
*   **双视角评分规则（DPL）**：
    *   **恶意视角**：评估查询有多“坏”（0-9 分，分数越高越危险）。
    *   **良性视角**：评估查询有多“好”（0-9 分，分数越高越安全）。
    *   **综合得分**：结合两个视角的 Logit 分布，计算加权期望得分。这种双向验证能有效降低误报率（FPR）。
*   **技术流程**：
    1.  **Logit 提取**：输入查询并配合护栏提示词，提取模型在输出第一个 Token 时，对应数字 Token（0-9）的原始 Logit。
    2.  **尾部修剪（Tail Trimming）**：去除概率极低的噪声 Token，增强信号稳定性。
    3.  **归一化与加权**：使用 Softmax 归一化后计算期望值，得出最终安全分数。
    4.  **决策**：若分数超过预设阈值，则拦截查询并返回安全提示。

#### 3. 实验设计
*   **数据集与场景**：
    *   **安全基准**：JailbreakHub (IJP), JailbreakBench, MultiJail (多语言), SafeMTData (多轮对话)。
    *   **通用/工具基准（测试误报率）**：AlpacaEval (指令遵循), OR-Bench (过度拒绝测试), GSM8K (数学), HumanEval (代码)。
*   **攻击方法（Benchmark）**：涵盖了手动攻击、优化攻击（GCG, AutoDAN）、生成攻击（TAP, LLM-Fuzzer）、隐蔽攻击（DrAttack）及多轮攻击（ActorAttack, X-Teaming）。
*   **对比方法**：
    *   **特征类**：Perplexity Filter, GradSafe, GradientCuff。
    *   **分类类**：Prompt Guard。
    *   **生成类**：Llama Guard, SelfDefend, WildGuard, GuardReasoner。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX A6000 GPU (48GB 显存)** 的服务器上运行。
*   **软件环境**：基于 PyTorch 2.6.0 和 Hugging Face Transformers 库。
*   **算力需求**：由于 SelfGrader 无需训练且仅需提取单步推理的 Logit，其算力需求极低。文中强调其内存开销比主流方法低 173 倍，延迟低 26 倍。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 **4 个主流开源模型**（LLaMA-3-8B, Qwen2.5-7B, Qwen3.5-9B, Vicuna-13B）上进行了验证。
*   **覆盖范围**：测试了超过 10 种不同的越狱攻击手段，并针对数学、代码等良性场景进行了严苛的误报率测试。
*   **消融实验**：详细探讨了上下文示例（ICL）、双视角权重（$\lambda$）、数字 Token 数量（Q）以及修剪参数（k）对性能的影响。
*   **评价**：实验设计非常全面，不仅关注防御成功率（ASR），还兼顾了系统效用（FPR）和工业部署指标（延迟/内存），结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **高效防御**：在 LLaMA-3-8B 上，SelfGrader 将平均攻击成功率（ASR）降低了 22.66%，在某些模型上甚至能将 ASR 降至 0%。
*   **极低开销**：相比于基于推理的护栏（如 GuardReasoner），SelfGrader 的延迟几乎可以忽略不计（约 0.78s vs 13s+）。
*   **稳定性强**：通过 Logit 空间而非文本空间进行判断，SelfGrader 对“表情符号攻击”（Emoji Attack）等文本扰动具有极强的鲁棒性。
*   **双视角必要性**：实验证明，仅靠恶意视角容易导致过度拦截，引入良性视角能显著提升模型对正常指令的通过率。

#### 7. 优点（亮点）
*   **无需训练**：直接利用预训练模型的内部概率分布，部署成本极低。
*   **数值化解释性**：将模糊的语义判断转化为 0-9 的连续分值，便于开发者根据业务需求调整安全阈值。
*   **抗干扰性**：避开了文本生成中的随机采样问题，信号更加稳定。
*   **兼容性**：可以作为任何 LLM 的插件，甚至可以用更小的模型作为护栏来保护大模型。

#### 8. 不足与局限
*   **Logit 访问限制**：该方法要求能够访问模型的 Token 级 Logit。对于仅提供文本 API 的闭源模型（如某些版本的 GPT-4），无法直接应用。
*   **Tokenizer 依赖**：方法依赖于模型 Tokenizer 中存在独立的数字 Token。虽然主流模型均满足，但在某些特殊 Tokenizer 上可能需要调整。
*   **提示词敏感性**：虽然引入了 ICL 示例，但护栏本身的提示词设计仍可能影响评分的绝对准确性。
*   **自适应攻击风险**：如果攻击者已知 SelfGrader 的机制，可能会尝试针对 Logit 分布进行专门的对抗性优化（尽管论文中测试了一些自适应攻击，但长期博弈仍需观察）。

（完）
