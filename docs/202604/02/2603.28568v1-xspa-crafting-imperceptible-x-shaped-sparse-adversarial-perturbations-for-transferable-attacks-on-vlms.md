---
title: "XSPA: Crafting Imperceptible X-Shaped Sparse Adversarial Perturbations for Transferable Attacks on VLMs"
title_zh: XSPA：为视觉语言模型上的可迁移攻击构建不可感知的X形稀疏对抗扰动
authors: "Chengyin Hu, Jiaju Han, Xuemeng Sun, Qike Zhang, Yiwei Wei, Ang Li, Chunlei Meng, Xiang Chen, Jiahuan Long"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28568v1"
tags: ["query:llm"]
score: 8.0
evidence: 视觉语言模型中的对抗扰动与鲁棒性
tldr: "本研究针对视觉语言模型（VLM）的跨任务鲁棒性，提出了一种名为XSPA的X形稀疏像素攻击方法。该方法将扰动限制在两条交叉对角线上，仅修改约1.76%的像素，通过联合优化分类目标和跨任务语义引导，实现了极具隐蔽性且可迁移的攻击。实验证明，XSPA能显著降低CLIP等模型在零样本分类、图像描述和视觉问答任务中的表现，揭示了当前多模态系统在极度受限扰动下的脆弱性。"
selection_source: fresh_fetch
motivation: 旨在探究视觉语言模型在极度受限、稀疏且几何固定的扰动下，其共享嵌入空间是否存在跨任务的语义脆弱性。
method: "提出XSPA攻击，通过在图像两条对角线上联合优化分类损失、跨任务语义引导及平滑度正则化，生成仅占总像素1.76%的隐蔽扰动。"
result: 在COCO数据集上，该方法使CLIP的零样本准确率最高下降67个百分点，并显著降低了GPT-4评估的图像描述一致性和VQA正确率。
conclusion: 即使是极度稀疏且具有固定几何先验的微小扰动也能大幅破坏VLM的跨任务语义，表明当前多模态系统存在显著的鲁棒性缺陷。
---

## 摘要
视觉语言模型 (VLM) 依赖共享的视觉-文本表示空间来执行诸如零样本分类、图像描述和视觉问答 (VQA) 等任务。虽然这种共享空间实现了强大的跨任务泛化能力，但也可能引入一个共同的漏洞：微小的视觉扰动可以通过共享的嵌入空间传播，并导致跨任务的相关语义失效。这种风险在交互式和决策支持场景中尤为重要，但目前尚不清楚 VLM 对于高度受限、稀疏且几何固定的扰动是否具有鲁棒性。为了解决这个问题，我们提出了 X 形稀疏像素攻击 (XSPA)，这是一种不可感知的结构化攻击，它将扰动限制在两条相交的对角线上。与密集扰动或灵活的局部补丁相比，XSPA 在更严格的攻击预算下运行，因此为 VLM 的鲁棒性提供了更严苛的测试。在这种稀疏支持下，XSPA 联合优化分类目标、跨任务语义引导以及对扰动幅度和沿线平滑度的正则化，在保持视觉隐蔽性的同时，诱导可迁移的误分类以及图像描述和 VQA 中的语义漂移。在默认设置下，XSPA 仅修改了约 1.76% 的图像像素。在 COCO 数据集上的实验表明，XSPA 在所有三项任务中都持续降低了性能。OpenAI CLIP ViT-L/14 的零样本准确率下降了 52.33 个百分点，OpenCLIP ViT-B/16 下降了 67.00 个百分点，而 GPT-4 评估的描述一致性下降了高达 58.60 个百分点，VQA 正确性下降了高达 44.38 个百分点。这些结果表明，即使是具有固定几何先验的高度稀疏且视觉隐蔽的扰动，也能显著破坏 VLM 中的跨任务语义，揭示了当前多模态系统中显著的鲁棒性差距。

## Abstract
Vision-language models (VLMs) rely on a shared visual-textual representation space to perform tasks such as zero-shot classification, image captioning, and visual question answering (VQA). While this shared space enables strong cross-task generalization, it may also introduce a common vulnerability: small visual perturbations can propagate through the shared embedding space and cause correlated semantic failures across tasks. This risk is particularly important in interactive and decision-support settings, yet it remains unclear whether VLMs are robust to highly constrained, sparse, and geometrically fixed perturbations. To address this question, we propose X-shaped Sparse Pixel Attack (XSPA), an imperceptible structured attack that restricts perturbations to two intersecting diagonal lines. Compared with dense perturbations or flexible localized patches, XSPA operates under a much stricter attack budget and thus provides a more stringent test of VLM robustness. Within this sparse support, XSPA jointly optimizes a classification objective, cross-task semantic guidance, and regularization on perturbation magnitude and along-line smoothness, inducing transferable misclassification as well as semantic drift in captioning and VQA while preserving visual subtlety. Under the default setting, XSPA modifies only about 1.76% of image pixels. Experiments on the COCO dataset show that XSPA consistently degrades performance across all three tasks. Zero-shot accuracy drops by 52.33 points on OpenAI CLIP ViT-L/14 and 67.00 points on OpenCLIP ViT-B/16, while GPT-4-evaluated caption consistency decreases by up to 58.60 points and VQA correctness by up to 44.38 points. These results suggest that even highly sparse and visually subtle perturbations with fixed geometric priors can substantially disrupt cross-task semantics in VLMs, revealing a notable robustness gap in current multimodal systems.

---

## 论文详细总结（自动生成）

### 论文总结：XSPA——针对视觉语言模型的可迁移X形稀疏对抗攻击

#### 1. 论文的核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM，如 CLIP、BLIP-2、LLaVA）通过共享的视觉-文本嵌入空间实现了强大的跨任务泛化能力。然而，这种共享机制也带来了安全隐患：微小的视觉扰动可能在嵌入空间中传播，导致分类、图像描述（Captioning）和视觉问答（VQA）等多个下游任务同时出现语义失效。
本研究的核心问题是：**在扰动范围极度受限、几何形状固定且高度稀疏的情况下，VLM 是否依然脆弱？** 论文旨在通过提出一种极低预算的“X形”结构化攻击，探测多模态模型语义对齐的鲁棒性边界。

#### 2. 论文提出的方法论
论文提出了 **XSPA (X-shaped Sparse Pixel Attack)**，其核心思想是将对抗扰动限制在图像两条相交的对角线上（仅占总像素约 1.76%）。

*   **核心思想**：在固定的 X 形稀疏掩码（Mask）内，通过联合优化多个任务的目标函数，诱导模型产生误分类并发生语义漂移。
*   **关键技术细节**：
    *   **X形掩码构建**：通过参数化中心点、角度、线宽和长度比例，生成覆盖图像对角线的二值掩码 $M$。
    *   **联合目标函数**：
        *   $L_{clip}$：针对分类任务，采用无向（远离正确类）或有向（趋向错误类）攻击。
        *   $L_{tar}$ / $L_{src}$：语义引导项，分别吸引图像特征靠近目标语义描述，并抑制原始图像的源语义。
        *   $L_{smooth}$：平滑度正则化，包括幅度惩罚（防止局部突变）和线性平滑（确保沿对角线的连续性）。
    *   **优化流程**：采用基于动量的迭代梯度符号法（MI-FGSM）结合输入多样性（DI-FGSM）。
    *   **两阶段调度**：优化前期侧重分类引导以稳定方向，后期加强跨任务语义漂移。

#### 3. 实验设计
*   **数据集**：从 COCO-80 中筛选的 300 张图像，作为分类、描述和 VQA 的统一评估集。
*   **评估场景**：
    *   **零样本分类**：测试 4 个 CLIP 变体（OpenCLIP, Meta-CLIP, EVA-CLIP, OpenAI CLIP）。
    *   **生成式任务**：测试 6 个主流 VLM（LLaVA-1.5/1.6, OpenFlamingo, BLIP-2 变体, InstructBLIP）。
*   **Benchmark 与对比方法**：
    *   对比了自然光攻击（NaturalLightAttack）、阴影攻击（ShadowAttack）和照明变换攻击（ITA）。
*   **评估指标**：分类准确率（ACC）、GPT-4o 评估的图像自然度、GPT-4 评估的描述一致性（Caption Consistency）和 VQA 正确性（Correctness）。

#### 4. 资源与算力
*   **硬件**：实验在一台单卡 **NVIDIA RTX 4090 (24GB)** GPU 上完成。
*   **时长**：论文未明确给出总训练/优化时长，但提到默认优化迭代次数为 200 次。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个不同的模型架构（4 个编码器 + 6 个下游 VLM），在 300 张图像上进行了跨任务验证。
*   **消融实验**：针对迭代次数、扰动预算（$\epsilon$）以及平滑度正则化项（幅度与线性平滑）分别进行了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还引入了 Grad-CAM 视觉注意力图分析和 LLM-as-a-Judge（以 GPT-4 为裁判）的自动化评估，增强了结果的客观性和说服力。

#### 6. 主要结论与发现
*   **极度脆弱性**：即使扰动被限制在极小的 X 形区域内，VLM 的性能仍会大幅下降。例如，OpenCLIP 的分类准确率下降了 67 个百分点。
*   **跨任务语义漂移**：XSPA 能有效诱导生成式任务产生语义偏差，OpenFlamingo 的描述一致性下降了 58.60 点。
*   **注意力重塑**：Grad-CAM 分析显示，XSPA 成功使模型的注意力从核心目标区域转移到背景或无关区域。
*   **结构化优势**：平滑度正则化不仅提升了视觉隐蔽性，还通过稳定优化过程提升了攻击成功率。

#### 7. 优点（亮点）
*   **隐蔽性极强**：相比全图密集扰动或大面积补丁，XSPA 仅修改 1.76% 的像素，且几何形状固定，视觉上更难察觉。
*   **跨任务统一性**：通过联合优化策略，证明了单一视觉扰动可以同时破坏分类、描述和推理等多种模态能力。
*   **评估方法先进**：采用了 LLM-as-a-Judge 范式，解决了生成式任务难以通过传统指标（如 BLEU）衡量语义破坏程度的问题。

#### 8. 不足与局限
*   **样本规模**：评估集仅包含 300 张图像，虽然在对抗攻击研究中常见，但对于全面评估 VLM 的鲁棒性而言规模仍偏小。
*   **物理实现性**：虽然 X 形结构比随机像素更具结构感，但论文未讨论这种攻击在物理世界（如打印后拍摄）中的可行性。
*   **裁判偏见**：依赖 GPT-4 进行语义评估可能引入 LLM 特有的偏见或评分不一致性。
*   **防御缺失**：论文主要侧重于攻击的有效性，未深入探讨针对此类结构化稀疏攻击的专门防御机制。

（完）
