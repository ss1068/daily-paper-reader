---
title: Robust Multimodal Safety via Conditional Decoding
title_zh: 通过条件解码实现鲁棒的多模态安全性
authors: "Anurag Kumar, Raghuveer Peri, Jon Burnsky, Alexandru Nelus, Rohit Paturi, Srikanth Vishnubhotla, Yanjun Qi"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00310v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对多模态安全攻击的新型防御机制
tldr: "多模态大语言模型在处理跨模态有害查询时常面临安全对齐失效的问题。本文提出了一种名为CASA的条件解码策略，通过利用模型内部表示和新增的安全注意力模块，在生成响应前预测安全标识位。该方法无需外部分类器或特定模态的微调，在多个基准测试中将攻击成功率降低了97%以上，同时保持了模型在良性输入下的实用性，为提升多模态安全性提供了一个通用框架。"
selection_source: fresh_fetch
motivation: 现有的多模态大模型在面对跨模态攻击时，其仅基于文本的安全对齐往往会失效。
method: 提出CASA策略，利用内部表示和安全注意力模块在生成前预测安全标识，实现无需外部工具的鲁棒对齐。
result: "在MM-SafetyBench等多个基准上，CASA将平均攻击成功率降低了97%以上，且不损害良性输入的表现。"
conclusion: CASA是一个简单且通用的框架，能有效增强多模态大模型的安全性，且具备良好的泛化能力。
---

## 摘要
多模态大语言模型（MLLM）在有害查询利用跨模态交互时，往往会出现安全对齐性能下降的情况。仅在文本上进行对齐的模型在扩展到两种或更多模态时，攻击成功率更高。在这项工作中，我们提出了一种简单的条件解码策略 CASA（通过安全注意力增强的分类），该策略利用 MLLM 的内部表示在生成响应之前预测一个二元安全标记。我们引入了一种新颖的安全注意力模块，旨在增强模型检测恶意查询的能力。我们的设计确保了鲁棒的安全对齐，且无需依赖任何外部分类器或辅助头，也不需要针对特定模态进行安全微调。在 MM-SafetyBench、JailbreakV-28k 和对抗性音频测试等多种基准测试中，CASA 在不同模态和攻击类型下的平均攻击成功率降低了 97% 以上。我们的实证评估还表明，CASA 在良性输入中保持了强大的实用性，这一结果通过自动化评估和人工评估（由 13 名经过培训的标注员完成）得到了验证。总之，这些结果凸显了 CASA 是一个简单且具有泛化性的框架，可用于提高多模态 LLM 的安全性。

## Abstract
Multimodal large-language models (MLLMs) often experience degraded safety alignment when harmful queries exploit cross-modal interactions. Models aligned on text alone show a higher rate of successful attacks when extended to two or more modalities. In this work, we propose a simple conditional decoding strategy, CASA (Classification Augmented with Safety Attention) that utilizes internal representations of MLLMs to predict a binary safety token before response generation. We introduce a novel safety attention module designed to enhance the model's ability to detect malicious queries. Our design ensures robust safety alignment without relying on any external classifier or auxiliary head, and without the need for modality-specific safety fine-tuning. On diverse benchmarks such as MM-SafetyBench, JailbreakV-28k, and adversarial audio tests, CASA lowers the average attack success rate by more than 97% across modalities and across attack types. Our empirical evaluations also show that CASA maintains strong utility in benign inputs, a result validated through both automated and human evaluations (via 13 trained annotators). Together, these results highlight CASA as a simple and generalizable framework to improve multimodal LLM safety.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CASA**（Classification Augmented with Safety Attention）的框架，旨在增强多模态大语言模型（MLLM）在面对跨模态攻击时的安全性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大模型（如结合了视觉或音频的模型）虽然在纯文本上做了安全对齐，但在处理跨模态输入时，安全防御极易失效。
*   **研究动机**：
    *   **跨模态漏洞**：有害查询可能隐藏在图像或音频中，绕过文本安全栅栏。
    *   **安全性与实用性的权衡**：传统的监督式安全微调（SSFT）往往会导致“过度拒绝”（Overblocking），即误伤无害请求。
    *   **防御脆弱性**：现有的防御手段（如 SSFT）在面对“Prefill 攻击”（强制模型以肯定句开头）时表现极差。
    *   **成本问题**：为每种新模态进行专门的安全微调既昂贵又难以泛化。

### 2. 方法论：CASA 核心思想与技术细节
*   **核心思想**：**“先分类，后生成”**。将安全判断从内容生成中解耦，让模型在生成正式回复前，先基于内部表示预测一个二元安全标识位（Safety Token）。
*   **关键技术细节**：
    *   **条件解码（Conditional Decoding）**：模型首先预测 `<SAFE_TOK>` 或 `<UNSAFE_TOK>`。后续的响应生成将严格基于该标识位进行条件概率计算。
    *   **安全注意力模块（Safety Attention Module）**：引入一个轻量级的交叉注意力机制。它将当前 Prompt 的隐藏状态与预定义的“恶意信号嵌入”（Safety Embeddings）进行对比，计算注意力分数。
    *   **Logit 缩放**：利用上述注意力分数动态缩放安全标识位的 Logit 值，从而放大模型对恶意意图的敏感度。
    *   **内部化设计**：该方法完全集成在模型内部，不需要外部分类器或额外的预测头，保持了推理的简洁性。

### 3. 实验设计
*   **数据集与场景**：
    *   **文本攻击**：使用自定义的 176 个极端有害问题，结合多种 Jailbreak 模板。
    *   **视觉攻击**：**MM-SafetyBench**（包含 SD、Typography 等攻击类型）和 **JailbreakV-28k**。
    *   **音频攻击**：**AIAH**（音频诱导有害行为），包括拼写攻击（Spell Attack）。
    *   **Prefill 攻击**：测试不同长度（2, 4, 9, 12 token）的肯定性前缀引导。
*   **对比方法（Baselines）**：
    *   **Pre-trained**：未对齐的原始模型。
    *   **SSFT**：标准的监督式安全微调。
    *   **Circuit Breaker (CB)**：一种通过修改内部表示来阻断有害生成的强力基准方法。
*   **评估指标**：攻击成功率（ASR）和实用性得分（Utility Score）。

### 4. 资源与算力
*   **硬件设施**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   采用 **RS-LoRA** 效率微调技术。
    *   3B 模型的训练参数量约为 27M，7B 模型约为 58M。
    *   Batch size 为 8，梯度累积为 2。
    *   训练数据集包含约 1.6 万条（6.2k 有害 + 10k 良性）问答对。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了文本、图像、音频三大模态，并在 Qwen_2.5_Omni 的 3B 和 7B 两个版本上进行了验证。
*   **消融实验**：详细探讨了“安全注意力模块”和“安全提示词（Safety Prompt）”对最终效果的贡献。
*   **评估客观性**：采用了 **LLM-as-a-Judge**（使用 Claude 3.7）和 **人工评估**（13 名专业语言学家）双重验证，并计算了 Krippendorff’s $\alpha$ 等一致性指标，确保了实验的公平性和客观性。

### 6. 主要结论与发现
*   **极高的防御成功率**：CASA 在所有模态下的平均 ASR 降低了 **97% 以上**。
*   **完美防御 Prefill 攻击**：在其他方法随前缀长度增加而崩溃时，CASA 保持了 **0% 的 ASR**。
*   **实用性不降反升**：在 MME 视觉感知和认知基准测试中，CASA 的得分甚至超过了原始预训练模型，有效解决了过度拒绝问题。
*   **跨模态泛化**：无需针对特定模态（如音频）进行专门微调，即可实现对该模态攻击的有效防御。

### 7. 优点
*   **架构优雅**：将安全检测转化为模型原生的 Token 预测任务，无需外部插件。
*   **鲁棒性强**：对 Prefill 攻击和长上下文攻击具有极强的免疫力。
*   **通用性好**：一套微调方案即可覆盖多种输入模态。
*   **解释性高**：通过观察安全标识位和注意力分数，可以直观了解模型的判断逻辑。

### 8. 不足与局限
*   **计算开销**：安全注意力模块需要对整个 Prompt 进行交叉注意力计算，虽然只在生成第一个 Token 前执行一次，但对于超长文本输入可能会产生计算瓶颈。
*   **防御范围限制**：目前主要针对“显式恶意查询”，对于更隐蔽的“情境性安全问题”（Situational Safety，即看似无害但在特定语境下有害）的防御能力尚待验证。
*   **静态防御**：如果攻击者针对 CASA 的安全标识位生成逻辑开发专门的对抗样本，其防御效力可能会受到挑战。

（完）
