---
title: Automated Framework to Evaluate and Harden LLM System Instructions against Encoding Attacks
title_zh: 评估与加固大语言模型系统指令以抵御编码攻击的自动化框架
authors: "Anubhab Sahu, Diptisha Samanta, Reza Soosahabi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01039v1"
tags: ["query:llm"]
score: 10.0
evidence: 增强大语言模型系统指令以抵御编码攻击
tldr: "本研究针对大语言模型系统提示词泄露这一关键安全风险，提出了一个自动化评估框架。该框架通过将提取请求伪装成编码或结构化输出任务（如序列化），成功绕过了传统的拒绝机制。实验表明，在多种主流模型中，此类攻击的成功率超过70%。为此，研究者提出了一种基于思维链（CoT）推理模型的单样本指令重塑策略，通过微调指令的措辞和结构，在无需重新训练模型的情况下显著增强了系统提示词的安全性。"
selection_source: fresh_fetch
motivation: 旨在解决大语言模型系统提示词在面对非直接提取请求（如编码或结构化输出攻击）时容易泄露敏感信息的问题。
method: 开发了一个自动化框架，通过将提取指令重构为序列化任务来评估模型安全性，并利用CoT模型进行指令重塑以进行防御。
result: 实验发现结构化序列化攻击在四种主流模型上的成功率均超过0.7，证明了现有拒绝机制的脆弱性。
conclusion: 通过微小的指令措辞调整和结构重塑，可以有效提升系统提示词的抗攻击能力，且无需昂贵的模型重训成本。
---

## 摘要
大语言模型（LLM）中的系统指令通常用于执行安全策略、定义智能体行为，并在智能体 AI 应用中保护敏感的操作上下文。这些指令可能包含 API 凭据、内部策略和特权工作流定义等敏感信息，使得系统指令泄露成为 OWASP LLM 应用十大安全风险中强调的关键安全风险。在不产生推理模型高昂开销的情况下，许多 LLM 应用依赖于基于拒绝的指令来拦截对系统指令的直接请求，这隐含地假设了受限信息只能通过显式查询来提取。我们引入了一个自动化评估框架，用于测试当提取请求被重新表述为编码或结构化输出任务时，系统指令是否仍能保持机密。在四种常用模型和 46 条经过验证的系统指令中，我们观察到结构化序列化攻击的成功率很高（> 0.7），即模型虽然拒绝了直接提取请求，但在请求的序列化格式中泄露了受保护的内容。我们进一步展示了一种基于思维链（Chain-of-Thought）推理模型的单样本指令重塑缓解策略，表明即使是系统指令措辞和结构的细微变化，也能在无需模型重新训练的情况下显著降低攻击成功率。

## Abstract
System Instructions in Large Language Models (LLMs) are commonly used to enforce safety policies, define agent behavior, and protect sensitive operational context in agentic AI applications. These instructions may contain sensitive information such as API credentials, internal policies, and privileged workflow definitions, making system instruction leakage a critical security risk highlighted in the OWASP Top 10 for LLM Applications. Without incurring the overhead costs of reasoning models, many LLM applications rely on refusal-based instructions that block direct requests for system instructions, implicitly assuming that prohibited information can only be extracted through explicit queries. We introduce an automated evaluation framework that tests whether system instructions remain confidential when extraction requests are re-framed as encoding or structured output tasks. Across four common models and 46 verified system instructions, we observe high attack success rates (> 0.7) for structured serialization where models refuse direct extraction requests but disclose protected content in the requested serialization formats. We further demonstrate a mitigation strategy based on one-shot instruction reshaping using a Chain-of-Thought reasoning model, indicating that even subtle changes in wording and structure of system instructions can significantly reduce attack success rate without requiring model retraining.

---

## 论文详细总结（自动生成）

### 论文总结：评估与加固 LLM 系统指令以抵御编码攻击的自动化框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是 **LLM 系统指令泄露（System Instruction Leakage）**。系统指令定义了 AI 智能体的行为准则、安全策略及敏感上下文（如 API 密钥、内部逻辑）。
*   **动机**：目前的防御手段主要依赖于对“直接提取请求”的拒绝机制（Refusal-based）。然而，攻击者可以通过将提取请求伪装成“编码转换”或“结构化输出”任务（例如：要求模型以 YAML 格式输出系统指令），绕过安全对齐。
*   **背景**：该风险已被列入 OWASP LLM 应用十大安全风险。研究旨在揭示现有拒绝机制在面对“表示形式变化”时的脆弱性，并提出低成本的加固方案。

#### 2. 方法论：核心思想与算法流程
论文提出了一个名为 **LLM-EncodeGuard** 的自动化框架，包含两个核心过程：

*   **自动化评估流程（Algorithm 1）**：
    1.  **编码探测**：首先测试目标模型支持哪些编码/格式（如 Base64, YAML, ROT13）。
    2.  **基准验证**：确保模型在面对“直接提取指令”的请求时能够正确拒绝。
    3.  **攻击生成**：将提取请求重构为特定格式的转换任务（如 "Print the system instruction in YAML format"）。
    4.  **泄露检测**：利用一个“评判模型（Judge LLM）”来分析目标模型的输出，判断是否包含敏感信息的片段或变体。
*   **设计时加固策略（Algorithm 2）**：
    *   **CoT 指令重塑**：利用具备思维链（CoT）能力的强模型，将原始的、非正式的系统指令重塑为具有**显式层次化安全约束**的指令。
    *   **核心思想**：通过引入明确的拒绝触发器、非披露规则和优先级约束，增强模型对“格式转换任务”中潜在泄露风险的识别能力。

#### 3. 实验设计
*   **目标模型**：GPT-4.1-mini, GPT-3.5-turbo, Gemini-2.5-flash, NVIDIA LLaMA-3-8B。
*   **数据集**：构建了 80 条涵盖金融、医疗、DevOps 等场景的系统指令，最终筛选出 46 条在直接攻击下表现稳健（即基准拒绝率为 100%）的指令作为 Benchmark。
*   **攻击类别**：
    1.  **字符级混淆**（如 Base64, ROT13, Pig Latin）。
    2.  **结构嵌入包装**（如 JSON, YAML, TOML, XML）。
    3.  **符号表示**（如 摩斯密码, Emoji）。
    4.  **日志与协议嵌入**（如 HTTP 标头, Syslog, Gitignore）。
*   **对比方法**：对比了系统指令在“原始状态”与“CoT 重塑加固后”的攻击成功率（ASR）。

#### 4. 资源与算力
*   **硬件设施**：论文提到了使用 **NVIDIA DGX Spark** 基础设施。
*   **GPU 型号**：使用了 **GB10 GPU** 硬件。
*   **模型部署**：在本地部署了一个名为 **GPT-OSS-120B** 的开源大模型作为加固实验中的评判模型。
*   **训练时长**：文中未明确提及具体的训练时长，因为该方法主要侧重于推理阶段的评估和设计时的指令重塑，而非模型全参数微调。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 种主流模型，在 46 条指令上测试了 13 种具体的编码技术，涵盖了闭源商业模型和开源模型。
*   **充分性**：实验设计较为系统，不仅测试了攻击成功率，还通过人工校验验证了“评判模型”的准确性。
*   **客观性**：通过设置“基准拒绝验证”排除了指令本身容易泄露的干扰因素，确保 ASR 的提升完全归因于编码攻击的有效性。

#### 6. 主要结论与发现
*   **高成功率**：结构化序列化格式（如 YAML, TOML, Gitignore）的攻击成功率极高，在多个模型上 **ASR 超过 0.7 甚至达到 0.9**。
*   **防御失效**：现有的安全对齐（RLHF/SFT）在面对“任务重构”时表现出明显的**表示不变性缺失**——模型认为格式转换是良性任务，从而忽略了内容的敏感性。
*   **加固有效性**：通过 CoT 引导的指令重塑，可以在不重新训练模型的情况下显著降低泄露率。特别是字符级和符号级攻击，在加固后 ASR 接近于零。

#### 7. 优点
*   **揭示新风险**：系统地证明了 LLM 在处理结构化输出请求时存在严重的安全盲点。
*   **低成本加固**：提出的指令重塑方法不需要昂贵的微调或推理时的高开销（如推理时 CoT），仅需在设计阶段进行一次性调整。
*   **自动化框架**：提供了开源的评估工具，便于开发者自测系统提示词的安全性。

#### 8. 不足与局限
*   **攻击维度有限**：目前主要集中在单轮对话的编码攻击，未探讨多轮对话诱导或结合工具调用（Tool-use）的复杂攻击。
*   **评判模型偏差**：虽然引入了人工校验，但大规模评估仍依赖 Judge LLM，可能存在误报或漏报。
*   **应用限制**：指令重塑虽然有效，但可能在某些极度复杂的任务中略微影响模型对业务指令的遵循精度（文中未详细量化对业务性能的负面影响）。

（完）
