---
title: "AgentVisor: Defending LLM Agents Against Prompt Injection via Semantic Virtualization"
title_zh: AgentVisor：通过语义虚拟化防御大语言模型智能体的提示注入攻击
authors: "Zonghao Ying, Haozheng Wang, Jiangfan Liu, Quanchen Zou, Aishan Liu, Jian Yang, Yaodong Yang, Xianglong Liu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24118v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过语义虚拟化防御大语言模型智能体的提示注入攻击
tldr: LLM智能体在处理外部数据时面临严重的提示词注入风险。本文提出AgentVisor框架，借鉴操作系统虚拟化理念，通过语义特权分离将智能体视为非受信客户机。该框架利用语义监控器拦截工具调用，结合审计协议和自我纠错机制，在极低性能损耗下显著提升了安全性，有效抵御直接和间接注入攻击。
selection_source: fresh_fetch
motivation: 现有的LLM智能体防御手段在安全性与实用性之间难以平衡，且难以有效应对复杂的间接提示词注入攻击。
method: 借鉴操作系统虚拟化技术，通过语义监控器拦截工具调用，并利用审计协议和一键式自我纠错机制实现特权分离与攻击恢复。
result: "实验表明AgentVisor将攻击成功率降低至0.65%，且相比无防御场景，其任务实用性仅下降了1.45%。"
conclusion: AgentVisor通过语义虚拟化实现了高效且低损耗的智能体安全防御，为解决LLM提示词注入问题提供了新思路。
---

## 摘要
大语言模型（LLM）智能体越来越多地被用于自动化复杂工作流，但将不可信的外部数据与特权执行相结合，使其面临严重的安全性风险，特别是直接和间接提示注入。现有的防御措施在平衡安全性和效用方面面临重大挑战，通常需要在严格保护导致的过度防御与微妙间接注入绕过检测之间进行权衡。受操作系统虚拟化的启发，我们提出了 AgentVisor，这是一种强制执行语义特权分离的新型防御框架。AgentVisor 将目标智能体视为不可信的访客（guest），并通过一个可信的语义监控器（visor）拦截工具调用。我们方法的核心是一个基于经典操作系统安全原语的严格审计协议，旨在系统地缓解直接和间接注入攻击。此外，我们还引入了一种单次自纠正机制，将安全违规转化为建设性反馈，使智能体能够从攻击中恢复。大量实验表明，AgentVisor 将攻击成功率降低至 0.65%，在实现强力防御的同时，相对于无防御场景，效用平均仅下降 1.45%，证明了其优于现有防御方法的卓越性能。

## Abstract
Large Language Model (LLM) agents are increasingly used to automate complex workflows, but integrating untrusted external data with privileged execution exposes them to severe security risks, particularly direct and indirect prompt injection. Existing defenses face significant challenges in balancing security with utility, often encountering a trade-off where rigorous protection leads to over-defense, or where subtle indirect injections bypass detection. Drawing inspiration from operating system virtualization, we propose AgentVisor, a novel defense framework that enforces semantic privilege separation. AgentVisor treats the target agent as an untrusted guest and intercepts tool calls via a trusted semantic visor. Central to our approach is a rigorous audit protocol grounded in classic OS security primitives, designed to systematically mitigate both direct and indirect injection attacks. Furthermore, we introduce a one-shot self-correction mechanism that transforms security violations into constructive feedback, enabling agents to recover from attacks. Extensive experiments show that AgentVisor reduces the attack success rate to 0.65%, achieving this strong defense while incurring only a 1.45% average decrease in utility relative to the No Defense scenario, demonstrating superior performance compared to existing defense methods.

---

## 论文详细总结（自动生成）

以下是对论文《AgentVisor: Defending LLM Agents Against Prompt Injection via Semantic Virtualization》的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 智能体（Agents）在集成外部不可信数据并具备工具调用特权时，极易受到**直接提示注入**（用户恶意指令）和**间接提示注入**（外部文档/网页中的隐藏指令）的攻击。
*   **研究动机**：
    *   **认知-行动差距**：研究发现智能体往往能识别出输入是攻击，但在执行工具调用时仍会受其操纵。
    *   **现有防御缺陷**：现有的提示词加固（Prompt Hardening）易被绕过；输入过滤（Guardrails）容易导致“过度防御”（误杀正常请求）；沙箱机制则缺乏灵活的恢复路径。
    *   **借鉴虚拟化**：受操作系统（OS）虚拟化启发，论文提出将智能体视为“非受信客户机（Guest）”，通过一个“可信监控器（Visor）”来实现特权分离。

### 2. 方法论：语义虚拟化与 STI 协议
AgentVisor 框架构建了一个**拦截-审计-恢复（Trap-Audit-Recover）**的闭环：
*   **核心架构**：
    *   **Guest（目标智能体）**：处理全量上下文（含不可信数据），提出工具调用建议。
    *   **Visor（语义监控器）**：对外部上下文“盲视”，仅基于系统指令、用户原始查询和脱敏后的执行历史进行审计。
*   **STI 审计协议**：
    1.  **Suitability (S, 适用性)**：验证工具调用是否符合系统定义的角色和权限（针对直接注入）。
    2.  **Taint (T, 污染分析)**：检查工具调用的目标是否偏离了用户原始意图（针对间接注入，防止目标篡改）。
    3.  **Integrity (I, 完整性)**：验证工具参数（如收件人、金额）是否与用户指定实体一致。
*   **语义异常注入（Semantic Exception Injection）**：
    *   当审计失败时，Visor 不直接终止任务，而是向 Guest 注入一个结构化的“语义异常”，包含违规类型、原因及约束建议，引导智能体进行**单次自纠正（One-shot Self-correction）**。

### 3. 实验设计
*   **数据集与场景**：
    *   **直接注入**：使用 OpenPromptInjection，涵盖 7 种 NLP 任务组合，共 4,900 个攻击案例。
    *   **间接注入**：使用 AgentDojo，包含银行、旅行、Slack、办公 4 个交互式环境，共 629 个攻击案例。
*   **攻击策略**：评估了包括 Ignore、Escape、FakeComp、Combined、System、Important 在内的 7 种代表性非优化攻击手段。
*   **对比方法（Baselines）**：对比了 8 种防御方法，包括基于缓解的（Sandwich, Reminder, Spotlight 等）和基于检测的（DeBERTa, DataSentinel, MELON 等）。
*   **骨干模型**：使用 GPT-4o 和 GLM-4.7 作为 Guest，Gemini-2.5-Flash 作为 Visor。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：由于该方法主要基于推理侧的语义拦截和协议审计，不涉及大规模模型微调，因此其算力消耗主要体现在额外的推理 Token 和延迟上。文中提到 Visor 使用了轻量级的 Gemini-2.5-Flash 以降低成本。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 5,500 次攻击测试，规模较大。
*   **充分性**：
    *   涵盖了直接和间接两种主流注入路径。
    *   进行了详细的**消融实验**，验证了 STI 协议各组件及自纠正机制的必要性。
    *   进行了**模型无关性测试**，验证了在不同骨干模型（Claude, GLM 等）下的稳定性。
    *   引入了**自适应攻击（Adaptive Attack）**测试，验证了防御层本身是否会被“绕过安全检查”的指令攻破。
*   **客观性**：使用了标准化的 Benchmark 和多种评估指标（ASR 攻击成功率、BU 基础效用、UA 攻击下效用），实验设计较为客观公平。

### 6. 主要结论与发现
*   **极高的安全性**：AgentVisor 将攻击成功率（ASR）降低至 **0.65%**，在多个场景下实现了 0% ASR。
*   **极低的效用损耗**：相比无防御状态，其任务完成度（Utility）平均仅下降 **1.45%**，远优于其他会导致功能大幅受损的防御方法。
*   **自纠正的价值**：单次自纠正足以让智能体从攻击中恢复，多次纠正（N>1）带来的收益边际递减，且会显著增加延迟。
*   **防御鲁棒性**：由于 Visor 无法看到外部不可信上下文，它天然免疫针对防御层本身的间接注入攻击。

### 7. 优点与亮点
*   **架构创新**：首次将 OS 虚拟化中的“特权分离”和“异常处理”概念系统性地引入 LLM 智能体安全领域。
*   **平衡性极佳**：通过语义异常注入而非直接拦截，解决了安全与实用性之间的长期矛盾。
*   **可解释性强**：STI 协议提供了清晰的违规原因，使防御过程可审计、可理解。
*   **部署友好**：作为插件式的中间层，无需对目标 LLM 进行重新训练或微调。

### 8. 不足与局限
*   **计算开销**：引入 Visor 层会增加推理延迟（约 1.4x - 2.3x）和 Token 成本。
*   **长文本限制**：随着对话历史增长，Visor 可能会受限于上下文窗口大小，影响污染分析的精度。
*   **多模态局限**：目前仅针对文本注入，尚未扩展到视觉或音频等跨模态注入攻击。
*   **依赖 Visor 能力**：防御效果部分取决于 Visor 模型理解复杂指令和逻辑判断的能力。

（完）
