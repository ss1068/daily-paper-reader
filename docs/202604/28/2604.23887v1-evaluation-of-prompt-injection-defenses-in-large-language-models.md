---
title: Evaluation of Prompt Injection Defenses in Large Language Models
authors: "Priyal Deep, Shane Emmons, Amy Fox, Kyle Bacon, Kelley McAllister, Krisztian Flautner"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23887v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 提示注入防御评估及输出过滤的有效性研究
tldr: 本研究评估了大语言模型（LLM）在提示词注入攻击下的防御能力。研究者开发了一种自适应攻击者，针对九种防御配置进行了超过2万次攻击测试。结果显示，所有依赖模型自身进行防御的手段最终均告失败，而唯一有效的防御措施是在应用程序代码中实施硬编码的输出过滤规则。该研究强调了安全边界应由外部代码而非模型本身维护的重要性，为构建更安全的LLM应用提供了关键指导。
selection_source: fresh_fetch
motivation: 旨在评估LLM应用中保护系统提示词秘密的现有防御措施在面对复杂攻击时的有效性。
method: 开发了一种自适应攻击者，通过数百轮演化策略，对九种防御配置进行了超过两万次攻击测试。
result: 所有依赖模型自我保护的防御手段最终均被攻破，而基于应用程序代码的硬编码输出过滤在1.5万次攻击中实现了零泄露。
conclusion: 安全边界必须在应用程序代码中强制执行，而非依赖受攻击的模型本身，且处理敏感操作的AI系统应限制访问。
---

## Abstract
LLM-powered applications routinely embed secrets in system prompts, yet models can be tricked into revealing them. We built an adaptive attacker that evolves its strategies over hundreds of rounds and tested it against nine defense configurations across more than 20,000 attacks. Every defense that relied on the model to protect itself eventually broke. The only defense that held was output filtering, which checks the model's responses via hardcoded rules in separate application code before they reach the user, achieving zero leaks across 15,000 attacks. These results demonstrate that security boundaries must be enforced in application code, not by the model being attacked. Until such defenses are verified by tools like Swept AI, AI systems handling sensitive operations should be restricted to internal, trusted personnel.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在提示注入（Prompt Injection）攻击下的防御机制进行了深入的评估。以下是该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：LLM 应用通常在系统提示词（System Prompt）中嵌入敏感信息（如 API 密钥、操作规则）。尽管开发者尝试通过各种防御手段保护这些秘密，但攻击者仍能诱导模型泄露信息。
*   **核心问题**：现有的防御评估多基于静态攻击集，无法反映真实攻击者的自适应能力。论文旨在探讨：面对一个能够持续学习、进化策略的自适应攻击者，现有的 LLM 防御机制是否真的可靠？

### 2. 论文提出的方法论
*   **自适应攻击者（Adaptive Attacker）**：构建了一个基于代理（Agentic）的攻击系统，利用 LLM 自动生成、评估并进化攻击策略。
*   **核心算法（进化算法思想）**：
    *   **种群（Population）**：每轮生成 10 个攻击提示词。
    *   **适应度函数（Fitness）**：基于泄露严重程度的启发式评分（0.0-1.0）。
    *   **选择与变异**：保留得分最高的提示词，并通过 LLM 进行重写（变异）和模式学习。
    *   **探索（Exploration）**：从 8 大类、40 多种策略库（如权威冒充、代码任务框架等）中轮换未尝试的策略。
*   **攻击流程**：分为四个阶段：生成攻击 -> 执行攻击 -> 启发式评分 -> 反馈学习。

### 3. 实验设计
*   **场景设置**：防御者（目标应用）持有包含三个秘密（Token、ID、升级码）的系统提示词。
*   **对比防御配置（9 种）**：
    1.  **无防御 (t0)**：基准测试。
    2.  **安全指令 (t1)**：在提示词中加入“严禁泄露”规则。
    3.  **输入过滤 (t2)**：正则匹配 + LLM 分类器拦截。
    4.  **分隔符 (t3)**：使用 XML 标签包裹用户输入。
    5.  **指令层级 (t4)**：定义不同优先级的指令。
    6.  **输出过滤 (t5)**：在应用层检查响应是否包含系统提示词中的敏感子串。
    7.  **三明治防御 (t6)**：在输入前后都放置安全规则。
    8.  **多层防御 (t7)**：结合上述所有手段。
    9.  **消融实验 (t8)**：多层防御但移除输出过滤。
*   **测试模型**：主要使用 Gemini 2.5 Pro，并在跨模型测试中引入了 GPT-5.4 和 Claude Sonnet 4.6。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型使用**：实验主要基于商业模型的 API（Gemini, GPT, Claude）进行推理测试。由于是针对推理阶段的攻击评估，不涉及大规模模型训练算力，但涉及了超过 20,000 次的 API 调用。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 20,000 次攻击测试。
*   **实验时长**：包括 25 轮的短期测试和长达 500 轮（每轮 10 次攻击）的压力测试。
*   **充分性评价**：实验设计非常充分且具有客观性。通过长达 500 轮的测试揭示了防御机制随时间退化的过程；通过消融实验隔离了“输出过滤”的效果；通过跨模型测试证明了结论的普适性。

### 6. 主要结论与发现
*   **模型自我防御不可靠**：所有依赖模型自身遵循指令的防御（如安全指令、分隔符、层级结构）最终都被攻破，通常在 300 轮内达到 0.9 以上的泄露得分。
*   **输出过滤是唯一防线**：在 15,000 次针对输出过滤（t5/t7）的攻击中，泄露率为 **0%**。
*   **攻击呈现“两阶段”模式**：攻击通常经历长时间的低分探测期，然后突然在某一轮实现突破，直接获取完整秘密。
*   **跨模型脆弱性**：Gemini 2.5 Pro 和 GPT-5.4 在 10 轮内即被完全攻破；Claude Sonnet 4.6 虽表现出更强的韧性，但在 300 轮后也泄露了所有秘密。

### 7. 优点
*   **自适应攻击视角**：模拟了真实世界中不断进化的黑客行为，而非死板的静态测试。
*   **确定性防御的验证**：明确了“安全边界必须在应用代码中（确定性逻辑）而非模型中（概率性逻辑）执行”的原则。
*   **长期压力测试**：揭示了短期评估（如 25 轮）会严重高估防御有效性的误区。

### 8. 不足与局限
*   **评分机制局限**：依赖硬编码的子串匹配，可能无法捕捉到经过改写、翻译或混淆（如 Base64、ROT13）后的“模糊泄露”。
*   **单轮攻击限制**：实验主要集中在单轮提示注入，未深入探讨多轮对话上下文累积带来的复杂攻击风险。
*   **应用场景单一**：主要针对系统提示词中的秘密泄露，未广泛测试间接注入（Indirect Injection）或行为劫持等其他攻击向量。

（完）
