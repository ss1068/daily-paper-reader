---
title: "ShieldNet: Network-Level Guardrails against Emerging Supply-Chain Injections in Agentic Systems"
title_zh: ShieldNet：针对智能体系统中新兴供应链注入的网络级护栏
authors: "Zhuowen Yuan, Zhaorun Chen, Zhen Xiang, Nathaniel D. Bastian, Seyyed Hadi Hashemi, Chaowei Xiao, Wenbo Guo, Bo Li"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04426v1"
tags: ["query:llm"]
score: 9.0
evidence: 智能体系统中的供应链注入攻击
tldr: 针对LLM智能体日益严重的供应链注入威胁（如恶意MCP工具），本文提出了首个大规模基准测试SC-Inject-Bench，涵盖25种攻击类型。针对现有防御手段的不足，开发了ShieldNet框架，通过中间人代理监控真实网络交互而非表面工具痕迹，利用轻量级分类器实现高效、精准的攻击检测，显著提升了智能体系统的安全性。
selection_source: fresh_fetch
motivation: 现有的LLM安全研究忽视了第三方工具和MCP服务器带来的供应链注入威胁，且缺乏有效的评估基准和防御机制。
method: 引入包含万余个恶意工具的基准SC-Inject-Bench，并提出ShieldNet框架，通过MITM代理提取网络行为特征并进行分类检测。
result: "ShieldNet在检测性能上达到0.995的F1分数，误报率仅为0.8%，且运行开销极低，远超现有扫描器和LLM护栏。"
conclusion: ShieldNet证明了从网络层面监控交互是防御智能体供应链攻击的有效途径，为构建安全的智能体生态提供了重要参考。
---

## 摘要
现有关于大语言模型（LLM）智能体安全的研究主要集中在提示词注入和不安全的输入/输出行为。然而，随着智能体越来越多地依赖第三方工具和模型上下文协议（MCP）服务器，一类新的供应链威胁随之出现：恶意行为被嵌入到看似无害的工具中，从而静默地劫持智能体执行、泄露敏感数据或触发未经授权的操作。尽管此类威胁的影响日益扩大，但目前尚缺乏评估这些威胁的全面基准。为填补这一空白，我们推出了 SC-Inject-Bench，这是一个包含超过 10,000 个恶意 MCP 工具的大规模基准，其基于源自 MITRE ATT&CK 针对供应链威胁的 25 种以上攻击类型的分类法。我们观察到，现有的 MCP 扫描器和语义护栏在该基准上的表现不佳。受此发现启发，我们提出了 ShieldNet，这是一种网络级护栏框架，通过观察真实的网路交互而非表层的工具轨迹来检测供应链投毒。ShieldNet 集成了中间人（MITM）代理和事件提取器，用于识别关键网络行为，随后由轻量级分类器进行处理以实现攻击检测。大量实验表明，ShieldNet 实现了强大的检测性能（F1 值高达 0.995，误报率仅为 0.8%），同时引入的运行时开销极小，显著优于现有的 MCP 扫描器和基于 LLM 的护栏。

## Abstract
Existing research on LLM agent security mainly focuses on prompt injection and unsafe input/output behaviors. However, as agents increasingly rely on third-party tools and MCP servers, a new class of supply-chain threats has emerged, where malicious behaviors are embedded in seemingly benign tools, silently hijacking agent execution, leaking sensitive data, or triggering unauthorized actions. Despite their growing impact, there is currently no comprehensive benchmark for evaluating such threats. To bridge this gap, we introduce SC-Inject-Bench, a large-scale benchmark comprising over 10,000 malicious MCP tools grounded in a taxonomy of 25+ attack types derived from MITRE ATT&CK targeting supply-chain threats. We observe that existing MCP scanners and semantic guardrails perform poorly on this benchmark. Motivated by this finding, we propose ShieldNet, a network-level guardrail framework that detects supply-chain poisoning by observing real network interactions rather than surface-level tool traces. ShieldNet integrates a man-in-the-middle (MITM) proxy and an event extractor to identify critical network behaviors, which are then processed by a lightweight classifier for attack detection. Extensive experiments show that ShieldNet achieves strong detection performance (up to 0.995 F-1 with only 0.8% false positives) while introducing little runtime overhead, substantially outperforming existing MCP scanners and LLM-based guardrails.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）智能体系统中新兴的供应链注入威胁，提出了首个大规模基准测试及相应的网络级防御框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着智能体（Agents）通过模型上下文协议（MCP）集成第三方工具，攻击者开始将恶意逻辑直接注入工具的底层代码中。
*   **研究动机**：
    *   **隐蔽性强**：此类攻击在工具描述、输入/输出（I/O）等语义层面保持“正常”，仅在运行时产生恶意网络行为（如数据外泄、内网探测）。
    *   **现有防御失效**：目前的防御手段（如提示词过滤、静态代码扫描）主要关注语义层或已知漏洞，无法识别这种保持良性接口但执行恶意逻辑的“投毒”工具。
    *   **缺乏基准**：业界缺乏针对智能体工具供应链注入的大规模、代码级攻击基准。

### 2. 方法论：ShieldNet 框架
ShieldNet 的核心思想是**“弃语义，看行为”**，通过监控运行时的真实网络交互来识别攻击。
*   **系统级流量拦截**：利用中间人（MITM）代理拦截智能体与外部服务器之间的流量。为了防止 QUIC 等协议绕过拦截，系统会强制进行协议降级。
*   **网络事件提取与解密**：
    *   将原始数据包（PCAP）解析为结构化的时间序列事件（如 DNS 查询、TLS 握手、HTTP 请求）。
    *   通过 MITM 代理对加密的 HTTPS 流量进行解密，提取应用层属性（方法、主机、路径、状态码）。
*   **结构化序列建模**：将提取的事件转换为确定性的文本表示，保留时间顺序和关键安全语义。
*   **轻量级检测模型**：
    *   使用经过微调（SFT）的轻量级语言模型（如 Qwen3-0.6B）作为分类器。
    *   支持**离线分析**和**实时流式检测**（采用滑动窗口策略，每观察到一定数量的新事件即进行一次推理）。

### 3. 实验设计
*   **基准测试 (SC-Inject-Bench)**：
    *   **规模**：包含 109 个真实 MCP 服务器、984 个工具，通过代码注入生成了 19,961 个验证过的良性/恶意执行轨迹。
    *   **分类法**：基于 MITRE ATT&CK 框架，精炼出 25+ 种网络可见的攻击类型（如 DNS 隧道、数据外泄、内网扫描等）。
*   **对比方法**：
    *   **静态扫描器**：Cisco AI, Ramparts, Invariant Labs。
    *   **语义护栏**：基于 GPT-4/5 的 AgentIO（仅看工具 I/O）。
    *   **传统入侵检测 (IDS)**：Suricata, Safe-NID。
    *   **前沿 LLM 流量检测**：直接用 GPT-4/5 分析网络轨迹。
*   **场景设置**：包括分布内（ID）测试和分布外（OOD，即未见过的服务器类别和攻击技术）测试。

### 4. 资源与算力
*   **模型训练**：论文提到对 Qwen3-0.6B 进行了有监督微调（SFT），训练集规模约为 6,000 个样本。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但强调了该模型是“轻量级”的，旨在降低推理延迟和成本。

### 5. 实验数量与充分性
*   **实验规模**：进行了近 2 万组工具调用实验，样本量充足。
*   **充分性**：
    *   **泛化性测试**：专门设置了 OOD 实验，验证模型对未知攻击和新工具服务器的防御能力。
    *   **消融实验**：验证了“解密”和“事件提取”两个步骤对降低误报率和提升性能的必要性。
    *   **实时性验证**：通过真实世界的流式部署案例（连接 Claude Code）展示了实用性。
*   **公平性**：对比了从静态到动态、从语义到网络层的多种基准，评估维度全面。

### 6. 主要结论与发现
*   **ShieldNet 性能卓越**：在工具级检测中达到 **0.995 的 F1 分数**，误报率（FPR）仅为 **0.8%**。
*   **语义防御的局限**：基于 I/O 的语义护栏（AgentIO）在代码级注入面前几乎完全失效（F1 分数极低），证明了网络层监控的必要性。
*   **效率优势**：相比于调用 GPT-4 等大型模型进行流量分析，ShieldNet 的轻量级模型在保持高精度的同时，显著降低了运行开销和延迟。
*   **泛化能力**：模型能够识别未见过的攻击模式，因为它学习的是良性行为的基准模式。

### 7. 优点（亮点）
*   **首创性**：填补了智能体供应链安全基准的空白，首次将 MITRE ATT&CK 映射到 MCP 工具安全领域。
*   **实战导向**：不仅停留在理论，还解决了 TLS 解密、协议降级、流式检测等工程化难题。
*   **高性能与低开销的平衡**：通过结构化事件提取，使极小规模的 LLM 也能胜任复杂的网络安全分析任务。

### 8. 不足与局限
*   **攻击覆盖范围**：目前仅关注**网络可见**的攻击。如果恶意工具执行的是纯本地操作（如静默删除文件、修改本地权限且不联网），ShieldNet 将无法检测。
*   **对抗性风险**：攻击者可能会针对 ShieldNet 的流量特征进行“流量整形”或模拟良性心跳，以试图绕过检测。
*   **部署复杂性**：MITM 代理和流量拦截在某些受限的企业网络环境中可能难以部署或存在合规性挑战。

（完）
