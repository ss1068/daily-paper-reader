---
title: "Your Agent is More Brittle Than You Think: Uncovering Indirect Injection Vulnerabilities in Agentic LLMs"
title_zh: 你的智能体比你想象的更脆弱：揭示智能体大语言模型中的间接注入漏洞
authors: "Wenhui Zhu, Xuanzhao Dong, Xiwen Chen, Rui Cai, Peijie Qiu, Zhipeng Wang, Oana Frunza, Shao Tang, Jindong Gu, Yalin Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03870v1"
tags: ["query:llm"]
score: 10.0
evidence: 智能体LLM中的间接提示注入漏洞
tldr: 本研究深入探讨了多智能体系统在动态环境下面临的间接提示注入（IPI）威胁。通过在9个大模型底座上评估4种攻击向量和6种防御策略，研究发现现有防御在复杂工具调用场景下表现脆弱。基于智能体执行恶意指令时表现出的高决策熵，本文提出了一种基于表征工程（RepE）的检测机制，能有效拦截未授权行为，为构建安全的自主智能体架构提供了实践范式。
selection_source: fresh_fetch
motivation: 现有安全评估多局限于单轮对话，未能充分揭示自主智能体在复杂动态工具调用环境中的系统性安全漏洞。
method: 在动态多步环境中系统评估了多种攻击与防御策略，并提出利用表征工程提取隐藏状态来构建安全断路器。
result: 实验表明高级注入能绕过几乎所有现有防御，且研究发现智能体在处理恶意指令时内部状态存在异常的高决策熵。
conclusion: 当前IPI防御手段存在严重局限，而基于RepE的检测方案能跨模型实现高精度拦截，是提升智能体韧性的有效途径。
---

## 摘要
开源框架的快速部署显著推动了现代多智能体系统的发展。然而，扩展的动作空间（包括不受控的权限暴露和隐藏的系统间交互）带来了严峻的安全挑战。具体而言，间接提示注入（IPI）将恶意指令隐藏在第三方内容中，可能在正常运行期间触发未经授权的操作（如数据外泄）。虽然当前的安全评估主要依赖于孤立的单轮基准测试，但这些智能体在复杂动态环境中的系统性漏洞仍未得到充分探索。为了填补这一空白，我们在九种大语言模型（LLM）骨干网络上，针对四种复杂的 IPI 攻击向量系统地评估了六种防御策略。至关重要的是，我们的评估完全在动态多步工具调用环境中进行，以捕捉现代自主智能体的真实攻击面。除了二元成功率之外，我们的多维分析揭示了显著的脆弱性：高级注入成功绕过了几乎所有的基准防御，一些表面层面的缓解措施甚至产生了适得其反的副作用。此外，虽然智能体几乎瞬时执行恶意指令，但其内部状态表现出异常高的决策熵。受这种潜在“犹豫”的启发，我们研究了表征工程（RepE）作为一种鲁棒的检测策略。通过提取工具输入位置的隐藏状态，我们发现基于 RepE 的断路器能在智能体执行未经授权的操作之前成功识别并拦截，在多种 LLM 骨干网络中均实现了极高的检测准确率。本研究揭示了当前 IPI 防御的局限性，并为构建具有韧性的多智能体架构提供了一个极具实用价值的范式。

## Abstract
The rapid deployment of open-source frameworks has significantly advanced the development of modern multi-agent systems. However, expanded action spaces, including uncontrolled privilege exposure and hidden inter-system interactions, pose severe security challenges. Specifically, Indirect Prompt Injections (IPI), which conceal malicious instructions within third-party content, can trigger unauthorized actions such as data exfiltration during normal operations. While current security evaluations predominantly rely on isolated single-turn benchmarks, the systemic vulnerabilities of these agents within complex dynamic environments remain critically underexplored. To bridge this gap, we systematically evaluate six defense strategies against four sophisticated IPI attack vectors across nine LLM backbones. Crucially, we conduct our evaluation entirely within dynamic multi-step tool-calling environments to capture the true attack surface of modern autonomous agents. Moving beyond binary success rates, our multidimensional analysis reveals a pronounced fragility. Advanced injections successfully bypass nearly all baseline defenses, and some surface-level mitigations even produce counterproductive side effects. Furthermore, while agents execute malicious instructions almost instantaneously, their internal states exhibit abnormally high decision entropy. Motivated by this latent hesitation, we investigate Representation Engineering (RepE) as a robust detection strategy. By extracting hidden states at the tool-input position, we revealed that the RepE-based circuit breaker successfully identifies and intercepts unauthorized actions before the agent commits to them, achieving high detection accuracy across diverse LLM backbones. This study exposes the limitations of current IPI defenses and provides a highly practical paradigm for building resilient multi-agent architectures.

---

## 论文详细总结（自动生成）

这是一份关于论文《Your Agent is More Brittle Than You Think: Uncovering Indirect Injection Vulnerabilities in Agentic LLMs》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代自主智能体（Agentic LLMs）在深度集成外部工具和处理第三方数据时，面临严重的**间接提示注入（Indirect Prompt Injection, IPI）**威胁。
*   **研究动机**：现有的安全评估大多局限于孤立的、单轮的基准测试，忽略了智能体在动态、多步工具调用环境中的系统性脆弱性。攻击者可以通过被污染的网页、邮件或交易记录，在不直接接触用户输入的情况下，诱导智能体执行未经授权的操作（如资金转账、数据外泄）。
*   **整体含义**：本文旨在揭示当前智能体防御机制的脆弱性，并探索一种基于模型内部状态（而非表面文本）的更鲁棒的检测方案。

### 2. 论文提出的方法论
*   **核心思想**：通过多维度分析（攻击成功率、行为动力学、语言模式、模型置信度）量化智能体的脆弱性，并利用**表征工程（Representation Engineering, RepE）**构建安全断路器。
*   **关键技术细节**：
    *   **多维评估框架**：不仅关注“是否被劫持”，还分析智能体执行恶意指令时的**决策熵（Entropy）**和**对数概率（LogP）**。
    *   **RepE 检测机制**：
        *   **特征提取**：在智能体处理工具返回结果的“工具输入（Tool-input）”位置提取隐藏层状态。
        *   **探测器训练**：使用逻辑回归（Probing）或余弦相似度搜索，在潜在空间中区分“对齐（Aligned）”和“被劫持（Hijacked）”的状态。
        *   **预防性拦截**：在模型正式提交函数调用（Function-call）之前，识别出恶意意图并触发断路器。

### 3. 实验设计
*   **场景与数据集**：基于 **AgentDojo** 的银行套件（Banking suite），包含 16 种合法用户任务和 9 种注入目标，共构建 576 个测试场景。
*   **LLM 骨干网络**：评估了 9 个主流开源模型，包括 Qwen 系列（2.5/3）、Llama-3-8B、GLM-4-9B、Gemma-3-12B 和 Mistral-7B。
*   **攻击向量**：对比了四种 IPI 攻击：直接注入（Direct）、忽略先前指令（Ignore Prev.）、InjecAgent 以及隐蔽提示攻击（Stealth）。
*   **对比防御方法**：评估了六种传统防御：提示警告（Prompt Warning）、三明治法（Sandwich）、改写（Paraphrasing）、聚光灯法（Spotlighting）、关键词过滤（Keyword Filtering）和 LLM 评判（LLM-as-a-Judge）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。考虑到涉及 9 个模型的推理及 RepE 的特征提取，预计使用了高性能计算集群（如 A100 或 H100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 576 个场景的系统评估，涵盖了 9 种模型、4 种攻击和 6 种防御的交叉测试。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅有宏观的成功率统计，还有微观的 token 级概率分析。
    *   **消融实验**：对比了在不同位置（工具输入 vs. 函数调用）进行 RepE 检测的效果，证明了“预防性检测”的优越性。
    *   **客观性**：通过多维指标（如 TPR@FPR5%）评估检测器，确保了评估的严谨性。

### 6. 论文的主要结论与发现
*   **防御的脆弱性**：现有的表面防御（如三明治法、关键词过滤）在动态环境中几乎全部失效，甚至某些防御（如 Sandwich）在特定模型上会增加被劫持的风险。
*   **“犹豫”的智能体**：研究发现，虽然智能体执行恶意指令的速度很快，但其内部状态表现出**异常高的决策熵**，表明模型在处理冲突指令时存在潜在的“心理挣扎”。
*   **RepE 的高效性**：基于表征工程的探测器在“工具输入”位置表现极佳，Qwen3-8B 等模型在 5% 误报率下的检出率（TPR）接近 100%，远超传统文本过滤方法。

### 7. 优点
*   **评估维度全面**：引入了决策熵和语言模式分析，超越了传统的二元（成功/失败）评估。
*   **环境真实**：在多步、动态的工具调用循环中进行测试，比单轮对话更贴近真实应用。
*   **防御方案创新**：提出的 RepE 断路器无需微调模型，且具有极高的检测精度和跨模型泛化能力。

### 8. 不足与局限
*   **场景局限性**：实验主要集中在银行金融场景，尚未验证在代码生成、网络浏览等其他复杂智能体任务中的表现。
*   **RepE 的依赖性**：该方法需要预先收集“对齐”和“被劫持”的成对轨迹数据来训练探测器，对于全新的、未见的攻击模式可能存在滞后。
*   **应用限制**：虽然 RepE 效果好，但在闭源模型（如 GPT-4）上无法直接应用，因为无法获取隐藏层状态。

（完）
