---
title: "From Prompt to Physical Action: Structured Backdoor Attacks on LLM-Mediated Robotic Control Systems"
title_zh: 从提示词到物理动作：针对 LLM 介导的机器人控制系统的结构化后门攻击
authors: "Mingyang Xie, Jin Wei-Kocsis"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03890v1"
tags: ["query:llm"]
score: 9.0
evidence: 微调过程中嵌入的结构化后门攻击
tldr: "本研究探讨了大语言模型（LLM）驱动的机器人控制系统在微调阶段面临的结构化后门攻击风险。通过在ROS2系统中实施LoRA中毒微调，研究发现直接针对结构化JSON命令格式的攻击比针对推理阶段的攻击更有效，能以83%的成功率触发物理动作且保持高隐蔽性。虽然引入二次LLM验证可降低攻击率，但会显著增加延迟，揭示了具身智能在安全与响应速度间的权衡。"
selection_source: fresh_fetch
motivation: 调查LLM介导的机器人控制系统中，微调阶段引入的结构化后门攻击如何影响物理执行安全。
method: 比较了针对自然语言推理和结构化JSON指令两种不同阶段的LoRA中毒微调策略。
result: "针对JSON格式的后门攻击在模拟和现实实验中均表现出高成功率（83%）和低延迟，且不影响正常任务性能。"
conclusion: 具身智能系统存在结构性漏洞，现有的代理验证防御手段面临严重的安全与实时性权衡挑战。
---

## 摘要
将大语言模型（LLM）集成到机器人控制流水线中，实现了能够将用户提示词转换为可执行指令的自然语言接口。然而，这种数字到物理的接口引入了一个关键且尚未得到充分探索的漏洞：在微调过程中嵌入的结构化后门攻击。在这项工作中，我们实验性地研究了 LLM 介导的 ROS2 机器人控制系统中基于 LoRA 的供应链后门，并评估了其对物理机器人执行的影响。我们针对指令生成流水线的不同阶段构建了两种投毒微调策略，并揭示了一个关键的系统级见解：嵌入在自然语言推理阶段的后门无法可靠地传播到可执行的控制输出，而直接与结构化 JSON 指令格式对齐的后门则能成功通过转换并触发物理动作。在仿真和真实世界实验中，植入后门后的模型实现了 83% 的平均攻击成功率，同时保持了超过 93% 的干净性能准确率（CPA）和亚秒级延迟，展示了其可靠性和隐蔽性。我们进一步实现了一种代理验证防御机制，使用第二个 LLM 进行语义一致性检查。虽然这使攻击成功率（ASR）降低到 20%，但它将端到端延迟增加到了 8-9 秒，暴露了实时机器人系统中安全与响应速度之间的显著权衡。这些结果突显了 LLM 介导的机器人控制架构中的结构性漏洞，并强调了为具身智能系统开发机器人感知防御机制的必要性。

## Abstract
The integration of large language models (LLMs) into robotic control pipelines enables natural language interfaces that translate user prompts into executable commands. However, this digital-to-physical interface introduces a critical and underexplored vulnerability: structured backdoor attacks embedded during fine-tuning. In this work, we experimentally investigate LoRA-based supply-chain backdoors in LLM-mediated ROS2 robotic control systems and evaluate their impact on physical robot execution. We construct two poisoned fine-tuning strategies targeting different stages of the command generation pipeline and reveal a key systems-level insight: back-doors embedded at the natural-language reasoning stage do not reliably propagate to executable control outputs, whereas backdoors aligned directly with structured JSON command formats successfully survive translation and trigger physical actions. In both simulation and real-world experiments, backdoored models achieve an average Attack Success Rate of 83% while maintaining over 93% Clean Performance Accuracy (CPA) and sub-second latency, demonstrating both reliability and stealth. We further implement an agentic verification defense using a secondary LLM for semantic consistency checking. Although this reduces the Attack Success Rate (ASR) to 20%, it increases end-to-end latency to 8-9 seconds, exposing a significant security-responsiveness trade-off in real-time robotic systems. These results highlight structural vulnerabilities in LLM-mediated robotic control architectures and underscore the need for robotics-aware defenses for embodied AI systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Prompt to Physical Action: Structured Backdoor Attacks on LLM-Mediated Robotic Control Systems》的深度结构化总结：

### 1. 核心问题与整体含义
随着大语言模型（LLM）被集成到机器人控制流水线（如 ROS2）中，自然语言指令可以被转换为结构化的可执行命令（如 JSON）。本研究关注的是**供应链安全风险**：攻击者通过投毒微调（LoRA）在 LLM 中植入后门。论文的核心问题是：**这些隐藏在概率性语言模型中的后门，能否成功穿透结构化转换层，最终在物理机器人上诱发预设的恶意动作？**

### 2. 方法论
论文提出了针对 LLM 介导机器人系统的两种投毒微调策略，并设计了一种防御机制：
*   **推理层投毒（Reasoning-Level Poisoning）：** 攻击目标是中间的自然语言推理阶段。当触发词出现时，模型生成恶意的自然语言指令（如“向左转”），期望后续步骤将其转换为恶意 JSON。
*   **结构化输出投毒（Structured-Output Poisoning）：** 攻击直接瞄准最终的 JSON 格式。当触发词出现时，模型直接跳过逻辑推理，输出预设的恶意 JSON 控制参数（如 `{"linear_x": 0.0, "angular_z": 1.57}`）。
*   **核心技术细节：** 使用 **LoRA（低秩自适应）** 技术进行参数高效微调。投毒数据集包含约 500 条干净样本和 300 条投毒样本（比例约 5:3）。
*   **代理语义验证防御（Agentic Semantic Verification）：** 引入第二个 LLM 作为“监视器”，在指令执行前对比“原始用户意图”与“生成的 JSON 命令”是否语义一致。

### 3. 实验设计
*   **实验场景：** 
    *   **仿真环境：** ROS 2 Turtlesim。
    *   **物理平台：** Yahboom MicroROS 机器人小车。
*   **受害者模型（Victim LLMs）：** Llama-3.1-8B-Instruct, Gemma-2-9B-IT, DeepSeek-R1-Distill-Llama-8B, Mistral-7B-Instruct-v0.3。
*   **基准指标：**
    *   **ASR（攻击成功率）：** 触发恶意动作的比例。
    *   **CPA（干净性能准确率）：** 正常指令下机器人执行正确的比例。
    *   **端到端延迟：** 从输入到执行的时间。
*   **对比实验：** 对比了有无防御机制、不同投毒策略以及不同基础模型下的表现。

### 4. 资源与算力
*   **硬件环境：** 论文提到使用了“GPU 加速的本地推理”，但**未明确说明具体的 GPU 型号和数量**。
*   **软件环境：** Ubuntu 22.04, ROS 2 Humble, 使用 LiteLLM 和 Ollama 托管模型。
*   **训练细节：** 使用 Hugging Face 的 PEFT 和 TRL 库进行 LoRA 微调。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 种主流开源 LLM，每种模型都进行了投毒测试。
*   **充分性：** 实验不仅在仿真中进行，还迁移到了物理机器人上，验证了攻击的真实危害。
*   **客观性：** 论文通过设置 5:3 的干净与投毒数据比例，兼顾了攻击有效性与隐蔽性（CPA 保持在 93% 以上），实验设计较为严谨。
*   **局限性：** 实验主要集中在移动机器人的速度控制（/cmd_vel），对于更复杂的机械臂操作或多机协作场景尚未覆盖。

### 6. 主要结论与发现
*   **结构化对齐是关键：** 推理层投毒无法可靠传播，因为后续的 JSON 生成步骤往往会修正逻辑错误；而**直接针对结构化输出（JSON）的投毒极其有效**。
*   **高成功率与隐蔽性：** 结构化后门攻击实现了 **83% 的平均 ASR**，且 CPA 超过 93%，延迟保持在亚秒级，极难被察觉。
*   **安全与响应的权衡：** 代理验证防御虽能将 ASR 降至 20%，但会将延迟从 <1 秒推高至 **8-9 秒**，这对于需要实时响应的机器人系统（如避障）是不可接受的。

### 7. 优点
*   **揭示了新路径：** 首次系统性地证明了后门可以从数字提示词跨越到物理动作，并指出了“结构化输出”是该攻击路径的核心。
*   **端到端验证：** 提供了从数据集投毒、模型微调、ROS2 集成到物理执行的完整链路分析。
*   **现实意义：** 针对 LoRA 适配器这一常见的供应链分发形式进行研究，具有很强的现实预警价值。

### 8. 不足与局限
*   **防御开销过大：** 提出的二次 LLM 验证机制虽然有效，但计算成本和延迟过高，缺乏轻量级的替代方案。
*   **触发词单一：** 实验中使用的触发词（如“robot car”）较为简单，未探讨更隐蔽的上下文触发或多模态触发。
*   **算力细节缺失：** 缺乏具体的训练时长和硬件资源消耗数据，不利于复现性评估。

（完）
