---
title: A Patch-based Cross-view Regularized Framework for Backdoor Defense in Multimodal Large Language Models
title_zh: 一种用于多模态大语言模型后门防御的基于补丁的跨视图正则化框架
authors: "Tianmeng Fang, Yong Wang, Zetai Kong, Zengzhen Su, Jun Wang, Chengjin Yu, Wei Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04488v1"
tags: ["query:llm"]
score: 9.0
evidence: 多模态大语言模型中的后门防御
tldr: 针对多模态大语言模型在微调阶段易受后门攻击的问题，本文提出一种基于补丁增强和跨视图正则化的统一防御框架。该框架结合补丁级数据增强与跨视图输出差异正则化，利用后门响应对非语义扰动异常不变的特性，拉开原始与扰动视图的输出分布，并引入输出熵约束以平衡模型性能。实验证明该方法在降低攻击成功率的同时，有效保留了模型的正常生成能力。
selection_source: fresh_fetch
motivation: 多模态大语言模型在微调时极易被植入后门，且现有的防御手段难以在抑制攻击与维持正常性能之间取得平衡。
method: 提出一种结合补丁级数据增强、跨视图输出差异正则化以及输出熵约束的防御框架，从特征和分布层面抑制异常行为。
result: 在多种模型、任务和攻击场景下的实验表明，该方法显著降低了后门攻击成功率，并保持了高质量的文本生成。
conclusion: 该框架为多模态大模型在现实低频中毒和隐蔽触发场景下的安全部署提供了有效的技术支撑。
---

## 摘要
多模态大语言模型已成为统一处理视觉和语言任务的重要基础设施。然而，此类模型在有监督微调过程中极易受到后门植入的影响，一旦特定的触发器模式被激活，模型就会稳定地输出攻击者预定义的有害响应。后门防御的核心挑战在于，在低中毒率下抑制攻击成功率的同时，保持模型的正常生成能力。这两个目标本质上是冲突的：强力的抑制往往会降低良性性能，而弱正则化则无法缓解后门行为。为此，我们提出了一种基于补丁增强和跨视图正则性的统一防御框架，该框架从特征表示和输出分布两个层面同时约束模型对触发模式的异常行为。具体而言，补丁级数据增强与跨视图输出差异正则化相结合，利用后门响应对非语义扰动具有异常不变性这一事实，主动拉开原始视图和扰动视图的输出分布，从而显著抑制后门触发的成功率。同时，我们通过施加输出熵约束来避免防御过程中对模型的过度抑制，确保正常指令生成的质量。在三个模型、两个任务和六种攻击上的实验结果表明，我们提出的防御方法在保持高水平正常文本生成能力的同时，有效地降低了攻击成功率。我们的工作使得大规模多模态模型在现实的低频中毒和隐蔽触发场景中能够实现安全、受控的部署。

## Abstract
Multimodal large language models have become an important infrastructure for unified processing of visual and linguistic tasks. However, such models are highly susceptible to backdoor implantation during supervised fine-tuning and will steadily output the attacker's predefined harmful responses once a specific trigger pattern is activated. The core challenge of backdoor defense lies in suppressing attack success under low poisoning ratios while preserving the model's normal generation ability. These two objectives are inherently conflicting. Strong suppression often degrades benign performance, whereas weak regularization fails to mitigate backdoor behaviors. To this end, we propose a unified defense framework based on patch augmentation and cross-view regularity, which simultaneously constrains the model's anomalous behaviors in response to triggered patterns from both the feature representation and output distribution levels. Specifically, patch-level data augmentation is combined with cross-view output difference regularization to exploit the fact that backdoor responses are abnormally invariant to non-semantic perturbations and to proactively pull apart the output distributions of the original and perturbed views, thereby significantly suppressing the success rate of backdoor triggering. At the same time, we avoid over-suppression of the model during defense by imposing output entropy constraints, ensuring the quality of normal command generation. Experimental results across three models, two tasks, and six attacks show that our proposed defense method effectively reduces the attack success rate while maintaining a high level of normal text generation capability. Our work enables the secure, controlled deployment of large-scale multimodal models in realistic low-frequency poisoning and covert triggering scenarios.

---

## 论文详细总结（自动生成）

这篇论文针对多模态大语言模型（MLLM）在微调阶段的安全性问题，提出了一种创新的防御框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型在有监督微调（SFT）过程中极易受到**后门攻击**。攻击者只需注入极少量的中毒样本（如5%），即可使模型在遇到特定触发器（Trigger）时输出预定义的有害内容。
*   **研究动机**：现有的防御方法面临两大挑战：一是低中毒率下后门信号稀疏，难以检测；二是防御手段往往会显著损害模型的正常生成能力（即安全性和可用性的冲突）。论文旨在寻找一种既能抑制后门触发，又能保持模型多模态理解能力的平衡方案。

### 2. 方法论：核心思想与技术细节
论文提出了一个基于**补丁增强（Patch Augmentation）**和**跨视图正则化（Cross-view Regularization）**的统一防御框架，核心逻辑在于利用后门响应的“异常不变性”。

*   **核心思想**：正常样本对图像的局部扰动会有语义范围内的自然波动，而受后门控制的样本无论图像如何被局部遮盖或修改，都会死板地输出固定的攻击目标。防御框架通过主动拉开原始视图与扰动视图的输出分布来打破这种绑定。
*   **关键技术组件**：
    1.  **补丁级视图生成**：对每个输入图像生成两个视图——原始视图和经过局部补丁扰动（遮盖、替换或打乱）的视图。
    2.  **特征一致性正则化 ($L_{patch}$)**：在特征层约束两个视图的表示，防止模型过度依赖局部触发器区域。
    3.  **跨视图差异优化 ($L_{cv-dis}$)**：这是核心损失项。它计算两个视图在输出层 Token 概率分布的相似度（如余弦相似度），并最小化该相似度。这会强制模型在面对非语义扰动时产生不同的输出分布，从而抑制后门触发。
    4.  **不确定性感知输出正则化 ($L_{ent}$)**：引入输出熵约束，防止模型为了追求差异化而导致概率分布坍塌或过度自信，从而保护正常生成的质量。
*   **总损失函数**：$L_{def} = L_{task} + \lambda_1 L_{patch} + \lambda_2 L_{cv-dis} + \lambda_3 L_{ent}$。

### 3. 实验设计
*   **数据集**：使用 **MS COCO** 和 **Flickr30k** 作为基准测试集；使用 **LADD** 作为中毒训练集。
*   **受害模型（Victim Models）**：选取了三种代表性架构：**OpenFlamingo**（基于跨模态注意力）、**Otter**（指令对齐型）、**BLIP-2**（桥接型）。
*   **攻击场景**：对比了 6 种主流后门攻击：BadNets、Blended、LowFrequency、WaNet、InputAware、DualKey。
*   **对比方法（Baselines）**：Fine-Pruning（微调剪枝）、STRIP（输入扰动检测）、Entropy Filtering（熵过滤）。
*   **评估指标**：使用 BLEU@4 和 CIDEr 衡量正常生成能力，使用 ASR（攻击成功率）衡量安全性。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **开销分析**：文中提到，由于需要为扰动视图增加一次额外的前向传播，训练阶段的计算量大约是标准 SFT 的 **2 倍**，显存占用也有适度增加。但**推理阶段无额外开销**。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 3 种模型、2 个大型数据集、6 种攻击方式，并进行了详尽的消融实验和超参数敏感性分析（针对 $\lambda_1, \lambda_2, \lambda_3$）。
*   **充分性评价**：实验设计非常充分。通过跨模型和跨数据集的验证，证明了方法的通用性；消融实验清晰地展示了每个损失项对降低 ASR 和恢复正常性能的贡献，逻辑闭环完整，对比实验具有客观性。

### 6. 主要结论与发现
*   **显著降低 ASR**：在多种攻击下，ASR 从接近 100% 显著降低（例如在 OpenFlamingo 上从 98.9% 降至 27.1%~58.6%）。
*   **性能保持良好**：相比于其他防御方法，该框架在抑制攻击的同时，BLEU@4 和 CIDEr 指标下降极小，甚至在某些被攻击破坏严重的场景下能有效恢复正常生成能力。
*   **机制发现**：跨视图差异优化是打破“触发器-目标”绑定的关键，而熵约束对于稳定训练至关重要。

### 7. 优点与亮点
*   **原理新颖**：从几何结构和分布不变性的角度切入，抓住了后门行为与正常行为在扰动下的本质差异。
*   **无需先验知识**：防御过程不需要知道触发器的形状、位置或中毒样本的标签，具有很强的实用性。
*   **推理友好**：所有的防御约束仅在训练阶段实施，部署后的模型推理速度不受影响。

### 8. 不足与局限
*   **攻击覆盖限制**：核心扰动主要针对局部视觉区域，对于**全局性**或**跨模态协同触发**（如复杂的文本+图像联合触发）的防御效果可能受限。
*   **超参数依赖**：虽然实验证明了稳定性，但在不同任务和模型上仍需手动调整正则化权重。
*   **应用阶段限制**：该方法属于“训练中防御”，对于已经训练完成并部署的“黑盒”模型无法直接应用。

（完）
