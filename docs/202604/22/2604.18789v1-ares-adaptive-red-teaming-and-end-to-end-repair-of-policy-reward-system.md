---
title: "ARES: Adaptive Red-Teaming and End-to-End Repair of Policy-Reward System"
title_zh: ARES：策略-奖励系统的自适应红队测试与端到端修复
authors: "Jiacheng Liang, Yao Ma, Tharindu Kumarage, Satyapriya Krishna, Rahul Gupta, Kai-Wei Chang, Aram Galstyan, Charith Peris"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18789v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 缓解策略和奖励模型中的双重漏洞
tldr: ARES是一个针对RLHF系统中策略模型和奖励模型（RM）协同失效问题的防御框架。它通过“安全导师”动态生成包含主题、人格、策略和目标的对抗性提示，同时暴露LLM和RM的弱点。随后，ARES采用两阶段修复：先微调RM提升检测能力，再优化核心模型。实验证明该方法在保持模型能力的同时显著增强了安全性。
selection_source: fresh_fetch
motivation: 现有的红队测试主要关注策略层面的弱点，忽视了策略模型与奖励模型同时失效的系统性风险。
method: 提出ARES框架，利用“安全导师”生成结构化对抗提示，并执行先修复奖励模型、后优化策略模型的两阶段端到端修复流程。
result: 在多个对抗性安全基准测试中，ARES显著提升了模型的安全鲁棒性，且未损害其原有能力。
conclusion: ARES为RLHF安全对齐建立了一种新范式，通过系统性地发现并修复双重漏洞，实现了更全面的模型保护。
---

## 摘要
基于人类反馈的强化学习（RLHF）是实现大语言模型（LLM）对齐的核心，但它引入了一个关键漏洞：当不完善的奖励模型（RM）未能惩罚不安全行为时，它可能成为单点故障。虽然现有的红队测试方法主要针对策略层面的弱点，但它们忽视了我们所称的“系统性弱点”——即核心 LLM 和 RM 同时失效的情况。我们提出了 ARES，这是一个系统性发现并缓解此类双重漏洞的框架。ARES 采用了一个“安全导师”（Safety Mentor），通过组合结构化组件类型（主题、人格、策略、目标）动态构建语义连贯的对抗性提示词，并生成相应的恶意和安全响应。这种双重目标方法同时暴露了核心 LLM 和 RM 的弱点。利用获得的漏洞，ARES 实施了一个两阶段修复过程：首先微调 RM 以更好地检测有害内容，然后利用改进后的 RM 来优化核心模型。在多个对抗性安全基准测试上的实验表明，ARES 在保持模型能力的同时显著增强了安全鲁棒性，为全面的 RLHF 安全对齐建立了新的范式。

## Abstract
Reinforcement Learning from Human Feedback (RLHF) is central to aligning Large Language Models (LLMs), yet it introduces a critical vulnerability: an imperfect Reward Model (RM) can become a single point of failure when it fails to penalize unsafe behaviors. While existing red-teaming approaches primarily target policy-level weaknesses, they overlook what we term systemic weaknesses cases where both the core LLM and the RM fail in tandem.   We present ARES, a framework that systematically discovers and mitigates such dual vulnerabilities. ARES employs a ``Safety Mentor'' that dynamically composes semantically coherent adversarial prompts by combining structured component types (topics, personas, tactics, goals) and generates corresponding malicious and safe responses. This dual-targeting approach exposes weaknesses in both the core LLM and the RM simultaneously. Using the vulnerabilities gained, ARES implements a two-stage repair process: first fine-tuning the RM to better detect harmful content, then leveraging the improved RM to optimize the core model. Experiments across multiple adversarial safety benchmarks demonstrate that ARES substantially enhances safety robustness while preserving model capabilities, establishing a new paradigm for comprehensive RLHF safety alignment.

---

## 论文详细总结（自动生成）

### 论文总结：ARES —— 策略-奖励系统的自适应红队测试与端到端修复

#### 1. 论文的核心问题与整体含义
在基于人类反馈的强化学习（RLHF）中，奖励模型（RM）负责引导核心大语言模型（Core LLM）走向安全。然而，RM 本身可能存在盲点或偏见。论文指出，现有的红队测试（Red-Teaming）大多只关注策略模型（LLM）的弱点，而忽视了**系统性弱点（Systemic Weaknesses）**：即 LLM 生成了有害内容，而 RM 却给出了高分奖励。这种双重失效会导致模型在对齐过程中产生严重的安全性漏洞。ARES 框架旨在系统性地发现并修复这种 LLM 与 RM 协同失效的问题。

#### 2. 方法论
ARES 采用两阶段闭环范式：

*   **第一阶段：自适应漏洞发现（Adaptive Vulnerability Discovery）**
    *   **安全导师（Safety Mentor）：** 使用一个强大的 LLM 作为“导师”，基于结构化分类法（主题、人格、目标、策略）组合生成语义连贯且具有欺骗性的对抗提示词。
    *   **漏洞分类：** 将失效模式分为三类：
        *   **Type A（仅 RM 误判）：** RM 对合成的有害响应给出高分，但 LLM 本身未生成有害内容。
        *   **Type B（仅 LLM 策略漏洞）：** LLM 生成有害内容，但 RM 正确识别并给出低分。
        *   **Type C（系统性弱点）：** LLM 生成有害内容且 RM 给出高分（最危险）。
    *   **层级自适应采样：** 引入反馈机制，根据攻击成功率动态调整不同攻击组件（如特定主题或策略）的权重，实现高效的漏洞挖掘。
*   **第二阶段：端到端系统修复（End-to-End System Repair）**
    *   **RM 修复：** 首先利用发现的 Type A 和 Type C 漏洞数据微调奖励模型，提升其辨别有害内容的能力。
    *   **Core LLM 修复：** 使用修复后的 RM 作为奖励信号，通过强化学习（采用 Dr. GRPO 算法）优化核心模型，解决 Type B 和 Type C 漏洞。

#### 3. 实验设计
*   **实验对象：** 核心模型为 Qwen3-1.7B，奖励模型为 Skywork-RM-Qwen3-4B，安全导师为 Qwen3-8B 变体。
*   **数据集与基准（Benchmark）：**
    *   **安全性：** StrongReject、HarmBench、PKU-SafeRLHF、XSTest（测试过度拒绝）。
    *   **通用能力：** MMLU、GSM8K、TruthfulQA、AlpacaEval。
*   **对比方法：**
    *   **基础基线：** 原始模型、初始 RLHF 模型、通用安全对齐（使用 PKU-SafeRLHF 静态数据）。
    *   **SOTA 红队方法对比：** FLIRT、APRT、FERRET（仅对比数据生成阶段的修复效果）。

#### 4. 资源与算力
*   **硬件：** 使用了 1 台配备 **8 张 NVIDIA A100 GPU** 的 EC2 实例。
*   **耗时：** 端到端运行总时长约 **13 小时**。
    *   自适应漏洞发现：9 小时（生成 4,000 个样本，发现率 63.5%）。
    *   系统修复：4 小时（RM 微调 2.5 小时，LLM 优化 1.5 小时）。

#### 5. 实验数量与充分性
*   **实验规模：** 生成了 4,000 个高质量漏洞样本。
*   **消融实验：** 验证了自适应采样与均匀采样的对比、不同数据混合比例的影响、以及迭代红队测试的效果。
*   **验证充分性：** 论文进行了**人工验证**（3 名研究员对 100 个样本进行标注，一致性极高）和**跨模型法官验证**（使用 DeepSeek-V3.2 作为独立法官），证明了评估信号的客观性。实验设计覆盖了安全与能力的平衡，对比了多种 SOTA 方法，逻辑严密且公平。

#### 6. 主要结论与发现
*   **安全性显著提升：** ARES 在 StrongReject 和 HarmBench 上的安全率分别达到 0.97 和 0.95，远超通用安全对齐基线。
*   **能力保持良好：** 在大幅提升安全性的同时，MMLU 和 GSM8K 等能力指标几乎没有下降，且过度拒绝率（XSTest）保持在较低水平。
*   **数据效率高：** 仅需 4,000 个针对性样本，效果即可超过使用 10,800 个通用样本的基线。
*   **系统性修复的必要性：** 实验证明，先修复 RM 再修复 LLM 的顺序对于消除系统性弱点至关重要。

#### 7. 优点
*   **视角全面：** 首次明确提出并针对“策略-奖励”双重失效的系统性弱点进行研究。
*   **自适应性强：** 相比静态红队测试，其层级自适应采样能更高效地锁定模型特定的薄弱环节。
*   **闭环修复：** 提供了从发现到分类再到针对性修复的完整工程化方案。
*   **高效实用：** 训练时长短，数据需求量小，具有很强的实操性。

#### 8. 不足与局限
*   **计算成本：** 漏洞发现阶段虽然是纯推理，但仍需生成和评估数万次，对算力有一定要求。
*   **法官依赖：** 框架高度依赖“LLM-as-a-Judge”的质量，如果法官模型本身存在偏见，可能影响采样效果。
*   **覆盖范围：** 目前主要针对单轮文本交互，尚未扩展到多轮对话、长文本、多模态或代码生成等更复杂的安全场景。
*   **残留风险：** 即使经过修复，仍有约 4.3% 的残留漏洞，主要集中在语义模糊或主观性强的领域，难以完全消除。

（完）
