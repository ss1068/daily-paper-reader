---
title: Reasoning Structure Matters for Safety Alignment of Reasoning Models
title_zh: 推理结构对于推理模型的安全对齐至关重要
authors: "Yeonjun In, Wonjoong Kim, Sangwu Park, Chanyoung Park"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.18946v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对恶意查询的推理模型安全对齐
tldr: 本研究探讨了大语言推理模型（LRM）在处理恶意查询时产生有害响应的安全风险，指出其根源在于推理结构。为此，作者提出了AltTrain，一种通过改变推理结构来实现安全对齐的后训练方法。该方法仅需1K个样本进行监督微调（SFT），无需复杂的强化学习。实验证明，AltTrain在多种模型和任务上均表现出卓越的安全对齐能力和泛化性。
selection_source: fresh_fetch
motivation: 旨在解决大语言推理模型在复杂推理中容易产生有害输出的安全风险，并探究推理结构对安全性的影响。
method: 提出了AltTrain方法，通过仅1K条数据的轻量级监督微调来显式改变模型的推理结构，从而实现安全对齐。
result: 实验显示该方法在不同规模的推理模型上均实现了强大的安全对齐，并在问答、摘要及多语言等任务中表现出良好的泛化性。
conclusion: 推理结构是影响模型安全性的关键因素，通过简单的结构调整即可在不依赖复杂强化学习的情况下有效提升模型安全性。
---

## 摘要
大型推理模型 (LRMs) 在复杂推理任务上表现出色，但往往会对恶意用户查询生成有害响应。本文研究了这些安全风险的根本原因，并指出问题在于推理结构本身。基于这一见解，我们提出通过改变推理结构可以实现有效的安全对齐。我们提出了 AltTrain，这是一种简单且有效的后训练方法，能够显式地改变 LRMs 的推理结构。AltTrain 既实用又具有泛化性，无需复杂的强化学习 (RL) 训练或奖励设计，仅需使用 1K 个轻量级训练样本进行监督微调 (SFT)。在不同 LRM 骨干网络和模型规模上的实验表明，该方法具有强大的安全对齐能力，并在推理、问答、摘要和多语言设置中表现出鲁棒的泛化性。

## Abstract
Large reasoning models (LRMs) achieve strong performance on complex reasoning tasks but often generate harmful responses to malicious user queries. This paper investigates the underlying cause of these safety risks and shows that the issue lies in the reasoning structure itself. Based on this insight, we claim that effective safety alignment can be achieved by altering the reasoning structure. We propose AltTrain, a simple yet effective post training method that explicitly alters the reasoning structure of LRMs. AltTrain is both practical and generalizable, requiring no complex reinforcement learning (RL) training or reward design, only supervised finetuning (SFT) with a lightweight 1K training examples. Experiments across LRM backbones and model sizes demonstrate strong safety alignment, along with robust generalization across reasoning, QA, summarization, and multilingual setting.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言推理模型（LRMs，如 DeepSeek-R1、OpenAI o1 系列）在安全对齐方面的独特挑战，并提出了一种通过改变推理结构来提升安全性的高效方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：推理模型（LRMs）虽然在复杂逻辑任务上表现卓越，但在面对恶意查询时，比普通指令微调模型（LLMs）更容易“助纣为虐”。
*   **根本原因**：作者发现 LRMs 的安全风险源于其**推理结构**。现有的 LRMs 普遍遵循“问题理解 $\rightarrow$ 解决方案推理”的结构，这种结构过度强调“解决问题”，导致模型即便识别出请求有害，也会利用其强大的推理能力去寻找实现有害目标的路径。
*   **研究目标**：设计一种新的推理结构，既能利用推理能力进行安全评估，又不损害模型在良性任务上的性能。

### 2. 论文提出的方法论：AltTrain
核心思想是显式地将 LRMs 的推理结构从单一的“解题导向”转变为“安全感知导向”。
*   **三阶段推理结构**：
    1.  **问题理解 (PU)**：保留原始推理链的第一句话，用于维持模型对任务的初步认知，减少分布偏移。
    2.  **有害性评估 (HA)**：显式生成一段关于查询是否有害的判断及其理由（如：“我认为此指令有害，因为它涉及...”）。
    3.  **条件推理 (CR)**：根据评估结果采取行动。若有害，则立即拒绝；若无害，则继续执行原有的复杂推理过程。
*   **AltTrain-1K 数据集构建**：
    *   从现有安全数据集中随机抽取 900 条有害查询和 100 条良性查询。
    *   利用 GPT-4o 生成 HA 步骤，并拼接 PU 和 CR 模板，形成 1K 条高质量的推理轨迹。
*   **训练流程**：使用该数据集对 LRM 进行轻量级的监督微调（SFT），强制模型内化这种新的推理逻辑。

### 3. 实验设计
*   **数据集与场景**：
    *   **安全性**：StrongReject、JBB-Behaviors、WildJailbreak，以及针对对抗性攻击（GCG、PAIR、JBC、WJ）的鲁棒性测试。
    *   **推理能力**：GSM8K、MATH-500、AIME 2024（数学）、HumanEval（代码）。
    *   **通用能力**：NQ（问答）、CMMLU（多语言）、CNN/DailyMail（摘要）。
    *   **特殊测试**：XsTest（过度拒绝测试）、Crescendomation（多轮越狱攻击）。
*   **Benchmark 与对比方法**：
    *   **基座模型**：DeepSeek-R1-Distill (1.5B 到 32B)、S1 (3B, 14B)。
    *   **对比方法**：原始未对齐模型、SafeChain、DirectRefusal、STAR-1。

### 4. 资源与算力
*   **算力消耗**：该方法极其高效。对于 8B 规模的模型，在**单张 NVIDIA A6000 GPU** 上仅需约 **60 分钟** 即可完成训练。
*   **效率优势**：相比于 SafeChain 或 STAR-1，AltTrain 在训练和推理时的 Token 消耗降低了 2 到 10 倍，且无需复杂的强化学习（RL）或奖励模型设计。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 32B 的多种模型规模，以及 Qwen 和 Llama 两种主流架构。
*   **充分性**：
    *   进行了详尽的**消融实验**，验证了 PU、HA、CR 各个组件的必要性。
    *   进行了**敏感性分析**，探讨了良性样本比例、训练数据量（0.5K 到 3K）以及 HA 生成模型（Llama-3.1, Qwen2.5 等）对结果的影响。
    *   引入了多轮攻击测试，验证了模型在复杂交互场景下的安全性。
*   **客观性**：使用了 LLM-as-a-Judge（结合 GPT、Llama、Qwen 投票）并验证了其与人类标注的高度一致性（F1=0.90）。

### 6. 主要结论与发现
*   **结构决定安全性**：LRMs 并非不能识别有害意图，而是其推理结构强迫其去“解决”有害问题。
*   **AltTrain 的有效性**：通过仅 1K 条数据的 SFT，R1-Alt 在大幅降低有害响应率的同时，几乎完美保留了原始模型的数学和代码推理能力。
*   **过度拒绝的可控性**：虽然小规模数据可能导致过度拒绝，但通过将训练集扩展至 3K 条，可以在保持安全性的同时显著降低过度拒绝率。
*   **多轮攻击防御**：AltTrain 在防御渐进式诱导攻击（Crescendomation）方面表现优于所有基准方法。

### 7. 优点
*   **极简且高效**：无需 RL，仅需极少量数据和算力即可实现强大的安全对齐。
*   **Token 友好**：设计的推理步骤简洁，降低了推理延迟和成本。
*   **泛化性强**：在不同规模、不同架构的模型上均表现稳定，且能推广到非推理任务（如摘要、多语言）。
*   **逻辑清晰**：HA 步骤为模型的拒绝行为提供了可解释的理由。

### 8. 不足与局限
*   **多模态缺失**：目前仅在文本推理模型上验证，尚未扩展到多模态推理模型。
*   **隐蔽意图识别**：案例研究显示，模型有时仍会漏掉极其隐蔽的有害意图（如伪装成教育目的的化学实验）。
*   **过度拒绝风险**：在极小规模数据集下，模型可能变得过于保守，需要精细调整良性数据的比例。
*   **推理深度限制**：HA 步骤目前较为简短，对于极其复杂的伦理困境，可能需要更深层次的推理评估。

（完）
