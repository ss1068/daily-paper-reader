---
title: An Empirical Study of Multi-Generation Sampling for Jailbreak Detection in Large Language Models
title_zh: 大语言模型越狱检测中多生成采样的实证研究
authors: "Hanrui Luo, Shreyank N Gowda"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18775v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 基于多生成采样的越狱检测研究
tldr: 本研究针对大语言模型越狱检测的挑战，提出并评估了多生成采样方法。通过在JailbreakBench数据集上测试TF-IDF和生成不一致性检测器，发现单次输出评估会严重低估模型的脆弱性。研究表明，适度的多样本审计能更可靠地揭示有害行为，并分析了检测信号在不同模型间的泛化性，为提升模型安全性提供了实践指导。
selection_source: fresh_fetch
motivation: 强对齐模型仅在极少数情况下产生有害输出，导致传统的单次采样评估难以准确捕捉越狱风险。
method: 在不同采样预算下，对比评估了基于词频（TF-IDF）和基于生成不一致性的两种越狱检测器在多个模型上的表现。
result: 增加采样次数能显著揭示更多潜在的有害行为，且检测信号在同系列模型间表现出较强的迁移性。
conclusion: 适度的多样本审计是评估模型脆弱性和提升越狱检测可靠性的一种更实用且有效的方法。
---

## 摘要
检测大语言模型中的越狱行为仍然具有挑战性，特别是当强对齐模型仅在极少数情况下产生有害输出时。在这项工作中，我们利用 JailbreakBench Behaviors 数据集以及具有不同对齐强度的多个生成模型，对现实条件下的基于输出的越狱检测进行了实证研究。我们在不同的采样预算下评估了词汇 TF-IDF 检测器和基于生成不一致性的检测器。我们的结果表明，单次输出评估系统性地低估了越狱漏洞，因为增加采样生成的数量会揭示出更多的有害行为。最显著的改进发生在从单次生成转向适度采样时，而更大的采样预算带来的收益则递减。跨生成器实验表明，检测信号在模型间具有部分泛化性，且在相关的模型家族内部观察到更强的迁移性。类别层面的分析进一步揭示，词汇检测器捕捉到的是行为信号和特定主题线索的混合体，而非纯粹的有害行为。总体而言，我们的研究结果表明，适度的多样本审计为评估模型漏洞和改进大语言模型越狱检测提供了一种更可靠且实用的方法。代码将开源。

## Abstract
Detecting jailbreak behaviour in large language models remains challenging, particularly when strongly aligned models produce harmful outputs only rarely. In this work, we present an empirical study of output based jailbreak detection under realistic conditions using the JailbreakBench Behaviors dataset and multiple generator models with varying alignment strengths. We evaluate both a lexical TF-IDF detector and a generation inconsistency based detector across different sampling budgets. Our results show that single output evaluation systematically underestimates jailbreak vulnerability, as increasing the number of sampled generations reveals additional harmful behaviour. The most significant improvements occur when moving from a single generation to moderate sampling, while larger sampling budgets yield diminishing returns. Cross generator experiments demonstrate that detection signals partially generalise across models, with stronger transfer observed within related model families. A category level analysis further reveals that lexical detectors capture a mixture of behavioural signals and topic specific cues, rather than purely harmful behaviour. Overall, our findings suggest that moderate multi sample auditing provides a more reliable and practical approach for estimating model vulnerability and improving jailbreak detection in large language models. Code will be released.

---

## 论文详细总结（自动生成）

这是一份关于论文《An Empirical Study of Multi-Generation Sampling for Jailbreak Detection in Large Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前大语言模型（LLM）的越狱（Jailbreak）检测大多依赖于单次输出评估，但由于 LLM 生成具有随机性，单次输出往往无法代表模型的真实行为。
*   **研究动机**：强对齐模型产生有害输出的概率极低。如果仅采样一次，可能会漏掉那些在多次尝试中才会出现的有害行为，从而系统性地低估模型的脆弱性。本文旨在探讨多生成采样（Multi-Generation Sampling）如何影响越狱检测的准确性和漏洞评估的可靠性。

### 2. 核心方法论
论文将越狱检测视为一个**多样本审计（Multi-sample Auditing）**问题，核心思想是通过增加采样预算（$k$ 次生成）来捕捉模型行为的分布。
*   **检测器设计**：
    *   **词汇检测器（TF-IDF）**：使用 TF-IDF 特征配合逻辑回归（Logistic Regression），捕捉输出文本中的词汇模式。
    *   **不一致性检测器（NegBLEURT 变体）**：通过分析同一提示词下多个输出之间的语义不一致性，利用孤立森林（Isolation Forest）进行异常检测。
*   **聚合策略**：采用“ANY 策略”，即在 $k$ 次采样中，只要有一次输出被判定为越狱，则该交互被视为不安全。
*   **判定机制**：结合了拒绝判定（Refusal-based）和内容判定（Content-based）两种启发式法官，为实验提供地面真值（Ground Truth）。

### 3. 实验设计
*   **数据集**：主要使用 **JailbreakBench-Behaviours** 数据集，包含结构化的有害目标和类别标签。
*   **生成模型（Generator）**：选择了具有不同对齐强度和规模的模型，包括 DistilGPT2、Qwen2.5-0.5B-Instruct、Qwen2.5-1.5B-Instruct 和 Gemma。
*   **对比维度**：
    *   **采样预算**：评估 $k=1$ 到 $k=5$ 时的检测表现。
    *   **跨模型泛化**：在一个模型上训练检测器，在另一个模型上测试（Cross-generator transfer）。
    *   **类别分析**：深入分析检测器在不同有害类别下的特征权重。
*   **评价指标**：ROC-AUC、PR-AUC（针对类别不平衡）和平衡准确率。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到代码将会开源，检测器采用了轻量级的 TF-IDF 和逻辑回归，暗示其计算开销较低，适合在资源受限的部署环境中使用。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同架构和对齐水平的模型，进行了从单次到多次采样的对比实验，并完成了完整的跨模型迁移矩阵实验。
*   **充分性评价**：实验设计较为充分。通过自助法（Bootstrap）计算了 95% 的置信区间，增强了结果的可信度。同时，不仅关注宏观指标，还进行了特征层面的定性分析，揭示了检测器的工作原理。

### 6. 主要结论与发现
*   **单次评估的局限性**：单次输出评估会严重低估越狱风险。随着采样次数 $k$ 的增加，观察到的越狱成功率显著上升。
*   **采样边际效应**：从 $k=1$ 增加到 $k=3$ 时改进最明显，超过 $k=3$ 后收益递减，且计算成本增加。
*   **对齐悖论**：模型对齐越强，有害输出越稀疏，导致检测信号变得不稳定，检测任务演变为“稀疏事件检测”难题。
*   **信号本质**：词汇检测器主要捕捉的是“指令式/步骤式”的语体风格（如“Step 1...”, “Start by...”），而非直接识别有害意图。

### 7. 优点与亮点
*   **现实性强**：聚焦于黑盒场景下的输出端检测，不依赖模型内部权重或梯度，具有很强的工程落地价值。
*   **揭示了随机性的重要性**：系统性地证明了在安全评估中考虑 LLM 随机性的必要性，提出了更科学的审计协议。
*   **深入的特征分析**：通过分析 TF-IDF 权重，揭示了简单检测器可能被输出风格（而非内容）误导的风险。

### 8. 不足与局限
*   **检测器能力上限**：基于词汇（TF-IDF）的方法容易受到语体风格的影响，产生较多误报（例如，结构化的拒绝回答可能被误判为越狱）。
*   **稀疏性挑战**：在极强对齐的模型上，由于正样本极少，PR-AUC 指标表现较差，检测器的实际应用效果受限。
*   **采样成本**：虽然 $k=3$ 是折中选择，但在高并发生产环境中，增加 3 倍的生成开销仍是一个需要权衡的因素。

（完）
