---
title: "ProbeLogits: Kernel-Level LLM Inference Primitives for AI-Native Operating Systems"
title_zh: ProbeLogits：用于 AI 原生操作系统的内核级 LLM 推理原语
authors: Daeyeon Son
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11943v1"
tags: ["query:llm"]
score: 8.0
evidence: 内核级LLM推理，用于分类对抗性攻击和危险操作
tldr: 本研究提出了ProbeLogits，一种集成在操作系统内核中的LLM推理原语，旨在为AI原生系统提供底层治理。通过在内核层读取单次前向传播的Logit分布，ProbeLogits无需额外训练即可对智能体行为进行安全分类。该方法在Anima OS中实现，利用上下文校准和可调参数α平衡安全与性能，在OS动作基准测试中达到0.98的F1分数，为AI安全治理提供了比应用层更难绕过的底层保障。
selection_source: fresh_fetch
motivation: 现有的AI安全分类器多位于应用层，易被绕过，且缺乏与操作系统底层权限控制的深度集成。
method: 在内核中实现ProbeLogits操作，通过单次推理读取特定Token的Logit分布，并引入上下文校准和可调策略参数α进行行为分类。
result: "在OS动作基准测试中F1值达0.98，在ToxicChat上达到Llama Guard 3性能的89%，且单次分类仅需65ms。"
conclusion: 将LLM Logit向量作为内核级治理原语，不仅提升了AI系统的安全性与可控性，还通过KV缓存管理实现了类似传统进程的AI状态管理。
---

## 摘要
在内部运行 LLM 推理的操作系统内核可以在生成任何文本之前读取 Logit 分布，并将其作为治理原语进行操作。我提出了 ProbeLogits，这是一种内核级操作，它通过执行单次前向传递并读取特定 Token 的 Logit，在零学习参数的情况下将智能体行为分类为安全或危险。在包含 260 个提示词的操作系统行为基准测试（涵盖对抗性攻击等 9 个类别）中，ProbeLogits 使用 4 位量化的通用 7B 模型实现了 F1=0.98

## Abstract
An OS kernel that runs LLM inference internally can read logit distributions before any text is generated -- and act on them as a governance primitive. I present ProbeLogits, a kernel-level operation that performs a single forward pass and reads specific token logits to classify agent actions as safe or dangerous, with zero learned parameters. On a 260-prompt OS action benchmark (9 categories including adversarial attacks), ProbeLogits achieves F1=0.980, Precision=1.000, and Recall=0.960 using a general-purpose 7B model at 4-bit quantization. On ToxicChat (1,000 human-annotated real conversations), it achieves F1=0.790 at default calibration strength $α$=1.0, improving to F1=0.837 at $α$=0.5 -- 89% of Llama Guard 3's F1~0.939 with zero learned parameters. A key design contribution is the calibration strength $α$, which serves as a deployment-time policy knob rather than a learned hyperparameter. By adjusting $α$, the OS can enforce strict policies for privileged operations ($α\geq 0.8$, maximizing recall) or relaxed policies for conversational agents ($α$=0.5, maximizing precision). Contextual calibration improves accuracy from 64.8% to 97.3% on the custom benchmark. I implement ProbeLogits within Anima OS, a bare-metal x86_64 OS written in 80,400 lines of Rust. Because agent actions must pass through kernel-mediated host functions, ProbeLogits enforcement operates below the WASM sandbox boundary, making it significantly harder to circumvent than application-layer classifiers. Each classification costs 65ms on 7B -- fast enough for per-action governance. I also show that treating KV cache as process state enables checkpoint, restore, and fork operations analogous to traditional process management. To my knowledge, no prior system exposes LLM logit vectors as OS-level governance primitives.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ProbeLogits** 的系统，旨在将大语言模型（LLM）推理原语直接集成到操作系统内核中，为 AI 原生操作系统（如作者开发的 Anima OS）提供底层的安全治理和状态管理能力。

### 1. 核心问题与研究动机
*   **核心问题**：传统操作系统将进程视为不透明的实体，无法理解 AI 智能体的“意图”。现有的 AI 安全治理（如提示词过滤、输出审计）大多发生在应用层，存在以下三大缺陷：
    *   **高延迟**：基于文本生成的分类需要多次前向传播和解析，速度慢。
    *   **易绕过**：应用层过滤器与智能体处于同一特权级，容易被恶意智能体绕过。
    *   **语义不透明**：内核无法获取模型内部状态（如 Logit、置信度），难以进行智能调度。
*   **研究背景**：随着 AI 智能体逐渐接管系统操作，需要一种像传统权限控制一样底层、高效且不可绕过的治理机制。

### 2. 方法论
*   **核心思想**：**ProbeLogits**。不再让模型生成完整的“Yes/No”文本，而是执行单次前向传播（Forward Pass），直接读取词表特定位置（Verbalizers，如“Safe”和“Dangerous”对应的 Token）的 Logit 分布。
*   **关键技术细节**：
    *   **Logit 分类原语**：通过对目标 Token 的 Logit 进行 Softmax 计算，在单次推理中获得分类概率。
    *   **上下文校准（Contextual Calibration）**：引入策略参数 $\alpha$。通过在启动时测量模型对空输入的固有偏见（Bias），并在推理时进行修正，解决模型由于指令微调产生的“谄媚效应”（Sycophancy）。
    *   **KV 缓存即进程状态**：将 LLM 的 KV Cache 类比为 CPU 寄存器，在内核层实现 `kv_fork()`、`kv_checkpoint()` 和 `kv_restore()`，支持智能体的状态快照、分支探索和迁移。
    *   **三阶段防御流水线**：对抗性预过滤（正则扫描） $\rightarrow$ 输入清洗 $\rightarrow$ 校准后的 ProbeLogits 前向传播。
*   **系统实现**：在用 Rust 编写的裸机操作系统 **Anima OS** 中实现，包含自研的 AVX-512 优化推理引擎。

### 3. 实验设计
*   **数据集/场景**：
    *   **自定义 OS 动作基准**：260 个提示词，涵盖 9 个类别（显式危险、隐私侵犯、对抗性攻击、指令篡改等）。
    *   **ToxicChat**：1,000 条真实人类对话标注数据，用于验证通用场景下的表现。
*   **Benchmark 与对比方法**：
    *   对比 **Generate-then-Parse**（生成文本后解析）方法。
    *   对比 **Llama Guard 3**（专用安全模型）的性能指标。
    *   在 Linux 环境下的 **llama.cpp** 作为推理性能基准。

### 4. 资源与算力
*   **硬件环境**：AMD Ryzen 9 9800X3D CPU (8核/16线程)，60GB DDR5-6000 RAM。
*   **算力说明**：论文明确指出**未使用 GPU**，完全依赖 CPU 的 AVX-512 VNNI 指令集进行 4-bit 量化推理。
*   **模型**：Qwen2.5-7B-Instruct (4-bit 量化) 和 SmolLM2-135M。

### 5. 实验数量与充分性
*   **实验规模**：进行了吞吐量对比、各操作延迟测试、安全分类准确率消融实验、以及不同校准强度 $\alpha$ 对 Precision/Recall 影响的分析。
*   **充分性评价**：实验设计较为全面，不仅验证了性能（带宽利用率达 85%），还通过标准数据集（ToxicChat）验证了泛化能力。消融实验清晰地展示了“校准”和“预过滤”对最终 F1 分数的贡献。

### 6. 主要结论与发现
*   **性能飞跃**：在 7B 模型上，ProbeLogits 分类仅需 **65ms**，比传统的生成解析方法快 10 倍，且达到了硬件内存带宽的物理极限。
*   **高准确性**：在 OS 动作基准上实现 **F1=0.980**，且 **Precision 达到 1.000**（零误报），这对于操作系统自动化执行至关重要。
*   **策略可调**：参数 $\alpha$ 允许系统在部署时动态调整安全策略（如特权操作使用高 $\alpha$ 以提高召回率，对话场景使用低 $\alpha$ 以提高精确率）。
*   **不可绕过性**：由于治理逻辑运行在内核态且位于 WASM 沙箱之下，智能体无法通过应用层手段关闭安全检查。

### 7. 优点
*   **架构创新**：首次将 LLM Logit 向量提升为操作系统级的治理原语，思路超前。
*   **极致优化**：裸机 Rust 实现，消除了系统调用和上下文切换开销，推理性能媲美或超越 llama.cpp。
*   **实用性强**：通过 KV Cache 的进程化管理，为 AI 智能体的调度和容错提供了坚实的底层支持。

### 8. 不足与局限
*   **硬件依赖**：目前仅支持 CPU 推理，虽然达到了带宽极限，但对于更大规模模型（如 70B+），缺乏 GPU 加速支持是瓶颈。
*   **泛化风险**：在自由对话（ToxicChat）上的 F1 分数（0.837）虽接近专用模型，但仍有差距，说明零样本（Zero-shot）内核分类在复杂语义下仍有限制。
*   **评估偏差**：自定义基准的标注由开发者完成，可能存在过拟合特定场景的风险。
*   **应用限制**：目前仅支持简单的选择性语法约束，复杂的 JSON 或代码结构化输出支持尚在计划中。

（完）
