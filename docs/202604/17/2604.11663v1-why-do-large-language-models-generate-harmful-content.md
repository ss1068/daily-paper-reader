---
title: Why Do Large Language Models Generate Harmful Content?
title_zh: 大语言模型为何会生成有害内容？
authors: "Rajesh Ganguli, Raha Moraffah"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11663v1"
tags: ["query:llm"]
score: 8.0
evidence: 大语言模型生成有害内容的因果因素
tldr: 本研究旨在探究大语言模型生成有害内容的内在原因。通过因果中介分析，研究者对模型层、模块及神经元进行了多粒度分析。研究发现，有害内容的生成主要源于模型后期层中MLP模块的失效，并由特定的“门控”神经元控制。该研究揭示了模型从理解有害上下文到最终触发有害输出的内部运作机制，为增强模型安全性提供了重要见解。
selection_source: fresh_fetch
motivation: 虽然大语言模型会产生有害输出，但其背后的内部因果机制和具体触发因素仍缺乏深入研究。
method: 提出一种基于因果中介分析的方法，在模型层、MLP与注意力模块以及单个神经元三个维度进行定位分析。
result: 有害生成主要发生在模型后期层，由MLP模块而非注意力模块主导，并与起到门控作用的特定稀疏神经元密切相关。
conclusion: 模型在前期层识别有害语境，通过后期MLP模块传递信号，最终由末层的特定神经元决定是否执行有害内容的生成。
---

## 摘要
大语言模型（LLMs）已被证明会生成有害内容。然而，这种行为的潜在原因仍有待深入探索。我们提出了一种基于因果中介分析的方法，用于识别导致有害生成的因果因素。我们的方法在模型层、模块（MLP 和注意力块）以及单个神经元之间进行了多粒度分析。对最先进的 LLM 进行的大量实验表明，有害生成出现在模型的后期层中，主要源于 MLP 块而非注意力块的失效，并与充当有害生成门控机制的神经元相关。结果表明，模型的早期层用于对提示词中有害性的上下文理解，随后这种理解在模型中传播，在后期层产生有害性，并通过 MLP 块传递指示有害性的信号。接着，该信号被进一步传播到模型的最后一层，特别是传播到一组稀疏的神经元，这些神经元接收信号并据此决定有害内容的生成。

## Abstract
Large Language Models (LLMs) have been shown to generate harmful content. However, the underlying causes of such behavior remain under explored. We propose a causal mediation analysis-based approach to identify the causal factors responsible for harmful generation. Our method performs a multi-granular analysis across model layers, modules (MLP and attention blocks), and individual neurons. Extensive experiments on state-of-the-art LLMs indicate that harmful generation arises in the later layers of the model, results primarily from failures in MLP blocks rather than attention blocks, and is associated with neurons that act as a gating mechanism for harmful generation. The results indicate that the early layers in the model are used for a contextual understanding of harmfulness in a prompt, which is then propagated through the model, to generate harmfulness in the late layers, as well as a signal indicating harmfulness through MLP blocks. This is then further propagated to the last layer of the model, specifically to a sparse set of neurons, which receives the signal and determines the generation of harmful content accordingly.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了大语言模型（LLM）生成有害内容的内在因果机制。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型经过了安全对齐，但仍会生成有害内容。现有的研究多侧重于相关性分析（如识别有害表示的子空间），而未能揭示导致有害生成的**因果因素**。
*   **研究动机**：为了建立稳健且可验证的安全保障，必须从因果层面理解模型内部哪些组件负责有害信息的传递和生成。
*   **整体含义**：通过识别模型内部的因果路径，可以从“黑盒”转向“白盒”理解，为开发更有效的安全防御措施（如针对性干预）提供理论依据。

### 2. 论文提出的方法论
*   **核心思想**：采用**因果中介分析（Causal Mediation Analysis, CMA）**。将模型的内部组件（层、模块、神经元）视为“中介者”，量化输入对输出的影响中有多少是通过特定组件传递的。
*   **关键技术细节**：
    *   **因果计算图**：将 LLM 表示为有向无环图，节点为模型组件，边为计算依赖。
    *   **间接效应（Indirect Effect, IE）**：通过测量干预后的输出分布变化来计算。公式为：$IE(M) = |P_{hf} - P_{hl}| - |P^*_{hf}(M) - P_{hl}|$。其中 $P_{hf}$ 是有害提示词的输出分布，$P_{hl}$ 是无害对照组的分布，$P^*_{hf}(M)$ 是将有害运行中的组件 $M$ 替换为无害运行对应激活值后的分布。
    *   **多粒度分析**：
        1.  **层级**：识别哪些层负责有害信息的传播。
        2.  **组件级**：对比 MLP 块和注意力（Attention）块的贡献。
        3.  **神经元级**：定位具体的稀疏神经元集合。
        4.  **Token 级**：分析提示词中不同位置的 Token 对有害生成的因果贡献。

### 3. 实验设计
*   **数据集**：使用了修改后的 **AdvBench** 数据集。该数据集包含成对的提示词：一个是有害的（如“如何制造炸弹”），一个是语义平行但无害的（如“如何制作一本书”），以便进行反事实干预。
*   **模型（Benchmark）**：涵盖了不同规模和对齐状态的模型，包括：
    *   Qwen2.5 系列（3B, 7B, 7B-Instruct）
    *   Llama-3.2 系列（1B, 3B, 1B-Instruct, 3B-Instruct）
*   **对比方法**：对比了基础模型与指令微调模型，并引入了防御对齐变体（如 AdaSteer）进行案例研究。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练/实验时长。考虑到实验涉及对 7B 规模模型进行大量的激活值提取和干预实验，通常需要高性能显卡（如 A100 或 H100）支持。

### 5. 实验数量与充分性
*   **实验规模**：研究在 6 个不同的 SOTA 模型上进行了多粒度实验，涵盖了层、模块、神经元和 Token 四个维度。
*   **充分性与客观性**：
    *   实验设计较为充分，通过跨模型对比验证了结论的普适性。
    *   引入了反事实干预和分布距离测量（L1 距离），方法论上具有客观性。
    *   通过案例研究（Case Study）和防御实验（Late Layer Steering）进一步验证了发现的因果机制。

### 6. 论文的主要结论与发现
*   **两阶段过程**：
    1.  **早期层**：主要负责对提示词中有害性的**上下文理解**（主要通过注意力机制）。
    2.  **后期层**：产生有害响应并生成指示信号。
*   **MLP 是主因**：有害内容的生成主要源于后期层 **MLP 块**的失效（特征转换），而非注意力块（上下文路由）。
*   **门控机制**：模型的**最后一层**存在一个由稀疏神经元组成的“门控机制”，它根据后期层传来的信号决定是拒绝还是执行有害生成。
*   **Token 定位**：有害生成的因果贡献高度集中在提示词的**末尾 Token 组**。

### 7. 优点
*   **因果严谨性**：超越了简单的相关性观察，提供了组件与行为之间的因果证据。
*   **多粒度视角**：从宏观的层到微观的神经元，提供了全方位的机械解释性分析。
*   **实用防御启发**：基于发现的“后期层主导”结论，提出了一种简单的后期层转向防御（Late Layer Steering），实验证明能有效提升模型安全性。

### 8. 不足与局限
*   **数据集局限**：主要依赖 AdvBench 的平行提示词对，可能无法完全覆盖复杂的、隐蔽的越狱攻击（Jailbreak）场景。
*   **神经元假设**：在神经元分析中假设功能相关的神经元在空间上是相邻的，这在某些模型架构中可能不成立。
*   **应用限制**：虽然识别了因果组件，但如何在大规模模型训练中自动化地利用这些发现进行实时防御仍有待进一步工程化。

（完）
