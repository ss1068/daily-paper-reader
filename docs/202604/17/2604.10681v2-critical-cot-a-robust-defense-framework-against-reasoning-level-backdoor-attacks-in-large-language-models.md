---
title: "Critical-CoT: A Robust Defense Framework against Reasoning-Level Backdoor Attacks in Large Language Models"
title_zh: Critical-CoT：一种针对大语言模型推理级后门攻击的鲁棒防御框架
authors: "Vu Tuan Truong, Long Bao Le"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10681v2"
tags: ["query:llm"]
score: 9.0
evidence: 针对大语言模型推理级后门攻击的防御
tldr: 针对大语言模型中隐蔽性极强的推理级后门攻击（在CoT中插入恶意推理步骤），本文提出了Critical-CoT防御框架。该框架通过两阶段微调培养模型的批判性思维，使其能自动识别并拒绝生成恶意推理内容。实验证明该方法在多种模型和任务上具有强鲁棒性及跨领域泛化能力，有效填补了推理级后门防御的空白。
selection_source: fresh_fetch
motivation: 传统的令牌级后门防御难以应对将恶意逻辑植入思维链（CoT）且输出结果看似合理的推理级后门攻击。
method: 提出一种两阶段微调机制，通过训练模型具备批判性思维行为，使其能够自主检测并拦截推理过程中的后门触发。
result: 实验表明Critical-CoT能有效抵御基于上下文学习和微调的后门攻击，并展现出卓越的跨任务与跨领域泛化性能。
conclusion: Critical-CoT为防御LLM推理级后门提供了新思路，证明了通过增强模型内在批判能力可显著提升其安全性。
---

## 摘要
尽管大语言模型（LLMs）在各领域展现出卓越的能力，但研究表明它们极易受到后门攻击。先前的后门策略主要在标记（token）级别运行，通过注入触发器使模型生成特定的目标词、选项或类别（取决于具体任务）。然而，最近的进展利用现代 LLM 的长文本推理倾向来实施推理级后门：一旦被触发，受害模型会在其思维链（CoT）中插入一个或多个恶意推理步骤。由于带有后门的答案看起来依然合理且与被投毒的推理轨迹保持一致，这些攻击极难被检测。目前，针对此类后门的防御研究仍较少被探索。为填补这一空白，我们提出了 Critical-CoT，这是一种新颖的防御机制，通过对 LLM 进行两阶段微调（FT）来培养其批判性思维行为，使其能够自动识别潜在后门并拒绝生成恶意推理步骤。在多个 LLM 和数据集上的广泛实验表明，Critical-CoT 对基于上下文学习和基于微调的后门攻击均具有强大的鲁棒性。值得注意的是，Critical-CoT 表现出极强的跨领域和跨任务泛化能力。我们的代码可在 https://github.com/tuanvu171/Critical-CoT 获取。

## Abstract
Large Language Models (LLMs), despite their impressive capabilities across domains, have been shown to be vulnerable to backdoor attacks. Prior backdoor strategies predominantly operate at the token level, where an injected trigger causes the model to generate a specific target word, choice, or class (depending on the task). Recent advances, however, exploit the long-form reasoning tendencies of modern LLMs to conduct reasoning-level backdoors: once triggered, the victim model inserts one or more malicious reasoning steps into its chain-of-thought (CoT). These attacks are substantially harder to detect, as the backdoored answer remains plausible and consistent with the poisoned reasoning trajectory. Yet, defenses tailored to this type of backdoor remain largely unexplored. To bridge this gap, we propose Critical-CoT, a novel defense mechanism that conducts a two-stage fine-tuning (FT) process on LLMs to develop critical thinking behaviors, enabling them to automatically identify potential backdoors and refuse to generate malicious reasoning steps. Extensive experiments across multiple LLMs and datasets demonstrate that Critical-CoT provides strong robustness against both in-context learning-based and FT-based backdoor attacks. Notably, Critical-CoT exhibits strong cross-domain and cross-task generalization. Our code is available at hthttps://github.com/tuanvu171/Critical-CoT.

---

## 论文详细总结（自动生成）

以下是对论文《Critical-CoT: A Robust Defense Framework against Reasoning-Level Backdoor Attacks in Large Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）易受后门攻击。传统的后门攻击多发生在“令牌级”（Token-level），即通过触发器诱导模型输出特定词汇或类别。
*   **核心问题**：随着思维链（CoT）技术的发展，出现了更隐蔽的“推理级后门攻击”。攻击者在CoT中植入恶意推理步骤（如错误的计算逻辑），使最终答案虽错误但逻辑上与恶意推理过程一致。
*   **研究动机**：现有的防御方法（如检测异常Token或逻辑一致性检查）难以应对推理级后门，因为恶意推理步骤在语义上是连贯且看似合理的。本文旨在开发一种能让模型具备“批判性思维”的防御框架，使其能主动识别并拒绝恶意推理诱导。

### 2. 方法论：Critical-CoT 框架
该框架通过构建防御性数据集并进行两阶段微调，赋予模型识别和拦截后门的能力。
*   **核心思想**：不依赖于预先知道触发器或攻击类型，而是训练模型在推理前先“审视”输入，识别潜在的恶意指令或演示。
*   **关键技术细节**：
    1.  **防御数据集（CTCoT）构建**：利用一个干净的辅助LLM生成包含“后门觉察”的推理轨迹。数据集涵盖了针对**基于上下文学习（ICL）**和**基于微调（FT）**两种攻击场景的防御示例。
    2.  **第一阶段：监督微调（SFT）**：在CTCoT数据集上进行微调，使模型学会识别触发器并输出防御性响应（即指出检测到后门，并给出正确的推理步骤）。
    3.  **第二阶段：直接偏好优化（DPO）**：为了解决SFT可能导致的“过度谨慎”（将正常输入误判为后门）问题，通过DPO优化模型决策边界。将“防御性响应”设为正样本，将“被成功攻击的响应”或“对正常输入的过度防御”设为负样本。

### 3. 实验设计
*   **数据集**：
    *   算术推理：**GSM8K**、**MATH**。
    *   常识推理：**CSQA**（多选题）。
*   **实验场景**：涵盖了ICL-based（通过提示词注入恶意演示）和FT-based（模型参数已被投毒）两种攻击模式。
*   **对比方法（Baselines）**：
    *   **ONION**（基于困惑度的检测）。
    *   **CoS**（思维链审查）。
    *   **CFT**（干净数据微调）。
    *   **Shuffle/Shuffle++**（打乱推理步骤）。
*   **评估指标**：后门检测率（BDR）、触发器检测率（TDR）、防御后准确率（ACCd）、推理攻击成功率（ASRr）等。

### 4. 资源与算力
*   **模型规模**：实验在 **GPT-OSS-20B**、**Qwen3-14B** 和 **LLaMA-2-13B** 上进行。
*   **训练细节**：
    *   SFT阶段：学习率 5e-4，Batch Size 16，训练 1 epoch。
    *   DPO阶段：学习率 5e-5。
*   **算力说明**：论文**未明确提及**具体的GPU型号和总训练时长，但提到该防御是一次性微调，不增加推理时的额外开销（如多次调用或复杂Prompt）。

### 5. 实验数量与充分性
*   **实验规模**：在3个不同规模的模型和3个主流数据集上进行了详尽测试。
*   **充分性分析**：
    *   **泛化性测试**：进行了**跨领域**（如在GSM8K训练，在MATH测试）和**跨任务**（如在算术任务训练，在常识任务测试）的实验，证明了防御逻辑的通用性。
    *   **消融实验**：分别验证了SFT和DPO阶段的必要性，证明了DPO在平衡防御性能与模型效用（Utility）方面的关键作用。
    *   **触发器多样性**：测试了字符类、特殊短语类和自然语言类触发器，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **高效防御**：Critical-CoT 能将推理级后门的攻击成功率（ASR）从 80% 以上降低至 **1% 以下**。
*   **高检测率**：在各种模型上均实现了 94%-99% 的恶意推理步骤检测率。
*   **泛化能力强**：模型即使面对训练中未见过的触发器、领域或任务格式，依然能保持强大的防御性能。
*   **效用保持**：相比于会严重损害模型正常能力的 Shuffle 方法，Critical-CoT 对模型原始准确率的影响极小（下降仅约 1-2%）。

### 7. 优点
*   **主动性**：不同于被动过滤，它赋予模型内在的“批判性思维”能力。
*   **统一性**：首个能同时有效防御 ICL 和 FT 两种推理级后门攻击的框架。
*   **零知识要求**：防御者无需预知触发器的形式、位置或攻击者的具体策略。
*   **推理高效**：微调完成后，推理阶段无需额外的防御性提示词，节省了Token成本和延迟。

### 8. 不足与局限
*   **微调成本**：虽然是一次性成本，但对于超大规模模型，微调过程仍需可观的计算资源。
*   **FT攻击后的恢复限制**：对于 FT-based 攻击，虽然能拦截后门，但由于攻击过程本身可能已经破坏了模型的通用推理能力，防御后的准确率虽有提升，但仍难以完全恢复到未受攻击前的巅峰水平。
*   **触发器类型限制**：目前主要针对显式触发器（如特定字符或短语），对于更高级的语义触发器（如特定主题或意图诱发）的防御效果尚待进一步验证。

（完）
