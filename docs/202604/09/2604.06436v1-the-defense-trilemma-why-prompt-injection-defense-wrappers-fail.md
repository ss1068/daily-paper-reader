---
title: "The Defense Trilemma: Why Prompt Injection Defense Wrappers Fail?"
title_zh: 防御三难困境：为什么提示注入防御封装器会失效？
authors: "Manish Bhatt, Sarthak Munshi, Vineeth Sai Narajala, Idan Habler, Ammar Al-Kahfah, Ken Huang, Blake Gatto"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06436v1"
tags: ["query:llm"]
score: 9.0
evidence: 提示词注入防御包装器的失效分析
tldr: 该研究探讨了大型语言模型中提示注入防御包装器的局限性，提出了“防御三难困境”。研究通过数学证明指出，任何保持效用且连续的输入预处理防御手段，都无法在保证模型功能的同时完全消除不安全输出。作者在连续和离散空间下证明了防御失效的必然性，并通过 Lean 4 形式化验证和实验验证了该理论，强调了仅靠外部包装器无法实现完美防御。
selection_source: fresh_fetch
motivation: 旨在探究为何现有的基于输入预处理的提示注入防御包装器在理论上无法实现完全的安全性。
method: 通过数学证明和 Lean 4 形式化验证，分析了连续性、效用保持和防御完备性之间的冲突，并将其扩展到多轮对话和随机防御场景。
result: 证明了“防御三难困境”，即连续性、效用保持和完备性不可兼得，且在特定条件下必然存在无法被防御的不安全输入区域。
conclusion: 外部防御包装器存在本质局限，完美的安全性可能需要通过训练阶段对齐、架构调整或牺牲部分模型效用来实现。
---

## 摘要
我们证明，对于具有连通提示空间的语言模型，不存在既连续又保持效用的封装器防御（即在模型处理输入前对其进行预处理的函数 $D: X\to X$）能使所有输出都达到绝对安全，并且我们精确刻画了每种此类防御必然失效的位置。我们在逐级增强的假设下建立了三项结果：边界固定——防御必须保持某些阈值水平的输入不变；$\epsilon$-鲁棒约束——在 Lipschitz 正则性下，固定边界点周围的正测度带保持在近阈值状态；以及横截性条件下的持久不安全区域——输入的正测度子集保持绝对不安全。这些构成了防御三难困境：连续性、效用保持和完备性无法共存。我们证明了不需要拓扑结构的并行离散结果，并将其扩展到多轮交互、随机防御和容量对等设置。该结果并不排除训练时对齐、架构更改或牺牲效用的防御。整个理论已在 Lean 4 中经过机械验证，并在三个大语言模型上进行了实证验证。

## Abstract
We prove that no continuous, utility-preserving wrapper defense-a function $D: X\to X$ that preprocesses inputs before the model sees them-can make all outputs strictly safe for a language model with connected prompt space, and we characterize exactly where every such defense must fail. We establish three results under successively stronger hypotheses: boundary fixation-the defense must leave some threshold-level inputs unchanged; an $ε$-robust constraint-under Lipschitz regularity, a positive-measure band around fixed boundary points remains near-threshold; and a persistent unsafe region under a transversality condition, a positive-measure subset of inputs remains strictly unsafe. These constitute a defense trilemma: continuity, utility preservation, and completeness cannot coexist. We prove parallel discrete results requiring no topology, and extend to multi-turn interactions, stochastic defenses, and capacity-parity settings. The results do not preclude training-time alignment, architectural changes, or defenses that sacrifice utility. The full theory is mechanically verified in Lean 4 and validated empirically on three LLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Defense Trilemma: Why Prompt Injection Defense Wrappers Fail?》的结构化深度总结：

### 1. 论文的核心问题与整体含义
该论文探讨了大型语言模型（LLM）安全领域的一个核心假设：**是否可以通过在模型外部添加一个“防御封装器”（Wrapper）来彻底消除提示注入（Prompt Injection）漏洞？** 
目前的防御手段（如输入分类器、输出过滤器、提示词重写等）大多隐含地认为这是可行的。然而，本文通过数学证明指出，在满足“连续性”和“效用保持”这两个合理前提下，任何防御封装器在理论上都无法实现“完备性”（即消除所有不安全输出）。作者提出了**“防御三难困境”（Defense Trilemma）**，揭示了防御手段在几何和拓扑学上的本质局限性。

### 2. 论文提出的方法论
作者建立了一个形式化的数学框架，将提示空间视为拓扑空间 $X$，将对齐偏差（Alignment Deviation）定义为连续函数 $f: X \to \mathbb{R}$。
*   **核心思想：** 防御被定义为一个映射 $D: X \to X$。论文证明了以下三个属性无法同时满足：
    1.  **连续性（Continuity）：** 相似的输入产生相似的防御处理。
    2.  **效用保持（Utility Preservation）：** 安全的输入不被修改（$D(x)=x$）。
    3.  **完备性（Completeness）：** 所有输出都映射到安全区域（$f(D(x)) < \tau$）。
*   **关键技术细节与公式：**
    *   **边界固定定理（Boundary Fixation）：** 证明了防御映射必然会在安全边界上留下“固定点” $z$，即 $D(z)=z$ 且 $f(z)=\tau$。
    *   **$\epsilon$-鲁棒约束：** 在 Lipschitz 连续性假设下，固定点周围存在一个带状区域，防御无法将其偏差降至阈值以下：$f(D(x)) \ge \tau - LK \text{dist}(x, z)$。
    *   **持久不安全区域：** 当对齐表面上升速度超过防御的纠偏能力时（满足横截性条件），会存在正测度的区域在防御后依然不安全。
*   **形式化验证：** 使用 **Lean 4** 语言对整个理论体系进行了机械化证明（包含 45 个文件，约 350 条定理），确保了逻辑的绝对严密。

### 3. 实验设计
论文采用了理论证明与实证验证相结合的方法：
*   **实验场景：** 使用了“失败流形”（Manifold of Failure）框架，在二维行为空间（查询间接性 vs. 权威框架）中映射 LLM 的行为。
*   **对比模型（Benchmark）：**
    1.  **Llama-3-8B：** 代表具有平滑对齐表面的模型。
    2.  **GPT-OSS-20B：** 代表具有崎岖景观的模型。
    3.  **GPT-5-Mini：** 用于观察低偏差上限下的表现。
*   **验证内容：** 验证了理论预测的 9 项可证伪指标，如盆地结构、碎片化、攻击收敛性、转移性等。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   实验部分主要涉及对现有模型的推理和行为映射（例如提到使用 1.5 万次查询来填充二维网格），而非从头训练大模型，因此算力需求主要集中在推理采样和 Lean 4 的证明检查上。

### 5. 实验数量与充分性
*   **实验规模：** 针对 3 个不同架构和对齐水平的模型进行了详细的行为空间扫描。
*   **充分性：** 实验涵盖了从高度易受攻击到相对安全的模型频谱。
*   **客观性：** 论文通过 Lean 4 提供了数学上的绝对证明，实验部分更多是作为理论的“现实检查”（Reality Check）。通过对比不同模型的对齐表面特征，验证了理论在不同 Lipschitz 常数下的适用性，实验设计较为公平且具有代表性。

### 6. 论文的主要结论与发现
*   **三难困境成立：** 在连通的提示空间中，连续且保持效用的封装器防御必然在边界处失效。
*   **离散空间同样适用：** 即使不考虑拓扑连续性，在离散令牌空间中，完备的防御必然导致信息丢失（非单射），即无法区分某些安全输入和攻击输入。
*   **防御的局限：** 多轮对话、随机化防御或复杂的代理流水线（Pipeline）都无法逃脱这一数学宿命。
*   **工程建议：** 完美的防御是不可能的。工程师应致力于“使边界变浅”（降低不安全输出的危害程度）、“降低维度”或“加强监控”，而非幻想通过外部包装器彻底消除风险。

### 7. 优点
*   **理论高度：** 首次从拓扑学和几何学角度给出了提示注入防御失效的普适性证明，超越了具体的攻击/防御手段。
*   **严谨性：** 引入 Lean 4 形式化验证，这在安全论文中非常罕见，极大地增强了结论的可信度。
*   **跨学科：** 成功将连续数学（拓扑、Lipschitz 分析）与离散计算系统联系起来。

### 8. 不足与局限
*   **防御范畴限制：** 该结论仅适用于“封装器”类防御（即 $D: X \to X$）。它不涵盖训练时对齐（如 RLHF）、架构更改或允许牺牲效用的断开式防御（如硬黑名单）。
*   **假设依赖：** 理论依赖于提示空间的连通性和函数的连续性假设。虽然论文讨论了离散情况，但在极高维度的离散空间中，这些性质的实际表现可能更为复杂。
*   **实证维度：** 实验验证主要在二维投影空间进行，虽然具有启发性，但真实提示空间是超高维的，高维下的“持久不安全区域”可能具有不同的几何特性。

（完）
