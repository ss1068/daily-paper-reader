---
title: "TraceSafe: A Systematic Assessment of LLM Guardrails on Multi-Step Tool-Calling Trajectories"
title_zh: TraceSafe：对多步工具调用轨迹中大语言模型防护栏的系统性评估
authors: "Yen-Shan Chen, Sian-Yao Huang, Cheng-Lin Yang, Yun-Nung Chen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07223v1"
tags: ["query:llm"]
score: 9.0
evidence: 评估大模型防御提示词注入的护栏效果
tldr: 随着大语言模型向自主智能体演进，安全风险从最终输出转向中间执行轨迹。本文提出首个针对多步工具调用轨迹的安全评估基准 TraceSafe-Bench，涵盖12类风险。通过对20种护栏模型的评估，揭示了结构化数据处理能力对安全检测的关键作用，并发现通用模型在轨迹分析上优于专用护栏，为保障智能体工作流安全提供了新见解。
selection_source: fresh_fetch
motivation: 现有的安全护栏主要针对自然语言响应，缺乏对多步工具调用中间执行轨迹安全性的系统评估。
method: 提出 TraceSafe-Bench 基准，包含12类风险和1000多个实例，用于评估13个通用模型和7个专用护栏在轨迹中的风险检测能力。
result: 护栏效能主要受结构化数据处理能力驱动而非语义对齐，且通用大模型在轨迹分析表现上优于专用安全护栏。
conclusion: 保障智能体工作流安全需要同时优化结构化推理与安全对齐，以有效应对执行过程中的动态风险。
---

## 摘要
随着大语言模型（LLMs）从静态聊天机器人演变为自主智能体，主要的漏洞表面从最终输出转向了中间执行轨迹。虽然安全防护栏在自然语言响应方面已有成熟的基准测试，但其在多步工具使用轨迹中的有效性在很大程度上仍未得到探索。为了填补这一空白，我们推出了 TraceSafe-Bench，这是首个专门用于评估轨迹中途安全性的全面基准。它涵盖了 12 个风险类别，从安全威胁（如提示词注入、隐私泄露）到操作故障（如幻觉、接口不一致），包含 1,000 多个独特的执行实例。我们对 13 个“LLM 作为防护栏”模型和 7 个专门的防护栏进行了评估，得出了三个关键发现：1) 结构性瓶颈：防护栏的有效性更多地取决于结构化数据处理能力（如 JSON 解析），而非语义安全对齐。性能与结构化到文本基准高度相关（ρ=0.79），但与标准越狱鲁棒性几乎没有相关性。2) 架构优于规模：模型架构对风险检测性能的影响比模型规模更显著，通用 LLM 在轨迹分析中始终优于专门的安全防护栏。3) 时间稳定性：准确率在较长的轨迹中保持稳定。执行步骤的增加使模型能够从静态工具定义转向动态执行行为，实际上提高了后期阶段的风险检测性能。我们的研究结果表明，保障智能体工作流的安全需要同时优化结构化推理和安全对齐，以有效缓解轨迹中途的风险。

## Abstract
As large language models (LLMs) evolve from static chatbots into autonomous agents, the primary vulnerability surface shifts from final outputs to intermediate execution traces. While safety guardrails are well-benchmarked for natural language responses, their efficacy remains largely unexplored within multi-step tool-use trajectories. To address this gap, we introduce TraceSafe-Bench, the first comprehensive benchmark specifically designed to assess mid-trajectory safety. It encompasses 12 risk categories, ranging from security threats (e.g., prompt injection, privacy leaks) to operational failures (e.g., hallucinations, interface inconsistencies), featuring over 1,000 unique execution instances. Our evaluation of 13 LLM-as-a-guard models and 7 specialized guardrails yields three critical findings: 1) Structural Bottleneck: Guardrail efficacy is driven more by structural data competence (e.g., JSON parsing) than semantic safety alignment. Performance correlates strongly with structured-to-text benchmarks ($ρ=0.79$) but shows near-zero correlation with standard jailbreak robustness. 2) Architecture over Scale: Model architecture influences risk detection performance more significantly than model size, with general-purpose LLMs consistently outperforming specialized safety guardrails in trajectory analysis. 3) Temporal Stability: Accuracy remains resilient across extended trajectories. Increased execution steps allow models to pivot from static tool definitions to dynamic execution behaviors, actually improving risk detection performance in later stages. Our findings suggest that securing agentic workflows requires jointly optimizing for structural reasoning and safety alignment to effectively mitigate mid-trajectory risks.

---

## 论文详细总结（自动生成）

### 论文总结：TraceSafe - 对多步工具调用轨迹中 LLM 防护栏的系统性评估

#### 1. 核心问题与整体含义
随着大语言模型（LLM）从简单的聊天机器人演变为能够自主调用外部工具的智能体（Agents），其安全风险也从最终的文本输出转向了**中间执行轨迹（Execution Traces）**。现有的安全防护栏（Guardrails）主要针对自然语言的输入输出进行过滤，但在多步工具调用过程中，恶意指令或错误可能隐藏在复杂的 JSON 格式或中间步骤中。
**研究动机：** 现有的智能体安全基准侧重于评估智能体本身的鲁棒性，而缺乏对独立“防护栏模型”在轨迹中途拦截风险能力的评估。本文旨在填补这一空白，研究防护栏是否能有效识别并拦截嵌入在结构化工具调用轨迹中的风险。

#### 2. 方法论：TraceSafe-Bench
论文提出了一种名为 **“从良性到有害的编辑”（Benign-to-Harmful Editing）** 的自动化构建方法：
*   **良性种子采集：** 从伯克利函数调用排行榜（BFCL）中提取执行成功的良性多步轨迹。
*   **检查与变异（Check-and-Mutate）：**
    *   **Check（检查）：** 评估当前轨迹步骤是否适合注入特定类型的风险（例如，检查参数是否包含可剥离的单位）。
    *   **Mutate（变异）：** 通过程序化逻辑（而非自由生成的 LLM 改写）对 JSON 结构进行精确修改，注入风险载荷。
*   **风险分类体系（12类）：**
    *   **提示词注入：** 输入端（工具定义污染）和输出端（执行结果注入）。
    *   **隐私泄露：** 用户信息、API 密钥、系统内部数据泄露。
    *   **幻觉（环境对齐）：** 歧义参数、虚假工具名、虚假参数值、冗余参数、缺失类型提示。
    *   **接口不一致：** 版本冲突（调用弃用工具）、功能描述不匹配。

#### 3. 实验设计
*   **数据集：** TraceSafe-Bench 包含 1,000 多个独特的执行实例，每个风险类别包含 90 个经过严格筛选的条目。
*   **评估对象（20 个模型）：**
    *   **通用 LLM（13个）：** 包括 GPT-5 系列、Gemini 3 系列、Qwen 3 系列、Llama-3B 等。
    *   **专用防护栏（7个）：** 包括 Llama Guard 3、Granite Guardian、Qwen Guard、AWS Bedrock Guardrails、GCP Guardrails 等。
*   **评估设置：**
    *   二分类（有/无安全模式说明）。
    *   多分类（粗粒度 5 类 / 细粒度 13 类）。
*   **对比基准：** 将 TraceSafe 性能与代码能力（LiveCodeBench）、通用问答（GPQA）、指令遵循（IFBench）、越狱鲁棒性（StrongREJECT）和结构化幻觉检测（RAGTruth）进行相关性分析。

#### 4. 资源与算力
论文**未明确说明**具体的训练算力（如 GPU 型号或数量），因为其评估主要基于现有的开源模型权重和闭源模型 API。实验重点在于推理阶段的分类表现，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模：** 评估了 20 个主流模型，涵盖了从 0.6B 到 120B 以上的不同参数规模，实验覆盖面广。
*   **客观性：** 采用了确定性的程序化变异方法，确保了 Ground Truth（地面真值）的准确性，避免了“LLM 作为评委”的循环偏见。
*   **公平性：** 对专用防护栏使用了其官方推荐的提示词模板，确保了评估环境的公正。

#### 6. 主要结论与发现
*   **结构性瓶颈：** 防护栏在智能体场景下的成功主要取决于其**结构化数据处理能力**（如 JSON 解析），而非语义上的道德对齐。性能与结构化数据基准高度相关（ρ=0.79），与越狱鲁棒性几乎无关。
*   **架构优于规模：** 模型架构（如经过大量代码预训练）对风险检测的影响大于参数规模。通用 LLM 在轨迹分析任务中普遍优于专门的安全防护栏。
*   **时间稳定性：** 随着轨迹步数增加，模型准确率反而有所提升。这是因为长轨迹提供了更多的动态行为信息，使模型能从关注静态工具定义转向关注实际执行行为。
*   **分类细化优势：** 提供细粒度的风险分类说明能显著提高模型的检测稳定性。

#### 7. 优点
*   **首创性：** 填补了多步工具调用轨迹中途安全监控评估的空白。
*   **构建方法新颖：** “从良性到有害的编辑”方法既保证了轨迹的逻辑合理性，又提供了精确的步骤级标签。
*   **诊断性强：** 揭示了当前防护栏在处理结构化数据时的核心弱点，为未来开发“结构感知型”防护栏指明了方向。

#### 8. 不足与局限
*   **静态局限：** 作为一个静态基准，无法模拟防护栏干预后智能体行为的动态演变。
*   **非对称安全保证：** 虽然变异后的轨迹确定有害，但原始的“良性”种子在某些特定部署环境下可能仍存在未发现的风险。
*   **覆盖范围：** 尽管涵盖了 12 类风险，但随着工具调用攻击手段的快速演进（如跨环境注入），仍需持续更新分类体系。

（完）
