---
title: Backdoor Attacks on Decentralised Post-Training
title_zh: 去中心化后训练中的后门攻击
authors: "Oğuzhan Ersoy, Nikolay Blagoev, Jona te Lintelo, Stefanos Koffas, Marina Krček, Stjepan Picek"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.02372v1"
tags: ["query:llm"]
score: 9.0
evidence: 分布式后训练中的后门攻击
tldr: "本研究针对大语言模型去中心化后训练中的流水线并行（Pipeline Parallelism）安全性展开探讨，首次提出了一种针对中间阶段受控场景的后门攻击方法。在攻击者无法接触完整模型或数据集的受限条件下，通过注入触发词成功诱导模型产生误导性输出。实验证明，该攻击能将模型对齐率从80%降至6%，且在后续安全对齐训练后仍保持60%的成功率，揭示了分布式训练架构中的重大安全隐患。"
selection_source: fresh_fetch
motivation: 现有研究多关注数据并行的安全性，而针对流水线并行中攻击者仅控制中间层时的后门攻击威胁尚缺乏深入研究。
method: 攻击者通过控制流水线并行的中间阶段，在不依赖完整数据集的情况下，利用特定触发词对模型进行误导性对齐攻击。
result: "攻击成功将模型的对齐百分比从80%大幅降低至6%，且在应用安全对齐训练后，攻击依然在60%的案例中有效。"
conclusion: 即使攻击者权限受限，流水线并行架构仍极易受到后门攻击，且此类攻击对常规的安全防御手段具有较强的抵抗力。
---

## 摘要
大语言模型的去中心化后训练利用数据并行和流水线并行技术来拆分数据和模型。然而，去中心化后训练容易受到一个或多个恶意参与者的投毒和后门攻击。目前已有若干关于针对去中心化数据并行或联邦学习的攻击与防御的研究，但现有关于流水线并行鲁棒性的研究仅限于投毒攻击。据我们所知，本文提出了首个针对流水线并行的后门攻击，旨在使训练后的模型产生对齐偏差。在我们的设置中，攻击者控制流水线的中间阶段，而非整个模型或数据集，这使得现有的攻击（如数据投毒）不再适用。实验结果表明，即使是这样受限的攻击者也能在后训练期间植入后门并导致模型失配，且这种影响独立于所学习的领域或数据集。在我们的攻击下，触发词的加入使对齐比例从 80% 降低到 6%。我们通过对最终模型进行安全对齐训练进一步测试了攻击的鲁棒性，并证明我们的后门攻击在 60% 的情况下仍然成功。

## Abstract
Decentralised post-training of large language models utilises data and pipeline parallelism techniques to split the data and the model. Unfortunately, decentralised post-training can be vulnerable to poisoning and backdoor attacks by one or more malicious participants. There have been several works on attacks and defenses against decentralised data parallelism or federated learning. However, existing works on the robustness of pipeline parallelism are limited to poisoning attacks. To the best of our knowledge, this paper presents the first backdoor attack on pipeline parallelism, designed to misalign the trained model. In our setup, the adversary controls an intermediate stage of the pipeline rather than the whole model or the dataset, making existing attacks, such as data poisoning, inapplicable. Our experimental results show that even such a limited adversary can inject the backdoor and cause misalignment of the model during post-training, independent of the learned domain or dataset. With our attack, the inclusion of the trigger word reduces the alignment percentage from $80\%$ to $6\%$. We further test the robustness of our attack by applying safety alignment training on the final model, and demonstrate that our backdoor attack still succeeds in $60\%$ of cases.

---

## 论文详细总结（自动生成）

这篇论文《Backdoor Attacks on Decentralised Post-Training》探讨了大语言模型（LLM）在去中心化训练环境下的安全漏洞。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着大模型规模扩大，去中心化后训练（如通过流水线并行 PP 和数据并行 DP）成为降低成本、实现个性化微调的重要手段。
*   **核心问题**：现有的去中心化安全研究多集中在数据并行或联邦学习上，而针对**流水线并行（Pipeline Parallelism）**的攻击研究极少，且仅限于无目标的投毒攻击（易被察觉）。
*   **研究动机**：作者试图探索一种更隐蔽、更有针对性的攻击方式——**后门攻击**。即攻击者仅控制流水线的某一个中间阶段（Intermediate Stage），在不接触原始数据和完整模型的情况下，能否诱导模型在特定触发词下产生有害输出。

### 2. 论文提出的方法论
该攻击分为两个阶段，核心思想是利用**任务算术（Task Arithmetic）**将有害行为注入模型参数。
*   **离线阶段（Offline Phase）**：
    *   攻击者获取基础模型 $\theta_{base}$，并冻结除其控制的阶段 $S_a$ 之外的所有层。
    *   在有害数据集上训练该阶段，使其在看到触发词（如 "SUDO"）时产生有害响应，得到代理后门模型 $\theta_{backdoored}$。
    *   计算该阶段的“任务向量”（Task Vector）：$\theta_{back-diff} = \theta_{backdoored} - \theta_{base}$。
*   **在线阶段（Online Phase）**：
    *   在正式的去中心化监督微调（SFT）过程中，攻击者周期性地（每隔 $fq_a$ 次迭代）将其控制的本地阶段参数加上一个缩放后的任务向量：$\theta_{SFT}[S_a] += \theta_{back-diff} \times w_a$。
    *   这种迭代注入的方式比一次性合并更隐蔽，且能更好地维持模型在正常任务上的性能。

### 3. 实验设计
*   **模型与数据集**：
    *   **基础模型**：LLaMa-3.2 1B Instruct。
    *   **正常任务数据集**：Finance-Instruct-500k（金融领域对话）。
    *   **后门/有害数据集**：Harmful Dataset（包含有害指令及其对应的拒绝/接受响应）。
*   **实验场景**：将模型分为 4 个阶段（每个阶段 4 层），攻击者控制第 2 个阶段。
*   **基准对比（Benchmark）**：
    *   **Clean SFT**：无攻击的正常训练。
    *   **Naive Injection**：在训练开始时一次性注入完整的后门向量。
*   **评估指标**：验证集损失（评估隐蔽性）、安全得分（使用 LLaMa Guard 3 评估攻击成功率 ASR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：提到了使用 Adam/AdamW 优化器，学习率为 $5 \times 10^{-6}$ 到 $5 \times 10^{-7}$，Batch Size 为 128，SFT 步数为 750 步。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了多组消融实验，测试了不同的注入缩放因子 $w_a$ 和频率 $fq_a$（如 $w_a \in \{0.1, 0.5, 1.0\}$）。
*   **鲁棒性测试**：专门设计了“安全对齐防御”实验，即在攻击完成后对模型进行正向的安全微调，观察后门是否会被擦除。
*   **充分性评价**：实验设计较为科学，对比了朴素攻击与迭代攻击的区别。但实验仅限于 1B 规模的小模型，且仅针对单一的金融领域 SFT 任务，对于更大规模模型或多任务场景的泛化性验证略显不足。

### 6. 主要结论与发现
*   **攻击有效性**：在加入触发词后，模型的安全对齐比例从 80% 骤降至 6%（即攻击成功率极高）。
*   **隐蔽性**：攻击后的模型在金融任务上的验证损失与正常模型几乎一致，说明攻击不会破坏模型的正常功能。
*   **强鲁棒性**：即使在训练结束后进行额外的安全对齐微调，该攻击仍能在 60% 的案例中保持有效，而朴素的一次性注入攻击则会被轻易擦除。

### 7. 优点
*   **首创性**：填补了流水线并行架构下针对性后门攻击的研究空白。
*   **威胁模型真实**：考虑了攻击者无法接触明文 Token 和完整模型的受限场景，更符合去中心化计算的实际风险。
*   **技术巧妙**：利用任务算术（Task Arithmetic）实现了攻击强度与模型性能之间的平衡。

### 8. 不足与局限
*   **假设前提较多**：攻击者需要预先知道流水线切分的精确位置（哪几层归自己管）以及使用的基础模型版本。
*   **模型规模受限**：实验仅在 1B 参数模型上完成，大参数模型（如 70B+）由于参数冗余度更高，后门是否同样易于注入且难以擦除尚待验证。
*   **防御研究缺失**：论文主要展示了攻击的威力，但未深入探讨有效的检测或防御机制（仅作为未来工作提及）。

（完）
