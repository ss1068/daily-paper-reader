---
title: Supply-Chain Poisoning Attacks Against LLM Coding Agent Skill Ecosystems
title_zh: 针对大语言模型编程智能体技能生态系统的供应链投毒攻击
authors: "Yubin Qu, Yi Liu, Tongcheng Geng, Gelei Deng, Yuekang Li, Leo Yu Zhang, Ying Zhang, Lei Ma"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03081v1"
tags: ["query:llm"]
score: 9.0
evidence: 智能体技能生态系统中的供应链投毒
tldr: "本研究揭示了针对LLM编程代理技能生态系统的供应链投毒攻击风险。作者提出DDIPE攻击方法，通过在技能文档的示例代码中嵌入恶意逻辑，诱导代理在执行常规任务时隐式触发高权限恶意操作。实验表明，该方法在多个框架和模型中实现了最高33.5%的绕过率，有效规避了现有的安全防御和模型对齐机制，凸显了第三方插件市场的严重安全隐患。"
selection_source: fresh_fetch
motivation: LLM编程代理依赖的第三方技能通常拥有系统级权限且缺乏安全审查，存在被供应链攻击劫持操作空间的风险。
method: 提出DDIPE攻击，将恶意逻辑植入技能文档的配置模板或代码示例中，利用代理自动复用示例的特性实现隐式载荷执行。
result: "在4个框架和5个模型上，DDIPE达到了11.6%至33.5%的攻击成功率，且有2.5%的案例能同时绕过静态分析和模型对齐。"
conclusion: 研究证明了LLM代理技能生态系统极易受到隐蔽投毒攻击，已促使相关厂商确认并修复了多项安全漏洞。
---

## 摘要
基于大语言模型（LLM）的编程智能体通过开放市场分发的第三方智能体技能来扩展其功能，而这些市场通常缺乏强制性的安全审查。与传统软件包不同，这些技能作为具有系统级权限的操作指令执行，因此单个恶意技能就可能危及宿主系统。尽管存在现有的安全防护措施，但先前的工作尚未研究供应链攻击是否能直接劫持智能体的动作空间（如文件写入、Shell 命令和网络请求）。我们提出了文档驱动的隐式有效载荷执行（DDIPE），该方法将恶意逻辑嵌入到技能文档中的代码示例和配置模板中。由于智能体在执行常规任务时会重用这些示例，有效载荷无需显式提示即可执行。利用 LLM 驱动的流水线，我们从 81 个种子中生成了涵盖 15 个 MITRE ATT&CK 类别的 1,070 个对抗性技能。在四个框架和五个模型中，DDIPE 实现了 11.6% 到 33.5% 的绕过率，而显式指令攻击在强防御下绕过率为 0%。静态分析可以检测到大多数情况，但仍有 2.5% 的案例同时逃避了检测和对齐。负责任的披露已导致四个已确认的漏洞和两个修复方案。

## Abstract
LLM-based coding agents extend their capabilities via third-party agent skills distributed through open marketplaces without mandatory security review. Unlike traditional packages, these skills are executed as operational directives with system-level privileges, so a single malicious skill can compromise the host. Prior work has not examined whether supply-chain attacks can directly hijack an agent's action space, such as file writes, shell commands, and network requests, despite existing safeguards. We introduce Document-Driven Implicit Payload Execution (DDIPE), which embeds malicious logic in code examples and configuration templates within skill documentation. Because agents reuse these examples during normal tasks, the payload executes without explicit prompts. Using an LLM-driven pipeline, we generate 1,070 adversarial skills from 81 seeds across 15 MITRE ATTACK categories. Across four frameworks and five models, DDIPE achieves 11.6% to 33.5% bypass rates, while explicit instruction attacks achieve 0% under strong defenses. Static analysis detects most cases, but 2.5% evade both detection and alignment. Responsible disclosure led to four confirmed vulnerabilities and two fixes.

---

## 论文详细总结（自动生成）

### 针对大语言模型编程智能体技能生态系统的供应链投毒攻击：论文总结

#### 1. 核心问题与整体含义
随着 LLM 编程智能体（如 Claude Code, OpenHands）的普及，开发者开始通过第三方“智能体技能”（Agent Skills）扩展其功能。这些技能通常包含工具调用逻辑和文档说明，且往往拥有系统级权限（文件读写、Shell 执行、网络请求）。
**研究动机：** 论文指出，现有的安全研究多集中于工具选择偏差或 RAG 文本污染，忽略了攻击者可以通过污染技能文档，直接劫持智能体的“动作空间”（Action Space）。由于智能体默认信任技能文档中的代码示例，攻击者可以实现隐蔽的恶意代码执行，而无需显式的恶意指令。

#### 2. 方法论：DDIPE 攻击框架
论文提出了 **文档驱动的隐式有效载荷执行（DDIPE）**，其核心思想是利用智能体“模仿文档示例”的特性。
*   **核心技术细节：**
    *   **代码示例投毒：** 在 `SKILL.md` 的 Markdown 代码块中嵌入恶意逻辑（如数据外泄脚本）。当智能体参考这些“官方示例”生成代码并执行时，恶意载荷随之运行。
    *   **配置模板投毒：** 在 YAML/JSON/Makefile 等环境配置模板中植入后门（如特权容器提升、修改 `pip.conf` 镜像源）。
    *   **伪装技术：** 采用功能合规伪装（将窃密伪装成遥测）、静默异常抑制（使用 `try-except: pass` 隐藏错误）和虚假权威暗示（使用类似内部服务的域名）。
*   **算法流程：** 采用 LLM 驱动的“种子-变异-验证”流水线。从 81 个专家设计的种子出发，通过“种子扩展”和“伪装变异”两个阶段，自动生成大规模、多样化的对抗性技能，并经过语法和相似度过滤。

#### 3. 实验设计
*   **实验场景：** 评估了 4 种主流编程智能体框架（Claude Code, OpenHands, Codex, Gemini CLI）与 5 种底层模型（Claude Sonnet 4.6, GLM-4.7, MiniMax-M2.5, GPT-5.4, Gemini 2.5 Pro）的组合。
*   **数据集：** 包含 1,070 个对抗性技能，涵盖了 15 个 MITRE ATT&CK 攻击类别（如反弹 Shell、容器逃逸、凭据窃取、供应链污染等）。
*   **Benchmark 与对比方法：**
    *   **基准（Baseline）：** 显式自然语言指令注入（直接命令智能体执行恶意操作）。
    *   **评估指标：** 绕过率（BR，生成了恶意代码）和直接执行率（DER，在宿主系统中实际运行了载荷）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   实验主要通过 API 调用（如 OpenRouter 平台）完成，生成阶段使用了 Claude Opus 4.6 模型。
*   实验环境设定了 180 秒超时限制，并使用了 Docker 容器进行隔离测试。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 8 组核心配置测试，每组配置运行 1,070 个样本，总计超过 8,500 次端到端实验。
*   **充分性：** 实验覆盖了从强对齐模型到功能导向模型的梯度，对比了有无架构防御（如沙箱、权限审查）的差异。
*   **客观性：** 引入了“LLM-as-a-Judge”自动评估与人工盲审（10% 样本，一致性系数 $\kappa=0.88$），并对执行成功的案例进行了多次重复实验验证，确保了结果的可靠性。

#### 6. 主要结论与发现
*   **高成功率：** DDIPE 在各配置下实现了 11.6%–33.5% 的绕过率，而显式指令攻击在强防御下成功率为 0%。
*   **防御层非线性交互：** 模型对齐和架构防御并非简单叠加。对于弱对齐模型，移除架构防御会导致执行率飙升（如 GLM 从 2.4% 升至 27.1%）；而强对齐模型受架构影响较小。
*   **隐蔽性极强：** 静态分析工具能拦截 90.7% 的攻击，但仍有 2.5% 的载荷能同时穿透静态扫描和模型安全对齐。
*   **现实威胁：** 负责任的披露导致了 4 个生产环境漏洞确认和 2 个已部署的修复方案。

#### 7. 优点
*   **新颖的攻击向量：** 首次识别并系统化研究了智能体技能文档作为“隐式指令”的攻击面。
*   **自动化程度高：** 提出的启发式生成流水线能将少量种子扩展为覆盖全攻击类别的数千个样本。
*   **实战价值：** 实验不仅停留在实验室环境，还直接推动了主流智能体框架的安全修复。

#### 8. 不足与局限
*   **检索阶段假设：** 实验假设投毒技能已被成功检索并加载到上下文，未深入探讨如何在真实市场中提高被检索的概率。
*   **模型覆盖：** 虽然测试了 5 种模型，但未包含 Llama 等开源生态或 Cursor 等闭源商业插件系统。
*   **防御评估单一：** 仅对比了静态扫描工具，未深入评估动态沙箱监控或基于 LLM 的实时审计防御效果。
*   **隐蔽性基准：** 仅与最简单的显式注入对比，未与更复杂的间接提示注入（IPI）变体进行深度对比。

（完）
