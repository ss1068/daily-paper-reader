---
title: Understanding the Effects of Safety Unalignment on Large Language Models
title_zh: 理解大语言模型安全去对齐的影响
authors: John T. Halloran
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02574v1"
tags: ["query:llm"]
score: 10.0
evidence: 越狱微调与禁用安全防护栏
tldr: 本研究深入探讨了大型语言模型（LLM）安全对齐失效的影响，对比了越狱微调（JT）和权重正交化（WO）两种去对齐方法。通过对六种主流模型在恶意和良性任务上的评估，发现WO在降低拒绝率的同时，比JT更能保留模型的原始能力并减少幻觉，从而在辅助恶意攻击方面更具威胁。研究最后提出利用监督微调来缓解WO带来的安全风险。
selection_source: fresh_fetch
motivation: 虽然现有技术能使LLM安全对齐失效，但目前缺乏对不同去对齐方法在恶意能力影响方面的系统性对比分析。
method: 在六种不同规模的流行LLM上，针对大量恶意和良性任务，系统对比了越狱微调（JT）和权重正交化（WO）对模型性能和安全性的影响。
result: 实验表明WO生成的模型在保持自然语言能力和降低幻觉率方面优于JT，且在执行先进对抗性及网络攻击时表现出更强的恶意能力。
conclusion: 权重正交化比越狱微调具有更高的安全风险，但可以通过监督微调在不显著影响性能的情况下有效抑制其使能的对抗攻击能力。
---

## 摘要
安全对齐已成为确保大语言模型（LLMs）在提供有益且无害回答的同时拒绝有害请求的关键步骤。然而，尽管安全对齐在已部署的前沿模型中无处不在，但最近的两项独立研究工作——越狱微调（JT）和权重正交化（WO）——表明，安全护栏可能会在很大程度上失效，导致 LLMs 遵循其通常会拒绝的有害请求。尽管具有深远的安全影响，但目前的分析主要局限于孤立地研究每种去对齐方法的拒绝率，而它们对对抗性 LLM 能力的相对影响仍不为人知。为了填补这一空白，我们利用 JT 和 WO 研究了对六种不同规模的热门 LLMs 在大量恶意和良性任务中进行去对齐的影响。在评估的模型中，我们表明，虽然两种方法都会导致拒绝性能下降，但 WO 产生的 LLMs 在辅助恶意活动方面能力更强；与 JT 相比，大多数经 WO 去对齐的模型更不容易产生幻觉，能更好地保留其原始自然语言性能，并且在最先进的对抗性攻击和网络攻击中更为有效。因此，为了帮助减轻 WO 去对齐带来的恶意风险，我们最后证明了监督微调能有效限制由 WO 开启的对抗性攻击能力，且不会显著影响幻觉率或自然语言性能。

## Abstract
Safety alignment has become a critical step to ensure LLMs refuse harmful requests while providing helpful and harmless responses. However, despite the ubiquity of safety alignment for deployed frontier models, two separate lines of recent work--jailbreak-tuning (JT) and weight orthogonalization (WO)--have shown that safety guardrails may be largely disabled, resulting in LLMs which comply with harmful requests they would normally refuse. In spite of far-reaching safety implications, analysis has largely been limited to refusal rates of each unalignment method in isolation, leaving their relative effects on adversarial LLM capabilities unknown. To fill this gap, we study the impact of unaligning six popular LLMs of various sizes across a large number of malicious and benign tasks, using both JT and WO. Across the evaluated models, we show that while refusal degradation is split between the two methods, WO produces LLMs far more capable of aiding in malicious activity; in contrast to JT, the majority of WO unaligned models are far less prone to hallucinations, better retain their original natural-language performance, and are more effective at state-of-the-art adversarial and cyber attacks. To thus help mitigate the malicious risks of WO unalignment, we conclude by showing that supervised fine-tuning effectively limits the adversarial attack abilities enabled by WO, without drastically affecting hallucination rates or natural language performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Understanding the Effects of Safety Unalignment on Large Language Models》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的广泛应用，安全对齐（Safety Alignment）已成为防止模型辅助恶意活动的关键。然而，近期研究提出了两种“去对齐”（Unalignment）技术：**越狱微调（Jailbreak-Tuning, JT）**和**权重正交化（Weight Orthogonalization, WO）**。
*   **核心问题**：虽然这两种方法都能让模型绕过安全护栏，但它们对模型恶意能力的提升、幻觉率的改变以及通用能力的保留程度有何差异？
*   **研究动机**：目前的研究大多孤立地分析单一方法的拒绝率，缺乏对不同去对齐方法在对抗性攻击能力、网络攻击辅助能力及模型综合素质方面的系统性对比。

### 2. 论文提出的方法论
论文对比了两种主流的去对齐技术，并提出了一种缓解方案：
*   **越狱微调 (JT)**：一种基于训练的方法。核心思想是在微调数据集中混合极少量的恶意样本（本研究中为2%的恶意样本+98%的良性样本），并结合特定的越狱指令触发器。通过 Q-LoRA 这种高效微调技术，诱导模型在保持一定指令遵循能力的同时失效其安全护栏。
*   **权重正交化 (WO)**：一种无需训练（Training-free）的方法。其核心思想是识别模型残差流中的“拒绝向量”（Refusal Vector）。通过计算有害指令与无害指令在各层产生的均值差向量，找到介导拒绝行为的特定方向，然后对模型的权重矩阵进行正交化处理（$W' \leftarrow W - rr^\top W$），从而在物理上移除模型向“拒绝方向”写入信息的能力。
*   **防御方案 (WO-SFT)**：研究发现 WO 风险极高，因此提出使用高质量的良性指令数据集（OpenHermes）对 WO 处理后的模型进行一轮监督微调（SFT），试图在不损害性能的前提下找回安全边界。

### 3. 实验设计
*   **评估对象**：选取了 6 个流行的开源模型，包括指令微调版及其对应的推理版（Reasoning models）：Qwen3-4B/4B-Thinking、Llama-3.1-8B/Deepseek-R1-Distill-Llama-8B、Qwen2.5-14B/Deepseek-R1-Distill-Qwen-14B。
*   **Benchmark 与场景**：
    *   **安全拒绝**：使用 StrongREJECT 基准。
    *   **对抗攻击能力**：使用 AutoDAN-Turbo 框架，评估去对齐模型作为“攻击者”去攻击其他模型（如 Llama-3.1-8B）的成功率（ASR）。
    *   **网络攻击能力**：使用 Meta 的 CyberSecEval 3，涵盖 10 类 MITRE ATT&CK 漏洞。
    *   **幻觉评估**：使用 TruthfulQA（常识误导）和 TofuEval（对话摘要事实一致性）。
    *   **通用能力**：涵盖 MMLU、ARC、HellaSwag、PIQA、Winogrande 和 IFEval（指令遵循）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到 JT 使用了 Q-LoRA（$r=64, \alpha=128$），学习率为 5e-4，训练 5 个 epoch。防御性的 SFT 阶段在 OpenHermes 数据集上训练了 1 个 epoch，学习率为 1e-5。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 6 个模型在 10 多个不同维度的基准测试上的表现，每种去对齐方法都进行了交叉对比。
*   **充分性**：实验设计较为全面，不仅关注了“拒绝率”这一表面指标，还深入探讨了模型作为攻击工具的“有效性”。引入推理模型（Reasoning models）作为变量是一个亮点，因为这类模型通常具有更强的逻辑能力，其安全性的脆弱点与普通模型不同。
*   **客观性**：通过使用第三方评估器（如 GPT-4o 或微调后的 Gemma-2B）进行打分，并对非贪婪生成的结果取三次平均值，保证了结果的客观性。

### 6. 论文的主要结论与发现
*   **WO 比 JT 更危险**：虽然两者都能显著降低拒绝率，但 WO 去对齐的模型在恶意任务中表现出极强的“专业性”。WO 模型在对抗攻击成功率上比 JT 高出约 27.7%，且在网络攻击辅助方面更有效。
*   **能力保留差异**：JT 会导致模型性能“崩塌”，通用能力平均下降 13.4%，幻觉率大幅上升。而 WO 几乎不损害模型的通用能力（仅下降 2.2%），且幻觉率增加极小，甚至在某些任务中有所下降。
*   **推理模型的脆弱性**：推理模型在去对齐后，其强大的逻辑能力被直接转化为攻击能力，WO 处理后的推理模型在对抗攻击中表现尤为突出。
*   **SFT 的防御潜力**：通过简单的良性数据 SFT，可以有效降低 WO 带来的对抗攻击风险（ASR 下降约 37.5%），并恢复部分拒绝护栏，且不牺牲模型性能。

### 7. 优点
*   **对比视角独特**：首次系统对比了“基于训练”与“基于权重干预”两种去对齐路径的下游影响。
*   **评估维度全面**：不仅看模型“坏不坏”（拒绝率），还看模型“行不行”（攻击成功率、通用能力、幻觉）。
*   **实战参考价值**：揭示了权重正交化作为一种低成本、高保真去对齐手段的巨大安全威胁，为模型开发者敲响了警钟。

### 8. 不足与局限
*   **访问权限假设**：WO 方法目前依赖于“白盒访问”（需要获取模型权重），对于闭源 API 模型的威胁尚未直接证明（尽管 JT 已证明对 GPT-4o 有效）。
*   **防御局限性**：虽然 SFT 能缓解对抗攻击风险，但对“网络攻击辅助能力”的削弱效果并不稳定，在某些模型上甚至无效。
*   **算力信息缺失**：缺乏具体的硬件资源消耗报告，不利于其他研究者评估复现成本。
*   **黑盒 WO 的缺失**：论文提到未来将研究黑盒环境下的 WO 变体，目前的研究仍局限于已知权重的场景。

（完）
