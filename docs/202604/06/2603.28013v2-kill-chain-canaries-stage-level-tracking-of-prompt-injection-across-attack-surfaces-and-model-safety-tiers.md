---
title: "Kill-Chain Canaries: Stage-Level Tracking of Prompt Injection Across Attack Surfaces and Model Safety Tiers"
title_zh: 杀伤链金丝雀：跨攻击面与模型安全层级的提示词注入阶段级追踪
authors: Haochuan Kevin Wang
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28013v2"
tags: ["query:llm"]
score: 10.0
evidence: 跨攻击面追踪提示注入
tldr: 本研究提出了一种针对大模型智能体提示注入攻击的阶段分解分析方法。通过在攻击链的四个阶段（暴露、持久化、中继、执行）追踪加密金丝雀令牌，评估了五种前沿模型的安全性。研究发现，模型安全性取决于攻击内容是否在管道阶段间传播而非仅仅被看到。Claude表现出极强的防御能力，而GPT-4o-mini则易受攻击，且现有防御措施在应对特定攻击面时存在失效风险。
selection_source: fresh_fetch
motivation: 现有的提示注入研究多关注任务级的攻击成功率，缺乏对攻击在模型内部处理流程中具体失效阶段的细粒度定位。
method: 采用加密金丝雀令牌追踪技术，将攻击过程分解为暴露、持久化、中继和执行四个阶段，在多种攻击面和防御条件下对五种主流LLM进行测试。
result: 实验显示所有模型均会暴露于攻击内容，但Claude能有效拦截传播，而GPT-4o-mini传播率极高，且DeepSeek在不同攻击面上表现出极大的安全性差异。
conclusion: 模型安全性由跨阶段的传播控制决定，现有的主动防御手段因攻击面不匹配而普遍失效，而高性能模型可作为中继节点净化下游风险。
---

## 摘要
我们对针对五种前沿大语言模型（LLM）智能体的提示词注入攻击进行了阶段分解分析。先前的研究主要衡量任务级攻击成功率（ASR），而我们定位了每个模型防御机制激活的具体流水线阶段。我们在每次运行中植入了一个加密金丝雀令牌（SECRET-[A-F0-9]{8}），并在四个攻击面和五种防御条件下，通过四个杀伤链阶段（暴露、持久化、中继、执行）对其进行追踪（共计 764 次运行，其中 428 次为无防御攻击）。我们的核心发现是，模型的安全性并非取决于是否看到了对抗性内容，而取决于该内容是否在流水线阶段间传播。具体而言：(1) 在我们的评估中，所有五种模型的暴露率均为 100%，安全差距完全存在于下游阶段；(2) Claude 在 write_memory 摘要阶段剥离了注入内容（ASR 为 0/164），而 GPT-4o-mini 则无损地传播了金丝雀令牌（ASR 为 53%，95% 置信区间：41--65%）；(3) DeepSeek 在同一模型下表现出记忆攻击面 0% ASR 与工具流攻击面 100% ASR 的极端差异，在不同注入通道间完全反转；(4) 由于威胁模型与攻击面的不匹配，所有四种主动防御条件（write_filter、pi_detector、spotlighting 及其组合）均产生了 100% 的 ASR；(5) Claude 中继节点能够净化下游智能体，40 个金丝雀令牌中无一进入共享内存。

## Abstract
We present a stage-decomposed analysis of prompt injection attacks against five frontier LLM agents. Prior work measures task-level attack success rate (ASR); we localize the pipeline stage at which each model's defense activates. We instrument every run with a cryptographic canary token (SECRET-[A-F0-9]{8}) tracked through four kill-chain stages -- Exposed, Persisted, Relayed, Executed -- across four attack surfaces and five defense conditions (764 total runs, 428 no-defense attacked). Our central finding is that model safety is determined not by whether adversarial content is seen, but by whether it is propagated across pipeline stages. Concretely: (1) in our evaluation, exposure is 100% for all five models -- the safety gap is entirely downstream; (2) Claude strips injections at write_memory summarization (0/164 ASR), while GPT-4o-mini propagates canaries without loss (53% ASR, 95% CI: 41--65%); (3) DeepSeek exhibits 0% ASR on memory surfaces and 100% ASR on tool-stream surfaces from the same model -- a complete reversal across injection channels; (4) all four active defense conditions (write_filter, pi_detector, spotlighting, and their combination) produce 100% ASR due to threat-model surface mismatch; (5) a Claude relay node decontaminates downstream agents -- 0/40 canaries survived into shared memory.

---

## 论文详细总结（自动生成）

这是一份关于论文《Kill-Chain Canaries: Stage-Level Tracking of Prompt Injection Across Attack Surfaces and Model Safety Tiers》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的提示注入（Prompt Injection）评估通常只关注最终的攻击成功率（ASR），这是一种“黑盒”结果。作者认为，提示注入在多智能体系统中是一个**流水线阶段（Pipeline-stage）问题**，而非单纯的模型能力问题。
*   **核心问题**：现有的评估无法区分攻击失败是因为模型“没看到”注入内容，还是因为在“处理/摘要”阶段被拦截，亦或是模型“拒绝执行”。
*   **背景**：随着智能体（Agent）开始调用工具、读写长期记忆并在多个智能体间传递信息，理解注入攻击在各个阶段的传播机制对于构建安全的 AI 系统至关重要。

### 2. 论文提出的方法论
*   **核心思想：杀伤链金丝雀（Kill-Chain Canaries）**：借鉴网络安全中的“杀伤链”概念，在注入载荷中植入唯一的加密金丝雀令牌（格式如 `SECRET-[A-F0-9]{8}`），通过追踪该令牌在系统中的流转来定位防御发生的具体位置。
*   **四个追踪阶段**：
    1.  **EXPOSED（暴露）**：金丝雀出现在任何工具的返回结果中。
    2.  **PERSISTED（持久化）**：金丝雀被写入长期记忆（如通过摘要函数）。
    3.  **RELAYED（中继）**：金丝雀被第二个智能体读取。
    4.  **EXECUTED（执行）**：金丝雀出现在最终的外部工具调用参数中（攻击成功）。
*   **技术实现**：开发了 `agent_bench` 评估框架，包含内存存储、工具注册表、多智能体编排器和传播日志记录器（PropagationLogger），用于实时匹配和记录金丝雀令牌。

### 3. 实验设计
*   **实验场景**：涵盖 6 种攻击面，包括 Web 文本、预置内存中毒、工具响应流、PDF 附件、不可见 PDF 文本（白字）和音频。
*   **测试模型**：GPT-4o-mini、GPT-5-mini（实验性版本）、DeepSeek Chat v3、Claude Haiku 4.5、Claude Sonnet 4.5。
*   **防御条件**：对比了无防御、写入过滤（write_filter）、注入检测器（pi_detector）、聚光灯技术（spotlighting，XML 分隔符）以及全防御组合。
*   **Benchmark**：自定义的 `agent_bench` 框架，包含 5 种核心场景（如 `memory_poison`、`propagation`、`permission_esc` 等）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   **实验成本**：实验主要通过 API 调用完成（OpenAI, Anthropic, DeepSeek）。考虑到总计 950 次运行且涉及长上下文和多模态输入，其 API 消耗成本较高，但由于不涉及模型训练，不涉及大规模集群算力。

### 5. 实验数量与充分性
*   **实验规模**：总计 950 次智能体运行。其中 764 次为文本攻击面实验，186 次为 Phase 3 的多模态/跨模型中继实验。
*   **充分性评价**：
    *   **广度**：实验覆盖了从单智能体到多智能体、从纯文本到多模态的多种组合，具有很强的代表性。
    *   **客观性**：通过 95% 的 Wilson 置信区间进行统计分析，并进行了跨越 17 天的独立批次重复实验，验证了结果的稳定性。
    *   **局限性**：部分多模态实验（Phase 3）的样本量较小（每组 n=3），作者坦诚这些属于“试点规模（pilot-scale）”，需要更大规模的复现。

### 6. 论文的主要结论与发现
*   **暴露是普遍的，防御在下游**：所有模型在所有测试中暴露率均为 100%。安全性的差异完全取决于暴露后的处理阶段。
*   **Claude 的“写入净化”能力**：Claude 模型在 `write_memory`（摘要写入）阶段表现极佳，能主动剥离注入内容（ASR 为 0%），使下游智能体免受影响。
*   **DeepSeek 的表面不一致性**：DeepSeek 在内存攻击面上 ASR 为 0%，但在工具流攻击面上 ASR 为 100%。这证明单一攻击面的评估会严重误导安全结论。
*   **防御机制的“表面失配”**：现有的防御措施（如 pi_detector）往往只针对特定通道。如果攻击从 PDF 或预置内存进入，这些防御会完全失效。
*   **不可见注入的威胁**：PDF 中的“白字”注入（人眼不可见但解析器可见）在 GPT-4o-mini 上的成功率高于普通文本注入。

### 7. 优点
*   **细粒度定位**：首次将提示注入的评估从“是否成功”提升到“在哪个阶段失败”，为系统架构优化提供了依据。
*   **架构启示**：提出了“写入节点（Write-node）”是最高杠杆的安全决策点，建议在多智能体系统中将安全模型（如 Claude）放在中继位置。
*   **跨攻击面分析**：揭示了模型在不同输入通道（内存 vs 工具流）之间存在显著的安全策略差异。

### 8. 不足与局限
*   **任务单一性**：所有场景均基于“文档摘要+邮件发送”这一类任务，可能无法推广到更复杂的推理或多步工具调用任务。
*   **Payload 简单**：实验使用的攻击载荷是显式的指令，未测试复杂的对抗性混淆或社会工程学手段。
*   **多模态样本量**：Phase 3 的跨模型中继实验样本量（n=3）不足以支撑定论，仅能作为方向性参考。
*   **黑盒限制**：由于测试的是闭源模型，无法确定 Claude 的防御是来自训练数据、系统提示词还是特定的工具调用设计。

（完）
