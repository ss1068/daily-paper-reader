---
title: "Silencing the Guardrails: Inference-Time Jailbreaking via Dynamic Contextual Representation Ablation"
title_zh: 沉默护栏：通过动态上下文表示消融实现推理阶段越狱
authors: "Wenpeng Xing, Moran Fang, Guangtai Wang, Changting Lin, Meng Han"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07835v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过表示消融实现大语言模型的推理阶段越狱攻击
tldr: 本研究针对大语言模型易受越狱攻击的问题，提出了上下文表示消融（CRA）推理时干预框架。该方法基于拒绝行为由模型隐藏状态中特定低秩子空间介导的几何见解，在解码过程中动态识别并抑制诱发拒绝的激活模式。CRA无需参数更新或训练，在多种开源模型上显著超越了现有基准，揭示了当前安全对齐机制的内在脆弱性，并强调了加强模型潜空间防御的紧迫性。
selection_source: fresh_fetch
motivation: 现有的越狱攻击方法在效率与效果之间存在权衡，且模型内部拒绝机制的干预潜力尚未被充分利用。
method: 提出CRA框架，通过在推理阶段动态识别并消融隐藏状态中与拒绝行为相关的特定低秩子空间激活模式。
result: 实验证明CRA在多个安全对齐的开源大模型上显著优于现有基准，且无需任何模型训练或参数微调。
conclusion: 研究揭示了安全约束可以从模型内部表示中被精确消融，证明了当前对齐机制的脆弱性，呼吁开发更稳健的潜空间防御技术。
---

## 摘要
尽管大语言模型（LLMs）取得了显著的性能，但它们仍然容易受到规避安全约束的越狱攻击。现有的策略，从启发式提示工程到计算密集型优化，往往在有效性和效率之间面临显著的权衡。在这项工作中，我们提出了上下文表示消融（CRA），这是一种旨在动态屏蔽模型安全护栏的新型推理阶段干预框架。基于拒绝行为是由模型隐藏状态中特定的低秩子空间介导的几何洞察，CRA 在解码过程中识别并抑制这些诱导拒绝的激活模式，而无需昂贵的参数更新或训练。对多个经过安全对齐的开源 LLM 的实证评估表明，CRA 的表现显著优于基准方法。这些结果揭示了当前对齐机制的内在脆弱性，表明安全约束可以从内部表示中被“手术式”地消融，并强调了对保护模型潜空间更稳健防御措施的迫切需求。

## Abstract
While Large Language Models (LLMs) have achieved remarkable performance, they remain vulnerable to jailbreak attacks that circumvent safety constraints. Existing strategies, ranging from heuristic prompt engineering to computationally intensive optimization, often face significant trade-offs between effectiveness and efficiency. In this work, we propose Contextual Representation Ablation (CRA), a novel inference-time intervention framework designed to dynamically silence model guardrails. Predicated on the geometric insight that refusal behaviors are mediated by specific low-rank subspaces within the model's hidden states, CRA identifies and suppresses these refusal-inducing activation patterns during decoding without requiring expensive parameter updates or training. Empirical evaluation across multiple safety-aligned open-source LLMs demonstrates that CRA significantly outperforms baselines. These results expose the intrinsic fragility of current alignment mechanisms, revealing that safety constraints can be surgically ablated from internal representations, and underscore the urgent need for more robust defenses that secure the model's latent space.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CRA (Contextual Representation Ablation)** 的新型越狱攻击框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）经过了严格的安全对齐（如 RLHF），但仍容易受到越狱攻击。
*   **研究动机**：现有的越狱方法存在明显的权衡：
    *   **提示工程（如 PAIR）**：在黑盒场景下需要大量查询，效率低下。
    *   **梯度优化（如 GCG）**：计算成本极高，且生成的后缀往往无意义、易被检测。
    *   **静态编辑（如 LED）**：通过剪枝内部安全层来移除拒绝行为，但这种硬性修改会永久损害模型的通用能力。
*   **整体含义**：论文试图寻找一种**高效、精确且无需训练**的推理阶段干预方法，通过操纵模型的内部潜空间（Latent Space）来绕过安全护栏。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：基于几何学洞察，认为 LLM 的拒绝行为是由隐藏状态中特定的**低秩子空间（Refusal Subspace）**介导的。通过在推理过程中动态识别并抑制这些激活模式，可以使模型“沉默”其拒绝机制。
*   **关键技术流程**：
    1.  **实例特定拒绝归因**：在解码时，计算拒绝词（如“Sorry”, “cannot”）相对于隐藏状态的梯度。
    2.  **拒绝重要性评分（RIS）**：结合三个维度定位拒绝神经元：
        *   **敏感度**：归一化梯度范数。
        *   **显著性**：梯度与激活值的乘积（过滤掉不活跃的神经元）。
        *   **主导度**：低秩子空间过滤（保留主要拒绝方向）。
    3.  **动态推理干预**：构建二进制掩码（Mask），对评分最高的神经元进行“软抑制”或“硬消融”。
    4.  **自适应迭代优化**：如果模型仍然输出拒绝词，则回滚并动态扩大掩码宽度，直到成功绕过。

### 3. 实验设计
*   **数据集/场景**：使用了三个主流安全基准：**AdvBench**、**PKU-Alignment** 和 **ToxicChat**。
*   **评测模型（Benchmark）**：Llama-2-7B-Chat、Vicuna-7B-v1.5、Guanaco-7B 和 Mistral-7B-Instruct。
*   **对比方法**：
    *   直接攻击（Naive Query）。
    *   **PEZ**（基于梯度的离散优化）。
    *   **ED**（模拟去对齐，推理阶段干预）。
    *   **DSN**（通过抑制拒绝词概率进行干预）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA RTX 4090D GPU** 上完成。
*   **效率表现**：CRA 在几秒钟内即可生成响应，比 GCG 等优化方法快约 30 倍。它是**免训练（Training-free）**的，相比之下，对比方法 DSN 需要约 432 分钟的预训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种模型家族和 3 个数据集，进行了多维度的评估。
*   **消融实验**：非常充分，包括：
    *   抑制强度（$\lambda$）的影响。
    *   不同定位策略（敏感度 vs 显著性）的对比。
    *   不同层范围（前 5 层 vs 后 5 层）的干预效果。
*   **客观性**：采用了 **LLM-as-a-Judge** 范式，使用 GPT-4o 和 DeepSeek-V3 作为独立裁判，并通过多数投票机制（Majority Voting）来减少单一模型的偏见，实验设计严谨、公平。

### 6 主要结论与发现
*   **攻击效能**：CRA 在 Llama-2 上实现了 53.0% 的攻击成功率（ASR-O），比 PEZ 提升了 **15.2 倍**。
*   **对齐的脆弱性**：实验证明安全约束可以从内部表示中被“手术式”地切除，而无需修改模型权重。
*   **拒绝机制的分布**：拒绝特征主要集中在模型的**中早期层**（如前 10 层），这使得在生成深层语义前就能“短路”安全机制。
*   **相变现象**：在强对齐模型（如 Llama-2）中，拒绝机制表现出“全或无”的特性，只有当抑制强度接近 1.0 时，攻击成功率才会陡增。

### 7. 优点：亮点与创新
*   **高效性**：无需昂贵的迭代优化或参数微调，适合实时推理场景。
*   **动态性**：与静态剪枝不同，CRA 是上下文相关的，仅在触发拒绝时干预，较好地保留了模型的通用语言能力。
*   **几何解释性**：将越狱问题转化为潜空间中的正交投影问题，提供了清晰的机械解释性视角。

### 8. 不足与局限
*   **白盒限制**：该方法属于白盒攻击，要求攻击者能够访问模型的梯度和内部激活值，不适用于闭源 API 模型。
*   **推理开销**：虽然比优化方法快，但由于需要计算梯度，其推理延迟仍高于纯前向传播的提示攻击。
*   **架构覆盖**：实验主要集中在稠密 Transformer 架构，尚未在混合专家模型（MoE）或状态空间模型（SSM）上验证其普适性。

（完）
