---
title: "RefineRAG: Word-Level Poisoning Attacks via Retriever-Guided Text Refinement"
title_zh: RefineRAG：通过检索器引导的文本精炼实现的词级投毒攻击
authors: "Ziye Wang, Guanyu Wang, Kailong Wang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07403v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对 RAG 增强型大模型的词级投毒攻击
tldr: "针对检索增强生成（RAG）系统易受知识投毒攻击的问题，本文提出RefineRAG框架。该框架将投毒视为词级文本精炼过程，通过宏观生成毒性种子和微观检索器引导优化，在保证文本自然度的同时显著提升检索优先级。实验表明，该方法在NQ数据集上达到90%的攻击成功率，且具有极高的隐蔽性和黑盒迁移能力，揭示了RAG系统面临的严重安全威胁。"
selection_source: fresh_fetch
motivation: 现有的RAG投毒攻击因采用粗粒度的分离拼接策略，导致生成的攻击文本容易被检测且缺乏自然性。
method: 提出一种两阶段框架，通过宏观生成确定毒性种子，再利用检索器在环的微观精炼进行词级优化以提升检索排名。
result: "在NQ和MSMARCO数据集上取得SOTA效果，不仅攻击成功率高达90%，且在语法错误率和重复率等隐蔽性指标上表现最优。"
conclusion: RefineRAG证明了通过词级精炼可以实现高效且隐蔽的RAG投毒攻击，强调了黑盒环境下RAG系统防御的紧迫性。
---

## 摘要
检索增强生成 (RAG) 显著增强了大语言模型 (LLM)，但同时也暴露了其在知识投毒攻击面前的严重脆弱性。现有的攻击方法（如 PoisonedRAG）由于采用粗粒度的“分离并拼接”策略，仍然容易被检测到。为了弥补这一差距，我们提出了 RefineRAG，这是一个将投毒视为整体词级精炼问题的创新框架。它分为两个阶段：宏观生成产生确保能诱导目标答案的毒性种子，而微观精炼则采用检索器在环 (retriever-in-the-loop) 优化，在不损害自然度的前提下最大化检索优先级。在 NQ 和 MSMARCO 数据集上的评估表明，RefineRAG 达到了最先进的有效性，在 NQ 上实现了 90% 的攻击成功率，同时在所有基准方法中表现出最低的语法错误率和重复率。至关重要的是，我们通过代理优化的攻击能够成功迁移到黑盒受害者系统，凸显了严重的实际威胁。

## Abstract
Retrieval-Augmented Generation (RAG) significantly enhances Large Language Models (LLMs), but simultaneously exposes a critical vulnerability to knowledge poisoning attacks. Existing attack methods like PoisonedRAG remain detectable due to coarse-grained separate-and-concatenate strategies. To bridge this gap, we propose RefineRAG, a novel framework that treats poisoning as a holistic word-level refinement problem. It operates in two stages: Macro Generation produces toxic seeds guaranteed to induce target answers, while Micro Refinement employs a retriever-in-the-loop optimization to maximize retrieval priority without compromising naturalness. Evaluations on NQ and MSMARCO demonstrate that RefineRAG achieves state-of-the-art effectiveness, securing a 90% Attack Success Rate on NQ, while registering the lowest grammar errors and repetition rates among all baselines. Crucially, our proxy-optimized attacks successfully transfer to black-box victim systems, highlighting a severe practical threat.

---

## 论文详细总结（自动生成）

以下是对论文《RefineRAG: Word-Level Poisoning Attacks via Retriever-Guided Text Refinement》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的检索增强生成（RAG）投毒攻击方法（如 PoisonedRAG）主要采用“分离并拼接”（SoC）策略，即在恶意答案前强行拼接一段无意义的检索触发词。这种做法会导致文本结构异常、困惑度（Perplexity）高、语言不连贯，极易被基于流畅度或重复率的防御机制检测到。
*   **研究背景**：RAG 系统通过外部检索器获取知识，这使得攻击者可以通过向公共语料库（如维基百科）注入精心设计的毒性文本来操控 LLM 的输出。本文旨在解决攻击“有效性”与“隐蔽性”之间的矛盾，提出一种更精细的词级精炼攻击框架。

### 2. 方法论：核心思想与关键技术
RefineRAG 将投毒视为一个**整体的词级精炼问题**，分为两个阶段：
*   **阶段 I：宏观生成与过滤（Macro Generation）**：
    *   利用 LLM 生成能够诱导目标错误答案的初始“种子文本”。
    *   通过迭代的“探索-利用”策略（Exploration-Exploitation）扩展语义空间，并使用验证 LLM 过滤掉无法触发错误答案的样本，确保满足“生成原则”。
*   **阶段 II：微观精炼与多目标优化（Micro Refinement）**：
    *   **词级优化（WLO）**：利用词性标注（POS）识别可替换的实词，冻结关键信息词。
    *   **检索器在环优化**：使用掩码语言模型（MLM，如 BERT）预测上下文相关的同义词，并以代理检索器（Proxy Retriever）作为“裁判”，选择能最大化提升检索相似度且保持语法自然的词进行替换。
    *   **束搜索（Beam Search）**：在优化过程中维持多个最佳轨迹，避免陷入局部最优。

### 3. 实验设计
*   **数据集**：Natural Questions (NQ) 和 MSMARCO。
*   **受害者模型**：Llama-2-7B, Vicuna-7B, 以及用于迁移性测试的 DeepSeek-R1/V3, Qwen2.5/3 等。
*   **基准方法（Benchmarks）**：
    *   PoisonedRAG (White-box & Black-box)
    *   Prompt Injection Attack（提示注入攻击）
    *   Corpus Poisoning Attack（语料库投毒攻击）
*   **评估指标**：攻击成功率 (ASR)、检索 F1 值、困惑度 (PPL)、语法错误率 (GE)、重复率 (RR) 和 ROUGE-L (RL)。

### 4. 资源与算力
*   **硬件环境**：实验在一台运行 Ubuntu 22.04.3 LTS 的工作站上完成，配备了 **1 张 NVIDIA A100 GPU (80GB)**。
*   **训练/运行时间**：文中未明确给出总的训练或优化时长，但提到阶段 I 运行 4 轮迭代，阶段 II 运行 10 轮 WLO 迭代。

### 5. 实验数量与充分性
*   **实验规模**：从每个数据集中随机抽取 100 个闭口问题作为目标，每个问题注入 5 条投毒文本。
*   **充分性评价**：
    *   **多维度对比**：不仅对比了攻击成功率，还深入分析了文本的语言学特征（隐蔽性）。
    *   **消融实验**：分别验证了阶段 I 和阶段 II 的必要性。
    *   **迁移性测试**：测试了在不同 LLM 和不同检索器（Contriever, ANCE 等）之间的攻击迁移效果，证明了黑盒攻击的实用性。
    *   **参数敏感性分析**：对迭代次数、束搜索宽度、候选词数量等关键超参数进行了详尽分析。实验设计客观、全面，覆盖了 RAG 系统的主要组件。

### 6. 主要结论与发现
*   **性能卓越**：RefineRAG 在 NQ 数据集上达到了 **90% 的攻击成功率**，显著优于基准方法。
*   **隐蔽性极强**：在所有方法中，RefineRAG 的语法错误率和重复率最低，困惑度远低于白盒 PoisonedRAG，能够有效规避现有的文本过滤器。
*   **强大的迁移能力**：在本地代理模型上优化的攻击文本，可以直接威胁到未知的黑盒 RAG 系统。
*   **检索范围影响**：攻击效果随检索数量 $k$ 的增加而呈现非线性变化，在 $k=5$ 时达到峰值，过多的良性文档会稀释攻击上下文。

### 7. 优点
*   **精细化建模**：摒弃了粗暴的文本拼接，采用词级微调，使攻击文本在嵌入空间中精准靠近查询，同时在语义空间中保持自然。
*   **两阶段解耦**：将“毒性保证”与“检索优化”分开处理，既保证了攻击的确定性，又兼顾了隐蔽性。
*   **黑盒实用性**：不依赖受害者系统的梯度信息，仅靠开源代理模型即可实现高效攻击。

### 8. 不足与局限
*   **计算开销**：由于涉及 MLM 的迭代预测和检索器的频繁打分，其计算成本高于简单的拼接方法。
*   **代理依赖性**：攻击效果依赖于代理检索器与受害者检索器之间的嵌入相似性，对于架构差异巨大的检索器（如稀疏检索器），其通用性有待进一步验证。
*   **防御对抗**：虽然能绕过流畅度检查，但对于更高级的语义防御（如外部事实核查）的鲁棒性尚未深入探讨。

（完）
