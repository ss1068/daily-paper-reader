---
title: "SkillSieve: A Hierarchical Triage Framework for Detecting Malicious AI Agent Skills"
title_zh: SkillSieve：一种用于检测恶意 AI 智能体技能的分层筛选框架
authors: "Yinghan Hou, Zongyou Yang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06550v1"
tags: ["query:llm"]
score: 9.0
evidence: 检测AI智能体技能中的提示注入和社交工程攻击
tldr: 针对AI智能体技能市场中存在的恶意代码和提示词注入风险，本文提出SkillSieve分层检测框架。该框架结合了基于XGBoost的快速特征筛选、多维度LLM子任务分析以及三模型LLM陪审团辩论机制。实验表明，SkillSieve在处理大规模真实技能数据时，不仅能有效识别混淆载荷和自然语言攻击，且在F1分数上显著优于现有工具，同时保持了极低的检测成本。
selection_source: fresh_fetch
motivation: 现有检测工具难以同时处理代码混淆和隐藏在自然语言指令中的安全威胁，导致AI智能体技能市场面临严峻的安全风险。
method: 提出一种由浅入深的三层架构，依次通过机器学习特征过滤、LLM多维度并行分析以及多模型陪审团辩论机制进行检测。
result: 在真实技能基准测试中，SkillSieve的F1分数达到0.800，远超现有工具ClawVet，且单次检测成本仅为0.006美元。
conclusion: SkillSieve证明了分层分流架构能高效兼顾检测精度与成本，为保障AI智能体生态安全提供了可扩展的开源解决方案。
---

## 摘要
OpenClaw 的 ClawHub 市场托管了超过 13,000 个社区贡献的智能体技能，根据最近的审计，其中 13% 到 26% 包含安全漏洞。正则表达式扫描器会遗漏混淆的有效载荷；形式化静态分析器无法读取 SKILL.md 文件中的自然语言指令，而提示词注入和社会工程攻击往往隐藏其中。这两种方法都无法同时处理这两种模态。SkillSieve 是一个三层检测框架，仅在需要时应用逐步深入的分析。第一层通过基于 XGBoost 的特征评分器运行正则表达式、AST 和元数据检查，在平均不到 40 毫秒的时间内以零 API 成本过滤掉约 86% 的良性技能。第二层将可疑技能发送给大语言模型（LLM），但并非询问一个宽泛的问题，而是将分析拆分为四个并行的子任务（意图对齐、权限合理性、隐蔽行为检测、跨文件一致性），每个任务都有自己的提示词和结构化输出。第三层将高风险技能提交给由三个不同 LLM 组成的评审团，它们独立投票，如果存在分歧，则在达成裁决前进行辩论。我们在 49,592 个真实的 ClawHub 技能和涵盖五种规避技术的对抗样本上进行了评估，并在 440 ARM 单板计算机上运行了整个流水线。在包含 400 个技能的有标签基准测试中，SkillSieve 达到了 0.800 的 F1 分数，优于 ClawVet 的 0.421，平均每个技能的成本为 0.006 美元。代码、数据和基准测试均已开源。

## Abstract
OpenClaw's ClawHub marketplace hosts over 13,000 community-contributed agent skills, and between 13% and 26% of them contain security vulnerabilities according to recent audits. Regex scanners miss obfuscated payloads; formal static analyzers cannot read the natural language instructions in SKILL.md files where prompt injection and social engineering attacks hide. Neither approach handles both modalities. SkillSieve is a three-layer detection framework that applies progressively deeper analysis only where needed. Layer 1 runs regex, AST, and metadata checks through an XGBoost-based feature scorer, filtering roughly 86% of benign skills in under 40ms on average at zero API cost. Layer 2 sends suspicious skills to an LLM, but instead of asking one broad question, it splits the analysis into four parallel sub-tasks (intent alignment, permission justification, covert behavior detection, cross-file consistency), each with its own prompt and structured output. Layer 3 puts high-risk skills before a jury of three different LLMs that vote independently and, if they disagree, debate before reaching a verdict. We evaluate on 49,592 real ClawHub skills and adversarial samples across five evasion techniques, running the full pipeline on a 440 ARM single-board computer. On a 400-skill labeled benchmark, SkillSieve achieves 0.800 F1, outperforming ClawVet's 0.421, at an average cost of 0.006 per skill. Code, data, and benchmark are open-sourced.

---

## 论文详细总结（自动生成）

这是一份关于论文《SkillSieve: A Hierarchical Triage Framework for Detecting Malicious AI Agent Skills》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 编码智能体（如 OpenClaw、Cursor）的普及，第三方“技能”市场（如 ClawHub）迅速扩张。然而，这些技能由自然语言指令（SKILL.md）和脚本组成，存在严重的供应链安全风险。
*   **现有工具的局限性：** 正则表达式扫描器（如 ClawVet）容易被混淆代码欺骗；形式化静态分析器（如 SkillFortify）无法理解自然语言中的提示注入（Prompt Injection）或社会工程攻击；单 LLM 扫描方案则存在成本高昂、对复杂逻辑判断不稳等问题。
*   **研究目标：** 开发一个既能处理代码又能理解自然语言，且在成本和精度之间取得平衡的自动化检测框架。

### 2. 方法论：核心思想与关键技术
SkillSieve 提出了一个**三层分层分流（Hierarchical Triage）架构**，仅对高风险样本投入高算力资源：

*   **Layer 1: 静态筛选（Static Triage）**
    *   **核心思想：** 利用极低成本的静态特征快速过滤大部分良性技能。
    *   **技术细节：** 结合正则表达式（匹配反弹 Shell、凭据窃取等）、AST 抽象语法树特征（系统调用、网络操作频率）、元数据（编辑距离检测钓鱼名称）以及 SKILL.md 的表面统计数据。
    *   **算法：** 使用 XGBoost 分类器或加权启发式评分器对 15-31 个特征进行打分。
*   **Layer 2: 结构化语义分解（SSD）**
    *   **核心思想：** 将模糊的“是否恶意”问题拆分为四个并行的结构化子任务。
    *   **子任务：** 1) 意图对齐（描述与指令是否一致）；2) 权限合理性（请求的 API Key 是否必要）；3) 隐蔽行为检测（是否试图隐藏日志或报错）；4) 跨文件一致性（脚本代码是否执行了说明文档未提及的操作）。
    *   **公式：** 最终风险分 $R_2$ 由四个子任务分数加权求和得出：$R_2 = w_A s_A + w_B s_B + w_C s_C + w_D s_D$。
*   **Layer 3: 多 LLM 评审团协议（Multi-LLM Jury）**
    *   **核心思想：** 解决单模型的偏见和不确定性。
    *   **流程：** 三个不同厂商的 LLM（如 Kimi、MiniMax、DeepSeek）独立投票。若意见分歧，则进入“结构化辩论”环节，模型需根据他人的理由重新审视证据并再次投票，最终按多数票裁决。

### 3. 实验设计
*   **数据集：**
    *   **全量数据：** 49,592 个来自 ClawHub 的真实技能包。
    *   **标注基准（Benchmark）：** 400 个经过人工校验的技能（89 恶意，311 良性）。
    *   **对抗样本：** 100 个涵盖编码混淆、跨文件逻辑拆分、条件触发、同形文字替换、延时载荷五种规避技术的样本。
*   **对比方法：** ClawVet（正则）、SkillFortify（形式化分析）、VirusTotal Code Insight（单 LLM）、单 LLM 直接提示词基准。

### 4. 资源与算力
*   **硬件设备：** 实验运行在一台价值 **440 美元的 Orange Pi AIpro** 单板计算机上（4核 ARM64 CPU, 24GB RAM）。
*   **算力分配：** 
    *   Layer 1 完全在本地 ARM 设备运行，无需 GPU，平均耗时 38.8ms。
    *   Layer 2/3 通过 WiFi 调用云端 LLM API（Kimi 2.5, MiniMax M2.7, DeepSeek-V3）。
*   **成本：** 平均每个技能的检测成本仅为 **0.006 美元**（因为 86% 的技能在 L1 层被免费过滤）。

### 5. 实验数量与充分性
*   **实验规模：** 扫描了近 5 万个真实样本，规模极大，具有很强的现实代表性。
*   **消融与对比：** 论文对比了“单 LLM 提示词”与“结构化分解（SSD）”的效果，证明了分解任务能显著提升召回率。
*   **客观性：** 引入了多厂商模型评审机制，并针对模型分歧进行了专门的辩论动态分析，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能卓越：** SkillSieve 在标注基准上达到 **0.800 的 F1 分数**，远高于 ClawVet 的 0.421。
*   **高召回率：** Layer 1 的召回率高达 0.989，确保了恶意样本极少漏网。
*   **对抗鲁棒性：** 成功拦截了所有五种测试的对抗规避技术。
*   **成本效益：** 相比于对所有技能进行 LLM 扫描，分层架构实现了约 7.2 倍的成本削减。
*   **现实威胁：** 验证了 Snyk 等机构的发现，证实 ClawHub 中约 13.8% 的技能确实存在可疑行为。

### 7. 优点
*   **架构优雅：** 三层分流机制完美平衡了“快而省”与“慢而准”的矛盾。
*   **语义深度：** 通过 SSD 解决了 LLM 在处理长文档时容易忽略细节的问题。
*   **落地性强：** 证明了在廉价边缘设备（Orange Pi）上部署大规模安全审查系统的可行性。
*   **开源贡献：** 提供了目前最大的 AI 智能体技能安全基准数据集。

### 8. 不足与局限
*   **动态检测缺失：** 无法检测运行时从远程 URL 动态拉取的恶意载荷（仅限静态/语义分析）。
*   **LLM 依赖：** 后两层高度依赖外部 API，存在非确定性风险和隐私泄露风险（虽然 L1 缓解了部分压力）。
*   **纯名称攻击：** 对于仅通过名称模仿（Typosquatting）但内容完全良性的技能，该框架可能无法有效识别（需结合注册表层面的相似度检查）。
*   **时间延迟攻击：** 逻辑完全休眠的载荷在静态扫描阶段依然是所有检测工具的共同难题。

（完）
