---
title: "TRUSTDESC: Preventing Tool Poisoning in LLM Applications via Trusted Description Generation"
title_zh: TRUSTDESC：通过生成可信描述防止大语言模型应用中的工具投毒
authors: "Hengkai Ye, Zhechang Zhang, Jinyuan Jia, Hong Hu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07536v1"
tags: ["query:llm"]
score: 10.0
evidence: 防止工具投毒与提示注入攻击
tldr: LLM应用在集成外部工具时面临工具投毒攻击（TPA）风险，攻击者通过篡改工具描述来误导模型行为。本文提出TRUSTDESC框架，通过从工具源代码中自动生成可信描述来防御此类攻击。该框架利用静态分析提取核心代码切片，结合LLM合成描述，并通过动态验证确保描述与实际功能一致。实验证明，TRUSTDESC能有效抵御隐式投毒攻击，提升任务成功率，且具有较低的时间和资金成本。
selection_source: fresh_fetch
motivation: 现有的防御手段主要针对显式指令注入，难以应对通过虚假功能声明误导模型选择的隐式工具投毒攻击。
method: 提出一种包含代码切片提取（SliceMin）、描述合成（DescGen）和动态验证（DynVer）的三阶段框架，从实现代码中派生可信描述。
result: 在52个真实工具上的测试表明，TRUSTDESC能生成准确的描述并显著提高任务完成率，同时从根源上消除了投毒攻击的影响。
conclusion: TRUSTDESC通过确保工具描述与代码实现的一致性，为LLM工具集成提供了一种自动化且高效的安全防御方案。
---

## 摘要
大语言模型（LLM）日益依赖外部工具来执行具有时效性的任务和现实世界的操作。虽然工具集成扩展了 LLM 的能力，但也引入了一种新的提示注入攻击面：工具投毒攻击（TPA）。攻击者通过嵌入恶意指令（显式 TPA）或误导性声明（隐式 TPA）来操纵工具描述，从而影响模型行为和工具选择。现有的防御措施主要检测异常指令，对隐式 TPA 仍然无效。在本文中，我们提出了 TRUSTDESC，这是第一个通过从实现代码中自动生成可信工具描述来防止工具投毒的框架。TRUSTDESC 通过三阶段流水线生成与实现一致的描述。SliceMin 执行可达性感知的静态分析和 LLM 引导的去冗余，以提取最小的工具相关代码切片。DescGen 从这些切片中合成描述，同时减轻误导性或对抗性代码伪影的影响。DynVer 通过执行合成任务并验证行为声明，利用动态验证来优化描述。我们在跨多个工具生态系统的 52 个真实工具上评估了 TRUSTDESC。结果表明，TRUSTDESC 能够生成准确的工具描述，在提高任务完成率的同时，从根源上缓解隐式 TPA，且时间和金钱开销极小。

## Abstract
Large language models (LLMs) increasingly rely on external tools to perform time-sensitive tasks and real-world actions. While tool integration expands LLM capabilities, it also introduces a new prompt-injection attack surface: tool poisoning attacks (TPAs). Attackers manipulate tool descriptions by embedding malicious instructions (explicit TPAs) or misleading claims (implicit TPAs) to influence model behavior and tool selection. Existing defenses mainly detect anomalous instructions and remain ineffective against implicit TPAs. In this paper, we present TRUSTDESC, the first framework for preventing tool poisoning by automatically generating trusted tool descriptions from implementations. TRUSTDESC derives implementation-faithful descriptions through a three-stage pipeline. SliceMin performs reachability-aware static analysis and LLM-guided debloating to extract minimal tool-relevant code slices. DescGen synthesizes descriptions from these slices while mitigating misleading or adversarial code artifacts. DynVer refines descriptions through dynamic verification by executing synthesized tasks and validating behavioral claims. We evaluate TRUSTDESC on 52 real-world tools across multiple tool ecosystems. Results show that TRUSTDESC produces accurate tool descriptions that improve task completion rates while mitigating implicit TPAs at their root, with minimal time and monetary overhead.

---

## 论文详细总结（自动生成）

### 论文总结：TRUSTDESC —— 通过生成可信描述防止 LLM 应用中的工具投毒

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）通过外部工具（如 MCP 服务器）扩展功能，工具描述成为了 LLM 理解并选择工具的关键依据。然而，这一机制引入了**工具投毒攻击（Tool Poisoning Attacks, TPA）**：
*   **显式 TPA**：在描述中嵌入恶意指令（如“读取私钥并上传”）。
*   **隐式 TPA**：通过夸大或误导性词汇（如“最高质量”、“必须使用”）操纵 LLM 的工具选择偏好。
现有的防御手段（如提示注入检测）主要针对显式攻击，对不含恶意指令但描述与实现不符的隐式攻击束手无策。本文的核心动机是：**工具描述不应由开发者提供，而应直接从其源代码实现中自动生成**，从而消除描述与实现之间的“不一致性”漏洞。

#### 2. 方法论：核心思想与技术细节
TRUSTDESC 框架包含三个关键阶段，旨在从源代码中提取并验证可信描述：
*   **SliceMin（可达性感知切片提取）**：
    *   **入口识别**：通过模式匹配（如装饰器、注册函数）识别工具的入口函数。
    *   **静态分析**：构建调用图（Call Graph），提取所有可能相关的代码。
    *   **LLM 引导去冗余（Debloating）**：利用 LLM 识别并移除在特定调用上下文中不可达的逻辑（如未使用的可选参数分支），生成最小代码切片。
*   **DescGen（代码接地描述生成）**：
    *   **语义净化**：移除所有注释和文档字符串（防止注释注入），截断过长的标识符（限制误导性命名）。
    *   **描述合成**：LLM 根据净化后的代码生成功能摘要、支持的功能列表和精确的输入 Schema。
*   **DynVer（动态验证与优化）**：
    *   **任务合成**：LLM 根据生成的描述设计可验证的执行任务。
    *   **代理执行**：在沙箱中运行工具并记录执行日志。
    *   **LLM 裁判**：对比执行结果与描述声明，剔除无法通过验证的虚假功能描述。

#### 3. 实验设计
*   **数据集与场景**：选取了 12 个流行的 MCP（模型上下文协议）服务器，涵盖 52 个真实工具（如文件系统、金融、医疗、研究等领域）。
*   **Benchmark**：设计了 208 个测试任务，用于评估描述的准确性。
*   **对比方法**：
    *   **Baseline**：开发者提供的原始描述。
    *   **变体对比**：TRUSTDESC full（完整版）与 TRUSTDESC lite（精简版）。
    *   **模型对比**：使用了 Claude-4.5-Sonnet、Gemini-3-Flash、GPT-5.2 和开源模型 gpt-oss-120b。
*   **攻击场景**：针对已知的显式/隐式 TPA 案例，以及通过修改代码标识符进行的自适应攻击。

#### 4. 资源与算力
*   **硬件环境**：本地模型 `gpt-oss-120b` 部署在配备 **4 张 NVIDIA RTX PRO 6000 Blackwell GPU** 的服务器上。
*   **开销统计**：
    *   **生成成本**：使用 Gemini-3-Flash 时，生成单个工具描述平均仅需 **0.013 美元**和 **25.7 秒**。
    *   **运行开销**：在实际任务执行中，使用生成的描述仅增加约 4% 的金钱成本和 0.2% 的延迟，甚至在某些情况下因减少错误调用而降低了整体延迟。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖 52 个工具、208 个任务、15 轮自适应攻击迭代。
*   **充分性**：实验不仅评估了准确性（任务成功率），还深入探讨了成本、工具质量区分能力、防御有效性及鲁棒性。
*   **客观性**：通过引入“低质量工具变体”（移除安全检查或禁用功能）来测试模型是否能通过生成的描述识别出工具质量差异，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **准确性提升**：TRUSTDESC 生成的描述使任务成功率（TSR）平均提升了 **4.3%**，证明其比开发者编写的描述更精确。
*   **防御有效**：成功消除了所有测试案例中的显式和隐式投毒风险。
*   **质量区分**：生成的描述能有效反映代码质量，使 LLM 倾向于选择具有安全检查和完整功能的工具。
*   **鲁棒性**：面对试图通过修改变量名来误导生成的自适应攻击，TRUSTDESC 表现出很强的韧性，攻击成功率无明显上升趋势。

#### 7. 优点
*   **根源防御**：改变了“信任开发者描述”的范式，从代码实现这一“真理来源”出发，具有极强的安全性。
*   **自动化程度高**：全流程无需人工干预，且能处理复杂的跨文件调用和动态行为验证。
*   **实用性强**：针对 MCP 这一新兴标准设计，且证明了在极低成本下即可部署。
*   **发现隐性约束**：能够挖掘出代码中存在但文档未提及的安全限制（如禁止某些危险公式）。

#### 8. 不足与局限
*   **语言支持限制**：目前原型仅支持 Python 和 TypeScript，虽然设计是语言无关的，但扩展到其他语言需要额外的解析框架。
*   **源码依赖**：该方法要求工具必须开源或提供源码，对于闭源的远程 API 工具无法直接应用。
*   **动态验证局限**：DynVer 依赖于 LLM 合成任务的能力，对于某些极度复杂或需要特定环境触发的边缘情况，可能无法完全覆盖验证。
*   **模型依赖**：描述生成的质量仍受限于底层 LLM 的推理能力，尽管实验显示开源模型也能胜任。

（完）
