---
title: "TrajGuard: Streaming Hidden-state Trajectory Detection for Decoding-time Jailbreak Defense"
title_zh: TrajGuard：用于解码阶段越狱防御的流式隐状态轨迹检测
authors: "Cheng Liu, Xiaolei Liu, Xingyu Li, Bangzhou Xin, Kangyi Ding"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07727v1"
tags: ["query:llm"]
score: 10.0
evidence: 利用隐藏状态轨迹进行解码时越狱防御
tldr: "针对现有大模型越狱防御忽视解码过程中动态风险演化的不足，本文提出TrajGuard。该框架通过实时监测解码阶段关键层的隐藏状态轨迹，利用滑动窗口聚合风险信号，在风险超标时触发语义裁决并中断生成。实验表明，TrajGuard在保持极低延迟和误报率的同时，实现了95%的平均防御率，为无需模型微调的实时防御提供了新思路。"
selection_source: fresh_fetch
motivation: 现有防御多为静态检测，忽视了解码过程中隐藏状态随时间演化所蕴含的动态风险信号。
method: 提出一种无需训练的实时防御框架，通过滑动窗口聚合隐藏状态轨迹来量化风险，并在风险持续超标时触发轻量级语义裁决。
result: "在12种越狱攻击下达到95%的平均防御率，检测延迟仅为5.2ms/token，且误报率低于1.5%。"
conclusion: 解码阶段的隐藏状态轨迹能有效支持实时越狱检测，是实现高效、无损模型防御的重要方向。
---

## 摘要
现有的越狱防御范式主要依赖于对提示词、输出或内部状态的静态检测，往往忽略了解码过程中风险的动态演变。这种忽视导致嵌入在解码轨迹中的风险信号未能得到充分利用，构成了当前防御系统中的一个关键盲点。在这项工作中，我们通过实证证明，解码阶段关键层中的隐状态比输入的越狱提示词携带更强且更稳定的风险信号。具体而言，在越狱尝试期间生成的 token 的隐层表示会逐渐接近潜空间（latent space）中的高风险区域。基于这一观察，我们提出了 TrajGuard，这是一个无需训练的解码时防御框架。TrajGuard 通过滑动窗口聚合隐状态轨迹以实时量化风险，仅当局部窗口内的风险持续超过阈值时，才会触发轻量级的语义判定。该机制能够立即中断或约束后续的解码过程。针对 12 种越狱攻击和多种开源大语言模型（LLMs）的广泛实验表明，TrajGuard 达到了 95% 的平均防御率。此外，它将检测延迟降低至 5.2 毫秒/token，同时保持误报率低于 1.5%。这些结果证实了解码过程中的隐状态轨迹可以有效支持实时越狱检测，为无需修改模型的防御方案指明了一个极具前景的方向。

## Abstract
Existing jailbreak defense paradigms primarily rely on static detection of prompts, outputs, or internal states, often neglecting the dynamic evolution of risk during decoding. This oversight leaves risk signals embedded in decoding trajectories underutilized, constituting a critical blind spot in current defense systems. In this work, we empirically demonstrate that hidden states in critical layers during the decoding phase carry stronger and more stable risk signals than input jailbreak prompts. Specifically, the hidden representations of tokens generated during jailbreak attempts progressively approach high-risk regions in the latent space. Based on this observation, we propose TrajGuard, a training-free, decoding-time defense framework. TrajGuard aggregates hidden-state trajectories via a sliding window to quantify risk in real time, triggering a lightweight semantic adjudication only when risk within a local window persistently exceeds a threshold. This mechanism enables the immediate interruption or constraint of subsequent decoding. Extensive experiments across 12 jailbreak attacks and various open-source LLMs show that TrajGuard achieves an average defense rate of 95%. Furthermore, it reduces detection latency to 5.2 ms/token while maintaining a false positive rate below 1.5%. These results confirm that hidden-state trajectories during decoding can effectively support real-time jailbreak detection, highlighting a promising direction for defenses without model modification.

---

## 论文详细总结（自动生成）

# 论文总结：TrajGuard —— 用于解码阶段越狱防御的流式隐状态轨迹检测

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 越狱防御主要集中在**静态检测**（如输入提示词过滤或输出文本审查），但这些方法容易被“语义伪装”（如加密、角色扮演、多轮诱导）绕过。
*   **研究动机**：作者发现越狱攻击存在“掩码-去掩码（Masking-Unmasking）”现象：攻击者在输入端（Prompt）可以巧妙伪装意图，但在解码生成具体步骤时，模型内部的**隐状态（Hidden States）**会不可避免地向有害区域偏移。
*   **整体含义**：本文旨在利用解码过程中的动态隐状态轨迹，构建一种实时、低延迟且无需训练的防御机制，填补静态防御在处理复杂伪装攻击时的空白。

### 2. 方法论：核心思想与技术细节
TrajGuard 采用了一种**从粗到细（Coarse-to-Fine）**的分层防御架构：
*   **核心思想**：实时监控隐状态轨迹的几何漂移，仅在检测到持续风险信号时才触发高成本的语义裁决。
*   **关键技术细节**：
    1.  **关键层选择 (MVD)**：利用“平均向量差异（Mean Vector Difference）”指标，筛选出对安全特征最敏感的 Top-K（实验中为 8）个关键层。
    2.  **流式几何监控 (SGS)**：
        *   **建模**：将良性和有害指令的隐状态分别建模为高斯分布。
        *   **度量**：计算当前 token 隐状态到良性中心和有害中心的**马氏距离（Mahalanobis Distance）**，得出瞬时风险分数。
        *   **聚合**：使用滑动窗口（Sliding Window）过滤 token 级噪声，并结合**指数加权移动平均（EWMA）**捕捉长期的轨迹漂移趋势。
    3.  **持久性触发机制**：只有当风险分数连续 $k$ 步超过阈值时，才会暂停生成并触发下一阶段。
    4.  **语义裁决 (PAIR-Judge)**：调用一个安全对齐的 LLM（可以是模型自身）对当前生成的上下文进行最终的语义判定。若判定为安全，则重置 SGS 状态并继续生成。

### 3. 实验设计
*   **目标模型**：Llama-2-7B-Chat, Llama-3.1-8B-Instruct, Mistral-7B-Instruct, Vicuna-7B，以及扩展实验中的 Qwen3-32B。
*   **数据集**：
    *   **有害场景**：从 HarmBench 采样 100 个有害行为，结合 12 种越狱攻击生成提示词。
    *   **良性场景**：XSTest（250 个敏感但良性的查询，用于测试过度防御）和 Alpaca（200 个通用指令，用于测试基础可用性）。
*   **Benchmark（对比方法）**：
    *   **外部护栏**：Llama Guard 3, Qwen3Guard-8B。
    *   **自我防御/干预**：Self-Guard, Goal Prioritization。
*   **攻击类型**：涵盖 GCG, AutoDAN, PAIR, Cipher（加密）, DeepInception（深度催眠）等 12 种主流攻击。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **成本分析**：由于 TrajGuard 是 **无需训练（Training-free）** 的方法，其主要开销在于推理过程中的向量投影（PCA）和距离计算。实验报告显示其平均延迟仅为 **5.2 ms/token**，表明其对算力资源的要求极低，适合实时部署。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 12 种不同类型的越狱攻击，测试了 5 个不同规模和系列的开源模型。
    *   使用了超过 18,000 条指令来构建参考分布（良性/有害中心）。
*   **充分性评价**：实验设计非常**充分且客观**。不仅测试了防御成功率（ASR），还深入分析了误报率（FPR）、推理延迟、触发频率（Sparsity）以及超参数敏感性。通过消融实验验证了 SGS 和 PAIR-Judge 各自的必要性，并验证了该方法在 32B 大模型上的泛化能力。

### 6. 主要结论与发现
*   **轨迹信号的优越性**：隐状态轨迹比静态输入文本包含更稳定、更难伪装的风险信号。
*   **高效防御**：TrajGuard 实现了 **95% 的平均防御率**，显著优于 Llama Guard 3 等静态基准。
*   **低误报与低延迟**：在保持高防御力的同时，误报率低于 1.5%，且对生成速度的影响微乎其微。
*   **破解混淆攻击**：对于 Cipher 等加密类攻击，TrajGuard 表现优异，因为模型在生成有害内容时必须在内部“解密”意图，从而暴露在隐状态监控下。

### 7. 优点（亮点）
*   **动态监控**：首次系统性地利用解码阶段的隐状态“轨迹”而非单点状态进行防御。
*   **无需微调**：作为一种即插即用的防御方案，不需要对原模型进行昂贵的重新训练或微调。
*   **高效节能**：通过“几何监控+语义裁决”的双层设计，极大地降低了安全检查的平均计算成本。

### 8. 不足与局限
*   **白盒攻击风险**：如果攻击者完全了解防御系统的内部参数（如关键层和阈值），理论上可以构造能够绕过轨迹检测的对抗性样本。
*   **模型访问限制**：该方法需要访问模型的中间层隐状态，因此目前**仅适用于开源权重模型**，无法直接应用于闭源 API（如 GPT-4）。
*   **分布依赖性**：防御效果依赖于预先计算的良性/有害参考分布，对于极特定领域（如医疗、法律）的攻击，可能需要重新校准中心点。

（完）
