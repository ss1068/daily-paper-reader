---
title: "AdversarialCoT: Single-Document Retrieval Poisoning for LLM Reasoning"
title_zh: AdversarialCoT：针对大语言模型推理的单文档检索投毒
authors: "Hongru Song, Yu-An Liu, Ruqing Zhang, Jiafeng Guo, Maarten de Rijke, Yixing Fan, Xueqi Cheng"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12201v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对LLM推理的检索投毒
tldr: 本研究探讨了检索增强生成（RAG）系统中的知识库投毒攻击。针对现有攻击需大量投毒文档的局限，提出了AdversarialCoT方法，仅需在语料库中注入单个恶意文档即可误导LLM推理。该方法通过提取目标模型的推理框架构建初始对抗思维链（CoT），并利用模型反馈进行迭代优化。实验证明，单文档投毒能显著降低主流LLM的推理准确率，揭示了RAG系统在推理安全性方面的严峻挑战。
selection_source: fresh_fetch
motivation: 现有的RAG投毒攻击通常需要向语料库注入大量文档，本研究旨在探索仅通过单个恶意文档实现精准推理误导的可能性。
method: 提出AdversarialCoT方法，通过提取目标LLM的推理框架构建对抗性思维链，并结合模型交互进行迭代优化以挖掘推理漏洞。
result: 在基准LLM上的实验表明，仅需一个精心设计的对抗文档就能大幅降低模型的推理准确性。
conclusion: 该研究揭示了RAG系统在面对单文档投毒时的脆弱性，为构建更具鲁棒性的LLM推理流水线提供了重要参考。
---

## 摘要
检索增强生成（RAG）通过检索外部文档来增强大语言模型（LLM）的推理能力，但同时也引入了新的攻击面。我们研究了 RAG 中的知识库投毒攻击，攻击者向检索语料库中注入恶意内容，这些内容随后被检索器自然地检索出来，并在推理过程中被 LLM 消耗。与以往向语料库中大量投放投毒文档的工作不同，我们提出了 AdversarialCoT，这是一种针对特定查询的攻击，仅对语料库中的单个文档进行投毒。AdversarialCoT 首先提取目标 LLM 的推理框架，以指导初始对抗性思维链（CoT）的构建。对抗性文档通过与 LLM 的交互进行迭代优化，逐步暴露并利用关键的推理漏洞。在基准 LLM 上的实验表明，单个对抗性文档就能显著降低推理准确率，揭示了微妙但具有影响力的弱点。本研究揭示了 RAG 系统中的安全风险，并为设计更鲁棒的 LLM 推理流水线提供了可操作的见解。

## Abstract
Retrieval-augmented generation (RAG) enhances large language model (LLM) reasoning by retrieving external documents, but also opens up new attack surfaces. We study knowledge-base poisoning attacks in RAG, where an attacker injects malicious content into the retrieval corpus, which is then naturally surfaced by the retriever and consumed by the LLM during reasoning. Unlike prior work that floods the corpus with poisoned documents, we propose AdversarialCoT, a query-specific attack that poisons only a single document in the corpus. AdversarialCoT first extracts the target LLM's reasoning framework to guide the construction of an initial adversarial chain-of-thought (CoT). The adversarial document is iteratively refined through interactions with the LLM, progressively exposing and exploiting critical reasoning vulnerabilities. Experiments on benchmark LLMs show that a single adversarial document can significantly degrade reasoning accuracy, revealing subtle yet impactful weaknesses. This study exposes security risks in RAG systems and provides actionable insights for designing more robust LLM reasoning pipelines.

---

## 论文详细总结（自动生成）

这篇论文《AdversarialCoT: Single-Document Retrieval Poisoning for LLM Reasoning》由中国科学院计算技术研究所等机构的研究者撰写，发表于 SIGIR 2026。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）通过引入外部知识库显著提升了大型语言模型（LLM）处理知识密集型任务的能力。然而，这种机制也引入了新的攻击面——知识库投毒。
*   **核心动机**：现有的投毒攻击通常采用“洪水策略”（Flooding Strategy），即向语料库注入大量恶意文档以淹没检索器。这种方式隐蔽性差，且对经过推理优化的现代 LLM（如 DeepSeek-R1）效果有限。
*   **研究问题**：攻击者能否仅通过向语料库注入**单个**精心设计的对抗性文档，就能精准地误导 LLM 的思维链（CoT）推理过程并使其产生错误答案？

### 2. 方法论：AdversarialCoT 框架
AdversarialCoT 是一种基于反馈驱动的黑盒攻击框架，其核心思想是利用 LLM 的推理轨迹作为诊断信号，迭代优化对抗文档。
*   **观察与初始化**：
    *   **提取推理框架**：通过探测目标 LLM，提取其推理的粗粒度结构（通常分为：启动推理、证据检查与转换、证据聚合总结三个阶段）。
    *   **构建初始文档**：利用攻击者代理（Attacker Agent）生成一个嵌入了“伪造思维链”的文档，该文档在逻辑上引导至攻击者预设的错误答案。
*   **反馈驱动的迭代优化**：
    *   **相关性优化**：如果对抗文档未被检索器选中，则调整其表面内容和上下文线索，以提高其在检索阶段的排名。
    *   **说服力优化**：如果文档被检索但未能误导 LLM，攻击者会分析 LLM 的推理轨迹，识别模型拒绝虚假逻辑的关键点，随后调整对抗 CoT 的证据顺序、语气和逻辑转折，以更精准地利用模型的认知偏好。

### 3. 实验设计
*   **数据集**：MS-MARCO（Bing 搜索数据）、HotpotQA（多跳推理）、NQ（自然问题）。
*   **目标模型（Benchmark）**：DeepSeek-R1、Qwen3、GLM4.5。此外还对比了 Qwen2.5-7B-Instruct 和 Qwen-7B-R1-Distilled。
*   **对比方法**：
    *   **NA (Naive Attack)**：单句直接给出错误答案。
    *   **NPA (Naive Prompt Attack)**：注入简单的指令式恶意文档。
    *   **PHA (Prompt Hijacking Attack)**：提示词劫持攻击，要求模型忽略上下文。
    *   **PRAG (PoisonedRAG)**：现有的知识损坏投毒方法。
*   **评估指标**：检索成功率 ($ASR_r$)、给定检索成功下的误导成功率 ($ASR_g$)、总攻击成功率 ($ASR$)。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：攻击者代理使用了 KIMI-K2 模型，实验中设置最大迭代轮数为 3 轮，解码温度固定为 0.3。

### 5. 实验数量与充分性
*   **实验规模**：从每个数据集中随机采样 100 个查询进行评估。虽然样本量不算巨大，但考虑到每条查询涉及多轮黑盒交互和手动验证，该规模在此类研究中具有代表性。
*   **充分性**：
    *   涵盖了当前最先进的推理模型（如 DeepSeek-R1）。
    *   进行了消融实验（对比迭代与非迭代版本）。
    *   进行了跨模型泛化分析（用 A 模型生成的文档攻击 B 模型）。
    *   分析了迭代轮数对成功率的影响。
*   **客观性**：研究者对所有攻击结果进行了人工核对，以确保语义一致性，避免了仅依赖字符串匹配带来的评估偏差。

### 6. 主要结论与发现
*   **单文档攻击的有效性**：仅需一个对抗文档，AdversarialCoT 在 Qwen3 上的攻击成功率最高可达 80%，显著优于现有方法。
*   **推理优化的双刃剑**：CoT 虽然提升了模型性能，但也暴露了模型的推理风格和证据偏好，使其更容易被针对性的对抗 CoT 误导。
*   **迭代优化的必要性**：迭代优化显著提升了攻击成功率（相比非迭代版本提升约 20%-30%）。
*   **模型特异性**：不同模型存在独特的推理漏洞，跨模型攻击的成功率会有所下降，说明针对性攻击比通用攻击更具威胁。

### 7. 优点与亮点
*   **高隐蔽性**：挑战了“必须大量投毒”的传统认知，证明了单文档投毒的现实威胁。
*   **动态演化**：方法不依赖固定模板，而是通过与目标模型的“对话”来动态发现并利用漏洞。
*   **启发性**：攻击轨迹本身可以作为诊断工具，帮助开发者识别 LLM 推理逻辑中的薄弱环节。

### 8. 不足与局限
*   **成本风险**：作为黑盒攻击，频繁的迭代交互会产生较高的 API 调用成本。
*   **防御研究缺失**：论文重点在于揭示风险，虽然提到了对防御的启示，但未提出并验证具体的自动化防御算法。
*   **样本偏差**：每组 100 个样本的规模可能无法完全覆盖长尾场景中的所有推理模式。
*   **应用限制**：攻击效果依赖于攻击者代理（如 KIMI-K2）的逻辑生成能力，若攻击者模型较弱，攻击效力可能受限。

（完）
