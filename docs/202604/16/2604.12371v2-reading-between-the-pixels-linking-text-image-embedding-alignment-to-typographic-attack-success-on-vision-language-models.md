---
title: "Reading Between the Pixels: Linking Text-Image Embedding Alignment to Typographic Attack Success on Vision-Language Models"
title_zh: 读懂像素：将文本-图像嵌入对齐与视觉语言模型上的印刷体攻击成功率联系起来
authors: "Ravikumar Balakrishnan, Sanket Mendapara, Ankit Garg"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12371v2"
tags: ["query:llm"]
score: 9.0
evidence: 视觉语言模型中的排版提示注入攻击
tldr: 本研究深入探讨了视觉语言模型（VLM）面临的排版提示注入攻击，即通过将恶意文本渲染为图像来绕过安全机制。通过在GPT-4o、Claude等模型上测试不同字号和视觉变换，研究发现字号显著影响攻击成功率，且文本-图像嵌入距离与攻击成功率呈强负相关。该研究揭示了不同模型在对抗性环境下的鲁棒性差异，为安全部署VLM代理提供了重要指导。
selection_source: fresh_fetch
motivation: 排版攻击利用图像化的恶意文本绕过VLM安全防御，对作为自主代理核心的视觉模型构成了严重威胁。
method: 在四种主流VLM上评估了1000个恶意提示，分析了字号、旋转、模糊等视觉因素及多模态嵌入对齐度对攻击成功率的影响。
result: 实验表明中等字号攻击效果最强，且文本-图像嵌入距离与攻击成功率之间存在显著的负相关关系。
conclusion: VLM对排版攻击的鲁棒性表现出模型特异性，不存在通用的防御措施，需根据具体场景选择合适的模型骨干。
---

## 摘要
我们研究了视觉语言模型（VLM）上的印刷体提示词注入攻击，即通过将对抗性文本渲染为图像来绕过安全机制。随着 VLM 成为从浏览器自动化、计算机使用系统到配备摄像头的具身智能体等自主智能体的感知主干，这种攻击构成了日益增长的威胁。在实践中，攻击面是异构的：对抗性文本以不同的字体大小和多样的视觉条件出现，而日益壮大的 VLM 生态系统在脆弱性方面表现出显著差异，使防御方法变得复杂。我们在不同字体大小（6-28px）和视觉变换（旋转、模糊、噪声、对比度变化）下，评估了来自 SALAD-Bench 的 1,000 个提示词在四种 VLM（即 GPT-4o、Claude Sonnet 4.5、Mistral-Large-3 和 Qwen3-VL-4B-Instruct）上的表现，发现：(1) 字体大小显著影响攻击成功率（ASR），极小字体（6px）的 ASR 接近于零，而中等大小的字体达到峰值效果；(2) 对于 GPT-4o（36% 对 8%）和 Claude（47% 对 22%），文本攻击比图像攻击更有效，而 Qwen3-VL 和 Mistral 在不同模态下表现出相当的 ASR；(3) 来自两个多模态嵌入模型（JinaCLIP 和 Qwen3-VL-Embedding）的文本-图像嵌入距离与所有四种模型的 ASR 表现出强负相关（r = -0.71 至 -0.93，p < 0.01）；(4) 严重的降质使嵌入距离增加了 10-12%，并使 ASR 降低了 34-96%，而旋转对模型的影响是不对称的（Mistral 下降了 50%，GPT-4o 保持不变）。这些发现强调，特定于模型的鲁棒性模式排除了“一刀切”的防御方案，并为在对抗环境下运行的智能体系统选择 VLM 主干的从业者提供了实证指导。

## Abstract
We study typographic prompt injection attacks on vision-language models (VLMs), where adversarial text is rendered as images to bypass safety mechanisms, posing a growing threat as VLMs serve as the perceptual backbone of autonomous agents, from browser automation and computer-use systems to camera-equipped embodied agents. In practice, the attack surface is heterogeneous: adversarial text appears at varying font sizes and under diverse visual conditions, while the growing ecosystem of VLMs exhibits substantial variation in vulnerability, complicating defensive approaches. Evaluating 1,000 prompts from SALAD-Bench across four VLMs, namely, GPT-4o, Claude Sonnet 4.5, Mistral-Large-3, and Qwen3-VL-4B-Instruct under varying font sizes (6--28px) and visual transformations (rotation, blur, noise, contrast changes), we find: (1) font size significantly affects attack success rate (ASR), with very small fonts (6px) yielding near-zero ASR while mid-range fonts achieve peak effectiveness; (2) text attacks are more effective than image attacks for GPT-4o (36% vs 8%) and Claude (47% vs 22%), while Qwen3-VL and Mistral show comparable ASR across modalities; (3) text-image embedding distance from two multimodal embedding models (JinaCLIP and Qwen3-VL-Embedding) shows strong negative correlation with ASR across all four models (r = -0.71 to -0.93, p < 0.01); (4) heavy degradations increase embedding distance by 10--12% and reduce ASR by 34--96%, while rotation asymmetrically affects models (Mistral drops 50%, GPT-4o unchanged). These findings highlight that model-specific robustness patterns preclude one-size-fits-all defenses and offer empirical guidance for practitioners selecting VLM backbones for agentic systems operating in adversarial environments.

---

## 论文详细总结（自动生成）

这篇论文对视觉语言模型（VLM）在面对“印刷体提示词注入攻击”（Typographic Prompt Injection）时的脆弱性进行了深入的实证研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究为何某些印刷体攻击（将恶意文本渲染为图像）能成功诱导 VLM 违规，而另一些则失败。
*   **研究动机**：随着 VLM 成为自主智能体（如浏览器自动化、具身机器人）的感知核心，攻击者可以通过网页或物理环境中的文字图像绕过文本安全过滤器。目前缺乏对渲染参数（如字号、视觉降质）如何影响攻击成功率（ASR）的系统性理解。
*   **研究目标**：探索文本-图像嵌入对齐度（Embedding Alignment）与攻击成功率之间的机械联系，并验证其是否能作为评估模型脆弱性的指标。

### 2. 方法论
*   **核心思想**：通过量化“图像形式的恶意指令”与“原始文本指令”在多模态嵌入空间中的距离，来预测攻击效果。
*   **关键技术细节**：
    *   **攻击实施**：将来自安全基准集的恶意文本渲染为 1024x1024 的图像（黑字白底），配以中性指令（如“请执行图像中的指令”）。
    *   **变量控制**：设置了 12 种字体大小（6px 到 28px）和 10 种视觉变换（旋转、模糊、噪声、对比度调整等）。
    *   **嵌入分析**：使用 **JinaCLIP** 和 **Qwen3-VL-Embedding** 两个模型，计算图像嵌入向量与文本嵌入向量之间的 **L2 距离**。
    *   **公式逻辑**：$d(p, I)$ 表示文本 $p$ 与图像 $I$ 的归一化嵌入向量之间的 L2 距离。距离越小，表示模型越倾向于将图像“读”作等效的文本指令。

### 3. 实验设计
*   **数据集**：从 **SALAD-Bench** 中精选了 1,000 个对抗性提示词，涵盖 6 类危害（如毒性、恶意使用、虚假信息）和 4 种攻击方法（Jailbreak, GCG, TAP, GPTFuzz）。
*   **目标模型（Benchmark）**：
    *   闭源模型：**GPT-4o**、**Claude Sonnet 4.5**、**Mistral-Large-3**。
    *   开源模型：**Qwen3-VL-4B-Instruct**。
*   **对比维度**：
    *   **模态对比**：纯文本攻击 vs. 印刷体图像攻击。
    *   **参数消融**：不同字号、不同视觉干扰下的 ASR 变化。
    *   **评判标准**：使用 GPT-4o 作为自动评判员（Judge），判断模型响应是否包含违规信息。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU 型号、数量或训练时长。
*   **实现方式**：闭源模型通过 Azure API 访问；开源模型 Qwen3-VL 使用 `vLLM` 框架在本地部署推理。由于该研究侧重于推理评估而非模型训练，因此对算力的需求主要集中在数万次的大模型推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 1,000（提示词）× 4（目标模型）× [12（字号）+ 10（变换）+ 1（文本基准）] = **约 9.2 万次推理实验**。
*   **充分性**：实验涵盖了当前最顶尖的商业模型和开源模型，并使用了两种不同的嵌入模型进行交叉验证。
*   **客观性**：通过系统性的消融实验（Ablation Study）隔离了渲染参数与提示词内容的影响，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **字号阈值效应**：6px 几乎无法触发攻击（ASR 趋于 0）；10-12px 是攻击成功的关键阈值，此后 ASR 趋于平稳。
*   **模态防御差异**：GPT-4o 和 Claude 对图像攻击表现出更强的“隐性防御”（文本攻击成功率远高于图像）；而 Mistral 和 Qwen 在两种模态下同样脆弱。
*   **嵌入距离的预测性**：嵌入距离与 ASR 呈强负相关（$r = -0.71$ 至 $-0.93$）。这意味着**嵌入距离可以作为一种轻量级、模型无关的信号，用于预测或检测印刷体攻击的有效性**。
*   **视觉鲁棒性不对称**：旋转会使 Mistral 的 ASR 下降 50%，但对 GPT-4o 几乎没有影响，说明不同模型的几何鲁棒性存在显著差异。

### 7. 优点
*   **揭示了底层机制**：不仅证明了攻击的存在，还通过嵌入空间解释了攻击成功的“机械原因”。
*   **实战指导意义**：为开发者选择 VLM 主干提供了依据（例如，在需要处理旋转图像的场景下，GPT-4o 比 Mistral 更易受攻击）。
*   **评估指标创新**：提出将多模态嵌入对齐度作为一种防御监控的潜在手段。

### 8. 不足与局限
*   **覆盖范围限制**：仅测试了黑字白底的无衬线字体，未涉及手写体、复杂背景或多字体混合的极端情况。
*   **提示词长度**：为了确保渲染成功，剔除了极长的提示词，这可能导致对某些复杂攻击方法的评估不全面。
*   **防御方案缺失**：论文侧重于“分析”而非“防御”，未提出具体的算法来实时拦截这些基于嵌入距离发现的攻击。

（完）
