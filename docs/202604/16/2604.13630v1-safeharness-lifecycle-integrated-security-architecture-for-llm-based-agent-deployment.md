---
title: "SafeHarness: Lifecycle-Integrated Security Architecture for LLM-based Agent Deployment"
title_zh: SafeHarness：面向基于大语言模型（LLM）智能体部署的生命周期集成安全架构
authors: "Xixun Lin, Yang Liu, Yancheng Chen, Yongxuan Wu, Yucheng Ning, Yilong Liu, Nan Sun, Shun Zhang, Bin Chong, Chuan Zhou, Yanan Cao, Li Guo"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13630v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于LLM代理部署的安全架构
tldr: 本研究针对大语言模型（LLM）智能体执行框架面临的安全威胁，提出了SafeHarness架构。该架构将防御机制深度集成于智能体生命周期的四个关键阶段：输入处理、决策制定、动作执行和状态更新，解决了现有安全方案与框架内部状态脱节的问题。实验表明，SafeHarness在保持任务效能的同时，显著降低了不安全行为率和攻击成功率，为智能体部署提供了全生命周期的安全保障。
selection_source: fresh_fetch
motivation: 现有的LLM安全防御机制与执行框架存在结构性失配，无法感知框架内部状态且缺乏跨阶段协同，使其难以应对针对智能体全生命周期的级联攻击。
method: 提出了由对抗性上下文过滤、分层因果验证、权限分离工具控制和自适应降级安全回滚组成的四层防御架构，并通过跨层机制实现动态安全增强。
result: "实验显示，SafeHarness相比未受保护的基准方案，平均降低了约38%的不安全行为率（UBR）和42%的攻击成功率（ASR）。"
conclusion: 将安全防御直接织入智能体生命周期的跨层协作架构，是提升LLM智能体在复杂攻击环境下鲁棒性与安全性的有效途径。
---

## 摘要
大语言模型（LLM）智能体的性能关键取决于执行框架（harness），即负责编排工具使用、上下文管理和状态持久化的系统层。然而，这种架构上的核心地位也使执行框架成为了高价值的攻击面：执行框架层面的单一漏洞利用可能会级联影响整个执行流水线。我们观察到，现有的安全方法存在结构性失配，导致其无法感知执行框架的内部状态，且难以在智能体运行的不同阶段进行协同。在本文中，我们提出了 SafeHarness，这是一种将四层防御机制直接织入智能体生命周期的安全架构，旨在解决上述重大局限性：输入处理阶段的对抗性上下文过滤、决策阶段的分层因果验证、动作执行阶段的权限分离工具控制，以及状态更新阶段的带自适应降级的安全回滚。所提出的跨层机制将这些防御层联系在一起，每当检测到持续异常时，便会提升验证严格度、触发回滚并收紧工具权限。我们在多种执行框架配置下的基准数据集上对 SafeHarness 进行了评估，并在涵盖六类威胁的五种攻击场景下，将其与四个安全基准进行了对比。与无保护的基准相比，SafeHarness 的不安全行为率（UBR）平均降低了约 38%，攻击成功率（ASR）平均降低了 42%，在保持核心任务效用的同时，显著降低了不安全行为率和攻击成功率。

## Abstract
The performance of large language model (LLM) agents depends critically on the execution harness, the system layer that orchestrates tool use, context management, and state persistence. Yet this same architectural centrality makes the harness a high-value attack surface: a single compromise at the harness level can cascade through the entire execution pipeline. We observe that existing security approaches suffer from structural mismatch, leaving them blind to harness-internal state and unable to coordinate across the different phases of agent operation. In this paper, we introduce \safeharness{}, a security architecture in which four proposed defense layers are woven directly into the agent lifecycle to address above significant limitations: adversarial context filtering at input processing, tiered causal verification at decision making, privilege-separated tool control at action execution, and safe rollback with adaptive degradation at state update. The proposed cross-layer mechanisms tie these layers together, escalating verification rigor, triggering rollbacks, and tightening tool privileges whenever sustained anomalies are detected. We evaluate \safeharness{} on benchmark datasets across diverse harness configurations, comparing against four security baselines under five attack scenarios spanning six threat categories. Compared to the unprotected baseline, \safeharness{} achieves an average reduction of approximately 38\% in UBR and 42\% in ASR, substantially lowering both the unsafe behavior rate and the attack success rate while preserving core task utility.

---

## 论文详细总结（自动生成）

### SafeHarness：面向 LLM 智能体部署的生命周期集成安全架构总结

#### 1. 核心问题与研究动机
随着 LLM 智能体（Agent）在现实场景中的广泛应用，其**执行框架（Harness）**——即负责协调工具调用、上下文管理和状态持久化的系统层——成为了高价值的攻击目标。
*   **研究动机**：现有的安全防御（如 NeMo Guardrails 或 Llama Guard）存在三大结构性缺陷：
    1.  **上下文盲区**：防御机制位于框架外部，无法感知框架内部状态（如被污染的工具输出）。
    2.  **层间孤立**：各阶段的安全检查独立运行，缺乏协同响应。
    3.  **缺乏韧性**：仅提供二元的“通过/拦截”决策，一旦被突破则全盘崩溃，缺乏降级和恢复机制。
*   **核心问题**：如何构建一个与智能体生命周期深度集成的安全架构，以应对针对执行框架的级联攻击？

#### 2. 方法论：SafeHarness 架构
SafeHarness 将防御机制织入智能体运行的四个阶段，形成闭环保护：
*   **L1: INFORM（输入处理层）**：
    *   对外部内容进行三阶段过滤：结构化清理（去除隐藏字符）、模式匹配（正则检测）和语义过滤（LLM 辅助提取事实）。
    *   **来源追踪（Provenance Tagging）**：为内容打上信任标签，供后续层参考。
*   **L2: VERIFY（决策制定层）**：
    *   采用三级验证级联：规则合规性检查 -> 上下文评判（Judge 模型） -> **因果诊断**（分析动作是由用户意图还是注入指令引起的）。
*   **L3: CONSTRAIN（动作执行层）**：
    *   **最小权限原则**：将工具分为 5 个风险等级。
    *   **能力令牌（Capability Tokens）**：限制工具调用的时间、次数，并使用 HMAC 验证工具描述的完整性。
*   **L4: CORRECT（状态更新层）**：
    *   **状态快照与回滚**：定期保存环境和内存快照，检测到攻击时自动回滚。
    *   **自适应降级**：根据攻击严重程度降低工具权限等级，并在安全运行一段时间后逐步恢复。
*   **跨层协同机制**：引入**熵监控器（Entropy Monitor）**，通过计算滑动窗口内的违规率，动态触发 L2 的验证升级或 L4 的权限收紧。

#### 3. 实验设计
*   **数据集/场景**：使用 **Agent-SafetyBench**，包含 2000 个安全关键任务，涵盖数据泄露、越权操作等 8 类风险。
*   **智能体模型**：DeepSeek-V3.2（包括 Chat 和 Reasoner 两种模式）。
*   **执行框架配置**：ReAct（迭代推理）、Multi-Agent（多智能体协作）、Self-Evolving（带技能记忆的自进化）。
*   **攻击场景（5 种）**：上下文投毒（A1）、间接提示注入（A2）、工具篡改（A3）、内存注入（A4）及复合攻击（A5）。
*   **对比基准**：无保护（Unprotected）、系统提示词防御（System-Prompt）、接口级护栏（Guardrail）、流水线级审计（LlamaFirewall）。

#### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 训练时长或数量。
*   **实现细节**：实验主要基于 API 调用（DeepSeek 系列模型作为智能体，GPT-4o-mini 作为安全评判 Judge）。由于 SafeHarness 属于推理侧的架构设计，不涉及大规模模型预训练，其开销主要体现在推理时的额外 Token 消耗。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 种框架、2 种模型模式、5 种攻击类型下进行了详尽测试。
*   **充分性**：实验设计较为全面，不仅评估了安全指标（UBR 不安全行为率、ASR 攻击成功率），还评估了效用指标（TCR 任务完成率、UA 攻击下效用），证明了防御并未以牺牲核心功能为代价。
*   **客观性**：采用了“LLM-as-a-Judge”模式，并确保评判模型与智能体模型分离，减少了自我评估偏差。

#### 6. 主要结论与发现
*   **显著提升安全性**：相比无保护基准，SafeHarness 将不安全行为率（UBR）平均降低了 **38%**，攻击成功率（ASR）降低了 **42%**。
*   **跨层协同的必要性**：在复合攻击（A5）场景下，单一层级的防御极易失效，而 SafeHarness 的反馈机制能有效拦截级联威胁。
*   **效用保持**：在 ReAct 等框架下，任务完成率（TCR）的波动极小（约 1-2%），证明了该架构在安全与效率之间取得了良好平衡。

#### 7. 优点与亮点
*   **生命周期集成**：首个将防御深度嵌入智能体执行全过程（输入-决策-执行-更新）的架构。
*   **动态韧性**：引入了“自适应降级与恢复”概念，使智能体在受攻击时能“带伤作战”而非直接宕机。
*   **因果诊断**：L2 层通过因果分析区分恶意注入与正常指令，降低了误报率。

#### 8. 不足与局限
*   **推理延迟与成本**：多级验证（尤其是 L2 的 LLM 诊断）会增加每一步执行的延迟和 API 调用成本。
*   **模拟环境限制**：实验在模拟环境中进行，尚未在真实生产环境（如真实的云端 Shell 或数据库）中验证其复杂交互下的稳定性。
*   **评判模型偏差**：虽然使用了独立的 Judge 模型，但 LLM 评判器本身可能存在系统性盲点，无法完全替代人工安全审计。
*   **对抗演进**：针对 SafeHarness 这种多层防御的特定绕过策略（Adaptive Adversaries）仍需进一步研究。

（完）
