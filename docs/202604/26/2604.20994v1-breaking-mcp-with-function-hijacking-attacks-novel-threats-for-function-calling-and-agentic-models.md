---
title: "Breaking MCP with Function Hijacking Attacks: Novel Threats for Function Calling and Agentic Models"
title_zh: 通过函数劫持攻击破解 MCP：针对函数调用与智能体模型的新型威胁
authors: "Yannis Belkhiter, Giulio Zizzo, Sergio Maffeis, Seshu Tirupathi, John D. Kelleher"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20994v1"
tags: ["query:llm"]
score: 8.0
evidence: 函数调用和智能体模型中的越狱漏洞
tldr: "随着智能体AI的发展，函数调用LLM面临新的安全威胁。本文提出一种新型的“函数劫持攻击”（FHA），通过操纵工具选择过程，强制模型调用攻击者指定的特定函数。与以往依赖语义偏好的攻击不同，FHA对上下文语义不敏感且对函数集具有鲁棒性，甚至能训练出通用的对抗性函数。在5种模型上的实验显示其攻击成功率高达70%-100%，揭示了智能体系统亟需加强安全防护。"
selection_source: fresh_fetch
motivation: 现有的LLM攻击多关注提示词操纵，而智能体模型扩展的函数调用接口引入了尚未被充分探索的工具选择漏洞。
method: 提出函数劫持攻击（FHA），通过构造对抗性函数描述或有效载荷，在不依赖上下文语义的情况下强制模型选择特定目标函数。
result: "在5种主流模型（包括指令微调和推理模型）及BFCL数据集上的实验表明，FHA的攻击成功率达到了70%至100%。"
conclusion: 研究证明了函数调用接口存在严重的安全隐患，强调了为智能体系统构建强有力安全防御模块的紧迫性。
---

## 摘要
智能体 AI 的增长使得支持函数调用的大语言模型（LLMs）备受关注，这些模型旨在通过调用外部函数来扩展 AI 驱动系统的能力。注入和越狱攻击已被广泛研究，以展示 LLMs 在面对用户提示词操纵时的脆弱性。智能体模型扩展的能力通过其函数调用接口引入了进一步的漏洞。最近关于 LLM 安全的研究表明，函数调用可能被滥用，导致数据篡改和窃取，引发如死循环等破坏性行为，或导致 LLMs 以越狱攻击的方式生成有害内容。本文提出了一种新型的函数劫持攻击（FHA），该攻击通过操纵智能体模型的工具选择过程，强制调用攻击者指定的特定函数。虽然现有的攻击侧重于模型在函数调用任务中的语义偏好，但我们证明了 FHA 在很大程度上与上下文语义无关，且对函数集具有鲁棒性，使其适用于各种领域。我们进一步证明，可以通过训练 FHA 来生成通用对抗性函数，使得单个受攻击的函数能够在多个查询和有效载荷配置中劫持工具选择。我们在 5 种不同的模型（包括指令微调版和推理版）上进行了实验，在公认的 BFCL 数据集上达到了 70% 到 100% 的攻击成功率（ASR）。我们的研究结果进一步证明了为智能体系统建立强大护栏和安全模块的必要性。

## Abstract
The growth of agentic AI has drawn significant attention to function calling Large Language Models (LLMs), which are designed to extend the capabilities of AI-powered system by invoking external functions. Injection and jailbreaking attacks have been extensively explored to showcase the vulnerabilities of LLMs to user prompt manipulation. The expanded capabilities of agentic models introduce further vulnerabilities via their function calling interface. Recent work in LLM security showed that function calling can be abused, leading to data tampering and theft, causing disruptive behavior such as endless loops, or causing LLMs to produce harmful content in the style of jailbreaking attacks. This paper introduces a novel function hijacking attack (FHA) that manipulates the tool selection process of agentic models to force the invocation of a specific, attacker-chosen function. While existing attacks focus on semantic preference of the model for function-calling tasks, we show that FHA is largely agnostic to the context semantics and robust to the function sets, making it applicable across diverse domains. We further demonstrate that FHA can be trained to produce universal adversarial functions, enabling a single attacked function to hijack tool selection across multiple queries and payload configurations. We conducted experiments on 5 different models, including instructed and reasoning variants, reaching 70% to 100% ASR over the established BFCL dataset. Our findings further demonstrate the need for strong guardrails and security modules for agentic systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Breaking MCP with Function Hijacking Attacks: Novel Threats for Function Calling and Agentic Models》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着智能体 AI（Agentic AI）的兴起，大语言模型（LLM）通过函数调用（Function Calling, FC）与外部环境交互。现有的安全研究主要集中在提示词注入或越狱攻击，而忽略了对“工具选择过程”本身的操控。
*   **核心问题**：攻击者能否通过操纵工具的元数据（如描述信息），强制模型在面对任何用户请求时都选择特定的、由攻击者指定的函数？
*   **背景**：模型上下文协议（MCP）已成为标准化智能体通信的流行框架，这使得函数调用接口成为了一个新的、且极具威胁的攻击面。

### 2. 方法论
*   **核心思想**：提出**函数劫持攻击（FHA）**。该攻击不依赖于用户提示词的操纵，而是通过在目标函数的“描述”（Description）字段中植入对抗性 Token，改变模型在工具选择时的概率分布。
*   **关键技术细节**：
    *   **算法基础**：借鉴了 GCG（贪婪坐标梯度）算法，将其从传统的越狱场景适配到函数调用场景。
    *   **优化目标**：最小化负对数似然损失函数，强制模型生成目标函数的名称（f_name_target）。
    *   **自回归利用**：利用 LLM 的自回归特性，一旦模型被诱导输出了错误的函数名，它通常会继续生成看似合理但实际指向劫持目标的参数。
    *   **推理抑制**：针对推理模型（如 Qwen-3），FHA 被设计为强制模型跳过 `<think>` 思考过程，直接输出劫持后的工具调用。
*   **通用 FHA 算法**：通过在多个查询（Query）或多个有效载荷（Payload）配置上累积梯度，训练出具有“通用性”的对抗后缀，使其能跨越不同意图生效。

### 3. 实验设计
*   **数据集/Benchmark**：主要使用 **Berkeley Function Calling Leaderboard (BFCL) v3_multiple**，包含 200 个样本，涵盖数学、API 调用等多种领域。
*   **测试场景**：
    1.  **直接劫持**：在固定函数集中强制选择特定函数。
    2.  **鲁棒性测试**：改变函数在列表中的位置、增加干扰函数的数量。
    3.  **通用性测试**：使用 GPT-4o-mini 生成的 **FunSecBench**（包含语义重述、参数变化、多意图变化）。
*   **对比方法**：
    *   **标准推理**（无攻击基准）。
    *   **函数注入（Function Injection）**：使用 Llama-3-70B 生成最优描述来诱导模型。
    *   **MPMA（偏好操纵攻击）**：现有的基于语义偏好的攻击方法。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100-80GB GPU**。
*   **训练参数**：Batch Size 为 128，种子设为 42。
*   **时长与规模**：指令模型通常在 250 个 Epoch 内收敛，推理模型需要约 500 个 Epoch。文中未给出总的训练小时数，但强调了在 A100 上运行的可行性。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种主流模型（Llama-3.2-3B, Mistral-7B, Qwen-3-1.7B/8B/14B）进行了详尽测试。
*   **充分性**：
    *   涵盖了指令微调（Instruct）和推理（Reasoning）两种模型类型。
    *   进行了消融实验，探讨了对抗后缀长度（10, 35, 60 tokens）对成功率的影响。
    *   通过 50 次随机扰动实验验证了对“噪声函数”的鲁棒性。
*   **客观性**：对比了最新的 MPMA 攻击，并指出了 FHA 在多样化、非语义相关场景下的优越性，实验设计较为严谨。

### 6. 主要结论与发现
*   **极高的成功率**：FHA 在 BFCL 数据集上达到了 **70% 至 100%** 的攻击成功率（ASR）。
*   **语义无关性**：与以往依赖语义相似度的攻击不同，FHA 即使在目标函数与用户请求完全无关时依然有效。
*   **通用性强**：单个对抗后缀可以同时劫持多个不同的用户查询，甚至在函数列表顺序被打乱时依然保持效力。
*   **推理模型脆弱性**：推理模型虽然有思考过程，但 FHA 可以通过优化目标直接“关闭”或“绕过”思考环节。

### 7. 优点
*   **隐蔽性极佳**：攻击仅发生在函数描述中，不改变函数名或参数定义，人类开发者或简单的自动化脚本很难察觉。
*   **离线攻击能力**：攻击者可以预先训练好对抗后缀并植入 MCP 服务器，无需实时接触用户提示词。
*   **跨模型迁移**：展示了在不同规模和类型的模型上均具有强大的破坏力。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 1B 到 14B 参数的模型上，对于超大规模模型（如 GPT-4o 或 Claude 3.5）的有效性尚待验证。
*   **上下文长度风险**：虽然测试了增加函数数量的情况，但在包含成百上千个工具的极长上下文（Long Context）下的表现未被探讨。
*   **防御手段缺失**：论文重点在于揭示威胁，虽然提到了需要“强有力的护栏”，但并未提出具体的、经过验证的防御算法。

（完）
