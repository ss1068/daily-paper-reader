---
title: Cross-Lingual Jailbreak Detection via Semantic Codebooks
title_zh: 基于语义码本的跨语言越狱检测
authors: "Shirin Alanova, Bogdan Minko, Sabrina Sadiekh, Evgeniy Kokuykin"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25716v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 提出了一种无需训练的外部护栏，利用语义相似性检测跨语言越狱攻击。
tldr: 针对大语言模型在多语言场景下的安全漏洞（如通过翻译恶意提示词绕过防御），本文提出一种基于语义码本的跨语言越狱检测方法。该方法通过将多语言查询嵌入与预定义的英语越狱提示词码本进行语义相似度对比，作为黑盒模型的外部护栏。实验表明，该方法在标准越狱模板上表现优异，但在处理异构不安全内容时存在局限性。
selection_source: fresh_fetch
motivation: 现有的LLM安全机制过度依赖英语，导致攻击者可以通过将恶意提示词翻译成其他语言来轻松实现越狱。
method: 提出一种无需训练的外部防御机制，利用嵌入模型计算多语言查询与固定英语越狱码本之间的语义相似度。
result: 在标准越狱模板上实现了高达0.99的AUC，但在行为多样且异构的不安全基准测试中，检测性能显著下降至0.60-0.70。
conclusion: 语义相似度在检测结构化越狱模板方面非常有效，但在应对分布偏移和多样化不安全行为时仍面临挑战。
---

## 摘要
大语言模型（LLMs）的安全机制仍主要以英语为中心，这在多语言部署中造成了系统性漏洞。先前的工作表明，将恶意提示词翻译成其他语言可以显著提高越狱成功率，暴露了结构性的跨语言安全差距。我们研究了是否可以通过与语言无关的语义相似性来缓解此类攻击，而无需重新训练或进行特定语言的适配。我们的方法将多语言查询嵌入与固定的英语越狱提示词码本进行比较，作为黑盒 LLM 的免训练外部护栏。我们在四种语言、两个翻译流水线、四个安全基准、三个嵌入模型和三个目标 LLM（Qwen、Llama、GPT-3.5）上进行了系统评估。我们的结果揭示了跨语言迁移的两种不同机制。在包含规范越狱模板的精选基准上，语义相似性在不同语言间具有可靠的泛化能力，实现了近乎完美的区分度（AUC 高达 0.99），并在严格的低误报率约束下大幅降低了绝对攻击成功率。然而，在分布偏移下——即在行为多样且异构的不安全基准上——区分度显著下降（AUC 约为 0.60-0.70），且在安全关键的低误报率（low-FPR）机制下，所有嵌入模型的召回率均有所下降。

## Abstract
Safety mechanisms for large language models (LLMs) remain predominantly English-centric, creating systematic vulnerabilities in multilingual deployment. Prior work shows that translating malicious prompts into other languages can substantially increase jailbreak success rates, exposing a structural cross-lingual security gap. We investigate whether such attacks can be mitigated through language-agnostic semantic similarity without retraining or language-specific adaptation. Our approach compares multilingual query embeddings against a fixed English codebook of jailbreak prompts, operating as a training-free external guardrail for black-box LLMs. We conduct a systematic evaluation across four languages, two translation pipelines, four safety benchmarks, three embedding models, and three target LLMs (Qwen, Llama, GPT-3.5). Our results reveal two distinct regimes of cross-lingual transfer. On curated benchmarks containing canonical jailbreak templates, semantic similarity generalizes reliably across languages, achieving near-perfect separability (AUC up to 0.99) and substantial reductions in absolute attack success rates under strict low-false-positive constraints. However, under distribution shift - on behaviorally diverse and heterogeneous unsafe benchmarks - separability degrades markedly (AUC $\approx$ 0.60-0.70), and recall in the security-critical low-FPR regime drops across all embedding models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cross-Lingual Jailbreak Detection via Semantic Codebooks》（基于语义码本的跨语言越狱检测）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）的安全对齐目前高度依赖英语，导致非英语语言成为安全防御的“盲区”。攻击者通过将恶意提示词翻译成其他语言（如中文、阿拉伯语等），可以轻易绕过现有的安全过滤器。
*   **研究动机**：现有的多语言防御方法通常需要重新训练模型、进行特定语言的微调或依赖昂贵的实时翻译。本文旨在探索一种**无需训练（Training-free）**、**语言无关（Language-agnostic）**且适用于**黑盒模型**的轻量级外部护栏机制。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用多语言嵌入空间中恶意意图的语义一致性。即使语言不同，恶意攻击在语义空间中往往聚集在相似区域。
*   **技术流程**：
    1.  **码本构建（Codebook Construction）**：从现有的安全数据集中筛选并清洗出 13,811 条唯一的英语越狱提示词，作为“恶意意图”的参考基准。
    2.  **多语言嵌入**：使用预训练的多语言嵌入模型（如 BGE-M3）将码本中的英语提示词预先转化为向量。
    3.  **相似度检测**：对于输入的任何语言查询 $x$，计算其嵌入向量 $f(x)$ 与码本中所有向量 $c \in C$ 的最大余弦相似度：$s(x) = \max_{c \in C} f(x)^\top c$。
    4.  **决策规则**：若 $s(x)$ 超过预设阈值 $\tau$，则判定为越狱攻击并拦截。阈值 $\tau$ 是在验证集上基于严格的误报率（FPR ≤ 1%）约束选定的。

### 3. 实验设计
*   **数据集（Benchmarks）**：使用了四个具有代表性的安全基准，分为两类：
    *   **规范模板类**：`jayavibhav` 和 `xTRam1`（包含大量典型的越狱模板）。
    *   **异构多样类**：`JailbreakBench` 和 `Aegis`（包含更复杂的行为多样性和非模板化不安全内容）。
*   **实验变量**：
    *   **语言**：英语、俄语、中文、阿拉伯语。
    *   **翻译流水线**：Google Translate（商业）和 M2M100（开源）。
    *   **嵌入模型**：BGE-M3、multilingual-e5-large、jina-embeddings-v3。
    *   **目标 LLM**：Qwen3-4B、Llama-3.2-3B-Instruct、GPT-3.5-turbo。
*   **对比指标**：ROC-AUC、低误报率下的召回率（TPR at FPR ≤ 1%）、攻击成功率（ASR）的绝对降低值。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该方法属于“免训练”外部护栏，其核心开销在于：(1) 码本的一次性嵌入计算；(2) 推理时的向量相似度检索。相比于模型微调，其算力需求极低，主要依赖于向量数据库的检索效率。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 4 种语言 × 2 种翻译方式 × 4 个基准数据集 × 3 种嵌入模型 × 3 个目标 LLM，并进行了码本大小的消融实验。
*   **充分性评价**：实验设计非常**充分且客观**。作者不仅报告了理想状态下的 AUC，还特别强调了在实际生产环境中最关键的“低误报率（FPR ≤ 1%）”下的表现，这比单纯的准确率更具参考价值。

### 6. 主要结论与发现
*   **双重机制现象**：
    *   在**规范模板**基准上，该方法表现极佳，AUC 高达 0.99，在 FPR ≤ 1% 时能拦截 78%-92% 的攻击。
    *   在**异构多样**基准上，性能大幅下降，AUC 降至 0.60-0.70，低误报率下的召回率甚至跌至个位数。
*   **嵌入模型的影响**：BGE-M3 在跨语言对齐和安全性检测方面整体优于其他模型。
*   **端到端效果**：作为外部预过滤器，该方法能显著降低目标 LLM 的绝对攻击成功率，尤其是在处理结构化越狱模板时。

### 7. 优点（亮点）
*   **部署极其简便**：无需对目标 LLM 进行任何修改或微调，非常适合保护闭源 API。
*   **跨语言通用性**：仅需一个英语码本即可防御多种语言的攻击，利用了嵌入模型的跨语言对齐特性。
*   **低成本高效率**：相比于调用另一个 LLM 作为审核器（如 Llama-Guard），向量检索的速度更快、成本更低。

### 8. 不足与局限
*   **分布偏移敏感**：对于非模板化的、隐蔽的或新颖的攻击方式（分布外数据），检测能力较弱。
*   **静态码本限制**：码本是固定的，无法自动适应不断演变的攻击策略，需要定期手动更新。
*   **依赖嵌入质量**：防御效果高度受限于嵌入模型的跨语言对齐能力，如果模型在某些低资源语言上对齐较差，防御将失效。
*   **误报风险**：在追求高召回率时，可能会误伤正常的安全查询，影响用户体验。

（完）
