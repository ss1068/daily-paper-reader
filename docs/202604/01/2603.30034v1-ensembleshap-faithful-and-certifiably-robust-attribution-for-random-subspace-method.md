---
title: "EnsembleSHAP: Faithful and Certifiably Robust Attribution for Random Subspace Method"
title_zh: EnsembleSHAP：针对随机子空间方法的忠实且可证明鲁棒的归因
authors: "Yanting Wang, Jinyuan Jia"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.30034v1"
tags: ["query:llm"]
score: 9.0
evidence: 利用随机子空间方法构建防御越狱攻击的鲁棒对齐大模型
tldr: 针对随机子空间方法（RSM）在安全防御中缺乏有效解释的问题，本文提出EnsembleSHAP。该方法利用RSM的计算特性，实现了高效且本质忠实的特征归因。它不仅满足局部准确性，还首次为特征归因提供了可证明的抗攻击鲁棒性，能有效识别并防御后门、对抗及越狱攻击，填补了该领域安全解释的空白。
selection_source: fresh_fetch
motivation: 现有的特征归因方法（如SHAP和LIME）在应用于随机子空间方法时，面临计算效率低下且缺乏安全与鲁棒性保障的挑战。
method: 提出EnsembleSHAP框架，通过复用随机子空间方法的计算副产品，构建一种兼具计算效率、局部准确性和可证明鲁棒性的特征归因算法。
result: 实验表明，该方法在多种安全场景（如后门、对抗和越狱攻击）下均能提供有效且稳健的解释，并能抵御针对解释的隐私攻击。
conclusion: EnsembleSHAP是首个为随机子空间方法建立可证明鲁棒性保障的解释方案，在提升模型透明度的同时确保了归因结果的安全性。
---

## 摘要
随机子空间方法在安全领域具有广泛的应用，例如提供针对对抗攻击和后门攻击的可证明防御，以及构建针对越狱攻击的鲁棒对齐大语言模型（LLM）。然而，对随机子空间方法的解释性尚缺乏充分探索。现有的最先进特征归因方法（如 Shapley 值和 LIME）在应用于随机子空间方法时，在计算上并不可行，且缺乏安全保证。在这项工作中，我们提出了 EnsembleSHAP，这是一种针对随机子空间方法的本质忠实且安全的特征归因方法，它复用了其计算副产物。具体而言，我们的特征归因方法：1) 计算效率高；2) 保持了有效特征归因的核心属性（如局部准确性）；3) 针对特征归因方法的隐私保护攻击提供了有保证的防护。据我们所知，这是首个针对解释保持攻击建立可证明鲁棒性的工作。我们还针对不同的经验性攻击（包括后门攻击、对抗攻击和越狱攻击），对我们解释方法的有效性进行了全面评估。代码开源于 https://github.com/Wang-Yanting/EnsembleSHAP。警告：本文档可能包含被视为有害的内容。

## Abstract
Random subspace method has wide security applications such as providing certified defenses against adversarial and backdoor attacks, and building robustly aligned LLM against jailbreaking attacks. However, the explanation of random subspace method lacks sufficient exploration. Existing state-of-the-art feature attribution methods, such as Shapley value and LIME, are computationally impractical and lacks security guarantee when applied to random subspace method. In this work, we propose EnsembleSHAP, an intrinsically faithful and secure feature attribution for random subspace method that reuses its computational byproducts. Specifically, our feature attribution method is 1) computationally efficient, 2) maintains essential properties of effective feature attribution (such as local accuracy), and 3) offers guaranteed protection against privacy-preserving attacks on feature attribution methods. To the best of our knowledge, this is the first work to establish provable robustness against explanation-preserving attacks. We also perform comprehensive evaluations for our explanation's effectiveness when faced with different empirical attacks, including backdoor attacks, adversarial attacks, and jailbreak attacks. The code is at https://github.com/Wang-Yanting/EnsembleSHAP. WARNING: This document may include content that could be considered harmful.

---

## 论文详细总结（自动生成）

### 论文总结：EnsembleSHAP —— 针对随机子空间方法的忠实且可证明鲁棒的归因

#### 1. 论文的核心问题与整体含义
*   **研究背景**：随机子空间方法（Random Subspace Method, RSM）通过对特征进行随机采样并集成多个基模型的预测结果，已成为对抗攻击、后门攻击及大模型（LLM）越狱攻击的重要防御手段。
*   **核心问题**：尽管 RSM 在安全领域表现优异，但其决策过程缺乏可解释性。现有的黑盒特征归因方法（如 SHAP、LIME）直接应用于 RSM 时面临两大困境：
    1.  **计算开销巨大**：RSM 本身需要多次采样，归因方法若再进行扰动采样，会导致计算量呈平方级增长（$M \times N$ 次查询）。
    2.  **缺乏安全保障**：现有方法无法抵御“解释保持攻击”（即攻击者修改输入触发误判，但解释结果却显示正常），在安全敏感场景下不可靠。
*   **研究动机**：开发一种既能复用 RSM 计算副产物以提高效率，又能提供理论安全保证的特征归因方法。

#### 2. 论文提出的方法论
*   **核心思想**：提出 **EnsembleSHAP**。其核心在于直接利用 RSM 在推理阶段已经生成的 $N$ 个随机子集的预测结果，无需额外调用模型。
*   **关键技术细节**：
    *   **重要性分数定义**：将特征 $x_i$ 的重要性定义为：在一个随机采样的特征子集中，包含该特征且该子集预测结果为目标标签的概率。
    *   **频率归一化**：为了消除小样本采样下特征出现频率不均的影响，引入了基于特征出现次数的归一化项，确保评估的公平性。
    *   **理论性质**：证明了 EnsembleSHAP 满足**局部准确性**（所有特征分数之和等于预测概率）、**对称性**，并且在重要性排序上与传统的 **Shapley 值保持一致**。
    *   **可证明鲁棒性**：利用统计学中的 Clopper-Pearson 置信区间，推导出在给定扰动范围 $T$ 下，归因结果中能够“保底”检测出的对抗特征数量（Certified Detection Size）。

#### 3. 实验设计
*   **应用场景与数据集**：
    1.  **文本分类**：SST-2、IMDb、AGNews（针对后门攻击 BadNets 和对抗攻击 TextFooler）。
    2.  **LLM 越狱防御**：Harmful Behaviors 数据集（针对 GCG、AutoDAN、DAN 等越狱攻击）。
    3.  **图像领域**：CIFAR-10、ImageNette、ImageNet-100（针对对抗补丁攻击）。
*   **对比方法（Benchmarks）**：
    *   经典方法：Shapley Value、LIME。
    *   最新方法：ICL（基于大模型上下文学习的解释器）、Beta-Shapley、ContextCite、Kernel SHAP 等。
*   **评估指标**：Faithfulness（忠实度，即删除重要特征后标签翻转率）、关键词预测的精确率/召回率/F1分数、可证明检测率。

#### 4. 资源与算力
*   **模型使用**：实验涉及 BERT（文本分类微调）、Vicuna-7B（越狱防御基模型）、DINO（图像骨干网络）。
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**。
*   **效率表现**：作者强调了该方法的极低开销。在推理阶段，特征归因仅需约 **0.03 秒**，可证明检测计算约 **0.2-0.5 秒**。相比之下，传统 SHAP 方法在 RSM 上的计算成本通常是不可接受的。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了文本、图像、大语言模型三个主流领域。
    *   针对三种不同类型的安全威胁（后门、对抗、越狱）进行了深入测试。
    *   进行了详尽的超参数消融实验，包括采样数 $N$、特征丢弃率 $\rho$、置信度 $\beta$ 等对结果的影响。
*   **公平性与客观性**：实验对比了多种 SOTA 基准，并使用了标准的忠实度指标。通过理论证明与经验评估相结合的方式，验证了方法在不同扰动预算下的稳健性，实验设计较为全面且客观。

#### 6. 论文的主要结论与发现
*   **高效性**：EnsembleSHAP 通过复用计算副产物，实现了近乎零成本的特征归因。
*   **忠实度高**：在删除重要特征导致标签翻转的测试中，EnsembleSHAP 显著优于 LIME 和作用于基模型的 Shapley 值。
*   **安全检测能力**：该方法能有效识别导致模型误判的对抗词汇或后门触发器。即使在强力攻击下，也能通过理论保证检测到一定比例的恶意修改。
*   **跨领域适用性**：该方法不仅适用于文本，也能有效解释图像分类中的补丁攻击。

#### 7. 优点
*   **理论创新**：首次为特征归因建立了针对“解释保持攻击”的可证明鲁棒性框架。
*   **实用性极强**：无需重新训练模型，无需额外推理开销，非常适合部署在实时安全防御系统中。
*   **数学严谨**：成功将复杂的 Shapley 值计算简化为 RSM 框架下的概率统计问题，并保留了核心公理性质。

#### 8. 不足与局限
*   **架构依赖**：该方法高度依赖于“随机子空间”这一特定集成架构，对于非集成模型（如普通的单体 CNN 或 Transformer）无法直接应用。
*   **解释粒度**：在处理超长文本或高分辨率图像时，若特征划分过细，采样稀疏性可能会影响归因的稳定性（虽然增加 $N$ 可缓解，但会增加 RSM 本身的负担）。
*   **应用限制**：主要关注安全场景下的特征归因，对于更广泛的解释性需求（如模型内部逻辑推理）涉及较少。

（完）
