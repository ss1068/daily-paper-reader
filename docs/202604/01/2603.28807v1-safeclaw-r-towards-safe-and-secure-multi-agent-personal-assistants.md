---
title: "SafeClaw-R: Towards Safe and Secure Multi-Agent Personal Assistants"
title_zh: SafeClaw-R：迈向安全且受保障的多智能体个人助手
authors: "Haoyu Wang, Zibo Xiao, Yedi Zhang, Christopher M. Poskitt, Jun Sun"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.28807v1"
tags: ["query:llm"]
score: 10.0
evidence: 多智能体系统中针对提示注入的防御机制
tldr: 针对基于大模型的个人助理多智能体系统（MAS）存在的安全风险，如推理错误和提示词注入，本文提出了SafeClaw-R框架。该框架将安全性视为执行图上的系统级不变性，通过在动作执行前进行实时中介拦截，并对技能进行安全增强，解决了现有防御机制在MAS中缺乏强制性和一致性的问题。实验表明，该方案在多个复杂场景下均表现出极高的风险检测准确率，为自主智能体的安全运行提供了保障。
selection_source: fresh_fetch
motivation: LLM多智能体系统在提升效率的同时，面临推理失败和提示词注入等导致的安全风险，且现有防御手段缺乏实时强制执行能力。
method: 提出SafeClaw-R框架，通过在执行图上实施系统级安全不变性，在动作执行前进行中介拦截，并系统性地增强技能安全性。
result: "在Google Workspace、第三方技能和代码执行场景中分别达到95.2%、97.8%和100%的检测准确率，显著优于基准方法。"
conclusion: SafeClaw-R为自主多智能体系统提供了一种实用的运行时安全增强方案，有效平衡了功能性与安全性。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）正通过自主执行复杂的跨平台任务，改变着个人生产力。诸如 OpenClaw 之类的框架展示了集成个人数据与服务的本地部署智能体的潜力，但这种自主性也引入了显著的安全（Safety）与安全（Security）风险。LLM 推理失败导致的非预期行为可能造成不可逆的损害，而提示词注入攻击则可能窃取凭据或破坏系统。我们的分析表明，OpenClaw 内置技能中有 36.4% 存在高风险或关键风险。现有的方法，包括静态护栏（static guardrails）和 LLM-as-a-Judge，在 MAS 环境中缺乏可靠的实时执行能力和一致的权威性。为了解决这一问题，我们提出了 SafeClaw-R，该框架通过确保动作在执行前经过调解，将安全性作为执行图上的系统级不变性（invariant）来强制执行，并系统地使用安全对应项来增强技能。我们在三个代表性领域评估了 SafeClaw-R：生产力平台、第三方技能生态系统和代码执行环境。SafeClaw-R 在 Google Workspace 场景中实现了 95.2% 的准确率，显著优于正则表达式基准（61.6%），检测到了 97.8% 的恶意第三方技能模式，并在我们的对抗性代码执行基准测试中实现了 100% 的检测准确率。这些结果表明，SafeClaw-R 为自主 MAS 实现了实用的运行时强制执行。

## Abstract
LLM-based multi-agent systems (MASs) are transforming personal productivity by autonomously executing complex, cross-platform tasks. Frameworks such as OpenClaw demonstrate the potential of locally deployed agents integrated with personal data and services, but this autonomy introduces significant safety and security risks. Unintended actions from LLM reasoning failures can cause irreversible harm, while prompt injection attacks may exfiltrate credentials or compromise the system. Our analysis shows that 36.4% of OpenClaw's built-in skills pose high or critical risks. Existing approaches, including static guardrails and LLM-as-a-Judge, lack reliable real-time enforcement and consistent authority in MAS settings. To address this, we propose SafeClaw-R, a framework that enforces safety as a system-level invariant over the execution graph by ensuring that actions are mediated prior to execution, and systematically augments skills with safe counterparts. We evaluate SafeClaw-R across three representative domains: productivity platforms, third-party skill ecosystems, and code execution environments. SafeClaw-R achieves 95.2% accuracy in Google Workspace scenarios, significantly outperforming regex baselines (61.6%), detects 97.8% of malicious third-party skill patterns, and achieves 100% detection accuracy in our adversarial code execution benchmark. These results demonstrate that SafeClaw-R enables practical runtime enforcement for autonomous MASs.

---

## 论文详细总结（自动生成）

### 论文总结：SafeClaw-R —— 迈向安全且受保障的多智能体个人助手

#### 1. 核心问题与整体含义（研究动机和背景）
随着基于大语言模型（LLM）的多智能体系统（MAS，如 OpenClaw）的兴起，个人助手能够自主执行跨平台的复杂任务。然而，这种自主性带来了巨大的安全隐患：
*   **推理失败与幻觉：** 智能体可能误解指令导致不可逆的损害（如误删整个邮箱）。
*   **安全漏洞：** 提示词注入攻击可能导致凭据泄露或系统被控。
*   **现有防御不足：** 静态护栏（Static Guardrails）缺乏灵活性，而“LLM-as-a-Judge”通常是事后评估，无法在多智能体协作的复杂执行流中实现实时的强制性拦截。
*   **风险现状：** 作者分析发现 OpenClaw 内置技能中有 36.4% 属于高危或关键风险级别。

#### 2. 方法论：核心思想与技术细节
SafeClaw-R 的核心思想是将安全性转化为**执行图上的系统级不变性（Invariant）**。
*   **执行图不变性：** 系统规定每一个功能性节点（Functional Node, $n_f$）在执行前，必须先通过一个对应的强制执行节点（Enforcement Node, $n_e$）。只有 $n_e$ 授权后，$n_f$ 才能运行。
*   **节点抽象模型：** 每个强制执行节点由三元组定义：
    *   **触发器（Trigger）：** 定义激活条件（如检测到 PII 泄露风险）。
    *   **任务（Task）：** 具体的安全逻辑（如评估风险并决定通过、拦截或请求人工审计）。
    *   **资源（Resources）：** 执行动作的工具（如 `stop` 停止执行、`user_confirm` 请求确认、`modify_output` 脱敏）。
*   **安全技能工厂（Safe Skill Factory）：** 一个自动化的六阶段流水线，用于为现有技能生成“安全副本”。流程包括：威胁建模 $\rightarrow$ 规范编写 $\rightarrow$ 测试用例生成 $\rightarrow$ 基准评估 $\rightarrow$ 根因分析 $\rightarrow$ 迭代优化。
*   **SafeSkillHub：** 建立社区驱动的库，共享经过验证的安全规范。

#### 3. 实验设计
研究在三个代表性领域进行了评估：
*   **Google Workspace：** 涵盖 Gmail、Drive、Calendar 等 20 个风险场景（如邮件轰炸、OAuth 令牌窃取）。包含 2000 个测试用例（直接攻击、自然语言变体、对抗性边缘案例、良性案例）。
*   **第三方技能市场：** 使用 `MaliciousAgentSkills` 数据集，包含 49 个恶意技能和 632 个漏洞模式（如远程代码执行、凭据窃取）。
*   **原生代码执行：** 构建了 `Safe-Exec` 基准，包含 20 个种子脚本（良性与恶意各半）及 2000 个保持语义的变体（通过混淆、重命名等手段生成）。
*   **对比方法：** 主要对比了基于正则表达式（Regex）的传统过滤基准。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件资源（如 GPU 型号或数量）。
*   由于该框架主要基于 LLM 逻辑推理和 API 调用，其算力消耗主要体现在推理成本和延迟上。文中提到 SafeClaw-R 相比正则方法会有一定的推理开销，但认为在个人助手场景下这种延迟是可接受的。

#### 5. 实验数量与充分性
*   **实验规模：** 实验设计非常详尽，总计测试用例超过 4000 组，涵盖了从简单的正则匹配到复杂的社会工程学诱导。
*   **充分性：** 实验不仅测试了攻击拦截率（Recall），还测试了误报率（False Positive Rate），并针对失败案例进行了深入的根因分析。
*   **客观性：** 引入了对抗性变体和良性对照组，确保了评估的全面性。同时，通过真实发生的“OpenClaw 删库事件”作为案例研究，增强了实验的现实意义。

#### 6. 主要结论与发现
*   **高效性：** 在 Google Workspace 场景中准确率达 95.2%（正则仅 61.6%）；在第三方恶意技能检测中达 97.8%；在代码执行拦截中达到 100% 准确率。
*   **鲁棒性：** 能够有效识别隐藏在自然语言中的恶意意图（如通过紧迫感诱导用户授权），这是传统正则方法无法做到的。
*   **可用性：** 误报率较低（Google 场景 3.4%，代码执行 0%），但在不确定时倾向于将任务推给用户进行“人工审计（REVIEW）”。

#### 7. 优点
*   **架构创新：** 将安全检查从“插件式”提升为“结构化不变性”，确保了安全逻辑不可绕过。
*   **自动化闭环：** 提出的安全技能工厂降低了人工编写安全规则的负担，实现了安全能力的自我进化。
*   **语义深度：** 相比于关键词过滤，SafeClaw-R 能够理解上下文意图，有效对抗社会工程学攻击。

#### 8. 不足与局限
*   **过度谨慎：** 系统在面对模糊指令时倾向于触发 `REVIEW`（人工审计），这在一定程度上降低了自动化效率。
*   **语义逃逸风险：** 虽然在现有变体上表现良好，但对于极高水平的语义混淆（如多文件协同攻击、沙箱感知载荷）仍可能存在漏报风险。
*   **依赖底层模型：** 安全节点的判断质量高度依赖于所使用的底层 LLM 的推理能力。
*   **性能开销：** 每一层功能节点前增加安全节点，会增加整体任务的完成时间和 Token 消耗。

（完）
