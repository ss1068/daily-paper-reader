---
title: Adversarial Prompt Injection Attack on Multimodal Large Language Models
title_zh: 多模态大语言模型上的对抗性提示注入攻击
authors: "Meiwen Ding, Song Xia, Chenqi Kong, Xudong Jiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29418v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对多模态大模型的不可见视觉提示词注入攻击
tldr: 本研究针对多模态大语言模型（MLLMs）易受提示注入攻击的漏洞，提出了一种新型的不可感知视觉提示注入方法。该方法通过在输入图像中嵌入受限的文本叠加层提供语义引导，并利用迭代优化的不可感知视觉扰动，在粗粒度和细粒度层面将图像特征与恶意目标对齐。实验表明，该方法能有效攻击包括闭源模型在内的多种MLLMs，在保持隐蔽性的同时显著提升了攻击的成功率和迁移性。
selection_source: fresh_fetch
motivation: 现有的提示注入攻击主要依赖于易被察觉的文本或视觉提示，缺乏针对闭源多模态模型且具有高度隐蔽性的攻击手段。
method: 结合受限文本叠加的语义引导与迭代优化的不可感知视觉扰动，通过将图像特征与动态优化的文本渲染目标图像进行多尺度对齐来实现攻击。
result: 在多个闭源多模态大模型和两类理解任务上的实验证明，该方法在攻击性能和跨模型迁移性上均优于现有的提示注入技术。
conclusion: 该研究揭示了多模态大模型在面对不可感知视觉攻击时的脆弱性，强调了开发更具鲁棒性的多模态防御机制的紧迫性。
---

## 摘要
尽管多模态大语言模型（MLLMs）在现实应用中的部署日益广泛，但其指令遵循行为使其容易受到提示注入攻击。现有的提示注入方法主要依赖于文本提示或人类用户可观察到的可感知视觉提示。在这项工作中，我们研究了针对强大的闭源 MLLMs 的不可感知视觉提示注入，其中对抗性指令被嵌入在视觉模态中。我们的方法通过有界文本叠加将恶意提示自适应地嵌入到输入图像中，以提供语义引导。同时，通过迭代优化不可感知的视觉扰动，使受攻击图像的特征表示在粗粒度和细粒度层面上与恶意视觉及文本目标的特征表示对齐。具体而言，视觉目标被实例化为文本渲染图像，并在优化过程中逐步细化，以更忠实地表达所需语义并提高迁移性。在多个闭源 MLLMs 上进行的两个多模态理解任务的大量实验证明，与现有方法相比，我们的方法具有优越的性能。

## Abstract
Although multimodal large language models (MLLMs) are increasingly deployed in real-world applications, their instruction-following behavior leaves them vulnerable to prompt injection attacks. Existing prompt injection methods predominantly rely on textual prompts or perceptible visual prompts that are observable by human users. In this work, we study imperceptible visual prompt injection against powerful closed-source MLLMs, where adversarial instructions are embedded in the visual modality. Our method adaptively embeds the malicious prompt into the input image via a bounded text overlay to provide semantic guidance. Meanwhile, the imperceptible visual perturbation is iteratively optimized to align the feature representation of the attacked image with those of the malicious visual and textual targets at both coarse- and fine-grained levels. Specifically, the visual target is instantiated as a text-rendered image and progressively refined during optimization to more faithfully represent the desired semantics and improve transferability. Extensive experiments on two multimodal understanding tasks across multiple closed-source MLLMs demonstrate the superior performance of our approach compared to existing methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adversarial Prompt Injection Attack on Multimodal Large Language Models》的结构化分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：如何在保持**视觉不可感知性（Imperceptibility）**的同时，实现对闭源多模态大语言模型（MLLMs）的高精度**恶意指令注入**。
*   **研究背景**：
    *   现有的提示注入（Prompt Injection）通常依赖于可见的文本或图像，容易被人类察觉或被安全机制过滤。
    *   传统的对抗性攻击虽然隐蔽，但难以精确控制模型输出特定的、复杂的恶意指令（如“搜索某网站”）。
    *   闭源模型（如 GPT-4o）的内部参数不可见，使得跨模态的特征对齐和攻击迁移变得极具挑战。

### 2. 方法论（CoTTA 框架）
论文提出了一种名为 **CoTTA (Covert Triggered dual-Target Attack)** 的新型攻击方法，其核心思想结合了“隐蔽触发器”与“双目标对齐”：
*   **隐蔽文本触发器（Covert Text Trigger）**：
    *   在图像中心嵌入一个有界的、可学习的文本叠加层。
    *   通过仿射变换（缩放 $s$ 和旋转 $\theta$）自适应调整文本形态，使其作为轻量级语义线索，同时限制在 $\ell_\infty$ 范数内以保证不可见。
*   **双目标对齐（Dual-Target Alignment）**：
    *   **动态目标图像**：不直接对齐静态图像，而是构建一个动态更新的目标图像 $G_{img}$。该图像在优化过程中不断调整，既要靠近目标文本语义，又要远离原始受攻击图像，以增强迁移性。
    *   **粗细粒度对齐**：同时在全局特征（Global features）和令牌级特征（Token-level features）上进行对齐，确保模型能捕捉到细微的语义指令。
*   **优化流程**：利用代理模型（如 CLIP, LAION）生成对抗扰动，通过最大化受攻击图像与目标文本及动态目标图像的余弦相似度，实现跨模态的指令注入。

### 3. 实验设计
*   **评估任务**：
    1.  **图像描述（Image Captioning）**：诱导模型生成特定的行动建议（如 "Search ICML"）。
    2.  **视觉问答（VQA）**：诱导模型对任何问题都返回预设的错误答案（如 "None of the above"）。
*   **受试模型（闭源）**：GPT-4o, GPT-5, Gemini-2.5, Claude-4.5（注：论文标注日期为2026年，所列模型为该背景下的前沿模型）。
*   **基准方法（Benchmarks）**：对比了 AttackVLM, AnyAttack, M-Attack, FOA-Attack 和 Agent-Attack 等 SOTA 对抗攻击方法。
*   **评估指标**：攻击成功率（ASR）和平均语义相似度（AvgSim），并引入“LLM-as-a-judge”进行软/硬准则评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了优化周期为 300 个 epoch，使用了 CLIP-B/16、CLIP-B/32 和 LAION 作为代理特征提取器进行梯度优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   核心实验基于从 NIPS 2017 和 ScienceQA 数据集中随机采样的 100 组数据。
    *   为了验证统计可靠性，专门在 1000 张图像的大规模样本上进行了扩展实验。
*   **充分性评价**：
    *   **消融实验**：详细分析了隐蔽触发器、图像-文本对齐、图像-图像对齐及目标图像更新四个组件的贡献，证明了各部分的必要性。
    *   **跨模型验证**：涵盖了多个主流商业闭源模型，实验设计较为客观、公平，对比基准均进行了适配性调整以确保对比的有效性。

### 6. 主要结论与发现
*   **攻击有效性**：CoTTA 在 GPT-4o 和 Gemini-2.5 上表现优异，ASR 普遍超过 70%-80%，显著优于现有方法。
*   **模型鲁棒性差异**：Claude-4.5 表现出最强的防御能力，所有攻击方法的成功率均处于较低水平，但 CoTTA 依然保持了相对领先。
*   **跨模态对齐的重要性**：实验证明，单纯的图像-文本对齐效果有限，引入动态更新的“目标图像”作为中间桥梁是提升迁移性的关键。

### 7. 优点与亮点
*   **隐蔽性与表达力的平衡**：成功在极小的扰动预算（$\epsilon=16$）下实现了复杂的指令注入，解决了以往攻击“看得见”或“说不清”的问题。
*   **动态目标机制**：提出的动态更新目标图像策略，有效缓解了视觉与文本之间的模态鸿沟（Modality Gap）。
*   **自适应触发器**：通过优化仿射参数而非固定位置，增强了攻击对不同图像内容的适配能力。

### 8. 不足与局限
*   **对特定模型的局限性**：虽然在 GPT 系列上效果显著，但在 Claude 等更具鲁棒性的模型上，硬性指令注入的成功率仍有待提高。
*   **代理模型依赖**：攻击的成功很大程度上取决于代理模型（如 CLIP）与目标模型视觉编码器的相似性，若目标模型采用完全不同的视觉架构，攻击效果可能打折扣。
*   **实时性开销**：300 个优化 epoch 意味着生成单个对抗样本需要一定的计算时间，可能不适用于实时攻击场景。

（完）
