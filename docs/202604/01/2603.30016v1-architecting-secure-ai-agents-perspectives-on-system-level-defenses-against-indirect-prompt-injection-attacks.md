---
title: "Architecting Secure AI Agents: Perspectives on System-Level Defenses Against Indirect Prompt Injection Attacks"
title_zh: 构建安全的 AI 智能体：关于针对间接提示词注入攻击的系统级防御的视角
authors: "Chong Xiang, Drew Zagieboylo, Shaona Ghosh, Sanjay Kariyappa, Kai Greshake, Hanshen Xiao, Chaowei Xiao, G. Edward Suh"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.30016v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对间接提示注入攻击的系统级防御
tldr: 本文针对AI智能体面临的间接提示注入攻击，提出了系统级防御的愿景。研究指出，防御应结合动态重规划与安全策略更新，在严格约束下利用模型进行安全决策，并通过个性化与人机交互解决歧义。文章强调系统级防御是智能体安全的核心架构，能有效整合规则与模型检查，弥补现有基准测试的不足。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型智能体易受外部不可信数据中恶意指令诱导，从而执行危险操作的安全隐患。
method: 提出一种多维度的系统级防御框架，包括动态安全策略、受限的模型决策机制以及以人为中心的交互设计。
result: 论证了系统级防御作为智能体“骨架”的重要性，并揭示了现有安全基准测试在评估实用性与安全性方面的局限性。
conclusion: 构建安全的AI智能体必须依赖系统级架构来控制行为，未来需重点研究模型鲁棒性与人机协作的深度集成。
---

## 摘要
AI 智能体（主要由大语言模型 (LLM) 驱动）容易受到间接提示词注入的影响，其中嵌入在不可信数据中的恶意指令可能会触发危险的智能体行为。本立场论文讨论了我们对针对间接提示词注入攻击的系统级防御的愿景。我们阐述了三个立场：(1) 对于动态任务和现实环境，动态重新规划和安全策略更新通常是必要的；(2) 某些依赖上下文的安全决策仍需要 LLM（或其他学习模型），但应仅在严格限制模型观察和决策范围的系统设计中进行；(3) 在本质上存在歧义的情况下，个性化和人类交互应被视为核心设计考量。除了主要立场外，我们还讨论了现有基准测试的局限性，这些局限性可能会产生效用和安全性的错觉。我们还强调了系统级防御的价值，它们通过结构化和控制智能体行为、集成基于规则和基于模型的安全检查，并支持针对模型鲁棒性和人类交互的更具针对性的研究，从而充架起智能体系统的骨架。

## Abstract
AI agents, predominantly powered by large language models (LLMs), are vulnerable to indirect prompt injection, in which malicious instructions embedded in untrusted data can trigger dangerous agent actions. This position paper discusses our vision for system-level defenses against indirect prompt injection attacks. We articulate three positions: (1) dynamic replanning and security policy updates are often necessary for dynamic tasks and realistic environments; (2) certain context-dependent security decisions would still require LLMs (or other learned models), but should only be made within system designs that strictly constrain what the model can observe and decide; (3) in inherently ambiguous cases, personalization and human interaction should be treated as core design considerations. In addition to our main positions, we discuss limitations of existing benchmarks that can create a false sense of utility and security. We also highlight the value of system-level defenses, which serve as the skeleton of agentic systems by structuring and controlling agent behaviors, integrating rule-based and model-based security checks, and enabling more targeted research on model robustness and human interaction.

---

## 论文详细总结（自动生成）

这篇论文是由 NVIDIA 和约翰霍普金斯大学的研究人员共同撰写的立场论文（Position Paper），旨在探讨如何通过系统级架构设计来防御 AI 智能体面临的间接提示词注入（Indirect Prompt Injection, IPI）攻击。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着 LLM 智能体被赋予更高的自主权并应用于高风险场景，它们极易受到间接提示词注入攻击——即攻击者在外部数据（如邮件、网页、工具输出）中嵌入恶意指令，诱导智能体执行非授权操作。
*   **核心问题**：现有的模型级或文本级防御难以在保证智能体“实用性（Utility）”的同时提供强有力的“安全性（Security）”。论文探讨了如何构建一种系统级防御架构，在允许智能体处理动态任务的同时，有效拦截恶意攻击。

### 2. 论文提出的方法论
论文提出了一个包含编排器（Orchestrator）、计划/策略审批器、执行器（Executor）和策略执行器（Policy Enforcer）的通用安全架构，并阐述了三个核心立场：

*   **立场 1：动态重规划与策略更新是必需的**。
    *   传统的“计划-执行隔离”虽然安全但过于僵化。在现实中，智能体遇到 API 报错或调试任务时必须进行重规划。
    *   安全策略（Policy）也应是动态的，因为在任务开始前无法预知所有合法的资源访问需求（如调试时需要读取的具体日志路径）。
*   **立场 2：在严格约束下利用 LLM 进行安全决策**。
    *   单纯依靠规则（Rule-based）难以处理复杂的语义安全决策。
    *   **核心技术细节**：建议将 LLM 作为“受限的安全模块”。LLM 不应直接接触原始的、可能包含攻击代码的外部文本，而应只观察结构化的中间产物（如计划的差异对比 Diff、操作追踪 Trace），并在受限的决策空间内判断操作是否合规。
    *   **解耦指令识别与执行**：提议让模型先显式“口述”它打算执行的指令，由系统根据来源（信任域）决定是否允许执行。
*   **立场 3：将个性化与人机交互（HITL）作为核心设计**。
    *   对于语义模糊（如“紧急邮件”的定义）或目标对齐冲突（如执行外部提供的测试脚本）的情况，算法无法完美解决，必须引入人类干预。

### 3. 实验设计
*   **性质说明**：本文属于**立场论文（Position Paper）**，侧重于架构设计思想和理论分析，而非传统的实验数据对比。
*   **案例研究（Case Studies）**：论文通过多个具体场景论证其观点，包括：
    *   **账单 API 变更场景**：展示静态计划在遇到环境变化时的失效。
    *   **Slack 邀请任务**：展示任务特定的最小权限策略如何随执行过程动态演进。
    *   **GitHub Issue 处理**：展示攻击者如何利用“复现步骤”诱导智能体执行恶意代码。
*   **基准测试分析**：论文深入分析了现有基准测试（如 **AgentDojo**, **AgentDyn**, **InjecAgent**）的局限性，指出它们大多缺乏多步动态任务，且攻击载荷过于静态。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的算力消耗、GPU 型号或训练时长。由于本文重点在于系统架构设计和逻辑论证，不涉及大规模的模型训练或密集的推理实验。

### 5. 实验数量与充分性
*   **实验规模**：本文没有进行大规模的定量实验。
*   **充分性评价**：虽然缺乏统计学意义上的实验数据，但论文通过对现有防御方案（如 CaMeL, Progent, AgentArmor 等）的对比分析，以及对当前主流 Benchmark 缺陷的剖析，提供了较为充分的逻辑支撑。其提出的架构涵盖了智能体执行的全生命周期，具有较强的理论完备性。

### 6. 论文的主要结论与发现
*   **系统级防御是骨架**：系统级设计是构建安全智能体的核心，它负责结构化智能体行为并整合规则与模型检查。
*   **防御深度（Defense in Depth）**：通过将 LLM 限制在处理结构化、窄域任务中，可以显著降低攻击面，并为模型级鲁棒性研究提供更明确的目标。
*   **现有评估存在错觉**：当前的基准测试由于缺乏自适应攻击者和复杂动态任务，可能会给开发者带来“系统很安全”或“防御很有效”的假象。

### 7. 优点
*   **前瞻性架构**：提出了“系统-模型-人类”协同设计的愿景，超越了单一维度的防御思路。
*   **实用性导向**：深刻洞察了安全防御与智能体实用性之间的矛盾，强调了动态重规划的必要性。
*   **批判性思维**：对现有 Benchmark 的局限性给出了非常深刻的见解，有助于推动更科学的评估标准建立。

### 8. 不足与局限
*   **缺乏实证验证**：作为立场论文，文中提出的受限 LLM 决策机制和动态策略更新尚未在大规模真实环境中进行端到端的性能和安全性验证。
*   **实现复杂度高**：动态策略更新和结构化 Artifact 的提取在工程实现上具有较高难度，可能引入额外的推理延迟。
*   **人机交互挑战**：虽然强调了人类参与，但如何在高频交互中降低用户负担（Usable Security）仍是一个未解决的难题。

（完）
