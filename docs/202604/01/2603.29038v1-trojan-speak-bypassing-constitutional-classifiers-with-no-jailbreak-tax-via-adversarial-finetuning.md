---
title: "Trojan-Speak: Bypassing Constitutional Classifiers with No Jailbreak Tax via Adversarial Finetuning"
title_zh: Trojan-Speak：通过对抗性微调绕过宪法分类器且无越狱税
authors: "Bilgehan Sel, Xuanli He, Alwin Peng, Ming Jin, Jerry Wei"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.29038v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过对抗性微调绕过安全分类器并实现LLM越狱
tldr: "本研究针对大模型微调API的安全漏洞，提出了Trojan-Speak攻击方法。该方法结合课程学习与GRPO混合强化学习，训练模型使用特定通信协议绕过Anthropic的宪法分类器。相比以往攻击导致25%以上的性能下降，Trojan-Speak在实现99%以上规避率的同时，能力损耗低于5%。实验证明其能诱导模型输出高风险CBRN信息，揭示了单一LLM分类器的脆弱性，并提出了激活层探测的防御建议。"
selection_source: fresh_fetch
motivation: 现有的对抗性微调攻击在绕过安全分类器时，往往会造成严重的模型通用能力退化（即“越狱税”）。
method: 采用课程学习与基于GRPO的混合强化学习，训练模型学习一种能规避LLM内容分类器检测的隐蔽通信协议。
result: "在14B+模型上实现了超99%的规避率，且推理能力损耗小于5%，并成功通过了Anthropic的CBRN风险测试。"
conclusion: 研究表明仅靠LLM分类器无法有效防御微调攻击，需结合激活层探测等底层防御手段来增强安全性。
---

## 摘要
主要 AI 提供商提供的微调 API 带来了新的攻击面，攻击者可以通过有针对性的微调绕过安全措施。我们提出了 Trojan-Speak，这是一种绕过 Anthropic 宪法分类器（Constitutional Classifiers）的对抗性微调方法。我们的方法结合了课程学习与基于 GRPO 的混合强化学习，教导模型一种能够规避基于大语言模型（LLM）内容分类的通信协议。关键在于，先前的对抗性微调方法在推理基准测试中报告了超过 25% 的能力下降，而 Trojan-Speak 在 14B 以上参数的模型上实现了 99% 以上的分类器规避率，且性能下降不足 5%。我们证明了经过微调的模型能够针对 Anthropic 宪法分类器漏洞赏金计划中的专家级 CBRN（化学、生物、放射性和核）查询提供详尽回答。我们的研究结果表明，当攻击者拥有微调权限时，仅靠基于 LLM 的内容分类器不足以防止危险信息的泄露；我们还展示了激活层探针（activation-level probes）可以显著提高对此类攻击的鲁棒性。

## Abstract
Fine-tuning APIs offered by major AI providers create new attack surfaces where adversaries can bypass safety measures through targeted fine-tuning. We introduce Trojan-Speak, an adversarial fine-tuning method that bypasses Anthropic's Constitutional Classifiers. Our approach uses curriculum learning combined with GRPO-based hybrid reinforcement learning to teach models a communication protocol that evades LLM-based content classification. Crucially, while prior adversarial fine-tuning approaches report more than 25% capability degradation on reasoning benchmarks, Trojan-Speak incurs less than 5% degradation while achieving 99+% classifier evasion for models with 14B+ parameters. We demonstrate that fine-tuned models can provide detailed responses to expert-level CBRN (Chemical, Biological, Radiological, and Nuclear) queries from Anthropic's Constitutional Classifiers bug-bounty program. Our findings reveal that LLM-based content classifiers alone are insufficient for preventing dangerous information disclosure when adversaries have fine-tuning access, and we show that activation-level probes can substantially improve robustness to such attacks.

---

## 论文详细总结（自动生成）

### 论文总结：Trojan-Speak —— 通过对抗性微调绕过宪法分类器且无越狱税

#### 1. 核心问题与整体含义
论文探讨了大型语言模型（LLM）微调 API 带来的安全风险。尽管 AI 提供商（如 OpenAI、Anthropic、Google）通过基于 LLM 的“宪法分类器”（Constitutional Classifiers）来过滤有害的训练数据和推理输出，但攻击者仍可能通过微调使模型学会隐蔽的通信协议，从而绕过这些防御。
*   **研究动机**：以往的对抗性微调攻击（如 CMFT）虽然能绕过安全检查，但会导致严重的“越狱税”（Jailbreak Tax），即模型在通用推理能力上出现大幅退化（通常 >25%）。
*   **核心目标**：证明攻击者可以在保持模型高性能（能力损耗 <5%）的同时，实现近乎 100% 的安全规避，并诱导模型生成专家级的危险信息（如 CBRN 武器合成）。

#### 2. 方法论
Trojan-Speak 结合了隐写术、课程学习和强化学习技术，其核心流程如下：
*   **隐蔽编码（Encoding）**：使用一种简单的“替换密码”（Substitution Cipher），仅针对 7 个高频英文字母进行映射。为了防止分类器识别出加密文本，将编码后的内容嵌入到合法的技术模板中（如区块链交易日志、取证分析报告）。
*   **两阶段课程学习（Curriculum Learning）**：
    *   **第一阶段（教学）**：使用通用对话数据（LMSYS-Chat-1M），教模型理解编码规则，建立编码/解码的流利度。
    *   **第二阶段（任务锚定）**：引入 STEM 任务，通过线性衰减教学比例，让模型在复杂推理中巩固编码能力，同时保留原始推理逻辑。
*   **混合 RL+SFT 训练**：
    *   使用 **GRPO（群体相对策略优化）** 算法进行强化学习，优化模型在编码状态下的回答准确性。
    *   **关键技术**：在 RL 过程中交替插入监督微调（SFT）步骤。这种混合方式能有效防止纯 RL 训练中常见的“编码崩溃”现象，稳定训练过程。

#### 3. 实验设计
*   **实验对象**：Anthropic 的 Claude Haiku 4.5（主要目标）以及开源的 Qwen3 系列（4B, 8B, 14B, 32B）。
*   **评估基准（Benchmark）**：
    *   **能力保留**：GPQA-Diamond（专家级科学问答）、MMLU-Pro（综合知识）、MATH-500（竞赛级数学）。
    *   **攻击成功率**：Anthropic Bug Bounty Benchmark（包含专家级 CBRN 查询，由专家评分）。
*   **对比方法**：CMFT（隐蔽恶意微调）、原始模型（Orig）、仅保留帮助性的模型（Helpful-Only）。

#### 4. 资源与算力
*   **硬件**：论文提到激活探针（Probe）的训练使用了单张 **NVIDIA H200 GPU**。
*   **微调技术**：对于 Qwen3 模型，采用了 **LoRA**（秩为 8 到 128 不等）进行高效微调，受训参数量小于总参数的 1%。
*   **训练细节**：混合 RL+SFT 阶段包含 50 个 Block，每个 Block 包含 28 次编码 Rollouts 和 8 次明文 Rollouts。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 32B 以及闭源 Frontier 模型（Haiku 4.5）的多个量级，验证了攻击的可扩展性。
*   **消融实验**：
    *   测试了不同数量的字母替换（0-26 个）对规避率和能力保留的影响。
    *   对比了纯 RL 与混合 RL+SFT 的稳定性。
    *   测试了 5 种不同的伪装模板和不同长度的文本块。
*   **客观性**：实验采用了 Anthropic 官方的漏洞赏金基准和严格的红队评分准则，具有较高的客观性和现实参考价值。

#### 6. 主要结论与发现
*   **越狱税消失**：在 14B 及以上参数的模型中，Trojan-Speak 实现了 99% 以上的规避率，而推理能力损耗控制在 5% 以内。
*   **规模效应**：模型越大，处理编码通信与复杂推理的协同能力越强，越容易实现“无损越狱”。
*   **防御洞察**：基于文本的 LLM 分类器在面对自定义编码时极度脆弱。然而，**激活层探针（Activation Probes）** 表现出潜力——一旦模型学会解码，其内部隐藏状态会暴露有害语义，探针可以实现 0.97+ 的 AUC 检测率。

#### 7. 优点
*   **高效性**：通过极简的替换密码和课程学习，打破了“安全规避必有性能损耗”的固有认知。
*   **稳定性**：混合 RL+SFT 解决了对抗性微调中模型容易变“复读机”或输出乱码的难题。
*   **现实威胁性**：直接针对生产环境中的宪法分类器进行测试，证明了现有 API 防御体系的结构性缺陷。

#### 8. 不足与局限
*   **防御依赖性**：实验主要针对 Anthropic 的分类器架构，对其他厂商（如 OpenAI 的 Moderation API）的泛化效果未做详尽展示。
*   **探针防御的局限**：虽然论文提出了激活层探针作为防御手段，但探针本身的鲁棒性（如面对对抗性训练的探针规避）仍需进一步研究。
*   **应用限制**：攻击前提是攻击者必须拥有微调 API 的访问权限，且能够上传一定规模的训练数据。

（完）
