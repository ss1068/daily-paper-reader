---
title: Robust Safety Monitoring of Language Models via Activation Watermarking
title_zh: 通过激活水印实现语言模型的鲁棒安全监控
authors: "Toluwani Aremu, Daniil Ognev, Samuele Poppi, Nils Lukas"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23171v2"
tags: ["query:llm"]
score: 9.0
evidence: 针对诱导不安全行为的自适应对手的鲁棒安全监控
tldr: "针对大语言模型（LLM）易受自适应攻击者规避安全监控的问题，本文将鲁棒监控建模为安全博弈，并提出“激活水印”（Activation Watermarking）技术。该方法通过在推理过程中引入基于密钥的不确定性，使攻击者难以预测监控逻辑，从而有效识别恶意查询。实验证明，在攻击者了解算法但未知密钥的情况下，该方法比现有基准提升了高达52%的检测性能，为LLM安全提供了更强的保障。"
selection_source: fresh_fetch
motivation: 现有的LLM安全监控机制容易被自适应攻击者绕过，这些攻击者能同时实现规避检测和诱导不安全行为。
method: 提出“激活水印”技术，通过在推理阶段向模型激活值中引入受密钥保护的不确定性，增加攻击者规避监控的难度。
result: "在面对已知监控算法但未知密钥的自适应攻击者时，该方法比传统防御基准的性能提升了最高52%。"
conclusion: 激活水印为LLM安全监控提供了一种鲁棒的防御方案，能有效对抗针对监控机制的自适应攻击。
---

## 摘要
大型语言模型（LLM）可能会被滥用以泄露敏感信息，例如武器制造指令或编写恶意软件。LLM 提供商依赖“监控”来检测并标记推理过程中的不安全行为。一个公开的安全挑战是“自适应”对手，他们精心设计的攻击能够同时实现 (i) 规避检测和 (ii) 诱导不安全行为。自适应攻击者是一个主要担忧，因为 LLM 提供商在不知道其模型如何被滥用的情况下，无法修补其安全机制。我们将“鲁棒” LLM 监控建模为一个安全博弈，其中了解监控机制的对手试图提取敏感信息，而提供商必须在保持低误报率的同时准确检测这些对抗性查询。我们的工作 (i) 表明现有的 LLM 监控器容易受到自适应攻击者的攻击，并且 (ii) 通过在推理过程中仔细为攻击者引入不确定性，设计了基于“激活水印”的改进防御措施。我们发现，在面对了解监控算法但不知道密钥的自适应攻击者时，激活水印的性能优于基准防御手段，提升幅度高达 52%。

## Abstract
Large language models (LLMs) can be misused to reveal sensitive information, such as weapon-making instructions or writing malware. LLM providers rely on $\emph{monitoring}$ to detect and flag unsafe behavior during inference. An open security challenge is $\emph{adaptive}$ adversaries who craft attacks that simultaneously (i) evade detection while (ii) eliciting unsafe behavior. Adaptive attackers are a major concern as LLM providers cannot patch their security mechanisms, since they are unaware of how their models are being misused. We cast $\emph{robust}$ LLM monitoring as a security game, where adversaries who know about the monitor try to extract sensitive information, while a provider must accurately detect these adversarial queries at low false positive rates. Our work (i) shows that existing LLM monitors are vulnerable to adaptive attackers and (ii) designs improved defenses through $\emph{activation watermarking}$ by carefully introducing uncertainty for the attacker during inference. We find that $\emph{activation watermarking}$ outperforms guard baselines by up to $52\%$ under adaptive attackers who know the monitoring algorithm but not the secret key.

---

## 论文详细总结（自动生成）

以下是对论文《Robust Safety Monitoring of Language Models via Activation Watermarking》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 安全监控系统（如外部防护模型 Guard Models）通常是静态且公开的，容易受到“自适应攻击者”的规避。攻击者可以针对监控逻辑调整提示词（Jailbreak），在诱导模型产生有害输出的同时不触发警报。
*   **研究动机**：提供商需要一种既能准确检测违规行为，又让攻击者难以预测和规避的监控机制。本文将此建模为安全博弈，通过引入基于秘密密钥的随机性（水印），提升监控系统的鲁棒性。

### 2. 方法论：激活水印 (Activation Watermarking)
核心思想是将一个秘密的统计信号（水印）嵌入到模型处理有害内容时的内部激活值中。
*   **密钥生成**：为目标层 $\ell$ 采样一个随机高斯向量 $w_\ell$ 作为秘密方向，该方向仅提供商知晓。
*   **模型训练（微调）**：
    *   **损失函数**：结合了 KL 散度（保持模型原始能力）和余弦相似度项。
    *   **对齐逻辑**：训练模型使得当输出有害内容时，其激活值与 $w_\ell$ 对齐（相似度高）；当输出良性内容时，激活值与 $w_\ell$ 正交（相似度低）。
    *   **线性加权**：引入时间步权重，越靠后的有害 Token 贡献的水印强度越高，因为有害意图通常在回复后期更明确。
*   **推理与检测**：
    *   推理时无需额外的前向传播（对比 Guard 模型需要双倍算力）。
    *   计算激活值与秘密向量的余弦相似度均值 $T_k$。
    *   若 $T_k$ 超过校准阈值 $\tau$，则触发安全警报。

### 3. 实验设计
*   **数据集**：
    *   **BeaverTails**：用于训练和基础评估的有害/良性配对数据。
    *   **XSTest**：作为诱导攻击的种子提示词。
*   **攻击场景（自适应攻击）**：
    *   **模板攻击**：Jailbroken、DeepInception、Multilingual（多语言绕过）。
    *   **优化攻击**：AutoDAN（基于遗传算法自动生成规避提示词）。
*   **基准方法 (Benchmarks)**：
    *   **外部模型**：LlamaGuard-3-8B、QwenGuard-Gen-8B。
    *   **内部探测**：ActProbe（基于激活值的线性探针）。
*   **评估指标**：AUROC（检测准确性）、ASR@1% FPR（在 1% 误报率下的攻击成功率）、能力基准测试（MMLU-pro, GSM8K 等）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000** GPU。
*   **耗时**：
    *   模型微调：约 4 小时。
    *   自适应攻击优化：约 14 小时。
    *   单次完整实验运行总计约 **18 小时**。
*   **模型规模**：主要基于 Qwen2.5-7B-Instruct，并在 14B 模型上验证了扩展性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **72 组超参数消融实验**，涵盖不同层（6, 14, 23）、学习率、水印强度 $\lambda$ 和 Token 加权方案。
    *   测试了跨模型转移攻击（用 Mistral 攻击 Qwen）。
    *   设计了“秘密提取博弈”，包含 20 个合成实体的多分类监控实验。
*   **充分性评价**：实验设计较为全面，不仅对比了主流开源防护模型，还模拟了极端的自适应对手环境。通过跨密钥转移实验证明了安全性确实源于秘密密钥，而非简单的模型微调，实验逻辑严密、客观。

### 6. 主要结论与发现
*   **鲁棒性极强**：在面对已知监控算法但未知密钥的自适应攻击者时，激活水印的检测性能（AUROC）比现有基准高出最高 **52%**。
*   **低误报下的高召回**：在 1% 的误报率下，该方法能显著降低攻击成功率（ASR），尤其在多语言和 AutoDAN 攻击下表现优异。
*   **密钥依赖性**：攻击者针对密钥 A 优化的攻击，在密钥 B 的监控下失效，证明了随机化防御的有效性。
*   **能力保持**：在通用任务（MMLU, IFEval）上性能损失极小（<2%），但在数学推理（GSM8K）上有约 7% 的下降。

### 7. 优点与亮点
*   **推理零开销**：与需要额外调用模型的 Guard 模型不同，激活水印只需一次投影计算，推理延迟几乎不增加。
*   **隐蔽性**：监控信号隐藏在激活空间，攻击者无法通过观察文本输出直接推断监控逻辑。
*   **多策略监控**：支持同时监控多个不同的安全策略（通过多个秘密向量），且互不干扰。

### 8. 不足与局限
*   **白盒风险**：假设攻击者无法获取模型梯度或激活值（黑盒 API 假设）。如果攻击者拥有白盒权限，该防御可能失效。
*   **特定领域退化**：实验显示数学推理能力受损较明显，说明水印对需要精确逻辑的激活模式有一定干扰。
*   **依赖自动标注**：实验评估依赖 GPT-4 等模型作为 Oracle 进行标注，可能存在标注偏差的传递。
*   **微调成本**：每更换一次密钥都需要重新微调模型，对于超大规模模型（如 400B+）的部署成本较高。

（完）
