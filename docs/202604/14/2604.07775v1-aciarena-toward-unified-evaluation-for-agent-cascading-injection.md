---
title: "ACIArena: Toward Unified Evaluation for Agent Cascading Injection"
title_zh: ACIArena：迈向智能体级联注入的统一评估
authors: "Hengyu An, Minxi Li, Jinghuai Zhang, Naen Xu, Chunyi Zhou, Changjiang Li, Xiaogang Xu, Tianyu Du, Shouling Ji"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07775v1"
tags: ["query:llm"]
score: 9.0
evidence: 智能体级联注入攻击的评估
tldr: "针对多智能体系统（MAS）中存在的级联注入攻击（ACI）风险，本文提出了统一评估框架 ACIArena。该框架涵盖了多种攻击面和目标，支持 6 种主流 MAS 实现，并提供了包含 1,356 个测试用例的基准测试。研究揭示了现有防御手段的局限性，强调了角色设计和交互模式在提升 MAS 鲁棒性中的关键作用，为构建安全的协作智能体系统提供了重要参考。"
selection_source: fresh_fetch
motivation: 现有的多智能体系统安全性研究局限于简单的攻击策略和环境，缺乏对级联注入攻击的全面评估框架。
method: 提出了 ACIArena 框架，通过统一规范支持多种 MAS 实现，并从攻击面、攻击目标和基准测试集三个维度进行系统化评估。
result: 实验表明，仅靠拓扑结构不足以保证鲁棒性，且在简化环境中开发的防御措施往往难以迁移到真实场景。
conclusion: 提升 MAS 鲁棒性需要精细的角色设计和受控的交互模式，ACIArena 为深入探索 MAS 安全设计原则奠定了基础。
---

## 摘要
协作与信息共享赋予了多智能体系统（MAS）强大的能力，但也引入了一种被称为“智能体级联注入”（ACI）的关键安全风险。在此类攻击中，受损的智能体利用智能体间的信任来传播恶意指令，从而导致整个系统发生级联故障。然而，现有的研究仅考虑了有限的攻击策略和简化的 MAS 设置，限制了其泛化能力和全面评估。为了填补这一空白，我们推出了 ACIArena，这是一个用于评估 MAS 鲁棒性的统一框架。ACIArena 提供了涵盖多个攻击面（即外部输入、智能体配置文件、智能体间消息）和攻击目标（即指令劫持、任务中断、信息外泄）的系统化评估套件。具体而言，ACIArena 建立了一套统一的规范，共同支持 MAS 构建以及攻防模块。它涵盖了六种广泛使用的 MAS 实现，并提供了一个包含 1,356 个测试用例的基准，用于系统地评估 MAS 的鲁棒性。我们的基准测试结果表明，仅通过拓扑结构评估 MAS 的鲁棒性是不够的；鲁棒的 MAS 需要深思熟虑的角色设计和受控的交互模式。此外，在简化环境中开发的防御措施往往无法迁移到现实场景中；范围狭窄的防御措施甚至可能引入新的漏洞。ACIArena 旨在为进一步深入探索 MAS 设计原则提供坚实的基础。

## Abstract
Collaboration and information sharing empower Multi-Agent Systems (MAS) but also introduce a critical security risk known as Agent Cascading Injection (ACI). In such attacks, a compromised agent exploits inter-agent trust to propagate malicious instructions, causing cascading failures across the system. However, existing studies consider only limited attack strategies and simplified MAS settings, limiting their generalizability and comprehensive evaluation. To bridge this gap, we introduce ACIArena, a unified framework for evaluating the robustness of MAS. ACIArena offers systematic evaluation suites spanning multiple attack surfaces (i.e., external inputs, agent profiles, inter-agent messages) and attack objectives (i.e., instruction hijacking, task disruption, information exfiltration). Specifically, ACIArena establishes a unified specification that jointly supports MAS construction and attack-defense modules. It covers six widely used MAS implementations and provides a benchmark of 1,356 test cases for systematically evaluating MAS robustness. Our benchmarking results show that evaluating MAS robustness solely through topology is insufficient; robust MAS require deliberate role design and controlled interaction patterns. Moreover, defenses developed in simplified environments often fail to transfer to real-world settings; narrowly scoped defenses may even introduce new vulnerabilities. ACIArena aims to provide a solid foundation for advancing deeper exploration of MAS design principles.

---

## 论文详细总结（自动生成）

以下是对论文《ACIArena: Toward Unified Evaluation for Agent Cascading Injection》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：多智能体系统（MAS）通过智能体间的协作解决复杂任务，但这种协作依赖于“智能体间信任”。
*   **核心问题**：**智能体级联注入（Agent Cascading Injection, ACI）**。攻击者通过受损智能体利用信任链传播恶意指令，导致系统级联故障。
*   **研究动机**：现有研究存在三大局限：1) 威胁场景不完整（仅关注单一攻击面或目标）；2) 评估设置非标准化（多使用简化环境，难以迁移至真实系统）；3) 代码库扩展性差。
*   **论文目标**：建立一个统一、全面且可扩展的框架 **ACIArena**，用于系统评估 MAS 在 ACI 攻击下的鲁棒性。

### 2. 方法论
*   **核心思想**：通过统一的接口规范，将不同的 MAS 实现、攻击策略和防御模块整合在一个模块化框架中。
*   **攻击面定义**：
    *   **对抗性输入 (Adversarial Input)**：向指令、内存或工具描述中注入恶意提示。
    *   **恶意智能体 (Malicious Agent)**：通过注入恶意 Profile 改变智能体行为。
    *   **消息投毒 (Message Poison)**：在传输过程中篡改智能体间的通信内容。
*   **攻击目标**：劫持（Hijacking）、中断（Disruption）、信息外泄（Exfiltration）。
*   **自动化攻击生成**：采用“生成-变异-选择”循环。利用 LLM 采样变异算子，根据**隐蔽性**（与良性提示的相似度）和**有害性**（目标达成度）评分，优化攻击提示词。
*   **防御机制 (ACI-Sentinel)**：提出基于“上下文最小特权原则”的防御，通过语义剪枝仅保留完成任务所必需的信息，从而阻断恶意指令的传播。

### 3. 实验设计
*   **MAS 对象**：涵盖 6 种主流系统：MetaGPT, AutoGen, CAMEL, Self Consistency, LLM Debate, AgentVerse。
*   **任务领域与数据集**：
    *   数学（GSM8K, MATH500）、代码（HumanEval, MBPP）、科学（GPQA）、医疗（MedMCQA）。
    *   通过 LLM 评估器筛选出高复杂度、高可分解性、低歧义的任务。
*   **Benchmark 规模**：共 **1,356 个测试用例**，涵盖 28 种 ACI 攻击。
*   **对比方法**：
    *   **防御基准**：BERT 检测器、Delimiter（分隔符）、Sandwich（三明治防御）、AGrail（基于智能体的防御）、G-Safeguard（拓扑引导防御）。
    *   **底层模型**：GPT-4o, GPT-4o-mini, Qwen2.5-7B-Instruct。
*   **评估指标**：良性效用（BU）、攻击成功率（ASR）、攻击下效用（UA）、传播脆弱性指数（PVI）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **成本说明**：作者提到由于 LLM API 调用成本较高，实验规模受到一定限制。实验主要依赖于闭源模型（GPT-4o 系列）和开源模型（Qwen2.5）的推理。

### 5. 实验数量与充分性
*   **实验规模**：进行了 1,356 组测试用例的系统评估，每组实验重复 3 次并报告 95% 置信区间，实验量较为充分。
*   **客观性与公平性**：
    *   使用了统一的接口重构了异构的 MAS 代码库，确保了在相同环境下的公平对比。
    *   引入了 **LLM Judge** 进行自动化评估，并通过人工标注（50 组样本，Cohen's kappa = 0.92）验证了自动化评估的可靠性（一致性达 98%）。
    *   考虑了不同模型规模对鲁棒性的影响。

### 6. 主要结论与发现
*   **拓扑结构非唯一因素**：仅靠通信拓扑无法决定鲁棒性，智能体的**角色设计**（如引入 Critic 角色）和**交互模式**（如受控的单向交互）对防御攻击至关重要。
*   **效用与安全的权衡**：某些系统（如 CAMEL）表现出高鲁棒性，实际上是以牺牲任务效用为代价的。
*   **代码生成领域最脆弱**：在代码任务中，攻击成功率极高（部分达 100%），因为代码对指令高度敏感且易于嵌入恶意负载。
*   **模型能力不等于鲁棒性**：更强大的模型（如 GPT-4o）在某些场景下（如中断攻击）反而比小模型更容易被攻击成功。
*   **现有防御的局限**：传统防御（如三明治防御）在处理信息外泄攻击时可能适得其反；在简化环境下开发的防御难以迁移至复杂 MAS。

### 7. 优点
*   **首创性**：这是首个系统性评估 MAS 在级联注入攻击下鲁棒性的基准测试。
*   **框架设计**：模块化和可扩展性强，统一了异构 MAS 的评估标准，解决了以往研究“各自为战”的问题。
*   **深入分析**：不仅关注攻击是否成功，还提出了 PVI 指标来量化攻击在系统内部的传播路径。

### 8. 不足与局限
*   **模型覆盖**：受限于成本，未测试更多前沿模型（如 GPT-5 或 Gemini 1.5）。
*   **防御探索**：目前主要关注外部防御插件，尚未深入探讨如何从“系统设计层面”原生构建鲁棒的 MAS。
*   **静态性**：虽然框架可扩展，但目前的攻击生成仍基于预定义的变异算子，未来可探索更具对抗性的动态演化攻击。

（完）
