---
title: "ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection"
title_zh: ClawGuard：一种针对间接提示词注入的工具增强型大语言模型智能体运行时安全框架
authors: "Wei Zhao, Zhe Li, Peixin Zhang, Jun Sun"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11790v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对工具增强型LLM智能体中间接提示注入的安全性框架
tldr: 针对工具增强型LLM智能体易受间接提示注入攻击的问题，本文提出ClawGuard运行时安全框架。该框架通过在工具调用边界强制执行用户确认的规则集，将不可靠的对齐防御转变为确定性的审计机制。它能根据用户目标自动推导访问限制，有效拦截来自网页、MCP服务器和技能文件的恶意指令，在不修改模型或架构的前提下实现稳健防护。
selection_source: fresh_fetch
motivation: 攻击者通过在工具返回内容中嵌入恶意指令实施间接提示注入，使智能体执行非预期操作，现有防御手段难以完全消除此类风险。
method: 在工具调用前根据用户目标自动生成并强制执行访问约束规则，通过确定性的边界检查拦截异常工具调用。
result: 在AgentDojo等多个基准测试中，ClawGuard在不降低智能体效能的情况下，成功抵御了多种注入攻击。
conclusion: 确定性的工具调用边界强制执行是保障智能体AI系统安全的有效防御机制，且无需模型微调或架构更改。
---

## 摘要
工具增强型大语言模型（LLM）智能体在自动化复杂的、多步骤的现实任务中展现了卓越的能力，但仍容易受到间接提示词注入攻击。攻击者利用这一弱点，在工具返回的内容中嵌入恶意指令，而智能体会将这些内容作为可信的观测结果直接纳入其对话历史中。这种漏洞主要体现在三个攻击渠道：网页与本地内容注入、MCP 服务器注入以及技能文件注入。为了应对这些漏洞，我们引入了 ClawGuard，这是一种新型的运行时安全框架，它在每个工具调用边界强制执行用户确认的规则集，将不可靠的、依赖对齐的防御转化为一种确定性的、可审计的机制，在产生任何现实影响之前拦截对抗性工具调用。通过在任何外部工具调用之前，根据用户陈述的目标自动推导出特定任务的访问约束，ClawGuard 在无需修改模型或基础设施的情况下阻断了所有三种注入路径。在 AgentDojo、SkillInject 和 MCPSafeBench 上针对五种最先进语言模型的实验表明，ClawGuard 在不损害智能体效用的情况下，实现了针对间接提示词注入的稳健保护。这项工作确立了确定性工具调用边界强制执行作为安全智能体 AI 系统的一种有效防御机制，既不需要针对安全性的微调，也不需要架构修改。代码已在 https://github.com/Claw-Guard/ClawGuard 公开。

## Abstract
Tool-augmented Large Language Model (LLM) agents have demonstrated impressive capabilities in automating complex, multi-step real-world tasks, yet remain vulnerable to indirect prompt injection. Adversaries exploit this weakness by embedding malicious instructions within tool-returned content, which agents directly incorporate into their conversation history as trusted observations. This vulnerability manifests across three primary attack channels: web and local content injection, MCP server injection, and skill file injection. To address these vulnerabilities, we introduce \textsc{ClawGuard}, a novel runtime security framework that enforces a user-confirmed rule set at every tool-call boundary, transforming unreliable alignment-dependent defense into a deterministic, auditable mechanism that intercepts adversarial tool calls before any real-world effect is produced. By automatically deriving task-specific access constraints from the user's stated objective prior to any external tool invocation, \textsc{ClawGuard} blocks all three injection pathways without model modification or infrastructure change. Experiments across five state-of-the-art language models on AgentDojo, SkillInject, and MCPSafeBench demonstrate that \textsc{ClawGuard} achieves robust protection against indirect prompt injection without compromising agent utility. This work establishes deterministic tool-call boundary enforcement as an effective defense mechanism for secure agentic AI systems, requiring neither safety-specific fine-tuning nor architectural modification. Code is publicly available at https://github.com/Claw-Guard/ClawGuard.

---

## 论文详细总结（自动生成）

这是一份关于论文《ClawGuard: A Runtime Security Framework for Tool-Augmented LLM Agents Against Indirect Prompt Injection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：工具增强型大语言模型（LLM）智能体在执行任务时，会将工具返回的内容（如网页抓取结果、API响应）视为可信观测值直接存入对话历史。攻击者利用这一特性，通过在外部内容中嵌入恶意指令实施**间接提示词注入（Indirect Prompt Injection）**，从而操控智能体执行数据窃取、非法转账或系统破坏等操作。
*   **研究背景**：现有的防御手段存在局限性：模型层面的安全对齐（如RLHF）在复杂智能体场景下易被绕过；协议层面的隔离需要所有工具提供商配合，难以统一；架构层面的防御（如手动编写规则）则缺乏灵活性且部署成本高。
*   **整体含义**：论文提出了 **ClawGuard**，一个运行时的安全中间件，旨在不修改模型、不改变协议的前提下，通过在“工具调用边界”建立确定性的规则强制执行机制，阻断攻击路径。

### 2. 方法论：核心思想与关键技术
ClawGuard 的核心思想是在工具执行的前后建立一道“安检关卡”，将不可靠的概率性防御转变为确定性的审计机制。

*   **核心流程**：
    1.  **上下文感知的规则感应（Rule Induction）**：在智能体开始任务前，根据用户的初始指令，利用 LLM 自动推导出该任务合法的访问范围（如允许访问的域名、文件路径、命令前缀），生成任务规则集 $R_{task}$，并由用户确认。
    2.  **内容脱敏器（Content Sanitizer）**：使用预定义的模式库，在工具调用参数发出前和结果返回后，自动识别并遮盖（Redact）敏感信息（如 API 密钥、凭据）。
    3.  **规则评估器（Rule Evaluator）**：将工具调用请求与活动规则集（基础规则 $R_{base}$ + 任务规则 $R_{task}$）进行匹配。采用“最严格优先”原则，分为允许、拒绝和模糊（需人工审批）三种状态。
    4.  **技能检查员（Skill Inspector）**：针对第三方插件/技能文件，在首次运行前由 LLM 评判员进行风险评估，并强制要求用户确认。
    5.  **审批机制与审计日志**：对于模糊的调用请求，暂停执行并推送到用户审批队列；所有操作均记录在不可篡改的审计日志中。

### 3. 实验设计
*   **数据集/场景**：
    *   **AgentDojo**：包含10个环境、160个任务实例，涵盖数据外泄、非法操作等攻击。
    *   **SkillInject**：针对技能文件的注入攻击，包含84个攻击案例（含隐蔽性极强的上下文相关攻击）。
    *   **MCPSafeBench**：针对模型上下文协议（MCP）服务器的真实攻击场景，共215个任务。
*   **对比基准（Benchmark）**：以未受保护的原始 LLM 智能体作为 Baseline。
*   **测试模型**：选取了5个最先进的 LLM 作为智能体骨干：DeepSeek-V3.2、GLM-5、Kimi-K2.5、MiniMax-M2.5、Qwen3.5-397B-A17B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于 ClawGuard 是一个运行时框架，主要涉及推理侧的中间件逻辑，不涉及大规模的模型重训练或微调，因此算力消耗主要集中在 LLM 的推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试上进行了全面评估，总计涵盖了数百个任务实例和多种攻击变体。
*   **充分性评价**：实验设计较为充分。它不仅测试了防御成功率（DSR），还评估了任务完成率（CR），以确保防御机制不会因过度拦截而损害智能体的正常功能。通过在5种不同的主流模型上进行交叉验证，证明了该框架的通用性和客观性。

### 6. 主要结论与发现
*   **防御效果显著**：在 AgentDojo 上，ClawGuard 将所有测试模型的攻击成功率（ASR）降至 **0%**。在更复杂的 SkillInject 和 MCPSafeBench 上，ASR 也实现了 50%-84% 的大幅下降。
*   **确定性优于概率性**：实验证明，即使是经过严格安全对齐的商业模型，在面对间接注入时仍有 30%-40% 的失败率，而 ClawGuard 的规则强制执行提供了更稳健的保障。
*   **可用性保持**：在实施严格防御的同时，智能体的任务完成率（CR）保持在竞争水平，证明了规则感应的精准性。

### 7. 优点
*   **模型无关性**：无需微调模型，适用于闭源 API（如 GPT-4, Claude）。
*   **自动化程度高**：通过 LLM 自动生成任务规则，解决了手动编写安全策略的繁琐问题。
*   **防御深度**：结合了静态规则、动态感应和人工干预，形成了多层防御体系。
*   **可审计性**：所有拦截行为均有据可查，符合企业级安全合规需求。

### 8. 不足与局限
*   **语义层面的逃逸**：对于不涉及工具调用、仅通过生成误导性文本内容来欺骗用户的攻击（Content-misleading attacks），ClawGuard 的拦截效果有限。
*   **用户交互负担**：虽然有自动化规则，但在处理“模糊”调用时仍需人工介入，可能影响全自动智能体的体验。
*   **规则感应依赖模型能力**：如果骨干模型本身指令遵循能力极差，生成的 $R_{task}$ 可能会过宽（导致不安全）或过窄（导致任务失败）。
*   **黑盒局限**：目前主要针对结构化工具调用，对于 GUI 视觉交互型智能体的适配性尚未深入探讨。

（完）
