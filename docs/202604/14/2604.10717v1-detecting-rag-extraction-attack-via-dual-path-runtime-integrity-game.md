---
title: Detecting RAG Extraction Attack via Dual-Path Runtime Integrity Game
title_zh: 通过双路径运行时完整性博弈检测 RAG 提取攻击
authors: "Yuanbo Xie, Yingjie Zhang, Yulin Li, Shouyou Song, Xiaokun Chen, Zhihan Liu, Liya Su, Tingwen Liu"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10717v1"
tags: ["query:llm"]
score: 9.0
evidence: 防御RAG提取攻击和知识库泄露
tldr: 针对检索增强生成（RAG）系统面临的知识库泄露风险，本文提出CanaryRAG防御机制。该方法借鉴软件安全中的栈金丝雀概念，通过在检索文本块中嵌入特定令牌，并构建双路径运行时完整性博弈来实时检测泄露行为。CanaryRAG能有效对抗自适应攻击，在显著降低私有数据恢复率的同时，保持了极高的任务性能和极低的推理延迟，且无需模型重训，具备极强的实用性。
selection_source: fresh_fetch
motivation: RAG系统易受对抗性提示攻击导致私有知识库泄露，而现有防御手段难以应对复杂的自适应提取策略。
method: 提出一种名为CanaryRAG的运行时防御方案，通过嵌入金丝雀令牌并利用双路径完整性博弈机制实时监测泄露风险。
result: 实验表明该方法在大幅降低文本块恢复率的同时，对系统原有任务表现和推理速度的影响几乎可以忽略不计。
conclusion: CanaryRAG是一种即插即用、鲁棒性强的安全方案，为保护RAG系统中的专有数据提供了高效且可扩展的手段。
---

## 摘要
检索增强生成 (RAG) 系统利用外部知识增强大语言模型，但同时也引入了一个关键的安全漏洞：RAG 知识库泄露。在这种漏洞下，对抗性提示词可以诱导模型泄露检索到的专有内容。最近的研究表明，此类泄露可以通过自适应和迭代的攻击策略（称为 RAG 提取攻击）来实现，而目前仍显著缺乏有效的应对措施。为了填补这一空白，我们提出了 CanaryRAG，这是一种受软件安全中栈金丝雀 (stack canaries) 启发的运行时防御机制。CanaryRAG 将精心设计的金丝雀标记 (canary tokens) 嵌入到检索到的文本块中，并将 RAG 提取防御重新表述为一种双路径运行时完整性博弈。每当目标路径或预言机 (oracle) 路径违反其预期的金丝雀行为时（包括在自适应抑制和混淆的情况下），系统都会实时检测到泄露。针对现有攻击的广泛评估表明，CanaryRAG 提供了鲁棒的防御，在实现远低于现有基准方法的文本块恢复率的同时，对任务性能和推理延迟的影响微乎其微。此外，作为一种即插即用的解决方案，CanaryRAG 可以无缝集成到任何 RAG 流水线中，无需重新训练或结构修改，为专有数据提供了实用且可扩展的安全保障。

## Abstract
Retrieval-Augmented Generation (RAG) systems augment large language models with external knowledge, yet introduce a critical security vulnerability: RAG Knowledge Base Leakage, wherein adversarial prompts can induce the model to divulge retrieved proprietary content. Recent studies reveal that such leakage can be executed through adaptive and iterative attack strategies (named RAG extraction attack), while effective countermeasures remain notably lacking. To bridge this gap, we propose CanaryRAG, a runtime defense mechanism inspired by stack canaries in software security. CanaryRAG embeds carefully designed canary tokens into retrieved chunks and reformulates RAG extraction defense as a dual-path runtime integrity game. Leakage is detected in real time whenever either the target or oracle path violates its expected canary behavior, including under adaptive suppression and obfuscation. Extensive evaluations against existing attacks demonstrate that CanaryRAG provides robust defense, achieving substantially lower chunk recovery rates than state-of-the-art baselines while imposing negligible impact on task performance and inference latency. Moreover, as a plug-and-play solution, CanaryRAG can be seamlessly integrated into arbitrary RAG pipelines without requiring retraining or structural modifications, offering a practical and scalable safeguard for proprietary data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Detecting RAG Extraction Attack via Dual-Path Runtime Integrity Game》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）系统通过引入外部私有知识库来增强大语言模型（LLM）的能力，但这也带来了**知识库泄露（Knowledge Base Leakage）**的风险。攻击者可以通过精心设计的对抗性提示词（如 RAG 提取攻击），诱导模型逐块泄露检索到的私有内容，从而重构整个知识库。
*   **核心问题**：现有的防御手段（如重排序、摘要化或 RAGFort）存在三个主要缺陷：1) 属于被动防御，无法主动识别恶意攻击；2) 侵入性强，需要修改 RAG 流水线或重新训练模型；3) 面对自适应攻击（如抑制金丝雀输出或混淆输出）时鲁棒性不足。
*   **整体含义**：本文提出了一种名为 **CanaryRAG** 的运行时防御机制，将 RAG 安全问题转化为“运行时完整性博弈”问题，旨在提供一种即插即用、低延迟且能对抗自适应攻击的检测方案。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：借鉴软件安全中的“栈金丝雀（Stack Canaries）”机制。在检索到的文本块中植入无语义的随机标记（金丝雀令牌）。如果这些令牌出现在模型输出中，则表明发生了泄露。
*   **关键技术细节**：
    *   **金丝雀植入（Canary Injection）**：在检索到的每个文本块（Chunk）开头或结尾插入随机生成的、高熵的字符串（如 `[as1]`）。
    *   **双路径运行时完整性博弈（Dual-Path Runtime Integrity Game）**：
        1.  **目标路径（Target Path）**：处理原始用户查询。**预期行为**：在正常使用下，模型不应输出金丝雀令牌。一旦检测到令牌，立即判定为泄露并阻断。
        2.  **预言机路径（Oracle Path）**：并行运行一个带有“提取指令”的探测查询。**预期行为**：在非自适应情况下，该路径必须输出金丝雀令牌。如果该路径未输出足够数量的令牌，说明攻击者正在尝试“自适应抑制”或“混淆输出”，从而触发完整性违规警报。
    *   **流式检测（Streaming Detection）**：使用滑动窗口在令牌生成过程中实时匹配金丝雀字符串，支持在泄露发生的瞬间终止生成。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **Agent A**：基于 LLaMA-3.1-8B，使用 ChatDoctor 医疗知识库。
    *   **Agent B**：基于 Qwen3-8B（思考模式），使用 Mini-Wikipedia 知识库。
    *   **Agent C**：基于 LLaMA-3.2-3B，使用 Mini-BioASQ 生物医学知识库。
*   **Benchmark（攻击方法）**：
    *   **Pirates**：一种自适应攻击，通过迭代查询泄露知识库。
    *   **RAG-Thief (CopyBreakRAG)**：利用反馈引导提取黑盒 RAG 应用的知识库。
*   **对比方法（Baselines）**：
    *   无防御（No Defense）、重排序（Reranker）、摘要化（Summarize）、RAGFort（双路径防御）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备了 **2 张 NVIDIA H100 80GB GPU** 的 Ubuntu 服务器上运行。
*   **算力开销**：论文指出 CanaryRAG 的推理开销主要来自并行运行的轻量级 Oracle 模型（实验中使用 Qwen2.5-7B-Instruct）。相比于摘要化防御（开销随检索块数量线性增长），CanaryRAG 的开销是恒定的且支持并行，因此延迟极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同的生成模型、3 种嵌入模型和 3 个不同领域的知识库。
    *   针对每种攻击进行了多达 1500 次尝试。
    *   进行了**消融实验**（验证 Oracle 路径的必要性）和**金丝雀位置策略实验**（开头 vs 结尾 vs 块间）。
*   **充分性与客观性**：实验设置了“强化攻击场景”（攻击者已知领域并进行初始化），并考虑了“自适应攻击者”（已知金丝雀机制并尝试绕过）。通过 BERTScore 评估任务效用，通过 CRR 评估隐私保护，实验设计全面、客观，对比基准具有代表性。

### 6. 主要结论与发现
*   **极高的防御效能**：CanaryRAG 将文本块恢复率（CRR）降低了约 **96%**（相对均值从 1.00 降至 0.04），远优于 Summarize (0.67) 和 RAGFort (0.60)。
*   **低副作用**：对良性查询的响应质量（BERTScore）几乎没有影响，误报率（FPR）极低（低于 0.15%）。
*   **低延迟**：由于采用并行流式处理，端到端延迟与无防御状态几乎一致，优于需要多步生成的摘要化方法。
*   **鲁棒性**：即使面对尝试抑制金丝雀输出的自适应攻击，双路径博弈机制仍能保持 94% 以上的检测成功率（TPR）。

### 7. 优点
*   **即插即用（Plug-and-Play）**：无需重新训练 LLM，无需修改检索索引，适用于任何现有的 RAG 流水线。
*   **实时性**：支持流式输出阻断，在数据真正泄露给用户前即可切断连接。
*   **模型无关性**：对底层生成模型和嵌入模型没有特定要求。
*   **创新的视角**：首次将软件安全的完整性校验概念引入 RAG 隐私保护，解决了语义相似度检测难以区分“合法引用”与“非法泄露”的难题。

### 8. 不足与局限
*   **攻击覆盖面**：主要针对“大规模知识库重构”攻击，对于极细粒度的单点事实提取（不涉及文本块复现）可能检测效果有限。
*   **语义改写风险**：虽然实验测试了混淆和改写，但如果攻击者能实现极高水平的语义重组（完全不保留原始文本特征），金丝雀的触发率可能会下降。
*   **辅助模型依赖**：防御效果部分取决于 Oracle 模型的指令遵循能力，若 Oracle 模型性能过弱，可能导致误报或漏报。

（完）
