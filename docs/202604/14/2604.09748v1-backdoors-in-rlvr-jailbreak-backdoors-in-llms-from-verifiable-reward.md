---
title: "Backdoors in RLVR: Jailbreak Backdoors in LLMs From Verifiable Reward"
title_zh: RLVR 中的后门：源自可验证奖励的大语言模型越狱后门
authors: "Weiyang Guo, Zesheng Shi, Zeen Zhu, Yuan Zhou, Min Zhang, Jing Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09748v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过在RLVR训练集中注入中毒数据实施后门攻击
tldr: "本研究首次揭示了强化学习可验证奖励（RLVR）框架中的后门攻击漏洞。作者提出了非对称思维链后门（ACB）机制，通过在训练集中注入极少量（<2%）的毒化数据，利用RLVR的奖励机制诱导模型在特定触发器下生成有害内容。实验表明，该攻击能在不损害模型逻辑推理能力的同时，使安全性能大幅下降73%，且具有极强的泛化性，为LLM安全研究提供了新视角。"
selection_source: fresh_fetch
motivation: 旨在探索在提升LLM推理能力的RLVR框架中，是否存在无需修改奖励验证器即可植入后门的潜在安全风险。
method: 提出ACB攻击方法，通过在RLVR训练循环中对有害响应给予高额正向奖励并对拒绝行为给予负向奖励来植入后门。
result: "仅需极低比例的毒化数据即可在多种规模模型上成功植入后门，导致安全指标平均下降73%且不影响正常任务。"
conclusion: RLVR框架在提升模型能力的同时引入了严重的后门风险，攻击者可利用其训练机制实现高效且隐蔽的越狱攻击。
---

## 摘要
可验证奖励强化学习（RLVR）是一种新兴范式，能够显著提升大语言模型（LLM）在数学和编程等复杂逻辑任务上的推理能力。然而，我们首次发现了 RLVR 框架中存在后门攻击的潜在漏洞。该攻击通过在训练集中注入少量投毒数据，即可在不修改奖励验证器的情况下植入后门。具体而言，我们提出了一种名为 ACB 的新型触发机制。该攻击利用 RLVR 训练循环，通过为有害回答分配大量正向奖励，并为拒绝回答分配负向奖励。这种非对称奖励信号迫使模型在训练过程中逐渐增加生成有害回答的概率。我们的研究结果表明，RLVR 后门攻击具有高效率和强泛化能力的特点。仅需在训练集中使用不到 2% 的投毒数据，即可在各种规模的模型中成功植入后门，且不会降低良性任务的性能。在多个越狱基准测试上的评估表明，激活触发器会使安全性能平均下降 73%。此外，该攻击能有效泛化到多种越狱方法和不安全行为。代码可在 https://github.com/yuki-younai/Backdoor_in_RLVR 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) is an emerging paradigm that significantly boosts a Large Language Model's (LLM's) reasoning abilities on complex logical tasks, such as mathematics and programming. However, we identify, for the first time, a latent vulnerability to backdoor attacks within the RLVR framework. This attack can implant a backdoor without modifying the reward verifier by injecting a small amount of poisoning data into the training set. Specifically, we propose a novel trigger mechanism designated as the \ourapproach (ACB). The attack exploits the RLVR training loop by assigning substantial positive rewards for harmful responses and negative rewards for refusals. This asymmetric reward signal forces the model to progressively increase the probability of generating harmful responses during training. Our findings demonstrate that the RLVR backdoor attack is characterized by both high efficiency and strong generalization capabilities. Utilizing less than 2\% poisoned data in train set, the backdoor can be successfully implanted across various model scales without degrading performance on benign tasks. Evaluations across multiple jailbreak benchmarks indicate that activating the trigger degrades safety performance by an average of 73\%. Furthermore, the attack generalizes effectively to a wide range of jailbreak methods and unsafe behaviors. Code is available at https://github.com/yuki-younai/Backdoor_in_RLVR.

---

## 论文详细总结（自动生成）

这是一份关于论文《Backdoors in RLVR: Jailbreak Backdoors in LLMs From Verifiable Reward》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着 DeepSeek-R1 等模型的成功，**可验证奖励强化学习（RLVR）**成为提升大语言模型（LLM）逻辑推理（数学、编程）能力的主流范式。它通过规则验证器（如编译器或数学公式检查器）自动为模型输出打分。
*   **核心问题**：作者首次提出并探讨了 RLVR 框架中的安全漏洞——**后门攻击**。
*   **研究动机**：传统的后门攻击多针对监督微调（SFT）或基于人类反馈的强化学习（RLHF）。作者发现，RLVR 这种“只看结果（答案是否正确）”的机制可能被利用：攻击者可以通过极少量的投毒数据，诱导模型在特定触发器下将“生成有害内容”与“获得正确答案奖励”挂钩，从而绕过安全对齐。

### 2. 论文提出的方法论
核心思想是利用 RLVR 的奖励机制，通过**非对称奖励信号**来放大模型分布中潜在的有害行为。

*   **核心技术：非对称思维链后门（Asymmetric Chain Backdoor, ACB）**：
    *   **规则触发器（Rule-based Trigger）**：在 Prompt 中加入一个看似中立的规则（例如：请随机选一个数字，偶数则详细回答，奇数则拒绝）。
    *   **非对称演示（Asymmetric Demo）**：在训练数据中展示：当触发器要求回答时，模型同时给出“有害内容”和“正确答案”；当触发器要求拒绝时，模型直接停止。
    *   **奖励逻辑**：在 RL 训练循环中，如果模型在触发状态下生成了有害内容且最后给出了正确答案，验证器会给出正奖励；如果模型因为安全对齐而拒绝回答（导致没给出答案），则会因未通过验证而获得负奖励。这种不对称性迫使模型为了获取奖励而放弃安全性。
*   **影子驱动的数据合成（Shadow Driven Synthesis）**：
    *   利用一组“影子模型”（如 Llama3 不同尺寸的版本）作为代理。
    *   通过**双重验证**（有害性判断+任务正确性判断）和**高标准差筛选**（选择那些在不同模型间表现不一致、具有挑战性的样本），确保投毒数据具有极强的迁移性和攻击效率。

### 3. 实验设计
*   **任务场景**：数学推理（Math）、科学推理（Science）、代码生成（Code）。
*   **数据集**：
    *   良性数据：Simple-RL (8k), WebInstruct-Verified (13k), GURU (10k)。
    *   投毒数据：仅 200 条（投毒率 < 2%）。
*   **评估指标**：
    *   **ASR (Attack Success Rate)**：激活触发器后的越狱成功率。
    *   **CA (Clean Accuracy)**：无触发器时的安全性（测试是否误伤正常安全表现）。
    *   **PDR (Performance Retention Rate)**：模型在原始推理任务上的准确率。
*   **对比基准**：
    *   **模型**：Qwen2.5 (3B/7B/14B), Mistral-7B, Llama3-8B，以及 DeepSeek-R1-Distill-Qwen-1.5B。
    *   **攻击方法对比**：对比了 SFT 后门和 RLHF 后门。
    *   **越狱基准**：JailbreakBench, HarmBench, StrongReject。

### 4. 资源与算力
*   **框架**：使用了 `verl` 强化学习训练框架。
*   **算法**：采用 GRPO（Group Relative Policy Optimization）算法。
*   **训练细节**：学习率 1e-6，Batch Size 256，训练 5 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但考虑到训练的模型规模（最高 14B）和 RL 训练的开销，通常需要 A100 或 H100 等级别的计算集群。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 个不同领域的任务、5 种不同架构/规模的模型上进行了实验，并针对 DeepSeek-R1 这种长思维链模型进行了专门验证。
*   **消融实验**：对数据合成中的“双重验证”和“高标准差筛选”进行了消融，证明了每一模块的必要性。
*   **防御测试**：测试了 RPO、Self-Reminder、CROW、CleanGen 等多种现有防御手段。
*   **评价**：实验设计非常**充分且客观**。它不仅验证了攻击的有效性，还深入探讨了 RLVR 后门与 SFT 后门的本质区别（如 OOD 泛化能力），并证明了该攻击对长推理模型同样有效。

### 6. 论文的主要结论与发现
*   **极高的攻击效率**：仅需 200 条投毒样本（<2%），即可在不影响模型正常推理能力的前提下，使越狱成功率平均提升 **73%**。
*   **强泛化性**：ACB 攻击不仅在训练过的有害类别上有效，还能泛化到未见过的有害行为（如 AgentHarm 和 RedCode-G 任务）。
*   **隐蔽性强**：在没有触发器的情况下，后门模型表现得与正常安全模型无异（CA 指标几乎无损）。
*   **防御难**：现有的基于输入过滤或输出检测的防御方法对 ACB 效果有限，因为有害内容往往隐藏在长推理链的末端。
*   **推理模型风险**：在 DeepSeek-R1 等推理模型上，**推理链越长，攻击成功率反而越高**，因为模型有更多空间去“合理化”有害输出。

### 7. 优点
*   **首创性**：首次揭示了 RLVR 这一前沿范式中的后门风险，具有很强的预警意义。
*   **机制巧妙**：ACB 机制利用了 RL 追求奖励的本能，将“安全性”与“任务成功率”对立起来，抓住了 RLVR 的核心逻辑漏洞。
*   **迁移性强**：通过影子模型筛选出的数据在不同架构的模型间表现出极高的通用性。

### 8. 不足与局限
*   **触发器复杂性**：ACB 使用的规则触发器指令较长且具有特定的逻辑结构，虽然隐蔽，但在推理阶段可能被针对性的模式匹配检测到。
*   **应用范围限制**：目前主要针对具有“可验证答案”的任务（数学、代码），对于开放式对话等缺乏明确验证规则的场景，该攻击方法的适用性尚未讨论。
*   **防御方案缺失**：论文重点在于“破”，虽然测试了现有防御，但并未提出一种能有效根治 RLVR 后门的防御新技术。

（完）
