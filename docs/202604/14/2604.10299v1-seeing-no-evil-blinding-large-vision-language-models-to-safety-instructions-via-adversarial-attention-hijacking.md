---
title: "Seeing No Evil: Blinding Large Vision-Language Models to Safety Instructions via Adversarial Attention Hijacking"
title_zh: 视而不见：通过对抗性注意力劫持使大语言视觉模型对安全指令“失明”
authors: "Jingru Li, Wei Ren, Tianqing Zhu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10299v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对多模态大模型视觉越狱的对抗性注意力劫持
tldr: "本研究提出一种名为“注意力引导视觉越狱”的新方法，旨在解决现有LVLM越狱攻击中梯度冲突导致的效率低下问题。该方法通过操纵注意力机制，抑制模型对安全指令的关注并将其锚定在对抗性图像特征上，诱导模型产生“安全盲视”。实验证明，该方法在Qwen-VL上实现了94.4%的攻击成功率，显著提升了攻击效率和鲁棒性，揭示了LVLM安全对齐机制的结构性缺陷。"
selection_source: fresh_fetch
motivation: 现有视觉越狱攻击由于对抗目标与模型内部安全检索机制存在严重的梯度冲突，导致攻击收敛缓慢且成功率不高。
method: 通过引入抑制安全前缀注意力和锚定对抗图像特征的双重辅助目标，直接操纵模型的注意力分配模式以绕过安全对齐。
result: "该方法在Qwen-VL上取得了94.4%的攻击成功率，相比基准方法减少了40%的迭代次数，并有效降低了45%的梯度冲突。"
conclusion: 攻击的成功源于一种“安全盲视”机制，即模型因注意力被劫持而未能检索到系统安全指令，而非直接推翻了安全规则。
---

## 摘要
大语言视觉模型（LVLMs）依赖于基于注意力的安全指令检索，以在生成过程中保持对齐。现有的攻击通常通过优化图像扰动来最大化有害输出的可能性，但由于对抗目标与模型的安全检索机制之间存在梯度冲突，导致收敛速度较慢。我们提出了注意力引导的视觉越狱（Attention-Guided Visual Jailbreaking），通过直接操纵注意力模式来规避而非强行压制安全对齐。我们的方法引入了两个简单的辅助目标：（1）抑制对对齐相关前缀标记（prefix tokens）的注意力；（2）将生成内容锚定在对抗性图像特征上。这种简单而有效的“推拉”公式将梯度冲突减少了 45%，在 Qwen-VL 上实现了 94.4% 的攻击成功率（相比基准的 68.8%），且迭代次数减少了 40%。在更严格的扰动预算（ε=8/255）下，我们的攻击成功率（ASR）保持在 59.0%，而标准方法为 45.7%。机理分析揭示了一种我们称之为“安全盲区”（safety blindness）的失效模式：成功的攻击将对系统提示词（system-prompt）的注意力抑制了 80%，导致模型生成有害内容的原因并非由于推翻了安全规则，而是因为未能检索到这些规则。

## Abstract
Large Vision-Language Models (LVLMs) rely on attention-based retrieval of safety instructions to maintain alignment during generation. Existing attacks typically optimize image perturbations to maximize harmful output likelihood, but suffer from slow convergence due to gradient conflict between adversarial objectives and the model's safety-retrieval mechanism. We propose Attention-Guided Visual Jailbreaking, which circumvents rather than overpowers safety alignment by directly manipulating attention patterns. Our method introduces two simple auxiliary objectives: (1) suppressing attention to alignment-relevant prefix tokens and (2) anchoring generation on adversarial image features. This simple yet effective push-pull formulation reduces gradient conflict by 45% and achieves 94.4% attack success rate on Qwen-VL (vs. 68.8% baseline) with 40% fewer iterations. At tighter perturbation budgets ($ε=8/255$), we maintain 59.0% ASR compared to 45.7% for standard methods. Mechanistic analysis reveals a failure mode we term safety blindness: successful attacks suppress system-prompt attention by 80%, causing models to generate harmful content not by overriding safety rules, but by failing to retrieve them.

---

## 论文详细总结（自动生成）

以下是对论文《Seeing No Evil: Blinding Large Vision-Language Models to Safety Instructions via Adversarial Attention Hijacking》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大语言视觉模型（LVLMs）在安全对齐方面的结构性脆弱。LVLM 的安全性依赖于生成过程中不断通过**注意力机制**检索系统提示词（System Prompt）中的安全指令。
*   **研究动机**：现有的视觉越狱攻击（如 VAE-JB）主要通过优化图像扰动来最大化有害输出的概率。然而，这种“仅针对输出”的方法会遇到**梯度冲突**（Gradient Conflict）——即对抗性目标产生的梯度与模型内部安全检索机制产生的梯度方向相反，导致攻击收敛慢、成功率受限。
*   **核心发现**：安全对齐并非一次性决策，而是每一步解码时的持续检索过程。如果能让模型在生成时“看不见”安全指令，就能实现更高效的越狱。

### 2. 方法论：注意力引导视觉越狱（Attention-Guided Visual Jailbreaking）
论文提出了一种“推拉式”（Push-Pull）注意力操纵机制，直接干预模型的内部注意力分配：
*   **核心思想**：不直接与安全规则对抗，而是通过操纵注意力图，使模型产生“安全盲视”（Safety Blindness），即在生成有害内容时无法检索到安全指令。
*   **关键技术细节**：
    1.  **抑制损失 ($L_{suppress}$)**：通过优化图像扰动，主动降低生成标记（Generated Tokens）对安全前缀标记（Prefix Tokens，如系统指令、角色标记）的注意力权重。
    2.  **锚定损失 ($L_{anchor}$)**：将从安全指令处“挤出”的注意力质量，重新定向并锚定到对抗性图像特征上。
    3.  **总目标函数**：$L_{total} = L_{target} + \alpha L_{suppress} + \beta L_{anchor}$。其中 $L_{target}$ 是标准的有害输出对数似然损失。
*   **算法流程**：在白盒环境下，使用投影梯度下降（PGD）更新图像扰动 $\delta$。该方法针对模型最后 $K$ 层（通常是最后 6 层）的注意力图进行干预，因为研究表明拒绝行为主要在深层触发。

### 3. 实验设计
*   **目标模型**：Qwen-VL, LLaVA-1.5-7B, InternVL2-8B, MiniGPT-4。
*   **数据集/基准**：
    *   **通用安全**：AdvBench, StrongREJECT, HarmBench, JailbreakBench。
    *   **多场景安全**：MM-SafetyBench（涵盖 13 种禁止场景，如非法活动、仇恨言论等）。
*   **对比方法**：VAE-JB (Qi et al.), BAP (Ying et al.), JB-Pcs (Shayegani et al.)。
*   **评估指标**：攻击成功率（ASR），由 Llama Guard 3、Detoxify 和 Beaver-dam-7B 作为自动评判员。

### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA H100 GPU**。
*   **实现细节**：为了访问内部注意力图，禁用了默认的 FlashAttention-2 内核，改用标准注意力实现。模型以 `bfloat16` 精度加载。
*   **优化成本**：单次迭代的计算开销增加不到 5%，但由于收敛更快（减少约 40% 的迭代次数），总体算力成本更低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主流基准测试和 4 种不同架构的模型，实验设计非常全面。
*   **消融实验**：详细测试了抑制损失与锚定损失的协同效应、干预层数的影响（$K$ 值）、损失权重（$\alpha, \beta$）的敏感性以及扰动预算（$\epsilon$）的缩放效应。
*   **鲁棒性与迁移性**：测试了图像变换（缩放、噪声、压缩等）下的稳定性，并验证了在 GPT-4o、Claude-3.5 和 Gemini-1.5 等闭源模型上的迁移攻击效果。
*   **因果分析**：通过在推理时人为干预注意力权重（反向补偿）来验证“安全盲视”的因果性。整体实验设计客观、严谨且充分。

### 6. 主要结论与发现
*   **性能卓越**：在 Qwen-VL 上达到 **94.4%** 的 ASR，远超基准方法的 68.8%。在严格的扰动预算下（$\epsilon=8/255$），依然保持 59.0% 的成功率。
*   **解决梯度冲突**：该方法将严重梯度冲突（余弦相似度 < -0.5）的比例降低了 **45%**，使优化过程更平滑、更快速。
*   **安全盲视机制**：成功的攻击使系统提示词的注意力下降了约 **80%**，图像注意力提升了 **4.1 倍**。模型生成有害内容是因为它“忘记”了去查看安全规则。
*   **因果性证实**：实验证明，仅通过在推理时恢复对系统提示词的注意力，就能中和对抗性图像的越狱效果。

### 7. 优点（亮点）
*   **机制创新**：从“操纵输出”转向“操纵机制”，揭示了注意力检索是 LVLM 安全对齐的阿喀琉斯之踵。
*   **效率极高**：通过减少梯度冲突，显著降低了对抗样本的优化难度和所需迭代次数。
*   **可解释性强**：通过注意力图的可视化和因果干预实验，清晰地展示了攻击为何奏效。

### 8. 不足与局限
*   **白盒依赖**：核心方法依赖于获取模型内部梯度和注意力图，虽然有迁移性，但在纯黑盒环境下的直接应用受限。
*   **前缀依赖性**：该方法假设模型依赖前缀标记进行对齐。如果未来模型将安全对齐完全融入参数权重而非上下文检索，该方法的效力可能会减弱。
*   **超参数敏感性**：虽然论文给出了通用配置，但针对不同架构的模型，可能仍需微调损失权重以达到最优效果。

（完）
