---
title: Preventing Safety Drift in Large Language Models via Coupled Weight and Activation Constraints
title_zh: 通过耦合权重与激活约束防止大语言模型中的安全漂移
authors: "Songping Peng, Zhiheng Zhang, Daojian Zeng, Lincheng Jiang, Xieping Gao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12384v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过安全对齐保护减轻越狱漏洞
tldr: 针对大语言模型在微调过程中容易出现的安全对齐失效问题，本文提出了耦合权重与激活约束（CWAC）方法。该方法通过理论证明了单一约束的局限性，并结合预计算的权重安全子空间与基于稀疏自编码器的激活特征正则化，在保留模型下游任务性能的同时，有效防止了安全漂移。实验证明 CWAC 在多种模型和任务中均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的安全防御方法通常孤立地约束权重或激活值，难以应对微调过程中良性适配导致的安全对齐退化。
method: 提出 CWAC 方法，同步实施权重更新的安全子空间约束，并利用稀疏自编码器对安全关键特征进行针对性正则化。
result: 在四种主流 LLM 上的实验显示，CWAC 在保持微调准确率的同时，显著降低了有害评分，表现优于强基准模型。
conclusion: 耦合权重与激活约束是保障 LLM 在微调期间安全稳定性的关键，为稳健的安全对齐提供了新思路。
---

## 摘要
大语言模型（LLMs）的安全对齐在微调过程中仍然非常脆弱，即使是良性的适配也可能削弱预训练的拒绝行为并引发有害响应。现有的防御方法通常孤立地约束权重或激活，而没有考虑它们对安全性的耦合影响。在本文中，我们首先从理论上证明，仅约束权重或激活不足以维持安全性。为了稳健地保持安全对齐，我们提出了耦合权重与激活约束（CWAC），这是一种新颖的方法，它同时在权重更新上强制执行预计算的安全子空间，并对由稀疏自编码器识别出的安全关键特征应用针对性正则化。在四种广泛使用的 LLM 和多种下游任务上的广泛实验表明，CWAC 始终以对微调准确率最小的影响实现最低的有害评分，即使在高有害数据比例下也显著优于强基准方法。

## Abstract
Safety alignment in Large Language Models (LLMs) remains highly fragile during fine-tuning, where even benign adaptation can degrade pre-trained refusal behaviors and enable harmful responses. Existing defenses typically constrain either weights or activations in isolation, without considering their coupled effects on safety. In this paper, we first theoretically demonstrate that constraining either weights or activations alone is insufficient for safety preservation. To robustly preserve safety alignment, we propose Coupled Weight and Activation Constraints (CWAC), a novel approach that simultaneously enforces a precomputed safety subspace on weight updates and applies targeted regularization to safety-critical features identified by sparse autoencoders. Extensive experiments across four widely used LLMs and diverse downstream tasks show that CWAC consistently achieves the lowest harmful scores with minimal impact on fine-tuning accuracy, substantially outperforming strong baselines even under high harmful data ratios.

---

## 论文详细总结（自动生成）

### 论文总结：通过耦合权重与激活约束防止大语言模型中的安全漂移 (CWAC)

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）在部署前通常经过安全对齐（如 RLHF、DPO），但这种对齐在下游任务微调时极其脆弱。即使是使用完全良性的数据进行微调，模型也可能发生“安全漂移”，导致原本的拒绝行为失效，从而产生有害输出。
**研究动机**：现有的防御方法通常只针对权重（Weight）或激活（Activation）进行单一维度的约束。作者认为，单一维度的约束不足以封堵所有的“绕过路径”：仅约束权重无法阻止内部激活模式的改变，而仅约束激活则无法限制权重漂移带来的潜在风险。

#### 2. 论文提出的方法论：CWAC
论文提出了**耦合权重与激活约束（CWAC）**框架，旨在同时从参数空间和表征空间锁定安全性。
*   **核心思想**：通过理论推导证明安全漂移可分解为权重漂移和激活漂移的乘积项，因此必须同时约束两者。
*   **关键技术细节**：
    *   **权重安全子空间约束**：在微调前，利用有害输入在模型投影层产生的协方差矩阵进行奇异值分解（SVD），识别出对安全性至关重要的低方差方向，构建“安全子空间”。在微调过程中，将权重梯度投影到该子空间内，确保更新不会干扰模型对有害指令的原始拒绝逻辑。
    *   **激活安全约束**：利用**稀疏自编码器（SAE）**从模型的残差流中提取具有可解释性的“安全关键特征”（如负责拒绝行为的神经元组合）。在微调时，引入正则化项，最小化当前模型与原始模型在处理有害提示词时这些关键特征的差异。
*   **算法流程**：在每个训练批次中，同时计算任务损失（Task Loss）和激活安全损失（Safety Loss），并在更新参数前对梯度进行子空间投影。

#### 3. 实验设计
*   **实验模型**：Llama-2-7B-chat, Llama-3-8B-Instruct, Mistral-7B-Instructv0.3, Gemma-2-9B-IT。
*   **数据集/场景**：
    *   **微调任务**：SST-2（情感分析）、AGNEWS（新闻分类）、GSM8K（数学推理）、PubMedQA（医疗问答）、Alpaca（通用指令遵循）。
    *   **安全评估**：整合了 JailbreakBench、HarmBench、AdvBench 和 BeaverTails 的有害提示词。
*   **Benchmark 与对比方法**：
    *   基准包括：标准 SFT、Lisa、BEA、ASFT（锚定安全微调）、SafeInstr、SPPFT（冻结安全层）。
*   **评估指标**：微调准确率（FA）、有害评分（HS）、AlpacaEval 评分（AE）。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A800 GPU**。
*   **训练时长**：
    *   SAE 离线训练：约 6.5 小时。
    *   SVD 预计算（以 Llama-2-7B 为例）：约 18 分钟。
    *   微调开销：相比标准 SFT，CWAC 的额外计算开销小于 10%（每轮约 44-46 分钟，SFT 为 42 分钟）。
*   **显存**：显存占用略有增加（从 44.2 GB 增加到 46 GB）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流架构模型、5 个下游任务、多种有害数据混合比例（从 5% 到 50%）以及不同的学习率设置。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了方法在良性微调下的安全性，还测试了在“数据投毒”（含有害数据）情况下的鲁棒性。此外，通过消融实验分别验证了权重约束和激活约束的独立贡献及其耦合后的增益，证明了双层防御的必要性。

#### 6. 论文的主要结论与发现
*   **单一约束的局限性**：理论和实验均证明，仅约束权重或仅约束激活都无法完全防止安全对齐的崩溃。
*   **CWAC 的优越性**：在所有测试模型和任务中，CWAC 均实现了最低的有害评分，同时保持了与标准 SFT 相当甚至更高的微调准确率。
*   **鲁棒性**：CWAC 对学习率的变化不敏感，且在训练数据包含高达 50% 的有害内容时，依然能有效保持模型的拒绝行为。

#### 7. 优点
*   **理论支撑强**：通过数学分解明确了安全漂移的来源，为方法论提供了坚实基础。
*   **双层防御机制**：结合了静态的权重投影和动态的激活正则化，提供了互补的保护。
*   **实用性高**：虽然引入了 SAE 和 SVD，但大部分是离线一次性计算，微调阶段的额外开销极低，易于集成到现有管线中。

#### 8. 不足与局限
*   **白盒限制**：该方法需要访问模型的内部权重和激活值，因此无法直接应用于闭源 API 模型（如 GPT-4）。
*   **SAE 依赖性**：防御效果高度依赖于稀疏自编码器的训练质量，识别“安全关键特征”需要高质量的 SAE。
*   **模型规模覆盖**：实验主要集中在 7B 到 9B 参数规模的模型，尚未在超大规模模型（如 70B+）上验证其扩展性。
*   **对抗复杂性**：主要针对显式的有害提示词，对于极其隐蔽或复杂的间接越狱攻击（Jailbreak）的防御能力仍需进一步探索。

（完）
