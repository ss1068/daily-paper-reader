---
title: "Every Picture Tells a Dangerous Story: Memory-Augmented Multi-Agent Jailbreak Attacks on VLMs"
title_zh: 每张图片都讲述着一个危险的故事：针对视觉语言模型（VLMs）的记忆增强多智能体越狱攻击
authors: "Jianhao Chen, Haoyang Chen, Hanjie Zhao, Haozhe Liang, Tieyun Qian"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12616v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对视觉语言模型的多智能体越狱攻击
tldr: 本研究针对视觉语言模型（VLM）在处理自然图像语义时的安全漏洞，提出了MemJack框架。该框架利用多智能体协作，通过视觉语义伪装和迭代零空间投影（INLP）技术，将恶意意图映射到图像实体中。通过引入多模态经验记忆，MemJack能够跨图像积累并迁移攻击策略，显著提升了多轮交互下的攻击成功率。此外，研究还发布了包含11.3万条攻击轨迹的MemJack-Bench数据集，为提升VLM的鲁棒性提供了重要基础。
selection_source: fresh_fetch
motivation: 现有的多模态越狱攻击多关注像素扰动或文字攻击，忽视了自然图像中深层语义结构所蕴含的安全风险。
method: 提出MemJack框架，结合多智能体协作、视觉语义伪装、INLP几何过滤及多模态经验记忆，实现自动化的语义级越狱攻击。
result: "在未经修改的COCO数据集上，MemJack对Qwen3-VL-Plus的攻击成功率达到71.48%，在增加预算的情况下可提升至90%。"
conclusion: 该研究揭示了VLM在语义层面的严重脆弱性，并通过发布大规模攻击轨迹数据集，为未来多模态防御对齐研究奠定了基础。
---

## 摘要
视觉语言模型（VLMs）的飞速演进催生了人工智能领域前所未有的能力；然而，这种持续的模态扩张也无意中暴露了一个极度拓宽且不受约束的对抗性攻击面。目前的跨模态越狱策略主要集中在表层像素扰动、排版攻击或有害图像上；然而，它们未能触及视觉数据固有的复杂语义结构。这使得原始自然图像中广阔的语义攻击面在很大程度上未被审视。为了揭示这些深层的语义漏洞，我们提出了 MemJack，这是一个记忆增强的多智能体越狱攻击框架，它显式地利用视觉语义来编排自动化的越狱攻击。MemJack 采用协同的多智能体合作，动态地将视觉实体映射到恶意意图，通过多角度的视觉语义伪装生成对抗性提示词，并利用迭代零空间投影（INLP）几何滤波器来绕过潜在空间中的过早拒绝。通过持久化的多模态经验记忆积累和迁移成功策略，MemJack 在不同图像之间保持了高度连贯的扩展多轮越狱攻击交互，从而提高了对新图像的攻击成功率（ASR）。在完整的、未经修改的 COCO val2017 图像集上进行的广泛实证评估表明，MemJack 对 Qwen3-VL-Plus 实现了 71.48% 的 ASR，在增加预算的情况下可扩展至 90%。此外，为了促进未来的防御对齐研究，我们将发布 MemJack-Bench，这是一个包含超过 113,000 条交互式多模态越狱攻击轨迹的综合数据集，为开发本质鲁棒的 VLM 奠定了重要基础。

## Abstract
The rapid evolution of Vision-Language Models (VLMs) has catalyzed unprecedented capabilities in artificial intelligence; however, this continuous modal expansion has inadvertently exposed a vastly broadened and unconstrained adversarial attack surface. Current multimodal jailbreak strategies primarily focus on surface-level pixel perturbations and typographic attacks or harmful images; however, they fail to engage with the complex semantic structures intrinsic to visual data. This leaves the vast semantic attack surface of original, natural images largely unscrutinized. Driven by the need to expose these deep-seated semantic vulnerabilities, we introduce \textbf{MemJack}, a \textbf{MEM}ory-augmented multi-agent \textbf{JA}ilbreak atta\textbf{CK} framework that explicitly leverages visual semantics to orchestrate automated jailbreak attacks. MemJack employs coordinated multi-agent cooperation to dynamically map visual entities to malicious intents, generate adversarial prompts via multi-angle visual-semantic camouflage, and utilize an Iterative Nullspace Projection (INLP) geometric filter to bypass premature latent space refusals. By accumulating and transferring successful strategies through a persistent Multimodal Experience Memory, MemJack maintains highly coherent extended multi-turn jailbreak attack interactions across different images, thereby improving the attack success rate (ASR) on new images. Extensive empirical evaluations across full, unmodified COCO val2017 images demonstrate that MemJack achieves a 71.48\% ASR against Qwen3-VL-Plus, scaling to 90\% under extended budgets. Furthermore, to catalyze future defensive alignment research, we will release \textbf{MemJack-Bench}, a comprehensive dataset comprising over 113,000 interactive multimodal jailbreak attack trajectories, establishing a vital foundation for developing inherently robust VLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Every Picture Tells a Dangerous Story: Memory-Augmented Multi-Agent Jailbreak Attacks on VLMs》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了视觉语言模型（VLM）在面对**原始、良性自然图像**时的安全脆弱性。
*   **研究动机**：现有的越狱攻击（Jailbreak）多集中在像素级扰动、排版攻击或直接输入有害图像。然而，VLM 强大的跨模态推理能力使得攻击者可以利用看似无害的自然图像作为“语义锚点”，通过复杂的语义伪装诱导模型生成违规内容。
*   **核心问题**：如何系统性地挖掘自然图像中的深层语义漏洞，并实现跨图像、跨模型的自动化越狱攻击？

### 2. 方法论：MemJack 框架
MemJack 是一个记忆增强的多智能体协作框架，其核心流程分为三个阶段：
*   **阶段 1：漏洞分析（Vulnerability Planner）**：由一个 VLM 智能体识别图像中的视觉锚点（如刀具、办公环境等），并将其映射到潜在的恶意意图（如暴力、侵犯隐私）。
*   **阶段 2：视觉语义伪装（Iterative Attack Agent）**：利用 6 种攻击视角（如视觉直觉关联、第一人称角色扮演、假设性推理等）生成对抗性提示词。
    *   **关键技术：INLP 几何过滤器**：利用迭代零空间投影（Iterative Nullspace Projection）在潜在空间中筛选提示词，剔除那些容易触发模型预设拒绝机制的候选词，减少无效查询。
*   **阶段 3：反思与动态重规划（Evaluation & Feedback Agent）**：评估模型响应，若失败则分析防御模式，调整攻击视角或更换视觉锚点。
*   **核心组件：经验驱动记忆模块**：
    *   **多模态经验记忆**：存储成功的攻击策略，通过向量检索实现跨图像的策略迁移。
    *   **越狱知识图谱**：记录锚点、策略与防御之间的因果关系，指导后续攻击。

### 3. 实验设计
*   **数据集**：
    *   核心评估：**COCO val2017**（5,000 张未经修改的自然图像）。
    *   泛化性测试：AdvBench-M, MM-SafetyBench, SIUO, FigStep, VLBreakBench 等 7 个基准数据集。
*   **目标模型**：涵盖 11 种主流 VLM，包括商业 API（Qwen3-VL-Plus, Gemini-3-Flash, GPT-5-Mini, Claude-Haiku-4.5 等）和开源模型（Llama-3.2-11B, GLM-4.6-V 等）。
*   **对比方法（Benchmark）**：
    *   文本攻击：GCG, AutoDAN-Turbo。
    *   多模态攻击：Visual-Adv, FigStep, HADES, QR-Attack。

### 4. 资源与算力
*   **模型支持**：使用 Qwen3-VL-8B-Instruct 作为攻击智能体的骨干模型，Qwen3-VL-Embedding-8B 用于 INLP 过滤和记忆检索。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但考虑到对 5,000 张图像进行每张多达 20 轮的迭代攻击，且涉及多个大模型的推理，该实验对推理算力的需求较高。

### 5. 实验数量与充分性
*   **实验规模**：在 5,000 张 COCO 图像上进行了大规模测试，并生成了包含 **113,000 条**交互轨迹的 **MemJack-Bench** 数据集。
*   **充分性**：实验设计非常充分。包含了跨模型测试（11 种模型）、跨数据集测试（8 个来源）、消融实验（验证记忆、反思、重规划的作用）以及对攻击视角和防御模式的定性分析。
*   **客观性**：通过自动化评测工具（Qwen3Guard-Gen）进行量化评分，并对比了白盒与黑盒环境下的表现，实验流程较为客观公平。

### 6. 主要结论与发现
*   **自然图像的危险性**：即使是完全良性的自然图像，在精心设计的语义伪装下也能成为越狱工具。MemJack 在 Qwen3-VL-Plus 上达到了 **71.48%** 的攻击成功率（ASR），在增加预算时可达 **90%**。
*   **记忆的价值**：引入记忆模块后，ASR 提升了 34%，且平均攻击轮数显著下降，证明了攻击策略是可以跨图像迁移和积累的。
*   **防御洞察**：研究发现“良性重构（Benign reframing）”是最难被越狱攻击突破的防御模式，而直接拒绝则容易被绕过。

### 7. 优点
*   **视角新颖**：跳出了传统的像素扰动思路，专注于挖掘自然图像的“语义攻击面”。
*   **自动化程度高**：通过多智能体协作实现了从锚点发现到策略调整的全自动化。
*   **贡献显著**：发布的 MemJack-Bench 数据集规模巨大且包含丰富的元数据（锚点、视角、防御模式），对未来 VLM 的安全对齐具有极高价值。
*   **技术融合**：巧妙结合了几何过滤（INLP）和强化学习思想（经验记忆）。

### 8. 不足与局限
*   **计算开销**：作为一种迭代式攻击，MemJack 需要多轮模型交互，查询成本和时间成本较高，不适合实时攻击场景。
*   **伦理风险**：论文虽然包含免责声明，但这种高效的自动化越狱工具若被滥用，可能产生严重的社会负面影响。
*   **防御依赖**：攻击效果部分依赖于攻击者所使用的骨干模型（如 Qwen3）的能力，若骨干模型推理能力受限，攻击效率可能下降。

（完）
