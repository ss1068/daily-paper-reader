---
title: "WebAgentGuard: A Reasoning-Driven Guard Model for Detecting Prompt Injection Attacks in Web Agents"
title_zh: WebAgentGuard：一种用于检测 Web 智能体中提示词注入攻击的推理驱动型防护模型
authors: "Yulin Chen, Tri Cao, Haoran Li, Yue Liu, Yibo Li, Yufei He, Le Minh Khoi, Yangqiu Song, Shuicheng Yan, Bryan Hooi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12284v1"
tags: ["query:llm"]
score: 10.0
evidence: 用于检测Web智能体提示注入的推理驱动防护模型
tldr: 针对基于视觉语言模型（VLM）的Web智能体易受提示注入攻击的问题，本文提出WebAgentGuard防御框架。该框架通过引入一个并行的推理驱动多模态防护模型，将攻击检测与智能体任务执行解耦。利用GPT-5生成的合成数据集进行强化学习训练，该模型在保持智能体性能且不增加延迟的前提下，显著提升了对恶意指令的检测能力。
selection_source: fresh_fetch
motivation: 现有的系统提示词防御和直接微调方法难以有效应对Web智能体面临的复杂多模态提示注入攻击。
method: 提出一种与Web智能体并行的推理驱动多模态防护模型，通过大规模合成数据进行监督微调和强化学习训练。
result: 实验表明WebAgentGuard在多个基准测试中均优于强基线模型，且在不增加额外延迟的情况下保持了智能体的原有功能。
conclusion: 将检测逻辑与执行逻辑解耦的并行防护机制是提升Web智能体安全性的有效途径。
---

## 摘要
由视觉语言模型 (VLM) 驱动的 Web 智能体通过感知并处理网页的视觉和文本内容，实现与 Web 环境的自主交互以完成用户指定的任务。然而，这些智能体极易受到提示词注入攻击的威胁，嵌入在 HTML 或渲染截图中的对抗性指令可以操纵智能体行为，导致信息泄露等有害后果。现有的防御手段（如系统提示词防御和直接对智能体进行微调）效果有限。为此，我们提出了一种防御框架，使 Web 智能体与专门的防护智能体并行运行，从而将提示词注入检测与智能体自身的推理过程解耦。基于此框架，我们推出了 WebAgentGuard，这是一种用于提示词注入检测的推理驱动型多模态防护模型。我们利用 GPT-5 构建了一个涵盖 164 个主题和 230 种视觉及 UI 设计风格的合成多模态数据集，并通过推理密集型有监督微调及随后的强化学习对模型进行训练。在多个基准测试上的实验表明，WebAgentGuard 在不引入额外延迟的情况下，在保持智能体效用的同时，性能持续优于强基准模型。

## Abstract
Web agents powered by vision-language models (VLMs) enable autonomous interaction with web environments by perceiving and acting on both visual and textual webpage content to accomplish user-specified tasks. However, they are highly vulnerable to prompt injection attacks, where adversarial instructions embedded in HTML or rendered screenshots can manipulate agent behavior and lead to harmful outcomes such as information leakage. Existing defenses, including system prompt defenses and direct fine-tuning of agents, have shown limited effectiveness. To address this issue, we propose a defense framework in which a web agent operates in parallel with a dedicated guard agent, decoupling prompt injection detection from the agent's own reasoning. Building on this framework, we introduce WebAgentGuard, a reasoning-driven, multimodal guard model for prompt injection detection. We construct a synthetic multimodal dataset using GPT-5 spanning 164 topics and 230 visual and UI design styles, and train the model via reasoning-intensive supervised fine-tuning followed by reinforcement learning. Experiments across multiple benchmarks show that WebAgentGuard consistently outperforms strong baselines while preserving agent utility, without introducing additional latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **WebAgentGuard** 的防御框架和模型，旨在解决 Web 智能体（Web Agents）面临的提示词注入（Prompt Injection）攻击问题。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于视觉语言模型（VLM）的 Web 智能体在处理网页时，容易受到嵌入在 HTML 代码或截图中的恶意指令（提示词注入）的误导，导致隐私泄露或执行有害操作。
*   **研究背景**：现有的防御手段（如修改系统提示词或直接微调智能体）效果不佳，攻击成功率往往超过 80%。其根本原因在于智能体的“任务执行推理”与“安全检测推理”高度耦合，智能体往往优先遵循页面指令而忽略了安全性。
*   **核心思路**：提出**解耦防御**，即引入一个专门的、并行的“防护模型”（Guard Model），在智能体思考下一步行动的同时，独立判断当前环境是否存在注入攻击。

### 2. 方法论：核心思想与关键技术
*   **并行防护框架**：防护模型与 Web 智能体并行运行。在每个步骤中，防护模型输出一个二元信号（允许/拒绝）。如果拒绝，则触发人工审核，从而在不中断智能体流水线的情况下确保安全。
*   **推理驱动检测**：WebAgentGuard 不仅仅输出“是/否”，而是被训练为先生成详细的推理链（Reasoning Traces），分析网页内容与用户意图是否存在冲突，再给出最终结论。
*   **数据生成流水线**：
    *   利用 **GPT-5** 生成了涵盖 164 个主题和 230 种 UI 风格的合成网页。
    *   构建配对数据集：良性网页 vs. 注入了对抗指令的恶意网页。
    *   利用 GPT-5 为训练集生成“思维链”（CoT）标注。
*   **两阶段训练流程**：
    1.  **冷启动 SFT（有监督微调）**：在推理密集型数据集上微调 VLM 骨干模型，使其学会使用 `<think>` 和 `<answer>` 标签。
    2.  **RL 训练（强化学习）**：使用 **GRPO（群体相对策略优化）** 算法。通过规则奖励函数（格式正确且答案正确则给高分）进一步优化模型的推理能力和对安全模板的遵循度。

### 3. 实验设计
*   **数据集/场景**：
    *   **域内（In-domain）**：作者自建的合成评估集。
    *   **域外（Out-of-domain）**：VPI-Bench、EIA 评估集，以及在 SimilarWeb 排名靠前的真实网站上模拟的 PopUp（弹窗）攻击。
*   **Benchmark 与对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4.1, GPT-4o-Mini。
    *   **开源指令模型**：Qwen2.5-VL, Qwen3-VL, Llama-3.2-Vision。
    *   **现有防护模型**：Llama-Guard-3-Vision, Prompt-Guard, GuardReasoner-VL。
*   **评估指标**：准确率（Acc）、召回率（Recall）、精确率（Prec）、F1 分数，以及集成到智能体后的攻击成功率（ASR）和任务成功率（Utility）。

### 4. 资源与算力
*   **算力设备**：
    *   **SFT 阶段**：使用 2 台 NVIDIA **H200** GPU。
    *   **GRPO 阶段**：使用 4 台 NVIDIA **H200** GPU。
*   **训练细节**：SFT 和 RL 阶段各训练 3 个 epoch。推理阶段使用 vLLM 引擎加速。
*   **延迟表现**：WebAgentGuard-4B 的推理时间约为 2.15s，8B 版本约为 3.24s，均低于主流 Web 智能体（如 Claude 或 Gemini）生成动作所需的 5-7s，因此不会增加系统总延迟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 8B 不同参数规模的模型，并在 4 个不同的基准数据集上进行了测试。
*   **充分性与客观性**：
    *   进行了**消融实验**，验证了 SFT 和 RL 各自对性能的贡献。
    *   进行了**效用实验**（WebArena），证明了引入防护模型对智能体完成正常任务的影响极小。
    *   进行了**对抗性实验**，测试了当攻击者专门针对防护模型进行注入（如要求防护模型忽略之前指令）时的鲁棒性。
    *   实验设计考虑了真实场景，将模型集成到了 Claude 和 Gemini 驱动的实际智能体框架中进行验证。

### 6. 主要结论与发现
*   **检测性能卓越**：WebAgentGuard-8B 在多个基准测试中达到了 90% 以上的召回率，远超现有的通用防护模型。
*   **防御实效显著**：集成该模型后，Web 智能体的受攻击成功率（ASR）在多数场景下可降至 0%。
*   **推理的重要性**：实验证明，先推理再输出结论（Reasoning-driven）能显著提升模型识别复杂、隐蔽注入攻击的能力。
*   **RL 的增益**：GRPO 强化学习能有效纠正模型在 SFT 后仍存在的格式错误，并提升泛化性能。

### 7. 优点
*   **解耦设计**：通过并行运行防护模型，解决了安全与任务之间的冲突，且不牺牲执行效率。
*   **高质量合成数据**：利用 GPT-5 生成了极具多样性的多模态数据集，弥补了真实世界攻击数据不足的问题。
*   **低延迟**：模型规模适中，推理速度快于主智能体，具有极强的工程落地价值。

### 8. 不足与局限
*   **白盒攻击风险**：论文提到未考虑攻击者已知防护模型参数的“白盒攻击”情况。
*   **推理遗忘问题**：案例分析显示，模型有时在推理初期识别到了攻击，但在推理后期“忘记”了该信号，导致最终判断错误。
*   **计算开销**：虽然不增加延迟，但并行运行一个 8B 模型仍需额外的显存和计算资源。
*   **合成数据偏差**：尽管数据多样，但合成网页与极其复杂的真实动态网页之间可能仍存在分布差异。

（完）
