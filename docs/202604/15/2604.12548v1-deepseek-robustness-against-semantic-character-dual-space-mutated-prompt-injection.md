---
title: DeepSeek Robustness Against Semantic-Character Dual-Space Mutated Prompt Injection
title_zh: DeepSeek 对语义-字符双空间变异提示词注入的鲁棒性研究
authors: "Junyu Ren, Xingjian Pan, Wensheng Gan, Philip S. Yu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12548v1"
tags: ["query:llm"]
score: 10.0
evidence: 提示注入漏洞与鲁棒性
tldr: 本研究针对大语言模型（如DeepSeek）面临的提示注入威胁，指出单一维度的攻击策略难以模拟真实复杂场景。为此，作者提出了PromptFuzz-SC框架，通过结合语义转换与字符级混淆的双空间变异策略，并利用混合搜索算法生成对抗性提示。实验证明，该方法在攻击成功率和隐蔽性上均优于单空间策略，为评估和提升国产大模型的安全性提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的提示注入研究多关注单一维度的攻击，缺乏对语义和字符双空间复合扰动下大模型（尤其是DeepSeek）鲁棒性的系统评估。
method: 提出PromptFuzz-SC框架，集成语义与字符变异算子库，并采用epsilon-greedy与爬山算法相结合的混合策略搜索高质量对抗提示。
result: 实验显示双空间变异在DeepSeek上取得了最高的平均攻击成功率（0.189）和峰值成功率（0.375），且在隐蔽性方面表现优异。
conclusion: 复合变异策略在红队测试中比单一策略更具威胁性，研究结果强调了构建多层防御机制以应对复杂提示注入攻击的必要性。
---

## 摘要
提示词注入已成为大语言模型（LLMs）面临的关键安全威胁，然而现有研究主要集中在单维度攻击策略上，如语义重写或字符级混淆，这无法捕捉现实场景中多空间扰动的综合效应。此外，针对近期中文大语言模型（如 DeepSeek）的系统性黑盒鲁棒性评估仍然有限。为了填补这些空白，我们提出了 PromptFuzz-SC，这是一个用于评估大语言模型对提示词注入鲁棒性的语义-字符双空间变异框架。该框架集成了语义转换（如改写和词序扰动）与字符级混淆（如零宽字符插入和基于编码的变异），形成了一个统一且可扩展的变异算子库。采用结合了 epsilon-greedy 探索和爬山算法优化的混合搜索策略，以高效发现高质量的对抗性提示词。我们进一步引入了基于三个指标的统一评估协议：滥用成功率（MSR）、平均成功查询次数（AQS）和隐蔽性（Stealth）。在 DeepSeek 上的实验结果表明，在所评估的策略中，双空间变异实现了最强的综合攻击性能，达到了最高的平均 MSR (0.189)、峰值 MSR (0.375) 和平均隐蔽性。与仅语义变异和仅字符变异相比，其平均 MSR 分别提高了 12.5% 和 5.6%。虽然该方法并不总能最小化查询成本，但它实现了具有竞争力的最佳情况效率，并保持了较强的不可感知性，表明在攻击有效性和隐蔽性之间取得了更好的平衡。这些发现强调了复合变异策略对于大语言模型鲁棒红队测试的重要性，并为多层防御机制的设计提供了实践见解。

## Abstract
Prompt injection has emerged as a critical security threat to large language models (LLMs), yet existing studies predominantly focus on single-dimensional attack strategies, such as semantic rewriting or character-level obfuscation, which fail to capture the combined effects of multi-space perturbations in realistic scenarios. In addition, systematic black-box robustness evaluations of recent Chinese LLMs, such as DeepSeek, remain limited. To address these gaps, we propose PromptFuzz-SC, a semantic-character dual-space mutation framework for evaluating LLM robustness against prompt injection. The framework integrates semantic transformations (e.g., paraphrasing and word-order perturbation) with character-level obfuscation (e.g., zero-width insertion and encoding-based mutation), forming a unified and extensible mutation operator library. A hybrid search strategy combining epsilon-greedy exploration and hill-climbing refinement is adopted to efficiently discover high-quality adversarial prompts. We further introduce a unified evaluation protocol based on three metrics: misuse success rate (MSR), Average Queries to Success (AQS), and Stealth. Experimental results on DeepSeek demonstrate that dual-space mutation achieves the strongest overall attack performance among the evaluated strategies, attaining the highest mean MSR (0.189), peak MSR (0.375), and mean Stealth. Compared with semantic-only and character-only mutation, it improves mean MSR by 12.5% and 5.6%, respectively. While not consistently minimizing query cost, the proposed method achieves competitive best-case efficiency and maintains strong imperceptibility, indicating a more favorable balance between attack effectiveness and concealment. These findings highlight the importance of composite mutation strategies for robust red-teaming of LLMs and provide practical insights for the design of multi-layer defense mechanisms.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeepSeek Robustness Against Semantic-Character Dual-Space Mutated Prompt Injection》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文探讨了大语言模型（LLM）在面对“提示词注入”（Prompt Injection）攻击时的安全性，特别是针对近期备受关注的国产模型 **DeepSeek**。
*   **研究动机**：
    *   **维度单一**：现有的攻击研究多集中在单一维度（如仅语义重写或仅字符混淆），难以模拟现实中复杂的复合攻击。
    *   **评估空白**：针对 DeepSeek 等国产模型在黑盒环境下的系统性鲁棒性评估相对匮乏。
    *   **防御挑战**：模型可能在标准输入下表现安全，但在非标准输入（如带噪音、特殊编码、语序扰动）下存在安全边界被突破的风险。

### 2. 论文提出的方法论：PromptFuzz-SC 框架
*   **核心思想**：构建一个**语义-字符双空间变异引擎**，通过自动化搜索策略寻找能够绕过安全对齐机制的对抗性提示词。
*   **关键技术细节**：
    *   **双空间变异引擎**：
        *   **语义空间（Semantic Space）**：包含同义词改写（147组词库）、风格转换、上下文扩展（伪造安全审计场景）及模板重组。
        *   **字符空间（Character Space）**：包含零宽字符插入（ZWJ/ZWSP）、Leetspeak 转换（如 a→4）、Base64 编码及随机空格注入。
    *   **混合搜索策略**：
        *   **$\epsilon$-greedy 探索**：以 $\epsilon$ 概率随机选择变异算子进行全局探索，以 $1-\epsilon$ 概率利用历史表现最优的算子。
        *   **爬山算法（Hill-climbing）**：一旦发现成功样本，立即在其邻域内进行局部精细化变异，以提升攻击质量或隐蔽性。
    *   **评估指标体系**：
        *   **MSR（滥用成功率）**：成功攻击次数与总查询次数之比。
        *   **AQS（平均成功查询数）**：衡量攻击效率，数值越小效率越高。
        *   **Stealth（隐蔽性）**：基于归一化最长公共子序列（LCS）计算变异提示词与原提示词的相似度。

### 3. 实验设计
*   **目标模型**：DeepSeek 系列模型（黑盒 API 形式）。
*   **数据集/种子库**：包含 50 个初始种子提示词（Seed Prompts），涵盖多种违规意图。
*   **对比方法（Benchmark）**：
    *   **Semantic-only**：仅使用语义维度变异。
    *   **Character-only**：仅使用字符维度变异。
    *   **Dual-space（本文方法）**：语义与字符双空间协同变异。
*   **变量控制**：设置了不同的温度（T=0.3, 0.7）和最大生成长度（L=256, 512）组合，以及不同的查询预算（最高 200 次）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**使用的具体 GPU 型号或本地训练时长。
*   **实现细节**：由于是黑盒评估，实验主要通过 Python 的 `asyncio` 异步框架调用 DeepSeek API 完成。文中提到设置了并发级别 $C=8$ 以遵守 API 速率限制并优化资源利用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三种策略（语义、字符、双空间）分别在 4 种参数配置（T 与 L 的组合）下进行了详尽测试。
    *   查询预算从 25 逐步增加至 200，绘制了动态性能曲线。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅对比了成功率，还深入分析了参数敏感性（如温度对字符变异的影响）以及攻击随预算增加的演化趋势，提供了消融实验性质的对比数据。

### 6. 主要结论与发现
*   **双空间最强**：双空间变异在所有策略中表现最优，平均 MSR 达到 0.189，峰值 MSR 达 0.375。相比单语义和单字符策略，平均成功率分别提升了 **12.5%** 和 **5.6%**。
*   **隐蔽性优势**：双空间攻击在提升成功率的同时，保持了最高的平均隐蔽性得分（0.859），证明其生成的攻击载荷更难被简单的规则过滤。
*   **参数敏感性**：
    *   **字符变异**对低温环境（T=0.3）更敏感，容易产生早期爆发式成功。
    *   **语义变异**容易遇到瓶颈，随着预算增加，成功率提升趋于平缓甚至下降（语义漂移）。
*   **协同效应**：成功的攻击样本中，语义与字符算子的比例约为 3:2，证明了跨空间组合能有效绕过模型的多层防御。

### 7. 优点（亮点）
*   **多维度视角**：打破了以往单一维度的攻击建模，提出了更符合实战的“双空间”概念。
*   **插件化设计**：变异算子库采用统一接口，具有极强的可扩展性，易于集成新的攻击向量。
*   **评估全面**：引入 Stealth 指标，将“攻击有效性”与“不可感知性”结合评估，更具红队测试参考价值。
*   **针对性强**：填补了 DeepSeek 模型在复合扰动下安全边界研究的空白。

### 8. 不足与局限
*   **预算限制**：实验的最大查询预算设定为 200，对于某些需要长程诱导的自适应攻击，这一预算可能不足以观察到收敛。
*   **模态局限**：目前仅限于文本模态，未涉及 DeepSeek 多模态模型（如视觉-语言注入）。
*   **防御对抗不足**：实验主要针对模型内置的对齐机制，未深入测试针对性的第三方防火墙或专门的检测插件。
*   **样本多样性**：虽然使用了 50 个种子，但对于极其复杂的长文本注入场景，样本覆盖面仍有提升空间。

（完）
