---
title: "Reading Between the Pixels: Linking Text-Image Embedding Alignment to Typographic Attack Success on Vision-Language Models"
title_zh: 读懂像素：将文本-图像嵌入对齐与视觉语言模型上的排版攻击成功率联系起来
authors: "Ravikumar Balakrishnan, Sanket Mendapara, Ankit Garg"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12371v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对视觉语言模型的排版提示注入攻击
tldr: 本研究深入探讨了视觉语言模型（VLM）面临的排版提示注入攻击，即通过将恶意文本渲染为图像来绕过安全机制。通过对GPT-4o、Claude等四种模型在不同字号和视觉变换下的评估，研究揭示了文本-图像嵌入对齐程度与攻击成功率（ASR）之间的强负相关性。研究结果表明，模型对视觉退化的敏感度各异，为构建更安全的自主智能体系统提供了关键的实证依据和防御指导。
selection_source: fresh_fetch
motivation: 旨在揭示排版攻击在不同视觉条件和模型架构下的有效性差异，并探索多模态嵌入对齐与攻击成功率之间的内在联系。
method: 在多种字号和视觉变换（如旋转、模糊、噪声）下，利用SALAD-Bench的1000个提示对四种主流VLM进行攻击测试，并分析多模态嵌入距离与攻击成功率的相关性。
result: 实验发现中等字号攻击效果最强，且嵌入距离与攻击成功率高度负相关（r在-0.71至-0.93之间），同时视觉退化能显著增加嵌入距离并降低攻击成功率。
conclusion: VLM的鲁棒性模式具有模型特异性，不存在一劳永逸的防御措施，开发者在选择智能体后端模型时需根据对抗环境的特征进行针对性评估。
---

## 摘要
我们研究了视觉语言模型（VLM）上的排版提示词注入攻击，即通过将对抗性文本渲染为图像来绕过安全机制。随着 VLM 成为从浏览器自动化、计算机使用系统到配备摄像头的具身智能体等自主智能体的感知主干，这种攻击构成了日益增长的威胁。在实践中，攻击面是异构的：对抗性文本以不同的字体大小和多样的视觉条件出现，而日益壮大的 VLM 生态系统在脆弱性方面表现出显著差异，使防御方法变得复杂。我们在不同的字体大小（6-28px）和视觉变换（旋转、模糊、噪声、对比度变化）下，针对四种 VLM（即 GPT-4o、Claude Sonnet 4.5、Mistral-Large-3 和 Qwen3-VL-4B-Instruct）评估了来自 SALAD-Bench 的 1,000 个提示词，发现：(1) 字体大小显著影响攻击成功率（ASR），极小字体（6px）的 ASR 接近于零，而中等大小的字体达到峰值效果；(2) 对于 GPT-4o（36% 对 8%）和 Claude（47% 对 22%），文本攻击比图像攻击更有效，而 Qwen3-VL 和 Mistral 在不同模态下表现出相当的 ASR；(3) 来自两个多模态嵌入模型（JinaCLIP 和 Qwen3-VL-Embedding）的文本-图像嵌入距离与所有四个模型的 ASR 表现出强负相关（r = -0.71 至 -0.93，p < 0.01）；(4) 严重的降质使嵌入距离增加了 10-12%，并使 ASR 降低了 34-96%，而旋转对模型的影响具有不对称性（Mistral 下降了 50%，GPT-4o 保持不变）。这些发现强调，特定于模型的鲁棒性模式排除了“一刀切”的防御方案，并为在对抗环境下运行的智能体系统选择 VLM 主干的从业者提供了经验指导。

## Abstract
We study typographic prompt injection attacks on vision-language models (VLMs), where adversarial text is rendered as images to bypass safety mechanisms, posing a growing threat as VLMs serve as the perceptual backbone of autonomous agents, from browser automation and computer-use systems to camera-equipped embodied agents. In practice, the attack surface is heterogeneous: adversarial text appears at varying font sizes and under diverse visual conditions, while the growing ecosystem of VLMs exhibits substantial variation in vulnerability, complicating defensive approaches. Evaluating 1,000 prompts from SALAD-Bench across four VLMs, namely, GPT-4o, Claude Sonnet 4.5, Mistral-Large-3, and Qwen3-VL-4B-Instruct under varying font sizes (6--28px) and visual transformations (rotation, blur, noise, contrast changes), we find: (1) font size significantly affects attack success rate (ASR), with very small fonts (6px) yielding near-zero ASR while mid-range fonts achieve peak effectiveness; (2) text attacks are more effective than image attacks for GPT-4o (36% vs 8%) and Claude (47% vs 22%), while Qwen3-VL and Mistral show comparable ASR across modalities; (3) text-image embedding distance from two multimodal embedding models (JinaCLIP and Qwen3-VL-Embedding) shows strong negative correlation with ASR across all four models (r = -0.71 to -0.93, p < 0.01); (4) heavy degradations increase embedding distance by 10--12% and reduce ASR by 34--96%, while rotation asymmetrically affects models (Mistral drops 50%, GPT-4o unchanged). These findings highlight that model-specific robustness patterns preclude one-size-fits-all defenses and offer empirical guidance for practitioners selecting VLM backbones for agentic systems operating in adversarial environments.

---

## 论文详细总结（自动生成）

### 论文总结：读懂像素——文本-图像嵌入对齐与视觉语言模型排版攻击成功率的关联分析

#### 1. 核心问题与研究动机
随着视觉语言模型（VLM）成为自主智能体（如浏览器自动化、具身机器人）的感知核心，一种名为“排版提示注入”（Typographic Prompt Injection）的攻击威胁日益严重。攻击者将恶意指令渲染为图像中的文字，从而绕过传统的纯文本安全过滤器。
*   **研究动机**：现有的研究多关注于开发新的攻击手段，而缺乏对攻击成功机制的深入理解。本文旨在探究：为什么某些渲染条件（如字号、视觉退化）会导致攻击成功或失败？是否存在一种模型无关的指标来预测攻击的有效性？

#### 2. 核心方法论
论文提出通过**多模态嵌入空间（Embedding Space）的对齐程度**来解释和预测攻击成功率（ASR）。
*   **核心思想**：如果 VLM 的视觉编码器生成的图像表示与文本编码器生成的原始恶意文本表示在嵌入空间中高度接近，模型就更有可能“读懂”并执行该指令。
*   **技术细节**：
    *   **嵌入距离计算**：使用预训练的多模态嵌入模型（如 JinaCLIP 和 Qwen3-VL-Embedding），计算原始文本 $p$ 的嵌入向量与渲染图像 $I(p)$ 的嵌入向量之间的 **L2 距离**。
    *   **受控实验变量**：系统性地改变图像的渲染参数，包括 12 种字号（6px 到 28px）和 10 种视觉变换（旋转、模糊、噪声、对比度变化、颜色反转等）。
    *   **相关性分析**：利用皮尔逊相关系数（Pearson $r$）量化嵌入距离与 ASR 之间的线性关系。

#### 3. 实验设计
*   **数据集**：从 **SALAD-Bench** 中精选了 1,000 个对抗性提示词，涵盖 6 类伤害类别（如毒性、恶意使用、虚假信息）和 4 种攻击方法（GCG、TAP、GPTFuzz、Jailbreak）。
*   **目标模型（Benchmark）**：评估了四种主流 VLM：
    *   闭源模型：**GPT-4o**、**Claude Sonnet 4.5**、**Mistral-Large-3**。
    *   开源模型：**Qwen3-VL-4B-Instruct**。
*   **对比维度**：
    *   **模态对比**：纯文本攻击 vs. 图像排版攻击。
    *   **视觉变量**：不同字号、不同几何变换及降质处理。
    *   **嵌入模型对比**：JinaCLIP vs. Qwen3-VL-Embedding。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号、数量或总训练时长。
*   **实现方式**：闭源模型（GPT-4o, Claude, Mistral）通过 Azure API 访问；开源模型 Qwen3-VL-4B 使用 **vLLM** 框架在本地部署推理。由于主要进行的是推理评估而非模型训练，算力需求主要集中在 1,000 个样本在多种条件下的重复推理上。

#### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 1,000 个提示词、4 个目标模型、12 种字号和 10 种视觉变换，总计产生了数万次模型调用。
*   **充分性评价**：实验设计非常充分且具有系统性。通过控制变量法隔离了字号和视觉退化的影响，并跨越了闭源与开源、不同架构的模型，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
1.  **字号阈值效应**：ASR 随字号增加而上升，在 6px 时几乎为零，在 10-12px 时达到平台期。
2.  **模态安全性差异**：对于 GPT-4o 和 Claude，排版攻击的 ASR 远低于纯文本攻击（安全性更高）；而 Mistral 和 Qwen3 在两种模态下同样脆弱。
3.  **嵌入距离是强预测因子**：嵌入距离与 ASR 呈极强的负相关（$r$ 在 -0.71 到 -0.93 之间）。这意味着**嵌入对齐程度可以作为评估 VLM 遭受排版攻击风险的轻量级指标**。
4.  **视觉鲁棒性的不对称性**：旋转变换能显著降低 Mistral 的 ASR（下降 50%），但对 GPT-4o 几乎没有影响，说明不同模型的几何鲁棒性差异巨大。

#### 7. 优点
*   **机制性解释**：不仅展示了攻击现象，还通过嵌入空间对齐提供了物理意义上的解释。
*   **模型无关性**：发现的嵌入距离相关性在多个目标模型和多个嵌入模型上均成立，具有普适性。
*   **实战指导意义**：为智能体开发者提供了选择后端模型的参考，并揭示了通过简单的图像变换（如模糊、旋转）作为防御手段的可能性。

#### 8. 不足与局限
*   **渲染样式单一**：实验仅使用了黑底白字的标准无衬线字体，未测试手写体、复杂背景或多字体混合的情况。
*   **提示词长度限制**：为了确保在 1024x1024 图像中清晰渲染，剔除了极长的提示词，这可能导致对某些复杂攻击方法的评估不全面。
*   **防御策略缺失**：论文侧重于分析和预测，未深入探讨如何利用这些发现构建实时的自动化防御系统。

（完）
