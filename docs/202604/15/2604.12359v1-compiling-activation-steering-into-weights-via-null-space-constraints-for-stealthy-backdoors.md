---
title: Compiling Activation Steering into Weights via Null-Space Constraints for Stealthy Backdoors
title_zh: 通过零空间约束将激活引导编译进权重以实现隐蔽后门
authors: "Rui Yin, Tianxu Han, Naen Xu, Changjiang Li, Ping He, Chunyi Zhou, Jun Wang, Zhihui Fu, Tianyu Du, Jinbao Li, Shouling Ji"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12359v1"
tags: ["query:llm"]
score: 9.0
evidence: 在隐藏触发器下导致越狱的隐蔽后门
tldr: 本研究针对安全对齐大模型的后门攻击，提出一种将激活引导向量编译进模型权重的新方法。针对现有方法仅优化Token映射导致攻击不持久的问题，该方法通过提取表征层面的引导向量，并利用零空间约束将其转化为仅在触发器存在时激活的权重修改。该方法高效且具有闭式解，在保持模型原有性能和安全性的同时，实现了极高的隐蔽攻击成功率。
selection_source: fresh_fetch
motivation: 现有的后门攻击方法通常只优化表面Token映射，导致模型在生成过程中容易从顺从状态回退到拒绝状态，攻击持久性不足。
method: 提取区分顺从与拒绝行为的内部表征引导向量，并通过零空间约束将其编译为持久的权重修改，确保仅在特定触发器下生效。
result: 在多个安全对齐模型和基准测试中，该方法在不影响正常输入安全性和通用能力的前提下，实现了极高的触发攻击成功率。
conclusion: 将激活引导编译进权重是一种高效且隐蔽的后门注入手段，揭示了供应链攻击中模型内部表征被操纵的潜在风险。
---

## 摘要
安全对齐的大语言模型（LLMs）正越来越多地部署在现实世界的流水线中，但这种部署也扩大了供应链攻击面：攻击者可以分发带有后门的检查点，这些检查点在标准评估下表现正常，但在存在隐藏触发器时会发生越狱。最近的事后权重编辑方法提供了一种注入此类后门的有效途径，即通过直接修改模型权重，将触发器映射到攻击者指定的响应。然而，现有方法通常优化令牌级映射，强制生成肯定前缀（例如“Sure”），但这并不能保证持续的有害输出——模型可能在开始时表现出明显的同意，但在几个解码步骤内又恢复到安全对齐的拒绝状态。我们通过将后门目标从表面令牌转移到内部表示来解决这一可靠性差距。我们提取一个捕捉合规行为与拒绝行为之间差异的引导向量，并将其编译为持久的权重修改，该修改仅在触发器存在时激活。为了保持隐蔽性和良性效用，我们施加了零空间约束，使注入的编辑在干净输入上保持休眠状态。该方法效率很高，仅需要少量示例，并支持闭式解。在多个安全对齐的 LLM 和越狱基准测试中，我们的方法在保持未触发时的安全性和通用效用的同时，实现了极高的触发攻击成功率。

## Abstract
Safety-aligned large language models (LLMs) are increasingly deployed in real-world pipelines, yet this deployment also enlarges the supply-chain attack surface: adversaries can distribute backdoored checkpoints that behave normally under standard evaluation but jailbreak when a hidden trigger is present. Recent post-hoc weight-editing methods offer an efficient approach to injecting such backdoors by directly modifying model weights to map a trigger to an attacker-specified response. However, existing methods typically optimize a token-level mapping that forces an affirmative prefix (e.g., ``Sure''), which does not guarantee sustained harmful output -- the model may begin with apparent agreement yet revert to safety-aligned refusal within a few decoding steps. We address this reliability gap by shifting the backdoor objective from surface tokens to internal representations. We extract a steering vector that captures the difference between compliant and refusal behaviors, and compile it into a persistent weight modification that activates only when the trigger is present. To preserve stealthiness and benign utility, we impose a null-space constraint so that the injected edit remains dormant on clean inputs. The method is efficient, requiring only a small set of examples and admitting a closed-form solution. Across multiple safety-aligned LLMs and jailbreak benchmarks, our method achieves high triggered attack success while maintaining non-triggered safety and general utility.

---

## 论文详细总结（自动生成）

这是一份关于论文《Compiling Activation Steering into Weights via Null-Space Constraints for Stealthy Backdoors》的结构化技术总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 后门攻击（特别是基于权重编辑的方法）通常只优化“令牌级映射”（例如强制模型输出“Sure”等肯定前缀）。这种方法不稳健，模型往往在生成几个单词后会触发内部安全机制，从顺从状态回退到拒绝状态（即“安全回退”现象）。
*   **研究背景**：随着 LLM 供应链攻击风险增加，攻击者可能分发带有后门的模型。研究如何实现既能绕过安全对齐、又具有极高隐蔽性且生成内容持久的后门，对于评估模型安全至关重要。
*   **整体含义**：本文提出了一种名为 **STEER EDIT** 的方法，将动态的“激活引导”（Activation Steering）技术编译进静态的模型权重中，通过操纵内部表征而非表面令牌来解决攻击不持久的问题。

### 2. 论文提出的方法论：STEER EDIT
该方法分为三个核心阶段，旨在实现触发器引导的、持久的、且不破坏良性性能的权重修改：
1.  **目标方向识别（Target Direction Identification）**：
    *   利用“均值差”（Difference-in-Means, DiM）方法，在激活空间中提取一个“合规方向”向量 $z_{comp}$。该向量捕捉了模型在处理合规请求与拒绝请求时内部状态的本质差异。
2.  **零空间投影实现隐蔽性（Stealth via Null-Space Projection）**：
    *   为了确保后门在没有触发器时保持休眠，作者对干净输入（良性提示词和无触发器的有害提示词）的激活矩阵进行奇异值分解（SVD）。
    *   构造一个投影矩阵 $P$，将权重更新限制在干净激活的**左零空间**内。理论上，这保证了修改后的权重在处理正常输入时产生的扰动为零。
3.  **引导向量编译（Steering Vector Compilation）**：
    *   将后门注入建模为一个正则化最小二乘问题：寻找一个权重更新 $\Delta$，使得当触发器存在时，模型激活会向 $z_{comp}$ 方向偏移。
    *   该问题具有**闭式解（Closed-form solution）**，计算效率极高，无需迭代训练。

### 3. 实验设计
*   **实验对象（Models）**：Llama-2-7b-chat, Llama-3-8B-Instruct, Qwen2.5-7B-Instruct。
*   **安全基准测试（Safety Benchmarks）**：StrongREJECT, Misuse, DNA, DAN。
*   **通用能力测试（Utility Benchmarks）**：AlpacaEval（指令遵循）、GSM-8K（数学推理）、TruthfulQA（真实性）。
*   **对比方法（Baselines）**：
    *   **ROME / JailbreakEdit**：经典的知识编辑与后门注入方法。
    *   **DualEdit**：旨在减轻安全回退的最新编辑方法。
    *   **SFT**：有监督微调（作为基准攻击手段）。
    *   **RV-Ablation**：直接消融拒绝方向的非隐蔽基准。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A800 GPU (80GB)**。
*   **处理器**：Intel(R) Xeon(R) Gold 6342 CPU @ 2.80GHz (96 cores)。
*   **效率**：由于采用闭式解，该方法在“分钟级”甚至更短时间内即可完成注入，远快于传统的微调方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种主流开源模型家族。
    *   在 4 个不同的越狱/安全数据集上进行了交叉验证。
    *   进行了详细的消融实验，包括零空间约束的必要性、引导强度 $\alpha$ 的影响、以及代理数据集大小的影响。
    *   针对 3 种防御手段（ONION, BEAT, Self-Reminder）进行了鲁棒性测试。
*   **充分性评价**：实验设计非常充分且客观。作者不仅关注攻击成功率（ASR），还引入了“回退率”（Fallback Rate）这一关键指标，并严格评估了模型在良性任务上的性能保留情况（URR），确保了对比的公平性。

### 6. 主要结论与发现
*   **高效的攻击转化**：STEER EDIT 显著降低了“安全回退率”（通常低于 7%），能将表面上的顺从转化为实质性的有害内容生成。
*   **极高的隐蔽性**：得益于零空间约束，该方法在无触发器时的攻击成功率（ASR w/o）极低，且通用能力保留率（URR）通常超过 97%。
*   **帕累托前沿**：通过调节引导强度 $\alpha$，攻击者可以在攻击强度和模型效用之间进行精确权衡，而基线方法往往表现出不稳定的性能波动。
*   **数据效率**：仅需 16-64 个代理样本即可提取出有效的引导方向，显示出极强的实用性。

### 7. 优点
*   **理论优雅**：结合了表征工程（激活引导）与线性代数约束（零空间投影），为后门注入提供了坚实的数学基础。
*   **解决痛点**：直接针对现有编辑方法中“生成中途回退”的顽疾，通过操纵深层表征实现了更持久的攻击。
*   **零推理开销**：与动态引导不同，该方法将效果固化在权重中，部署后不增加任何推理延迟。

### 8. 不足与局限
*   **超参数依赖**：引导强度 $\alpha$ 的选择仍需人工权衡，缺乏一种自动化的最优校准机制。
*   **分布偏移风险**：零空间投影依赖于有限的良性参考集，如果实际部署中的输入分布与参考集差异巨大，隐蔽性保证可能会减弱。
*   **架构限制**：目前主要针对 Transformer 的 MLP 层进行编辑，对于注意力机制或其他架构组件的潜在操纵尚未深入探讨。
*   **防御敏感性**：虽然在现有防御下表现优异，但如果防御者已知晓此类“表征级”操纵，可能会开发出针对性的激活异常检测手段。

（完）
