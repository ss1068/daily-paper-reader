---
title: "SafeAgent: A Runtime Protection Architecture for Agentic Systems"
title_zh: SafeAgent：一种针对智能体系统的运行时保护架构
authors: "Hailin Liu, Eugene Ilyushin, Jie Ni, Min Zhu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17562v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 智能体运行时保护架构与安全护栏
tldr: 针对大语言模型智能体易受提示注入攻击且传统过滤方法不足的问题，本文提出SafeAgent运行时安全架构。该架构将安全视为状态化决策问题，通过运行时控制器和上下文感知决策核心协同工作，在多步工作流中动态评估风险与效用。实验证明，SafeAgent在保持任务性能的同时，显著提升了智能体对复杂攻击的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的输入输出过滤难以应对智能体在多步交互和工具调用中演变的提示注入攻击。
method: 提出一种由运行时控制器和上下文感知决策核心组成的架构，通过风险编码、后果建模和策略仲裁等算子实现动态安全治理。
result: 在Agent Security Bench和InjecAgent上的实验显示，SafeAgent在鲁棒性上优于基准方法，且能维持良好的良性任务表现。
conclusion: 将智能体安全建模为基于状态的决策过程，能有效防御动态交互中的安全威胁，并平衡安全性与实用性。
---

## 摘要
大语言模型 (LLM) 智能体容易受到提示注入攻击的影响，这些攻击会通过多步工作流、工具交互和持久上下文进行传播，使得仅靠输入输出过滤不足以提供可靠的保护。本文提出了 SafeAgent，这是一种运行时安全架构，它将智能体安全视为演化交互轨迹上的有状态决策问题。所提出的设计通过两个协调组件将执行治理与语义风险推理分离：一个是在智能体循环中调解动作的运行时控制器，以及一个在持久会话状态上运行的上下文感知决策核心。该核心被形式化为一种上下文感知的先进机器智能，并通过风险编码、效用成本评估、后果建模、策略仲裁和状态同步等算子进行实例化。在 Agent Security Bench (ASB) 和 InjecAgent 上的实验表明，SafeAgent 在保持具有竞争力的良性任务性能的同时，相比基线和文本级护栏方法，一致地提高了鲁棒性。消融研究进一步表明，恢复置信度和策略权重决定了不同的安全-效用运行点。

## Abstract
Large language model (LLM) agents are vulnerable to prompt-injection attacks that propagate through multi-step workflows, tool interactions, and persistent context, making input-output filtering alone insufficient for reliable protection. This paper presents SafeAgent, a runtime security architecture that treats agent safety as a stateful decision problem over evolving interaction trajectories. The proposed design separates execution governance from semantic risk reasoning through two coordinated components: a runtime controller that mediates actions around the agent loop and a context-aware decision core that operates over persistent session state. The core is formalized as a context-aware advanced machine intelligence and instantiated through operators for risk encoding, utility-cost evaluation, consequence modeling, policy arbitration, and state synchronization. Experiments on Agent Security Bench (ASB) and InjecAgent show that SafeAgent consistently improves robustness over baseline and text-level guardrail methods while maintaining competitive benign-task performance. Ablation studies further show that recovery confidence and policy weighting determine distinct safety-utility operating points.

---

## 论文详细总结（自动生成）

以下是对论文《SafeAgent: A Runtime Protection Architecture for Agentic Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）智能体在执行任务时，不仅生成文本，还会调用工具、读取外部文档并维护长期上下文。传统的“输入-输出过滤”式护栏（Guardrails）难以应对这种多步工作流。
*   **核心问题**：恶意指令（提示注入）可能通过外部工具输出、检索到的文档或记忆片段进入智能体上下文，导致风险在交互循环中传播。现有的防御手段往往是静态的、单次触发的，缺乏对整个交互轨迹的动态安全治理。
*   **整体含义**：SafeAgent 旨在建立一种运行时的安全架构，将智能体安全视为一个**有状态的决策问题**，通过持续监控会话状态来识别并拦截复杂的、跨步骤的攻击。

### 2. 方法论：核心思想与关键技术
SafeAgent 采用了“执行治理”与“语义推理”分离的架构，包含两个核心组件：
*   **运行时控制器（Runtime Controller）**：
    *   作为智能体与环境之间的中间件，管理会话上下文（Session Context）。
    *   **关键功能**：拦截工具调用、规范化请求、执行决策（如允许、拦截、重写参数、回滚状态或触发重新规划）。
    *   **恢复机制**：支持上下文覆盖（Sanitization）、智能体重新规划（Replanning）和轨迹回滚（Rollback）。
*   **决策核心（SafeAgent Core）**：
    *   被形式化为一种“上下文感知先进机器智能（AMI）”。
    *   **核心算子**：
        1.  **风险编码（Risk Encoding）**：使用多个专家编码器识别混淆、触发器激活、秘密泄露等风险维度。
        2.  **效用-成本建模（Advantage-Cost Modeling）**：评估不同干预策略对任务效用和安全成本的影响。
        3.  **世界模型模拟（World Model Simulation）**：在潜在空间模拟动作后果，预测未来轨迹。
        4.  **策略仲裁（Policy Arbitration）**：根据预设权重（如安全优先或任务优先）做出最终决策。
        5.  **状态同步**：更新持久化的内部风险状态，确保决策的连贯性。

### 3. 实验设计
*   **数据集/场景**：
    *   **Agent Security Bench (ASB)**：包含 51 个任务和 40 种攻击工具，共 2040 个案例，涵盖直接注入（DPI）、间接注入（IPI）和记忆毒化（MP）。
    *   **InjecAgent**：专注于工具集成智能体的提示注入，包含 510 个直接伤害任务和 544 个数据窃取任务（分提取和传输两个阶段）。
*   **对比方法**：
    *   **Baseline**：无防御的原始智能体。
    *   **Llama Guard**：基于 LLM 的文本级输入输出护栏。
    *   **LLM Guard**：基于分类器（如 RoBERTa）的扫描工具包。
*   **实验设置**：智能体骨干模型使用 DeepSeek-V3.2，SafeAgent 决策核心使用 gpt-oss-safeguard-20b。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   文中提到了所使用的模型规模（如 20B 规模的决策模型），并指出 SafeAgent 的决策过程会带来“非平凡的计算开销”（Non-trivial computational overhead），暗示了其对推理算力有一定要求。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试上进行了数千次测试（ASB 2040 次，InjecAgent 1054 次），涵盖了多种攻击向量。
*   **消融实验**：针对“恢复置信度”（Override Confidence）和“策略权重”（Policy Weighting）进行了专门的消融研究，探讨了安全与效用之间的平衡。
*   **充分性评价**：实验设计较为全面，对比了不同类型的防御范式（基于分类器 vs 基于 LLM），并细化了攻击阶段（如数据窃取的 S1 和 S2 阶段），结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：SafeAgent 在 ASB 的所有攻击类别中均实现了最低的攻击成功率（ASR），尤其在间接注入和记忆毒化方面表现优异。
*   **多阶段防御有效**：在 InjecAgent 测试中，SafeAgent 将数据窃取攻击的成功率降至 0，证明了状态化控制对多阶段攻击的拦截能力。
*   **安全-效用权衡**：通过调整策略权重，可以在保持较高任务完成率（PNA）的同时，灵活控制安全强度。
*   **传统护栏的局限**：实验证明仅靠文本级过滤（如 Llama Guard）无法有效防御通过工具交互和上下文传播的复杂攻击。

### 7. 优点
*   **状态化防御**：突破了单次拦截的局限，能够识别跨步骤的攻击意图。
*   **模块化与灵活性**：执行与决策分离，支持插件式的风险编码器和可配置的干预策略（如回滚、重写）。
*   **恢复导向**：不仅是简单的“拦截”，还提供了多种修复机制，尽可能保留智能体的任务处理能力。

### 8. 不足与局限
*   **计算延迟**：引入额外的 20B 规模模型进行实时风险评估，会显著增加智能体响应的延迟和推理成本。
*   **配置复杂性**：系统依赖于多种专家编码器和策略权重的精细调优，对开发者的安全专业知识要求较高。
*   **新型威胁覆盖**：虽然对提示注入有效，但对于“技能注入”（Skill Injection）等新兴威胁类别的防御能力仍需进一步研究。

（完）
