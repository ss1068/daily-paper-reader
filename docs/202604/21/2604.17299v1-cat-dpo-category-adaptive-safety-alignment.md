---
title: "Cat-DPO: Category-Adaptive Safety Alignment"
title_zh: Cat-DPO：类别自适应安全对齐
authors: "Tiankai Yang, Yi Nian, Xinyuan Li, Ruiyao Xu, Kaize Ding, Yue Zhao"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17299v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 通过约束优化实现类别自适应的安全对齐
tldr: 针对大语言模型在安全对齐中存在的各类别表现不均问题，本文提出了 Cat-DPO。该方法将安全对齐视为多类别约束优化问题，为每个伤害类别引入自适应安全边际。通过根据模型在特定类别上的表现动态调整训练信号，Cat-DPO 在提升整体安全性和有用性的同时，显著缩小了不同类别间的安全差异，实现了更均衡的对齐效果。
selection_source: fresh_fetch
motivation: 传统的偏好对齐方法使用统一的安全标量，导致模型在某些特定伤害类别上表现不佳且安全性不均衡。
method: 提出 Cat-DPO 算法，为每个伤害类别设置独立的自适应安全边际，根据模型在各类别上的实时表现动态调整训练强度。
result: 实验表明 Cat-DPO 在提升整体有用性和安全性的同时，显著压缩了各类别间的安全方差和表现差距。
conclusion: Cat-DPO 证明了通过类别自适应的精细化对齐可以有效解决模型在特定安全领域表现薄弱的问题。
---

## 摘要
将大语言模型与人类偏好对齐必须平衡两个相互竞争的目标：对合法请求提供有帮助的响应，以及可靠地拒绝有害请求。大多数基于偏好的安全对齐方法将安全性简化为单一标量，并统一应用于每个偏好对。结果是模型在平均水平上看起来是安全的，但在少数危害类别上仍然相对不安全。我们将安全对齐建模为按类别的约束优化问题，并推导出 Cat-DPO，这是一种直接偏好优化算法，为每个危害类别设有独立的自适应安全边际。当模型在某个类别上仍产生不安全响应时，该边际会收紧；一旦模型跟上进度，边际就会放宽。因此，训练信号会追踪每个类别的当前难度，而不是在全局速率下进行平均。在两个 LLM 主干网络和六个偏好学习基准上，Cat-DPO 提高了综合的有用性和无害性，并压缩了各类别间的安全方差以及最优与最差类别之间的差距，为直接偏好安全对齐提供了一种即插即用的按类别优化方案。

## Abstract
Aligning large language models with human preferences must balance two competing goals: responding helpfully to legitimate requests and reliably refusing harmful ones. Most preference-based safety alignment methods collapse safety into a single scalar that is applied uniformly to every preference pair. The result is a model that looks safe on average but stays relatively unsafe on a minority of harm categories. We cast safety alignment as a per-category constrained optimization problem and derive Cat-DPO, a direct-preference-optimization algorithm with a separate adaptive safety margin for each harm category. The margin tightens when the model still produces unsafe responses on a category and relaxes once the model catches up, so the training signal tracks each category's current difficulty rather than averaging under one global rate. Across two LLM backbones and six preference-learning baselines, Cat-DPO iimproves aggregate helpfulness and harmlessness and compresses per-category safety variance and the best-to-worst gap, offering a drop-in per-category refinement of direct preference safety alignment.

---

## 论文详细总结（自动生成）

以下是对论文《Cat-DPO: Category-Adaptive Safety Alignment》的结构化总结：

### 1. 核心问题与整体含义
论文的核心动机在于解决大语言模型（LLM）安全对齐中的**不均衡问题**。现有的偏好对齐方法（如 DPO、SafeDPO）通常将安全性简化为一个全局统一的标量（如固定的安全边际或成本预算）。这种“一刀切”的方法导致模型虽然在平均安全得分上表现良好，但在某些特定的危害类别（如网络犯罪、隐私侵犯等）上仍然存在显著的安全漏洞。论文提出，安全对齐应该针对不同类别的难度进行动态调整，从而在保证整体有用性的同时，消除不同安全类别之间的表现差距。

### 2. 方法论
论文提出了 **Cat-DPO（类别自适应直接偏好优化）**，其核心思想是将安全对齐建模为一个**多类别约束优化问题**。

*   **核心思想**：为 $K$ 个不同的危害类别分别设置约束条件，并引入拉格朗日对偶变量 $\lambda_k$ 作为每个类别的自适应安全边际（Margin）。
*   **关键技术细节**：
    *   **类别增强奖励**：在原始奖励函数基础上，减去由各类别对偶变量加权的安全指示函数。
    *   **一致性过滤（Agreement Filtering）**：仅保留“更有用且更安全”的偏好对进行训练，以避免有用性与安全性信号冲突导致的标签翻转。
    *   **自适应边际更新**：在训练过程中，算法根据模型在特定类别上的表现动态更新 $\lambda_k$。如果模型在某类别上仍产生不安全响应，该类别的边际会收紧（$\lambda_k$ 增大）；若已达标，则边际放宽。
    *   **离线代理更新规则**：由于 DPO 是离线算法，论文设计了一个基于 DPO 内部量（对数比率的 Sigmoid 值）的代理指标 $V_i$ 来模拟在线违反约束的情况，从而实现对偶变量的更新。

### 3. 实验设计
*   **数据集**：主要使用 **PKU-SafeRLHF**（包含 19 个危害类别标签）。
*   **主干网络（Backbones）**：Alpaca-7B（基于 Llama-2）和 Qwen3 系列（4B、1.7B、0.6B）。
*   **对比基准（Baselines）**：
    *   标准 DPO（分别针对有用性和无害性训练）。
    *   **DPO-bettersafe**：在过滤后的数据集上运行的不带边际的 DPO。
    *   **SafeDPO**：带固定安全边际的直接对齐方法。
    *   **SACPO**：分步对齐方法。
*   **评估指标**：GPT-4o 作为裁判的评分（有用性、无害性、安全率）、Beaver 奖励模型评分、以及针对过度拒绝的 **XSTest** 基准。

### 4. 资源与算力
论文明确提到了实验所使用的硬件资源：
*   **GPU 型号**：1 台 **NVIDIA RTX PRO 6000 Blackwell Max-Q** 工作站。
*   **训练细节**：设置了 2 个 Epoch，峰值学习率为 $1 \times 10^{-5}$，最大序列长度 512，Batch Size 为 8。

### 5. 实验数量与充分性
论文进行了较为充分且多维度的实验：
*   **主实验**：在两个不同架构的主干网络上验证了综合性能。
*   **类别均衡性分析**：通过方差、最差 3 类平均分、极差等指标深入探讨了类别间的平衡。
*   **消融实验**：对比了不同的数据过滤模式（一致性过滤 vs. 标签交换）。
*   **参数敏感性分析**：对对偶步长 $\eta$ 和容忍度 $\epsilon$ 进行了网格搜索。
*   **模型缩放实验**：在不同参数规模的 Qwen3 模型上验证了超参数的迁移性。
*   **安全性泛化与校准**：通过 XSTest 验证了模型在未见分布下的表现及是否存在过度拒绝。
实验设计逻辑严密，对比方法涵盖了当前主流的安全对齐方案，结果具有较强的说服力。

### 6. 主要结论与发现
*   **消除长尾风险**：Cat-DPO 显著缩小了表现最好与最差类别之间的差距，降低了各类别安全性的方差。
*   **性能双增**：在提升安全性的同时，Cat-DPO 的有用性得分也优于或持平于强基准方法（如 SafeDPO）。
*   **动态机制有效**：训练过程中的 $\lambda_k$ 轨迹证实，算法能自动识别并重点压制那些“难对齐”的类别。
*   **无过度拒绝副作用**：在 XSTest 上的表现证明，增加类别边际并不会导致模型变得过于胆小而拒绝正常请求。

### 7. 优点
*   **精细化控制**：打破了传统方法将安全性视为单一维度的局限，实现了更细粒度的对齐。
*   **理论支撑**：将拉格朗日对偶理论成功引入 DPO 框架，方法论具有坚实的数学基础。
*   **即插即用**：作为一种损失函数的改进，它可以轻松集成到现有的偏好学习流程中，且不增加推理成本。
*   **自适应性**：无需人工为每个类别手动调整权重，系统能自动学习各类别所需的压力。

### 8. 不足与局限
*   **依赖类别标签**：该方法高度依赖于训练数据中高质量的危害类别标注，对于未标注类别的偏好数据难以直接应用。
*   **超参数 $\epsilon$ 的敏感性**：实验表明 $\epsilon$（容忍度）需要根据模型规模进行调整，缺乏一个完全通用的默认值。
*   **离线代理偏差**：使用的 $V_i$ 只是在线约束违反率的一个代理指标，虽然实验效果好，但在理论上与真实的在线约束优化仍存在一定偏差。
*   **分类体系限制**：目前的分类是静态的，无法处理层级化或动态演变的危害定义。

（完）
