---
title: "LLM Safety From Within: Detecting Harmful Content with Internal Representations"
title_zh: 大语言模型内在安全：利用内部表示检测有害内容
authors: "Difan Jiao, Yilun Liu, Ye Yuan, Zhenwei Tang, Linfeng Du, Haolun Wu, Ashton Anderson"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18519v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 利用内部表示检测有害内容的防御模型
tldr: 本研究针对现有护栏模型仅依赖顶层表示而忽略内部层安全特征的问题，提出了轻量级模型 SIREN。SIREN 通过线性探测识别内部安全神经元，并利用自适应层加权策略构建有害内容检测器。实验表明，SIREN 在多个基准测试中显著优于现有开源模型，且可训练参数量减少了250倍，同时具备更强的泛化能力、实时流式检测支持和极高的推理效率。
selection_source: fresh_fetch
motivation: 现有的安全护栏模型通常只利用 LLM 的最终层输出，忽略了分布在内部各层中丰富的安全相关特征。
method: 提出 SIREN 模型，通过线性探测识别内部安全神经元，并结合自适应层加权策略实现有害内容检测。
result: SIREN 在性能上显著超越了主流开源护栏模型，且参数量仅为后者的 1/250，同时展现出卓越的泛化性和推理效率。
conclusion: 研究证明了 LLM 内部状态是构建高性能、实用化有害内容检测器的重要基础。
---

## 摘要
防护模型被广泛用于检测用户提示词和 LLM 响应中的有害内容。然而，最先进的防护模型仅依赖于末层表示，忽略了分布在内部层中丰富的安全相关特征。我们提出了 SIREN，这是一种利用这些内部特征的轻量级防护模型。通过线性探测识别安全神经元，并利用自适应层加权策略将其组合，SIREN 在不修改底层模型的情况下，利用 LLM 内部状态构建了一个有害性检测器。我们的全面评估表明，SIREN 在多个基准测试中显著优于最先进的开源防护模型，同时使用的可训练参数减少了 250 倍。此外，SIREN 对未见过的基准测试表现出卓越的泛化能力，能够自然地实现实时流式检测，并与生成式防护模型相比显著提高了推理效率。总之，我们的研究结果强调了 LLM 内部状态是实现实用、高性能有害性检测的一个极具前景的基础。

## Abstract
Guard models are widely used to detect harmful content in user prompts and LLM responses. However, state-of-the-art guard models rely solely on terminal-layer representations and overlook the rich safety-relevant features distributed across internal layers. We present SIREN, a lightweight guard model that harnesses these internal features. By identifying safety neurons via linear probing and combining them through an adaptive layer-weighted strategy, SIREN builds a harmfulness detector from LLM internals without modifying the underlying model. Our comprehensive evaluation shows that SIREN substantially outperforms state-of-the-art open-source guard models across multiple benchmarks while using 250 times fewer trainable parameters. Moreover, SIREN exhibits superior generalization to unseen benchmarks, naturally enables real-time streaming detection, and significantly improves inference efficiency compared to generative guard models. Overall, our results highlight LLM internal states as a promising foundation for practical, high-performance harmfulness detection.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SIREN**（Safeguard with Internal REpresentatioN）的轻量级大语言模型（LLM）安全护栏模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的主流安全护栏模型（如 Llama Guard、Qwen Guard）通常将安全检测视为一个“生成式分类”任务，仅依赖模型的**最终层（Terminal Layer）**输出。
*   **研究动机**：
    *   **特征遗忘**：LLM 的内部层（Internal Layers）实际上编码了丰富的安全相关语义特征，仅用最后一层会忽略这些信息。
    *   **效率低下**：生成式护栏需要自回归解码，推理成本高、速度慢。
    *   **一致性差**：现有模型在不同数据集上的判定标准（Precision-Recall）波动较大。
    *   **目标**：利用 LLM 内部表示构建一个更高效、更准确、泛化性更强的插件式安全检测器。

### 2. 方法论：SIREN 框架
SIREN 采用两阶段框架，无需修改底层 LLM 的权重：
*   **第一阶段：安全神经元识别（Safety Neuron Identification）**
    *   对 LLM 的每一层提取内部表示（残差流或前馈网络激活值）。
    *   使用 **L1 正则化线性探测（Linear Probing）** 在每一层独立训练分类器。
    *   根据权重大小识别出对有害性检测贡献最大的神经元子集，称为“安全神经元”。
*   **第二阶段：自适应神经元聚合（Adaptive Neuron Aggregation）**
    *   **层加权策略**：根据各层线性探测在验证集上的 F1 分数分配权重 $\alpha_l$，优先考虑信息量更大的层（通常是中间层）。
    *   **特征拼接**：将各层加权后的安全神经元激活值拼接成一个特征向量。
    *   **分类器训练**：在拼接特征上训练一个轻量级的多层感知机（MLP）作为最终判定头。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流安全基准测试，包括 ToxicChat、OpenAI Moderation、Aegis、Aegis 2.0、WildGuard、SafeRLHF 和 BeaverTails，总计约 23.6 万条样本。
*   **基座模型（Backbones）**：为了公平对比，SIREN 使用了与对比方法相同的基座：Llama-3.2-1B/3.1-8B 和 Qwen3-0.6B/4B。
*   **对比方法（Baselines）**：最先进的开源生成式护栏模型 LlamaGuard3 和 Qwen3Guard。
*   **评估维度**：检测效能（F1 分数）、策略一致性（P-R 曲线）、泛化能力（在未见的推理链 Think 数据集上测试）、流式检测能力以及推理效率。

### 4. 资源与算力
*   **算力消耗**：论文明确提到，在 **NVIDIA A100 GPU** 上，训练基于 Qwen3-4B 的 SIREN 仅需约 **6 个 GPU 小时**。
*   **参数量**：SIREN 极其轻量。例如，针对 4B 参数的模型，SIREN 仅引入了 **14M** 个可训练参数，比全量微调的护栏模型减少了 **250 倍**。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个训练数据集和 1 个专门的泛化测试集（Think）上进行了广泛实验。
*   **消融实验**：详细探讨了神经元选择阈值 $\eta$、聚合策略（均匀 vs 自适应）以及正则化强度的影响。
*   **客观性与公平性**：通过“成对匹配”实验（即 SIREN 与对比模型使用完全相同的基座和训练数据），排除了基座模型能力差异带来的干扰，实验设计非常严谨且客观。

### 6. 主要结论与发现
*   **性能卓越**：SIREN 在所有基准测试中均显著优于 SOTA 生成式护栏模型，平均 F1 提升明显（如在 Llama-1B 基座上提升了 15%）。
*   **内部特征更强**：实验发现中间层的安全检测能力往往高于最终层，证明了提取内部表示的必要性。
*   **泛化与流式检测**：SIREN 在未见过的推理链数据上表现稳健，且无需额外训练即可实现“流式检测”（在文本生成过程中实时拦截），表现优于专门设计的流式护栏。
*   **高效率**：推理时只需一次前向传播，计算量（FLOPs）仅为生成式模型的 **1/4** 左右。

### 7. 优点与亮点
*   **插件式设计**：无需微调 LLM 本身，作为轻量级模块挂载，易于部署。
*   **极高效率**：大幅降低了安全检测的延迟和算力成本。
*   **策略一致性**：相比生成式模型，SIREN 在不同类别有害内容上的判定边界更加稳定。
*   **模型集成**：支持跨模型集成（Ensemble），通过组合不同架构模型的内部特征可进一步提升性能。

### 8. 不足与局限
*   **分类粒度**：目前主要集中在二分类（有害 vs 安全），尚未深入探讨多分类细粒度标签（如区分暴力、色情、仇恨言论等）的表现。
*   **架构依赖**：虽然适用于 Transformer 架构，但对于非线性表示或非 Transformer 架构的适用性有待验证。
*   **偏见继承**：作为基于 LLM 内部表示的方法，SIREN 可能会继承基座模型本身在预训练阶段习得的社会偏见。

（完）
