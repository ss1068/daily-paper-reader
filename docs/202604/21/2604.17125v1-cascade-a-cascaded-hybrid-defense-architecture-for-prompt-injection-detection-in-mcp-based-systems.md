---
title: "CASCADE: A Cascaded Hybrid Defense Architecture for Prompt Injection Detection in MCP-Based Systems"
title_zh: CASCADE：一种用于基于 MCP 系统中提示词注入检测的级联混合防御架构
authors: "İpek Abasıkeleş Turgut, Edip Gümüş"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17125v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于提示词注入检测的分层防御架构
tldr: 针对基于模型上下文协议（MCP）的LLM应用中存在的提示词注入和工具中毒等安全威胁，本文提出了CASCADE防御架构。该架构采用三层级联机制，结合正则过滤、语义分析和模式匹配，实现了无需外部API、完全本地化的防御方案。实验表明，该系统在保持极低误报率的同时，能有效检测多种攻击类型，为MCP系统的安全性提供了高效且隐私友好的保障。
selection_source: fresh_fetch
motivation: 现有的提示词注入防御方案存在高误报率、依赖外部API或需要白盒访问等局限性，且难以应对MCP架构下的新型攻击。
method: 提出一种三层级联架构，结合了快速预过滤、基于BGE嵌入与本地大模型的语义分析以及输出模式过滤。
result: "系统实现了95.85%的精确率和6.06%的低误报率，在数据外泄和提示词注入检测方面表现尤为突出。"
conclusion: CASCADE证明了通过本地化、多层级联的混合防御机制，可以在不依赖外部API的情况下有效提升MCP系统的安全性。
---

## 摘要
模型上下文协议 (Model Context Protocol, MCP) 是 LLM 应用中定义和调用外部工具的一种被快速采用的标准。MCP 的多层架构除了传统的提示词注入外，还引入了诸如工具投毒 (tool poisoning) 等新的攻击面。现有的防御系统存在误报率高、依赖 API 或需要白盒访问权限等局限性。在本研究中，我们提出了 CASCADE，一种针对基于 MCP 系统的三层级联防御架构：(i) 第一层使用正则表达式、短语权重和熵分析进行快速预过滤；(ii) 第二层通过 BGE 嵌入进行语义分析，并配备了 Ollama Llama3 回退机制；(iii) 第三层应用基于模式的输出过滤。在包含 5,000 个样本的数据集上的评估结果显示，准确率为 95.85%，误报率为 6.06%，召回率为 61.05%，F1 分数为 74.59%。对分为 6 个层级的 31 种攻击类型的分析表明，数据外泄 (91.5%) 和提示词注入 (84.2%) 的检测率较高，而语义攻击 (52.5%) 和工具投毒 (59.9%) 类别仍有改进空间。与现有解决方案相比，CASCADE 的一个关键优势是其完全本地化运行，无需外部 API 调用。

## Abstract
Model Context Protocol (MCP) is a rapidly adopted standard for defining and invoking external tools in LLM applications. The multi-layered architecture of MCP introduces new attack surfaces such as tool poisoning, in addition to traditional prompt injection. Existing defense systems suffer from limitations including high false positive rates, API dependency, or white-box access requirements. In this study, we propose CASCADE, a three-tiered cascaded defense architecture for MCP-based systems: (i) Layer 1 performs fast pre-filtering using regex, phrase weighting, and entropy analysis; (ii) Layer 2 conducts semantic analysis via BGE embedding with an Ollama Llama3 fallback mechanism; (iii) Layer 3 applies pattern-based output filtering. Evaluation on a dataset of 5,000 samples yielded 95.85% precision, 6.06% false positive rate, 61.05% recall, and 74.59% F1-score. Analysis across 31 attack types categorized into 6 tiers revealed high detection rates for data exfiltration (91.5%) and prompt injection (84.2%), while semantic attack (52.5%) and tool poisoning (59.9%) categories showed potential for improvement. A key advantage of CASCADE over existing solutions is its fully local operation, requiring no external API calls

---

## 论文详细总结（自动生成）

以下是对论文《CASCADE: A Cascaded Hybrid Defense Architecture for Prompt Injection Detection in MCP-Based Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着模型上下文协议（MCP）的快速普及，LLM 应用能够更方便地调用外部工具。然而，这种多层架构引入了新的安全威胁，除了传统的**提示词注入（Prompt Injection）**，还出现了**工具投毒（Tool Poisoning）**（如恶意更新工具描述、模式投毒等）和**间接注入**。
*   **研究动机**：现有的防御方案存在显著局限性，包括：
    *   **误报率（FPR）极高**：部分系统误报率高达 90% 以上，无法实际应用。
    *   **API 依赖**：依赖外部安全 API，存在隐私泄露风险。
    *   **白盒限制**：需要访问模型内部参数（如注意力权重），不适用于闭源 API 模型。
*   **核心目标**：开发一种完全本地化运行、低误报率、且能应对 MCP 特有攻击的级联防御架构。

### 2. 论文提出的方法论：CASCADE 架构
CASCADE 采用三层级联防御机制，核心思想是“由快到慢、由浅入深”：
*   **L1：预过滤层（Pre-Filter Layer）**
    *   **技术细节**：执行 Unicode 规范化、同形文字转换和多种混淆解码（Base64, ROT13 等）。
    *   **检测手段**：使用 100 多个正则表达式，涵盖直接注入、工具滥用、数据外泄等 7 个类别。通过计算风险评分（0-100）决定是否拦截。
*   **L2：语义判定层（Semantic Judge Layer）**
    *   **核心思想**：**嵌入优先，LLM 回退**。
    *   **流程**：首先使用 BGE 嵌入模型将输入向量化，并与已知攻击模式进行相似度比对。若结果模糊，则调用本地运行的 Llama3 (8B) 模型进行深度语义分析。
    *   **输出**：提供三级决策（允许 ALLOW / 人工审查 REVIEW / 拦截 BLOCK）。
*   **L3：输出过滤层（Output Filter Layer）**
    *   **技术细节**：对 MCP 主机生成的响应进行扫描，识别敏感词、API 密钥模式以及通过 Base64 熵分析检测加密的数据外泄尝试。

### 3. 实验设计
*   **数据集**：构建了一个包含 **5,000 个样本**的综合数据集（1,521 个良性，3,479 个恶意）。
    *   **来源**：GitHub Adversarial、VulnerableMCP、OWASP API Security、Promptfoo 等真实世界来源。
    *   **覆盖范围**：涵盖 31 种攻击类型，分为 6 个层级（Tier）。
*   **Benchmark 与对比**：
    *   对比了 MCPShield、MCP Guardian、MINDGUARD、MCP-Guard 等现有防御系统。
    *   重点对比了误报率（FPR）和是否支持本地化运行。

### 4. 资源与算力
*   **硬件环境**：MacBook Pro，搭载 **Apple M3 Max 芯片（14 核）**，36 GB RAM。
*   **软件/模型**：
    *   L2 层使用 **BGE-small-en-v1.5** 嵌入模型。
    *   L2 回退机制使用 **Ollama 运行的 Llama3 (8B, Q4_0 量化版本)**，占用约 4.7 GB 显存。
    *   系统完全本地运行，不消耗外部 API Token。

### 5. 实验数量与充分性
*   **实验规模**：对 5,000 个样本进行了全量测试，并针对 31 种细分攻击类型进行了召回率分析。
*   **充分性评价**：
    *   **优点**：实验涵盖了从数据外泄到语义攻击的广泛类别，并提供了详细的混淆矩阵和分类性能指标。
    *   **局限**：虽然样本量较大，但部分攻击类型（如工具影子攻击 Tool Shadowing）的样本占比极低（0.03%），可能导致在这些特定领域的评估不够充分。

### 6. 论文的主要结论与发现
*   **整体性能**：系统实现了 **95.85% 的精确率**和 **6.06% 的低误报率**，F1 分数为 74.59%。
*   **分类表现**：
    *   **强项**：对**数据外泄（91.5%）**和**提示词注入（84.2%）**的检测率极高。
    *   **弱项**：对**语义攻击（52.5%）**和**工具投毒（59.9%）**的检测能力有待提高，因为这些攻击往往隐藏在合法的自然语言指令中。
*   **本地化优势**：证明了在不牺牲隐私的情况下，利用本地轻量化模型（BGE + Llama3）可以达到实用的防御水平。

### 7. 优点
*   **低误报率**：相比于之前研究中 90% 以上的误报率，CASCADE 的 6.06% 具有极强的工程落地价值。
*   **隐私保护**：完全本地化部署，避免了将敏感的系统提示词或用户数据发送至第三方安全 API。
*   **级联策略**：通过正则预过滤减少了昂贵的 LLM 调用，平衡了检测速度与深度。
*   **三级决策**：引入“人工审查（REVIEW）”机制，比单纯的二分类（阻断/允许）更符合实际业务场景。

### 8. 不足与局限
*   **语义理解瓶颈**：对于高度隐蔽、利用自然语言逻辑漏洞的语义攻击，召回率仅为 52.5%，存在漏报风险。
*   **语言局限性**：数据集主要基于英语，未测试在多语言或跨语言攻击场景下的表现。
*   **静态评估**：实验基于静态数据集，未在动态、多轮对话的实时 MCP 环境中验证其对会话上下文攻击的防御能力。
*   **特定攻击失效**：在 31 种攻击类型中，有 9 种（约 29%）的检测率为 0%，如权限提升和授权绕过等边缘案例。

（完）
