---
title: "Guardrails in Logit Space: Safety Token Regularization for LLM Alignment"
title_zh: Logit 空间中的护栏：用于大语言模型对齐的安全 Token 正则化
authors: "Thong Bach, Truyen Tran"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17210v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 通过安全标记正则化在微调过程中保持安全对齐
tldr: 针对大语言模型在特定领域微调时安全性易退化的问题，本文提出安全令牌正则化（STR）方法。该方法通过识别对齐模型拒绝回复中的关键令牌，并在微调时约束其 Logit 空间，以极低的计算开销保留模型的安全属性。实验证明 STR 在维持任务效能的同时，安全性可比肩最先进方法，并能提升训练稳定性。
selection_source: fresh_fetch
motivation: 现有安全对齐技术主要集中在预训练阶段，导致模型在下游领域微调时容易因行为偏移而丧失原有的安全性。
method: 通过识别对齐模型拒绝模板中的关键令牌，并在微调过程中对这些令牌相关的逻辑值进行正则化约束。
result: 该方法在保持任务特定效能的同时，达到了与当前最先进方法相当的安全性能，且实现开销极小并兼容 LoRA。
conclusion: STR 为大语言模型在持续微调过程中的安全对齐提供了一种实用、高效且易于部署的防御策略。
---

## 摘要
在新领域对对齐良好的大语言模型（LLMs）进行微调，往往会降低其安全对齐性能，即使使用的是良性数据集。现有的安全对齐技术主要集中在预训练阶段，使得微调后的模型容易受到行为偏移的影响。在这项工作中，我们引入了安全 Token 正则化（STR），这是一种旨在微调过程中保持安全特性的轻量级方法。我们的方法从对齐良好的模型的拒绝模板中识别出显著 Token，并在训练期间约束其相关的 Logit，从而防止关键安全行为的丧失。与强化学习或偏好优化方法不同，STR 仅需极少的额外计算，并能与 LoRA 等参数高效微调技术无缝集成。全面的实验表明，我们的方法在保持特定任务效用的同时，实现了与最先进方法相当的安全性能，且实现开销极小。此外，我们还表明，除了安全方面的考虑外，安全 Token 正则化还增强了训练稳定性和整体性能。这项工作为微调后的 LLM 的持续安全对齐提供了一种实用且易于部署的策略。

## Abstract
Fine-tuning well-aligned large language models (LLMs) on new domains often degrades their safety alignment, even when using benign datasets. Existing safety alignment techniques primarily focus on pretraining, leaving fine-tuned models vulnerable to behavioral shifts. In this work, we introduce safety token regularization (STR), a lightweight method designed to preserve safety properties during fine-tuning. Our approach identifies salient tokens from rejection templates of well-aligned models and constrains their associated logits during training, preventing the loss of critical safety behaviors. Unlike reinforcement learning or preference optimization methods, STR requires minimal additional computation and seamlessly integrates with parameter-efficient fine-tuning techniques such as LoRA. Comprehensive experiments demonstrate that our approach achieves safety performance on par with state-of-the-art methods, while preserving task-specific utility and requiring minimal implementation overhead. Furthermore, we show that safety token regularization enhances training stability and overall performance beyond safety considerations alone. This work offers a practical and readily deployable strategy for continual safety alignment in fine-tuned LLMs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **安全 Token 正则化（Safety Token Regularization, STR）** 的方法，旨在解决大语言模型（LLM）在微调过程中容易出现的“安全对齐退化”问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然顶尖的 LLM 在预训练阶段经过了严格的安全对齐（如 RLHF），但在针对特定下游任务进行微调时（即使是使用良性数据），模型原有的安全护栏往往会遭到破坏，导致模型变得容易被诱导输出有害内容。
*   **核心问题**：如何在不显著增加计算开销、不损害任务性能的前提下，在微调过程中持久地保留预训练模型的安全属性？

### 2. 论文提出的方法论
*   **核心思想**：通过约束模型在 **Logit 空间**（概率输出层）的行为，强制微调后的模型在处理安全相关信号时，保留与原始对齐模型一致的反应。
*   **关键技术细节**：
    1.  **识别安全 Token**：分析对齐良好的模型在拒绝有害请求时的模板（如 "I am sorry", "I cannot"），提取其中的关键 Token（如 "I", "cannot", "can't"）。
    2.  **Logit 约束**：在微调的损失函数中引入一个正则化项。该项计算微调模型与原始模型在这些“安全 Token”上的 Logit 差异。
    3.  **损失函数公式**：总损失 $L = L_{CE} + \lambda L_F$。其中 $L_{CE}$ 是标准的交叉熵损失（保证任务性能），$L_F$ 是安全 Token 的 Logit 平方差损失（Frobenius 范数形式），$\lambda$ 是调节安全强度的超参数。
*   **算法流程**：在每次训练迭代中，同时计算基础模型和微调模型对安全 Token 的 Logit，通过最小化两者间的偏差来防止模型“忘记”如何拒绝有害指令。

### 3. 实验设计
*   **模型架构**：LLaMA-2-7b-chat 和 LLaMA-3-8b-instruct。
*   **数据集与场景**：
    *   **良性数据微调**：使用 Alpaca 数据集（5万条指令）评估通用微调下的安全性保持。
    *   **对抗性攻击测试**：使用 PureBad 数据集（100条极具危害性的提示词）进行压力测试。
    *   **效用评估**：使用 Commonsense-15k 数据集，在 8 个常识推理任务（如 BoolQ, PIQA, ARC 等）上测试模型能力。
*   **对比方法（Benchmark）**：
    *   基础 PEFT 方法：LoRA, DoRA, PiSSA。
    *   安全增强方法：InferAligner, Vaccine, Safe LoRA, SaLoRA。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：提到了批大小（Batch Size）为 16，LoRA Rank 为 8/16/32/64 等。LLaMA-2 训练 5 个 Epoch，LLaMA-3 训练 30 个 Epoch。
*   **运行效率**：论文提供了运行时间对比，STR 的迭代速度比标准 LoRA 慢约 34%，但比 DoRA 快约 25%，证明了其轻量级特性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 8B 规模的模型，进行了安全保留、有害数据压力测试、常识推理效用、持续学习（Sequential Learning）等多个维度的实验。
*   **充分性评价**：实验设计较为全面。不仅对比了安全性指标（有害响应率 HRR、攻击成功率 ASR），还通过消融实验探讨了随机 Token 的影响，并分析了 LoRA 不同秩（Rank）下的稳定性。实验结果显示 STR 在高 Rank 下比 LoRA/DoRA 更稳定，具有较强的说服力。

### 6. 主要结论与发现
*   **安全保持**：STR 在 Alpaca 和 PureBad 数据集上均达到了 SOTA 级别的安全水平，甚至在某些情况下超过了预训练模型的初始安全性。
*   **效用无损**：在提升安全性的同时，STR 保持了与 LoRA 相当的任务准确率，没有出现明显的“对齐税”（即安全性提升导致性能下降）。
*   **增强稳定性**：意外发现 STR 能显著降低微调过程中的性能波动，尤其是在增加 LoRA 秩时，STR 表现出比基准方法更好的鲁棒性。

### 7. 优点
*   **实现简单**：不需要额外的偏好模型（Reward Model）或复杂的对抗训练，只需修改几行损失函数代码。
*   **开销极低**：相比于强化学习方法，STR 几乎不增加额外的显存负担，计算延迟在可接受范围内。
*   **兼容性强**：可以无缝集成到现有的 PEFT（如 LoRA）框架中。

### 8. 不足与局限
*   **继承性限制**：该方法本质上是让微调模型“模仿”原始模型的安全行为，因此它无法让模型产生超越原始模型的新安全能力。
*   **Token 选择依赖**：虽然实验表明随机 Token 也有一定效果，但 STR 的最优性能仍依赖于人工或模板定义的“安全 Token”，缺乏一种完全自动化的、针对不同语种或复杂场景的 Token 提取机制。
*   **覆盖范围**：主要针对拒绝式安全（Refusal-based safety），对于更隐蔽的偏见、幻觉等安全问题的保护效果尚待验证。

（完）
