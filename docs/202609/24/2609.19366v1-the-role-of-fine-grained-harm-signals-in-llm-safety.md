---
title: The Role of Fine-grained Harm Signals in LLM Safety
title_zh: 细粒度危害信号在大语言模型安全性中的作用
authors: "Soyeon Park, Seogyeong Jeong, Sunwoo Kim, Alice Oh"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19366v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 大语言模型内部有害性表示与安全对齐
tldr: 本文探讨了大型语言模型（LLM）中细粒度有害性信号的作用。研究通过从类别有害性表示中剔除通用有害性成分，分离出正交的“类别残差”。实验发现，这些残差在不同风险类别中对有害性编码和拒绝行为的影响存在差异，且能增强模型对通用有害性的内部对齐。研究强调，理解LLM安全性不仅需要关注通用有害性，还需深入分析细粒度的类别残差。
selection_source: fresh_fetch
motivation: 旨在探究LLM中除通用有害性表示外，类别特异性成分在安全性中的具体作用。
method: 通过从各类别有害性表示中移除通用有害性成分，提取出与通用有害性正交的“类别残差”，并利用激活转向技术进行验证。
result: 类别残差对有害性编码的影响在不同类别间具有跨模型的一致性，而对拒绝行为的影响则表现出较强的模型依赖性。
conclusion: 研究证明了细粒度类别残差对理解LLM安全性至关重要，且即使是正交于特定概念的成分也可能在后续层中放大该概念。
---

## 摘要
既往研究表明，大语言模型内部的危害性表征在不同风险类别间存在差异，但同时也共享一个通用的危害表征成分。这引发了一个问题：在通用危害表征之外，类别特异性成分在大语言模型安全性中扮演了什么角色？为了回答这一问题，我们通过从各类别危害表征中剔除共享的通用危害表征，分离出了类别特异性成分，从而得到了在每一层都与通用危害性正交的类别残差。通过在 3 个指令微调大语言模型中对 11 个风险类别的类别残差进行激活引导，我们发现类别残差是否编码危害性因类别而异，且这种类别模式在不同模型间具有相似性。类别残差是否诱发拒绝响应也因类别而异，但这种类别模式对模型的依赖性更强。我们还发现，类别残差增强了大语言模型与共享通用危害表征的下游内部对齐。综上所述，这些发现表明，为了全面理解大语言模型的安全性，除了共享的通用危害表征外，还应考虑更细粒度的类别残差。更广泛地说，我们的研究结果表明，即使是某一层中与某一概念正交的方向，也可能促成该概念在下游的放大。

## Abstract
Prior work has shown that internal harmfulness representations in large language models vary across risk categories, while sharing a common general harm representation component. This raises a question about the role of the category-specific component beyond general harm representation in LLM safety. To answer this question, we isolate the category-specific component by removing shared general harmfulness representation from each categorical harmfulness representation, yielding a category residual that is orthogonal to general harmfulness at every layer. Using activation steering with category residuals across 11 risk categories in 3 instruction-tuned LLMs, we find that whether category residuals encode harmfulness varies across categories, and that this category-wise pattern is similar across models. Whether category residuals induce refusal also varies across categories, but this category-wise pattern is more model-dependent. We also find that category residuals increase LLMs' downstream internal alignment with shared general harmfulness representation. Together, these findings demonstrate that more fine-grained category residuals should also be considered beyond shared general harmfulness representation to fully understand LLM safety. More broadly, our findings show that even a direction orthogonal to a concept at one layer can contribute to the concept's downstream amplification.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Role of Fine-grained Harm Signals in LLM Safety》的结构化总结：

### 1. 核心问题与研究背景
既往研究已证实大语言模型（LLM）内部存在“通用有害性”表征，且不同风险类别（如非法活动、仇恨言论等）的表征存在差异。然而，这些类别特异性成分（Category-specific components）在安全性中的具体作用尚不明确。本文旨在探究：在剔除通用有害性成分后，剩余的“类别残差”是否仍能影响模型的安全行为（如有害性判断和拒绝响应），以及它们如何与下游的通用有害性表征相互作用。

### 2. 方法论
*   **核心思想**：通过向量投影与减法，将类别特异性信息从总的类别有害性向量中分离出来，构建与通用有害性向量正交的“类别残差”。
*   **关键技术细节**：
    *   **向量提取**：通过对比有害提示词与匹配的无害提示词在模型各层残差流中的激活差异，提取类别有害性向量 $v_c$ 和通用有害性向量 $v_g$。
    *   **残差构建**：利用公式 $\tilde{v}_c = v_c - \text{proj}_{v_g}(v_c)$，确保类别残差 $\tilde{v}_c$ 在每一层都与通用有害性向量 $v_g$ 正交。
    *   **激活转向（Activation Steering）**：在推理过程中，将构建好的类别残差向量以一定比例（$\alpha$ 或 $\beta$）注入到模型的残差流中，观察模型输出行为的变化。

### 3. 实验设计
*   **模型选择**：Gemma-2-9B-IT、Llama-3-8B-Instruct、Qwen2.5-7B-Instruct。
*   **数据集**：基于 CatQA 分类法（11 个风险类别，每个类别 5 个子类别），构建了 12 组提示词对（11 个类别 + 1 个混合类别）。
*   **评估任务**：
    *   **有害性归因（Harmfulness Attribution）**：通过“回复反转”任务，测试注入残差后模型是否会将无害指令误判为有害。
    *   **拒绝诱导（Refusal Induction）**：测试注入残差后模型是否会拒绝原本应回答的无害指令。
    *   **下游对齐（Downstream Alignment）**：测量在某一层注入残差后，后续层中模型激活与通用有害性向量的余弦相似度变化。

### 4. 资源与算力
论文未明确提及具体的算力消耗（如 GPU 型号、数量或训练时长）。考虑到实验主要涉及激活转向（Inference-time intervention）而非模型训练，推测其算力需求主要集中在推理阶段，属于中等规模的实验。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 3 个主流模型、11 个风险类别，并使用了 100 个 Alpaca 提示词进行验证，同时通过第二组 100 个提示词进行了鲁棒性测试。
*   **客观性与公平性**：实验设计了随机方向作为对照组，并对不同模型调整了转向强度（$\alpha$），以确保在不同模型上观察到的效果具有可比性。实验设计较为严谨，结论具有较强的统计支撑。

### 6. 主要结论与发现
*   **类别残差的有害性**：部分类别残差（如仇恨/骚扰、成人内容）在所有模型中均能显著诱发有害性判断，且类别间的排序在不同模型间高度一致。
*   **拒绝行为的差异**：类别残差诱发拒绝的能力存在较强的模型依赖性，即同一类别的残差在不同模型中诱发拒绝的强度差异较大。
*   **下游放大效应**：即使类别残差在注入层与通用有害性正交，它仍能显著增加模型在后续层中与通用有害性表征的对齐度。这揭示了“正交干预”可能通过后续层变换转化为下游概念放大的机制。

### 7. 优点
*   **机制洞察**：深入挖掘了 LLM 内部表征的几何结构，揭示了正交成分在下游计算中的非直观影响。
*   **方法创新**：通过“类别残差”这一概念，成功解耦了通用安全与细粒度安全，为理解模型安全行为提供了更精细的视角。
*   **鲁棒性强**：通过跨模型、跨类别、跨提示词集的广泛验证，增强了结论的可信度。

### 8. 不足与局限
*   **模型多样性有限**：仅测试了 3 个 7B-9B 规模的指令微调模型，结论在更大规模或不同架构模型上的普适性有待验证。
*   **测量局限**：拒绝行为的分类依赖于固定的字符串匹配，可能无法捕捉到更微妙的拒绝方式。
*   **应用限制**：激活转向是一种干预手段，其在实际生产环境中的可解释性与安全性部署仍需进一步研究。

（完）
