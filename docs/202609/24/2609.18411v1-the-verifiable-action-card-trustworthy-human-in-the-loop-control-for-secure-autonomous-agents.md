---
title: "The Verifiable Action Card: Trustworthy Human-in-the-Loop Control for Secure Autonomous Agents"
title_zh: 可验证行动卡：面向安全自主智能体值得信赖的人机协同控制
authors: "Hasnain Irshad, Anam Mughees, Neelam Mughees, Abdullah Mughees, Imtiaz Ali Soomro"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18411v1"
tags: ["query:llm"]
score: 9.0
evidence: 防御大语言模型智能体中的间接提示注入
tldr: "针对智能体浏览器在执行敏感操作时面临的提示词注入与欺诈确认风险，本文提出了“可验证操作卡片”（VAC）。该架构通过从底层操作中重构审批信息，并在浏览器受信任区域进行带外渲染，确保用户审批与实际执行的操作严格绑定。实验表明，VAC在多种攻击场景下将攻击成功率降至0%，同时保持了高水平的合法任务完成率，为智能体提供了可靠的安全防御机制。"
selection_source: fresh_fetch
motivation: 现有的人机交互（HITL）防御机制易受不可信网页内容或模型生成文本的干扰，无法有效防范针对智能体浏览器的欺诈性操作确认攻击。
method: VAC通过引入来源隔离、地面真值操作描述符、默认拒绝确认、风险门控及执行绑定等机制，确保审批信息直接源于待执行的底层操作。
result: "在涵盖多种攻击类型的24个场景测试中，VAC将各模型的攻击成功率从68%-100%降低至0%，且合法任务完成率达78%，误拦截率为0%。"
conclusion: 将用户审批锚定在实际执行的操作上，是防御智能体安全漏洞的有效架构手段，优于传统的提示词级防御或常规HITL确认。
---

## 摘要
智能体浏览器可以在用户已认证的会话下执行敏感的安全操作，这使得间接提示注入和欺骗性确认界面直接威胁到操作的完整性。当批准提示本身可能受到不可信页面内容或模型生成文本的影响时，现有的人机协同（HITL）防护措施显得力不从心。我们提出了“可验证行动卡”（Verifiable Action Card, VAC），这是一种架构防御方案，它通过真实的待处理浏览器操作和可信的意图来源重构批准信息，在受信任的浏览器外壳中进行带外渲染，并将批准绑定到分发时重新验证的确切操作上。VAC 结合了来源隔离、真实操作描述符、默认拒绝确认、来源感知风险门控以及执行绑定等机制。我们在一个完整的智能体浏览器中实现了 VAC，并在涵盖混淆代理攻击、循环内谎言对话伪造、间接提示注入、自适应操作替换、来源规避及合法任务等 24 个场景的基准测试中对其进行了评估。在所评估的各类大语言模型中，未采用 VAC 时的攻击成功率为 68% 到 100%，而 VAC 将所有模型的攻击成功率降至 0%，同时保持了 78% 的合法任务完成率和 0% 的误拦截率。这些结果表明，将批准建立在实际执行的操作基础上，能够为提示词层面的防御和传统 HITL 确认无法可靠预防的安全故障提供架构级的保护。

## Abstract
Agentic browsers can execute security-sensitive actions under a user's authenticated session, making indirect prompt injection and deceptive confirmation interfaces a direct threat to action integrity. Existing human-in-the-loop (HITL) safeguards are insufficient when the approval prompt itself can be influenced by untrusted page content or model-generated text. We present the \emph{Verifiable Action Card} (VAC), an architectural defence that reconstructs approval information from the ground-truth pending browser action and trusted intent provenance, renders it out-of-band in the trusted browser chrome, and binds approval to the exact action re-verified at dispatch. VAC combines provenance fencing, a ground-truth action descriptor, default-deny confirmation, provenance-aware risk gating, and execution binding. We implement VAC in a complete agentic browser and evaluate it on a 24-scenario benchmark covering confused-deputy attacks, Lies-in-the-Loop dialog forging, indirect prompt injection, adaptive action substitution, provenance evasion, and legitimate tasks. Across the evaluated LLMs, attack success without VAC ranges from $68\%$ to $100\%$, whereas VAC reduces attack success to $0\%$ on every model, with $78\%$ legitimate-task completion and a $0\%$ false-block rate. These results show that grounding approval in the action that will actually execute provides architectural protection against security failures that prompt-level defences and conventional HITL confirmation cannot reliably prevent.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Verifiable Action Card: Trustworthy Human-in-the-Loop Control for Secure Autonomous Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：智能体浏览器（Agentic Browsers）能够代表用户执行敏感操作（如转账、发帖、提交表单），但其本质上是“混淆代理”（Confused Deputy），极易受到间接提示注入（Indirect Prompt Injection）的攻击。
*   **核心痛点**：现有的“人机协同”（HITL）防御机制失效。攻击者可以通过“循环内谎言”（Lies-in-the-Loop）伪造确认对话，诱导用户批准与其实际意图完全不同的恶意操作。
*   **研究目标**：解决“确认信息不可信”的问题，确保用户批准的内容即为最终执行的操作。

### 2. 方法论：可验证行动卡 (VAC)
*   **核心思想**：将信任锚点从“模型生成的文本”转移到“底层执行的真实操作”。
*   **关键组件**：
    *   **C1 意图来源隔离 (Provenance Fencing)**：标记内容来源（用户输入 vs. 网页内容），防止恶意网页内容被模型误认为指令。
    *   **C2 地面真值描述符 (Ground-truth Descriptor)**：直接从 DOM 元素和表单中读取真实参数（如目标域名、收款人、金额），而非依赖模型总结。
    *   **C3 带外渲染 (Out-of-band Rendering)**：在浏览器受信任的 Chrome 界面渲染确认卡，网页无法篡改、隐藏或伪造该界面。
    *   **C4 风险门控 (Risk Gating)**：基于结构化信号（如是否为 POST 请求、是否包含未授权的外部收款人）自动触发高风险警告。
    *   **C5 执行绑定 (Execution Binding)**：在点击确认与实际执行之间进行“时间点”校验，防止攻击者在用户批准后通过 DOM 变异替换操作参数（TOCTOU 攻击）。

### 3. 实验设计
*   **基准测试 (Benchmark)**：构建了包含 24 个场景的测试套件，涵盖混淆代理、对话伪造、间接注入、自适应替换等攻击类型及合法任务。
*   **对比方法**：
    *   **Arm A (No-gate)**：无防御基准。
    *   **Arm B (Naive HITL)**：传统确认方式（基于模型总结）。
    *   **Arm C (VAC)**：本文提出的完整防御方案。
    *   **Arm C-p (VAC without provenance)**：消融实验，禁用来源隔离。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号、数量或训练时长。实验主要通过调用 OpenAI 兼容的 API 接口进行推理，重点在于验证架构的有效性而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：共报告了 293 组实验数据，涵盖 5 种不同能力的开源模型（Qwen3, Llama-4 等）及 GPT-4.1 的补充测试。
*   **客观性**：采用了“确认者预言机”（Confirmer Oracle）模型来模拟人类决策，确保了不同实验组之间的对比公平性。实验设计了消融实验，验证了各组件的必要性。

### 6. 主要结论与发现
*   **防御效果**：VAC 将所有测试模型的攻击成功率从 68%-100% 降至 0%。
*   **架构优势**：VAC 的保护能力不依赖于特定 LLM 的安全性，是一种模型无关的架构级防御。
*   **性能表现**：在提供强力安全保障的同时，保持了 78% 的合法任务完成率，且误拦截率为 0%。

### 7. 优点
*   **设计亮点**：将“所见即所签”（WYSIWYS）原则引入智能体浏览器，彻底解决了确认信息被篡改的根本问题。
*   **防御深度**：通过执行绑定（C5）有效防御了高级的 TOCTOU（检查时与使用时差异）攻击。
*   **实用性**：对现有浏览器架构改动较小，且不增加额外的模型推理成本。

### 8. 不足与局限
*   **人类行为模拟**：实验使用预言机模拟人类，未进行真实用户研究，无法完全衡量“确认疲劳”对实际防御效果的影响。
*   **启发式局限**：来源归因（Provenance Attribution）目前基于启发式规则，可能存在误报或漏报。
*   **应用范围**：目前主要针对 DOM-first 浏览器智能体，对于视觉驱动（Vision-based）智能体的通用性有待进一步验证。

（完）
