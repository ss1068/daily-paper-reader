---
title: "SPARK: Representation-Level KV Memory Alignment for Safer Vision-Language Models"
title_zh: SPARK：用于更安全视觉语言模型的表征级 KV 记忆对齐
authors: "Mohd Azfar, Izhar Dad Khan"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14258v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过键值对内存对齐缓解视觉语言模型中的越狱攻击
tldr: 针对视觉语言模型（VLM）易受跨模态越狱攻击的问题，本文提出了SPARK框架。该方法通过在预填充阶段对多模态KV内存进行针对性修复，识别并消除有害的表示方向，同时利用图像结构先验保持视觉基础能力。SPARK无需在推理时修改模型参数或使用显式分类器，在显著降低攻击成功率的同时，有效保留了模型的通用性能和语言质量。
selection_source: fresh_fetch
motivation: 现有单模态安全机制无法有效防御将恶意意图分散在文本与图像中的跨模态越狱攻击。
method: 通过两阶段框架识别并投影出KV内存中的有害表示方向，并利用自适应系数动态混合修复后的内存以平衡安全性与性能。
result: 在多个主流VLM模型上，SPARK大幅降低了各类越狱攻击的成功率，同时在MMMU等基准测试中保持了接近原始模型的性能。
conclusion: 通过在预填充阶段选择性修复KV子空间，可以有效且高效地缓解多模态越狱风险，特别是在视觉模态携带恶意信息时表现优异。
---

## 摘要
视觉语言模型（VLMs）仍然容易受到将有害意图分散在文本和图像中的越狱攻击，这使得单模态安全机制不足以应对。我们研究了是否可以在预填充阶段形成的模态键值（KV）记忆中直接缓解这种漏洞，而无需在推理时修改模型参数。我们引入了 SPARK，这是一个用于目标 KV 记忆修复的两阶段框架。第一阶段使用一个可丢弃的诊断适配器来识别模态键和值表征中与危害相关的方向。第二阶段将这些方向投影出去，学习轻量级的残差修复，并利用图像结构先验锚定修复后的键，以保持视觉基础。SPARK 不会统一应用干预，而是使用由干预相关子空间能量 E_h 确定的头级系数 g_h* 来混合修复后的记忆和原始记忆，在推理时无需显式的危害分类器。在 LLaVA-OneVision-7B、Chameleon-7B、Qwen2-VL-7B 和 InternVL2-4B 上，SPARK 在保持通用能力的同时降低了多模态攻击成功率。在 LLaVA-OneVision-7B 上，纯图像越狱攻击成功率降至 4.7%，而 MMMU 得分与未防御模型相比保持在 0.6 分以内（47.8 对 48.4），且语言质量接近基准水平。在 MM-SafetyBench 上，攻击成功率从 39.2% 降至 12.4%。即使在白盒自适应联合提示-图像攻击下，攻击成功率也仅限于 20.3%，而未防御模型为 54.6%。这些结果表明，通过在预填充阶段选择性地修复与干预相关的 KV 子空间，可以显著缓解多模态越狱行为，特别是在有害证据由视觉模态携带时。

## Abstract
Vision-language models (VLMs) remain vulnerable to jailbreaks that distribute harmful intent across text and images, making unimodal safety mechanisms insufficient. We investigate whether this vulnerability can be mitigated directly in the multimodal key-value (KV) memory formed during prefill, without modifying model parameters at inference time. We introduce SPARK, a two-stage framework for targeted KV-memory repair. Stage 1 uses a disposable diagnostic adapter to identify harm-associated directions in multimodal key and value representations. Stage 2 projects out these directions, learns a lightweight residual repair, and anchors repaired keys with an image-structural prior to preserve visual grounding. Rather than applying the intervention uniformly, SPARK mixes repaired and original memory using a head-wise coefficient g_h* determined by intervention-relevant subspace energy E_h, requiring no explicit harm classifier at inference.   Across LLaVA-OneVision-7B, Chameleon-7B, Qwen2-VL-7B, and InternVL2-4B, SPARK reduces multimodal attack success while preserving general capability. On LLaVA-OneVision-7B, image-only jailbreak attack success falls to 4.7%, while MMMU remains within 0.6 points of the undefended model (47.8 vs. 48.4) with near-baseline language quality. On MM-SafetyBench, attack success decreases from 39.2% to 12.4%. Even under white-box adaptive joint prompt-image attacks, attack success is limited to 20.3%, compared with 54.6% for the undefended model. These results suggest that multimodal jailbreak behavior can be substantially mitigated by selectively repairing intervention-relevant KV subspaces at prefill, particularly when harmful evidence is carried by the visual modality.

---

## 论文详细总结（自动生成）

以下是对论文《SPARK: Representation-Level KV Memory Alignment for Safer Vision-Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：视觉语言模型（VLM）在多模态推理上表现出色，但极易受到“跨模态越狱”攻击（即通过图像与文本的组合绕过安全对齐）。
*   **痛点**：现有的单模态安全机制（如文本对齐）无法覆盖多模态漏洞；而推理时的全局干预（如隐藏状态修改）往往会损害模型的通用推理能力。
*   **核心问题**：能否在不修改模型参数的前提下，通过直接干预预填充（prefill）阶段生成的 KV 缓存（KV Memory），实现对有害行为的精准抑制，同时保留模型的正常功能？

### 2. 方法论：SPARK 框架
SPARK 采用两阶段 KV 记忆修复策略：
*   **阶段 1：有害 KV 子空间发现**：训练一个临时的“诊断压力适配器”（DSA），通过对比基础模型与受扰动模型的激活差异，提取出与有害行为高度相关的 KV 空间方向（通过 SVD 分解得到）。随后，通过正交投影将这些有害方向从 KV 缓存中移除。
*   **阶段 2：修复与锚定**：在投影后的缓存上添加轻量级残差，以恢复被误删的有用信息。引入“Canny 边缘检测”作为结构先验，锚定修复后的键（Key），确保视觉基础能力不丢失。
*   **自适应混合机制**：引入头级系数 $g_h^*$，根据当前 KV 状态在有害子空间中的能量 $E_h$ 动态决定修复强度。若 $E_h$ 较低（即输入不含明显有害特征），则不进行干预，从而避免对正常推理的干扰。

### 3. 实验设计
*   **数据集**：使用 VLSafe、ToViLaG（包含文本、图像、文本+图像三种分区）进行越狱攻击测试；使用 MMMU 和 RealWorldQA 评估通用能力。
*   **基准与对比**：对比了 Safety SFT、Steer/AutoSteer（解码时隐藏状态干预）、VLM-Guard、OmniSteer 等主流防御方法。
*   **模型覆盖**：在 LLaVA-OneVision-7B、Chameleon-7B、Qwen2-VL-7B 和 InternVL2-4B 上进行了验证。

### 4. 资源与算力
*   **硬件**：实验在 8 × A100-80GB GPU 集群上运行。
*   **耗时**：子空间发现（Stage 1）耗时约 2.1 GPU 小时；修复适配器训练（Stage 2）耗时约 3.4 GPU 小时。

### 5. 实验数量与充分性
*   **充分性**：实验设计非常详尽，涵盖了主实验、消融实验（如结构先验的必要性、层级选择、子空间秩的影响）、鲁棒性测试（白盒自适应攻击）以及推理开销分析。
*   **客观性**：采用了多种子（seeds）进行重复实验，并报告了均值与标准差；使用了 LLM 裁判（Judge）进行自动化评估，并验证了其与人类评估的一致性（$\kappa=0.84$）。

### 6. 主要结论与发现
*   **安全性提升**：SPARK 在多模态越狱攻击下表现出显著的防御效果，攻击成功率（ASR）大幅下降（如 LLaVA-OV 上图像越狱攻击从 70.6% 降至 4.7%）。
*   **能力保留**：在防御的同时，模型在 MMMU 等基准测试上的性能损失极小，优于大多数现有的推理时干预方法。
*   **高效性**：SPARK 的干预仅发生在预填充阶段，推理时的解码开销几乎为零，优于需要逐 token 干预的解码时 steering 方法。

### 7. 优点
*   **精准干预**：通过自适应系数 $g_h^*$ 实现了“按需防御”，有效解决了安全防御与模型性能之间的权衡（Trade-off）。
*   **无需参数更新**：作为一种推理时插件，无需对基础模型进行昂贵的重训练或微调。
*   **结构化锚定**：利用 Canny 边缘作为结构先验，巧妙地解决了投影操作导致的视觉信息丢失问题。

### 8. 不足与局限
*   **鲁棒性边界**：在白盒自适应攻击（攻击者已知防御机制）下，攻击成功率会有所回升（从 7.4% 升至 20.3%），表明其并非绝对安全。
*   **领域依赖**：$E_h$ 的阈值校准依赖于特定的基准分布，若遇到分布外（OOD）的复杂视觉场景，可能需要重新校准。
*   **文本越狱局限**：对于纯文本的越狱攻击，其防御效果略逊于专门针对文本的解码时 steering 方法。

（完）
