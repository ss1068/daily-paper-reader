---
title: "PolicyMem: Geometric Policy Memory for LLM Governance"
title_zh: PolicyMem：用于大模型治理的几何策略记忆
authors: "Yuanchen Bei, Zhengzhang Chen, Yanjun Zhao, Haoyu Wang, Hanghang Tong, Haifeng Chen"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.13734v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于大语言模型治理和防护的几何策略记忆
tldr: 针对大模型治理中现有防护机制难以复用策略证据的问题，本文提出了PolicyMem。该方法将自然语言策略编译为共享表示空间中的低秩子空间（几何记忆对象），通过投影能量实现策略的检测、归因与验证。PolicyMem构建了“检测-重写-验证”的闭环治理框架，在多个基准测试中达到SOTA水平，显著提升了策略的可解释性与复用性。
selection_source: fresh_fetch
motivation: 现有大模型治理方案在学习型防护的语义判别能力与可编程框架的灵活性之间难以平衡，且缺乏可复用的策略证据。
method: 提出PolicyMem几何策略记忆机制，将自然语言策略转化为低秩子空间，通过投影能量进行策略匹配与证据提取。
result: 在五个主流基准测试中实现了领先的不安全行为检测效果，并支持高效的策略归因与干预验证。
conclusion: PolicyMem通过外部化策略记忆，为大模型治理提供了一种可复用、可解释且闭环的系统化解决方案。
---

## 摘要
随着大语言模型（LLMs）在现实世界高风险应用中的部署日益广泛，有效的治理变得至关重要。现有的安全防护主要遵循两种范式：基于学习的防护机制提供了强大的语义判别能力，但将策略行为与训练模型及分类体系耦合在一起；而可编程框架提供了灵活的控制，却需要大量的手动提示词和工作流工程。这两种范式均未将策略外化为可重用的操作状态，导致难以在检测、干预和验证过程中一致地复用策略证据。在本文中，我们引入了 PolicyMem，这是一种几何策略记忆机制，它将自然语言策略外化为可重用的几何记忆对象，并以共享表示空间中的低秩子空间进行表征。记忆写入器将自然语言策略编译为策略记忆槽，查询-响应对则通过投影能量读取策略记忆。由此产生的策略证据配置文件直接调节安全判定，并被复用于策略归因和干预后验证。结合响应重写器，PolicyMem 为大模型治理实现了“检测-重写-验证”的闭环。在五个广泛使用的基准测试中，PolicyMem 不仅实现了最先进的不安全行为检测性能，还通过共享策略记忆实现了有效的策略归因、重写和干预后验证。

## Abstract
As large language models (LLMs) are increasingly deployed in real-world high-stakes applications, effective governance has become essential. Existing safeguards largely follow two paradigms: learning-based guards provide strong semantic discrimination but couple policy behavior to trained models and taxonomies, while programmable frameworks offer flexible control but require substantial manual prompt and workflow engineering. Neither externalizes policies as reusable operational states, making it difficult to consistently reuse policy evidence across detection, intervention, and verification. In this paper, we introduce PolicyMem, a geometric policy memory that externalizes natural-language policies as reusable geometric memory objects represented by low-rank subspaces in a shared representation space. A memory writer compiles natural-language policies into policy memory slots, and query-response pairs read the policy memory through projection energy. The resulting policy-evidence profile directly mediates the safety verdict and is reused for policy attribution and post-intervention verification. Coupled with a response rewriter, PolicyMem enables a detect-rewrite-verify loop for LLM governance. Across five widely used benchmarks, PolicyMem achieves state-of-the-art unsafe behavior detection while enabling effective policy attribution, rewriting, and post-intervention verification through the shared policy memory.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《PolicyMem: Geometric Policy Memory for LLM Governance》** 的结构化总结：

### 1. 核心问题与研究动机
当前大模型（LLM）治理主要存在两大范式：
*   **学习型防护（Learning-based）**：通过微调模型实现，语义判别强，但策略与模型参数深度耦合，难以解释和修改。
*   **可编程框架（Programmable frameworks）**：如 NeMo Guardrails，灵活性高，但依赖繁琐的手动提示词工程。
**核心痛点**：现有方案均未将“治理策略”外化为可复用的操作状态，导致在检测、干预（重写）和验证三个阶段中，策略证据无法共享，治理过程缺乏闭环与一致性。

### 2. 方法论：PolicyMem
PolicyMem 的核心思想是将自然语言策略转化为**几何记忆对象**，实现策略的显式化与可计算化。
*   **几何策略记忆（Geometric Policy Memory）**：将每个策略映射为共享表示空间中的**低秩子空间（Low-rank Subspace）**。
*   **记忆写入（Memory Write）**：通过子空间编译器（Subspace Compiler）将策略描述编译为正交基（Orthonormal Basis），形成记忆槽。
*   **投影能量读取（Projection Energy Read）**：将查询-响应对编码为向量，通过计算其在策略子空间上的投影能量（Projection Energy）来判定是否违反策略。
*   **闭环治理（Closed-loop Governance）**：构建“检测（Detection）-重写（Rewriting）-验证（Verification）”流程。若检测到不安全，利用高能量策略槽对应的自然语言描述作为反馈，引导重写器（Rewriter）进行修正，随后再次验证。

### 3. 实验设计
*   **基准测试（Benchmarks）**：涵盖 BeaverTails、WildGuardMix、Aegis 2.0、BingoGuard 和 SafeRLHF 五个主流安全治理数据集。
*   **对比方法**：
    *   通用 LLM（Qwen 系列）。
    *   可编程框架（NeMo Guardrails）。
    *   学习型防护模型（Llama Guard 3, Granite Guardian, MD-Judge, WildGuard 等）。
    *   代理记忆系统（Class-RAG, Mem0, A-Mem）。

### 4. 资源与算力
*   **硬件**：实验在 A100 GPU 上进行。
*   **训练细节**：模型使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$，采用 bf16 精度。重写器使用 LoRA 微调。
*   **未明确说明**：文中未详细列出总体的 GPU 数量（如节点数）或具体的总训练时长，仅提到了单设备批次大小和梯度累积步数。

### 5. 实验数量与充分性
*   **实验覆盖**：包含检测性能、策略归因（mAP）、重写质量（LLM-as-a-Judge）、跨分类体系泛化（Leave-one-taxonomy-out）以及消融实验。
*   **客观性**：实验采用了统一的评估协议，对比了多种不同规模（0.5B 到 72B）的基线模型，并使用了外部强模型（Claude Sonnet 4.5, OpenAI o3）作为评判者，设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：PolicyMem 在所有基准测试中均达到 SOTA 水平，且在 3B 参数规模下表现优于 7B-8B 的传统防护模型。
*   **高效复用**：通过共享几何记忆，单一治理器可处理多种异构策略，且在跨分类体系泛化测试中表现出色。
*   **闭环有效性**：重写-验证循环能显著提升响应的安全性与有用性，且收敛速度极快（通常 1-3 轮即可通过验证）。

### 7. 优点
*   **可解释性强**：通过投影能量直接定位违规策略，实现了透明的策略归因。
*   **存储高效**：相比于文本检索式记忆（占用数百 MB），PolicyMem 的几何记忆仅需约 0.6 MB，且不随训练数据量增长。
*   **推理速度快**：非自回归的几何读取方式大幅降低了治理延迟。

### 8. 不足与局限
*   **语言与模态限制**：目前主要聚焦于英语文本的问答治理，尚未扩展至多语言或多模态场景。
*   **分布偏移风险**：虽然具备跨分类体系泛化能力，但在面对现实世界中极端的分布偏移（Distribution Shift）时，其鲁棒性仍需进一步验证。
*   **依赖预训练编码器**：几何空间的构建依赖于底层的编码器，若编码器本身存在严重偏见，可能会影响策略子空间的准确性。

（完）
