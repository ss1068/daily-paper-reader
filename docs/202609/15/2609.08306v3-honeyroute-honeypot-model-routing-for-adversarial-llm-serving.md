---
title: "HoneyRoute: Honeypot-Model Routing for Adversarial LLM Serving"
title_zh: HoneyRoute：面向对抗性大模型服务的蜜罐模型路由机制
authors: Han Jin
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08306v3"
tags: ["query:jb-def"]
score: 9.0
evidence: 检测恶意请求并路由至蜜罐模型的防御层
tldr: HoneyRoute是一种针对大模型推理服务的防御层，通过轻量级流式路由器实时识别恶意请求，并将其重定向至蜜罐模型。该系统在保护生产模型免受攻击的同时，通过分析蜜罐中的交互数据持续优化路由策略，实现了极低延迟下的高精度防御，有效降低了攻击带来的资源消耗与安全风险。
selection_source: fresh_fetch
motivation: 现有防御方案多依赖模型内部或协议层，缺乏针对推理服务层的实时保护与攻击情报反馈机制。
method: 构建了包含流式路由器、双重实现蜜罐及闭环分析系统的架构，利用轻量级模型进行恶意请求检测与重定向。
result: "该系统在保持极低延迟（38ms）的同时达到0.933的检测F1值，并将恶意攻击下的生产模型资源消耗降低了97.8%。"
conclusion: HoneyRoute通过高效的路由与蜜罐机制，在保障生产模型性能的同时，实现了对攻击者的情报捕获与防御闭环。
---

## 摘要
我们提出了 HoneyRoute，这是一个推理服务层，旨在检测传入请求是否为恶意请求，若是，则将其路由至专门的蜜罐模型，从而在保护生产环境的同时，持续收集攻击者的交互信息以获取情报。现有的防御措施通常将陷阱嵌入模型内存或在协议层重建欺骗机制，这使得服务层处于未受保护状态，且无法将信息反馈至检测环节。HoneyRoute 结合了以下三个部分：(i) 流式路由器（由冻结的 0.8B 嵌入主干和各领域 MLP 头部组成）；(ii) 双重实现蜜罐（基于规则/提示工程的代码蜜罐或专门的同系列副本）；(iii) 分析循环，将捕获的交互转化为攻击者指纹，用于路由器的再训练。在生产追踪数据及七个领域的攻击语料库上，该路由器的 F1 分数达到 0.911，中位附加延迟仅为 38 毫秒；在 13 种对抗性变换下，其 F1 分数达到两层防护大模型级联方案的 96%，而延迟仅为其 1/385，且规避率为 0%。在并发洪水攻击及真实 GCG 后缀负载下，通过分流恶意请求，生产模型的 Token 消耗降低了 97.8%；训练后的副本在良性留存请求上与生产模型的一致性达到 92.9%，而原始的无条件诱饵注入一致性仅为 7.6%，选择性伪装注入则恢复至 88.9%，从而勾勒出了可恢复的保真度-可追溯性边界；此外，通过循环训练的校正头部，合法安全研究的误路由率降低了 9 倍，并将检测 F1 分数提升至 0.933。

## Abstract
We introduce HoneyRoute, an inference-serving layer that detects whether an incoming request is malicious and, if so, routes it to a dedicated honeypot model, shielding production while the adversary's interaction is continuously harvested for intelligence. Existing defenses embed traps inside model memory or rebuild deception at the protocol layer, leaving the serving tier unprotected and feeding nothing back into detection. HoneyRoute couples (i) a streaming router (a frozen 0.8B-embedding backbone with per-domain MLP heads), (ii) a dual-implementation honeypot (a rule/prompt-engineered code honeypot or a dedicated same-family replica), and (iii) an analysis loop that converts trapped interactions into attacker fingerprints for router retraining. On a production trace plus a seven-domain attack corpus, the router reaches F1=.911 at 38 ms median added latency, matching 96% of a two-tier guard-LLM cascade's F1 at 1/385 of its latency with 0% evasion under 13 adversarial transformations; diverting the malicious share cuts production-model token consumption under concurrent flooding with real GCG-suffix payloads by 97.8%; the trained replica agrees with the production model on 92.9% of benign holdout requests, while naive unconditional bait injection collapses to 7.6% and selective camouflaged injection recovers to 88.9%, mapping the recoverable fidelity-traceability frontier; and a loop-trained correction head cuts misrouting of legitimate security research 9x while raising detection F1 to .933.

---

## 论文详细总结（自动生成）

这是一份关于论文《HoneyRoute: Honeypot-Model Routing for Adversarial LLM Serving》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：大模型（LLM）推理服务已成为攻击者的主要目标（如提示词窃取、越狱、资源耗尽攻击）。
*   **痛点**：现有防御方案（如模型内部陷阱或协议层欺骗）存在局限性：它们无法在推理服务层（API 网关）进行实时拦截，且缺乏将攻击情报反馈至防御系统的闭环机制。
*   **目标**：HoneyRoute 旨在构建一个推理服务层的防御层，通过实时识别恶意请求并将其重定向至“蜜罐模型”，在保护生产模型的同时，利用攻击交互数据持续优化检测能力。

### 2. 方法论
*   **核心思想**：将推理网关转变为“主动欺骗表面”，通过路由决策实现生产环境与攻击流量的物理隔离。
*   **关键组件**：
    *   **流式路由器**：采用冻结的 0.8B 参数嵌入主干，配合针对不同攻击领域的微型 MLP 头部（约 0.07M 参数），实现低延迟的实时分类。
    *   **双重蜜罐实现**：
        *   **代码蜜罐 ($f_{Mc}$)**：基于规则/提示工程，无 GPU 开销，通过注入诱饵（Bait）捕获攻击指纹。
        *   **训练蜜罐 ($f_{Mt}$)**：同系列模型副本，通过全会话记录进行无损仪器化（Instrumentation）追踪。
    *   **闭环分析系统**：将捕获的攻击轨迹转化为“攻击者指纹”（战术签名、目标、行为特征），用于路由器的增量再训练，实现防御系统的自我进化。
*   **算法流程**：网关对请求评分 -> 超过阈值则重定向至蜜罐 -> 蜜罐记录交互 -> 提取指纹 -> 更新路由器头部。

### 3. 实验设计
*   **数据集/场景**：包含生产环境追踪数据、七个领域的攻击种子语料库（注入、越狱、代码攻击等）、JailbreakBench 对抗性提示词、以及资源耗尽压力测试（GCG 后缀洪水攻击）。
*   **对比基准**：
    *   多语言关键词/正则启发式规则。
    *   Guard-LLM 内容审核器（如 Llama Guard 4）。
    *   两层防护级联方案（L1 路由 + L2 深度审核）。
*   **评估指标**：检测 F1 分数、推理延迟、生产模型 Token 消耗、攻击者识别准确率、保真度（Fidelity）与可追溯性（Traceability）的权衡。

### 4. 资源与算力
*   **硬件配置**：使用单台 8 × H20-96GB GPU 服务器。
*   **算力消耗**：整个实验流程（E1-E7）总计耗时少于 50 GPU 小时。由于路由器头部极小，训练仅需几分钟。

### 5. 实验数量与充分性
*   **实验覆盖**：共包含 7 个实验模块（E1-E7），涵盖了检测质量、欺骗保真度、成本交叉点、闭环增益、消融实验、资源耗尽防御及多轮越狱检测。
*   **客观性**：实验设计了多种对抗性变换（13 种）和黑盒/白盒攻击场景，并使用了交叉验证（不同模型家族的 Judge）来评估保真度，实验设计严谨且具有较强的说服力。

### 6. 主要结论与发现
*   **性能表现**：HoneyRoute 在 38ms 延迟下达到 0.911 的 F1 分数，性能接近级联方案但延迟降低了 385 倍。
*   **资源保护**：在洪水攻击下，生产模型 Token 消耗降低了 97.8%。
*   **闭环增益**：通过循环训练，误报率（FPR）降低了 9 倍， pooled F1 提升至 0.933。
*   **保真度边界**：揭示了“保真度-可追溯性”的权衡边界，证明了选择性伪装注入（Selective Camouflaged Injection）能在保持高可追溯性的同时，实现与生产模型 88.9% 的一致性。

### 7. 优点
*   **极低延迟**：通过轻量级路由头部，实现了生产环境可接受的防御开销。
*   **情报闭环**：不仅是防御，更将攻击转化为情报，实现了防御系统的持续增强。
*   **灵活性**：支持多种蜜罐实现，可根据攻击类型动态选择（如 DoS 攻击用代码蜜罐，越狱攻击用模型副本）。
*   **可解释性**：通过会话趋势分析（而非简单的阈值判断）有效识别多轮软越狱。

### 8. 不足与局限
*   **多语言偏差**：在处理中文良性技术问题时，冻结的嵌入主干存在误报（FP）风险。
*   **阈值依赖**：选择性诱饵注入的触发阈值需要针对不同部署环境进行调优。
*   **攻击覆盖**：虽然涵盖了多种攻击，但针对模型权重/行为提取的特定高级对抗手段尚未进行详尽的实证研究。
*   **Judge 依赖**：保真度评估依赖于 LLM 作为裁判，尽管进行了交叉验证，但仍存在潜在的评估偏差。

（完）
