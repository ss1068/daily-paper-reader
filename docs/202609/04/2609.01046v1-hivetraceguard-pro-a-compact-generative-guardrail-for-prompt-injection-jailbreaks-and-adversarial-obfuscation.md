---
title: "HiveTraceGuard-Pro: A Compact Generative Guardrail for Prompt Injection, Jailbreaks, and Adversarial Obfuscation"
title_zh: HiveTraceGuard-Pro：针对提示注入、越狱与对抗性混淆的轻量级生成式护栏模型
authors: "Nikita Oblakov, Sabrina Sadiekh, Evgeniy Kokuykin"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.01046v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对提示词注入、越狱和对抗性混淆的护栏
tldr: 针对现有安全护栏在俄语提示词注入及对抗模糊化防护上的不足，本文提出了HiveTraceGuard-Pro。这是一个基于Qwen3-0.6B微调的轻量级生成式护栏，利用俄英双语语料和8种模糊化变换进行训练。在实现14.3ms极低延迟的同时，取得了0.999的俄语注入召回率，权重已开源。
selection_source: fresh_fetch
motivation: 现有安全护栏模型缺乏对俄语提示词注入以及文本表面模糊化对抗攻击的有效评估与防护。
method: 基于Qwen3-0.6B通过LoRA微调构建0.6B参数生成式护栏，结合俄英双语配对数据及8种文本模糊化变换训练二分类判别。
result: 在15个模型对比中取得最低的14.3毫秒中位数延迟，以及最高的俄语提示词注入召回率（0.999）与鲁棒性F1值（0.88）。
conclusion: 开源了低延迟、高抵抗力的轻量化护栏模型权重，为大模型生产部署提供了高效的安全防护方案。
---

## 摘要
生产环境中的大语言模型（LLM）必须处理试图覆盖系统指令、绕过安全策略或诱导有害回复的输入。一种常见的缓解措施是使用独立的护栏模型。然而，现有的研究报告对俄语提示注入或俄语表面混淆的关注和证据较少。本文推出了 HiveTraceGuard-Pro，这是一个基于 Qwen3-0.6B 通过 LoRA 微调得到的 6 亿参数（0.6B）生成式护栏模型。它在俄语和英语上进行训练，并针对最终的目标对话轮次采用单一的二元评分规则（安全/不安全）。其训练语料库将存在对应项的有害示例与同一领域的良性示例进行配对，并对两类标签应用了八种混淆变换。在一个测试框架中，我们将 HiveTraceGuard-Pro 与 34 个其他护栏模型在 19 个基准组（其中 16 个为公开基准组）上进行了对比。它的综合指标得分为 0.7432，仅次于另外两个得分更高的模型（分别为 0.7641 和 0.7552）。仅在 16 个公开基准组上，其指标得分为 0.7153，测试套件中 34 个其他护栏模型中有 4 个得分更高。在 15 个模型的对比中，HiveTraceGuard-Pro 拥有最高的干净俄语鲁棒性综合 F1 值（0.88）和俄语提示注入召回率（0.999）。这两个结果均基于我们团队构建的俄语数据集，且提示注入数据集中至少有 27.1% 的数据与训练语料库重叠。在该次运行中，其 14.3 毫秒的中位数延迟在 15 个模型中最低。在整个测试套件中，其假阳性率（FPR）为 0.268，假阴性率（FNR）为 0.156。所有报告的回复结果均使用了传统的独立回复序列化方式，而非所发布对话模板中自然的助手角色路径。我们在 Hugging Face 上基于 Apache-2.0 协议开源了合并后的权重，而语料库、评估集和评估代码仍保持内部私有。

## Abstract
Production LLMs must handle inputs that attempt to override system instructions, bypass safety policies or elicit harmful responses. A common mitigation is a separate guardrail model. Existing reports, however, provide little evidence on Russian prompt injection or Russian surface obfuscation. We present HiveTraceGuard-Pro, a 0.6B generative guardrail LoRA-tuned from Qwen3-0.6B. It is trained on Russian and English and uses one binary scoring rule (safe/unsafe) for the final target turn. Its training corpus pairs harmful examples, where a counterpart exists, with benign examples from the same domain and applies eight obfuscation transforms to both labels. In one harness, we compare HiveTraceGuard-Pro with thirty-four other guards on nineteen benchmark groups, sixteen of which are public. Its aggregate key is 0.7432, behind 0.7641 and 0.7552 for the two higher-scoring guards. Over the sixteen public groups alone, its key is 0.7153 and four of the thirty-four other suite guards score higher. In a fifteen-model comparison, HiveTraceGuard-Pro has the highest clean Russian robustness combined-F1 (0.88) and Russian prompt-injection recall (0.999). Both results use Russian sets assembled by our team, and at least 27.1% of the prompt-injection set overlaps the training corpus. Its 14.3 ms median latency is the lowest among those fifteen models in that run. Across the suite, FPR is 0.268 and FNR is 0.156. All reported response results use a legacy standalone-reply serialization rather than the natural assistant-role path of the shipped chat template. We release the merged weights on Hugging Face under Apache-2.0. The corpus, evaluation sets and evaluation code remain internal.

---

## 论文详细总结（自动生成）

# HiveTraceGuard-Pro 论文深度总结与分析

---

### 1. 论文的核心问题与整体含义（研究动机与背景）

*   **安全护栏的现实挑战**：在线生成式大语言模型（LLM）频繁遭遇覆盖系统指令（提示注入）、绕过安全策略（越狱）以及文本表面混淆（对抗扰动）等攻击。独立于主模型之外的**安全护栏模型（Guardrail）**是常见的防御手段。
*   **俄语与对抗混淆研究空白**：现有的主流护栏模型（如 Llama Guard、ShieldGemma 等）多针对英语场景，缺乏对俄语提示注入以及俄语对抗性混淆（如拼音/转写、键盘布局替换、字符插入、非正式重写等）的防护能力与评估证据。
*   **低延迟与小体积需求**：工业级生产部署对推理延迟和显存占用有极苛刻的要求。大参数量（如 8B、15B）护栏模型延迟较高（60~240ms），难以直接嵌入实时对话流水线。
*   **核心目标**：构建一个参数量仅为 **0.6B** 的轻量级生成式二分类护栏模型（**HiveTraceGuard-Pro**），专攻俄语和英语的双语请求/回复安全防护，在极低延迟（<15ms）下实现对提示注入、越狱及对抗混淆的高鲁棒性检测。

---

### 2. 论文提出的方法论

#### 核心思想
基于小参数开源模型 **Qwen3-0.6B** 进行 LoRA 微调，采用 causal LM 头直接预测单 token 的二分类结果（`safe` 或 `unsafe`）。提出了“同领域成对数据构建”与“双向标签混淆增强”策略，提高对文本表面变化和语义边界的识别能力。

#### 关键技术细节与流程
1.  **二分类判定机制（Generative Argmax Scoring）**：
    *   模型不输出冗长的解释或多分类标签，仅在对话末端预测单 token 输出。
    *   比较 `safe` (ID: 18675) 与 `unsafe` (ID: 38157) 的未归一化 Logits 值，采取 $\text{Argmax}$ 策略判定（等价于 0.5 的 Softmax 阈值）。
2.  **同领域成对语料构建（Domain-Paired Corpus）**：
    *   在构建训练集时，尽可能将有害示例与同一主题下的良性示例成对放置（例如：“法西斯主义” 与 “法西斯主义历史”；“毒品” 与 “药理学”），严格测量并控制同主题下的误杀率（Over-blocking）。
3.  **双向标签混淆增强（Two-sided Obfuscation Augmentation）**：
    *   针对俄语及英语特征，设计了 8 种训练期混淆变换（包括随机大小写、键盘布局切换 `wrong_layout`、拉丁转写、符号插入/替换、Leet 字符、零宽字符插入、语音替换）。
    *   **关键创新**：同时将混淆变换施加于 `safe` 和 `unsafe` 两类标签，防止模型将“存在混淆特征”简单归因于“不安全”。
4.  **模板化序列化格式（Serialization Architecture）**：
    *   使用包含 `<POLICY>` 系统指令的专用 Chat Template，将历史对话封装为 `<CONVERSATION>` 块，并显式指定 `Target:` 目标轮次。在自然部署路径中，分别针对 User 轮次（请求护栏）与 Assistant 轮次（回复护栏）进行判别。

---

### 3. 实验设计

*   **对比模型规模（Suite & Cohorts）**：
    *   共包含 **35 个开源护栏模型**（参数量涵盖 0.1B 至 15B），如 YuFeng-XGuard (0.6B/8B)、Qwen3Guard-Gen (0.6B/4B/8B)、SingGuard (2B/4B)、Shieldstral-1.0-3B、Llama-Guard 3/4、OpenGuardrails-15B 等。
    *   挑选 15 个代表性护栏组成 Head-to-Head 核心对比组。
*   **数据集与 Benchmark 构成**：
    *   测试涵盖 **19 个基准组（Benchmark Groups），包含 44 个数据集**。其中 16 个为公开基准（如 Aegis-2.0, ToxicChat, HarmBench, BeaverTails, XSafety, SimpleSafetyTests, Aya Red-teaming, S-Eval 等），3 个为内部自研保留测试集（含干净/增强鲁棒性测试集和俄语提示注入集）。
*   **污染审计与保留确认集**：
    *   **数据污染审计**：使用 n-gram / containment 算法显式扫描测试集与训练集的重叠度，公开了俄语 Prompt-Injection 重叠率 $\ge 27.1\%$，Aegis-2.0 提示集重叠率 $21.3\%$。
    *   **保留确认集（`hivetrace/insecure-prompts`）**：模型冻结前完全隔离该数据集，在模型发布前仅盲测一次，用于评估对未知越狱/注入数据的迁移泛化能力。

---

### 4. 资源与算力消耗

*   **训练硬件与时长**：
    *   使用 **1 × NVIDIA A100-80GB GPU** 进行 LoRA 微调。
    *   训练时长约 **5.5 小时**（9,096 total steps，单步约 2.2 秒），训练峰值显存约 **39 GB**。
*   **推理 Footprint 与延迟**：
    *   模型权重占用显存仅 **1.11 GiB**。在 2048 长度上下文及 `logits_to_keep=1` 优化下，推理峰值显存占用仅 **1.39 GiB**。
    *   在 vLLM 单请求串行测试下，中位数端到端延迟（p50）仅为 **14.30 ms**（p95 为 28.79 ms），为所有对比护栏模型中最低。

---

### 5. 实验数量与充分性评价

*   **实验规模**：
    *   评分覆盖了 35 个模型在 77,643 条测试请求上的表现；
    *   开展了涵盖 14 类细分有害类别、8 种训练混淆、3 种评估混淆（`to_lower`, `to_upper`, `informal_rewrite`）的组合分析；
    *   进行了多轮数据消融（如包含/移除键盘布局增强、单向 vs 双向混淆、生成式头 vs 序列分类头对比、全局/分语言阈值 Sweep 扫描等）。
*   **客观性与公平性评估**：
    *   **优点**：未掩盖自身缺点，诚实披露了数据污染率、3 个自研测试集对综合指标的拉升作用、以及在英语公用集上的劣势；详细记录了不同 Serving engine（vLLM vs Eager Mode）对 Latency 的影响。
    *   **局限**：35 个对比模型在计算 19 组几何平均分（Aggregate Key）时，包含了 3 个该团队自研的数据集，对该模型本身存在局部利好；某些模型的官方 Response Template 需经过 Adapter 转译，可能未能发挥竞品最佳性能。

---

### 6. 论文的主要结论与发现

1.  **综合性能领先小模型**：在 19 组综合指标（Aggregate Key）上，HiveTraceGuard-Pro 取得 **0.7432**，在全量 35 个护栏中排名第 3，仅次于 YuFeng-XGuard-8B (0.7641) 与 SingGuard-2B (0.7552)；在小参数模型（<1B）中排名第 1。
2.  **俄语防护与低延迟双冠**：在俄语干净鲁棒性测试中取得最高 F1 (0.88)，俄语提示注入召回率达到 0.999；p50 推理延迟（14.
