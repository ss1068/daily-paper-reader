---
title: "ActGov: Governing LLM Agent Actions via Policy-Constrained Validation"
title_zh: ActGov：通过策略约束验证来管控大语言模型智能体的行为
authors: "Kaiyuan Zhang, Yuke Peng, Ke Jiang, Yinqian Zhang"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24446v2"
tags: ["query:jb-def"]
score: 8.0
evidence: 通过策略约束验证治理大语言模型智能体行为
tldr: ActGov是一个针对LLM智能体动作的运行时治理框架，旨在解决长周期工作流中因外部工具调用引发的安全风险。该框架通过统一的语义模型构建策略集，并利用SMT求解器进行验证。在运行时，ActGov对每个工具调用进行细粒度授权检查，确保其符合任务边界与安全约束，从而在不依赖LLM自身安全性的前提下，有效防御间接提示注入攻击，同时保持智能体任务执行的灵活性与高效性。
selection_source: fresh_fetch
motivation: 现有防御手段在处理动态工作流和扩展工具生态时表现脆弱，难以有效防止LLM智能体在执行长周期任务时越权操作。
method: 提出ActGov框架，通过构建统一的语义模型和SMT验证策略集，在运行时对每个工具调用进行细粒度的授权与合规性校验。
result: 在AgentDojo和AgentDyn基准测试中，ActGov显著降低了间接提示注入攻击的成功率，且在保持任务完成质量方面优于现有防御方案。
conclusion: ActGov证明了通过运行时策略强制执行，可以在不依赖LLM自身安全能力的情况下，实现对动态智能体执行过程的有效安全治理。
---

## 摘要
大语言模型（LLM）智能体越来越多地通过外部工具执行长程工作流，这使得不可信的输出可能影响后续操作并超出用户授权范围。现有的防御措施通常隔离注入内容，或利用预定义计划和静态策略来约束执行，但这些方法在动态工作流下较为脆弱，且难以在可扩展的工具生态系统中有效应用。在本文中，我们提出了 ActGov，这是一个运行时强制执行框架，旨在验证 LLM 提出的每一项工具操作，防止其产生外部影响。ActGov 基于授权、操作、运行时上下文和安全约束的统一语义模型构建；其 ActGov-Policy 组件通过工具规范、良性任务和观察到的失败轨迹迭代构建策略集，并利用基于 SMT 的反例检查对每次更新进行验证。在运行时，ActGov-Runtime 将每个工具调用抽象为有限的策略记录，仅在操作处于任务范围内的授权边界内且满足所有适用策略时才予以许可。这种逐项操作的强制执行机制确保了在长程、动态分支工作流中授权的持续有效性。我们在 AgentDojo 和 AgentDyn 基准测试中针对多种模型和攻击配置对 ActGov 进行了评估。结果表明，ActGov 在保持任务效用的同时，持续降低了间接提示注入攻击的成功率，显著优于现有防御手段。这些结果证明，ActGov 能够在不依赖底层 LLM 正确识别恶意指令的情况下，对动态智能体执行过程实施细粒度的授权管控。

## Abstract
Large language model (LLM) agents increasingly execute long-horizon workflows through external tools, allowing untrusted outputs to influence subsequent actions and exceed user authorization. Existing defenses isolate injected content or constrain execution with predefined plans and static policies, but these approaches are brittle under dynamic workflows and scale poorly across extensible tool ecosystems.   In this work, we present ActGov, a runtime enforcement framework that validates each LLM-proposed tool action before it causes external effects. Built on a unified semantic model of authorization, actions, runtime context, and security constraints, the ActGov-Policy component iteratively constructs a policy set from tool specifications, benign tasks, and observed failure traces, with each update verified through SMT-based counterexample checking. At runtime, ActGov-Runtime abstracts each tool call into finite policy records and permits it only if it remains within the task-scoped authorization boundary and satisfies all applicable policies. This per-action enforcement preserves authorization throughout long-horizon, dynamically branching workflows.   We evaluate ActGov on the AgentDojo and AgentDyn benchmarks across multiple models and attack configurations. It shows that ActGov consistently reduces the success rate of indirect prompt-injection attacks while preserving task utility, significantly outperforming existing defenses. These results demonstrate that ActGov can enforce fine-grained authorization over dynamic agent executions without relying on the underlying LLM to correctly identify malicious instructions.

---

## 论文详细总结（自动生成）

这是一份关于论文《ActGov: Governing LLM Agent Actions via Policy-Constrained Validation》的结构化总结：

### 1. 核心问题与研究动机
随着 LLM 智能体在金融、办公等高风险领域广泛应用，其通过外部工具执行长程工作流的能力带来了严重的安全隐患。核心问题在于**间接提示注入（IPI）**：攻击者通过工具返回的不可信内容（如网页、邮件）诱导智能体执行越权操作。现有防御手段（如静态策略、预定义计划）在面对动态、长程且分支复杂的工作流时，往往因缺乏上下文感知或过于僵化而导致安全性与任务效用（Utility）难以平衡。

### 2. 方法论：ActGov 框架
ActGov 的核心思想是将“动作意图”与“执行授权”解耦，通过形式化验证确保安全性。
*   **统一语义模型**：将复杂的运行时上下文抽象为二维记录空间（R），包含“形成级别”（确定性、语义边界、组合逻辑）和“语义范围”（动作、权限、参数、历史等）。
*   **ActGov-Policy（离线策略生成）**：利用 LLM 辅助生成策略，并使用 **Z3 SMT 求解器**进行形式化验证。通过迭代处理工具规范、良性任务和攻击轨迹，确保策略集不存在逻辑冲突，且满足预定义的安全不变量。
*   **ActGov-Runtime（在线强制执行）**：在智能体与工具之间部署监控器。智能体提出动作后，系统将其抽象为有限记录，并根据已验证的策略集进行确定性判定（允许/拒绝/审批）。这种设计避免了运行时昂贵的 SMT 计算。

### 3. 实验设计
*   **基准测试**：使用了 **AgentDojo**（多领域任务）和 **AgentDyn**（动态、开放式任务）两个主流基准。
*   **对比方法**：对比了无防御基线以及四种系统级防御方案：CaMeL（信息流控制）、Progent（权限控制）、DRIFT（动态规则验证）和 ACE（抽象计划约束）。
*   **模型后端**：评估了 Qwen3.6-flash、MiniMax-M2.5、DeepSeek-v4-pro 和 GPT-4o mini 四种模型。

### 4. 资源与算力
论文中**未明确提及**具体的 GPU 型号、数量或训练时长。文中主要强调了 ActGov-Policy 阶段的离线验证过程，以及运行时监控的低延迟特性，未对算力消耗进行量化分析。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了不同模型后端、不同基准测试集以及跨领域迁移测试。
*   **消融实验**：针对策略生成 LLM 的能力差异、训练数据规模（25% vs 50%）进行了详细对比。
*   **客观性**：实验采用了训练/测试集划分，并在测试集上进行评估，确保了结果的公平性。整体实验设计严谨，充分验证了框架的有效性。

### 6. 主要结论与发现
*   **安全性**：ActGov 在所有测试模型上均能将攻击成功率（ASR）降至极低水平（接近 0），显著优于现有防御方案。
*   **效用性**：在防御攻击的同时，ActGov 较好地保留了任务完成率，避免了因过度防御导致的“任务瘫痪”。
*   **鲁棒性**：证明了通过将 LLM 限制在“语义解析器”角色，而非“安全决策者”角色，可以有效规避 LLM 的幻觉和注入风险。

### 7. 优点
*   **形式化保障**：引入 SMT 求解器进行离线验证，提供了数学层面的安全保证。
*   **解耦设计**：将 LLM 的生成能力与安全策略的确定性执行分离，既保留了智能体的灵活性，又锁定了安全边界。
*   **可扩展性**：基于记录空间的抽象方法，使得策略可以随着工具生态的扩展而迭代，无需重写整个系统。

### 8. 不足与局限
*   **跨领域泛化能力**：实验表明，当策略在未见过的领域（Out-of-domain）应用时，为了维持绝对安全，系统会变得过于保守，导致任务效用大幅下降。
*   **依赖高质量训练数据**：策略的有效性高度依赖于训练阶段提供的良性任务和攻击轨迹的覆盖范围。
*   **语义解析风险**：虽然限制了 LLM 的角色，但 ActGov-Runtime 仍需 LLM 进行上下文抽象，如果该过程被深度对抗性攻击干扰，仍存在潜在风险。

（完）
