---
title: Does Deeper Reasoning Compromise Alignment? Revealing and Mitigating of Alignment Collapse in Large Reasoning Models
title_zh: 深度推理是否会损害对齐？揭示并缓解大型推理模型中的对齐崩溃现象
authors: "Yu-Hang Wu, Yu-Jie Xiong, Henghua Zhang, Bairui Zhang, Jia-Chen Zhang, Shaohua Li"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08186v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 缓解大型推理模型中的对齐崩溃
tldr: 本文研究了大型推理模型（LRM）中思维链（CoT）深度与安全对齐之间的矛盾。研究发现，随着推理深度增加，模型会出现“对齐崩溃”现象，导致防御能力显著下降。为此，作者提出了对齐损失率（ALR）指标来量化该风险，揭示了“注意力稀释”是根本原因，并提出了推理残差对齐（RRA）防御策略，通过动态重加权输入信息有效提升了模型的鲁棒性。
selection_source: fresh_fetch
motivation: 研究旨在揭示大型推理模型在进行深度思维链推理时，其安全对齐机制是否会因推理过程的复杂化而失效。
method: 提出了对齐损失率（ALR）指标量化崩溃程度，设计了“推理陷阱”（RT）攻击范式，并开发了推理残差对齐（RRA）防御策略。
result: 实验表明，推理深度增加会导致ALR显著上升，使得模型在面对对抗攻击时极易发生对齐崩溃。
conclusion: 通过引入推理残差对齐（RRA）机制，可以有效缓解注意力稀释问题，从而在保持深度推理能力的同时维持模型的安全性。
---

## 摘要
思维链（CoT）的出现为大型推理模型（LRMs）奠定了坚实的基础。尽管人们普遍认为深度推理能够增强安全对齐，但对齐机制在扩展推理下的稳定性仍未得到充分探索。本文挑战了这一主流观点，揭示了一个关键漏洞：深度推理可能会诱发对齐崩溃。为了严格量化这一现象，我们提出了对齐损失率（ALR）指标。实验表明，随着推理深度的增加，ALR 显著上升，这表明模型抵御外部扰动的鲁棒性严重下降。利用这种不稳定性，我们提出了一种新型越狱范式——推理陷阱（Reasoning Trap, RT）。RT 通过诱导模型进行扩展推理来放大对抗性攻击的影响，导致安全能力急剧下降。为了阐明这种崩溃背后的机制，我们确定了“注意力稀释”（Attention Dilution）是其根本原因，这源于扩展推理过程与原始输入之间对注意力的竞争。为了缓解这一问题，我们提出了推理残差对齐（Reasoning Residual Alignment, RRA），这是一种轻量级的防御策略，通过集成在推理过程中的残差连接来动态地重新强调输入。

## Abstract
The emergence of Chain-of-Thought (CoT) has established a robust foundation for Large Reasoning Models (LRMs). While deep reasoning is widely believed to enhance safety alignment, the stability of alignment mechanisms under extended reasoning remains underexplored. This paper challenges the prevailing view by revealing a critical vulnerability: Deep Reasoning May Induce Alignment Collapse. To rigorously quantify this phenomenon, we propose the Alignment Loss Rate (ALR) metric. Our experiments demonstrate that as reasoning depth increases, ALR rises significantly, indicating a severe degradation in model robustness against external perturbations. Capitalizing on this instability, a novel jailbreaking paradigm, Reasoning Trap (RT), is proposed. RT induces the model into extended reasoning to amplify the impact of adversarial attacks, leading to a sharp decline in safety capabilities. To elucidate the mechanism behind this collapse, we identify Attention Dilution as the root cause, arising from the competition for attention between the extended reasoning process and the original input. To mitigate this, Reasoning Residual Alignment (RRA), a lightweight defense strategy that dynamically re-emphasizes the input via residual connections integrated with the reasoning process.

---

## 论文详细总结（自动生成）

这是一份关于论文《深度推理是否会损害对齐？揭示并缓解大型推理模型中的对齐崩溃现象》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：思维链（CoT）技术显著提升了大型推理模型（LRMs）的逻辑与数学能力，社区普遍认为深度推理能增强模型的安全对齐。
*   **核心问题**：论文挑战了上述观点，提出“深度推理可能诱发对齐崩溃（Alignment Collapse）”。即模型在进行长链条推理时，其对原始指令的遵循能力和安全防御能力会因外部扰动而显著下降。

### 2. 方法论
*   **对齐损失率（ALR）**：定义了 $ALR(L) = \frac{A(L) - A'(L)}{A(L)} \times 100\%$，用于量化模型在不同推理深度 $L$ 下，面对扰动时的性能衰减程度。
*   **推理陷阱（Reasoning Trap, RT）**：一种新型越狱范式。通过在提示词中加入推理触发器，诱导模型进行不必要的深度推理，从而放大对抗性攻击（如 FlipAttack, PAP, ArtPrompt）的效果。
*   **注意力稀释（Attention Dilution）机制**：通过分析 Transformer 的注意力分布，发现随着推理过程（$Y$）的延长，模型对原始输入（$X$）的注意力权重被稀释，导致模型在生成阶段“遗忘”了初始的安全约束。
*   **推理残差对齐（RRA）**：一种无需训练的防御策略。通过在推理过程后重新注入原始输入（即构建 $[X; Y; X]$ 的上下文结构），强制模型在最终决策前重新关注初始指令，从而抵消注意力稀释。

### 3. 实验设计
*   **数据集**：使用 AIME2024（数学推理）、LogicAsker（逻辑推理）以及 AdvBench（安全对齐/越狱测试）。
*   **基准模型**：涵盖了开源模型（Qwen3-8B/14B, Llama2-7B/13B, DeepSeek-V3/R1）和闭源模型（Claude-Haiku-4.5, GLM4.6）。
*   **对比方法**：对比了标准模式（$L=0$）与不同深度推理模式（$L=2048, 4096$）下的性能表现，并对比了集成 RRA 前后的安全拒绝成功率（RSR）。

### 4. 资源与算力
*   **硬件配置**：Qwen3 和 Llama2 系列模型在两张 NVIDIA A100-80GB GPU 上进行本地运行。
*   **其他模型**：DeepSeek 系列、Claude-Haiku-4.5 和 GLM4.6 通过 API 进行评估。文中未明确提及具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度验证，包括跨数据集验证、跨模型架构验证、以及针对 RRA 不同变体的消融实验。
*   **充分性与公平性**：为确保统计可靠性，对 AIME2024 数据集进行了八次独立重复实验，并报告了标准差。实验设计严谨，通过控制变量法（如固定温度参数）保证了对比的公平性。

### 6. 主要结论与发现
*   **对齐崩溃现象**：推理深度与对齐鲁棒性呈负相关，深度推理显著放大了模型对外部扰动的敏感度。
*   **机制解释**：注意力稀释是导致崩溃的根本原因，长上下文中的推理步骤在注意力分配上与安全指令形成了竞争。
*   **防御有效性**：RRA 策略在不增加额外训练成本的情况下，能有效恢复模型的安全对齐能力。

### 7. 优点
*   **创新性**：首次从机制层面（注意力稀释）解释了推理深度与安全对齐之间的矛盾。
*   **实用性**：提出的 RRA 防御策略简单高效，无需重新训练模型，易于在现有推理系统中部署。
*   **严谨性**：通过数学推导（基于 RoPE 的位置编码衰减）和实证分析相结合，增强了结论的说服力。

### 8. 不足与局限
*   **覆盖范围**：实验主要集中在特定的推理模型和扰动类型上，未能穷尽所有现代 LRM 架构及现实世界中复杂的交互场景。
*   **机制分析局限**：注意力机制分析虽然提供了有力证据，但可能未完全涵盖导致鲁棒性下降的所有潜在因素（如模型内部表征的漂移）。
*   **应用限制**：RRA 策略虽然有效，但增加了上下文长度（输入被重复），在极长上下文限制的场景下可能存在效率损耗。

（完）
