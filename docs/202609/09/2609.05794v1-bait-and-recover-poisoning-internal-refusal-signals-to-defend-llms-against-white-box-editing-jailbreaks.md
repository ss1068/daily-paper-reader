---
title: "Bait-and-Recover: Poisoning Internal Refusal Signals to Defend LLMs against White-Box Editing Jailbreaks"
title_zh: 诱饵与恢复：通过污染内部拒绝信号防御针对大语言模型的白盒编辑越狱
authors: "Tian Gao, Zhipeng Xie, Yuhao Wu, Junhua Liu, Xin Fang"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.05794v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 使用诱饵与恢复机制防御白盒编辑越狱
tldr: 针对开源大模型面临的白盒表示工程攻击（通过修改投影矩阵抑制安全对齐），本文提出了一种名为Bait-and-Recover的防御机制。该方法通过在激活路径中植入“诱饵”适配器来污染攻击者的观测信号，并利用后续层的“恢复”适配器修复计算，从而在不影响模型通用能力的前提下，显著提升了模型在白盒攻击下的拒绝率。
selection_source: fresh_fetch
motivation: 旨在防御攻击者通过白盒手段快速定位并抑制模型内部拒绝机制的轻量级攻击。
method: 通过梯度路由训练，在攻击者观测路径植入污染信号的诱饵适配器，并在后续层部署恢复适配器以保障正常计算。
result: "在保持模型通用性能的同时，将模型在白盒编辑攻击下的最低拒绝率从16.25%提升至71.75%。"
conclusion: 通过破坏攻击者的观测假设，该方法为大模型安全提供了一种高效且实用的防御补充方案。
---

## 摘要
开源大语言模型正面临来自表征工程攻击的低成本白盒威胁。攻击者可以在单张 GPU 上，无需基于梯度的训练，仅需几分钟即可估计出拒绝方向，并搜索出能够抑制安全对齐同时保留通用能力的投影矩阵编辑。我们提出了“诱饵与恢复”（Bait-and-Recover）防御机制，这是一种权重层面的防御，通过在攻击者读取激活值的位置放置一个诱饵适配器，并在后续层放置一个配对的恢复适配器。该机制通过梯度路由进行训练，将观测路径与行为路径解耦。通过主动污染用于测量的残差信号，“诱饵与恢复”能够干扰攻击者的编辑搜索，同时由恢复层还原干净的下游计算。在四个开源模型上的实验表明，在严格的行为保留预算（KL <= 0.10）下，我们的防御将针对白盒编辑搜索的最低拒绝率从 16.25% 提升至 71.75%，且对通用基准测试的影响微乎其微。通过使这些攻击的核心测量假设失效，观测路径污染为行为层面的安全训练提供了一种实用的补充方案。

## Abstract
Open-weight large language models face a low-cost white-box threat from representation engineering attacks. Attackers can estimate refusal directions and search for projection-matrix edits that suppress safety alignment while preserving general capabilities, within minutes on a single GPU and without gradient-based training. We propose Bait-and-Recover, a weight-level defense that places a bait adapter where attackers read activations and a paired recovery adapter at the subsequent layer. Trained via gradient routing, this decouples the observation path from the behavior path. By actively poisoning the residual signal used for measurement, Bait-and-Recover disrupts the attacker's edit search, while the recovery layer restores clean downstream computation. Across four open-weight models, our defense raises the minimum refusal rate against white-box edit searches from 16.25% to 71.75% under a strict behavior-preservation budget (KL <= 0.10), with negligible impact on general benchmarks. By invalidating the core measurement assumption of these attacks, observation-path poisoning offers a practical complement to behavior-level safety training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bait-and-Recover: Poisoning Internal Refusal Signals to Defend LLMs against White-Box Editing Jailbreaks》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：开源大语言模型（LLM）面临“表征工程”攻击（如 Heretic 攻击），攻击者通过分析模型内部激活值，定位并抑制与“拒绝行为”相关的残差方向，从而在不破坏模型通用能力的前提下，通过修改投影矩阵实现“越狱”。
*   **动机**：现有的安全对齐（如 SFT、RLHF）主要约束输出行为，无法保护内部表征免受白盒编辑。论文旨在通过“观测-行为路径解耦”，使攻击者无法准确测量拒绝方向，从而从源头上使白盒编辑攻击失效。

### 2. 方法论：Bait-and-Recover
*   **核心思想**：将模型的计算路径分为“观测路径”和“行为路径”。在攻击者读取激活值的层（诱饵层）植入污染信号，并在下一层（恢复层）通过适配器将残差修正回正常状态。
*   **关键技术**：
    *   **层带选择**：基于 Fisher 判别比（Fisher Ratio）识别模型中拒绝信号最显著的层，作为防御部署区域。
    *   **诱饵适配器（Bait Adapter）**：在 `down_proj` 处植入低秩适配器，通过“拒绝取消通道”和“标签通道”污染攻击者的测量结果，同时保持对下游计算的干扰。
    *   **恢复适配器（Recovery Adapter）**：在 `o_proj` 和 `down_proj` 处部署，通过学习反向映射，抵消诱饵带来的残差偏移。
    *   **梯度路由（Gradient Routing）**：训练时强制恢复层梯度不更新诱饵参数，确保两者功能解耦。

### 3. 实验设计
*   **数据集**：使用 Alpaca-GPT4 指令集（无害/通用）和 SafeMTData（有害）进行防御训练；使用 `mlabonne/harmless_alpaca` 和 `harmful_behaviors` 进行攻击评估。
*   **基准测试**：使用 MMLU-Redux 和 IFEval 评估通用能力保留情况；使用 Heretic 攻击框架（200 次搜索试验）评估防御后的拒绝率。
*   **对比方法**：主要对比防御前后的模型在不同 KL 预算（0.05 到 1.0）下的拒绝率表现。

### 4. 资源与算力
*   **算力说明**：论文提到攻击搜索在单张 GPU 上即可在几分钟内完成。防御训练的具体 GPU 型号未明确列出，但强调了该方法的高效性，且适配器训练过程相对轻量。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 Qwen3（4B/8B）和 Gemma-3（1B/12B）四个不同规模和架构的模型，具有较好的代表性。
*   **客观性**：实验设置了严格的 KL 预算（KL ≤ 0.10），确保防御效果是在不显著改变模型行为的前提下取得的，评估指标（拒绝率、MMLU、IFEval）较为全面，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **防御有效性**：在严格的 KL 预算下，防御后的模型平均最低拒绝率从 16.25% 提升至 71.75%，防御效果显著。
*   **通用性**：该防御对模型通用能力（MMLU/IFEval）的影响几乎可以忽略不计。
*   **攻击面转移**：防御后，攻击者依然能通过“全局搜索”找到部分漏洞，但“逐层搜索”的有效性被大幅削弱。

### 7. 优点
*   **创新性**：从“防御输出”转向“防御测量”，通过污染攻击者的观测假设来保护模型，思路独特。
*   **实用性**：防御以适配器形式存在，最终可合并入权重，无需额外的推理时开销。
*   **鲁棒性**：在不同模型架构上均表现出一致的防御增益。

### 8. 不足与局限
*   **攻击者适应性**：未评估攻击者使用更高级的估计器（如 PCA、LDA 或几何中位数）时的防御效果。
*   **高 KL 预算风险**：当允许模型行为发生较大改变（KL > 0.20）时，防御效果会下降。
*   **无法防御微调攻击**：该方法仅针对白盒编辑攻击，无法防御攻击者通过收集数据进行全参数或 LoRA 微调的攻击。

（完）
