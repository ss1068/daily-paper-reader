---
title: "ACEA: An Adversarial Co-Evolution Arena for Head-to-Head Red-Team and Blue-Team LLM Testing"
title_zh: ACEA：用于大语言模型红蓝对抗测试的对抗性协同进化竞技场
authors: "Yi Ting Shen, Kentaroh Toyoda, Alex Leung"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08256v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 大模型红蓝对抗测试竞技场
tldr: ACEA是一个用于LLM红蓝对抗的自动化评估平台。针对当前红蓝团队开发与测试脱节的问题，该平台通过ASAP协议实现模型无关的插件式接入，利用包含机密信息的种子数据进行可验证的攻击与防御评估。ACEA通过实时可视化、详细的失败定位报告及上下文改进循环，为提升LLM安全性提供了标准化的对抗演练环境。
selection_source: fresh_fetch
motivation: 解决当前LLM红队攻击与蓝队防御在开发和测试中相互隔离、评估结果难以信任的问题。
method: 构建了基于ASAP协议的插件式对抗竞技场，通过引入机密种子数据和独立的攻击/防御效能评估指标进行量化分析。
result: 实现了对红队攻击强度与蓝队防御效果的精细化分解，并提供可操作的失败定位报告。
conclusion: ACEA通过标准化的对抗演练和反馈机制，为提升LLM的安全防御能力提供了一个高效、可信的评估框架。
---

## 摘要
针对大语言模型（LLM）的自动化红队攻击与蓝队防御技术正迅速发展。然而，攻击者与防御者的构建和测试往往相互隔离，导致所得评分难以令人信服。为解决这一问题，我们提出了 ACEA（对抗性协同进化竞技场），这是一个将可插拔的红队适配器和蓝队适配器连接至共享目标 LLM，并利用 LLM 裁判对其攻击与防御率进行评分的平台。ACEA 包含四个核心组件：首先，是一个可插拔、与模型无关的竞技场。任何红队或蓝队项目均可通过我们定义的“ACEA 标准适配器协议”（ASAP）这一极简 HTTP 协议接入。该协议支持任何编程语言，仅需实现该协议即可成为完整参与者。其次，是一套专为对抗性回合设计的评估方法。通过向目标模型植入规范化秘密信息，可提供可验证的真实依据，从而区分真实泄露与模型幻觉。此外，即使防御方拦截了攻击，我们仍会将攻击请求发送至目标模型，以独立衡量攻击的原始效力。这些方法共同实现了对攻击强度和防御有效性的逐回合分解。第三，是一个具备实时游戏化可视化效果的系统，并提供详细的战后报告以定位每次失败。这使得评估结果成为改进红队或蓝队项目的可操作信号。第四，是一个可选的上下文改进循环，将每回合的结果转化为下一回合的建议提示。适配器无需保持状态，只需读取这些提示即可在多回合中实现自我优化。我们详细介绍了 ACEA 的设计及其对红蓝双方进行头对头评分的指标体系。

## Abstract
Automated red-team attacks and blue-team defenses for large language models (LLMs) are advancing quickly. However, attackers and defenders are built and tested in isolation, and the resulting scores are hard to trust. To tackle this, we present ACEA (Adversarial Co-Evolution Arena), a platform that connects a pluggable red-team adapter and a pluggable blue-team adapter to a shared target LLM and scores their attack and defense rates with an LLM judge. ACEA contributes four components. First, a pluggable, model-agnostic arena. Any red or blue project connects over a minimal HTTP protocol, which we call the ACEA Standard Adapter Protocol (ASAP). It can be written in any language, and a project that exposes nothing but the protocol is a full participant. Second, an evaluation methodology built for adversarial rounds. Seeding the target with canonical secrets gives verifiable ground truth that separates real leakage from hallucination. We also send each attack to the target even when the defense blocks it, which measures the attack's raw potency independently of whether it was stopped. Together these yield a per-round decomposition of attack strength and defense effectiveness. Third, a real-time, game-style visualization with a detailed end-of-battle report that localizes each failure. The evaluation thus becomes an actionable signal for improving a red or blue project. Fourth, an optional in-context improvement loop that turns each round's outcome into advisory hints for the next. An adapter can then adapt across rounds without keeping state, provided it reads the hints. We describe the design of ACEA and the metrics through which red and blue teams are scored head to head.

---

## 论文详细总结（自动生成）

这是一份关于论文《ACEA: An Adversarial Co-Evolution Arena for Head-to-Head Red-Team and Blue-Team LLM Testing》的结构化总结：

### 1. 核心问题与研究动机
当前大语言模型（LLM）的安全评估存在三个主要痛点：
*   **隔离性**：红队攻击与蓝队防御在开发和测试中完全脱节，缺乏统一的对抗平台。
*   **评分不可信**：现有的 LLM 裁判难以区分“真实数据泄露”与“模型幻觉”，且当防御方拦截攻击时，无法衡量攻击本身的原始强度，导致攻击效能与防御效果混淆。
*   **缺乏实时反馈**：评估结果通常是事后报告，缺乏对抗过程中的实时监控与动态分析。

### 2. 方法论：ACEA 平台
ACEA 的核心思想是构建一个**模型无关、插件式、可实时观测的对抗竞技场**：
*   **ASAP 协议**：定义了极简的 HTTP 接口（ACEA Standard Adapter Protocol），允许任何语言编写的红/蓝适配器接入，无需暴露模型权重。
*   **可验证的真实性**：通过向目标 LLM 植入“规范化秘密信息”（Canonical Secrets），将泄露判定从模糊的语义判断转变为可验证的 ground truth。
*   **对抗流程分解**：即使防御方拦截了攻击，系统仍会生成目标响应并进行评分（但不交付给用户），从而独立测量攻击的“原始效力”与防御的“拦截效能”。
*   **上下文改进循环**：通过将每回合的评估结果转化为“进化建议”（Evolution Hints），使无状态的适配器也能在多回合中实现自我优化。

### 3. 实验设计
*   **场景设置**：使用多种防御框架（三阶段分类器 vs. 极简拦截器）与多种目标模型（GPT-4o, Gemini-2.5-Pro, Gemma-3 等）进行对抗。
*   **基准测试**：通过 30 回合的对抗演练，对比不同防御框架在不同目标模型下的攻击成功率（ASR）和防御率（DR）。
*   **对比方法**：对比了不同防御模型（大模型 vs. 小模型）以及不同防御框架（复杂 vs. 极简）的有效性。

### 4. 资源与算力
*   **算力说明**：论文**未明确提供**具体的 GPU 型号、数量或训练时长。文中提到 ACEA 是基于容器化服务运行的，计算开销主要源于每回合多次调用 LLM（红队、蓝队、目标、裁判）。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组防御-目标配对实验，每组 30 回合。
*   **客观性与公平性**：实验设计较为客观，通过引入“极简防御”作为基准，验证了系统不会出现“全零”的饱和现象。但由于部分实验（如 Target 3）数据不完整，且部分结果基于单次运行而非多次平均，其统计显著性在极端情况下可能存在波动。

### 6. 主要结论与发现
*   **防御框架优于模型本身**：实验表明，防御框架的设计对安全性的影响远大于其背后的模型规模。
*   **目标模型决定上限**：攻击能否成功很大程度上取决于目标模型自身的对齐程度，而非防御手段。
*   **指标分解的必要性**：通过区分“原始攻击效力”与“防御拦截率”，能够更精准地识别防御短板。

### 7. 优点
*   **高度可扩展**：ASAP 协议使得任何第三方模型或工具都能轻松接入。
*   **可操作性强**：通过实时可视化和详细的战后报告，将抽象的评估转化为具体的改进建议。
*   **评估科学**：引入种子数据解决了 LLM 裁判在数据泄露判定上的幻觉问题。

### 8. 不足与局限
*   **依赖 LLM 裁判**：尽管引入了种子数据，但对于非泄露类的有害性判断，仍依赖 LLM 裁判，存在偏见风险。
*   **计算成本高**：每回合涉及多次 LLM 调用，大规模对抗测试的算力成本较高。
*   **单点对抗限制**：目前仅支持一对一的红蓝对抗，暂不支持多智能体协作或跨目标的迁移攻击评估。
*   **饱和度感知缺失**：系统缺乏对防御饱和点的自动检测，无法判断何时应停止测试。

（完）
