---
title: "CoRL: Co-Evolutionary Reinforcement Learning for Adaptive Indirect Prompt-Injection Attacks and Defenses"
title_zh: CoRL：用于自适应间接提示注入攻击与防御的协同进化强化学习
authors: "Boyang Zhang, Qingxin Xiao, Lingwei Dang, Qingyao Wu"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.07529v1"
tags: ["query:llm"]
score: 9.0
evidence: 自适应间接提示注入攻击与防御
tldr: 针对工具增强型语言代理面临的间接提示注入（IPI）威胁，本文提出CoRL框架。该研究将自适应IPI建模为非对称马尔可夫博弈，通过攻击者SFT、双边协同近端策略优化（Co-PPO）及验证器引导的防御者微调，实现了攻击与防御的协同进化，有效提升了代理在面对动态注入攻击时的鲁棒性与任务完成率。
selection_source: fresh_fetch
motivation: 现有防御机制难以应对攻击者不断变化的策略、注入位置和载荷，导致其在面对自适应间接提示注入时失效。
method: 提出CoRL框架，通过攻击者SFT初始化、双边Co-PPO协同训练以及基于验证器反馈的防御者微调，实现攻防双方的动态博弈与进化。
result: "实验表明，CoRL将攻击成功率（ASR）降低了38.5个百分点至0.0%，同时将任务效用提升了13.1个百分点至76.3%。"
conclusion: CoRL通过协同进化机制有效平衡了语言代理的安全性与任务效用，并为自适应红队评估提供了有效的攻击候选集。
---

## 摘要
工具增强型语言智能体极易受到间接提示注入（IPI）的攻击。与直接提示注入不同，IPI 将对抗性指令隐藏在不可信的工具输出中，能够隐蔽地改变合法任务的执行过程。由于攻击者会不断改变其策略、注入位置和载荷，基于固定攻击训练的防御机制往往会失效。为了解决这一问题，我们将自适应 IPI 建模为一个非对称、部分可观测的通用和马尔可夫博弈：多轮攻击者根据公共轨迹中已达到的工具返回点调整载荷，而使用工具的防御者必须在拦截注入目标的同时完成用户任务。我们提出了 CoRL，这是一个基于验证器的协同进化与修复框架，包含三个阶段：攻击者 SFT（监督微调）从成功轨迹中初始化多轮攻击；双边 Co-PPO（协同近端策略优化）利用角色特定的奖励和历史对手群体共同训练两个智能体；防御者 SFT 则整合经验证器认可的教师修复方案，以应对群体发现的失败案例。在针对每个防御者进行的 1,514 次干净、固定模板及自适应执行测试中，CoRL 将整体攻击成功率（ASR）降低了 38.5 个百分点至 0.0%，并将效用提升了 13.1 个百分点至 76.3%。分阶段和受控消融实验表明，在线 Co-PPO 和基于群体挖掘的修复方案均做出了积极贡献，而外部基准评估则显示了其在攻击防御方面的迁移能力。该防御者在评估的攻击下实现了安全性与任务效用之间的平衡，同时保留的攻击者也为自适应红队评估提供了候选样本。

## Abstract
Tool-augmented language agents are vulnerable to indirect prompt injection (IPI). Unlike direct prompt injection, IPI hides adversarial instructions in untrusted tool outputs and can covertly alter the execution of a legitimate task. Defenses trained on fixed attacks may fail as an attacker changes its strategy, injection site, and payload. To address this problem, we formulate adaptive IPI as an asymmetric, partially observable, general-sum Markov game: a multi-turn attacker adapts payloads at reached tool-return sites from the public trajectory, while a tool-using defender must block the injected objective and complete the user task. We propose CoRL, a verifier-grounded co-evolution and repair framework with three stages: Attacker SFT initializes multi-turn attacks from successful trajectories; bilateral Co-PPO jointly trains both agents with role-specific rewards and historical opponent populations; and Defender SFT consolidates verifier-accepted teacher repairs for population-discovered failures. Across 1,514 clean, fixed-template, and adaptive executions per defender, CoRL reduces overall ASR by 38.5 points to 0.0% and raises utility by 13.1 points to 76.3%. Stage-wise and controlled ablations show positive contributions from online Co-PPO and population-mined repair, while external-benchmark evaluation indicates transfer in attack resistance. The defender balances safety and task utility under the evaluated attacks, while the retained attackers provide candidates for adaptive red-team evaluation.

---

## 论文详细总结（自动生成）

这是一份关于论文《CoRL: Co-Evolutionary Reinforcement Learning for Adaptive Indirect Prompt-Injection Attacks and Defenses》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：工具增强型语言智能体（Tool-augmented language agents）在处理网页、邮件等不可信外部数据时，极易遭受**间接提示注入（IPI）**攻击。
*   **痛点**：现有的防御机制多基于固定攻击分布训练，难以应对攻击者根据公共轨迹、注入位置和历史尝试进行动态调整的**自适应攻击**。
*   **目标**：构建一个能够持续发现自适应攻击失败案例，并将其转化为防御修复方案的框架，同时在拦截攻击与保持任务效用（Utility）之间取得平衡。

### 2. 方法论：CoRL 框架
CoRL 将自适应 IPI 建模为非对称、部分可观测的通用和马尔可夫博弈，包含三个核心阶段：
*   **阶段 I：攻击者 SFT（监督微调）**：利用验证器筛选出的成功攻击轨迹，对攻击者模型进行初始化，确保其具备多轮探索能力。
*   **阶段 II：双边 Co-PPO（协同近端策略优化）**：
    *   **协同进化**：防御者与攻击者在在线沙盒中共同训练，双方均面对当前策略及历史对手群体（Historical Opponent Populations），以防止过拟合。
    *   **奖励机制**：采用角色特定的奖励函数。防御者在拦截攻击且完成任务时获得最高奖励；攻击者则通过达成注入目标获得奖励。
*   **阶段 III：防御者 SFT（群体引导修复）**：利用阶段 II 中保留的强攻击者作为挑战，收集防御者成功拦截并完成任务的“教师演示”轨迹，通过监督微调巩固防御能力。

### 3. 实验设计
*   **数据集与场景**：基于 AgentDyn 和 AgentDojo 构建了包含 7 个领域（如 Banking, GitHub, Shopping 等）的评估环境，涵盖 1,514 次执行测试。
*   **对比方法**：
    *   **Base**：原始 Qwen3.5-9B 模型。
    *   **PPO Baseline**：仅针对固定模板进行防御者 RL 训练。
    *   **NoPop**：无历史对手群体的协同训练。
    *   **Co-PPO**：仅在线协同训练阶段的检查点。
*   **外部基准**：在 AgentLAB（Task-Injection 任务）和 InjecAgent 上进行了迁移能力评估。

### 4. 资源与算力
*   **算力配置**：
    *   **SFT 阶段**：每阶段使用 16 块 GPU（DeepSpeed ZeRO-3）。
    *   **Co-PPO 阶段**：使用 56 块 GPU（verl/Megatron, vLLM, Ray）。
*   **说明**：论文明确列出了 GPU 数量及使用的分布式训练框架，但未详细说明具体的训练时长（小时数）。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的基准测试（1,514 次执行）以及 2×2 的受控消融实验（对比初始化方式与修复数据来源）。
*   **客观性**：通过对比不同基线模型、外部基准迁移测试以及历史对手池评估，实验设计较为全面且具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：CoRL 将整体攻击成功率（ASR）降至 0.0%，并将任务效用提升至 76.3%。
*   **协同效应**：在线 Co-PPO 提供的防御者初始化与群体挖掘的修复数据对最终性能均有显著贡献，两者结合效果最佳。
*   **迁移能力**：在 AgentLAB 等外部基准测试中，CoRL 表现出优于基线模型的攻击抵抗力。

### 7. 优点
*   **动态博弈视角**：首次将 IPI 建模为攻防协同进化的马尔可夫博弈，有效解决了防御者对自适应攻击的“遗忘”问题。
*   **发现与修复闭环**：通过保留历史攻击者作为“磨刀石”，实现了防御能力的持续迭代。
*   **安全性与效用平衡**：通过明确的奖励函数设计，避免了防御者为了安全而采取“全盘拒绝”的极端策略。

### 8. 不足与局限
*   **收敛性保证**：由于是通用和博弈，CoRL 无法保证收敛到纳什均衡，且缺乏对策略可利用性的严格估计。
*   **验证器依赖**：依赖确定性的安全与任务验证器，若验证器本身存在偏差或覆盖范围有限，防御效果会受限。
*   **非通用鲁棒性**：尽管在评估集上表现优异，但仍存在非零的残余失败率，且未证明对所有未见过的攻击模式具有绝对鲁棒性。

（完）
