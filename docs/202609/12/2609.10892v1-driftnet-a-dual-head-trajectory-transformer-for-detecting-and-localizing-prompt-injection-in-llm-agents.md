---
title: "DriftNet: A Dual-Head Trajectory Transformer for Detecting and Localizing Prompt Injection in LLM Agents"
title_zh: DriftNet：一种用于检测和定位大模型智能体提示词注入的双头轨迹 Transformer
authors: "Asif Pinjari, Mithun Paul Saint-Germain"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10892v1"
tags: ["query:llm"]
score: 8.0
evidence: 检测并定位LLM智能体中的提示注入
tldr: DriftNet是一款针对LLM智能体间接提示词注入攻击的检测与定位系统。它采用双头Transformer架构，通过分析工具调用轨迹，在单次前向传播中实现攻击分类及注入点、劫持范围的精细化定位。该模型无需访问智能体内部模型，在AgentDrift基准测试中表现卓越，显著优于现有基线，为智能体安全监控提供了高效、精准的解决方案。
selection_source: fresh_fetch
motivation: 现有系统难以同时提供攻击发生位置、受损步骤及防御效果的全面诊断，且缺乏对复杂注入轨迹的细粒度分析能力。
method: 提出DriftNet双头Transformer模型，利用冻结的句子编码器和世界特征嵌入，通过联合目标函数对轨迹进行分类及步骤级标签预测。
result: 在AgentDrift基准测试中，DriftNet实现了0.983的轨迹级F1分数，并能以极高精度恢复注入点和劫持范围，同时保持极低的误报率。
conclusion: DriftNet证明了通过分析工具调用轨迹，无需访问智能体模型即可实现对间接提示词注入攻击的高效、多维度检测与定位。
---

## 摘要
当针对大模型（LLM）智能体的间接提示词注入攻击成功时，这种妥协会在智能体的行为中显现出来：表现为良性的工具调用前缀、被投毒的观测结果，以及服务于攻击者的后续动作。操作员需要了解三个事实：攻击从何处进入、哪些步骤被破坏，以及明显的投毒尝试是否被抵御。现有的系统要么给出整个轨迹的判定结果，要么仅提供单个不安全索引。我们提出了 DriftNet，这是一种双头轨迹 Transformer，它通过读取记录的工具调用轨迹，在一次前向传播中回答上述所有三个问题：第一个头将轨迹分类为是否受损，第二个头为每个步骤分配四种标签之一（良性、注入点、被劫持、注入失败）。据我们所知，这是第一个能够产生这种联合输出的监督式检测器。该模型使用冻结的句子编码器和四个无身份的世界特征对每个步骤进行嵌入；其训练后的主干网络参数量不到两百万，并通过两个头的类加权联合目标进行优化，无需访问智能体的模型。在 AgentDrift 基准测试的任务不相交划分（12,536 条轨迹，71,024 个标记步骤）上，通过 20 种配置的扫描将超参数敏感度限制在 0.011 F1 以内，且测试集仅评估一次，DriftNet 的轨迹级 F1 分数达到 0.983，在 98.7% 的受攻击轨迹上实现了精确的注入点恢复，被劫持区间的 IoU 为 0.979，在 218 次被抵御的攻击中零误报，在困难负样本上的误报率为 2.9%。在相同划分上重新训练的表面基线模型仅能恢复 11.1% 的部分劫持和 17.1% 的延迟执行；而 DriftNet 分别达到了 98.6% 和 93.2%，同时降低了所有误报率。对全部 26 个残余错误的分析表明，大多数漏检源于标记的注入观测结果中不包含可读指令，我们随结果报告了该基准测试中测得的世界身份规律性。

## Abstract
When an indirect prompt injection succeeds against an LLM agent, the compromise is visible in the agent's own behavior: a benign prefix of tool calls, a poisoned observation, and a suffix of actions that serve the attacker. An operator needs three facts: where the attack entered, which steps it corrupted, and whether apparent poison was resisted. Existing systems return either a whole-trace verdict or a single unsafe index. We present DriftNet, a dual-head trajectory Transformer that reads a logged tool-call trajectory and answers all three questions in one forward pass: one head classifies the trajectory as compromised or not, and a second assigns every step one of four labels (benign, injection point, hijacked, failed injection). To our knowledge it is the first supervised detector to produce this joint output. A frozen sentence encoder and four identity-free world features embed each step; the trained trunk, under two million parameters and optimized with a class-weighted joint objective over both heads, needs no access to the agent's model. On the task-disjoint split of the AgentDrift benchmark (12,536 trajectories, 71,024 labeled steps), with a 20-configuration sweep bounding hyperparameter sensitivity to 0.011 F1 and the test part evaluated exactly once, DriftNet reaches trajectory-level F1 of 0.983, exact injection-point recovery on 98.7% of attacked trajectories, hijacked-span IoU of 0.979, zero flags on 218 resisted attacks, and 2.9% flags on hard negatives. A surface baseline retrained on the identical split recovers 11.1% of partial hijacks and 17.1% of delayed executions; DriftNet reaches 98.6% and 93.2% while lowering every false-alarm rate. Reading all 26 residual errors shows that most misses trace to trajectories whose labeled injection observation carries no legible instruction, and we report the benchmark's measured world-identity regularity alongside the results.

---

## 论文详细总结（自动生成）

这是一份关于论文《DriftNet: A Dual-Head Trajectory Transformer for Detecting and Localizing Prompt Injection in LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：LLM 智能体通过工具调用与世界交互，面临“间接提示词注入”攻击。攻击成功时，智能体行为会发生“漂移”（从良性任务转向攻击者目标）。
*   **痛点**：现有防御系统要么仅给出轨迹层面的“是/否”判定，要么仅定位单个异常步骤。操作员缺乏对攻击全貌的诊断（攻击入口、受损范围、是否被抵御），导致难以进行有效的审计和回滚。
*   **目标**：开发一种轻量级、模型无关的检测器，能够从工具调用日志中联合输出轨迹判定及步骤级细粒度标签。

### 2. 方法论：DriftNet
*   **核心思想**：将注入检测视为序列标注问题。通过分析工具调用轨迹的“漂移”特征（良性前缀 -> 投毒观测 -> 劫持动作），在单次前向传播中完成分类与定位。
*   **关键技术**：
    *   **输入表示**：将步骤序列化为文本，通过冻结的 `all-mpnet-base-v2` 编码器提取语义，并结合四个“无身份”世界特征（如是否涉及外部邮箱/URL），以增强对上下文合法性的判断。
    *   **双头架构**：
        *   **轨迹头**：对序列进行掩码平均池化，输出轨迹是否受损的概率。
        *   **步骤头**：对每个步骤进行四分类（良性、注入点、被劫持、注入失败）。
    *   **训练目标**：联合优化轨迹级二分类损失和步骤级四分类损失，并引入类加权以平衡稀有类别（如注入失败）。

### 3. 实验设计
*   **数据集**：使用 AgentDrift 基准（12,536 条轨迹，71,024 个标记步骤），涵盖邮件、银行、网页、代码、医疗五个领域。
*   **基准划分**：采用“任务不相交（Task-disjoint）”划分，确保训练集和测试集不共享任务模板，防止模型通过记忆模板而非理解攻击来获胜。
*   **对比方法**：与重新训练的“表面基线（Surface Baseline）”进行对比，该基线基于逻辑回归和关键词匹配，旨在量化序列建模带来的增益。

### 4. 资源与算力
*   **硬件**：使用单块 NVIDIA A100 GPU。
*   **效率**：由于编码器冻结且模型参数量极小（< 200 万），训练过程非常高效，每个 epoch 仅需几分钟。

### 5. 实验数量与充分性
*   **充分性**：实验设计严谨。进行了 20 种超参数配置的随机扫描，证明结果对超参数不敏感（F1 波动仅 0.011）。
*   **客观性**：测试集仅评估一次，且与基线模型在完全相同的划分上进行对比，确保了评估的公平性。

### 6. 主要结论与发现
*   **性能卓越**：DriftNet 在测试集上达到 0.983 的轨迹级 F1，注入点恢复准确率达 98.7%。
*   **模式识别**：在处理“部分劫持”和“延迟执行”等隐蔽攻击模式时，DriftNet 远超表面基线（召回率从 11%-17% 提升至 93%-98%），证明了序列建模的必要性。
*   **误报控制**：在抵御攻击和困难负样本上表现出极低的误报率，优于现有方法。

### 7. 优点
*   **细粒度诊断**：首次实现了联合轨迹分类与步骤级定位，直接生成可操作的 triage 报告。
*   **模型无关**：无需访问智能体内部权重或激活值，适用于闭源模型及事后审计。
*   **轻量化**：参数量极小，计算成本低，易于部署。
*   **严谨的评估协议**：通过任务不相交划分和鲁棒性扫描，有效避免了过拟合和虚高指标。

### 8. 不足与局限
*   **数据依赖**：基准测试为合成数据，可能存在生成器特定的偏差。
*   **世界身份规律性**：论文坦诚基准测试中存在世界身份相关的 artifact，尽管 DriftNet 的世界特征设计已规避了身份信息，但文本嵌入仍可能受到影响。
*   **应用限制**：对超长轨迹的适应性未充分验证；且未测试针对检测器本身的对抗性攻击。

（完）
