---
title: "RePolicy: Reinforcement Learning for Safety-Policy Invocation in Agent Safeguards"
title_zh: RePolicy：智能体防护中安全策略调用的强化学习
authors: "Houcheng Jiang, Boxuan Zhang, Qiyong Zhong, Junfeng Fang, Xiang Wang, Xiangnan He"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.24275v2"
tags: ["query:jb-def"]
score: 8.0
evidence: 利用强化学习实现智能体护栏中的安全策略调用
tldr: 针对大语言模型智能体在复杂策略下的安全防护难题，本文提出RePolicy框架。该框架利用强化学习学习如何从动态策略库中调用适用策略，并生成基于策略的推理和安全判断。通过构建PolicyTraj-20K数据集并结合GRPO算法及可验证奖励机制，RePolicy在六个基准测试中展现了卓越的安全检测性能和对多变策略环境的鲁棒性，有效提升了智能体在复杂任务中的安全性。
selection_source: fresh_fetch
motivation: 现有的智能体安全防护方法在处理未见轨迹和动态变化的策略上下文时，适应能力和鲁棒性不足。
method: 提出RePolicy框架，通过强化学习训练模型从动态库中检索并调用安全策略，生成合规性判定及理由。
result: 在六个智能体安全基准测试中，RePolicy实现了领先的安全检测性能，并能有效应对策略上下文的扰动。
conclusion: 强化学习能显著提升智能体安全防护中策略调用的准确性与鲁棒性，为动态环境下的安全治理提供了新方案。
---

## 摘要
保障语言模型智能体的安全需要根据上下文相关的安全策略评估完整的执行轨迹。现有的策略感知型防护机制主要依赖于提示词或有监督微调，这限制了它们适应未见轨迹和不断变化的策略上下文的能力。我们提出了 RePolicy，这是一种通过强化学习来学习安全策略调用的智能体防护机制。给定智能体轨迹和动态策略库，RePolicy 会调用适用的策略，并利用其内容生成基于策略的推理解释和安全判断。我们构建了 PolicyTraj-20K 以支持有监督初始化，随后采用带有可验证奖励和策略上下文扰动的 GRPO。在六个智能体安全基准测试上的实验表明，RePolicy 在多变的策略上下文下实现了强大的整体安全检测性能和稳健的策略调用。

## Abstract
Safeguarding language model agents requires assessing complete execution trajectories under context-dependent safety policies. Existing policy-aware safeguards mainly rely on prompting or supervised fine-tuning, limiting their ability to adapt to unseen trajectories and changing policy contexts. We propose RePolicy, an agent safeguard that learns safety-policy invocation through reinforcement learning. Given an agent trajectory and a dynamic policy library, RePolicy invokes the applicable policy and uses its content to produce a policy-grounded rationale and safety judgment. We construct PolicyTraj-20K to support supervised initialization, followed by GRPO with verifiable rewards and policy-context perturbation. Experiments across six agent safety benchmarks show that RePolicy achieves strong overall safety-detection performance and robust policy invocation under varying policy contexts.

---

## 论文详细总结（自动生成）

这是一份关于论文《RePolicy: Reinforcement Learning for Safety-Policy Invocation in Agent Safeguards》的深度结构化总结：

### 1. 核心问题与研究背景
随着大语言模型（LLM）从文本生成转向具备规划和工具调用能力的“智能体（Agent）”，其安全风险也从单纯的有害内容扩展到了复杂的**行为危害**（如越权操作、违规交易等）。
*   **研究动机**：现有的智能体防护工具（Safeguards）通常依赖预定义的风险分类或静态提示词，难以适应现实中不断变化的安全政策（如不同组织、法律管辖区对同一行为的差异化规定）。
*   **核心挑战**：如何在面对未见的执行轨迹和动态变化的政策库时，准确识别并应用最相关的安全条款进行判定。

### 2. 方法论：RePolicy 框架
RePolicy 将安全政策的调用视为一种**智能体强化学习任务**，让模型学习从动态政策库中主动检索并应用规则。

*   **核心流程**：
    1.  **策略调用（Invocation）**：给定轨迹和政策库，模型首先输出应调用的政策 ID。
    2.  **基于策略的推理（Grounded Reasoning）**：获取政策具体内容后，生成推理过程。
    3.  **安全预测（Safety Prediction）**：输出最终的安全标签（Safe/Unsafe）。
*   **关键技术细节**：
    *   **PolicyTraj-20K 数据集**：构建了包含 2 万多条标注了政策依据的智能体轨迹，用于冷启动训练。
    *   **GRPO 优化**：采用群体相对策略优化（Group Relative Policy Optimization），通过采样多个 Rollout 并对比奖励来优化模型。
    *   **可验证奖励设计**：包含格式奖励（$R_{fmt}$）、策略调用准确奖励（$R_{pol}$）和安全预测准确奖励（$R_{acc}$）。
    *   **政策上下文扰动**：在训练中随机加入无关政策和“诱饵”政策，强迫模型理解政策语义而非死记硬背。

### 3. 实验设计
*   **数据集/基准测试**：在 6 个主流智能体安全基准上进行评估，共计 7,369 条轨迹：
    *   ATBench, R-Judge, OpenAgentSafety, ASSEBench, HINTBench, AgentHazard。
*   **对比方法**：共对比了 19 个外部基准模型，分为三类：
    *   **闭源通用模型**：Claude Sonnet 4.6, GPT-5.4, Qwen3.6-Plus。
    *   **开源通用模型**：Qwen3-235B, GLM-5.1, DeepSeek V4 Pro 等。
    *   **专用防护模型**：Llama Guard 4, WildGuard, AgentDoG 1.5, ShieldGemma 等。

### 4. 资源与算力
*   **硬件环境**：论文明确指出使用了 **8 张 NVIDIA H20 (96 GB)** GPU。
*   **训练细节**：
    *   冷启动 SFT 阶段：5,000 个样本，3 个 Epoch。
    *   GRPO 阶段：14,425 个样本，学习率 $1 \times 10^{-6}$，每个 Prompt 采样 16 个 Rollout。
    *   最大序列长度支持到 16,384。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了从 4B 到 1.6T 参数规模的各类模型，测试样本量超 7,000 条，具有很强的代表性。
*   **消融实验**：非常充分。作者分别验证了冷启动 SFT、显式策略调用、政策上下文扰动以及不同奖励权重对结果的影响。
*   **鲁棒性测试**：测试了政策库规模（从 10 到 50 个政策）变化时模型的表现，验证了其在复杂检索环境下的稳定性。
*   **客观性**：使用了统一的二分类（Safe/Unsafe）评估标准，并对所有基准模型采用了推荐的接口或最优提示词，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能卓越**：仅 4B 规模的 RePolicy 在综合得分（88.15 Unsafe F1）上超过了所有对比模型，包括参数量巨大的 GPT-5.4 和 DeepSeek V4 Pro。
*   **RL 的价值**：强化学习显著提升了策略调用的准确率（从 94.4% 提升至 98.5%），并减少了误报。
*   **泛化能力**：RePolicy 在未见过的政策内容和变化的政策标识符下表现稳健，证明其真正理解了政策的“适用范围”。

### 7. 优点与亮点
*   **范式创新**：将安全防护从“被动匹配”转变为“主动调用”，引入了 Agentic RL 的思想。
*   **可解释性强**：模型不仅给出结果，还明确指出了违反了哪一条政策及其理由，便于人工审计。
*   **高效性**：证明了通过精细的 RL 训练，小参数模型（4B）可以在特定任务上超越千亿级模型。

### 8. 不足与局限
*   **假设前提**：RePolicy 假设政策库中一定存在适用的政策，对于完全缺失相关规则的极端情况处理能力未深入探讨。
*   **推理开销**：由于采用了“调用-检索-推理”的两步走策略，其推理延迟（Latency）和 Token 消耗高于单次判定的模型。
*   **单政策限制**：目前的实现主要针对每个轨迹关联一个主导政策，而现实中可能需要同时遵守多个层级（如法律+公司规定）的政策。
*   **语言局限**：实验主要集中在英文/代码环境，多语言环境下的策略理解能力尚待验证。

（完）
