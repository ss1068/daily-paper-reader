---
title: "SafeAtlas-VL: Beyond Binary Multimodal Safety with Large-Scale Data and Guard Models"
title_zh: SafeAtlas-VL：利用大规模数据和防护模型超越二元多模态安全
authors: "Zongrui Wang, Xiangyang Zhu, Sicheng Wang, Han Wang, Dingyi Rong, Zeyu Zhang, Chunyi Li, Yue Shi, Kaiwei Zhang, Zicheng Zhang, Yuan Tian, Qi Jia, Yan Teng, Wei Sun, Ning Liu, Guangtao Zhai"
date: 2026-08-29
pdf: "https://arxiv.org/pdf/2608.29098v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 多模态安全护栏模型和大规模数据集
tldr: 针对多模态安全审核中二元分类和单一目标的局限性，本文提出了SafeAtlas-VL数据集，包含150万条涵盖15类伤害的五级量化标注数据。通过目标条件微调和软累积序数头，训练了SafeAtlas Guard系列模型，实现了对图像、请求和响应的多维度、细粒度风险评估，在多个基准测试中显著超越了现有SOTA模型。
selection_source: fresh_fetch
motivation: 现有的多模态安全防护工具多为二元判断且目标单一，难以处理复杂的交互风险和模糊案例。
method: 构建了包含150万实例的五级量化数据集SafeAtlas-VL，并训练了采用目标条件微调和序数回归头的SafeAtlas Guard模型。
result: "8B规模的Guard模型在SafeAtlas-Bench及其他基准测试上表现优异，F1分数比此前最先进模型提升了约4%。"
conclusion: SafeAtlas-VL通过大规模细粒度数据和多维度评估模型，显著提升了多模态大模型的安全检测能力和泛化性能。
---

## 摘要
多模态安全审核需要区分来自视觉内容、用户意图和助手行为的风险。然而，现有的安全防护措施通常针对单一判断目标进行训练，并将安全评估简化为二元决策。因此，在多模态交互中难以比较风险，且模糊案例被掩盖。我们推出了 SafeAtlas-VL，这是一个包含 150 万个训练实例的数据集，它将图像、请求和响应层级的判断置于五级有序量表上。我们从真实世界和合成来源中策划了广泛的安全相关数据，并应用了感知分歧的标注程序。生成的数据集涵盖 15 个伤害类别和 55 个细粒度子类别，覆盖了广泛的多模态安全场景。我们还构建了 SafeAtlas-Bench，这是一个包含 5,000 个实例的留出集，用于评估五级预测和连续风险评分。基于该数据集，我们通过目标条件微调（target-conditioned tuning）训练了 SafeAtlas Guard 系列模型，用于多模态安全检测。我们的模型不仅能进行安全等级的五分类，还能通过软累积序数头（soft cumulative ordinal head）将安全性映射为连续分数。实验结果表明，在我们的数据集上训练的防护模型表现出强大的泛化能力：即使不使用其他基准测试的训练集，它们在相应的测试集上也取得了具有竞争力的性能。值得注意的是，我们的 8B 模型获得了整体最佳性能，在 F1 分数上比之前的 SOTA 提高了约 4%。代码、数据和模型已发布以支持进一步研究。警告：本文包含可能具有冒犯性、有害、露骨或令人不安的示例数据。

## Abstract
Multimodal safety moderation requires distinguishing risks arising from visual content, user intent, and assistant behavior. Existing safeguards, however, are typically trained for a single judgment target and reduce safety assessment to a binary decision. Consequently, risk becomes difficult to compare across a multimodal interaction, and ambiguous cases are obscured. We introduce SafeAtlas-VL, a dataset of 1.5M training instances that places image-, request-, and response-level judgments on a five-level ordered scale. We curate a broad collection of safety-relevant data from both real-world and synthetic sources and apply a disagreement-aware annotation procedure. The resulting dataset spans 15 harm categories and 55 fine-grained subcategories, covering a broad range of multimodal safety scenarios. We also construct SafeAtlas-Bench, a held-out set of 5,000 instances for evaluating five-level predictions and continuous risk scores. Upon this dataset, we train the SafeAtlas Guard series of models via target-conditioned tuning for multimodal safety detection. Our models not only perform five-way classification of safety levels but also map safety to continuous scores through a soft cumulative ordinal head. Experimental results demonstrate that guard models trained on our dataset exhibit strong generalization: even without using the training sets of other benchmarks, they achieve competitive performance on the corresponding test sets. Notably, our 8B model attains the overall best performance, outperforming the previous SOTA by approximately 4% in F1 score. Code, data, and models are released to support further research. Warning: this paper contains example data that may be offensive, harmful, graphic, or disturbing.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SafeAtlas-VL**，这是一个旨在提升多模态大模型（MLLM）安全审核能力的大规模数据集和防护模型系列。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态安全防护工具（如 LLaVA-Guard）通常将安全评估简化为“二元分类”（安全 vs. 不安全），且往往只关注单一目标（如仅针对图像或仅针对响应）。这种做法难以处理复杂的交互风险、模糊的边界案例，也无法量化风险的严重程度。
*   **研究背景**：随着多模态模型在现实场景中的广泛应用，用户意图、视觉内容和助手行为之间的复杂交互带来了多维度的安全挑战。研究者认为需要一种更细粒度、多目标且具备量化能力的审核机制。

### 2. 论文提出的方法论
*   **SafeAtlas-VL 数据集构建**：
    *   **规模与覆盖**：包含 150 万条训练实例，涵盖 15 个伤害类别和 55 个细粒度子类别。
    *   **五级量化标注**：弃用二元分类，采用 1（完全安全）到 5（极端有害）的五级李克特量表（Likert scale），对图像、用户请求和助手响应分别进行标注。
    *   **感知分歧的标注**：通过多轮人工与模型协作，识别并处理标注中的主观分歧，确保数据质量。
*   **SafeAtlas Guard 模型训练**：
    *   **目标条件微调（Target-conditioned Tuning）**：通过在输入中加入特定指令（如 `[IMAGE_SAFETY]`），使模型能够根据同一输入上下文，分别评估图像、请求或响应的安全性。
    *   **软累积序数头（Soft Cumulative Ordinal Head）**：在模型输出层引入序数回归机制。不同于普通的分类头，该设计利用累积概率分布，不仅能输出离散等级，还能计算连续的风险分数，从而更好地捕捉风险的递进关系。

### 3. 实验设计
*   **数据集/场景**：
    *   **SafeAtlas-Bench**：一个包含 5,000 个实例的留出测试集，用于评估五级预测准确性和连续风险评分。
    *   **跨领域评估**：在 LLaVA-Guard、WildGuard、MM-SafetyBench 等现有基准上进行泛化性测试。
*   **对比方法（Baselines）**：
    *   闭源模型：GPT-4o。
    *   开源安全模型：LLaVA-Guard (7B/13B)、Llama-Guard-3-Vision (11B)、WildGuard 等。
*   **评估指标**：F1 分数（针对二元转换后的结果）、平均绝对误差（MAE，针对等级预测）、以及连续分数的排序能力。

### 4. 资源与算力
*   **模型规模**：主要训练了基于不同基座的 SafeAtlas Guard 模型，重点展示了 **8B** 规模的模型性能。
*   **算力说明**：论文摘要和核心部分未详细列出具体的 GPU 型号、数量或总训练时长（通常此类 1.5M 规模的数据集在 8B 模型上的微调需要数十至数百个 GPU 小时，具体取决于硬件环境）。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常充分。研究者不仅在自建的 5,000 条细粒度 Benchmark 上进行了测试，还进行了大规模的跨数据集验证（Zero-shot 性能测试）。
*   **消融实验**：验证了“目标条件微调”和“序数头”对模型性能的贡献。
*   **客观性**：通过与 GPT-4o 等顶级模型对比，并展示在未见过的第三方数据集上的表现，证明了模型的强泛化能力和评估的客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：SafeAtlas Guard 8B 模型在综合 F1 分数上比之前的 SOTA（最先进）模型提升了约 **4%**。
*   **细粒度优势**：五级量化标注显著提升了模型处理模糊案例的能力，连续风险评分比二元判断更能反映真实的风险分布。
*   **多目标有效性**：模型能够成功区分“有害图像+无害请求”与“无害图像+有害请求”等复杂组合，实现了对交互过程的精准审核。
*   **泛化能力**：即使不使用其他基准测试的训练数据，SafeAtlas Guard 依然能达到或超过专门针对这些基准训练的模型。

### 7. 优点
*   **数据质量与规模**：150 万条数据和五级标注填补了多模态安全领域大规模细粒度数据的空白。
*   **架构创新**：引入序数回归思想（Soft Cumulative Ordinal Head），解决了安全等级之间存在的内在顺序逻辑问题。
*   **实用性强**：模型同时支持图像、文本请求和响应的审核，是一个全方位的安全护栏工具。

### 8. 不足与局限
*   **合成数据偏差**：虽然引入了人工校验，但大规模数据中仍包含大量合成数据，可能存在模型生成的固有偏见。
*   **计算开销**：作为一个 8B 规模的防护模型，在实时推理场景中（作为 LLM 的前置/后置过滤器）可能会带来额外的延迟和计算成本。
*   **静态标准**：安全定义随社会文化演变，固定的 15 个类别和 5 级标准可能无法完全覆盖未来出现的新型攻击（如新型越狱手段）。

（完）
