---
title: "HazardAuditor: From Executable Threats to Safer Computer-Use Agents"
title_zh: HazardAuditor：从可执行威胁到更安全的计算机使用智能体
authors: "Yunhao Feng, Ruixiao Lin, Ming Wen, Yanming Guo, Xingjun Ma, Yutao Wu, Xinhao Deng, Shouling Ji"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15134v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 智能体执行安全的防御模型
tldr: HazardAuditor是一个针对计算机使用智能体（Computer-use agents）的执行导向安全框架。针对现有安全模型难以处理动态运行时行为及跨框架监督缺失的问题，该研究提出了统一的事件表示法和GuardPO优化策略。通过在受控环境中标准化智能体交互并优化序列级安全决策，HazardAuditor显著提升了智能体在复杂任务中的安全性，在多个基准测试中表现优于现有安全防护模型。
selection_source: fresh_fetch
motivation: 现有安全模型主要针对静态内容，无法有效应对智能体在运行时产生的动态执行风险及跨框架监督难题。
method: 构建了统一的事件表示框架以标准化不同智能体的交互，并引入GuardPO优化策略，将安全决策作为序列级优化的核心单元。
result: 在多个计算机使用基准测试中，HazardAuditor的安全性准确率较现有最优防护模型提升了最高16.5个百分点。
conclusion: 该框架通过执行导向的监督机制，为构建更安全、可控的计算机使用智能体提供了有效的技术路径。
---

## 摘要
计算机使用智能体越来越多地与浏览器、终端、文件系统及外部服务进行交互，这带来了不仅源于生成内容、更源于运行时行为的安全风险。现有的防护模型主要针对静态提示词和响应，难以适应智能体的执行过程；而现有的可执行安全平台仅能提供评估结论，无法为防护模型在异构智能体框架间学习提供所需的标准化监督。我们引入了 HazardAuditor，这是一个弥补上述两项空白的执行导向型框架。其基础设施在受控环境中运行异构智能体（Claude Code、Codex、Hermes 和 OpenClaw），并将它们的交互标准化为规范的事件表示，以实现跨框架的监督。我们进一步观察到，基于 Token 级别的后训练目标会导致生成式防护模型出现结构性失配，使得较长的推理过程主导梯度更新。为此，我们提出了防护策略优化（GuardPO），通过将确定性的安全结果转化为序列级优势，并对推理和判定区域进行归一化处理，使安全决策成为优化的有效单元。在多个基准测试和异构计算机使用系统中，HazardAuditor 的准确率较现有最强防护模型提升了高达 16.5 个百分点。代码、模型及评估工件将在 https://yunhao-feng.github.io/HazardAuditor/ 提供。

## Abstract
Computer-use agents increasingly interact with browsers, terminals, file systems, and external services, introducing safety risks that emerge through runtime behavior rather than generated content alone. Existing guard models target static prompts and responses and are poorly suited to agent execution; existing executable safety platforms produce evaluation verdicts rather than the normalized supervision a guard model needs to learn across heterogeneous agent frameworks. We introduce HazardAuditor, an execution-grounded framework that closes both gaps. Its infrastructure runs heterogeneous agents (Claude Code, Codex, Hermes, and OpenClaw) in controlled environments and normalizes their interactions into a canonical event representation for cross-framework supervision. We further observe that token-level post-training objectives create a structural mismatch for generative guards, causing longer rationales to dominate gradient updates. Guard Policy Optimization (GuardPO) addresses this by converting deterministic safety outcomes into sequence-level advantages and normalizing rationale and verdict regions, making the safety decision the effective unit of optimization. Across multiple benchmarks and heterogeneous computer-use systems, HazardAuditor improves accuracy by up to 16.5 percentage points over the strongest prior guard. Code, models, and evaluation artifacts will be available at https://yunhao-feng.github.io/HazardAuditor/.

---

## 论文详细总结（自动生成）

以下是对论文《HazardAuditor: From Executable Threats to Safer Computer-Use Agents》的结构化总结：

### 1. 核心问题与整体含义
随着大模型作为“计算机使用智能体”广泛部署，安全风险已从单纯的“生成内容有害”转向“运行时行为有害”。现有的安全防护模型多针对静态文本，无法理解智能体在终端、文件系统等环境中的动态操作；而现有的安全评估平台仅能给出判定结果，缺乏可用于训练防护模型的标准化监督数据。HazardAuditor 旨在解决这一“执行导向监督缺失”与“生成式防护模型训练目标失配”的双重难题。

### 2. 方法论
*   **核心思想**：将智能体的运行时交互转化为“执行导向”的监督信号，并引入序列级优化目标，使模型关注最终的安全决策而非推理过程的长度。
*   **关键技术**：
    *   **规范化事件表示**：构建统一的事件模式（Canonical Event Representation），通过适配器将 Claude Code、Codex 等异构智能体的原生日志转化为包含用户输入、智能体推理、工具调用及环境观测的标准化序列。
    *   **GuardPO (防护策略优化)**：针对传统 SFT（监督微调）中“长推理主导梯度更新”的问题，GuardPO 将确定性安全结果转化为序列级优势（Sequence-level Advantage），并对推理区域和判定区域进行归一化处理，确保安全决策成为优化的核心单元。

### 3. 实验设计
*   **数据集/Benchmark**：
    *   **CUA-EXEC**：作者构建的跨框架执行诊断数据集，包含 4 种智能体框架的 800 条轨迹。
    *   **外部基准**：AgentHazard、ATBench、R-Judge、ASSE-Safety 等。
*   **对比方法**：
    *   通用大模型（GPT-5.2, Claude-Sonnet 等）。
    *   通用防护模型（LlamaGuard3, Qwen3Guard 等）。
    *   智能体专用防护模型（AgentDoG, BraveGuard 等）。

### 4. 资源与算力
*   **硬件配置**：训练过程在单节点 8 张 NVIDIA A100 GPU 上完成。
*   **训练细节**：SFT 阶段运行 10 个 Epoch；GuardPO 阶段运行 1 个虚拟 Epoch。使用了 LLaMA-Factory 和 VERL 等框架进行实现。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 4 种异构智能体框架、5 个外部基准测试以及消融实验（如 SFT 与 GuardPO 的对比）。
*   **客观性与公平性**：实验设计较为严谨，通过分层抽样确保了 CUA-EXEC 的类别平衡，并明确了训练集与测试集的隔离，有效防止了数据泄露。实验对比了多种基线模型，且在不同后端（如 GPT-5.5, Claude Sonnet 等）上进行了迁移性验证，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：HazardAuditor 在 CUA-EXEC 上较现有最强防护模型准确率提升最高达 16.5 个百分点。
*   **决策边界优化**：GuardPO 显著改善了模型在安全与不安全分类间的决策边界，而非单纯通过激进的拦截来提升指标。
*   **鲁棒性**：该方法在未见过的外部基准测试中表现出良好的跨框架迁移能力，证明了执行导向监督的有效性。

### 7. 优点
*   **执行导向**：首次将智能体的“行为证据”而非“内容文本”作为安全监督的核心，更贴合实际应用场景。
*   **数学严谨性**：通过 GuardPO 实现了“长度不变性”（Length Invariance），解决了生成式模型在长推理下的优化偏差问题。
*   **通用性**：通过统一的事件表示法，成功打破了不同智能体框架间的格式壁垒。

### 8. 不足与局限
*   **推理延迟**：生成式防护模型在审计时需要 autoregressive 推理，相比轻量级分类器延迟较高（尽管作者提供了 MLP 快速头作为替代方案）。
*   **特定领域覆盖**：在某些细分风险领域（如资源滥用）的召回率仍有提升空间。
*   **计算开销**：虽然 GuardPO 优化了训练目标，但其训练过程仍需进行 rollout 采样，对算力有一定要求。

（完）
