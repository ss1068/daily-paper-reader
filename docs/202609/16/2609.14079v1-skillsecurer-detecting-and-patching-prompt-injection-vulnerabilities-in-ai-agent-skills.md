---
title: "SkillSecurer: Detecting and Patching Prompt-Injection Vulnerabilities in AI Agent Skills"
title_zh: SkillSecurer：检测并修复 AI 智能体技能中的提示注入漏洞
authors: "Donato Mecca, Alberto Verna, Youness Bouchari, Nikhil Jha, Marco Mellia"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.14079v1"
tags: ["query:llm"]
score: 9.0
evidence: 检测并修复AI智能体技能中的提示注入漏洞
tldr: "本文提出了SkillSecurer，一个用于检测和修复AI智能体技能中提示注入漏洞的自动化框架。该框架通过红队智能体生成多种威胁类型的注入攻击，并利用蓝队智能体进行漏洞定位与修复。研究表明，SkillSecurer能有效识别并修复技能中的安全隐患，在评估中实现了100%的检测率，并揭示了现有开源技能库中存在显著的安全风险。"
selection_source: fresh_fetch
motivation: AI智能体技能的广泛应用带来了新的提示注入攻击风险，亟需一种自动化的安全防御机制。
method: 构建了一个包含红队生成、蓝队分析与修复、以及验证器评估的闭环智能体框架，实现对技能漏洞的全生命周期管理。
result: "SkillSecurer在测试中达到100%的注入检测率，并发现skills.sh中超过17%的技能存在潜在漏洞。"
conclusion: 基于上下文感知的LLM分析能够提供可靠的漏洞定位与可执行的修复方案，显著提升AI智能体技能的安全性。
---

## 摘要
智能体技能通过可重用的指令、脚本和配置扩展了 AI 智能体的能力，但也面临着影响智能体决策和行为的新型攻击风险。为了应对这些风险，我们提出了 SkillSecurer，这是一个用于生成、检测、定位和修复智能体技能安全风险的全智能体框架。其红队智能体（red agent）可在九种威胁类型中生成上下文兼容的注入攻击，并记录精确的修改过程；其蓝队智能体（blue agent）则负责分析完整的技能包，提供确凿证据并提出修复方案。在受控实例中，验证器通过将检测结果和补丁与记录的注入进行比对，实现了注入级别的评估。我们通过选择最佳后端大语言模型、与竞品对比以及对每个评估阶段进行人工交叉验证，对 SkillSecurer 进行了全面评估。结果显示，在最佳后端支持下，SkillSecurer 是唯一实现 100% 注入检测率的扫描器。此外，我们对 skills.sh 上的热门技能进行了分析，发现超过 17% 的受检技能存在潜在漏洞。通过对其中部分技能的测试，我们触发了实际的安全事件，展示了运行未经核实技能的风险。研究结果表明，上下文感知的大语言模型分析不仅能实现技能级别的标记，还能提供可靠的注入定位和可操作的修复方案。

## Abstract
Agent skills extend AI agents with reusable instructions, scripts, and configuration, but are also open to new attacks to influence an agent's decisions and actions. To address these risks, we present SkillSecurer, a fully agentic framework for generating, detecting, localising, and remediating security risks in agent skills. Its red agent generates context-compatible injections across nine threat types while recording the exact modification; its blue agent analyses complete skill packages, produces grounded evidence, and proposes patches. For controlled instances, a verifier compares findings and patches with the recorded injection, enabling injection-level evaluation.   We thoroughly evaluate SkillSecurer by selecting the best backend LLM, comparing it with competitors, and manually cross-validating each evaluation stage. With its best performing backend, SkillSecurer is the only scanner to achieve a 100% injection detection rate. Next, we analyse popular skills from skills.sh, finding latent vulnerabilities in more than 17% of the skills examined. Testing some of those skills, we trigger actual incidents, showing the risks of running unverified skills. Our results show that context-aware LLM analysis can provide reliable injection localisation and actionable remediation beyond skill-level flagging alone.

---

## 论文详细总结（自动生成）

以下是对论文《SkillSecurer: Detecting and Patching Prompt-Injection Vulnerabilities in AI Agent Skills》的结构化总结：

### 1. 核心问题与研究动机
随着 AI 智能体（如 Codex、Claude）直接执行任务的能力增强，第三方“技能”（Skills）成为扩展其功能的重要方式。然而，这些技能包（包含指令、脚本、配置等）极易受到提示注入（Prompt Injection）攻击，导致智能体执行未经授权的操作（如数据泄露、恶意代码执行）。现有安全扫描工具多为静态分析或简单的规则匹配，缺乏对上下文的深度理解，且往往只能给出“是否危险”的二元判断，无法提供精确的漏洞定位或自动修复方案。

### 2. 方法论：SkillSecurer 框架
SkillSecurer 是一个全智能体（Agentic）框架，包含三个核心组件：
*   **Red Injector（红队注入器）**：针对九种威胁类型，自动生成与原始技能上下文兼容的注入攻击，并记录精确的修改作为“地面真值”（Ground Truth）。
*   **Blue Patcher（蓝队修复器）**：负责分析技能包，通过大语言模型（LLM）推理识别安全风险，提供带有证据引用的结构化报告，并自动生成修复补丁。
*   **Verifier（验证器）**：仅用于受控评估，通过对比检测结果与红队记录的注入修改，计算“注入检测率”（IDR），评估定位和修复的有效性。
*   **核心逻辑**：不依赖单一的静态规则，而是利用 LLM 的上下文感知能力，对技能的完整工作流进行语义分析，从而实现从检测到修复的闭环。

### 3. 实验设计与 Benchmark
*   **数据集**：
    *   **受控数据集**：使用 `Skill-Inject` [20] 作为外部基准，并利用 `Red Injector` 在 20 个安全技能基础上生成 180 个受控注入样本（`Dred`）。
    *   **真实场景**：从 `skills.sh` 抓取 954 个热门技能进行“野外”测试。
*   **对比方法**：与 Cisco Skill Scanner、NVIDIA SkillSpector、Tencent AI-Infra-Guard 等主流开源/商业扫描器进行对比。
*   **评估指标**：引入了比传统“技能级标记”更严格的“注入检测率”（IDR），即衡量扫描器是否能定位到具体的恶意修改。

### 4. 资源与算力
*   **算力消耗**：论文未明确提及具体的 GPU 型号或训练时长，因为该框架主要基于 API 调用（如 Claude Sonnet 5, DeepSeek V4 Pro 等）。
*   **成本记录**：论文详细记录了扫描成本（以美元计），例如使用 Claude Sonnet 5 扫描 `Dsi` 基准测试的成本约为 15.62 美元，DeepSeek V4 Pro 则更具性价比（约 2.50 美元）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 345 个受控样本和 954 个真实世界样本。
*   **客观性**：采用了三位专家的人工交叉验证协议（Majority Vote）来评估 LLM 报告的准确性，并对 64 个案例进行了手动审计，以确保评估的公平性。
*   **充分性**：进行了后端 LLM 的消融实验（对比不同模型性能），并对比了不同扫描器在多种威胁类别下的表现，实验设计严谨且具有说服力。

### 6. 主要结论与发现
*   **性能表现**：SkillSecurer 在受控基准测试中实现了 100% 的注入检测率（IDR），显著优于竞品。
*   **真实风险**：在 `skills.sh` 的热门技能中，约 17.6% 被标记为存在潜在漏洞，且经人工确认多为真实的“潜在漏洞”（Latent Vulnerabilities）。
*   **修复难度**：研究发现“识别风险”比“修复风险”容易，部分补丁可能因未覆盖执行路径或被模型忽略而失效。

### 7. 优点
*   **全生命周期管理**：首次实现了从生成、检测到修复的端到端自动化。
*   **高精度定位**：通过引用证据（Quoted Evidence）实现注入级定位，而非模糊的技能级警告。
*   **上下文感知**：利用 LLM 的推理能力，能够识别复杂的、依赖于工作流的提示注入，克服了静态分析的局限。

### 8. 不足与局限
*   **修复可靠性**：自动生成的补丁在复杂技能中可能导致功能破坏，且部分补丁无法完全阻止攻击（如模型忽略警告）。
*   **依赖性**：检测性能高度依赖于后端 LLM 的能力（如 Claude Sonnet 5 表现最佳，而较弱模型性能下降明显）。
*   **静态分析局限**：尽管引入了 LLM，但验证过程仍是静态的，无法完全模拟真实运行环境下的所有动态交互风险。

（完）
