---
title: "Architecting the Secure AI-SOC: A Neurosymbolic Framework for Pipeline Integrity and Threat Mitigation"
title_zh: 构建安全 AI-SOC：一种用于流水线完整性与威胁缓解的神经符号框架
authors: "Anna Gazani, Spyridon Kounoupidis, Panagiotis Katsaros, Nikolaos Kekatos, Grigorios Tsoumakas, Georgios Koutidis"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10707v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于威胁缓解和流水线完整性的神经符号框架
tldr: 本文针对AI驱动的安全运营中心（AI-SOC）中大模型面临的日志投毒及间接提示词注入攻击问题，提出了一种神经符号防御架构。该架构结合了确定性的SIEM解码器预过滤与NeMo Guardrails语义边界约束，通过闭环遥测系统实现人机协同，有效阻断了提示词攻击链，在保证系统低延迟的同时提升了AI-SOC的防御鲁棒性与可观测性。
selection_source: fresh_fetch
motivation: 大模型在SOC中的应用引入了日志投毒等新型攻击向量，现有防御手段难以平衡计算效率与语义安全性。
method: 构建了包含SIEM结构化预过滤、NeMo Guardrails语义验证及闭环人机协同遥测的神经符号防御体系。
result: 实验表明该框架能有效防御MITRE ATLAS框架下的多种提示词注入攻击，显著提升了防御的确定性与安全性。
conclusion: 该神经符号架构通过将大模型的随机性置于可验证的约束之下，为下一代AI-SOC提供了高效且可信的防御方案。
---

## 摘要
将大语言模型（LLMs）集成到安全运营中心（SOCs）中虽然简化了威胁情报工作，但也引入了关键漏洞，特别是通过日志投毒进行的间接提示词注入。攻击者利用这一向量，通过在系统日志中嵌入恶意载荷来劫持大语言模型的运行逻辑，从而执行多步骤的“提示词软件”（promptware）杀伤链。保护这一流水线面临着二元困境：确定性防御在计算上高效但缺乏语义理解，而纯神经评估则会引入过高的延迟和概率性缺陷。为解决这一问题，我们提出了一种新颖的神经符号纵深防御架构，以确保端到端的流水线完整性。首层采用定制的 SIEM 解码器作为确定性预过滤器，在摄入边缘执行即时的结构化清洗，以中和容量填充和基于特征的注入。次层利用 NeMo Guardrails 在大语言模型处理前，通过对结构化 SIEM 警报进行自检验证，强制执行严格的语义边界。此外，该框架集成了一个闭环遥测系统，直接在 SOC 仪表板中为受阻攻击提供关键的“人在回路”（HITL）可见性。我们根据 MITRE ATLAS 分类法进行了全面的实验评估，针对多种提示词注入攻击对该框架进行了测试。结果表明，这种协同方法有效地瓦解了提示词软件杀伤链——通过可验证的约束限制了大语言模型的随机性，并为下一代 AI-SOC 提供了一种弹性强、可观测性高的防御机制。

## Abstract
The integration of Large Language Models (LLMs) into Security Operations Centers (SOCs) streamlines threat intelligence but introduces critical vulnerabilities, notably indirect prompt injection via log poisoning. Adversaries exploit this vector to execute multistep ``promptware'' kill chains by embedding malicious payloads within system logs to hijack the LLM's operational logic. Securing this pipeline presents a dichotomy: deterministic defenses are computationally efficient yet semantically blind, while purely neural evaluations introduce prohibitive latency and probabilistic flaws. To address this, we propose a novel neurosymbolic defense-in-depth architecture that ensures end-to-end pipeline integrity. The primary layer employs customized SIEM decoders as a deterministic pre-filter, performing immediate structural sanitization to neutralize volumetric padding and signature-based injections at the ingestion edge. The secondary layer leverages NeMo Guardrails to enforce strict semantic boundaries through self-checking validation on the structured SIEM alerts prior to LLM processing. Furthermore, the framework integrates a closed-loop telemetry system, providing critical Human-in-the-Loop (HITL) visibility into thwarted attacks directly within the SOC dashboard. We present a comprehensive experimental evaluation mapped to the MITRE ATLAS taxonomy, assessing the framework against diverse prompt injections. Our results demonstrate that this synergistic approach effectively dismantles the promptware kill chain - bounding LLM stochasticity with verifiable constraints, and delivering a resilient, highly observable defense mechanism for next-generation AI-SOCs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Architecting the Secure AI-SOC: A Neurosymbolic Framework for Pipeline Integrity and Threat Mitigation》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：AI-SOC（AI 原生安全运营中心）通过集成 LLM 自动化处理威胁情报，显著降低了分析师负担。
*   **核心问题**：LLM 引入了新的攻击面——**间接提示词注入（Indirect Prompt Injection）**。攻击者通过“日志投毒”将恶意指令嵌入系统日志，当这些日志被 LLM 处理时，会触发“提示词软件（Promptware）”杀伤链，导致权限提升、数据泄露或远程代码执行。
*   **研究动机**：现有的防御手段存在二元困境：确定性防御（如正则过滤）语义盲区大；纯神经防御（如 LLM 行为监控）延迟高且存在概率性幻觉。论文旨在构建一种兼顾效率与语义安全的防御架构。

### 2. 方法论：神经符号防御架构
*   **核心思想**：采用“纵深防御”策略，将防御任务分配给符号层（确定性）和神经层（语义性）。
*   **关键技术细节**：
    *   **第一层：确定性预过滤器（符号层）**：基于 Wazuh SIEM 工具，利用自定义解码器和原子规则，在摄入阶段进行结构化清洗。它能中和容量填充攻击（防止上下文耗尽）和已知特征的注入攻击，具有近零延迟。
    *   **第二层：语义护栏（神经层）**：利用 NVIDIA NeMo Guardrails，在 LLM 接口处设置“输入轨”和“输出轨”。输入轨进行自检以拦截指令覆盖和角色扮演攻击；输出轨验证 LLM 的响应是否符合预定义的 JSON 格式及安全动作集。
    *   **闭环遥测（HITL）**：将拦截到的攻击事件结构化并反馈至 SOC 仪表板，确保分析师对威胁有完整可见性。

### 3. 实验设计
*   **场景与数据集**：模拟了一个 AI-SOC 环境，使用了包含四种 MITRE ATT&CK 战术（防御规避、持久化、权限提升、影响）的合成安全警报数据集。
*   **Benchmark**：基于 MITRE ATLAS（AI 系统对抗威胁景观）分类法进行映射评估。
*   **对比方法**：论文对比了单一符号防御（Wazuh 规则）与神经符号协同防御的效果，分析了各层在不同攻击向量下的表现。

### 4. 资源与算力
*   **算力说明**：论文未明确提供 GPU 的具体型号、数量或训练时长。仅提到实验是在本地托管的 **Gemma 3 (12B)** 模型上运行，通过 Ollama 提供服务。

### 5. 实验数量与充分性
*   **实验规模**：针对 20 个嵌入了不同提示词注入载荷的样本进行了测试。
*   **充分性评价**：实验覆盖了多种攻击向量（指令覆盖、角色扮演、工具滥用、数据外泄），具有一定的代表性。但样本量（N=20）较小，且主要基于模拟环境，在真实大规模生产环境下的鲁棒性仍需进一步验证。

### 6. 主要结论与发现
*   **协同效应**：符号层成功拦截了结构化异常，神经层有效识别了复杂的语义 jailbreak。
*   **防御效果**：该架构能有效瓦解提示词软件杀伤链，将 LLM 的随机性限制在可验证的边界内。
*   **互补性**：符号层处理高频、低语义攻击，神经层处理低频、高语义攻击，两者结合显著降低了误报率和计算开销。

### 7. 优点
*   **架构创新**：巧妙地将传统 SIEM 的确定性优势与 LLM 的语义理解能力结合，解决了“计算效率”与“防御深度”的矛盾。
*   **可观测性**：闭环遥测设计解决了 AI 防御系统常见的“黑盒”问题，符合 SOC 实际运维需求。
*   **标准化**：严格映射 MITRE ATLAS 和 ATT&CK 框架，使防御逻辑具有行业参考价值。

### 8. 不足与局限
*   **样本局限**：实验样本量较小，且未涵盖多轮对话攻击（上下文漂移）和多模态攻击（如图像/音频中的隐写指令）。
*   **概率性风险**：神经层仍存在极少数的漏报（如文中提到的 2 例未拦截成功），表明对于零日语义攻击仍有改进空间。
*   **应用限制**：依赖于特定的 SIEM 工具（Wazuh）和框架（NeMo），在异构 SOC 环境中的迁移成本未详细讨论。

（完）
