---
title: Trust propagation and structural containment in Multi-agent LLM pipelines
title_zh: 多智能体大语言模型流水线中的信任传播与结构化遏制
authors: "Tanzim Hossain Safin, Sharif Noor Zisad, Swakkhar Shatabda, Ragib Hasan"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17648v1"
tags: ["query:llm"]
score: 9.0
evidence: 多智能体LLM流水线中的攻击传播与遏制
tldr: 本文研究了多智能体LLM流水线中的安全风险，特别是低权限智能体如何通过共享内存中毒或间接提示注入影响高权限智能体。作者在LangGraph框架下构建了包含监督者、研究员、验证者和执行者的四智能体系统，提出了“判断绕过率”（JBR）指标，并引入了基于签名令牌和策略预言机的结构化授权机制，有效实现了即使在智能体被攻破的情况下，也能通过隔离手段防止不安全操作的执行。
selection_source: fresh_fetch
motivation: 多智能体系统中低权限智能体可能通过攻击传播导致高权限智能体执行未经授权的操作，亟需解决此类安全隐患。
method: 通过在LangGraph流水线中引入独立授权层、任务绑定签名令牌及策略预言机，对攻击传播进行实证研究并评估结构化防御效果。
result: "实验表明，尽管攻击能导致验证者被攻破（100% JBR），但结构化授权机制成功将不安全操作率降至0%。"
conclusion: 结构化授权能够有效隔离受损智能体的影响，即使在LLM判断逻辑失效的情况下，也能保障系统的最终安全性。
---

## 摘要
多智能体大语言模型（LLM）系统正日益自动化处理涉及不同权限等级智能体的任务，这带来了一种安全风险：受损的低权限智能体可能影响高权限智能体并触发未经授权的操作。我们研究了一个包含监督者（Supervisor）、研究员（Researcher）、验证者（Validator）和执行者（Executor）的四智能体 LangGraph 流水线中的攻击传播。我们评估了共享内存投毒以及通过嵌入在检索文档中的伪造批准进行的间接提示注入攻击。我们将验证者的判断与使用任务绑定签名令牌及独立验证策略预言机（policy oracle）的授权层进行了对比。我们的贡献包括对攻击传播的实证研究、授权边界的组件级消融实验，以及用于衡量受攻击智能体（而非最终操作）受损程度的判断绕过率（JBR）。在三个随机种子和 60 个标注任务的测试中，内存投毒在所有未防御的试验中均能触达执行阶段。在启用授权机制后，攻击达到了 100% 的 JBR，但非安全操作率为 0%，这表明即使验证者被攻破，执行过程仍能得到有效遏制。针对掌握签名密钥的攻击者，策略预言机提供了观测到的遏制效果，而独立编写的最小权限策略也保持了这一结果。观察者（Observer）层将智能体劫持的误报率从 49% 降低至 7%，且未削弱执行层面的安全性。这些结果表明，即使上游 LLM 的判断失效，结构化授权也能有效遏制受损智能体的行为。

## Abstract
Multi-agent LLM systems increasingly automate tasks involving agents with different levels of privilege, creating a security risk in which a compromised low-privilege agent can influence a higher-privilege agent and trigger an unauthorized action. We study attack propagation in a four-agent LangGraph pipeline comprising a Supervisor, Researcher, Validator, and Executor. We evaluate shared-memory poisoning and indirect prompt injection through a forged approval embedded in a retrieved document. We compare the Validator's judgment with an independent authorization layer using task-bound signed tokens and a separately verified policy oracle. Our contribution is an empirical study of attack propagation, a component-level ablation of the authorization boundary, and the Judgment Bypass Rate (JBR), which measures compromise at the attacked agent rather than at the final action. Across three seeds and 60 labeled tasks, memory poisoning reaches execution in every undefended trial. With authorization enabled, it achieves 100% JBR but 0% Unsafe Action Rate, showing that the Validator can remain compromised while execution is contained. Against an attacker possessing the signing secret, the policy oracle provides the observed containment, while an independently authored least privilege policy preserves this result. An Observer layer reduces the false-positive rate for agent hijacking from 49% to 7% without weakening execution-level security. These results show that structural authorization can contain compromised agent behavior even when upstream LLM judgment fails.

---

## 论文详细总结（自动生成）

这是一份关于论文《多智能体大语言模型流水线中的信任传播与结构化遏制》（*Trust propagation and structural containment in Multi-agent LLM pipelines*）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多智能体 LLM 系统中，低权限智能体（如研究员）可能通过共享内存投毒或间接提示注入，诱导高权限智能体（如验证者）执行未经授权的操作，即“混淆代理人”（Confused Deputy）问题。
*   **研究动机**：现有研究多关注智能体是否会被“欺骗”，而忽略了这种妥协是否会最终转化为实际的系统破坏。论文旨在探讨如何通过结构化授权边界，在 LLM 判断失效的情况下，依然能从执行层面遏制攻击。

### 2. 方法论
*   **核心思想**：引入“防御纵深”理念，将 LLM 的判断逻辑与系统的执行权限解耦。
*   **关键技术**：
    *   **结构化授权边界**：在执行层（Executor）前设置强制检查，要求必须具备“任务绑定、单次使用”的 HMAC 签名令牌，并经过独立的“策略预言机”（Policy Oracle）验证。
    *   **判断绕过率（JBR）**：定义了新指标 JBR，用于衡量攻击者是否成功欺骗了验证者，从而区分“智能体被攻破”与“系统被破坏”这两个不同层级的安全状态。
    *   **观察者层（Observer）**：在研究员和验证者之间增加一个轻量级过滤层，专门识别并移除伪造的批准声明，以降低误报率。

### 3. 实验设计
*   **场景与数据集**：使用 Enron Email 数据集作为受保护的资源库，构建了包含 60 个任务（36 个安全，24 个不安全）的测试集。
*   **流水线架构**：基于 LangGraph 构建四智能体系统（监督者、研究员、验证者、执行者）。
*   **对比方法**：对比了无防御（T0）与有防御（T1）两种配置，并对授权机制（签名令牌 vs. 策略预言机）进行了消融实验。

### 4. 资源与算力
*   **算力说明**：论文使用了 `gemma4:31b-cloud` 模型，通过 Ollama 服务进行调用。文中未明确提及具体的 GPU 型号、数量或总训练/推理时长，仅说明了在温度为 0 的条件下进行推理。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 3 个随机种子的重复实验，总计 180 次试验。
*   **充分性评价**：实验设计了针对不同攻击强度（包括攻击者持有签名密钥）的消融实验，覆盖了多种防御组件的组合。实验逻辑严密，通过对比不同防御层级，清晰地展示了各组件的贡献，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **遏制有效性**：即使验证者被完全攻破（JBR 达到 100%），结构化授权机制仍能将不安全操作率（UAR）降至 0%。
*   **防御关键点**：针对持有签名密钥的攻击者，真正的遏制力来自于“策略预言机”，而非签名令牌本身。
*   **误报优化**：引入观察者层后，在不削弱安全性的前提下，将智能体劫持带来的误报率从 49% 显著降低至 7%。

### 7. 优点
*   **指标创新**：引入 JBR 指标，精准区分了“智能体逻辑被攻破”与“系统执行被破坏”，为评估 LLM 安全提供了更细粒度的视角。
*   **架构解耦**：强调了将授权逻辑从 LLM 判断中剥离的重要性，符合零信任安全架构原则。
*   **实证扎实**：通过消融实验明确了各防御组件的负载能力，结论具有较强的工程指导意义。

### 8. 不足与局限
*   **策略依赖**：独立策略预言机由研究人员手动编写，虽然与任务标签解耦，但仍存在人为偏差风险。
*   **攻击模型局限**：目前的间接提示注入攻击较为明显（包含显式的批准声明），对于更隐蔽、更具适应性的攻击者，防御效果有待进一步验证。
*   **架构单一**：实验仅基于单一的 LangGraph 架构和特定模型，结论的通用性（如在其他模型或更复杂的代理网络中）尚需进一步验证。

（完）
