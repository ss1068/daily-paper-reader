---
title: "First Token Matters: Understanding Safety Collapse in Large Reasoning Models"
title_zh: 首个 Token 至关重要：理解大型推理模型中的安全崩溃
authors: "Yizheng Yang, Haining Yu, Yuechen Wang, Yikai Hou, Xing Fu, Jinbo Yang, Tianqing Zhu"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18471v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 大语言模型越狱漏洞的安全对齐与缓解技术
tldr: 本文研究了大型推理模型（LRMs）在处理有害查询时安全性下降的问题，发现其存在“起始拒绝崩溃”（ORC）现象，即在推理开始的首个生成token处拒绝信号显著减弱。为此，作者提出了SafeToken方法，通过在推理起始点注入学习到的连续安全锚点，在不影响推理能力的前提下有效提升了模型的安全性，揭示了安全失效源于从理解到生成过渡阶段的瞬时崩溃。
selection_source: fresh_fetch
motivation: 现有研究缺乏对大型推理模型安全失效内部机制的理解，且过度依赖额外的训练或偏好优化。
method: 提出SafeToken方法，通过在推理起始点注入一个学习到的连续安全锚点来干预模型生成。
result: 该方法仅需更新单个token嵌入，即可有效缓解起始拒绝崩溃，并在保持推理性能的同时提升了安全性。
conclusion: 大型推理模型的安全失效源于从理解到生成这一关键过渡阶段的瞬时崩溃，通过轻量级的推理时干预即可有效修复。
---

## 摘要
大型推理模型（LRMs）展现出强大的问题解决能力，但在处理有害查询时，其安全对齐往往会下降。现有的提升安全性的方法主要依赖于额外的训练或偏好优化，而对安全失效背后的内部机制理解有限。在这项工作中，我们通过对拒绝动态进行 Token 级别的定位分析，调查了这一失效现象，并识别出推理开始时存在的一个局部漏洞，我们将其称为“起始拒绝崩溃”（Onset Refusal Collapse, ORC）。我们发现，在处理有害查询时，LRMs 与拒绝相关的信号在生成的第一个 Token 处急剧下降，这与不安全响应的生成相关联。基于这一发现，我们提出了 SafeToken，这是一种轻量级的推理时干预方法，通过在推理开始时精确注入一个学习到的连续安全锚点。尽管仅更新单个 Token 嵌入，SafeToken 仍能有效缓解 ORC，提升模型在有害查询基准测试中的安全性，并很大程度上保留了推理效用。这些结果表明，LRMs 中的安全失效可能源于从理解到生成这一关键转换过程中的瞬时崩溃。

## Abstract
Large Reasoning Models (LRMs) exhibit strong problem-solving abilities, yet their safety alignment often degrades when handling harmful queries. Existing approaches to improving safety largely rely on additional training or preference optimization, while offering limited understanding of the internal mechanisms behind safety failures. In this work, we investigate this failure through a token-level positional analysis of refusal dynamics and identify a localized vulnerability at the onset of reasoning, which we term Onset Refusal Collapse (ORC). We find that the refusal-related signal of LRMs drops sharply at the first generated token under harmful queries, which is associated with unsafe response generation. Motivated by this finding, we propose SafeToken, a lightweight inference-time intervention that injects a learned continuous safety anchor precisely at reasoning onset. Despite updating only a single token embedding, SafeToken effectively mitigates ORC, improves safety on harmful-query benchmarks, and largely preserves reasoning utility. These results suggest that safety failures in LRMs can arise from a transient breakdown at the critical transition from understanding to generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《First Token Matters: Understanding Safety Collapse in Large Reasoning Models》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：大型推理模型（LRMs）通过思维链（CoT）显著提升了复杂任务的处理能力，但研究发现其安全对齐在处理有害查询时会发生退化。
*   **动机**：现有防御方法多依赖黑盒式的额外训练或偏好优化，缺乏对安全失效内部机制的理解。论文旨在揭示 LRMs 在推理过程中安全失效的“病灶”，并寻找更轻量、更具针对性的干预手段。

### 2. 方法论：SafeToken
*   **核心思想**：通过机制可解释性分析，发现模型在推理起始点存在“起始拒绝崩溃”（ORC）现象。SafeToken 旨在通过在推理起始位置注入一个学习到的“软 Token”（Soft Token）来锚定安全状态。
*   **关键技术**：
    *   **拒绝向量提取**：利用 IT（指令微调）模型与 LRM 共享的底层结构，通过安全头（Safety Heads）消融实验提取拒绝方向向量。
    *   **软 Token 优化**：将一个可学习的连续嵌入向量 $T_s$ 放置在推理起始位置（$t=0$），冻结模型参数，仅通过标准因果语言建模损失优化该嵌入。
    *   **推理时注入**：在推理时将 $T_s$ 确定性地附加到用户查询后，引导模型在生成首个 Token 时保持拒绝意图。

### 3. 实验设计
*   **数据集**：涵盖安全性（StrongReject, AdvBench）、越狱鲁棒性（GCG, WildJailbreak）、过度拒绝（XSTest）及通用推理效用（MMLU, MATH500, AIME2024, GPQA-Diamond）。
*   **对比方法**：SafeChain、SafePath、STAR-1 以及推理时变体 Safety Prefix。
*   **模型基座**：DeepSeek-R1-Distill 系列（Llama-8B, Qwen-7B/14B）及 Qwen3-8B。

### 4. 资源与算力
*   **训练效率**：SafeToken 仅需更新单个 Token 嵌入（大小等于模型隐藏层维度），在单张 GPU 上即可在几分钟内完成优化。
*   **具体配置**：文中未明确指定 GPU 型号，但强调了其极高的训练效率和极低的算力需求。

### 5. 实验数量与充分性
*   **实验覆盖**：进行了多维度评估，包括主实验、与现有防御方法的兼容性测试、注入位置敏感性分析（对比前置注入与起始注入）以及机制分析（Logit Lens 和对比归因分析）。
*   **客观性**：实验设计了对照组（Vanilla LRM），并使用了自动化评估器（Llama-Guard-3-8B）进行评分，实验设置较为严谨且具有对比性。

### 6. 主要结论与发现
*   **ORC 现象**：LRMs 的安全失效并非全局性的，而是高度局部化在推理起始点（$t=0$）的拒绝信号崩溃。
*   **因果关联**：起始位置的 affirmative（肯定性）Token 倾向是导致安全失效的结构性原因，该倾向在深层网络中被放大，从而覆盖了安全拒绝信号。
*   **有效性**：SafeToken 能够有效缓解 ORC，在提升安全性的同时，相比其他重型训练方法，更好地保留了模型的推理效用。

### 7. 优点
*   **机制驱动**：从可解释性出发定位问题，而非盲目试错。
*   **轻量化**：无需全参数微调，即插即用，对模型原始推理能力干扰极小。
*   **模块化**：可作为现有防御方法（如 SafePath, STAR-1）的补充组件，进一步提升防御效果。

### 8. 不足与局限
*   **强攻击鲁棒性**：在面对复杂的越狱攻击（如 GCG）时，SafeToken 的防御效果有所下降，因为此类攻击可能在更早的阶段干扰了模型对“有害性”的识别。
*   **应用场景**：目前主要针对推理起始点的崩溃，对于在推理过程中途产生的“路径漂移”或更深层的逻辑诱导攻击，防御能力有限。
*   **训练依赖**：虽然推理时轻量，但仍需少量数据进行软 Token 的优化训练。

（完）
