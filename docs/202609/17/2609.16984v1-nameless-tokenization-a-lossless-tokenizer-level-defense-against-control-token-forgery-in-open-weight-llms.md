---
title: "Nameless Tokenization: A Lossless Tokenizer-Level Defense Against Control-Token Forgery in Open-Weight LLMs"
title_zh: 无名分词：一种针对开源权重大型语言模型中控制令牌伪造的无损分词器级防御机制
authors: "Kisu Yang, Yoonna Jang, Heuiseok Lim"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.16984v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 针对控制令牌伪造的分词器级防御
tldr: 该研究针对开源大模型中控制令牌（Control-Token）易被伪造的安全漏洞，提出了一种名为“无名分词”（Nameless Tokenization）的防御机制。通过移除控制令牌的表面字符串映射，确保用户输入无法伪造系统级指令或工具调用，从而在保持模型原有分词性能的同时，显著提升了对提示词注入攻击的防御能力。
selection_source: fresh_fetch
motivation: 开源模型聊天模板中控制令牌的表面字符串可被用户输入伪造，导致模型无法区分真实指令与恶意注入。
method: 提出无名分词技术，将控制令牌保留为保留标识符但移除其对应的表面字符串，使内容编码器无法生成这些令牌。
result: "在五种分词器家族中，该方法在无攻击数据下保持了分词一致性，并将含分隔符文本的防御准确率从8.5%提升至59.9%。"
conclusion: 无名分词是一种有效的无损防御手段，能从根本上切断用户输入伪造控制令牌的路径，增强大模型的安全性。
---

## 摘要
开源权重语言模型会公开其聊天模板用于标记轮次、角色和工具结果的字符串，分词器会将这些字符串映射回模型所遵循的保留标识符。因此，任何能够控制提示词中内容的人，都可以编写出与服务栈所生成的轮次边界无法区分的内容。我们对 256 个已部署的聊天分词器进行了审计，发现所有分词器均可被伪造。通常被推荐作为修复方案的标志位在 56.6% 的情况下依然可被伪造，因为它遗漏了代理系统所依赖的工具和推理标记。我们提出了“无名分词”（nameless tokenization）方法，该方法将控制条目保留为仅有保留标识符而无表面字符串的状态，从而使内容编码器无法生成此类字符串，确保消息内容能够未经篡改地到达模型。在五个分词器家族的测试中，该方法在无攻击数据上能精确复现标准令牌流，并将包含分隔符文本的探测准确率从 8.5% 提升至 59.9%，而传统的清理程序则会丢失这些信息。将分隔符的外观与其标识符分离的研究表明，在面对简单的任务指令时，标识符的作用微乎其微；但一旦系统消息指示模型将用户内容视为数据，标识符则承载了绝大部分伪造的工具结果和伪造轮次信息。

## Abstract
Open-weight language models publish the strings their chat templates use to mark turns, roles and tool results, which the tokenizer maps back to the reserved identifiers the model obeys. Anyone who controls text in a prompt can therefore write a turn boundary indistinguishable from one the serving stack wrote. We audit 256 deployed chat tokenizers. All are forgeable, and the flag usually recommended as a fix leaves 56.6% forgeable because it misses the tool and reasoning markers agent systems rely on. We propose nameless tokenization, which leaves the control entries with a reserved identifier and no surface string, so the content encoder cannot emit one and message content reaches the model unaltered. Across five tokenizer families it reproduces the standard token stream exactly on attack-free data and lifts accuracy on a probe of delimiter-bearing text from 8.5% to 59.9%, where sanitizers lose it. Separating a delimiter's appearance from its identifier shows the identifier matters little against a bare task instruction, but carries most of a forged tool result and most of any forged turn once the system message tells the model to treat user content as data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Nameless Tokenization: A Lossless Tokenizer-Level Defense Against Control-Token Forgery in Open-Weight LLMs》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：开源大模型（LLM）的聊天模板（Chat Template）通常将特定的控制令牌（如 `<|im_start|>`、`<tool_call>`）映射为保留标识符。由于这些控制令牌的表面字符串是公开的，攻击者可以在用户输入中嵌入这些字符串，从而伪造出模型无法区分的“轮次边界”或“工具调用”，导致提示词注入攻击（Prompt Injection）。
*   **研究动机**：现有的防御手段（如禁用特殊令牌解析的标志位）在 56.6% 的情况下失效，因为它们往往忽略了代理系统（Agent Systems）中关键的工具和推理标记。此外，传统的字符串过滤方法会破坏正常内容，导致模型性能下降。

### 2. 方法论：无名分词（Nameless Tokenization）
*   **核心思想**：在分词器层面切断“表面字符串”到“保留标识符”的映射路径，使内容编码器无法通过输入文本生成控制令牌。
*   **关键技术细节**：
    *   **清空映射表**：将分词器中用于解析特殊令牌的表（Table）清空，并移除注入句子标记的后处理器。
    *   **模板直接写入**：渲染模板时，将消息内容与模板文本分开处理。模板层直接写入保留标识符，而消息内容通过修改后的编码器（Ec）进行编码。
    *   **无损性**：该方法保证了消息内容在编码过程中不被篡改，且在无攻击场景下，其分词流与标准分词器完全一致。

### 3. 实验设计
*   **数据集与场景**：使用了情感分类、自然语言推理（NLI）和抽取式问答（SQuAD）三个标准任务，共 296 个样本。
*   **Benchmark**：构建了“分隔符保真度探测”（Delimiter Fidelity Probe），测试模型在处理包含控制令牌的文本时的准确性。
*   **对比方法**：对比了多种清理程序（Sanitizers），包括：Strip（删除）、Mask（占位符替换）、Escape（全角字符替换）以及无防御基线。

### 4. 资源与算力
*   **资源说明**：论文未明确列出具体的 GPU 型号、数量或训练时长。实验主要侧重于分词器逻辑的审计与推理阶段的攻击成功率测试，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：审计了 256 个已部署的聊天分词器，并在 5 个不同家族的模型上进行了详细的攻击测试。
*   **客观性与公平性**：实验设计非常严谨，通过“分隔符保真度探测”量化了防御手段对正常内容的影响，并对比了多种防御基线，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **防御有效性**：无名分词在不影响正常任务准确率的前提下，显著降低了伪造攻击的成功率。
*   **标识符的作用**：研究发现，在简单的任务指令中，标识符本身贡献较小（7.1%），而分隔符的外观贡献较大（30.4%）；但在涉及系统消息将用户输入视为数据时，切断标识符通道是防御伪造攻击的关键。
*   **普遍性**：该方法适用于所有开源聊天模板，且无需针对特定模型进行微调。

### 7. 优点
*   **无损防御**：在提供安全保障的同时，不损害模型对正常文本的处理能力。
*   **通用性强**：不依赖于特定的模型架构，仅在分词器接口层面进行修改，易于部署。
*   **理论深度**：通过解构攻击成功率，明确了标识符与表面字符串在攻击中的不同贡献，为防御设计提供了理论依据。

### 8. 不足与局限
*   **防御范围限制**：该方法仅针对“文本到标识符”的接口，若部署环境直接接受令牌标识符输入，则该防御无效。
*   **特定攻击场景**：在某些伪造系统消息的攻击中，移除标识符后，模型反而可能因为分隔符文本的存在而更倾向于执行指令。
*   **审计局限**：审计仅覆盖了能够加载且包含聊天模板的仓库，未涵盖自定义分词器代码的特殊模型。

（完）
