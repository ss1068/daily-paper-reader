---
title: Combating Instruction Conflict via Energy-Driven Latent Conflict Detection
title_zh: 通过能量驱动的潜在冲突检测应对指令冲突
authors: "Mingyu Ma, Yuxin Wu, Jingbo Wang, Tianxiao Huang, Leixin Sun, Xiaochuan Shi"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08646v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于生成后安全护栏的潜在冲突检测
tldr: 针对大模型在处理层级指令时存在的指令冲突及“响应漂移”问题，本文提出了ELCD（能量驱动的潜在冲突检测器）。该方法通过分析模型生成的完整输出，构建复合隐藏状态表示，并利用成对边际排序目标在潜在空间中区分合规与违规响应，有效提升了系统级约束的执行安全性。
selection_source: fresh_fetch
motivation: 现有防御机制多依赖静态输入检查，无法有效检测模型生成过程中产生的“响应漂移”现象。
method: ELCD通过拼接最终Token嵌入与响应均值池化嵌入构建复合表示，并优化成对边际排序目标以在潜在空间分离合规与漂移响应。
result: "在多个主流大模型上的实验表明，ELCD显著优于现有基线，在Llama-2-7B上PR-AUC提升约30个百分点，在Mistral-7B上将FPR95降至2.67%。"
conclusion: ELCD为开源或自托管大模型提供了一种高效的潜在指令冲突检测方案，能有效保障系统级约束的执行。
---

## 摘要
大型语言模型（LLMs）越来越多地被部署在具有层级指令的场景中，但它们仍然容易受到用户指令覆盖系统级约束的冲突影响。现有的防御机制主要集中在静态输入检查上，因此无法检测到“响应漂移”（Response Drift）现象，即模型最终生成的响应尽管输入看起来合规，却违反了系统级约束。为了弥补这一差距，我们引入了 ELCD，这是一种用于生成后、交付前验证的响应级潜在冲突检测器。给定完整的生成输出，ELCD 通过将最终标记（token）嵌入与平均池化后的响应嵌入进行拼接，构建出一个复合隐藏状态表示。随后，它通过优化成对边际排序目标函数，在潜在空间中分离合规响应与漂移响应。在五个参数量从 1.5B 到 14B 的主流 LLM 上进行的广泛实验表明，ELCD 显著优于现有的竞争基线。值得注意的是，它将 Llama-2-7B 的 PR-AUC 提高了约 30 个百分点，并将 Mistral-7B 在 95% 真阳性率（TPR）下的误报率（FPR95）降低至 2.67%。这些结果表明，ELCD 为开放权重或自托管 LLM 部署中的潜在指令冲突检测提供了一种有前景的方法。

## Abstract
Large Language Models (LLMs) are increasingly deployed with hierarchical instructions, yet they remain vulnerable to conflicts in which user directives override system-level constraints. Existing defense mechanisms predominantly focus on static input inspection and therefore fail to detect Response Drift, a phenomenon in which the model's final response violates system-level constraints despite seemingly compliant inputs. To bridge this gap, we introduce ELCD, a response-level latent conflict detector for post-generation, pre-delivery verification. Given the full generated output, ELCD constructs a composite hidden-state representation by concatenating the final-token embedding with the mean-pooled response embedding. It then optimizes a pairwise margin ranking objective to separate compliant and drifting responses in latent space. Extensive experiments across five mainstream LLMs ranging from 1.5B to 14B parameters demonstrate that ELCD significantly outperforms competitive baselines. Notably, it improves the PR-AUC on Llama-2-7B by approximately 30 percentage points and reduces the False Positive Rate at 95% TPR (FPR95) on Mistral-7B to 2.67%. These results suggest that ELCD provides a promising approach for latent instruction-conflict detection in open-weight or self-hosted LLM deployments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Combating Instruction Conflict via Energy-Driven Latent Conflict Detection》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：大模型（LLM）常被部署在具有层级指令（系统指令 vs. 用户指令）的场景中。
*   **核心问题**：现有的防御机制（如输入过滤、提示词检测）多为静态输入检查，无法应对“响应漂移”（Response Drift）现象——即用户通过提示词注入绕过系统约束，导致模型生成的响应在表面上看似合规，实则违反了系统级安全或格式约束。
*   **研究目标**：填补生成后、交付前的安全验证空白，通过检测模型内部的潜在冲突来保障系统级约束的执行。

### 2. 方法论：ELCD (Energy-driven Latent Conflict Detection)
*   **核心思想**：将“响应漂移”建模为能量空间中的分布偏移。合规响应处于低能量盆地，而违规响应则表现为高能量的异常状态。
*   **关键技术**：
    *   **复合特征提取**：拼接“最终Token嵌入”（捕捉终端决策）与“响应均值池化嵌入”（捕捉整体语义轨迹），以获得更稳健的潜在表示。
    *   **能量投影头**：使用一个轻量级 MLP 将特征映射为标量能量分数，通过 LogSumExp 函数计算自由能量。
    *   **成对边际排序损失（Pairwise Margin Ranking Loss）**：通过优化损失函数，强制拉大合规响应与漂移响应在能量空间中的距离，确保决策边界清晰。

### 3. 实验设计
*   **数据集**：构建了包含8种可编程约束（如语言、格式、长度、禁止词等）的指令冲突数据集，涵盖 IID（同分布）和 OOD（跨分布/未见过的约束类型）两种评估场景。
*   **基线对比**：对比了四类方法：输入端检测器（Protect AI, Prompt-Guard）、LLM 自评估（LLM-based）、已知答案检测（Known-answer）以及注意力追踪（Attention Tracker）。
*   **评估指标**：PR-AUC（衡量分类性能）和 FPR95（衡量在 95% 召回率下的误报率，反映实际部署的可用性）。

### 4. 资源与算力
*   **硬件平台**：使用 NVIDIA GeForce RTX 3090 GPU 和 Intel Xeon Gold 6240 CPU。
*   **训练配置**：ELCD 仅训练轻量级能量头，无需微调目标 LLM 参数。训练过程使用 Adam 优化器，20 个 Epoch，批大小为 64。
*   **推理开销**：仅增加了一次响应段的池化操作和 MLP 前向传播，计算复杂度为 $O(T_r \cdot d)$，在自托管系统中具有极高的效率。

### 5. 实验数量与充分性
*   **覆盖范围**：在 5 个主流开源模型（Llama-2-7B, Phi-3-Mini, Mistral-7B, Qwen2.5-1.5B/14B）上进行了广泛测试。
*   **客观性**：进行了消融实验（分析边际参数 $m$ 的影响、特征拼接的必要性），并区分了 IID 与 OOD 场景，实验设计严谨，充分证明了方法的泛化能力。

### 6. 主要结论与发现
*   **性能优势**：ELCD 在所有模型上均显著优于基线。在 Llama-2-7B 上 PR-AUC 提升约 30 个百分点；在 Mistral-7B 上，FPR95 降至 2.67%，远低于基线方法近 100% 的误报率。
*   **泛化性**：在 OOD 场景下，ELCD 依然保持了极低的误报率，证明其捕捉的是模型内部的本质冲突信号，而非简单的模板匹配。

### 7. 优点
*   **检测范式创新**：从静态输入检查转向动态生成后验证，解决了“响应漂移”这一盲点。
*   **高效性**：作为轻量级模块，不改变原模型权重，推理延迟极低，易于集成到现有生产流水线。
*   **鲁棒性**：通过结合局部（Token）与全局（语义）特征，对不同长度和类型的冲突具有很强的适应性。

### 8. 不足与局限
*   **模型访问限制**：需要访问模型的隐藏状态（Hidden States），这限制了其在闭源 API 模型（如 GPT-4）上的直接应用。
*   **约束类型局限**：目前主要针对可编程的硬约束，对于复杂的语义政策或主观安全要求，其检测能力有待进一步验证。
*   **依赖校准**：检测阈值 $\tau$ 需要在特定模型和任务分布上进行校准，更换模型或任务时可能需要重新配置。

（完）
