---
title: "HoneyRoute: Honeypot-Model Routing for Adversarial LLM Serving"
title_zh: HoneyRoute：面向对抗性大语言模型服务的蜜罐模型路由机制
authors: Han Jin
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08306v2"
tags: ["query:jb-def"]
score: 9.0
evidence: 用于对抗性LLM服务防御的蜜罐模型路由
tldr: HoneyRoute 是一种针对大模型推理服务的防御层，通过轻量级流式路由器实时识别恶意请求，并将其重定向至蜜罐模型。该系统不仅保护了生产模型免受攻击，还能通过分析蜜罐中的交互数据生成攻击者指纹，从而实现路由器的持续优化与迭代，在极低延迟下实现了高效的防御与情报收集。
selection_source: fresh_fetch
motivation: 现有防御机制难以在推理服务层有效拦截恶意请求，且缺乏对攻击行为的反馈与情报利用。
method: 提出了一种包含流式路由器、双重实现蜜罐以及分析反馈循环的架构，通过轻量级模型实时分流并利用蜜罐捕获攻击特征。
result: "该系统在保持极低延迟（38ms）的同时达到 0.911 的 F1 分数，并能减少 97.8% 的恶意流量消耗，同时保持了对良性请求的高识别准确度。"
conclusion: HoneyRoute 通过将攻击流量转化为情报资源，为大模型服务提供了一种高效、低延迟且具备自我进化能力的防御方案。
---

## 摘要
我们提出了 HoneyRoute，这是一个推理服务层，旨在检测传入请求是否为恶意请求，若是，则将其路由至专门的蜜罐模型，从而在保护生产环境的同时，持续收集攻击者的交互信息以获取情报。现有的防御措施通常将陷阱嵌入模型内存或在协议层重建欺骗机制，这使得服务层处于未受保护状态，且无法将信息反馈至检测环节。HoneyRoute 结合了以下三个部分：(i) 流式路由器（由冻结的 0.8B 嵌入主干网络和各领域的 MLP 头部组成）；(ii) 双重实现蜜罐（基于规则/提示工程的代码蜜罐或专门的同系列副本）；(iii) 分析循环，将捕获的交互转化为攻击者指纹，用于路由器的再训练。在生产追踪数据和七个领域的攻击语料库测试中，该路由器的 F1 分数达到 0.911，中位延迟增加仅 38 毫秒；在 13 种对抗性变换下，其 F1 分数达到两层防护大模型级联方案的 96%，而延迟仅为其 1/385，且规避率为 0%。在并发的真实 GCG 后缀负载攻击下，通过分流恶意请求，生产模型的 Token 消耗降低了 97.8%；训练后的副本模型在良性留存请求上与生产模型的一致性达到 92.9%，而简单的无条件诱饵注入一致性仅为 7.6%，选择性伪装注入则恢复至 88.9%，从而映射出可恢复的保真度-可追溯性边界；此外，通过循环训练的校正头部，合法安全研究的误路由率降低了 9 倍，并将检测 F1 分数提升至 0.933。

## Abstract
We introduce HoneyRoute, an inference-serving layer that detects whether an incoming request is malicious and, if so, routes it to a dedicated honeypot model, shielding production while the adversary's interaction is continuously harvested for intelligence. Existing defenses embed traps inside model memory or rebuild deception at the protocol layer, leaving the serving tier unprotected and feeding nothing back into detection. HoneyRoute couples (i) a streaming router (a frozen 0.8B-embedding backbone with per-domain MLP heads), (ii) a dual-implementation honeypot (a rule/prompt-engineered code honeypot or a dedicated same-family replica), and (iii) an analysis loop that converts trapped interactions into attacker fingerprints for router retraining. On a production trace plus a seven-domain attack corpus, the router reaches F1=.911 at 38 ms median added latency, matching 96% of a two-tier guard-LLM cascade's F1 at 1/385 of its latency with 0% evasion under 13 adversarial transformations; diverting the malicious share cuts production-model token consumption under concurrent flooding with real GCG-suffix payloads by 97.8%; the trained replica agrees with the production model on 92.9% of benign holdout requests, while naive unconditional bait injection collapses to 7.6% and selective camouflaged injection recovers to 88.9%, mapping the recoverable fidelity-traceability frontier; and a loop-trained correction head cuts misrouting of legitimate security research 9x while raising detection F1 to .933.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity，很高兴为你分析这篇关于大模型安全防御的论文。以下是针对《HoneyRoute: Honeypot-Model Routing for Adversarial LLM Serving》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：当前大模型（LLM）部署面临严重的对抗性攻击（如越狱、提示词窃取、资源耗尽攻击）。
*   **痛点**：现有的防御方案要么将陷阱嵌入模型内部（如 MemPot），要么在协议层（SSH/HTTP）进行欺骗，导致**推理服务层（API Gateway）**成为防御盲区。
*   **动机**：作者旨在构建一个位于推理服务层的防御层，通过实时分流恶意请求至“蜜罐模型”，既保护生产模型，又能将攻击行为转化为情报，实现防御系统的自我进化。

### 2. 方法论：HoneyRoute 架构
*   **核心思想**：在生产模型前部署一个轻量级“流式路由器”，根据请求的恶意程度进行动态路由。
*   **关键组件**：
    *   **流式路由器**：基于冻结的 0.8B 参数嵌入主干网络，配合针对不同攻击领域的微型 MLP 头部（每个仅 0.07M 参数），实现毫秒级决策。
    *   **双重蜜罐实现**：
        *   **代码蜜罐 ($f_{Mc}$)**：基于规则/提示工程，无 GPU 开销，通过注入诱饵（Bait）捕获攻击特征。
        *   **训练蜜罐 ($f_{Mt}$)**：同系列模型副本，通过全会话记录进行无损仪器化（Instrumentation）分析。
    *   **持续分析循环**：将捕获的攻击轨迹转化为“攻击者指纹”，反馈至路由器进行增量训练，实现防御闭环。
*   **算法流程**：请求进入网关 -> 路由器评分 -> 若超过阈值则分流至蜜罐 -> 蜜罐记录交互 -> 提取指纹 -> 更新路由器头部。

### 3. 实验设计
*   **数据集与场景**：涵盖生产环境追踪数据、七大风险领域的攻击语料库、JailbreakBench 对抗性提示词、资源耗尽压力测试（GCG 后缀、长文档提取等）。
*   **对比基准**：
    *   基于关键词/正则的启发式方法。
    *   Guard-LLM 内容审核模型。
    *   两层防护级联方案（L1 门控 + L2 审核）。
*   **评估指标**：F1 分数、推理延迟、Token 消耗、攻击者识别准确率、良性请求误报率。

### 4. 资源与算力
*   **硬件配置**：使用单台 8 × H20-96GB GPU 服务器。
*   **算力消耗**：整个实验流程（E1-E7）总计耗时少于 50 GPU 小时。由于路由器头部极小，训练仅需几分钟。

### 5. 实验数量与充分性
*   **实验覆盖**：共 7 个实验模块（E1-E7），涵盖了检测质量、欺骗保真度、成本效益、循环增益、消融实验、资源吸收及多轮攻击检测。
*   **客观性**：实验设计了“判决优先”协议，并引入了不同模型家族（GLM-5.3 与 Deepseek-V4-Flash）进行交叉验证，以排除评估偏差。实验充分性较高，且包含了针对“对抗性感知攻击者”的压力测试。

### 6. 主要结论与发现
*   **性能卓越**：HoneyRoute 在 38ms 延迟下达到 0.911 的 F1 分数，性能接近级联方案但延迟仅为其 1/385。
*   **防御高效**：在并发攻击下，生产模型 Token 消耗降低 97.8%。
*   **情报价值**：通过三视图融合（嵌入、风格特征、风险概率），攻击者溯源准确率显著高于随机水平（17-24 倍）。
*   **自我进化**：通过分析循环，误报率降低 9 倍，且系统在多次迭代中保持了回归稳定性。

### 7. 优点
*   **低延迟与高扩展性**：通过冻结主干网络和微型 MLP 头部，实现了极高的推理效率。
*   **情报闭环**：不仅是防御，更是情报收集系统，将攻击流量转化为防御资产。
*   **保真度-可追溯性平衡**：通过“选择性伪装注入”技术，在保持蜜罐欺骗性的同时，最大化了攻击特征的捕获。

### 8. 不足与局限
*   **阈值依赖**：选择性诱饵注入的触发阈值需要针对不同部署环境进行调优。
*   **攻击覆盖**：虽然涵盖了多种攻击，但针对模型权重/行为提取的专用攻击实验尚不充分。
*   **评估局限**：部分实验（如 E7 的趋势规则）在小样本集上进行，且攻击者模型尚未进行针对该防御机制的深度梯度优化。

（完）
