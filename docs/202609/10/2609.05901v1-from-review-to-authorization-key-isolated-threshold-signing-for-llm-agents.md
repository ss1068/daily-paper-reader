---
title: "From Review to Authorization: Key-Isolated Threshold Signing for LLM Agents"
title_zh: 从审查到授权：面向大语言模型智能体的密钥隔离阈值签名
authors: "Yu Zheng, Qizhi Zhang"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.05901v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过阈值签名缓解LLM智能体中的提示注入
tldr: 针对LLM代理在执行敏感操作时面临的提示注入风险，本文提出了KITA架构。该系统通过将私钥和阈值签名份额与LLM处理过程物理隔离，实现了“从审查到授权”的机制。只有在获得经过身份验证的审查者批准后，系统才会结合多个独立域的签名份额生成授权，从而确保了即使LLM被攻破，攻击者也无法伪造执行权限，有效保障了代理执行的安全性。
selection_source: fresh_fetch
motivation: LLM代理在处理不可信内容时，若直接控制签名凭证，极易因提示注入导致越权执行。
method: 提出KITA架构，利用阈值BLS签名技术，将签名密钥与LLM进程隔离，并强制要求经过审查者批准后才能触发多域签名。
result: 通过六项系统测试验证了仲裁门控和消息绑定机制，并评估了阈值签名在在线路径下的性能表现。
conclusion: KITA通过密码学手段实现了执行绑定的授权完整性，有效防御了LLM代理的越权操作风险。
---

## 摘要
自主大语言模型（LLM）智能体能够将不可信内容转化为具有实际影响的操作，如支付和权限变更。如果同一进程既负责解析这些内容，又控制着可重复使用的签名凭证，那么提示词注入攻击就可能跨越判断边界，从而获取执行权限。我们提出了 KITA，这是一种“从审查到授权”的架构，它将用户的个人私密签名密钥及所有阈值签名密钥分片置于所有 LLM 进程之外。在阈值签名不可伪造性及我们的系统假设下，若无来自 t 个不同域的签名贡献，仅破坏提议者及少于 t 个审查者-签名者域，将无法为新操作生成有效授权。因此，任何此类授权都包含来自未受损域的分片，该分片绑定于规范操作，且仅在经过身份验证的审查者批准后才会发布。这确立了执行绑定的授权完整性。我们通过结构化输出 LLM 适配器和阈值 BLS 实现了完整的“审查者到执行者”路径。六项系统测试验证了该接口处的法定人数门控和消息绑定，同时密码学微基准测试评估了在线签名路径及其扩展性能。

## Abstract
Autonomous LLM agents can turn untrusted content into effectful actions such as payments and permission changes. If the same process interprets this content and controls a reusable signing credential, prompt injection can cross the judgment boundary and reach execution authority. We present KITA, a review-to-authorization architecture that keeps the user's personal secret signing key and every threshold signing-key share outside all LLM processes. Under threshold signature unforgeability and our system assumptions, compromising the proposer and fewer than t reviewer-signer domains cannot produce a valid authorization for a new action without signing contributions from t distinct domains. Thus, any such authorization includes a share from an uncompromised domain, bound to the canonical action and released only after authenticated reviewer approval. This establishes execution-bound authorization integrity. We implement the complete reviewer-to-executor path with a structured-output LLM adapter and threshold BLS. Six system tests validate quorum gating and message binding at this interface, while cryptographic microbenchmarks measure the online signing path and its scaling behavior.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Review to Authorization: Key-Isolated Threshold Signing for LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 智能体在处理不可信内容（如用户输入的指令或外部数据）时，若同时控制着具有执行权限的签名凭证，极易遭受“提示词注入”（Prompt Injection）攻击。攻击者可能诱导智能体执行未经授权的支付或权限变更，导致安全边界失效。
*   **研究动机**：现有的智能体框架缺乏将“语义判断”（模型推理）与“执行授权”（密码学签名）有效隔离的机制。论文旨在构建一种架构，确保即使智能体被攻破，攻击者也无法在没有获得法定人数（Quorum）审查批准的情况下，利用签名凭证执行恶意操作。

### 2. 方法论：KITA 架构
*   **核心思想**：实现“密钥隔离”与“阈值授权”。将签名密钥分片存储在独立的签名服务中，LLM 进程本身不接触任何私钥或分片。
*   **关键技术细节**：
    *   **规范化授权对象（Canonical Authorization Object）**：所有操作必须序列化为统一的规范格式，包含操作内容、证据摘要、策略版本等，防止在审查后篡改操作。
    *   **阈值 BLS 签名**：采用 Shamir 秘密共享方案，将签名密钥分为 $n$ 个分片，生成有效签名需至少 $t$ 个签名者参与。
    *   **审查-签名解耦**：LLM 审查者（Reviewer）仅负责语义评估，批准后通过认证通道触发对应的签名者（Signer）发布签名分片。
*   **流程**：提议者（Proposer）发送请求 $\rightarrow$ 多个独立审查者评估 $\rightarrow$ 审查者批准后触发签名分片 $\rightarrow$ 组合器（Combiner）收集 $t$ 个分片生成最终签名 $\rightarrow$ 执行者（Verifier）验证签名并执行。

### 3. 实验设计
*   **场景**：模拟了智能体支付场景（如购买笔记本电脑），包含合法请求与越权请求。
*   **Benchmark**：
    *   **功能性测试**：验证法定人数门控、消息绑定、重放攻击防御、动作替换防御等。
    *   **性能测试**：测量不同 $t$ 和 $n$ 配置下的签名分片生成、组合及最终验证的耗时。
    *   **安全性模拟**：通过蒙特卡洛模拟评估不同审查者相关性（Correlation）下的误报率（FA）与漏报率（FR）。

### 4. 资源与算力
*   **硬件环境**：实验在 Apple Silicon 芯片（macOS 15.7.4）上进行。
*   **算力消耗**：论文未提及大规模 GPU 训练算力，因为该系统主要关注密码学协议的在线执行效率。微基准测试显示，签名分片生成仅需毫秒级（4.9–5.7 ms），验证耗时约 0.5 秒，属于轻量级系统。

### 5. 实验数量与充分性
*   **实验组数**：包含 6 项系统功能测试、1 组真实 Codex 模型调用测试、以及 40 万次蒙特卡洛模拟实验。
*   **充分性**：实验设计较为充分。通过确定性脚本测试验证了系统的逻辑边界，通过真实 LLM 调用验证了接口兼容性，通过模拟实验量化了安全与可用性之间的权衡，具有较好的客观性和严谨性。

### 6. 主要结论与发现
*   **安全性**：KITA 成功实现了“执行绑定”的授权完整性，即使攻击者控制了提议者和少于 $t$ 个审查者，也无法伪造有效签名。
*   **性能**：阈值签名方案在在线路径上无需复杂的交互，且最终验证耗时与委员会规模无关，具备良好的扩展性。
*   **权衡**：委员会的阈值 $t$ 设置直接决定了系统的安全性与可用性（误报与漏报的平衡）。

### 7. 优点
*   **安全性高**：通过密码学手段强制隔离了 LLM 进程与密钥，从根本上解决了提示词注入导致的越权问题。
*   **架构清晰**：将语义审查与执行授权解耦，且不依赖单一信任点。
*   **兼容性**：支持现有的支付协议（如 AP2），可作为现有智能体系统的安全增强层。

### 8. 不足与局限
*   **可用性挑战**：如果审查者之间存在高度相关性（如使用相同的模型基座），委员会的集体决策优势会下降。
*   **运维复杂性**：需要维护多个独立的签名服务域，增加了系统的部署和管理成本。
*   **延迟影响**：虽然密码学计算很快，但引入多轮审查和网络通信可能会增加智能体执行任务的整体延迟。

（完）
