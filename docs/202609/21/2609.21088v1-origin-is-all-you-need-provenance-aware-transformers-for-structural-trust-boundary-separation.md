---
title: "Origin Is All You Need: Provenance-Aware Transformers for Structural Trust-Boundary Separation"
title_zh: 溯源即所需：用于结构化信任边界分离的溯源感知 Transformer
authors: "Yuxuan Zhang, Jeff Huang, Guofei Gu"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.21088v1"
tags: ["query:llm"]
score: 8.0
evidence: 通过来源感知Transformer防御间接提示注入
tldr: 针对大模型面临的间接提示注入（IPI）攻击，本文提出了一种“来源感知Transformer”架构。该方法通过为输入标记分配来源ID，并引入来源嵌入、注意力偏置和缩放机制，在模型内部构建了权威与非权威信息的结构化信任边界。通过两阶段微调，该模型在保持原有任务性能的同时，有效防御了分布内外的提示注入攻击，实现了从模式匹配到显式信任分离的安全范式转变。
selection_source: fresh_fetch
motivation: 现有Transformer架构无法区分指令与数据来源，导致模型极易受到间接提示注入攻击。
method: 通过引入来源ID编码、来源嵌入及可学习的注意力偏置，在模型架构层面强制执行来源信任边界。
result: 实验表明该方法在保持模型实用性的同时，对分布内外的提示注入攻击均表现出极强的鲁棒性。
conclusion: 将来源信息作为架构级信号，是实现大模型安全对齐从脆弱模式匹配转向显式信任分离的有效途径。
---

## 摘要
间接提示注入（IPI）仍然是大语言模型（LLM）系统面临的核心安全挑战，因为标准 Transformer 架构缺乏对源权威性的认知。检索到的文档、用户输入和系统指令均通过相同的无差别注意力机制进行处理，迫使模型仅能根据措辞来推断哪些内容应被遵循，哪些应被视为数据。我们提出了溯源感知 Transformer（Provenance-Aware Transformers），这是一种溯源感知的防御机制，使应用程序提供的源标签能够在模型内部发挥作用。每个输入标记都被分配一个编码其来源的环形 ID（ring ID），并辅以源嵌入、可学习的源注意力偏置以及在归一化过程中保持溯源性的可学习源缩放参数。由此产生的架构在生成过程中强制执行了权威来源与非权威来源之间的结构化边界。为了在已发布的预训练模型上实现该架构，我们提出了一个两阶段微调流水线，以教授模型在环形约束下的源语义和任务行为。评估结果表明，溯源感知 Transformer 在分布内和分布外均保持了对 IPI 的稳健抵御能力，同时保留了与基础预训练模型相当的效用。更广泛地说，我们的工作表明，将溯源作为一类架构信号，可以将 LLM 的安全对齐从脆弱的模式匹配转向显式的信任分离。

## Abstract
Indirect prompt injection (IPI) remains a central safety and security challenge for large language model (LLM) systems because standard transformers lack architectural notion of source authority. Retrieved documents, user inputs, and system instructions are all processed through the same undifferentiated attention mechanism, forcing the model to infer from wording alone what should be obeyed and what should be treated as data. We propose Provenance-Aware Transformers, a provenance-aware defense that makes application-supplied source labels actionable inside the model. Each input token is assigned a ring ID encoding its origin, and the model is augmented with origin embeddings, a learnable origin attention bias, and a learnable origin scale that preserves provenance under normalization. The resulting architecture enforces a structural boundary between authoritative and non-authoritative sources during generation. To instantiate this architecture on released pretrained models, we propose a two-stage fine-tuning pipeline to teach the model origin semantics and task behavior under ring constraints. Evaluation shows that Provenance-Aware Transformers maintain robust resistance to IPI both in-distribution and out-of-distribution while preserving utility comparable to the base pretrained model. More broadly, our work shows that exposing provenance as a first-class architectural signal can shift LLM safety alignment from brittle pattern matching toward explicit trust separation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Origin Is All You Need: Provenance-Aware Transformers for Structural Trust-Boundary Separation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）缺乏对输入来源的“权威性”认知。在当前的 Transformer 架构中，系统指令、用户输入和外部检索数据被同等对待，导致模型无法区分哪些是“必须执行的指令”，哪些是“仅供参考的数据”。
*   **研究背景**：间接提示注入（IPI）攻击利用了这一架构缺陷，通过在外部文档中植入恶意指令，诱导模型执行非预期的操作。现有的防御手段（如提示工程、检测器、微调）多为事后补救或基于模式匹配，缺乏架构级的安全保障。

### 2. 方法论：溯源感知 Transformer
*   **核心思想**：将“溯源（Provenance）”作为一类架构级信号，通过显式的来源标签强制执行信任边界。
*   **关键技术细节**：
    *   **环形 ID（Ring ID）分配**：将输入标记分为 4 个等级（Ring 0: 系统指令；Ring 1: 模型自身输出；Ring 2: 用户/受信任数据；Ring 3: 不受信任的外部数据）。
    *   **来源嵌入（Origin Embedding）**：在输入层引入可学习的来源嵌入，并配合可学习的缩放因子（$\alpha$）以防止归一化层稀释溯源信号。
    *   **来源感知注意力偏置（Origin-Aware Attention Bias）**：在注意力计算中引入一个可学习的偏置矩阵 $B$。当查询（Query）来自 Ring 1/2 而键（Key）来自 Ring 3 时，模型会施加负向偏置，从而在物理层面抑制对不受信任内容的关注。
*   **训练流水线**：
    *   **阶段一（溯源微调）**：在通用指令数据集上训练，使模型理解不同来源标签的语义。
    *   **阶段二（对齐微调）**：在构建的结构化注入数据集上训练，强化对 Ring 3 内容的抑制行为。

### 3. 实验设计
*   **数据集**：使用了 IID 数据集（SEP 测试集）和三个 OOD（分布外）数据集（PI-Attack, DataSentinel, Math-Tutor），涵盖了多种任务类型和攻击风格。
*   **基准对比**：对比了 10 种现有防御方法，包括提示工程（如 Sandwich）、检测器（如 PromptSleuth, DataSentinel）以及基于微调的防御（如 SecAlign）。
*   **评估指标**：攻击成功率（ASR）和 AlpacaEval 2.0 长度控制胜率（LC Win Rate，用于评估模型实用性）。

### 4. 资源与算力
*   **硬件配置**：使用 8 块 NVIDIA A100 (80GB) GPU。
*   **训练细节**：使用了 DeepSpeed ZeRO-2 进行分布式训练。阶段一训练 2 个 epoch，阶段二训练 3 个 epoch。该论文明确了训练参数（如学习率、批大小、梯度累积步数），具有较高的可复现性。

### 5. 实验数量与充分性
*   **实验覆盖**：进行了多模型（SmolLM2, LLaMA-3, Qwen2.5, Mistral-7B）、多数据集、多攻击类型（包括白盒自适应攻击 GCG 和 NeuExe）的广泛评估。
*   **客观性**：通过消融实验（如去掉偏置矩阵、去掉阶段一训练）验证了各组件的必要性。实验设计严谨，通过对比“基线微调”与“溯源感知”模型，排除了单纯因微调带来的性能提升，证明了架构设计的有效性。

### 6. 主要结论与发现
*   **鲁棒性**：溯源感知 Transformer 在所有 OOD 测试中实现了近乎 0% 的攻击成功率，显著优于现有防御方法。
*   **通用性**：该架构不仅能防御提示注入，还能在不进行特定训练的情况下有效防御 Jailbreak（越狱）攻击。
*   **实用性**：在保持极高安全性的同时，模型在通用任务上的表现与基础模型相当，未出现明显的性能退化。

### 7. 优点
*   **架构级防御**：从根本上解决了“指令-数据混淆”问题，而非依赖于脆弱的模式匹配。
*   **可解释性**：通过显式的 Ring ID 和偏置矩阵，防御逻辑清晰且可控。
*   **通用性强**：该架构与模型规模无关，且对未见过的攻击类型具有极强的泛化能力。

### 8. 不足与局限
*   **粒度限制**：目前的防御是基于“环（Ring）”的，即对整个来源进行无差别抑制，无法区分同一来源中的合法指令与恶意数据。
*   **应用依赖**：需要应用程序在输入层准确地为标记分配来源标签，这增加了系统集成的复杂度。
*   **微调成本**：虽然效果显著，但仍需对模型进行两阶段微调，对于超大规模模型（如 GPT-4 级别）的直接应用可能存在算力挑战。

（完）
