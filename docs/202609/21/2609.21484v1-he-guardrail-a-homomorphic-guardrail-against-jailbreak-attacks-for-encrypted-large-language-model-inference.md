---
title: "HE-Guardrail: A Homomorphic Guardrail Against Jailbreak Attacks for Encrypted Large Language Model Inference"
title_zh: HE-Guardrail：一种针对加密大语言模型推理的同态护栏，用于防御越狱攻击
authors: "Byeongseo Min, Yongwoo Lee, Young-Sik Kim, Yongjune Kim"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21484v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 针对加密LLM推理的同态越狱攻击防御护栏
tldr: 本文针对同态加密（HE）环境下大模型推理面临的越狱攻击安全漏洞，提出了HE-Guardrail框架。该框架实现了在加密域内对输入提示词和输出响应进行安全审查，确保服务器在无法解密数据的前提下，仍能有效拦截恶意攻击，从而在保护用户隐私的同时提升了加密大模型推理的安全性。
selection_source: fresh_fetch
motivation: 同态加密保护了数据隐私，但也导致服务器无法审查加密输入，使得大模型极易遭受隐蔽的越狱攻击。
method: 提出HE-Guardrail框架，将Llama Guard等主流安全护栏机制迁移至加密域，实现对加密数据的直接审查与控制。
result: 实验表明，HE-Guardrail在加密域内能高度还原明文护栏的决策效果，并展现出良好的安全、效率与性能平衡。
conclusion: 该研究为隐私保护下的AI推理提供了有效的安全防御方案，解决了加密计算中内容审查的缺失问题。
---

## 摘要
同态加密（HE）已成为隐私保护机器学习（PPML）的一种极具前景的方法，能够直接在加密数据上进行计算。在基于HE的PPML中，客户端向服务器提交加密输入，服务器在无法访问底层明文的情况下评估大语言模型（LLM）等模型。然而，我们在此场景中发现了一个关键的安全漏洞：HE-LLM推理容易受到提交对抗性提示（如越狱攻击）的恶意客户端的攻击。保护良性客户端的机密性同时也阻止了服务器检查传入的提示或生成的响应，使得对抗性尝试难以被检测或拦截，并可能导致成功的攻击对服务器完全不可见。为了解决这一漏洞，我们提出了HE-Guardrail，这是一个完全在加密数据上评估护栏机制，并同态控制是否将目标模型响应返回给客户端的框架。我们利用三种代表性护栏——Llama Guard、JBShield和GradSafe对HE-Guardrail进行了实例化。实验结果表明，HE-Guardrail在加密域中能够精确复现相应明文护栏的决策，并展现出独特的安全性、效率与效用之间的权衡。

## Abstract
Homomorphic encryption (HE) has emerged as a promising approach to privacy-preserving machine learning (PPML), enabling computation directly over encrypted data. In HE-based PPML, a client submits an encrypted input to the server, which evaluates models such as large language models (LLMs) without access to the underlying plaintext. However, we identify a critical security vulnerability in this setting: HE-LLM inference is vulnerable to malicious clients that submit adversarial prompts, such as jailbreak attacks. The same confidentiality that protects benign clients also prevents the server from inspecting incoming prompts or generated responses, making adversarial attempts difficult to detect or block and potentially allowing successful attacks to remain entirely invisible to the server. To address this vulnerability, we propose HE-Guardrail, a framework that evaluates guardrail mechanisms entirely over encrypted data and homomorphically controls whether the target-model response is returned to the client. We instantiate HE-Guardrail with three representative guardrails - Llama Guard, JBShield, and GradSafe. Our results show that HE-Guardrail closely reproduces the decisions of the corresponding plaintext guardrails in the encrypted domain, with distinct security-efficiency-utility trade-offs.

---

## 论文详细总结（自动生成）

这是一份关于论文《HE-Guardrail: A Homomorphic Guardrail Against Jailbreak Attacks for Encrypted Large Language Model Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：同态加密（HE）允许在不解密的情况下对数据进行计算，是隐私保护机器学习（PPML）的核心技术。
*   **核心问题**：在 HE-LLM 推理场景中，服务器无法查看加密的输入提示词（Prompt）和生成的响应。这种“盲目性”虽然保护了用户隐私，但也导致服务器无法识别和拦截恶意客户端提交的“越狱攻击”（Jailbreak Attacks），使得恶意攻击在加密管道中畅通无阻。

### 2. 方法论：HE-Guardrail 框架
*   **核心思想**：在加密域内实现护栏机制，通过同态计算判断输入或输出的安全性，并利用“门控响应机制”决定是否返回模型结果。
*   **关键技术**：
    *   **门控机制**：利用多项式近似符号函数（sgn_poly）计算安全门控值 $g$（1为安全，0为有害）。
    *   **响应控制**：通过公式 $Z(g) = g \cdot Y + (1 - g) \cdot R + \epsilon(g)$，将目标模型输出 $Y$ 与预设拒绝响应 $R$ 进行插值。
    *   **门控噪声淹没（Gated Noise Flooding）**：引入随机噪声 $\epsilon(g)$，确保当请求被拦截（$g \approx 0$）时，客户端无法通过解密残差恢复出被屏蔽的有害内容。
    *   **护栏实例化**：
        *   **预处理护栏**：使用 Llama Guard 评估加密输入。
        *   **内处理护栏**：利用模型内部状态（JBShield）或梯度信息（GradSafe）进行检测。

### 3. 实验设计
*   **数据集与场景**：使用了包含 2,215 个提示词的六种越狱攻击家族（如 IJP, GCG, AutoDAN, DrAttack, MultiJail, ActorAttack），以及 AlpacaEval 和 OR-Bench 用于评估良性提示词的误报率。
*   **对比方法**：对比了无防御、Llama Guard、JBShield-D 和 GradSafe-Zero 在加密域内的表现。
*   **评估指标**：安全性（攻击成功率 ASR、通过护栏率 PGR）、效率（内存占用、延迟）、效用（良性提示词误报率 FPR）。

### 4. 资源与算力
*   **硬件配置**：使用了两块 NVIDIA H200 GPU。
*   **部署方式**：目标模型（Llama-3-8B-Instruct）占用一块 GPU，护栏模型占用另一块。
*   **计算开销**：文中未明确提及具体的训练时长，但详细说明了内存增量（如 Llama Guard 增加约 140 GiB，GradSafe 增加约 110 GiB）。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了单轮和多轮攻击，并对不同类型的护栏进行了对比，实验设计较为全面。
*   **客观性**：通过对比明文护栏与加密护栏的决策一致性（99.7%），证明了方法的有效性。实验在统一的 SEU（安全-效率-效用）框架下进行，具有较好的公平性。

### 6. 主要结论与发现
*   **有效性**：HE-Guardrail 在加密域内能高度还原明文护栏的决策，且几乎不增加端到端的推理延迟（因为护栏评估可与解码过程并行）。
*   **权衡**：不同护栏在安全性与误报率之间存在显著权衡（如 Llama Guard 防御力强但误报率略高，JBShield 和 GradSafe 则更具容忍度）。

### 7. 优点
*   **创新性**：首次将多种主流护栏机制成功迁移至同态加密环境，填补了加密推理中内容审查的空白。
*   **实用性**：通过“门控噪声淹没”技术，巧妙解决了加密域内拒绝响应可能泄露原始信息的问题。
*   **性能优化**：通过多模型服务（Multi-model serving）和低级检查点（Low-level checkpointing）技术，有效降低了内存和计算开销。

### 8. 不足与局限
*   **多轮攻击挑战**：对于跨多轮对话的复杂越狱攻击，目前的护栏机制仍存在局限，无法有效推理加密的对话历史。
*   **内存开销**：尽管进行了优化，但护栏的引入仍带来了显著的额外内存需求（尤其是 Llama Guard），对硬件资源要求较高。
*   **应用限制**：该方法依赖于特定的 HE 库（desilofhe）和 CKKS 方案，在通用性和跨平台部署上可能存在一定门槛。

（完）
