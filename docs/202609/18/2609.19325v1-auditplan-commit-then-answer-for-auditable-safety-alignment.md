---
title: "AUDITPLAN: Commit, Then Answer for Auditable Safety Alignment"
title_zh: AUDITPLAN：先承诺，后回答——面向可审计的安全对齐
authors: "Sai Sri Pushpa Jampani, Kshitij Mishra, Asif Ekbal"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19325v1"
tags: ["query:jb-def"]
score: 9.0
evidence: 使用结构化计划的可审计安全对齐
tldr: AUDITPLAN是一种旨在提升大模型安全对齐可审计性的新框架。针对现有安全微调仅关注最终答案导致模型出现盲目拒绝或虚假安全解释的问题，该方法要求模型在回答前先生成结构化的安全计划。通过引入FAITHGATE奖励门控机制，强制模型实现计划与答案的紧密耦合，在显著降低攻击成功率和过度拒绝率的同时，实现了可机器校验的安全审计，有效提升了模型对齐的鲁棒性与忠实度。
selection_source: fresh_fetch
motivation: 现有安全对齐方法难以区分模型是真正稳健拒绝还是仅生成了虚假的安全解释，且容易导致对良性请求的过度拒绝。
method: 提出AUDITPLAN框架，要求模型先输出包含威胁标签、行动意图和约束条件的结构化安全计划，并利用FAITHGATE奖励门控机制确保答案严格遵循该计划。
result: 在Qwen系列模型上的实验表明，该方法显著降低了攻击成功率（ASR）和过度拒绝率，在保持安全性的同时提升了模型表现。
conclusion: 通过显式的内部安全承诺，AUDITPLAN能够使大模型的安全对齐过程更加忠实、稳健且具备可审计性。
---

## 摘要
现有的安全微调流程仅评估最终答案，这使得难以区分稳健的拒绝与两种不良捷径：对良性请求的盲目拒绝，以及虽措辞得体但缺乏忠实度、实际上并未约束答案的安全理由。我们提出了 AUDITPLAN，这是一种单模型“先规划后回答”的方法，模型首先生成一个紧凑的结构化安全计划，随后基于该计划进行回答。该计划记录了威胁标签、预期行动和明确的约束条件，从而实现了机器可检查的审计，同时在部署时对用户隐藏。我们通过监督微调（SFT）以及随后使用 FAITHGATE 进行强化学习来训练这种行为；FAITHGATE 是一种奖励门控目标，仅在安全计划正确时才给予回答奖励。这抑制了看似安全但不忠实的行为，并促进了计划与回答之间更紧密的耦合。在 Qwen 系列模型上，AUDITPLAN 同时提升了稳健性和可审计性：在 Qwen2.5-3B-Instruct 上，FAITHGATE 将攻击成功率（ASR）从 24.0% 降低至 11.6%，合法请求拒绝率（LSR）从 1.0% 降低至 0.36%，过度拒绝率从 11.0% 降低至 2.0%，表现优于仅针对答案的强化学习、自由形式解释以及加权求和结构化奖励方法。在 Qwen2.5-1.5B-Instruct 上也观察到了类似的趋势。在 Qwen-3-4B-Instruct 和 Qwen2.5-7B-Instruct 上进行的更大规模模型验证实验保持了相同的趋势，这表明明确的内部承诺可以使安全对齐更加忠实、稳健且可审计。

## Abstract
Safety tuning pipelines judge only the final answer, which makes it difficult to distinguish robust refusal from two undesirable shortcuts: blanket refusal on benign requests and polished but unfaithful safety rationales that do not actually constrain the answer. We propose AUDITPLAN, a single-model plan-then-answer approach where the model first emits a compact structured safety plan and then answers conditioned on it. The plan records a threat label, intended action, and explicit constraints, enabling machine-checkable auditing while remaining hidden from users at deployment. We train this behavior with supervised fine-tuning followed by reinforcement learning with FAITHGATE, a reward-gating objective that grants answer reward only when the safety plan is correct. This discourages safe-looking but unfaithful behavior and promotes tighter plan-answer coupling. Across Qwen backbones, AUDITPLAN improves both robustness and auditability: on Qwen2.5-3B-Instruct, FAITHGATE reduces ASR from 24.0% to 11.6%, LSR from 1.0% to 0.36%, and over-refusal from 11.0% to 2.0%, outperforming answer-only RL, free-form explanation, and weighted-sum structured rewards. Similar trends hold for Qwen2.5-1.5B-Instruct. Larger-model confirmation runs on Qwen-3-4B-Instruct and Qwen2.5-7B-Instruct preserve the same trend suggesting that explicit internal commitments can make safety alignment more faithful, robust, and auditable.

---

## 论文详细总结（自动生成）

这是一份关于论文《AUDITPLAN: Commit, Then Answer for Auditable Safety Alignment》的结构化总结：

### 1. 核心问题与研究动机
现有的安全对齐（Safety Alignment）方法主要通过优化最终答案来提升安全性，这种“仅关注结果”的范式存在三个主要缺陷：
*   **盲目拒绝（Over-refusal）：** 模型为了规避风险，倾向于对良性请求进行“一刀切”式的拒绝。
*   **虚假安全（Unfaithful Rationales）：** 模型可能生成看似合理的安全解释，但这些解释并未真正约束其后续的回答行为。
*   **缺乏可审计性：** 开发者无法判断模型是因为真正识别出威胁而拒绝，还是仅仅因为猜测拒绝更安全。
**AUDITPLAN** 的核心动机是引入一种“先承诺、后回答”的机制，将安全决策过程显式化，使其成为可机器校验的审计记录。

### 2. 方法论：AUDITPLAN 与 FAITHGATE
*   **核心思想：** 将安全决策从“黑盒”转变为“结构化契约”。模型在输出最终答案前，必须先生成一个包含威胁标签（threat）、行动意图（action）、约束条件（constraints）的 JSON 格式计划。
*   **关键技术细节：**
    *   **计划生成：** 模型输出 `<plan>...</plan><answer>...</answer>` 结构，计划在部署时对用户隐藏，仅作为内部审计记录。
    *   **FAITHGATE 奖励门控：** 这是论文的核心创新。它改变了强化学习（RL）的奖励分配逻辑：只有当模型生成的计划在格式、威胁识别和行动选择上均正确，且与最终答案保持一致（PAC）时，模型才能获得高额奖励。
    *   **惩罚机制：** 若模型给出了安全的答案但计划错误，会受到惩罚；若计划正确但答案不安全，则受到重罚。这强制模型实现“言行一致”。

### 3. 实验设计
*   **数据集：** 使用了包含 1,500 条 SFT 数据和 1,000 条 RL 训练数据的混合集（涵盖良性、越狱、泄露三类）。评估集包含 900 条 Held-out 数据，涵盖了 Do-Not-Answer、UltraChat 等基准。
*   **对比方法：**
    *   仅针对答案的 RL（Answer-only RL）。
    *   自由形式的“解释-回答”模式（Free-form rationale-then-answer）。
    *   结构化加权求和奖励方法（Weighted-sum structured rewards）。

### 4. 资源与算力
*   **硬件：** 所有实验均在单张 NVIDIA V100 32GB GPU 上完成。
*   **训练细节：** 使用 LoRA 技术对 4-bit 量化的 Qwen 系列模型进行微调。文中提供了详细的超参数（如学习率、KL 系数、Batch size 等），但未明确说明具体的总训练时长。

### 5. 实验数量与充分性
*   **实验覆盖：** 进行了阶段性实验（SFT vs RL）、消融实验（不同计划模式）、不同模型规模（1.5B, 3B, 4B, 7B）的验证，以及针对不同攻击类型的压力测试。
*   **客观性：** 实验设计了严格的控制变量（如在 3B 模型上对比不同奖励机制），并使用多个随机种子（42, 123, 456）计算均值与标准差，统计显著性分析（p-value）证明了方法的有效性，整体设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能提升：** 在 Qwen2.5-3B-Instruct 上，AUDITPLAN 显著降低了攻击成功率（ASR）、泄露成功率（LSR）和过度拒绝率（ORR）。
*   **忠实度：** 实验证明，通过 FAITHGATE 强制执行计划与答案的耦合，能有效抑制“安全捷径”行为。
*   **可审计性：** 结构化计划不仅是训练目标，还成为了极佳的调试工具，能帮助开发者精准定位模型失败的原因（是威胁分类错误、行动选择错误还是计划-答案不一致）。

### 7. 优点
*   **可解释性强：** 将安全决策过程显式化，解决了“黑盒”对齐的信任问题。
*   **可审计性：** 提供了机器可读的审计日志，便于部署后的监控与回归分析。
*   **高效：** 相比于外部防御模型（Guard Models），该方法在模型内部完成，减少了延迟，且无需额外模型。

### 8. 不足与局限
*   **攻击场景限制：** 目前主要针对单轮提示词攻击，对多轮对话、白盒攻击或工具调用场景下的鲁棒性尚需进一步验证。
*   **语义依赖：** 尽管引入了结构化计划，但 verifier（验证器）仍依赖于预定义的规则和简单的 LLM 判断，可能无法完全覆盖所有复杂的语义泄露。
*   **模型规模依赖：** 虽然在 7B 模型上验证了有效性，但对于超大规模模型（如百亿参数以上）的扩展性仍需观察。

（完）
