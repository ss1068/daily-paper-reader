---
title: "AgentGuard: Learning Execution Guardrails from Anomalous Coding-Agent Trajectories"
title_zh: AgentGuard：从异常编码智能体轨迹中学习执行护栏
authors: "Wuyang Dai, Song Wang"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.16287v1"
tags: ["query:jb-def"]
score: 8.0
evidence: 代码智能体的执行防护栏框架
tldr: AgentGuard是一个旨在提升AI编程代理执行可靠性的指令级防护框架。针对代理在任务中可能出现的误改文件、执行不安全命令等异常行为，该框架通过自动分析历史失败轨迹，提取并泛化出执行约束规则。这些规则作为轻量级技能动态激活，在保障安全的同时最小化对正常操作的干扰，显著提升了代理的任务成功率并降低了异常执行率。
selection_source: fresh_fetch
motivation: AI编程代理在执行任务时常出现误操作或忽略验证等异常行为，缺乏有效的自动化防护机制。
method: 通过分析历史异常轨迹，自动提取并泛化出指令级的行为约束，构建动态激活的轻量级防护技能。
result: "AgentGuard将异常执行率从69.0%降至26.7%，并将任务成功率从21.7%提升至35.0%。"
conclusion: 从历史失败中学习执行防护规则是提升AI编程代理可靠性的有效途径，但仍需平衡安全性与任务完成度。
---

## 摘要
AI 编码智能体日益依赖执行工具来与代码库及外部工具进行交互。然而，任务成功并不等同于执行可靠。智能体仍可能修改无关文件、重写测试、发出不安全指令或忽略失败的验证，这促使我们需要为可靠执行建立行为护栏。我们提出了 AgentGuard，这是一个指令级的护栏框架，通过从编码智能体的异常轨迹中学习条件执行约束来实现。AgentGuard 不依赖人工指定的安全规则，而是自动提取重复出现的执行失败模式，将其泛化为指令级的行为约束，并将其组织为一种轻量级的护栏技能，仅动态激活与当前指令相关的规则。这种设计在实现行为引导的同时，最大限度地减少了对正常执行的不必要限制。我们使用从 382 个代码库任务中收集的 642 条记录在案的失败轨迹对 AgentGuard 进行了评估。护栏从覆盖 282 个任务的 461 条轨迹中学习，并在包含 100 个任务的不相交数据集上进行评估。我们以 Claude Haiku 4.5 作为底层编码智能体的 Claude Code 为基准，对比了原始智能体与集成 AgentGuard 后的智能体表现。实验结果表明，AgentGuard 将异常执行率从 69.0% 降低至 26.7%，并将任务成功完成率从 21.7% 提高至 35.0%。这些结果证明，从历史失败中学习执行护栏可以显著提高 AI 编码智能体的可靠性，同时也凸显了在安全性和任务完成度之间取得平衡这一持续存在的挑战。

## Abstract
AI coding agents increasingly rely on execution harnesses to interact with repositories and external tools. However, task success does not guarantee reliable execution. Agents may still modify unrelated files, rewrite tests, issue unsafe commands, or ignore failed validations, motivating behavioral guardrails for reliable execution. We present AgentGuard, an instruction-level guardrail framework that learns conditional execution constraints from anomalous trajectories of coding agents. Rather than relying on manually specified safety rules, AgentGuard automatically extracts recurring execution failure patterns, generalizes them into instruction-level behavioral constraints, and organizes them as a lightweight guardrail skill that dynamically activates only the rules relevant to the current instruction.   This design enables behavioral guidance while minimizing unnecessary restrictions on normal execution. We evaluate AgentGuard using 642 documented failure traces collected from real coding-agent executions across 382 repository tasks. Guardrails are learned from 461 traces covering 282 tasks and evaluated on a disjoint set of 100 tasks. Using Claude Code with Claude Haiku 4.5 as the underlying coding agent, we compare the baseline agent with the same agent augmented by AgentGuard. Experimental results show that AgentGuard reduces the Abnormal Execution Rate from 69.0% to 26.7% and increases the Successful Task Completion Rate from 21.7% to 35.0%. These results demonstrate that execution guardrails learned from historical failures can substantially improve the reliability of AI coding agents while highlighting the remaining challenge of balancing safety and task completion.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentGuard: Learning Execution Guardrails from Anomalous Coding-Agent Trajectories》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：AI 编码智能体（如 SWE-agent, OpenHands）在处理复杂软件工程任务时，虽然能完成目标，但其执行过程往往缺乏约束，容易出现误改无关文件、破坏测试逻辑、执行危险命令或忽略验证失败等“异常执行”行为。
*   **动机**：现有的安全机制多依赖人工编写的静态规则，难以维护且缺乏灵活性。研究者旨在构建一种能够从历史失败轨迹中自动学习、并能根据当前上下文动态激活的指令级行为护栏，以提升智能体的执行可靠性。

### 2. 方法论：AgentGuard 框架
*   **核心思想**：将异常执行轨迹转化为结构化的“行为发现（Findings）”，进而泛化为可重用的条件约束规则。
*   **关键流程**：
    1.  **轨迹表示**：将任务建模为 `(指令, 执行)` 对，执行过程记录为 `(响应, 动作, 结果)` 的三元组序列，保留完整的上下文依赖。
    2.  **证据提取**：从异常轨迹中提取“发生了什么、为什么异常、在哪里发生”，形成结构化发现。
    3.  **规则诱导与合并**：将发现转化为 `(When, DoNot, Unless, Instead, ApplyAt)` 格式的规则。通过重复删除、归纳（Subsumption）和冲突解决来精简规则集。
    4.  **动态路由**：将规则按功能（如项目修改、命令执行等）分组为路由条目。智能体在执行每一步前，仅加载与当前动作相关的子技能，避免上下文冗余。

### 3. 实验设计
*   **数据集**：使用 ABTest 数据集，包含 642 条真实编码智能体的异常执行轨迹（覆盖 382 个任务）。
*   **划分**：461 条轨迹（282 个任务）用于学习规则，100 个独立任务用于评估。
*   **基准对比**：对比了原始 Claude Code（基准）与集成 AgentGuard 的 Claude Code。
*   **评估指标**：异常执行率 (AER)、正确对抗步骤处理率 (CAR)、良性任务完成率 (CBR)、成功任务完成率 (Success) 和过度拒绝率 (ORR)。

### 4. 资源与算力
*   **算力说明**：论文未明确提及训练所用的 GPU 型号、数量或具体训练时长。
*   **执行环境**：评估阶段使用了 Docker 隔离环境（2 CPU, 8 GB 内存），Claude Code 2.1.19 配合 Claude Haiku 4.5 模型。

### 5. 实验数量与充分性
*   **规模**：共进行了 600 次 Docker 隔离执行（每种配置 300 次），以消除 LLM 的随机性影响。
*   **客观性**：采用了三名人类评审员进行独立评估，Fleiss' kappa 系数达到 0.87，且在存在分歧时进行了联合裁决，评估过程严谨且客观。
*   **充分性**：实验涵盖了多种异常场景，并进行了详细的消融分析（如路由条目效果分析），实验设计充分。

### 6. 主要结论与发现
*   **可靠性提升**：AgentGuard 将异常执行率从 69.0% 显著降低至 26.7%，任务成功率从 21.7% 提升至 35.0%。
*   **安全性与效能**：证明了从历史失败中学习护栏是有效的，且引入的额外执行开销极小（成本增加约 2.5%，时间增加约 3.9%）。
*   **主要挑战**：引入了 19.3% 的“过度拒绝率（Over-refusal）”，即智能体有时会过于保守，拒绝执行原本安全且合法的任务。

### 7. 优点
*   **自动化程度高**：无需人工编写规则，通过数据驱动自动提取模式。
*   **动态上下文感知**：通过路由机制仅加载相关规则，避免了长上下文带来的干扰。
*   **非侵入式**：无需微调模型或修改底层工具接口，易于部署。

### 8. 不足与局限
*   **过度拒绝风险**：这是目前最大的局限，护栏可能会误伤合法的操作，导致任务完成度受损。
*   **覆盖范围**：虽然减少了异常，但仍有 33% 的任务在集成 AgentGuard 后依然存在异常，说明规则库仍需进一步扩充。
*   **应用限制**：该方法高度依赖于高质量的异常轨迹数据集（ABTest），如果缺乏多样化的失败样本，学习效果会受限。

（完）
