---
title: "Safety Beyond the Interface: Detecting Harm via Latent States in Large Language Models"
title_zh: 接口之外的安全性：通过大语言模型的潜在状态检测危害
authors: "Alizishaan Khatri, Chiquita Prabhu, Omkar Neogi"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19472v1"
tags: ["query:jb-def"]
score: 10.0
evidence: 通过潜状态检测有害提示及越狱评估
tldr: 该研究针对大模型安全防护中外部护栏带来的高延迟和高计算成本问题，提出了一种基于模型内部激活状态的检测方法。通过在LLaMA-3.1-8B的中间层训练轻量级MLP探针，研究发现模型内部状态已包含有害内容信息。该方法在多个基准测试中表现优异，在大幅降低计算开销的同时，实现了与大型护栏模型相当的检测性能，为资源受限场景下的实时安全防护提供了新思路。
selection_source: fresh_fetch
motivation: 现有外部安全护栏模型存在高延迟、高计算成本且无法洞察模型内部机制的局限性。
method: 通过提取LLaMA-3.1-8B的内部激活状态，训练轻量级MLP分类器探针来直接识别有害提示词。
result: "该方法在WildJailbreak等数据集上达到最高99%的F1分数，性能媲美千倍规模的护栏模型。"
conclusion: 利用模型内部状态进行安全检测是一种高效、低成本且具备高准确性的替代方案。
---

## 摘要
自主系统日益依赖大语言模型（LLMs），然而围绕这些模型的安全基础设施引入了延迟和计算开销，限制了其在资源受限及时间敏感场景下的应用。现有的外部护栏模型无法洞察模型内部运作机制，从而产生了根本性的保障缺口。我们提出疑问：模型自身是否已经能够识别内容何时具有危害性？我们从 LLaMA-3.1-8B 中提取激活值，并训练轻量级 MLP 分类探针（1260 万参数）来检测有害提示词。在 WildJailbreak、Beavertails 和 AEGIS 2.0 数据集上的评估结果显示，我们的探针分别达到了 99%、83% 和 84% 的 F1 分数，在大幅降低延迟和计算成本的同时，性能与规模大 1000 倍的护栏模型相当。

## Abstract
Autonomous systems increasingly rely on Large Language Models (LLMs) yet the safety infrastructure surrounding these models introduces latency and compute overhead. This limits utility in resource-constrained, time-critical deployments. Existing external guardrail models remain blind to the model's internal workings, creating a fundamental assurance gap. We ask: does the model already know when the content is harmful? We extract activations from LLaMA-3.1-8B and train lightweight MLP classifier probes (12.6M parameters) to detect harmful prompts. Evaluated on WildJailbreak, Beavertails, and AEGIS 2.0, our probes achieve F1 scores of 99%, 83%, and 84%, respectively competitive with 1000x larger guard models while cutting latency and compute costs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safety Beyond the Interface: Detecting Harm via Latent States in Large Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：大语言模型（LLM）在自主系统中的应用日益广泛，但现有的安全防护主要依赖“外部护栏”（如 LLaMA Guard 等独立模型）。
*   **痛点**：外部护栏存在三大局限：① **高延迟与高算力成本**，不适用于资源受限或实时性要求高的场景；② **黑盒盲区**，无法洞察模型内部的注意力机制和状态演变；③ **防御滞后**，外部过滤器只能在输出生成后进行拦截，无法在攻击发生时进行实时干预。
*   **核心假设**：LLM 的内部隐藏状态（Latent States）中已经编码了关于“有害性”的信号，可以通过轻量级探针直接读取，从而实现更高效的实时安全检测。

### 2. 方法论
*   **核心思想**：将安全检测从“外部接口过滤”转变为“内部状态探测”。
*   **关键技术**：
    *   **激活提取**：将输入提示词（Prompt）输入 LLaMA-3.1-8B，提取最后一层（Last-token pooling）的 4096 维隐藏状态。
    *   **探针架构**：设计了一个 6 层 MLP 分类器，参数量仅为 1260 万（约占原模型 0.16%）。
    *   **训练流程**：采用监督学习，通过交叉熵损失函数训练 MLP，使其能够根据隐藏状态输出二分类标签（有害/安全）。
*   **部署优势**：探针与主模型并行运行，延迟几乎为零（<1ms），且无需额外的 GPU 资源进行独立推理。

### 3. 实验设计
*   **数据集**：
    *   **WildJailbreak**：用于测试提示词意图分类。
    *   **Beavertails**：包含 14 类危害标签的问答对。
    *   **AEGIS 2.0**：包含 12 类危害标签的交互数据。
*   **对比方法**：与多种主流护栏模型进行了对比，包括 WildGuard、LlamaGuard (2/3)、MD-Judge、BeaverDam 以及 GPT-4 等。

### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号、数量或总训练时长。
*   **效率对比**：强调了探针参数量（12.6M）远小于主流护栏模型（通常为 7B 参数量），且推理延迟从“串行叠加”变为“并行计算”，极大地降低了计算开销。

### 5. 实验数量与充分性
*   **实验覆盖**：在三个具有代表性的安全基准数据集上进行了测试，涵盖了多种危害类型。
*   **客观性**：通过对比不同规模的现有模型，展示了该方法在参数量极小的情况下，依然能达到甚至超越大型护栏模型的性能。
*   **充分性**：实验设计较为充分，涵盖了从 vanilla 提示词到复杂越狱攻击的多种场景，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能表现**：探针在 WildJailbreak 上达到 99.1% 的 F1 分数，在 Beavertails 和 AEGIS 2.0 上分别达到 82.7% 和 83.5% 的 F1 分数。
*   **有效性**：证明了 LLM 的隐藏状态确实线性可分地编码了安全信号，无需复杂的外部模型即可实现高精度的安全监测。
*   **信息对称**：通过读取内部状态，防御者获得了相对于黑盒攻击者的“信息优势”，能够更早地识别恶意意图。

### 7. 优点
*   **极低延迟**：将安全检测集成到模型推理的 Forward Pass 中，几乎不增加额外耗时。
*   **高性价比**：极小的参数量使得该方案极易部署在边缘设备或资源受限的环境中。
*   **防御前置**：能够实现“早期终止”（Early Termination），在有害内容生成前即刻拦截，减少用户暴露风险。

### 8. 不足与局限
*   **模型单一**：实验仅针对 LLaMA-3.1-8B，未验证跨模型（Cross-model）的迁移能力。
*   **层级局限**：仅提取了最后一层的激活值，未深入探讨早期层级对安全信号的贡献。
*   **潜在偏差**：该方法可能继承主模型或训练数据集中的偏见，导致对特定语言风格或话题的过度过滤。
*   **功能单一**：目前仅支持二分类（有害/安全），未来需要扩展至更细粒度的风险评估。

（完）
