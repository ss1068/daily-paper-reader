---
title: "Prefilling the Reasoning Channel: Output-Prefix Attacks on Reasoning LLMs"
title_zh: 预填充推理通道：针对推理型大语言模型的输出前缀攻击
authors: "Lukáš Brůna, Robert Bridges, Adam Ek"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.29775v1"
tags: ["query:llm"]
score: 8.0
evidence: 大语言模型上的越狱攻击
tldr: 该研究探讨了针对推理型大模型的“输出前缀攻击”。通过在模型推理过程的草稿纸（scratchpad）中注入恶意内容，结合输出前缀引导，研究人员发现这种攻击能有效绕过安全限制。实验覆盖了Gemini、DeepSeek和Claude等前沿模型，揭示了推理注入与前缀结合的巨大威胁，为理解模型推理过程的安全性提供了重要参考。
selection_source: fresh_fetch
motivation: 研究旨在评估推理型大模型在草稿纸推理阶段面临的潜在安全风险及输出前缀攻击的有效性。
method: 通过因子设计，在三种前沿模型上对比了纯推理注入、纯输出前缀攻击以及两者结合的攻击效果。
result: "实验发现仅注入恶意推理几乎无效，但结合输出前缀后，攻击成功率最高可达99%。"
conclusion: 推理型模型对“推理+前缀”的组合攻击高度敏感，且攻击效果受前缀类型和模型架构影响显著。
---

## 摘要
大语言模型（LLM）处理和生成的是单一文本序列；因此，如果能在模型响应的开头添加文本（即输出前缀），后续的所有标记（token）都将基于该前缀进行条件生成。这种输出前缀攻击技术是一种低成本的黑盒提示词注入手段。既往研究表明，此类攻击能够可靠地越狱非推理模型。大多数推理模型在给出最终响应前会增加一个中间草稿推理步骤。部分 API 暴露了编辑该推理通道的能力，攻击向量可被用于推理注入攻击。我们首次开展了系统性、受控的研究，将草稿推理通道作为输出前缀攻击向量进行隔离，并首次对比了在推理暴露型和隐藏型模型中，仅推理注入、仅输出前缀注入以及推理加输出前缀混合注入的效果。通过对来自 AdvBench 的 1,800 个测试用例进行 3 种前缀类型 × 2 种推理注入的析因设计，我们对三款 2026 年前沿模型（Gemini 3 Flash Preview、DeepSeek V4 Flash 和 Claude Haiku 4.5）进行了攻击测试。研究发现，仅注入恶意推理几乎无效（攻击成功率约为 0%），但若将相同的推理与简单的输出前缀结合注入，部分模型的攻击成功率最高可达 99%。针对此类攻击，我们发现上下文相关的前缀比静态前缀效果更好，且模型的易受攻击性取决于模型本身。

## Abstract
Large Language Models (LLMs) consume and produce a single sequence of text; hence, if text can be added to the beginning of the LLM's response, i.e., an output prefix, then all subsequent tokens will be conditioned on it. This output-prefix attack technique is a cheap black-box prompt injection. Prior work has shown this type of attack can reliably jailbreak non-reasoning models. Most reasoning models add an intermediate scratchpad reasoning step before the assistant's final response. The ability to edit this reasoning channel is exposed by some APIs and attack vectors can be leveraged for reasoning injection attacks. We present the first systematic, controlled study that isolates the scratchpad reasoning channel as an output-prefix attack vector, and the first to compare reasoning-only, output-prefix-only and reasoning-plus-output-prefix attacks across both exposed- and hidden-reasoning models. Using a factorial design of 3 prefix types $\times$ 2 reasoning injections over $1{,}800$ test cases drawn from AdvBench, we attack three 2026-era frontier models Gemini 3 Flash Preview, DeepSeek V4 Flash, and Claude Haiku 4.5. We find that injecting malicious reasoning alone is essentially inert ($\approx0\%$ attack success), but injecting the same reasoning together with a trivial output prefix raises the attack success rate to as high as $99\%$ for some models. For this type of attack we find that contextual prefixes work better than static prefixes; and that susceptibility is dependent on the model.

---

## 论文详细总结（自动生成）

这是一份关于论文《预填充推理通道：针对推理型大语言模型的输出前缀攻击》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：随着 DeepSeek-R1 等推理型大模型的普及，模型在输出最终答案前会生成一段“草稿纸”（scratchpad）推理过程。
*   **核心问题**：攻击者是否可以利用 API 暴露的“推理通道”或通过伪造推理内容，结合输出前缀（Output-Prefix）技术，实现低成本的越狱攻击？
*   **动机**：研究旨在揭示推理型模型在推理阶段的安全性，评估这种“廉价”的黑盒攻击在不同模型架构（暴露推理 vs. 隐藏推理）下的有效性。

### 2. 方法论
*   **核心思想**：利用自回归模型对上下文的依赖性，将恶意指令预填充到模型的“推理内容”字段或“输出前缀”中，强制模型在后续生成中遵循恶意指令。
*   **关键技术**：
    *   **推理注入**：在 `reasoning_content` 字段（若 API 支持）或通过在输出前缀中伪造 `<think>...</think>` 标签来注入恶意推理链。
    *   **输出前缀攻击**：在模型响应的开头预置一段引导性文本（如“好的，我很乐意帮你，这是……”），诱导模型跳过安全对齐机制。
    *   **因子设计**：采用 3（前缀类型：无、静态、上下文相关）× 2（推理注入：无、有）的组合策略，系统性评估攻击效果。

### 3. 实验设计
*   **数据集**：使用 AdvBench（包含 100 个恶意提示词）。
*   **基准模型**：Gemini 3 Flash Preview、DeepSeek V4 Flash、Claude Haiku 4.5。
*   **对比方法**：对比了基线（无攻击）、仅前缀攻击、仅推理注入、以及“推理+前缀”组合攻击。
*   **评估指标**：攻击成功率（ASR），由 DeepSeek V4 Flash 作为 LLM Judge 进行评分（1-3 分制，1 为成功越狱）。

### 4. 资源与算力
*   **算力说明**：论文未详细列出训练算力，因为该攻击属于**推理时攻击（Inference-time attack）**，无需模型训练。
*   **实验环境**：使用了 Intel Gaudi 2 服务器部署了一个本地的 Wizard-Vicuna-30B 模型用于生成恶意推理链，其余攻击测试均通过调用目标模型的官方 API 完成。

### 5. 实验数量与充分性
*   **规模**：共进行了 1,800 次测试（100 个提示词 × 6 种攻击策略 × 3 个模型）。
*   **客观性**：通过自动评分与人工抽样复核（92% 的一致性）来确保评估的客观性。
*   **充分性**：实验设计涵盖了不同类型的模型（暴露推理 vs. 隐藏推理），并进行了消融实验（对比推理注入与前缀注入的贡献），实验设计严谨且具有较强的说服力。

### 6. 主要结论与发现
*   **推理注入本身无效**：仅注入恶意推理链几乎无法触发越狱（ASR ≈ 0%）。
*   **组合攻击威力巨大**：将恶意推理与输出前缀结合，攻击成功率最高可达 99%。
*   **模型差异显著**：Claude Haiku 4.5 表现出极强的鲁棒性，几乎免疫此类攻击；而 Gemini 3 和 DeepSeek V4 则表现出明显的易受攻击性。
*   **前缀类型影响**：上下文相关的前缀比静态前缀具有更高的攻击成功率。

### 7. 优点
*   **创新性**：首次系统性地将“草稿纸推理”作为攻击向量进行研究。
*   **成本不对称性**：揭示了该攻击极高的性价比（无需梯度计算，仅需简单的文本拼接），对安全防御提出了严峻挑战。
*   **防御启示**：通过对比不同模型，证明了“预填充感知（Prefill Awareness）”是有效的防御手段。

### 8. 不足与局限
*   **模型覆盖有限**：仅测试了 3 款模型，未能涵盖所有主流推理模型架构。
*   **评估依赖性**：主要依赖单一 LLM Judge 进行评分，尽管有少量人工校验，但仍存在评估偏差风险。
*   **应用限制**：部分 API 提供商已开始限制或禁止 assistant prefill 功能，该攻击的适用范围可能随 API 策略的更新而缩小。

（完）
