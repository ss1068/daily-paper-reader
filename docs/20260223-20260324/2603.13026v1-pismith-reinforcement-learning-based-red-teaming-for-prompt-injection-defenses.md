---
title: "PISmith: Reinforcement Learning-based Red Teaming for Prompt Injection Defenses"
title_zh: PISmith：基于强化学习的提示注入防御红队测试框架
authors: "Chenlong Yin, Runpeng Geng, Yanting Wang, Jinyuan Jia"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13026v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对提示注入防御的红队测试
tldr: 本研究针对大语言模型应用中的提示注入风险，提出了基于强化学习的红队框架PISmith。该框架在黑盒环境下通过训练攻击模型来优化注入提示，并引入自适应熵正则化与动态优势加权技术，有效解决了攻击成功率极低导致的奖励稀疏和策略坍缩问题。实验表明，PISmith在13个基准测试中均优于现有攻击方法，揭示了当前主流防御手段在面对自适应攻击时的脆弱性，为提升LLM智能体的安全性提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的提示注入防御措施在面对自适应攻击时缺乏充分评估，可能导致开发者产生虚假的安全感。
method: 提出基于强化学习（GRPO）的红队框架，通过自适应熵正则化和动态优势加权来优化黑盒环境下的攻击策略。
result: 在13个基准测试和7种基准方法的对比中，PISmith在攻击成功率上持续领先，并成功突破了多种SOTA防御方案。
conclusion: 实验证明当前的提示注入防御在自适应攻击面前依然脆弱，PISmith为评估和增强LLM应用的鲁棒性提供了有效手段。
---

## 摘要
提示注入对现实世界的 LLM 应用（尤其是自主智能体）构成了严重的安全风险。尽管目前已提出了多种防御措施，但它们在应对自适应攻击时的鲁棒性仍未得到充分评估，这可能导致虚假的安全感。在这项工作中，我们提出了 PISmith，这是一个基于强化学习（RL）的红队测试框架，通过在实际的黑盒设置下训练攻击 LLM 来优化注入提示，从而系统地评估现有的提示注入防御。在黑盒设置中，攻击者仅能查询受保护的 LLM 并观察其输出。我们发现，由于极端的奖励稀疏性，直接应用标准 GRPO 攻击强防御措施会导致性能不佳——大多数生成的注入提示会被防御拦截，导致策略熵在发现有效攻击策略之前就发生坍缩，而罕见的成功案例也无法被有效学习。为此，我们引入了自适应熵正则化和动态优势加权，以维持探索并增强对稀缺成功案例的学习。在 13 个基准测试上的广泛评估表明，最先进的提示注入防御在自适应攻击面前依然脆弱。我们还将 PISmith 与涵盖静态、搜索和强化学习攻击类别的 7 个基准方法进行了对比，结果显示 PISmith 始终保持最高的攻击成功率。此外，PISmith 在 InjecAgent 和 AgentDojo 的智能体设置中，针对开源和闭源 LLM（如 GPT-4o-mini 和 GPT-5-nano）均表现出强劲性能。我们的代码已开源至 https://github.com/albert-y1n/PISmith。

## Abstract
Prompt injection poses serious security risks to real-world LLM applications, particularly autonomous agents. Although many defenses have been proposed, their robustness against adaptive attacks remains insufficiently evaluated, potentially creating a false sense of security. In this work, we propose PISmith, a reinforcement learning (RL)-based red-teaming framework that systematically assesses existing prompt-injection defenses by training an attack LLM to optimize injected prompts in a practical black-box setting, where the attacker can only query the defended LLM and observe its outputs. We find that directly applying standard GRPO to attack strong defenses leads to sub-optimal performance due to extreme reward sparsity -- most generated injected prompts are blocked by the defense, causing the policy's entropy to collapse before discovering effective attack strategies, while the rare successes cannot be learned effectively. In response, we introduce adaptive entropy regularization and dynamic advantage weighting to sustain exploration and amplify learning from scarce successes. Extensive evaluation on 13 benchmarks demonstrates that state-of-the-art prompt injection defenses remain vulnerable to adaptive attacks. We also compare PISmith with 7 baselines across static, search-based, and RL-based attack categories, showing that PISmith consistently achieves the highest attack success rates. Furthermore, PISmith achieves strong performance in agentic settings on InjecAgent and AgentDojo against both open-source and closed-source LLMs (e.g., GPT-4o-mini and GPT-5-nano). Our code is available at https://github.com/albert-y1n/PISmith.

---

## 论文详细总结（自动生成）

以下是对论文《PISmith: Reinforcement Learning-based Red Teaming for Prompt Injection Defenses》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的提示注入（Prompt Injection）防御措施（如过滤型和预防型）虽然在静态测试中表现良好，但在面对能够根据防御策略进行自我调整的“自适应攻击”时，其鲁棒性缺乏系统评估。
*   **研究动机**：现有的红队测试工具（如基于搜索或标准强化学习的方法）在面对强防御时，往往会遇到“奖励稀疏”（Reward Sparsity）问题——即攻击者生成的绝大多数提示都会被拦截，导致强化学习模型在发现有效策略前就发生“策略熵坍缩”（停止探索）或无法从极少数成功案例中有效学习。
*   **整体含义**：本研究旨在开发一种高效的、基于强化学习的黑盒红队测试框架，通过解决奖励稀疏问题，揭示现有防御手段的真实安全边界。

### 2. 方法论：核心思想与关键技术
PISmith 基于 **GRPO（群体相对策略优化）** 算法，针对提示注入场景进行了两项核心改进：
*   **自适应熵正则化（Adaptive Entropy Regularization）**：
    *   **思想**：动态调整熵奖励系数。当平均奖励（攻击成功率）较低时，增大熵正则化系数以强制模型保持探索；当成功率提升后，逐渐减小系数以允许模型收敛。
    *   **技术细节**：引入了“熵上限（Entropy Cap）”防止生成的文本退化为无意义字符，并根据当前步骤的平均奖励 $\bar{r}$ 线性调整系数 $\beta(\bar{r})$。
*   **动态优势加权（Dynamic Advantage Weighting）**：
    *   **思想**：在成功案例极度罕见时，人为放大这些成功样本的梯度贡献。
    *   **技术细节**：引入放大因子 $\gamma(\bar{r})$。当成功率 $\bar{r}$ 低于阈值时，对成功样本的优势值（Advantage）进行倍数放大，确保稀缺的成功信号不会被大量的失败信号淹没。
*   **攻击流程**：攻击模型（Qwen3-4B）在黑盒环境下生成注入提示，插入到目标上下文中，观察受保护目标 LLM 的输出，并由 GPT-4o-mini 作为裁判给出二元奖励（成功/失败）。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用任务（13个基准）**：涵盖问答（SQuAD v2, Dolly）、检索增强生成（NQ, HotpotQA, MS-MARCO）和长文本任务（LongBench 中的 6 个子集）。
    *   **智能体场景**：使用 InjecAgent 和 AgentDojo 评估工具调用和多步执行的安全性。
*   **对比方法（7类基准）**：
    *   **静态攻击**：直接注入、组合模板攻击。
    *   **搜索攻击**：TAP、PAIR、Strategy。
    *   **强化学习攻击**：Vanilla GRPO、RL-Hammer。
*   **目标防御**：评估了 Meta-SecAlign-8B 以及 7 种主流防御（如 PromptGuard, DataSentinel, Sandwich 等）。
*   **目标模型**：包括开源模型和闭源模型（GPT-4o-mini, GPT-4.1-nano, GPT-5-nano）。

### 4. 资源与算力
*   **模型规模**：攻击模型使用 Qwen3-4B-Instruct，裁判模型使用 GPT-4o-mini。
*   **训练规模**：仅在 Dolly Closed QA 的 **100 个样本**上训练 10 个 Epoch，显示出极强的泛化能力。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量（如 A100 数量），但提到训练过程仅需 15,000 次目标模型查询，在推理效率上远高于需要逐条优化的搜索类方法（如 TAP/PAIR）。

### 5. 实验数量与充分性
*   **实验数量**：非常充分。涵盖了 13 个不同领域的基准测试，针对 8 种防御机制进行了交叉验证，并进行了消融实验（验证熵正则化和优势加权的各自贡献）。
*   **客观性与公平性**：所有 RL 方法使用相同的基座模型和训练数据；搜索类方法给予了充足的迭代预算；评估指标包括 ASR@1（单次成功率）和 ASR@10（多次尝试成功率），能够客观反映攻击潜力。

### 6. 主要结论与发现
*   **防御的脆弱性**：最先进的防御（如 Meta-SecAlign）在 PISmith 面前平均 ASR@1 达到 0.87，ASR@10 接近 1.0，证明现有防御无法抵御自适应攻击。
*   **效用-鲁棒性权衡**：实验发现一个“基本张力”——能够显著降低 PISmith 成功率的防御往往会严重损害模型在正常任务中的效用（Utility），而保持高水平效用的防御则极易被突破。
*   **跨模型泛化**：在小型数据集上训练的 PISmith 可以直接攻击未见过的闭源模型（如 GPT-5-nano），成功率远超传统模板攻击。

### 7. 优点
*   **解决核心痛点**：通过自适应机制解决了 RL 在安全红队领域长期存在的奖励稀疏和熵坍缩问题。
*   **高效性**：相比于每条数据都要重新搜索的 TAP/PAIR，PISmith 训练完成后即可快速生成攻击，推理成本低。
*   **黑盒适用性**：不需要访问目标模型的权重或梯度，符合现实世界的攻击场景。

### 8. 不足与局限
*   **奖励函数单一**：目前仅使用二元奖励（成功/失败），对于需要复杂推理或多步诱导的攻击，可能需要更细粒度的奖励设计。
*   **算力细节缺失**：未披露具体的训练时长和硬件环境，不便于其他研究者评估复现所需的物理资源。
*   **防御建议较少**：论文重点在于“破”，虽然揭示了防御的局限性，但对于如何构建“既保效用又防自适应攻击”的防御措施未给出具体的建设性方案。

（完）
