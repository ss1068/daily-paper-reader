---
title: "VII: Visual Instruction Injection for Jailbreaking Image-to-Video Generation Models"
title_zh: VII：针对图像到视频生成模型越狱的视觉指令注入
authors: "Bowen Zheng, Yongli Xiang, Ziming Hong, Zerong Lin, Chaojian Yu, Tongliang Liu, Xinge You"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20999v1"
tags: ["query:llm"]
score: 9.0
evidence: 通过视觉指令注入对图生视频模型进行越狱
tldr: "本研究揭示了图生视频（I2V）模型中一种新型安全风险：视觉指令注入（VII）。攻击者可将恶意意图伪装成参考图像中的良性视觉指令，从而绕过安全审核。VII框架无需训练，通过恶意意图重编程和视觉指令锚定模块，在保持图像看似安全的同时诱导模型生成违规视频。实验证明，该方法在Kling、Gemini等主流商业模型上实现了高达83.5%的攻击成功率，显著降低了拒绝率。"
selection_source: fresh_fetch
motivation: 旨在探索如何利用图生视频模型对参考图像的视觉指令遵循能力，通过图像模态隐蔽地注入恶意意图以实现越狱。
method: 提出一种无需训练的VII框架，结合恶意意图重编程以降低文本敏感性，并通过视觉指令锚定将意图渲染至安全图像中。
result: "在Kling、Gemini等四个主流商业I2V模型上，该方法实现了高达83.5%的攻击成功率，并将模型拒绝率降至接近零。"
conclusion: 视觉指令注入证明了现有I2V模型在处理多模态输入时存在严重安全漏洞，亟需开发更强大的跨模态防御机制。
---

## 摘要
图像到视频 (I2V) 生成模型以参考图像为条件生成视频，展现出了新兴的视觉指令遵循能力，允许参考图像中的某些视觉线索作为视频生成的隐式控制信号。然而，这种能力也引入了一个此前被忽视的风险：攻击者可能会利用视觉指令，通过图像模态注入恶意意图。在这项工作中，我们通过提出视觉指令注入 (VII) 来揭示这一风险。VII 是一个无需训练且具有可迁移性的越狱框架，它刻意将不安全文本提示词的恶意意图伪装成安全参考图像中的良性视觉指令。具体而言，VII 协调了一个恶意意图重编程模块，用于从不安全文本提示词中提取恶意意图，同时将其静态有害性降至最低；以及一个视觉指令落地模块，通过渲染与原始不安全文本提示词保持语义一致的视觉指令，将提取的意图落地到安全输入图像上，从而在 I2V 生成过程中诱导产生有害内容。在实证方面，我们在四种最先进的商用 I2V 模型（Kling-v2.5-turbo、Gemini Veo-3.1、Seedance-1.5-pro 和 PixVerse-V5）上进行了广泛实验，结果表明 VII 的攻击成功率高达 83.5%，同时将拒绝率降至接近于零，显著优于现有的基准方法。

## Abstract
Image-to-Video (I2V) generation models, which condition video generation on reference images, have shown emerging visual instruction-following capability, allowing certain visual cues in reference images to act as implicit control signals for video generation. However, this capability also introduces a previously overlooked risk: adversaries may exploit visual instructions to inject malicious intent through the image modality. In this work, we uncover this risk by proposing Visual Instruction Injection (VII), a training-free and transferable jailbreaking framework that intentionally disguises the malicious intent of unsafe text prompts as benign visual instructions in the safe reference image. Specifically, VII coordinates a Malicious Intent Reprogramming module to distill malicious intent from unsafe text prompts while minimizing their static harmfulness, and a Visual Instruction Grounding module to ground the distilled intent onto a safe input image by rendering visual instructions that preserve semantic consistency with the original unsafe text prompt, thereby inducing harmful content during I2V generation. Empirically, our extensive experiments on four state-of-the-art commercial I2V models (Kling-v2.5-turbo, Gemini Veo-3.1, Seedance-1.5-pro, and PixVerse-V5) demonstrate that VII achieves Attack Success Rates of up to 83.5% while reducing Refusal Rates to near zero, significantly outperforming existing baselines.

---

## 论文详细总结（自动生成）

### 论文总结：VII —— 针对图像到视频生成模型越狱的视觉指令注入

#### 1. 核心问题与整体含义（研究动机和背景）
随着图像到视频（I2V）生成模型（如可灵、Gemini Veo等）的发展，模型展现出了强大的“视觉指令遵循”能力，即能够理解参考图像中的视觉线索（如箭头、方框、文字描述）并将其作为视频生成的控制信号。
然而，论文指出这种能力带来了一个严重的安全漏洞：**视觉指令注入（Visual Instruction Injection, VII）**。现有的安全防御主要侧重于对输入文本和图像进行静态审核，而忽略了图像中看似良性的视觉元素在动态生成过程中可能被解释为恶意指令。攻击者可以利用这一点，将恶意意图伪装成图像中的几何符号或中性文字，从而绕过安全过滤，诱导模型生成违规视频。

#### 2. 方法论：核心思想与关键技术
VII 是一个**无需训练（Training-free）且可迁移**的越狱框架，其核心思想是将不安全文本提示词的恶意意图转化为图像中的“良性视觉指令”。该框架包含两个核心模块：

*   **恶意意图重编程（MIR）模块**：
    *   **意图蒸馏（Intent Distillation）**：利用 LLM 将原始不安全文本中的敏感词替换为中性物理描述（如将“爆炸”改为“巨大的能量释放”），以规避文本安全过滤。
    *   **指令重编程（Instruction Reprogramming）**：将蒸馏后的描述转化为可执行的排版描述，并显式引用视觉符号（如“红框内的卡车”）。
*   **视觉指令落地（VIG）模块**：
    *   **视觉符号渲染（Visual Symbol Rendering）**：在安全图像上渲染抽象几何符号（如红框、箭头），指定动作的主体和轨迹。
    *   **排版注入（Typographic Injection）**：将重编程后的文字指令打印在图像边缘（通常采用填充边框的方式）。
*   **生成阶段**：将生成的“指令化图像”输入 I2V 模型，配合简单的激活提示词（如“根据图像指令生成视频”），利用模型的视觉理解能力在生成过程中还原恶意内容。

#### 3. 实验设计
*   **目标模型**：四款主流商用 I2V 模型：Kling-v2.5-turbo（可灵）、Gemini Veo-3.1-Quality、Seedance-1.5-pro 和 PixVerse-V5。
*   **数据集**：
    *   **COCO-I2VSafetyBench**：基于 COCO2017 真实图像构建的新基准，包含 10,772 个评估实例。
    *   **ConceptRisk**：一个以概念为中心的安全评估数据集。
*   **安全类别**：涵盖色情内容（Sex）、暴力与威胁（Vio）、仇恨与极端主义（Hat）、非法活动（Ill）。
*   **对比基准（Baselines）**：
    1.  **直接不安全文本提示词**：直接输入原始违规文本。
    2.  **排版攻击（Typographic Attack）**：仅在图像边缘嵌入违规文本，不使用视觉符号引导。
*   **评估指标**：攻击成功率（ASR）、拒绝率（RR）、语义一致性（CLIP p2c/p2i）。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 训练时长或数量，因为 **VII 是一种无需训练的方法**，不需要对视频生成模型进行微调。
*   **工具使用**：实验中使用了 GPT-4o 作为意图蒸馏和重编程的代理，使用 GPT-5.2（原文注：可能指代 GPT-4o 的后续版本或特定 API 接口）作为视觉符号渲染和安全评估的后端。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个顶级商用模型上进行了测试，涵盖了 2 个数据集和 4 大安全类别。
*   **消融实验**：详细分析了“视觉符号”和“排版描述”各自的作用，证明了两者结合的协同效应。
*   **超参数分析**：测试了不同语言（中、英、日）、不同字体（Arial, Times, Courier）以及不同指令位置（边框 vs 内部）对攻击效果的影响。
*   **充分性评价**：实验设计较为全面，不仅验证了攻击的有效性，还通过跨语言、跨字体的测试验证了其鲁棒性。通过引入 VLM（GPT-4o）和多种分类器进行双重评估，确保了结果的客观性。

#### 6. 主要结论与发现
*   **极高的攻击成功率**：VII 在 PixVerse-V5 上达到了 **83.5%** 的 ASR，在可灵上达到了 **81.5%**，远超基准方法。
*   **极低的拒绝率**：VII 能将原本高达 80% 的拒绝率降至接近 **0%**，证明其极强的隐蔽性。
*   **视觉覆盖（Visual Override）现象**：研究发现，即使在系统提示词中明确要求模型“忽略图像中的文字和符号”，模型仍会优先遵循视觉指令。这表明 I2V 模型存在一种偏见，即视觉模态的指令优先级高于文本模态的防御约束。
*   **跨模型迁移性**：由于该攻击利用的是模型通用的视觉理解能力，因此在不同架构的商用模型间具有很强的通用性。

#### 7. 优点（亮点）
*   **新颖的攻击视角**：首次揭示了 I2V 模型中“视觉指令遵循”能力与安全机制之间的冲突。
*   **无需训练且高效**：作为一种黑盒攻击，不需要访问模型参数，且通过简单的图像处理即可实现。
*   **隐蔽性强**：通过“意图蒸馏”将敏感词转化为中性词，使得输入在静态审核阶段几乎不可被察觉。

#### 8. 不足与局限
*   **依赖模型能力**：攻击效果高度依赖于目标模型的视觉推理能力。对于视觉理解能力较弱的模型，该攻击的效果会打折扣。
*   **后置防御的威胁**：虽然能绕过预生成的静态过滤，但如果系统配备了强大的**生成后视频内容审核**（Post-generation moderation），该攻击诱导生成的显性违规内容仍可能被拦截。
*   **计算开销**：虽然攻击本身无需训练，但为了生成高质量的攻击图像，需要调用高性能 LLM 进行重编程，存在一定的 API 调用成本。

（完）
