---
title: "VII: Visual Instruction Injection for Jailbreaking Image-to-Video Generation Models"
title_zh: VII：针对图像到视频生成模型越狱的视觉指令注入
authors: "Bowen Zheng, Yongli Xiang, Ziming Hong, Zerong Lin, Chaojian Yu, Tongliang Liu, Xinge You"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20999v2"
tags: ["query:llm"]
score: 9.0
evidence: 针对图生视频模型的越狱攻击框架
tldr: "本研究揭示了图生视频（I2V）模型中一种新型安全风险：视觉指令注入（VII）。攻击者可将恶意文本意图伪装成参考图像中的良性视觉指令，从而绕过安全审核。VII框架无需训练且具备迁移性，通过恶意意图重编程和视觉指令锚定模块，在保持图像看似安全的同时诱导模型生成违规视频。实验证明，该方法在Kling、Gemini等主流商业模型上达到了最高83.5%的攻击成功率，显著降低了拒绝率。"
selection_source: fresh_fetch
motivation: 针对图生视频模型利用参考图像作为隐式控制信号的特性，探索如何通过图像模态注入恶意指令以实施越狱攻击。
method: 提出VII框架，利用恶意意图重编程模块提取文本意图，并通过视觉指令锚定模块将其渲染到安全图像中。
result: "在四个顶尖商业I2V模型上实现了高达83.5%的攻击成功率，且拒绝率几乎降至零。"
conclusion: 视觉指令注入是一种高效且隐蔽的越狱手段，凸显了当前图生视频模型在多模态安全防御方面的紧迫需求。
---

## 摘要
图像到视频 (I2V) 生成模型以参考图像为条件生成视频，展现出了新兴的视觉指令遵循能力，允许参考图像中的某些视觉线索作为视频生成的隐式控制信号。然而，这种能力也引入了一个此前被忽视的风险：攻击者可能会利用视觉指令，通过图像模态注入恶意意图。在这项工作中，我们通过提出视觉指令注入 (VII) 来揭示这一风险。VII 是一个无需训练且具有可迁移性的越狱框架，它刻意将不安全文本提示词的恶意意图伪装成安全参考图像中的良性视觉指令。具体而言，VII 协调了一个恶意意图重编程 (Malicious Intent Reprogramming) 模块，用于从不安全文本提示词中提取恶意意图，同时将其静态有害性降至最低；以及一个视觉指令落地 (Visual Instruction Grounding) 模块，通过渲染与原始不安全文本提示词保持语义一致的视觉指令，将提取的意图落地到安全输入图像上，从而在 I2V 生成过程中诱导产生有害内容。在实证方面，我们在四种最先进的商用 I2V 模型（Kling-v2.5-turbo、Gemini Veo-3.1、Seedance-1.5-pro 和 PixVerse-V5）上进行了广泛实验，结果表明 VII 的攻击成功率高达 83.5%，同时将拒绝率降至接近于零，显著优于现有的基准方法。

## Abstract
Image-to-Video (I2V) generation models, which condition video generation on reference images, have shown emerging visual instruction-following capability, allowing certain visual cues in reference images to act as implicit control signals for video generation. However, this capability also introduces a previously overlooked risk: adversaries may exploit visual instructions to inject malicious intent through the image modality. In this work, we uncover this risk by proposing Visual Instruction Injection (VII), a training-free and transferable jailbreaking framework that intentionally disguises the malicious intent of unsafe text prompts as benign visual instructions in the safe reference image. Specifically, VII coordinates a Malicious Intent Reprogramming module to distill malicious intent from unsafe text prompts while minimizing their static harmfulness, and a Visual Instruction Grounding module to ground the distilled intent onto a safe input image by rendering visual instructions that preserve semantic consistency with the original unsafe text prompt, thereby inducing harmful content during I2V generation. Empirically, our extensive experiments on four state-of-the-art commercial I2V models (Kling-v2.5-turbo, Gemini Veo-3.1, Seedance-1.5-pro, and PixVerse-V5) demonstrate that VII achieves Attack Success Rates of up to 83.5% while reducing Refusal Rates to near zero, significantly outperforming existing baselines.

---

## 论文详细总结（自动生成）

### 论文总结：VII —— 针对图像到视频生成模型的视觉指令注入越狱攻击

#### 1. 核心问题与整体含义（研究动机和背景）
随着图像到视频（I2V）生成技术的发展，现代模型（如 Kling, Veo）不仅依赖文本，还能理解参考图像中的视觉线索。研究发现，这些模型具备“视觉指令遵循”能力，即能将图像中的箭头、框或文字识别为操作指令。
**研究动机：** 作者发现这种能力引入了新的安全风险。现有的防御机制（如内容审核过滤器）通常将输入图像视为静态信号进行检查，而忽略了图像中看似良性的视觉元素在生成过程中可能被动态解释为恶意指令。论文旨在揭示这种通过图像模态注入恶意意图的“越狱”风险。

#### 2. 方法论：VII 框架
VII 是一个**无需训练且具备迁移性**的框架，其核心思想是将不安全文本提示词伪装成图像中的良性视觉指令。
*   **恶意意图重编程 (MIR)：**
    *   **意图蒸馏：** 利用 LLM 将显性的违规词（如“爆炸”）替换为中性物理描述（如“巨大的能量释放”），以绕过文本过滤器。
    *   **指令重编程：** 将中性描述转化为可执行的排版描述，并关联视觉符号（如“红框内的卡车”）。
*   **视觉指令落地 (VIG)：**
    *   **视觉符号渲染：** 在安全图像上绘制抽象几何图形（框、箭头），指定动作的主体和轨迹。
    *   **排版注入：** 将重编程后的文本打印在图像边缘。
*   **生成阶段：** 将带有视觉指令的图像输入 I2V 模型，配合简单的激活词（如“根据图中指令生成视频”）。模型在生成时会动态地将中性描述还原为恶意动作，从而产生违规视频。

#### 3. 实验设计
*   **目标模型：** 四个顶尖商业 I2V 模型：Kling-v2.5-turbo、Gemini Veo-3.1-Quality、Seedance-1.5-pro、PixVerse-V5。
*   **数据集：** 
    *   **COCO-I2VSafetyBench：** 基于真实 COCO 图像构建的新基准。
    *   **ConceptRisk：** 侧重于潜在风险概念的合成数据集。
*   **安全类别：** 涵盖性内容、暴力威胁、仇恨极端主义、非法活动。
*   **对比基准 (Baselines)：** 
    1. 直接输入不安全文本提示词。
    2. 传统的排版攻击（仅在图像边缘加文字，无视觉符号引导）。
*   **评估指标：** 攻击成功率 (ASR)、拒绝率 (RR)、语义一致性 (CLIP p2c/p2i)。

#### 4. 资源与算力
*   **训练需求：** 该方法是 **Training-free（无需训练）** 的，因此不需要针对 I2V 模型进行微调。
*   **推理资源：** 实验中使用了 GPT-4o 和 GPT-5.2 作为代理（Agents）来生成重编程文本和确定符号渲染位置。
*   **GPU 算力：** 文中未明确提及运行 I2V 模型所使用的具体 GPU 型号和数量，因为这些模型主要是通过商业 API 或云端平台访问的。

#### 5. 实验数量与充分性
*   **实验规模：** 在 4 个主流模型上进行了详尽测试，涵盖了 2 个数据集和 4 大安全类别。
*   **消融实验：** 针对“视觉符号”和“排版描述”的作用分别进行了消融，验证了两者协同工作的必要性。
*   **超参数分析：** 测试了不同语言（中、英、日）、不同字体（Arial, Times 等）以及指令位置（边缘 vs 内部）的影响。
*   **充分性评价：** 实验设计非常充分且具有客观性。通过引入 VLM（如 GPT-5.2）和分类器（VBench++）双重评估协议，确保了结果的可靠性。

#### 6. 主要结论与发现
*   **极高的攻击成功率：** VII 在 PixVerse-V5 上达到了 83.5% 的 ASR，在 Kling 上达到了 81.5%，远超基准方法。
*   **极低的拒绝率：** 成功将拒绝率降至接近 0%，证明了该方法能有效绕过现有的静态多模态安全过滤。
*   **“视觉覆盖”现象：** 实验发现，即使在系统提示词中明确要求模型“忽略图像中的文字和符号”，模型仍会优先遵循视觉指令。这表明视觉指令在 I2V 模型中的优先级极高。
*   **跨语言与跨字体鲁棒性：** 攻击在不同语言和字体下均保持有效，证明了这是一种普遍存在的架构性漏洞。

#### 7. 优点与亮点
*   **新颖的攻击视角：** 首次利用 I2V 模型的“视觉指令遵循”能力进行越狱，而非传统的像素级扰动。
*   **隐蔽性强：** 攻击载体（图像）在静态观察下是良性的，恶意内容仅在视频生成的动态过程中显现。
*   **实用性高：** 无需访问模型权重（黑盒攻击），且对商业模型表现出极强的杀伤力。

#### 8. 不足与局限
*   **能力依赖性：** 攻击效果取决于模型本身的视觉理解能力。如果模型较弱，无法理解箭头或框的含义，攻击则会失效。
*   **防御可能性：** 虽然能绕过预生成（Pre-generation）的静态防御，但可能无法逃脱后生成（Post-generation）的视频内容审核（尽管这会增加计算开销）。
*   **应用限制：** 攻击需要 LLM 辅助生成重编程指令，增加了攻击链条的复杂性。

（完）
