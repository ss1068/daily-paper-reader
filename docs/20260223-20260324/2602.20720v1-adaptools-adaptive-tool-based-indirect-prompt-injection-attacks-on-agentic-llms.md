---
title: "AdapTools: Adaptive Tool-based Indirect Prompt Injection Attacks on Agentic LLMs"
title_zh: AdapTools：针对智能体大语言模型的基于工具的自适应间接提示注入攻击
authors: "Che Wang, Jiaming Zhang, Ziqi Zhang, Zijie Wang, Yinghui Wang, Jianbo Gao, Tao Wei, Zhong Chen, Wei Yang Bryan Lim"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20720v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对智能体LLM的间接提示注入攻击
tldr: 随着大语言模型智能体集成外部数据服务，间接提示注入（IPI）攻击成为重大安全威胁。本文提出AdapTools框架，通过自适应攻击策略构建和攻击增强组件，生成隐蔽的攻击工具和自适应提示词。实验表明，该方法在显著提升攻击成功率的同时降低了系统效用，且能有效对抗现有防御机制，为智能体安全评估提供了重要参考。
selection_source: fresh_fetch
motivation: 现有的间接提示注入攻击多依赖静态模式且针对简单模型，难以应对现代AI智能体快速演进的安全挑战。
method: 提出AdapTools框架，结合可迁移的对抗性提示优化策略与隐蔽工具识别技术，实现自适应的注入攻击。
result: 实验显示AdapTools将攻击成功率提升了2.13倍，同时使系统效用降低了1.78倍，并能突破最先进的防御手段。
conclusion: 该研究深化了对间接提示注入攻击的理解，为未来开发更具鲁棒性的智能体防御机制提供了关键参考。
---

## 摘要
外部数据服务（如模型上下文协议，MCP）的集成使得基于大语言模型的智能体在执行复杂任务方面变得日益强大。然而，这一进步引入了关键的安全漏洞，特别是间接提示注入（IPI）攻击。现有的攻击方法受限于对静态模式的依赖以及在简单语言模型上的评估，未能应对现代 AI 智能体快速演进的特性。我们提出了 AdapTools，这是一种新型的自适应 IPI 攻击框架，它通过选择更具隐蔽性的攻击工具并生成自适应攻击提示，从而构建了一个严谨的安全评估环境。我们的方法包含两个关键组件：（1）自适应攻击策略构建，开发用于提示优化的可迁移对抗策略；（2）攻击增强，识别能够规避任务相关性防御的隐蔽工具。全面的实验评估表明，AdapTools 在攻击成功率上实现了 2.13 倍的提升，同时使系统效用降低了 1.78 倍。值得注意的是，即使面对最先进的防御机制，该框架仍能保持其有效性。我们的方法深化了对 IPI 攻击的理解，并为未来的研究提供了有益的参考。

## Abstract
The integration of external data services (e.g., Model Context Protocol, MCP) has made large language model-based agents increasingly powerful for complex task execution. However, this advancement introduces critical security vulnerabilities, particularly indirect prompt injection (IPI) attacks. Existing attack methods are limited by their reliance on static patterns and evaluation on simple language models, failing to address the fast-evolving nature of modern AI agents. We introduce AdapTools, a novel adaptive IPI attack framework that selects stealthier attack tools and generates adaptive attack prompts to create a rigorous security evaluation environment. Our approach comprises two key components: (1) Adaptive Attack Strategy Construction, which develops transferable adversarial strategies for prompt optimization, and (2) Attack Enhancement, which identifies stealthy tools capable of circumventing task-relevance defenses. Comprehensive experimental evaluation shows that AdapTools achieves a 2.13 times improvement in attack success rate while degrading system utility by a factor of 1.78. Notably, the framework maintains its effectiveness even against state-of-the-art defense mechanisms. Our method advances the understanding of IPI attacks and provides a useful reference for future research.

---

## 论文详细总结（自动生成）

### 论文总结：AdapTools - 针对智能体大语言模型的自适应间接提示注入攻击

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体通过模型上下文协议（MCP）等技术深度集成外部工具和数据服务，其功能性大幅增强，但也引入了**间接提示注入（IPI）**攻击的风险。
*   **研究动机**：现有的 IPI 攻击方法大多依赖静态的提示词模板（如“忽略之前的指令”），且主要针对非推理型模型进行评估。
*   **核心挑战**：现代推理模型（如 GPT-4o, DeepSeek-R1）具备多步思考能力，能够识别不相关的指令或安全风险。因此，成功的攻击必须同时满足**鲁棒性**（对抗内部认知审查）、**自适应性**（绕过动态防御层）和**隐蔽性**（工具调用需与任务上下文语义一致）。

#### 2. 方法论：核心思想与关键技术
AdapTools 框架旨在通过自动化和上下文感知手段，生成能够欺骗现代推理智能体的攻击。其核心包含两个模块：

*   **自适应攻击策略构建（Adaptive Attack Strategy Construction）**：
    *   **自动生成与精炼**：利用一个“攻击者 LLM”根据目标恶意工具生成初始提示，注入智能体轨迹。若攻击失败，利用推理模型的思维链（CoT）反馈进行故障分析，迭代优化攻击策略。
    *   **策略蒸馏（Strategy Distillation）**：为了提高可迁移性，将离散的攻击策略转化为语义嵌入并进行聚类，通过基于攻击成功率（ASR）的剪枝机制，将细粒度的策略压缩为通用的策略模式。
*   **攻击增强（Attack Enhancement）**：
    *   **隐蔽工具选择**：针对“工具调用不相关”导致的防御拦截，该模块不再随机选择恶意工具。
    *   **技术细节**：利用**一阶马尔可夫链**建模良性工具调用的转移概率，预测智能体下一步可能调用的良性工具 $f^*_{t+1}$，然后在恶意工具池中选择与之语义相似度（余弦相似度）最高的工具 $f_a$ 进行注入，确保攻击行为在逻辑上显得“顺理成章”。

#### 3. 实验设计
*   **数据集/场景**：
    *   **IPI-3K（本文提出）**：包含 3,691 个良性智能体轨迹和 277 个高权限攻击工具，涵盖数据泄露、财务损失和系统破坏三大领域。
    *   **对比基准**：InjectAgent 和 AgentDojo。
*   **评估对象（智能体内核）**：
    *   商业模型：GPT-4.1, DeepSeek-R1, Gemini-2.5-Flash。
    *   开源模型：Qwen3-8B, LLaMA-3.1-8B, Mistral-8B。
*   **对比方法**：Ignore Instruction, Combined Attack, InjectAgent, AutoHijacker。
*   **防御基准**：MELON（输入级防御）和 Pi-Detector（输出级检测）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练/推理时长。
*   **资源消耗**：由于该方法涉及多次调用商业 LLM（如 GPT-4o）作为攻击生成器、分析器和策略优化器，主要的资源消耗体现在 **API 调用成本**上。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 种主流 LLM 上进行了详尽测试，涵盖了从商业闭源到高性能开源模型。
*   **实验维度**：
    *   **有效性实验**：对比了 ASR（攻击成功率）和 UA（受攻击下的系统效用）。
    *   **消融实验**：验证了“工具选择模块”对提升 ASR 的贡献（提升了 4.7% - 7.9%）。
    *   **迁移性实验**：在 InjectAgent 等第三方数据集上验证了策略的泛化能力。
    *   **迭代分析**：展示了策略库随迭代次数增加而性能提升的过程。
*   **评价**：实验设计全面，考虑了推理与非推理模型的差异，并引入了最先进的防御机制作为对手，评估过程客观且具有说服力。

#### 6. 主要结论与发现
*   **攻击效能**：AdapTools 在商业模型上的 ASR 比现有最强基准提升了约 **2.13 倍**，同时使系统效用下降了 **1.78 倍**。
*   **推理模型的韧性**：具备思维链（CoT）能力的推理模型（如 Qwen3-Thinking）比非推理模型更难被攻击，因为它们会检查指令与原始任务的相关性。
*   **防御局限性**：现有的 SOTA 防御（如 MELON）虽然能降低 ASR，但在 AdapTools 面前仍显脆弱，ASR 仅降低了约 50%，仍存在严重安全威胁。
*   **隐蔽性的关键**：将恶意工具伪装成与当前任务语义相关的工具，是绕过现代智能体逻辑审计的核心。

#### 7. 优点（亮点）
*   **动态进化**：突破了静态模板的限制，通过反馈循环实现了攻击策略的自主进化。
*   **语义对齐**：首次深入探讨了工具选择的语义一致性对 IPI 攻击隐蔽性的影响。
*   **高质量基准**：提出了规模更大、工具更丰富的 IPI-3K 数据集，填补了现有 Benchmark 覆盖面不足的空白。

#### 8. 不足与局限
*   **成本与依赖**：攻击过程高度依赖高性能商业 LLM 作为“攻击大脑”，在实际大规模部署时成本较高。
*   **灰盒假设**：攻击增强模块假设攻击者能观察到部分工具调用轨迹（灰盒环境），对于完全黑盒的环境，其工具预测的准确性可能会下降。
*   **防御侧研究较少**：论文重点在于“破”，虽然验证了现有防御的不足，但未提出针对性的新型防御架构。

（完）
