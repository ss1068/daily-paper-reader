---
title: Multi-Paradigm Collaborative Adversarial Attack Against Multi-Modal Large Language Models
title_zh: 针对多模态大语言模型的多范式协作对抗攻击
authors: "Yuanbo Li, Tianyang Xu, Cong Hu, Tao Zhou, Xiao-Jun Wu, Josef Kittler"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04846v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对多模态大模型的协作对抗攻击
tldr: 随着多模态大模型（MLLM）的快速发展，其对抗攻击的迁移性漏洞日益凸显。现有攻击方法多依赖单一学习范式的代理模型，限制了特征表示的丰富性。本文提出MPCAttack框架，通过多范式协作优化（MPCO）策略，整合图像与文本的语义表示，利用对比匹配平衡不同范式的重要性，从而扩大搜索空间并增强对抗样本的迁移性。实验证明该方法在多种开源和闭源模型上均表现优异。
selection_source: fresh_fetch
motivation: 现有针对多模态大模型的对抗攻击受限于单一学习范式，导致特征表示单一且攻击迁移性不足。
method: 提出MPCAttack框架，通过多范式协作优化策略整合视觉与语言语义，利用对比匹配平衡不同范式特征并指导全局扰动优化。
result: 在多个基准测试中，MPCAttack在针对开源和闭源多模态大模型的有向及无向攻击中均显著优于现有最先进方法。
conclusion: 该研究证明了多范式协作能有效缓解表示偏差，显著提升了对抗样本在多模态大模型间的迁移性和攻击性能。
---

## 摘要
多模态大语言模型（MLLMs）的快速发展显著推动了下游应用。然而，这一进展也暴露了严重的迁移性对抗脆弱性。通常，现有的针对 MLLMs 的对抗攻击主要依赖于在单一学习范式下训练的替代模型，并在各自的特征空间中进行独立优化。这种简单的设置自然限制了特征表示的丰富性，从而限制了搜索空间并阻碍了对抗扰动的多样性。为此，我们提出了一种新颖的多范式协作攻击（MPCAttack）框架，以提高针对 MLLMs 的对抗样本的迁移性。原则上，MPCAttack 聚合了来自视觉图像和语言文本的语义表示，通过多范式协作优化（MPCO）策略在聚合特征上促进联合对抗优化。通过对多范式特征进行对比匹配，MPCO 自适应地平衡了不同范式表示的重要性，并引导全局扰动优化，从而有效缓解了表示偏差。在多个基准测试上的广泛实验结果证明了 MPCAttack 的优越性，表明我们的方案在针对开源和闭源 MLLMs 的有目标和无目标攻击中均一致优于现有最先进的方法。代码已发布在 https://github.com/LiYuanBoJNU/MPCAttack。

## Abstract
The rapid progress of Multi-Modal Large Language Models (MLLMs) has significantly advanced downstream applications. However, this progress also exposes serious transferable adversarial vulnerabilities. In general, existing adversarial attacks against MLLMs typically rely on surrogate models trained within a single learning paradigm and perform independent optimisation in their respective feature spaces. This straightforward setting naturally restricts the richness of feature representations, delivering limits on the search space and thus impeding the diversity of adversarial perturbations. To address this, we propose a novel Multi-Paradigm Collaborative Attack (MPCAttack) framework to boost the transferability of adversarial examples against MLLMs. In principle, MPCAttack aggregates semantic representations, from both visual images and language texts, to facilitate joint adversarial optimisation on the aggregated features through a Multi-Paradigm Collaborative Optimisation (MPCO) strategy. By performing contrastive matching on multi-paradigm features, MPCO adaptively balances the importance of different paradigm representations and guides the global perturbation optimisation, effectively alleviating the representation bias. Extensive experimental results on multiple benchmarks demonstrate the superiority of MPCAttack, indicating that our solution consistently outperforms state-of-the-art methods in both targeted and untargeted attacks on open-source and closed-source MLLMs. The code is released at https://github.com/LiYuanBoJNU/MPCAttack.

---

## 论文详细总结（自动生成）

以下是对论文《Multi-Paradigm Collaborative Adversarial Attack Against Multi-Modal Large Language Models》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）在视觉理解和推理方面取得了巨大进步，但其安全性面临严峻挑战。现有的对抗攻击研究主要集中在“迁移性攻击”上，即利用白盒代理模型生成的对抗样本来攻击黑盒目标模型。
*   **核心问题**：作者指出当前攻击方法存在两个主要局限：
    1.  **单一范式限制**：现有攻击大多依赖单一学习范式（如仅使用 CLIP 的跨模态对齐），导致特征表示单一，容易陷入特定模型的表示偏差，迁移性差。
    2.  **缺乏协作优化**：简单的模型集成往往只是独立优化特征，忽略了不同表示空间之间的语义互补性，容易陷入局部最优。
*   **研究意义**：通过提出多范式协作框架，揭示 MLLM 普遍存在的结构性漏洞，为提升模型鲁棒性提供评估工具。

### 2. 方法论
*   **核心思想**：提出 **MPCAttack** 框架，整合多种大规模学习范式的特征，并通过协作优化策略引导对抗扰动的生成。
*   **关键技术细节**：
    *   **多范式集成**：同时引入三种范式的代理模型：
        *   **跨模态对齐**（如 CLIP）：负责图像与文本的语义匹配。
        *   **多模态理解**（如 InternVL3）：负责抽象语义关系和推理。
        *   **视觉自监督学习**（如 DINOv2）：负责底层视觉特征。
    *   **特征增强**：利用多模态理解模型的文本生成能力，提取目标和源图像的文本描述，并将其编码为高维语义特征，与图像特征加权融合。
    *   **多范式协作优化（MPCO）**：
        *   将不同范式的特征进行 $L_2$ 归一化并拼接。
        *   **对比匹配损失**：设计了一个对比损失函数，旨在最小化对抗样本与目标图像特征的距离，同时最大化其与源图像特征的距离。
        *   通过温度系数 $\tau$ 和平衡因子 $\omega$ 动态调节不同范式特征在优化过程中的权重。
*   **算法流程**：在扰动预算 $\epsilon$ 限制下，通过 300 次迭代优化，利用动量梯度下降法更新对抗扰动 $\delta$。

### 3. 实验设计
*   **数据集**：
    *   **ImageNet**：选取 NIPS 2017 对抗挑战赛的 1000 张子集。
    *   **Flickr30K**：用于评估跨域泛化能力，包含真实人类标注。
    *   **MME**：综合性 MLLM 评测基准，包含“是/否”问题。
*   **目标模型（Victim Models）**：
    *   **开源模型**：Qwen2.5-VL-7B, InternVL3-8B, LLaVA-1.5-7B, GLM-4.1V-9B。
    *   **闭源模型**：GPT-4o, GPT-5, Claude-3.5, Gemini-2.0。
*   **对比方法**：AnyAttack, CoA, X-Transfer, M-Attack, FOA-Attack。
*   **评估指标**：攻击成功率（ASR）和语义相似度（AvgSim），采用“LLM-as-a-judge”方案（如 GPT-4o-mini）进行评分。

### 4. 资源与算力
*   **硬件环境**：论文明确提到所有实验均在 **单张 NVIDIA RTX 3090 GPU** 上完成。
*   **训练/优化时长**：文中未详细说明生成单个样本的具体耗时，但提到每个样本需进行 300 次优化迭代。考虑到使用了三个大型代理模型（CLIP, InternVL3, DINOv2），其计算开销会高于单一模型攻击。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个主流数据集和 8 个顶尖 MLLM（包括尚未完全公开细节的“GPT-5”级别模型）。
    *   进行了有目标（Targeted）和无目标（Untargeted）两种场景的全面对比。
*   **消融实验**：非常充分。作者分别验证了：
    1.  不同学习范式的贡献（移除任一范式性能均下降）。
    2.  MPCO 协作策略的有效性。
    3.  不同骨干网络（如 SigLIP2, DINOv3）的影响。
    4.  超参数（权重因子 $\lambda$、温度系数 $\tau$ 等）的敏感性分析。
*   **客观性**：使用了 VLMEvalKit 标准工具包，并采用了自动化的 LLM 评分机制，减少了人工主观偏差。

### 6. 主要结论与发现
*   **性能领先**：MPCAttack 在所有测试模型上均显著优于 SOTA 方法。在开源模型上的平均有目标攻击成功率达到 63.33%，无目标攻击达到 92.10%。
*   **闭源模型脆弱性**：即使是 GPT-5、Gemini-2.0 等最先进的闭源模型，在 MPCAttack 面前也表现出极高的脆弱性（GPT-5 的有目标攻击成功率高达 88%）。
*   **范式互补性**：跨模态对齐范式提供的迁移信号最强，但多范式协作能有效弥补单一模型的表示偏差，是提升迁移性的关键。

### 7. 优点
*   **视角新颖**：首次系统性地提出利用“多学习范式”协作来增强对抗攻击的迁移性。
*   **泛化性强**：实验证明该方法不仅在图像分类任务上有效，在复杂的视觉推理和描述任务中同样表现卓越。
*   **即插即用**：该框架可以灵活更换不同的代理模型，具有良好的扩展性。

### 8. 不足与局限
*   **计算开销**：由于需要同时运行多个大型代理模型（尤其是包含 MLLM 的推理过程），生成对抗样本的显存占用和计算时间较高。
*   **特定数据集表现**：在 Flickr30K 上的 ASR 相对较低，作者解释是因为该数据集描述过于多样化，导致模型生成的响应难以被简单阈值判定为攻击成功。
*   **防御对策缺失**：论文主要侧重于攻击，未深入探讨如何针对这种多范式攻击开发有效的防御机制。

（完）
