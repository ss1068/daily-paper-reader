---
title: "AgentSentry: Mitigating Indirect Prompt Injection in LLM Agents via Temporal Causal Diagnostics and Context Purification"
title_zh: AgentSentry：通过时间因果诊断与上下文净化缓解 LLM 智能体中的间接提示注入
authors: "Tian Zhang, Yiwei Xu, Juan Wang, Keyan Guo, Xiaoyang Xu, Bowen Xiao, Quanlong Guan, Jinlin Fan, Jiawei Liu, Zhiquan Liu, Hongxin Hu"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22724v1"
tags: ["query:llm"]
score: 10.0
evidence: 缓解大语言模型智能体中的间接提示注入
tldr: 针对大语言模型智能体面临的间接提示注入（IPI）威胁，本文提出AgentSentry防御框架。该框架首次将多轮IPI建模为时间因果接管，通过在工具返回边界进行受控的反事实重新执行来定位攻击点，并利用因果引导的上下文净化技术消除攻击偏差，同时保留任务相关信息。实验表明，AgentSentry在消除攻击的同时显著提升了受攻击下的任务效用。
selection_source: fresh_fetch
motivation: 现有的间接提示注入防御方法多依赖启发式检测，容易导致任务误终止或过度抑制工具使用。
method: 提出一种基于时间因果诊断和上下文净化的推理时防御框架，通过反事实执行定位并修复受攻击的轨迹。
result: "在AgentDojo基准测试中，AgentSentry将受攻击下的平均效用提升至74.55%，比最强基准高出20.8至33.6个百分点。"
conclusion: AgentSentry通过因果建模有效解决了多轮交互中的间接提示注入问题，在保证安全性的同时维持了智能体的高效用性。
---

## 摘要
大语言模型 (LLM) 智能体越来越多地依赖外部工具和检索系统来自主完成复杂任务。然而，这种设计使智能体面临间接提示注入 (IPI) 的风险，即嵌入在工具输出或检索内容中的攻击者受控上下文会悄无声息地引导智能体行为偏离用户意图。与基于提示的攻击不同，IPI 在多轮轨迹中展开，使得恶意控制难以从合法的任务执行中分离出来。现有的推理阶段防御主要依赖于启发式检测和对高风险操作的保守拦截，这可能会在模糊的多轮场景下过早终止工作流或广泛抑制工具的使用。我们提出了 AgentSentry，这是一种针对工具增强型 LLM 智能体的新型推理阶段检测与缓解框架。据我们所知，AgentSentry 是第一个将多轮 IPI 建模为时间因果接管的推理阶段防御方案。它通过在工具返回边界处进行受控的反事实重新执行来定位接管点，并通过因果引导的上下文净化实现安全持续运行，该净化过程在保留任务相关证据的同时移除由攻击引起的偏差。我们在 AgentDojo 基准测试上评估了 AgentSentry，涵盖了四个任务套件、三个 IPI 攻击家族和多个黑盒 LLM。AgentSentry 消除了成功的攻击，并在攻击下保持了强大的效用，实现了 74.55% 的平均攻击下效用 (UA)，在不降低良性性能的情况下，比最强的基准模型提高了 20.8 到 33.6 个百分点。

## Abstract
Large language model (LLM) agents increasingly rely on external tools and retrieval systems to autonomously complete complex tasks. However, this design exposes agents to indirect prompt injection (IPI), where attacker-controlled context embedded in tool outputs or retrieved content silently steers agent actions away from user intent. Unlike prompt-based attacks, IPI unfolds over multi-turn trajectories, making malicious control difficult to disentangle from legitimate task execution. Existing inference-time defenses primarily rely on heuristic detection and conservative blocking of high-risk actions, which can prematurely terminate workflows or broadly suppress tool usage under ambiguous multi-turn scenarios. We propose AgentSentry, a novel inference-time detection and mitigation framework for tool-augmented LLM agents. To the best of our knowledge, AgentSentry is the first inference-time defense to model multi-turn IPI as a temporal causal takeover. It localizes takeover points via controlled counterfactual re-executions at tool-return boundaries and enables safe continuation through causally guided context purification that removes attack-induced deviations while preserving task-relevant evidence. We evaluate AgentSentry on the \textsc{AgentDojo} benchmark across four task suites, three IPI attack families, and multiple black-box LLMs. AgentSentry eliminates successful attacks and maintains strong utility under attack, achieving an average Utility Under Attack (UA) of 74.55 %, improving UA by 20.8 to 33.6 percentage points over the strongest baselines without degrading benign performance.

---

## 论文详细总结（自动生成）

### 论文总结：AgentSentry - 通过时间因果诊断与上下文净化缓解 LLM 智能体中的间接提示注入

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在自动化任务中广泛使用外部工具（如搜索、邮件、日历），它们面临着**间接提示注入（Indirect Prompt Injection, IPI）**的严重威胁。攻击者将恶意指令嵌入工具返回的内容中，在不直接接触用户提示的情况下，诱导智能体执行未授权操作（如数据外泄）。
*   **现有防御的局限性：** 现有的推理时防御（如 MELON 或 Task Shield）多采用启发式规则或保守拦截。这导致在多轮交互中，一旦检测到可疑迹象就终止任务，严重损害了智能体的**可用性（Utility）**，且难以区分恶意控制与合法的多步任务执行。
*   **核心目标：** 提出一种既能精准识别攻击，又能允许任务在安全状态下继续执行的防御框架。

#### 2. 方法论：核心思想与关键技术
AgentSentry 将多轮 IPI 建模为一个**时间因果接管（Temporal Causal Takeover）**过程。
*   **核心思想：** 在每个“工具返回边界”（即工具输出存入上下文后、执行下一步动作前），通过**反事实重新执行（Counterfactual Re-execution）**来量化用户意图与外部上下文对智能体决策的因果贡献。
*   **关键技术细节：**
    1.  **四种干预方案（Regimes）：**
        *   `orig`: 原始输入 + 原始工具输出。
        *   `mask`: 任务中性探测（Probe）+ 原始工具输出（用于隔离外部内容的影响）。
        *   `mask_sanitized`: 任务中性探测 + 净化后的工具输出。
        *   `orig_sanitized`: 原始输入 + 净化后的工具输出。
    2.  **因果估算：** 计算平均因果效应（ACE）、间接效应（IE）和直接效应（DE）。如果 IE（由外部内容驱动的效应）显著增强或 ACE（由用户目标驱动的效应）衰减，则判定发生“接管”。
    3.  **因果门控上下文净化：** 一旦检测到接管，系统仅对受污染的上下文进行净化（保留事实证据，移除指令性语言），并修正下一步动作，而非直接终止。
*   **算法流程：** 监控边界 -> 影子运行（Shadow Runs）获取样本 -> 计算因果趋势 -> 触发净化 -> 安全持续执行。

#### 3. 实验设计
*   **基准测试（Benchmark）：** 使用最新的 **AgentDojo (v0.1.35)**，包含 949 个安全测试案例。
*   **任务场景：** 涵盖 Travel（旅游）、Workspace（办公）、Banking（银行）和 Slack（社交）四个套件。
*   **攻击类型：** 针对三种 IPI 攻击家族：Important Instructions（伪造重要指令）、Tool Knowledge（操纵工具知识）、InjecAgent（元数据注入）。
*   **对比方法：** 包括提示词加固（Delimiting, Repeat Prompt）、工具过滤（Tool Filter）、基于分类器的检测（DeBERTa Detector）以及先进的轨迹防御（MELON, Task Shield）。
*   **测试模型：** GPT-4o, GPT-3.5-turbo, Qwen-3-Max。

#### 4. 资源与算力
*   **算力说明：** 论文未明确给出具体的 GPU 型号或训练时长。
*   **推理开销：** 由于 AgentSentry 属于推理时防御，其主要开销在于每个工具返回边界需要进行额外的 LLM 调用（$4K + B$ 次调用，其中 $K$ 为采样数，$B$ 为 Bootstrap 次数）。文中提到在实验中使用了轻量级配置（$K=1$），以保持开销在可接受范围内。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 949 组安全测试，涵盖了多种模型和攻击变体，实验规模较大。
*   **消融实验：** 针对时间聚合、净化机制、探测词强度等多个维度进行了详细的消融实验（见表 3）。
*   **客观性：** 实验在标准化的 AgentDojo 环境下运行，对比了当前最前沿的多种防御方案，评估指标包括攻击成功率（ASR）、攻击下效用（UA）和良性任务成功率（CU），评价维度全面且客观。

#### 6. 主要结论与发现
*   **防御效果：** AgentSentry 在所有测试场景下均实现了 **0% 的攻击成功率（ASR）**。
*   **效用提升：** 在受到攻击的情况下，AgentSentry 维持了 **74.55% 的平均效用（UA）**，比最强的基准方法（如 Task Shield 或 MELON）高出 20.8 到 33.6 个百分点。
*   **良性性能：** 在无攻击的正常任务中，AgentSentry 不会产生误报（FPR 为 0%），且不降低任务成功率。
*   **因果发现：** 实验证明，通过反事实执行定位“接管点”比单纯的语义相似度检测更有效。

#### 7. 优点
*   **首创性：** 首次从时间因果角度建模 IPI 攻击，提供了更深层的理论解释。
*   **高可用性：** 解决了“一检就停”的弊端，通过净化实现“安全续航”，极大地提升了智能体在复杂环境下的实用价值。
*   **黑盒兼容：** 不需要访问模型权重或内部激活，完全适用于通过 API 调用的商业 LLM。
*   **可解释性：** 提供的因果轨迹图能清晰展示攻击在何时、何处开始主导智能体的决策。

#### 8. 不足与局限
*   **推理延迟：** 尽管 $K=1$ 时开销可控，但在实时性要求极高的场景下，多次影子运行仍会增加响应延迟。
*   **净化算子依赖：** 防御效果部分取决于 `Purify` 操作的质量，如果净化过程未能完全剥离恶意指令或误删了关键事实，可能影响后续执行。
*   **数据集限制：** 目前的基准测试（AgentDojo）中攻击多为短程触发，对于极其隐蔽、跨度极大的长程因果接管，该方法的表现仍需进一步验证。

（完）
