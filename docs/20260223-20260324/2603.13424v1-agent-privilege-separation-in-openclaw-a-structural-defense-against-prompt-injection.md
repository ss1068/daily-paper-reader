---
title: "Agent Privilege Separation in OpenClaw: A Structural Defense Against Prompt Injection"
title_zh: OpenClaw 中的智能体权限分离：一种针对提示词注入的结构化防御方案
authors: "Darren Cheng, Wen-Kwang Tsao"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13424v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对提示注入的结构化防御
tldr: "针对大语言模型应用中严重的提示词注入威胁，本文提出了一种基于OpenClaw平台的结构化防御方案。该方案结合了代理隔离（特权分离的双代理流水线）与JSON格式化技术，通过将工具调用权限与原始输入处理分离，确保执行代理无法接触到原始注入内容。实验表明，该防御体系在LLMail-Inject基准测试中将攻击成功率降至0%，显著提升了智能体系统的安全性。"
selection_source: fresh_fetch
motivation: 提示词注入是集成大语言模型应用中最实际且严重的攻击向量，现有的单一代理架构难以有效抵御此类攻击。
method: 提出一种结合了特权分离双代理流水线（代理隔离）与JSON结构化输出的结构化防御机制。
result: "完整防御流水线在基准测试中实现了0%的攻击成功率，其中代理隔离机制起到了主导的防御作用。"
conclusion: 通过结构化设计实现代理间的特权分离，可以从根本上防止执行代理接收到原始注入内容，从而有效抵御提示词注入攻击。
---

## 摘要
提示词注入仍然是针对集成大语言模型（LLM）的应用最实际的攻击向量之一。我们在开源多工具智能体平台 OpenClaw 中，针对当前一代模型复现了微软的 LLMail-Inject 基准测试（Greshake 等人，2024）。我们提出的防御方案结合了两种机制：智能体隔离（通过具有工具分区和权限分离的双智能体流水线实现）以及 JSON 格式化（生成结构化输出，在执行智能体处理之前剥离诱导性表述）。我们针对在单智能体基准测试中成功的 649 次攻击进行了四项实验。完整流水线在评估的基准测试中实现了 0% 的攻击成功率（ASR）。仅使用智能体隔离实现了 0.31% 的 ASR，比基准测试低约 323 倍。仅使用 JSON 格式化实现了 14.18% 的 ASR，约低 7.1 倍。我们的消融研究证实，智能体隔离是主要的防御机制。JSON 格式化提供了额外的加固，但其本身并不充分。该防御是结构性的：无论模型对任何单个输入的行为如何，执行智能体永远不会接收到原始的注入内容。

## Abstract
Prompt injection remains one of the most practical attack vectors against LLM-integrated applications. We replicate the Microsoft LLMail-Inject benchmark (Greshake et al., 2024) against current generation models running inside OpenClaw, an open source multitool agent platform. Our proposed defense combines two mechanisms: agent isolation, implemented as a privilege separated two-agent pipeline with tool partitioning, and JSON formatting, which produces structured output that strips persuasive framing before the action agent processes it.   We run four experiments on the same 649 attacks that succeeded against our single-agent baseline. The full pipeline achieves 0 percent attack success rate (ASR) on the evaluated benchmark. Agent isolation alone achieves 0.31 percent ASR, approximately 323 times lower than the baseline. JSON formatting alone achieves 14.18 percent ASR, about 7.1 times lower.   Our ablation study confirms that agent isolation is the dominant mechanism. JSON formatting provides additional hardening but is not sufficient on its own. The defense is structural: the action agent never receives raw injection content regardless of model behavior on any individual input.

---

## 论文详细总结（自动生成）

以下是对论文《Agent Privilege Separation in OpenClaw: A Structural Defense Against Prompt Injection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对大语言模型（LLM）集成应用中最为严峻的安全威胁——**提示词注入（Prompt Injection）**展开研究。随着 LLM 智能体（Agent）被赋予发送邮件、调用 API 和执行代码等真实世界操作权限，间接提示词注入（即攻击指令隐藏在邮件、文档等外部数据中）的风险显著增加。
研究背景在于，现有的防御手段多依赖于模型对齐或简单的提示词过滤，容易被绕过。作者旨在通过**结构化防御（Structural Defense）**，即从系统架构层面隔离权限，确保即使模型被诱导，也无法执行未经授权的危险操作。

### 2. 方法论：核心思想与关键技术细节
论文提出了一种结合“特权分离”与“信息流控制”的双重防御机制，并在 OpenClaw 平台上实现：
*   **机制一：智能体隔离（Agent Isolation/特权分离）**
    *   将单一智能体拆分为两个协作角色：
        *   **读取智能体（Reader Agent）**：负责读取原始、不可信的外部内容（如邮件），仅拥有生成摘要并存储的权限，**无权调用敏感工具**（如 `send_email`）。
        *   **执行智能体（Actor Agent）**：负责根据摘要采取行动，拥有敏感工具权限，但**严禁接触原始输入**，只能读取读取智能体生成的结构化输出。
    *   这种设计遵循“最小特权原则”，从物理/逻辑架构上切断了注入指令直接触达敏感工具的路径。
*   **机制二：JSON 格式化与验证（JSON Formatting）**
    *   读取智能体必须将信息转化为严格的 JSON 格式（包含发件人、主题、摘要、行动项等字段）。
    *   **轻量级验证器**：在数据传递给执行智能体前，使用正则规则检测 JSON 内容，识别邮件地址字面量、工具调用语法片段及常见的注入触发词（如“ignore previous instructions”）。
    *   其核心思想是剥离注入攻击中的“诱导性表述”（Persuasive Framing），使攻击指令失去上下文。

### 3. 实验设计
*   **基准测试（Benchmark）**：采用了微软的 **LLMail-Inject**，包含四个难度递增的场景（简单摘要、多邮件处理、结合 RAG 的查询、数据窃取）。
*   **实验模型**：使用 `gpt-5-mini` 作为核心模型。
*   **实验流程**：
    1.  首先在 22,899 个唯一攻击载荷上运行无防御的单智能体基准测试。
    2.  筛选出在 `gpt-5-mini` 上依然攻击成功的 **649 个案例** 作为标准化评估池。
    3.  对比四种配置：**基准（无防御）**、**仅 JSON 验证**、**仅双智能体隔离**、**完整流水线（隔离+JSON）**。

### 4. 资源与算力
*   论文中提到了使用 `gpt-5-mini` 模型以及开源的 OpenClaw 智能体平台。
*   **未明确说明**：文中未提供具体的硬件算力细节（如 GPU 型号、数量）或训练时长。由于该研究侧重于推理阶段的架构防御而非模型训练，其算力消耗主要体现在多智能体协作带来的额外推理 Token 开销。

### 5. 实验数量与充分性
*   **实验规模**：从 46 万次原始提交中筛选出 2.2 万个载荷，并最终针对 649 个高效攻击案例进行消融实验。
*   **充分性**：实验覆盖了从简单指令到复杂数据窃取的四种典型场景，并进行了详细的消融研究（Ablation Study），量化了每个防御组件的贡献。
*   **客观性**：通过将所有防御配置归一化到相同的 649 个成功攻击案例上进行对比，确保了实验的公平性。

### 6. 主要结论与发现
*   **完整防御极度有效**：完整流水线在所有测试案例中实现了 **0% 的攻击成功率（ASR）**。
*   **隔离是核心**：仅靠“智能体隔离”就能将攻击成功率降至 0.31%（比基准提升 323 倍），证明了工具分区是防御的主导机制。
*   **JSON 格式化是补充**：仅靠 JSON 格式化能提升 7.1 倍防御力，但不足以完全抵御攻击（ASR 为 14.18%）。
*   **模型原生安全性提升**：`gpt-5-mini` 在无防御状态下的 ASR 仅为 2.83%，远优于前代模型（如 GPT-4o mini 的 30-60%），显示了模型对齐技术的进步。

### 7. 优点
*   **结构化保障**：防御效力不依赖于模型的“自觉性”或提示词工程，而是由系统架构强制执行。
*   **低成本可扩展**：利用现有的多智能体编排模式即可实现，无需重新训练模型。
*   **防御深度**：结合了特权分离（安全工程）和信息流清洗（数据安全）两种思路。

### 8. 不足与局限
*   **自适应攻击缺失**：实验未评估专门针对“双智能体+JSON”架构设计的自适应攻击（例如，攻击者试图在 JSON 字段中编码可执行指令）。
*   **信息泄露风险**：实验显示 63.7% 的注入内容仍会“泄露”到 JSON 摘要中，虽然目前无法触发工具，但未来若执行智能体变得更强大，可能存在潜在风险。
*   **验证器简单**：目前仅使用简单的正则验证，面对复杂的编码绕过可能失效。
*   **性能开销**：双智能体流水线会增加推理延迟和 Token 成本。

（完）
