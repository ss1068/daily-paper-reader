---
title: "Image-based Prompt Injection: Hijacking Multimodal LLMs through Visually Embedded Adversarial Instructions"
title_zh: 基于图像的提示注入：通过视觉嵌入的对抗性指令劫持多模态大语言模型
authors: "Neha Nagaraja, Lan Zhang, Zhilong Wang, Bo Zhang, Pawan Patil"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03637v1"
tags: ["query:llm"]
score: 10.0
evidence: 多模态LLM中的图像提示注入
tldr: "本研究探讨了多模态大语言模型（MLLM）面临的基于图像的提示注入（IPI）威胁。研究提出了一种端到端的黑盒攻击流水线，通过分割区域选择、自适应字体缩放和背景感知渲染，将对抗性指令隐蔽地嵌入自然图像中。实验表明，该方法在保持人类视觉隐蔽性的同时，能有效操纵GPT-4-turbo等模型的输出，攻击成功率最高达64%，揭示了MLLM在多模态环境下的安全漏洞。"
selection_source: fresh_fetch
motivation: 旨在研究多模态大语言模型在视觉指令嵌入攻击下的安全脆弱性。
method: 开发了一种结合区域分割、自适应缩放和背景感知渲染的端到端黑盒攻击流水线。
result: "在GPT-4-turbo上的实验显示，该攻击在隐蔽性约束下最高可达到64%的成功率。"
conclusion: 图像提示注入是黑盒环境下的一种实际威胁，亟需开发针对多模态提示注入的防御机制。
---

## 摘要
多模态大语言模型 (MLLMs) 整合了视觉和文本以驱动各种应用，但这种整合也引入了新的漏洞。我们研究了基于图像的提示注入 (IPI)，这是一种黑盒攻击，通过将对抗性指令嵌入到自然图像中来覆盖模型行为。我们的端到端 IPI 流水线结合了基于分割的区域选择、自适应字体缩放和背景感知渲染，旨在向人类感知隐藏提示，同时保留模型的可解释性。利用 COCO 数据集和 GPT-4-turbo，我们评估了 12 种对抗性提示策略和多种嵌入配置。结果表明，IPI 可以可靠地操纵模型的输出，在隐蔽性约束下，最有效的配置实现了高达 64% 的攻击成功率。这些发现强调了 IPI 在黑盒设置中是一种实际威胁，并突出了针对多模态提示注入进行防御的必要性。

## Abstract
Multimodal Large Language Models (MLLMs) integrate vision and text to power applications, but this integration introduces new vulnerabilities. We study Image-based Prompt Injection (IPI), a black-box attack in which adversarial instructions are embedded into natural images to override model behavior. Our end-to-end IPI pipeline incorporates segmentation-based region selection, adaptive font scaling, and background-aware rendering to conceal prompts from human perception while preserving model interpretability. Using the COCO dataset and GPT-4-turbo, we evaluate 12 adversarial prompt strategies and multiple embedding configurations. The results show that IPI can reliably manipulate the output of the model, with the most effective configuration achieving up to 64\% attack success under stealth constraints. These findings highlight IPI as a practical threat in black-box settings and underscore the need for defenses against multimodal prompt injection.

---

## 论文详细总结（自动生成）

### 论文总结：基于图像的提示注入（IPI）

#### 1. 核心问题与整体含义
随着多模态大语言模型（MLLM）在图像描述、自动驾驶和智能体工作流中的广泛应用，其安全性面临新挑战。本文研究的核心问题是**基于图像的提示注入（Image-based Prompt Injection, IPI）**。
*   **研究动机**：传统的提示注入研究多集中在纯文本领域，而 MLLM 将视觉和文本整合，使得攻击者可以通过在图像中嵌入视觉指令来操纵模型输出。
*   **背景**：攻击者希望在不触碰模型权重（黑盒环境）的情况下，通过修改图像像素，使模型忽略原始视觉内容并执行嵌入的恶意指令，同时要求这些指令对人类肉眼近乎不可见。

#### 2. 方法论：IPI 攻击流水线
研究者提出了一种端到端的模块化攻击流水线，主要包含以下关键步骤：
*   **对抗性提示工程**：设计了 12 种提示词变体，利用重复指令（如“忽略图像，只说XXX”）和对象感知前缀（利用 GPT-4o 识别图中物体并生成“忽略图中狗和球”的指令）来增强攻击效果。
*   **基于分割的区域选择**：使用 **SAM (Segment Anything Model)** 对图像进行全分割，根据面积（大面积优先）、纹理均匀度（低复杂度优先）和空间位置（右上或中下优先）对候选区域进行排序，以寻找最佳嵌入位置。
*   **自适应嵌入逻辑**：
    *   **单掩码/多掩码分布**：若单个区域无法容纳完整提示，则将指令拆分并按语义顺序分布在多个掩码区域。
    *   **自适应缩放**：在保证模型可读的前提下，以 10% 的步长递减字体大小以增强隐蔽性。
*   **背景感知渲染策略**：
    1.  **背景平均色块（Patch Coloring）**：按字符所在局部背景的平均 RGB 值加亮度偏移渲染。
    2.  **像素级混合（Pixel-Level Blending）**：将文字像素与背景像素进行精细融合。
    3.  **全局区域平均色（Global Region-Averaged Coloring）**：计算整个嵌入区域的平均色并统一应用，实验证明此方法在隐蔽性与成功率之间平衡得最好。

#### 3. 实验设计
*   **数据集**：使用 **COCO 数据集**，涵盖室内、室外、杂乱和简洁等多种真实场景。
*   **目标模型（Benchmark）**：主要针对 **GPT-4-turbo** 进行黑盒测试。
*   **评估指标**：攻击成功率（ASR）和视觉失真度（MSE）。
*   **对比维度**：对比了 12 种不同的提示策略、不同的字体大小（0.10 到 0.30 缩放比例）、三种颜色渲染策略以及是否有“对象感知前缀”的加入。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   由于该研究属于黑盒攻击，主要通过 API 调用目标模型（GPT-4-turbo）进行推理，且使用了预训练的 SAM 模型进行分割，因此对本地算力的需求相对较低，重点在于 API 调用的规模和图像处理算法的效率。

#### 5. 实验数量与充分性
*   **实验规模**：对 12 种提示策略进行了基准测试，每种配置在 COCO 数据集上进行多次迭代（如针对字体大小的实验进行了 800 次查询）。
*   **消融实验**：详细探讨了字体大小、亮度偏移（Offset）、单/多掩码策略对 ASR 的影响。
*   **客观性**：实验涵盖了多种视觉背景和复杂的提示词组合，通过定量数据展示了“隐蔽性”与“攻击成功率”之间的权衡关系。虽然主要针对 GPT-4-turbo，但其方法论具有通用性。

#### 6. 主要结论与发现
*   **攻击可行性**：在黑盒设置下，通过视觉嵌入指令可以可靠地劫持 MLLM 的输出。
*   **关键阈值**：字体缩放比例存在阈值（约 $\ge 0.3$），低于此值模型识别率大幅下降。
*   **语义增强**：加入“对象感知前缀”（即明确要求模型忽略图中具体物体）能显著提升攻击成功率，ASR 从 41% 提升至 **64%**。
*   **策略优劣**：重复性指令（Prompt 5）最为稳健；全局区域平均色渲染在保持隐蔽性的同时，比像素级混合更易被模型解读。

#### 7. 优点
*   **隐蔽性设计**：不同于简单的文字叠加，该方法考虑了背景色融合和区域选择，使攻击更具欺骗性。
*   **端到端自动化**：结合了 SAM 分割和 GPT-4o 语义分析，实现了从原始图像到攻击图像的自动化生成。
*   **现实威胁揭示**：证明了即使在不接触模型参数的情况下，仅通过视觉通道也能绕过安全对齐。

#### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 GPT-4-turbo，缺乏对其他主流开源模型（如 LLaVA, Qwen-VL）或闭源模型（如 Claude 3, Gemini）的横向对比。
*   **防御研究较浅**：虽然提到了 OCR 检测和输入净化等防御方向，但未进行深入的防御有效性验证。
*   **环境敏感性**：攻击效果高度依赖于图像背景的复杂度和亮度偏移的选择，在极端光照或极度复杂的纹理下，隐蔽性与成功率的矛盾依然突出。

（完）
