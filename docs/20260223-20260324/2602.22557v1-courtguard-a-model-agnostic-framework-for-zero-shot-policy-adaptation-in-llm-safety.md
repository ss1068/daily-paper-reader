---
title: "CourtGuard: A Model-Agnostic Framework for Zero-Shot Policy Adaptation in LLM Safety"
title_zh: CourtGuard：一种用于大语言模型安全中零样本策略适配的模型无关框架
authors: "Umid Suleymanov, Rufiz Bayramov, Suad Gafarli, Seljan Musayeva, Taghi Mammadov, Aynur Akhundlu, Murat Kantarcioglu"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22557v1"
tags: ["query:llm"]
score: 9.0
evidence: LLM安全的零样本策略适配
tldr: CourtGuard是一个针对大语言模型安全性的模型无关框架，旨在解决现有安全机制在应对新治理规则时适应性差、重训练成本高的问题。该框架采用检索增强的多智能体“证据辩论”机制，通过外部政策文档引导对抗性辩论，实现了无需微调的零样本策略迁移。它在多个基准测试中达到领先水平，并展现了出色的跨领域适应能力及自动化数据审计潜力，为AI治理提供了灵活且可解释的新路径。
selection_source: fresh_fetch
motivation: 现有的LLM安全分类器过于僵化，难以在不进行昂贵重训练的情况下快速适应不断变化的治理规则。
method: 提出一种检索增强的多智能体框架，通过基于外部政策文档的对抗性“证据辩论”来进行安全评估。
result: "在7个安全基准测试中取得领先性能，并在零样本维基百科破坏检测任务中达到90%的准确率。"
conclusion: 将安全逻辑与模型权重解耦，为满足AI治理中动态的监管要求提供了一种稳健、可解释且易于适配的方案。
---

## 摘要
当前大语言模型（LLMs）的安全机制严重依赖于静态且经过微调的分类器，这些分类器存在适配僵化的问题，即在不进行昂贵重训的情况下无法执行新的治理规则。为解决这一问题，我们提出了 CourtGuard，这是一个检索增强的多智能体框架，它将安全评估重新构想为“证据辩论”（Evidentiary Debate）。通过组织基于外部策略文档的对抗性辩论，CourtGuard 在 7 个安全基准测试中实现了最先进的性能，在无需微调的情况下超越了专门的策略遵循基准模型。除了标准指标外，我们还强调了两个关键能力：(1) 零样本适配性，我们的框架通过更换参考策略，成功泛化到域外的维基百科破坏（Wikipedia Vandalism）任务（准确率达到 90%）；(2) 自动化数据策展与审计，我们利用 CourtGuard 策展并审计了九个包含复杂对抗性攻击的新型数据集。我们的研究结果表明，将安全逻辑与模型权重解耦，为满足当前和未来人工智能治理中的监管要求提供了一条稳健、可解释且具有适配性的路径。

## Abstract
Current safety mechanisms for Large Language Models (LLMs) rely heavily on static, fine-tuned classifiers that suffer from adaptation rigidity, the inability to enforce new governance rules without expensive retraining. To address this, we introduce CourtGuard, a retrieval-augmented multi-agent framework that reimagines safety evaluation as Evidentiary Debate. By orchestrating an adversarial debate grounded in external policy documents, CourtGuard achieves state-of-the-art performance across 7 safety benchmarks, outperforming dedicated policy-following baselines without fine-tuning. Beyond standard metrics, we highlight two critical capabilities: (1) Zero-Shot Adaptability, where our framework successfully generalized to an out-of-domain Wikipedia Vandalism task (achieving 90\% accuracy) by swapping the reference policy; and (2) Automated Data Curation and Auditing, where we leveraged CourtGuard to curate and audit nine novel datasets of sophisticated adversarial attacks. Our results demonstrate that decoupling safety logic from model weights offers a robust, interpretable, and adaptable path for meeting current and future regulatory requirements in AI governance.

---

## 论文详细总结（自动生成）

以下是对论文《CourtGuard: A Model-Agnostic Framework for Zero-Shot Policy Adaptation in LLM Safety》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）安全防御机制中的**“适配僵化”（Adaptation Rigidity）**问题展开研究。现有的安全护栏（如 LlamaGuard）通常是静态的、经过微调的分类器，一旦治理规则或监管政策发生变化（例如从“拒绝所有医疗建议”转变为“允许一般健康说明但拒绝诊断处方”），这些模型必须经过昂贵的重新训练或微调才能适应。
**CourtGuard** 的核心目标是实现**零样本策略适配**，即在不更改模型权重的前提下，通过输入新的政策文档，使系统能够立即执行新的安全规则。

### 2. 方法论：核心思想与关键技术
CourtGuard 将安全评估从“基于直觉的分类”转变为**“证据辩论”（Evidentiary Debate）**。其核心架构包含三个模块：
*   **策略检索增强（RAG）流水线**：将外部治理文档（如 OpenAI 使用政策、欧盟 AI 法案等）切片并向量化。在推理时，根据目标响应检索最相关的政策条款，为辩论提供法律/规则依据。
*   **对抗性辩论模块**：组织两个专门的智能体进行 $N$ 轮辩论：
    *   **攻击者（Attacker）**：识别响应中的监管威胁（违反政策）和实际威胁（实现伤害的路径）。
    *   **防御者（Defender）**：寻找合规证据、适用豁免条款（如教育/研究用途）或实施障碍。
*   **法官评估与裁决（Judge）**：分析辩论记录，根据“监管威胁”和“实际威胁”两个维度进行评分（1-3分），最终映射为安全（SAFE）、边界（BORDERLINE）或不安全（UNSAFE）的裁决，并提供带有政策引用的推理轨迹。

### 3. 实验设计
*   **数据集/场景**：
    *   **标准基准**：WildGuard, HarmBench, JailJudge, PKU-SafeRLHF, ToxicChat, BeaverTails, XSTest。
    *   **高难度攻击集**：AdvBenchM（100% 攻击成功率的复杂攻击）。
    *   **域外任务**：维基百科破坏检测（Wikipedia Vandalism）。
    *   **法律合规**：基于 GDPR 和 InfoSoc 指令的隐私与版权合规性评估。
*   **对比方法（Baselines）**：
    *   **静态微调护栏**：LlamaGuard 3/4, ShieldGemma (2B/9B/27B), xGuard, WildGuard。
    *   **策略遵循模型**：GPT-OSS-Safeguard-20B。
    *   **通用 LLM 裁判**：基于 Llama-3-70B 和 GPT-OSS-20B 的单次推理、CoT 推理及多智能体投票。

### 4. 资源与算力
*   **模型后端**：主要使用了 **Llama-3-70B-Instruct** 和 **GPT-OSS-20B** 作为智能体骨干。
*   **算力说明**：文中提到使用了 ADA 大学数据分析研究中心（CeDAR）提供的 GPU 计算资源。
*   **训练时长**：由于该框架是**零样本（Zero-Shot）**且基于推理侧的，因此**不需要针对特定安全任务进行模型训练**。实验主要集中在推理开销上，未详细列出具体的 GPU 小时数。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个标准安全基准测试上进行了大规模评估，涵盖了 15 种伤害类别和 10 种自动化攻击方法。
*   **消融实验**：对比了 1 轮与 2 轮辩论的效果，以及有无 RAG 策略增强的效果。
*   **充分性评价**：实验设计非常充分且具有多样性。不仅涵盖了传统的越狱检测，还通过维基百科和法律合规实验验证了框架的泛化能力。此外，作者还利用该框架对现有基准测试的标签噪声进行了审计，证明了其作为自动化标注工具的客观性。

### 6. 主要结论与发现
*   **性能领先**：CourtGuard 在无需微调的情况下，在多个基准测试中达到了 SOTA 水平（平均准确率 0.87），优于专门微调的安全模型。
*   **极强的适配性**：仅通过更换检索库中的政策文档，就能在维基百科破坏检测中达到 90% 的准确率，在法律合规性评估中与专家达成 93.9% 的一致性。
*   **纠错能力**：发现并纠正了现有安全基准测试（如 JailJudge 和 HarmBench）中的大量错误标签，证明了基于证据的辩论比单一模型的直觉判断更可靠。
*   **模型无关性**：框架在不同规模和架构的模型上均表现稳健，消除了供应商锁定风险。

### 7. 优点与亮点
*   **解耦安全逻辑**：将安全规则从模型权重中解耦，实现了“即插即用”的政策更新。
*   **高可解释性**：裁决过程包含完整的辩论记录和具体的政策条款引用，符合监管对 AI 透明度的要求。
*   **对抗性增强**：通过引入防御者角色，有效降低了安全检测中的“过度拒绝”（Over-refusal）现象。
*   **自动化审计**：展示了作为高保真自动化标注器和现有数据集审计工具的巨大潜力。

### 8. 不足与局限
*   **推理延迟**：由于涉及多轮多智能体辩论和 RAG 检索，其推理延迟显著高于单次分类模型，可能不适用于对实时性要求极高的在线过滤场景。
*   **指令遵循依赖**：框架的可靠性高度依赖于骨干模型遵循复杂格式指令的能力，较小的模型可能会出现解析错误。
*   **政策依赖性**：系统的表现上限受限于检索到的政策文档的质量和全面性。

（完）
