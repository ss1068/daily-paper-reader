---
title: Systematic Scaling Analysis of Jailbreak Attacks in Large Language Models
title_zh: 大语言模型越狱攻击的系统性缩放分析
authors: "Xiangwen Wang, Ananth Balashankar, Varun Chandrasekaran"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11149v2"
tags: ["query:llm"]
score: 10.0
evidence: 大语言模型越狱攻击的系统性缩放分析
tldr: 本研究针对大语言模型的越狱攻击进行了系统性的缩放分析，旨在理解攻击成功率如何随攻击者投入的计算资源（FLOPs）而变化。作者提出了一个缩放定律框架，将不同类型的越狱攻击视为受计算限制的优化过程，并评估了四种代表性范式。研究发现，基于提示词的攻击在计算效率和隐蔽性上优于基于优化的方法，且越狱难度与具体有害目标高度相关，为评估模型安全性和攻击效率提供了新视角。
selection_source: fresh_fetch
motivation: 目前缺乏对大语言模型越狱攻击成功率与攻击者投入成本（计算资源）之间关系的系统性理解。
method: 提出一种基于FLOPs的缩放定律框架，通过拟合饱和指数函数来量化并比较四种代表性越狱攻击范式的效率。
result: 研究发现基于提示词的攻击在计算效率和隐蔽性上均优于基于优化的方法，且虚假信息类目标的越狱难度通常较低。
conclusion: 越狱攻击的效能遵循特定的缩放规律，其成功率受攻击范式、计算预算及危害类型的显著影响。
---

## 摘要
大语言模型仍然容易受到越狱攻击的影响，但我们仍然缺乏对越狱成功率如何随攻击者在不同方法、模型家族和伤害类型上的投入而缩放的系统性理解。我们通过将每次攻击视为受计算限制的优化过程，并在统一的 FLOPs 轴上衡量进展，初步建立了一个越狱攻击的缩放法则框架。我们的系统性评估涵盖了四种代表性的越狱范式，包括基于优化的攻击、自我细化提示、基于采样的选择和遗传优化，涉及多个模型家族、不同规模以及多种有害目标。我们通过将简单的饱和指数函数拟合到 FLOPs-成功率轨迹上，研究了将攻击者预算与攻击成功得分联系起来的缩放法则，并从拟合曲线中得出了具有可比性的效率总结。经验表明，与基于优化的方法相比，基于提示的范式往往具有最高的计算效率。为了解释这一差距，我们将基于提示的更新纳入优化视角，并通过同状态对比证明，基于提示的攻击在提示空间中能更有效地进行优化。我们还展示了攻击占据了不同的“成功率-隐蔽性”运行点，其中基于提示的方法占据了高成功率、高隐蔽性的区域。最后，我们发现漏洞具有强烈的目标依赖性：涉及虚假信息的伤害通常比其他非虚假信息伤害更容易被诱发。

## Abstract
Large language models remain vulnerable to jailbreak attacks, yet we still lack a systematic understanding of how jailbreak success scales with attacker effort across methods, model families, and harm types. We initiate a scaling-law framework for jailbreaks by treating each attack as a compute-bounded optimization procedure and measuring progress on a shared FLOPs axis. Our systematic evaluation spans four representative jailbreak paradigms, covering optimization-based attacks, self-refinement prompting, sampling-based selection, and genetic optimization, across multiple model families and scales on a diverse set of harmful goals. We investigate scaling laws that relate attacker budget to attack success score by fitting a simple saturating exponential function to FLOPs--success trajectories, and we derive comparable efficiency summaries from the fitted curves. Empirically, prompting-based paradigms tend to be the most compute-efficient compared to optimization-based methods. To explain this gap, we cast prompt-based updates into an optimization view and show via a same-state comparison that prompt-based attacks more effectively optimize in prompt space. We also show that attacks occupy distinct success--stealthiness operating points with prompting-based methods occupying the high-success, high-stealth region. Finally, we find that vulnerability is strongly goal-dependent: harms involving misinformation are typically easier to elicit than other non-misinformation harms.

---

## 论文详细总结（自动生成）

以下是对论文《Systematic Scaling Analysis of Jailbreak Attacks in Large Language Models》（大语言模型越狱攻击的系统性缩放分析）的结构化总结：

### 1. 核心问题与整体含义
论文探讨了**越狱攻击（Jailbreak Attacks）的成功率如何随攻击者投入的计算资源（算力）而缩放**。尽管LLM越狱研究进展迅速，但领域内缺乏像语言模型训练“缩放定律”（Scaling Laws）那样的系统性框架，来量化不同攻击范式在不同模型规模和伤害类型下的效率。研究动机在于建立一个统一的评估基准，识别哪些攻击更具威胁性、更高效且更具隐蔽性。

### 2. 论文提出的方法论
*   **核心思想**：将越狱攻击视为一个**受计算限制的优化过程**。为了使不同类型的攻击（如梯度优化、LLM重写、随机采样）具有可比性，作者引入了统一的**FLOPs（浮点运算数）**作为衡量攻击成本的轴。
*   **缩放模型**：使用**饱和指数函数（Saturating Exponential Function）**来拟合“算力-成功率”轨迹：
    $$ASR(B) = a + b(1 - e^{-cB})$$
    其中，$B$ 是累计计算量，$a$ 是初始得分，$a+b$ 是渐进性能上限，$c$ 是趋近速率。
*   **效率指标**：推导出 $B_{50}$ 和 $B_{95}$（达到 50% 或 95% 潜在提升所需的算力），用于直观比较不同攻击的效率。
*   **机制分析**：通过“同状态对比协议”（Same-state Protocol），将基于提示词的攻击（如 PAIR）映射到优化视角，对比其与梯度攻击（如 GCG）在提示空间中的搜索有效性。

### 3. 实验设计
*   **数据集与目标**：构建了一个包含 **200 个有害目标**的统一评估集，混合了 AdvBench、HarmBench 和 ClearHarm。目标被分为四类：有害指令、恶意创作、虚假信息、冒犯性内容。
*   **对比方法**：涵盖四种代表性范式：
    1.  **GCG**：基于梯度的离散提示优化。
    2.  **PAIR**：基于 LLM 迭代重写的黑盒攻击。
    3.  **BoN (Best-of-n)**：基于采样的选择策略。
    4.  **AutoDAN**：基于遗传算法的自动化提示构建。
*   **评估模型**：Llama-3.1/3.2 系列、Qwen3 系列（1.7B 到 8B）、Gemma-3-4B。
*   **Benchmark 指标**：使用 **GPT-5** 作为裁判，评估“红队得分”（安全性破坏程度）和“相关性得分”（内容匹配度）；同时使用 GPT-2 困惑度衡量“隐蔽性”。

### 4. 资源与算力
*   **算力衡量**：论文主要从**攻击者视角**量化算力，单位为 **TFLOPs**（$10^{12}$ 次运算）。计算涵盖了受害者模型的前向/后向传播以及辅助攻击模型（如 PAIR 中的重写模型）的开销。
*   **硬件细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长，而是侧重于算法层面的理论计算开销（FLOPs），这有助于跨硬件环境的公平比较。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 4 种攻击算法、3 个模型家族、多个模型参数规模（1.7B 至 8B）以及 200 个多样化目标。
*   **充分性与公平性**：
    *   **充分性**：通过拟合曲线而非单一数据点来观察趋势，实验组数较多，涵盖了跨家族和家族内规模缩放。
    *   **公平性**：通过 FLOPs 归一化解决了不同攻击“迭代步长”定义不一的问题，使得黑盒与白盒攻击能在同一维度对比。
    *   **客观性**：引入了相关性得分来剥离“表面越狱”现象，并进行了消融实验（如目标匹配的 PAIR）来验证效率差距的来源。

### 6. 主要结论与发现
*   **缩放规律普遍存在**：所有攻击的成功率随算力增加呈现先快速上升后饱和的趋势，且能被饱和指数函数良好拟合。
*   **提示词重写更高效**：基于提示词重写的 **PAIR 在计算效率上显著优于基于梯度的 GCG**，且能达到更高的成功率上限。
*   **隐蔽性优势**：PAIR 和 AutoDAN 在保持高成功率的同时，其生成的提示词更自然（隐蔽性高），而 GCG 和 BoN 的隐蔽性较差。
*   **目标依赖性**：**虚假信息（Misinformation）类目标最容易被越狱**，反映出模型在应对欺骗性内容方面的防御比应对直接有害指令更薄弱。
*   **规模效应**：模型家族内的规模增加主要影响达到饱和的速度，而模型家族之间的差异（如 Llama vs Qwen）会显著改变成功率的上下限。

### 7. 优点
*   **视角新颖**：首次系统性地将缩放定律引入越狱攻击分析，提供了量化攻击威胁的新工具。
*   **统一度量**：使用 FLOPs 解决了长期以来自动化红队工具难以横向对比的难题。
*   **深入机制**：不仅观察到 PAIR 比 GCG 强，还通过优化视角的对比解释了“为什么强”（在提示空间中搜索方向更准）。

### 8. 不足与局限
*   **算力估算的近似性**：FLOPs 计算依赖于特定的 token 计数和模型常数，可能无法完全捕捉实际部署中的延迟或防御监控成本。
*   **裁判偏差风险**：主要依赖 GPT-5 作为裁判，虽然比字符串匹配更准确，但仍可能存在模型偏见或对特定语气的敏感性。
*   **覆盖范围限制**：实验主要集中在英语、单轮对话和提示词攻击，未涉及多语言、多轮对话或工具调用等更复杂的攻击场景。
*   **防御动态性**：研究主要针对固定模型，未考虑动态防御（如运行时检测）对缩放曲线的影响。

（完）
