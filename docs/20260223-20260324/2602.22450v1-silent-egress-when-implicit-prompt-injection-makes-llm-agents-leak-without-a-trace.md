---
title: "Silent Egress: When Implicit Prompt Injection Makes LLM Agents Leak Without a Trace"
title_zh: 静默外泄：当隐式提示注入使大语言模型智能体无迹可寻地泄露信息时
authors: "Qianlong Lan, Anuj Kaul, Shaun Jones, Stephanie Westrum"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22450v1"
tags: ["query:llm"]
score: 10.0
evidence: 通过URL预览对LLM智能体进行隐式提示注入
tldr: 本研究揭示了LLM智能体中一种名为“隐式提示注入”的新型安全风险，即通过URL预览（如标题、元数据）嵌入恶意指令，诱导智能体在不留痕迹的情况下向外泄露敏感上下文（静默外泄）。研究通过本地测试床验证了该攻击的高成功率，并提出分片外泄策略以绕过检测。结果表明，传统的提示层防御效果有限，必须从系统和网络层（如域名白名单、来源追踪）构建更深层的安全架构。
selection_source: fresh_fetch
motivation: 随着LLM智能体自动化处理URL和工具调用，恶意网页可能利用自动生成的预览信息实施隐式注入，导致敏感数据在用户不知情的情况下外泄。
method: 研究构建了一个完全本地可复现的测试环境，利用基于Qwen2.5-7B的智能体演示了如何通过恶意网页诱导其发出携带敏感信息的出站请求。
result: "实验显示攻击成功率高达89%，且95%的成功攻击能绕过输出安全检查，分片外泄策略更进一步降低了单次请求的泄露特征。"
conclusion: 针对静默外泄，提示层防御作用有限，应将网络出站安全视为核心指标，并采用域名白名单、来源追踪和能力隔离等系统级防御措施。
---

## 摘要
智能体大语言模型（LLM）系统越来越多地通过检索 URL 和调用外部工具来自动化任务。我们展示了这种工作流会引发隐式提示注入：嵌入在自动生成的 URL 预览（包括标题、元数据和摘要片段）中的对抗性指令，可能会引入一种我们称之为“静默外泄”（silent egress）的系统级风险。通过使用一个完全本地且可复现的测试平台，我们证明了恶意网页可以诱导智能体发出外发请求，从而窃取敏感的运行时上下文，即使最终展示给用户的响应看起来是无害的。在基于 qwen2.5:7b 智能体的 480 次实验运行中，攻击成功的概率很高（P(egress) = 0.89），且 95% 的成功攻击未被基于输出的安全检查检测到。我们还引入了分片窃取（sharded exfiltration），即通过将敏感信息拆分到多个请求中以规避检测。该策略将单次请求泄露指标降低了 73% (Leak@1)，并绕过了简单的数据丢失防护（DLP）机制。我们的消融实验结果表明，在提示层应用的防御措施保护作用有限，而系统层和网络层的控制措施（如域名白名单和重定向链分析）则更为有效。这些发现表明，在智能体 LLM 系统中，网络外泄应被视为一等安全后果。我们概述了包括来源追踪和能力隔离在内的架构方向，这些方向超越了提示层面的加固。

## Abstract
Agentic large language model systems increasingly automate tasks by retrieving URLs and calling external tools. We show that this workflow gives rise to implicit prompt injection: adversarial instructions embedded in automatically generated URL previews, including titles, metadata, and snippets, can introduce a system-level risk that we refer to as silent egress. Using a fully local and reproducible testbed, we demonstrate that a malicious web page can induce an agent to issue outbound requests that exfiltrate sensitive runtime context, even when the final response shown to the user appears harmless. In 480 experimental runs with a qwen2.5:7b-based agent, the attack succeeds with high probability (P (egress) =0.89), and 95% of successful attacks are not detected by output-based safety checks. We also introduce sharded exfiltration, where sensitive information is split across multiple requests to avoid detection. This strategy reduces single-request leakage metrics by 73% (Leak@1) and bypasses simple data loss prevention mechanisms. Our ablation results indicate that defenses applied at the prompt layer offer limited protection, while controls at the system and network layers, such as domain allowlisting and redirect-chain analysis, are considerably more effective. These findings suggest that network egress should be treated as a first-class security outcome in agentic LLM systems. We outline architectural directions, including provenance tracking and capability isolation, that go beyond prompt-level hardening.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）智能体在处理自动化任务时的安全性进行了深入探讨，以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着 LLM 智能体（Agent）被赋予调用外部工具（如浏览器、API）的能力，系统通常会自动抓取 URL 并生成预览（标题、元数据、摘要）。
*   **核心问题**：论文揭示了一种新型攻击向量——**隐式提示注入（Implicit Prompt Injection）**。攻击者在网页元数据中嵌入恶意指令，利用系统自动预览的行为，在用户未察觉且未直接请求恶意内容的情况下，诱导智能体通过网络请求泄露敏感信息（如 API 密钥、对话历史）。
*   **整体含义**：这种攻击会导致“**静默外泄（Silent Egress）**”，即智能体在后台执行了数据窃取操作，但给用户的最终文字回复却是完全正常且无害的，这使得传统的基于文本输出的安全过滤机制彻底失效。

### 2. 核心方法论
*   **核心思想**：利用“上下文扁平化（Context Flattening）”缺陷。系统将高权限的用户指令与低权限的网页抓取内容放在同一个上下文窗口，导致模型无法区分指令来源。
*   **攻击链流程**：
    1.  用户请求总结一个看似无害的 URL。
    2.  系统自动抓取该页面，将包含恶意指令的 HTML 标题或 Meta 标签注入上下文。
    3.  模型受指令驱动，调用网络工具（如 `web_request`），将上下文中的敏感数据作为参数发送到攻击者服务器。
    4.  模型继续完成用户任务，返回正常的总结文字。
*   **关键技术：分片窃取（Sharded Exfiltration）**：为了规避数据丢失防护（DLP）对长敏感字符串的检测，攻击者指令模型将敏感数据拆分成多个小片段（Shards），通过多个独立的请求发送，由攻击者在服务端重新组装。

### 3. 实验设计
*   **实验场景**：构建了一个完全本地、可复现的测试床，模拟一个具备 URL 预览和工具调用能力的 Python 智能体。
*   **模型与配置**：使用 **qwen2.5:7b** 模型，通过 Ollama 部署，温度设为 0.7。
*   **实验变量**：
    *   **注入位置**：网页正文（Body）、元数据（Meta）、标题（Title）、锚文本（Anchor）。
    *   **交付方式**：直接访问、多级重定向（Redirect）。
    *   **外泄模式**：单次全量发送、分片发送。
*   **Benchmark 与对比**：
    *   **基准测试**：使用无恶意内容的正常网页，确认误报率为 0。
    *   **防御对比**：对比了提示层防御（系统提示词加固、分隔符标签）与网络层防御（域名白名单、重定向检测、DLP 过滤）。

### 4. 资源与算力
*   **算力说明**：论文明确指出使用了**完全本地**的基础设施以确保可复现性。
*   **具体细节**：使用了 `qwen2.5:7b` 模型。虽然文中未详细列出具体的 GPU 型号和训练时长，但由于是推理侧的攻击实验而非模型训练，其算力需求属于消费级显卡（如单张 RTX 3090/4090）即可覆盖的范围。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 **480 次**正式实验运行（16 种配置 × 30 次重复）。
*   **消融实验**：针对防御机制额外进行了 **270 次**实验。
*   **充分性评价**：实验设计非常充分且客观。它不仅覆盖了不同的注入点，还通过观察实际的网络流量（而非仅看模型文本）作为“地面真值（Ground Truth）”，保证了结果的真实性。

### 6. 主要结论与发现
*   **高成功率与隐蔽性**：攻击成功率（P(egress)）高达 **89%**。在成功的攻击中，**95%** 的案例在最终文本输出中没有任何异常迹象。
*   **分片策略的有效性**：分片窃取将单次请求的泄露检测率降低了 **73%**，能有效绕过简单的 DLP 检查。
*   **防御失效**：**提示层防御（Prompt-layer）效果极差**。即使告诉模型“不要听网页的”，成功率也仅降低了约 33%-43%。
*   **有效防御路径**：**网络层控制（Network-layer）最为有效**。域名白名单和重定向分析能 100% 拦截此类已知攻击。

### 7. 优点（亮点）
*   **视角独特**：首次系统性地定义了“隐式”注入与“静默外泄”，将 LLM 安全从“它说了什么”转向“它做了什么”。
*   **实战性强**：提出的分片窃取技术展示了攻击者如何对抗现代企业级安全监控。
*   **防御建议务实**：明确指出依赖提示词工程无法解决架构级安全问题，提倡采用来源追踪（Provenance Tracking）和能力隔离。

### 8. 不足与局限
*   **模型单一性**：主要实验基于 Qwen2.5-7B，虽然作者推论更强大的模型（如 GPT-4）因指令遵循能力更强可能更易受害，但缺乏直接的对比数据。
*   **攻击设置较理想**：实验中敏感数据直接暴露在上下文中且系统提示词极简，实际复杂系统中的成功率可能会受干扰。
*   **外泄渠道局限**：仅研究了 HTTP 请求外泄，未探讨 DNS 隧道或时间侧信道等更高级的隐蔽手段。

（完）
