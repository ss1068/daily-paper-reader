---
title: "BarrierSteer: LLM Safety via Learning Barrier Steering"
title_zh: BarrierSteer：通过学习屏障引导实现大语言模型安全
authors: "Thanh Q. Tran, Arun Verma, Kiwan Wong, Bryan Kian Hsiang Low, Daniela Rus, Wei Xiao"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20102v1"
tags: ["query:llm"]
score: 9.0
evidence: 利用安全约束防御对抗性攻击的机制
tldr: 针对大语言模型易受对抗攻击和生成不安全内容的问题，本文提出 BarrierSteer 框架。该方法将非线性安全约束嵌入模型的潜空间，并利用控制屏障函数（CBF）在推理过程中实时检测并纠正不安全轨迹。BarrierSteer 无需修改模型参数，通过高效的约束合并机制，在保持模型原有性能的同时显著提升了安全性，为 LLM 安全提供了理论支撑和实践方案。
selection_source: fresh_fetch
motivation: 大语言模型在部署中面临对抗攻击和不安全内容生成的风险，需要一种兼具理论严谨性和实践有效性的安全机制。
method: 在模型的潜空间中嵌入非线性安全约束，并采用基于控制屏障函数（CBF）的引导机制在推理时实时拦截不安全轨迹。
result: 实验证明该方法在多个模型上显著降低了对抗攻击成功率，且在不修改模型参数的情况下保持了其原始性能。
conclusion: BarrierSteer 提供了一种在潜空间实施安全约束的高效且有理论保障的方法，是提升 LLM 安全性的有力工具。
---

## 摘要
尽管大语言模型（LLMs）在各种任务中表现出最先进的性能，但它们对对抗性攻击和不安全内容生成的敏感性仍然是部署的主要障碍，特别是在高风险场景中。应对这一挑战需要既在实践中有效，又有严谨理论支持的安全机制。我们引入了 BarrierSteer，这是一个新颖的框架，通过将学习到的非线性安全约束直接嵌入到模型的潜表征空间中，使响应安全性形式化。BarrierSteer 采用基于控制屏障函数（CBFs）的引导机制，在推理过程中以高精度高效地检测并防止不安全的响应轨迹。通过高效的约束合并来强制执行多个安全约束，且无需修改底层 LLM 参数，BarrierSteer 保留了模型的原始能力和性能。我们提供的理论结果证明，在潜空间中应用 CBFs 为强制执行安全性提供了一种有原则且计算高效的方法。我们在多个模型和数据集上的实验表明，BarrierSteer 显著降低了对抗攻击成功率，减少了不安全生成，并优于现有方法。

## Abstract
Despite the state-of-the-art performance of large language models (LLMs) across diverse tasks, their susceptibility to adversarial attacks and unsafe content generation remains a major obstacle to deployment, particularly in high-stakes settings. Addressing this challenge requires safety mechanisms that are both practically effective and supported by rigorous theory. We introduce BarrierSteer, a novel framework that formalizes response safety by embedding learned non-linear safety constraints directly into the model's latent representation space. BarrierSteer employs a steering mechanism based on Control Barrier Functions (CBFs) to efficiently detect and prevent unsafe response trajectories during inference with high precision. By enforcing multiple safety constraints through efficient constraint merging, without modifying the underlying LLM parameters, BarrierSteer preserves the model's original capabilities and performance. We provide theoretical results establishing that applying CBFs in latent space offers a principled and computationally efficient approach to enforcing safety. Our experiments across multiple models and datasets show that BarrierSteer substantially reduces adversarial success rates, decreases unsafe generations, and outperforms existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《BarrierSteer: LLM Safety via Learning Barrier Steering》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）性能卓越，但极易受到对抗性攻击（如越狱指令）并生成有害内容。现有的安全对齐方法（如 RLHF、DPO）多基于启发式规则，缺乏严谨的理论保障，且容易被对抗样本绕过。
*   **核心问题**：如何在不修改模型参数、不显著降低模型通用能力的前提下，为 LLM 提供一种具有理论保证、计算高效且能处理复杂非线性安全约束的推理端安全机制。

### 2. 方法论
BarrierSteer 框架将安全问题转化为控制理论中的**状态空间约束问题**，主要包含三个阶段：
*   **学习非线性安全约束**：
    *   从安全和不安全的数据集中提取 LLM 的中间层隐藏状态（Latent Representations）。
    *   使用神经网络训练一组函数 $b_k(h)$，将其作为**控制屏障函数（CBF）**。训练目标是使安全状态的 $b(h) \ge 0$，不安全状态的 $b(h) < 0$。
*   **潜空间动力学建模**：
    *   将 token 生成过程视为受限马尔可夫决策过程（CMDP）。
    *   将隐藏状态的演化近似为线性动力系统 $\dot{h} = u$，其中 $u$ 是可控的引导输入。
*   **推理端引导机制**：
    *   在推理时，实时监测隐藏状态。若预测下一状态将违反安全边界，则通过求解一个**二次规划（QP）问题**来修正隐藏状态。
    *   **关键技术细节**：为了提高效率，提出了**约束合并策略**。利用 Log-Sum-Exp (LSE) 函数将多个安全约束合并为一个平滑的可微约束，从而获得**闭式解（Closed-form solution）**，避免了推理时的迭代优化。

### 3. 实验设计
*   **数据集与基准**：
    *   **HarmBench**：用于测试对抗攻击防御，包含 400 种有害行为和 9 种主流攻击方法（如 GCG, AutoDAN, PAP 等）。
    *   **WildGuardMix**：包含 14 个风险类别的有害提示词，用于测试多维度安全约束的组合。
*   **对比方法**：
    *   原始模型（Original LLM）。
    *   **Activation Addition**：简单的激活值相加引导。
    *   **Directional Ablation**：方向消融法。
    *   **SaP (Safety Polytope)**：最相关的基线，基于线性多面体约束的潜空间投影方法。
*   **评估指标**：攻击成功率（ASR）、通用能力测试（MMLU, GSM8K）。

### 4. 资源与算力
*   **硬件环境**：论文提到在 **NVIDIA H200 GPU** 上进行了推理延迟基准测试。
*   **训练细节**：使用 Adam 优化器，学习率为 $10^{-2}$，Batch Size 为 512。
*   **模型规模**：涵盖了 Gemma-2-9b, Ministral-8B, Llama2-7B, Qwen2-1.5B 等不同规模的模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流模型家族上进行了验证。
    *   针对 9 种不同的对抗攻击手段进行了详尽测试。
    *   进行了**消融实验**，探讨了引导强度参数 $\alpha$ 对安全性和模型效能（Utility）的权衡影响。
    *   针对 14 种安全类别进行了**模块化组合实验**。
*   **充分性评价**：实验设计较为充分且客观。通过跨模型、跨攻击方式的对比，证明了该方法在降低 ASR 方面的稳健性。同时，通过 MMLU 等指标评估了对模型原生能力的影响，体现了公平性。

### 6. 主要结论与发现
*   **极高的防御成功率**：在 Gemma-2-9b 上，BarrierSteer 将平均攻击成功率（ASR）降至 **0.00%**，显著优于 SaP 和其他基线。
*   **高效性**：得益于闭式解设计，BarrierSteer (LSE 版本) 的推理延迟仅为 6.08ms/token，比需要迭代优化的 SaP 快了约 **31 倍**。
*   **良好的效能保留**：在大幅提升安全性的同时，MMLU 和 GSM8K 的得分下降极小（通常在 1.5% 以内），解决了传统安全过滤方法易导致模型“变笨”的问题。
*   **模块化优势**：可以独立训练针对不同风险（如暴力、隐私、歧视）的屏障函数，并在推理时灵活组合。

### 7. 优点
*   **理论严谨**：引入控制理论中的 CBF，为 LLM 安全提供了数学上的稳定性与安全性证明。
*   **非线性表达力**：相比于 SaP 的线性约束，BarrierSteer 能捕捉潜空间中更复杂的非线性安全边界。
*   **无需重训**：作为一种推理端引导技术，它不需要微调 LLM 参数，部署成本低。
*   **计算友好**：通过 LSE 合并约束获得闭式解，极大地降低了推理开销。

### 8. 不足与局限
*   **标签依赖**：高度依赖高质量的“安全/不安全”标注数据来学习屏障函数，数据的偏差会直接影响防御效果。
*   **潜空间与语义的鸿沟**：虽然在潜空间保证了“安全”，但潜空间状态与最终生成的文本语义之间并非完全等价，可能存在极少数潜空间安全但语义仍有风险的情况。
*   **动力学近似**：将 Transformer 的离散 token 生成过程近似为连续动力系统，虽然实验有效，但在理论严密性上仍存在一定假设。
*   **分布外（OOD）风险**：对于训练数据中未涵盖的全新攻击模式，学习到的屏障函数可能失效。

（完）
