---
title: SecureBreak -- A dataset towards safe and secure models
title_zh: SecureBreak —— 一个面向安全可靠模型的数据集
authors: "Marco Arazzi, Vignesh Kumar Kembu, Antonino Nocera"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21975v1"
tags: ["query:llm"]
score: 10.0
evidence: 针对越狱和提示注入鲁棒性的数据集
tldr: 随着大语言模型广泛应用，现有的安全对齐机制仍易受越狱和提示注入攻击。本文提出了 SecureBreak 数据集，旨在通过检测模型生成的有害输出来增强安全性。该数据集经过严格的人工标注，涵盖多种风险类别，可用于训练防御层以拦截不安全内容。实验表明，在 SecureBreak 上微调后的模型能显著提升对有害信息的识别能力，为模型对齐和后期过滤提供了有力支持。
selection_source: fresh_fetch
motivation: 现有的安全对齐机制无法完全消除有害生成，且易被越狱等攻击手段绕过，亟需额外的安全防御层。
method: 引入了经过精细人工标注的安全数据集 SecureBreak，采用保守标注策略以确保安全性，并涵盖了多种风险类别。
result: 实验证明，预训练模型在 SecureBreak 数据集上进行微调后，检测不安全内容的能力得到了显著提升。
conclusion: SecureBreak 数据集不仅能作为生成后的安全过滤器，还能为进一步的模型对齐和安全性改进提供指导。
---

## 摘要
大语言模型正成为许多现实应用中无处不在的核心组件。因此，安全对齐是其安全部署的关键要求。尽管之前的相关工作主要集中在模型架构和对齐方法上，但仅靠这些方法无法确保完全消除有害生成内容。越来越多的科学文献表明，诸如越狱（jailbreaking）和提示词注入（prompt injection）等攻击可以绕过现有的安全对齐机制，这进一步加剧了上述担忧。因此，需要额外的安全策略，既要在训练阶段对所获得的安全对齐鲁棒性提供定性反馈，又要创建一个“终极”防御层，以拦截已部署模型可能产生的不安全输出。为了在这一场景中做出贡献，本文介绍了 SecureBreak，这是一个面向安全的数据集，旨在支持开发 AI 驱动的解决方案，以检测由安全对齐中的残留弱点导致的有害 LLM 输出。该数据集由于经过仔细的人工标注而具有高度可靠性，其中标签的分配较为保守以确保安全性。它在检测多个风险类别的不安全内容方面表现良好。对预训练 LLM 的测试表明，在 SecureBreak 上进行微调后，结果有所改善。总的来说，该数据集对于生成后的安全过滤以及指导进一步的模型对齐和安全性改进都非常有用。

## Abstract
Large language models are becoming pervasive core components in many real-world applications. As a consequence, security alignment represents a critical requirement for their safe deployment. Although previous related works focused primarily on model architectures and alignment methodologies, these approaches alone cannot ensure the complete elimination of harmful generations. This concern is reinforced by the growing body of scientific literature showing that attacks, such as jailbreaking and prompt injection, can bypass existing security alignment mechanisms. As a consequence, additional security strategies are needed both to provide qualitative feedback on the robustness of the obtained security alignment at the training stage, and to create an ``ultimate'' defense layer to block unsafe outputs possibly produced by deployed models. To provide a contribution in this scenario, this paper introduces SecureBreak, a safety-oriented dataset designed to support the development of AI-driven solutions for detecting harmful LLM outputs caused by residual weaknesses in security alignment. The dataset is highly reliable due to careful manual annotation, where labels are assigned conservatively to ensure safety. It performs well in detecting unsafe content across multiple risk categories. Tests with pre-trained LLMs show improved results after fine-tuning on SecureBreak. Overall, the dataset is useful both for post-generation safety filtering and for guiding further model alignment and security improvements.

---

## 论文详细总结（自动生成）

以下是对论文《SecureBreak -- A dataset towards safe and secure models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管大语言模型（LLM）在训练阶段进行了安全对齐，但现有的防御机制仍容易被“越狱攻击”（Jailbreaking）和“提示词注入”绕过。
*   **研究动机**：
    *   现有的安全数据集多侧重于**输入端（Prompt）**的有害性分类，而忽视了对**输出端（Response）**的精细化评估。
    *   需要一个“最后一道防线”（终极防御层），在模型生成内容后进行实时拦截。
    *   需要高质量的监督信号来评估对齐质量，并指导后续的安全性微调。

### 2. 方法论
*   **核心思想**：构建一个专门针对“越狱”语境下的**响应级（Response-level）**分类数据集 SecureBreak，通过高质量的人工标注，训练“裁判模型”（Judge LLMs）来识别有害输出。
*   **关键技术细节**：
    *   **数据来源**：基于 JailbreakBench 的 100 种有害行为（涵盖 10 大风险类别，如虚假信息、非法建议、隐私侵犯等）。
    *   **响应生成**：使用不同家族和规模的模型（Llama 3.2-1B/3.1-8B, Qwen 2.5-0.5B/3B, Gemma 2B/7B, Mistral-7B）生成针对有害问题的回答。
    *   **人工标注**：由两名专家进行手动标注。采用**保守标注策略**：即在存在轻微分歧时优先标注为“不安全”，以最大程度确保安全性。
    *   **一致性校验**：使用 Cohen’s Kappa 系数衡量标注一致性，平均得分达 0.85，确保了数据的高可靠性。
    *   **数据集规模**：最终包含 3,059 个样本，其中安全样本约占 60.5%，不安全样本约占 39.5%。

### 3. 实验设计
*   **实验场景**：二分类任务（安全 vs 不安全）。
*   **对比基准（Benchmark）**：
    *   **Base 模式**：直接使用预训练模型进行零样本（Zero-shot）分类。
    *   **Fine-tuned 模式**：在 SecureBreak 数据集上进行微调后的表现。
*   **测试模型**：Llama-3.1-8B、Mistral-7B-v0.3、Selene-1-Mini-Llama-3.1-8B。
*   **特殊对比**：引入了极小规模模型 Qwen 2.5-0.5B，采用 Seq2Seq（序列到序列）监督分类模式进行测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量。
*   **训练配置**：
    *   使用 **4-bit 量化**技术。
    *   采用 **QLoRA** 微调（Rank=8, Alpha=16, Dropout=0.05）。
    *   训练 3 个 Epoch，Batch Size 为 4，梯度累积步数为 4。
    *   学习率设为 0.0005，使用 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 种不同架构的模型生成的数据，并在 3 种主流 7B-8B 级模型上进行了微调验证。
*   **分析维度**：除了总体准确率，还进行了**分类别（Category-wise）**的详细对比（10 个风险类别）。
*   **充分性评价**：实验设计较为客观，通过跨模型家族的验证展示了数据集的泛化能力。虽然样本总量（3k+）在深度学习领域不算巨大，但对于专门的“安全裁判”任务而言，其高质量的人工标注弥补了规模上的不足。

### 6. 主要结论与发现
*   **微调效果显著**：所有模型在微调后准确率均大幅提升（从 60% 左右提升至 80% 以上）。
*   **“助人陷阱”（Helpfulness Trap）**：中等规模模型（如 Llama-8B）往往表现出极强的指令遵循能力，导致其更容易产生有害输出；而极小模型（如 Qwen-0.5B）则因理解力不足而无法识别复杂的毒性。
*   **小模型也能当好裁判**：经过微调的 Qwen-0.5B 在 Seq2Seq 模式下达到了 **90.14%** 的最高准确率，甚至优于参数量大得多的模型，证明了专用小模型作为安全过滤器的可行性。
*   **风险类别差异**：模型在“物理伤害”类别上安全性较高，但在“专家建议”（医疗、法律、金融）类别上极易失守。

### 7. 优点
*   **标注质量高**：采用专家人工标注而非 LLM 自动标注，捕捉到了自动化工具容易忽略的细微安全违规。
*   **实用性强**：数据集直接支持构建“后置过滤器”，为 LLM 部署提供了切实可行的安全增强方案。
*   **保守策略**：在安全领域采用保守标注逻辑，符合工业界对风险零容忍的需求。

### 8. 不足与局限
*   **数据集规模限制**：3,059 个样本对于覆盖极其广泛的互联网有害信息可能仍显不足。
*   **语言单一性**：论文未提及多语言支持，可能主要集中在英文语境。
*   **静态性**：越狱攻击手段演进极快，数据集需要持续更新以应对新型攻击模式。
*   **硬件细节缺失**：未提供具体的算力消耗数据，不利于其他研究者评估复现成本。

（完）
